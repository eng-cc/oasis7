# Runtime 安全与数值语义回归模板

审计轮次: 4

## 目的
- 为 `TASK-WORLD_RUNTIME-003` 提供统一的安全 / 数值语义回归记录模板。
- 将安全边界、数值边界、失败签名、证据路径与结论统一归档，供 `TASK-WORLD_RUNTIME-004` 发布门禁直接引用。

## 使用说明
- 每次 runtime 高风险改动后至少填写一份记录。
- 若存在未解释的数值漂移、权限绕过或 receipt / replay 异常，结论不得为 `pass`。
- 可同时记录 `test_tier_required` 与 `test_tier_full` 结果，但必须分开列出证据。

## 模板
### Meta
- 回归 ID:
- 日期:
- 负责人:
- 关联 PRD-ID:
- 关联任务:
- 测试层级: `test_tier_required` / `test_tier_full`
- 总结论: `pass` / `fail` / `blocked`

### 安全回归
| 检查项 | 目标 | 验证命令 / 方法 | 证据路径 | 结果 | 失败签名 |
| --- | --- | --- | --- | --- | --- |
| 权限 / 白名单 | 未授权主体不能触发关键动作 |  |  |  |  |
| 审计 / receipt | 关键动作可回溯且拒绝路径可见 |  |  |  |  |
| WASM 沙箱 / host capability | 模块不能越权访问宿主能力 |  |  |  |  |
| replay / checkpoint 恢复 | 恢复路径无审计断裂 |  |  |  |  |

### 数值语义回归
| 检查项 | 目标 | 验证命令 / 方法 | 证据路径 | 结果 | 失败签名 |
| --- | --- | --- | --- | --- | --- |
| tick / 时序 | 时序推进与暂停语义稳定 |  |  |  |  |
| 资源 / 经济 / 结算 | 数值源汇、扣减、结算语义稳定 |  |  |  |  |
| 边界值 / 极端参数 | 极端输入下无溢出、漂移或 silent clamp |  |  |  |  |
| 恢复后一致性 | 回滚 / 恢复后数值状态一致 |  |  |  |  |

### 问题与处置
| 问题 ID | 类别 (`security` / `numeric`) | 描述 | 严重级 | 负责人 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

### 结论摘要
- 通过项：
- 失败项：
- 阻断项：
- 下一步建议：

## 最小审查清单
- 是否同时覆盖安全与数值两大维度。
- 是否为每一项填写命令 / 方法、证据路径、结果与失败签名。
- `fail/blocked` 是否已有问题 ID 与负责人。
- 是否已将结果回写 `doc/world-runtime/project.md` 与对应 `.pm/tasks/task_<32hex>.execution.md`。
