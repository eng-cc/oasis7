# 2026-03-27

## 09:26:45 CST / viewer_engineer
- 完成内容:
  - 把 `crates/oasis7/src/viewer/web_bridge.rs` 的 `ViewerWebBridge::run()` 从串行 `accept -> serve_stream` 改成每个 websocket 一条独立桥接线程，补掉 browser/web-bridge 层最后一个单连接瓶颈。
  - 新增定向回归 `bridge_run_accepts_second_websocket_while_first_stays_open`，要求首个 websocket 保持打开时第二个 websocket 仍能透过 bridge 连到 upstream。
  - 用真实本地 hosted `oasis7_game_launcher --deployment-mode hosted_public_join --chain-disable` 栈复验双浏览器接入：两个独立 `agent-browser` session 最终都能收敛到 `debug_viewer:subscribed` 并看到同一份 live snapshot，不再永久停在第二页 detached。
- 验证:
  - `cargo fmt --all`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib bridge_run_accepts_second_websocket_while_first_stays_open -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_live_run_accepts_probe_while_viewer_session_is_open -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
  - `agent-browser --session hosted-concurrent-a open 'http://127.0.0.1:6101/?ws=ws://127.0.0.1:6102'`
  - `agent-browser --session hosted-concurrent-b open 'http://127.0.0.1:6101/?ws=ws://127.0.0.1:6102'`
  - `agent-browser --session hosted-concurrent-a get text body`
  - `agent-browser --session hosted-concurrent-b get text body`
- 遗留事项:
  - 第二个页面首轮 attach 仍可能短暂经历一次自动重连窗口后才进入 `subscribed`；当前已确认不是永久卡死，也不再阻断双页并发观察。
  - hosted player lane 的下一真实 blocker 仍是带 backend grant 的 `prompt_control_*` success evidence，而不是 browser/web-bridge 并发接入。

## 09:46:07 CST / producer_system_designer
- 完成内容:
  - 对齐 `doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md`、`doc/p2p/project.md` 与当前真实 hosted 联调状态，明确 `TASK-P2P-041-D` 的 transport / probe / 多页并发问题已不再是主 blocker。
  - 把真实联调分层结论正式落文：伪造 env signer 会被拒绝为 keypair mismatch；换成真实匹配 signer 后，不带 `--with-llm` 会停在 `llm_mode_required`；带 `--with-llm` 的 attached `prompt_control` 链路当前真实 blocker 为 `release_token does not map to an active player slot`。
  - 冻结当前判断：浏览器/runtime 仍显示 session 已注册且 agent 已绑定，但 admission 漂移到 `active_player_sessions=0` 且 `runtime_only_player_sessions=1`，说明 hosted session issuer 在 runtime 仍持有绑定时提前释放了 active slot；后续工程 owner 应优先修复该生命周期竞争，再补真实 success evidence。
- 验证:
  - `rg -n "release_token does not map to an active player slot|llm_mode_required|keypair mismatch|生命周期竞争|真实 signer" doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md doc/p2p/project.md doc/devlog/2026-03-27.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 本次仅完成状态对齐，尚未修复 `release_token` 生命周期竞争本体。
  - `TASK-P2P-041-D` 仍未取得“真实 signer + --with-llm + attached player session + backend grant + prompt_control success”的最终通过证据。

## 10:00:12 CST / runtime_engineer
- 完成内容:
  - 修复 hosted session issuer 的 `release_token` 生命周期竞争：`HostedPlayerSessionIssuer::observe_runtime_active_players()` 现改为先使用当前 runtime probe snapshot 给仍在 runtime 中绑定的 active slot 续租，再执行过期清理，避免本轮 probe 明明已看到该 player 仍活跃时，还先按旧 `last_seen` 把 release token 清掉。
  - 新增定向回归 `hosted_player_session_runtime_probe_refreshes_runtime_bound_slot_before_expiry_prune`，冻结“runtime 仍绑定时，不得因 probe 前的 lease 过线而把 active slot 误剪成 runtime-only occupancy”这一修复语义。
  - 执行 `oasis7_game_launcher` bin 全量单测，确认 hosted player-session / runtime presence / strong-auth 现有分支未被这次修复打坏。
- 验证:
  - `cargo fmt --all`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_runtime_probe_refreshes_runtime_bound_slot_before_expiry_prune -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_runtime_seen_slots_keep_full_lease_ttl -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_runtime_reconcile_releases_seen_players_missing_from_runtime -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
- 遗留事项:
  - 尚未重跑真实 hosted `prompt_control` 强鉴权成功链路，`release_token does not map to an active player slot` 是否已在真实栈消失还需实链验证。
  - 若真实链路通过，下一层 blocker 需要重新盘点；若仍失败，应继续检查 viewer heartbeat 与 issuer/reconnect 之间的时序竞争。

## 10:18:43 CST / runtime_engineer
- 完成内容:
  - 定位出 `release_token` 的第二层竞争不在 120s lease，而在 `pending_registration_ttl_ms`：当 player session issue 已超过 30s、但刚完成 `register_session` 且后台 runtime presence monitor 还没来得及看到新绑定时，首个 strong-auth grant 会把 slot 仍当作 pending 直接剪掉。
  - 在 public player plane 路由前补了同步 runtime reconcile：`admission/issue/refresh/release/strong-auth grant` 处理前会短连 `live_bind` 拉最新 snapshot，把刚完成 register 的 runtime binding 先回写到 issuer，再继续 hosted session / grant 逻辑。
  - 用真实 signer + `--with-llm` + browser-local ephemeral key + approval code 重新跑 hosted 栈，最终在未绑定的 `agent-1` 上确认 `prompt_control preview` 与 `prompt_control apply` 都已通过，`strongAuthLastGrantError = null`，且 runtime snapshot 显示 `agent-1` 已绑定当前 player。
- 验证:
  - `cargo fmt --all`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `agent-browser --session hosted-prompt-rerun2 open 'http://127.0.0.1:6101/?ws=...&hosted_access=...'`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-prompt-rerun2 'window.__AW_TEST__.setStrongAuthApprovalCode("preview-code")'`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-prompt-rerun2 'window.__AW_TEST__.sendPromptControl("preview", { agentId: "agent-1", ... })'`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-prompt-rerun2 'window.__AW_TEST__.sendPromptControl("apply", { agentId: "agent-1", ... })'`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-prompt-rerun2 'window.__AW_TEST__.getState()'`
  - `python3 - <<'PY' ... request_snapshot ... print(snapshot["model"]["agent_player_bindings"]) ... PY`
- 遗留事项:
  - 当前通过仍只覆盖 `prompt_control_*` 的 preview-grade backend reauth；`main_token_transfer` 继续保持 `blocked_until_strong_auth`。
  - 默认 `llm_bootstrap` 场景里 `agent-0` 已被历史 player 占用，真实 hosted 验证时需要选用未绑定 agent，或后续补更明确的占用提示 / handoff 流程。

## 10:47:11 CST / runtime_engineer
- 完成内容:
  - 给 runtime-live 的 `authoritative_recovery_ack/error` 补了结构化 `revoke_reason/revoked_by` contract，并在 runtime 侧按 `player_id + session_pubkey` 记住真实 revoke 元数据，让后续 `reconnect_sync/register_session` 再命中 `session_revoked` 时不再只回裸错误码。
  - `software_safe.js` 现会把上述 revoke 元数据保留到 guest fallback，并把 `Hosted Recovery` 面板文案、`__AW_TEST__.getState().authRevokeReason/authRevokedBy` 与 `hostedRecoveryHint` 一起更新；remote revoke / operator kick 后页面不会再静默自动补发新会话掩盖撤销原因。
  - 扩展 `runtime_authoritative_recovery_rotate_and_revoke_session_enforced_for_agent_chat`：新增断言覆盖 revoke ack、`ReconnectSync` revoked error 与 `RegisterSession` revoked error 的结构化 `revoke_reason/revoked_by`。
- 验证:
  - `cargo fmt --all`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_authoritative_recovery_rotate_and_revoke_session_enforced_for_agent_chat -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 当前只补了 recovery / public player 面的 revoke 结构化回流；更完整的 hosted handoff UI、operator 批量 kick 流程和对外更正模板仍待后续专题收口。
  - 若后续要让 runbook 直接引用浏览器证据，还需要补一份新的 agent-browser revoke drill 证据文档。

## 11:36:58 CST / qa_engineer
- 完成内容:
  - 新增 `doc/testing/evidence/hosted-world-browser-revoke-recovery-2026-03-27.md`，用真实 hosted 页面确认 external `revoke-session` 之后，下一轮 heartbeat 会把浏览器从 `player_session` 收敛到 `guest_session`。
  - 实测页面不仅返回 `authError=session_revoked...`，还会同步暴露 `authRevokeReason=operator_kick_for_drill`、`authRevokedBy=hosted-revoke-operator` 与带操作者/原因的 `Hosted Recovery` 文案，证明这轮结构化 revoke metadata 已真正到达玩家面。
  - 取证过程中确认一条执行前提：要让本地 hosted 栈使用最新 recovery contract，需先显式重编 `oasis7_viewer_live` sibling bin，再拉起 `oasis7_game_launcher`。
- 验证:
  - `env -u RUSTC_WRAPPER cargo build -q -p oasis7 --bin oasis7_viewer_live`
  - `env OASIS7_HOSTED_STRONG_AUTH_PUBLIC_KEY=... OASIS7_HOSTED_STRONG_AUTH_PRIVATE_KEY=... OASIS7_HOSTED_STRONG_AUTH_APPROVAL_CODE=preview-code env -u RUSTC_WRAPPER cargo run -q -p oasis7 --bin oasis7_game_launcher -- --deployment-mode hosted_public_join --viewer-static-dir crates/oasis7_viewer --viewer-host 127.0.0.1 --viewer-port 6201 --web-bind 127.0.0.1:6202 --live-bind 127.0.0.1:6203 --chain-disable --no-open-browser`
  - `source scripts/agent-browser-lib.sh && ab_open hosted-revoke-evidence 0 'http://127.0.0.1:6201/software_safe.html?ws=ws%3A%2F%2F127.0.0.1%3A6202&hosted_access=%7B%22deployment_mode%22%3A%22hosted_public_join%22%7D&test_api=1'`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-revoke-evidence 'window.__AW_TEST__.getState()'`
  - `env -u RUSTC_WRAPPER cargo run -q -p oasis7 --bin oasis7_pure_api_client -- --addr 127.0.0.1:6203 --client hosted-revoke-operator revoke-session --player-id hosted-player-0000019d2d3c4a63-00000003 --session-pubkey 5c9228b48496775b2a3410f742e4b20ff31ef9e6e9307bbdf5ab756d77cba4da --revoke-reason operator_kick_for_drill`
  - `sleep 35`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-revoke-evidence 'window.__AW_TEST__.getState()'`
  - `source scripts/agent-browser-lib.sh && ab_run hosted-revoke-evidence get text body`
  - `source scripts/agent-browser-lib.sh && ab_run hosted-revoke-evidence screenshot output/playwright/hosted-p2p-041/hosted-revoke-recovery.png`
- 遗留事项:
  - 当前 revoke 感知仍依赖固定 heartbeat 周期；若要进一步缩短玩家体感延迟，仍需补更主动的 push/presence 方案。
  - 本次证据只覆盖单玩家 remote revoke drill，尚未覆盖 operator 批量 kick / hosted handoff / incident correction 模板。

## 11:58:22 CST / liveops_community
- 完成内容:
  - 扩写 `doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.runbook.md`，正式补上 hosted operator 的 `session revoke` 实操步骤，明确何时需要 revoke、应收集哪些字段、推荐命令、预期回执，以及执行后如何在浏览器侧复核 `Hosted Recovery` / `authRevokeReason` / `authRevokedBy`。
  - 新增 `doc/testing/templates/hosted-world-share-correction-template.md`，统一错误 URL 更正、revoke 后重新获取 hosted player session 和 preview claims boundary 三类对外文案。
  - 回写专题 project 状态，收口 `TASK-P2P-041-F` 中“session revoke 实操步骤 / 对外更正模板仍待补齐”的过时表述。
- 验证:
  - `rg -n "revoke-session|Hosted Recovery|hosted-world-share-correction-template|preview" doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.runbook.md doc/testing/templates/hosted-world-share-correction-template.md doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md doc/p2p/project.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 远程 operator tunnel / reverse proxy 配置示例仍未落文。
  - hosted share announcement 模板和更完整的 hosted handoff / batch kick 沟通脚本仍待后续补齐。

## 12:03:37 CST / liveops_community
- 完成内容:
  - 继续扩写 `doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.runbook.md`，补齐远程 operator tunnel / reverse proxy 的最低暴露策略，明确公网只能暴露 `public join URL`、player WebSocket 与 `public player plane`，operator/control 面必须继续停留在 loopback、VPN 或人工 tunnel 内。
  - 新增 `doc/testing/templates/hosted-world-share-announcement-template.md`，统一 hosted world 首轮分享、重复提醒与 preview claims boundary 文案，并把“错误 URL 更正”继续留给现有 correction 模板处理。
  - 回写 `doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md` 与 `doc/p2p/project.md`，将 `TASK-P2P-041-F` 正式标记完成，清除“announcement/tunnel policy 仍待补齐”的过时 blocker。
- 验证:
  - `rg -n "hosted-world-share-announcement-template|reverse proxy|Tunnel / Reverse Proxy|TASK-P2P-041-F|分享 / 误分享 / 撤销 / 事故通报" doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.runbook.md doc/testing/templates/hosted-world-share-announcement-template.md doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md doc/p2p/project.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - `TASK-P2P-041-F` 已收口，但 `TASK-P2P-041-C/D/E` 仍需继续核对并补齐 product flow、strong-auth matrix 与 abuse evidence matrix。

## 12:18:52 CST / qa_engineer
- 完成内容:
  - 新增 `doc/testing/evidence/hosted-world-abuse-suite-matrix-2026-03-27.md`，把 replay、expiry、revocation、operator/public URL confusion、admission limit、capability bypass 六类 abuse 覆盖统一收敛成一份 topic 级证据矩阵。
  - 重跑本专题关键定向测试：`runtime_prompt_control_hosted_public_join*`、`runtime_agent_chat_requires_explicit_session_registration`、`runtime_session_register_*`、`runtime_authoritative_recovery_rotate_and_revoke_session_enforced_for_agent_chat`、`hosted_player_session_*`、`hosted_prompt_control_strong_auth_grant_*`、`hosted_strong_auth_*`、`hosted_mode_rejects_remote_private_control_plane_matrix`，确认当前 failure signature 与 browser/live evidence 一致。
  - 回写 `doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md` 与 `doc/p2p/project.md`，将 `TASK-P2P-041-B/C/D/E` 一并按当前 PRD 完成定义标记为完成，并把剩余内容降级为后续增强议题而非 blocker。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_prompt_control_hosted_public_join -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_agent_chat_requires_explicit_session_registration -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_session_register_ -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_authoritative_recovery_rotate_and_revoke_session_enforced_for_agent_chat -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_ -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_prompt_control_strong_auth_grant_ -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_strong_auth_ -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher hosted_mode_rejects_remote_private_control_plane_matrix -- --nocapture`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 本专题已完成；后续若要继续推进 production custody / wallet plugin / hosted handoff，应新开子专题，不再复用 `TASK-P2P-041`。

## 12:06:23 CST / producer_system_designer
- 完成内容:
  - 对 `doc/p2p/project.md` 做最后一轮 hosted-world 专题收口审计，删除 `TASK-P2P-041` 在模块总表中的过期 `first slice / 最新进展` 长列表，避免同一页同时保留阶段性结论和最终完成结论。
  - 将模块总表中的 hosted-world 摘要压缩为单一真值，只保留 `TASK-P2P-041-F`、`TASK-P2P-041-B/C/D/E` 与 `TASK-P2P-041` 三条最终完成记录，明确本专题已经闭环，剩余项全部转入新专题。
- 验证:
  - `rg -n "TASK-P2P-041-B|TASK-P2P-041-C|TASK-P2P-041-D|TASK-P2P-041-E|TASK-P2P-041-F|first slice|最新进展" doc/p2p/project.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - `TASK-P2P-041` 相关文档链路现已一致；后续若继续做 hosted-world 强化，需新开子专题。
## 10:23:49 CST / liveops_community
- 完成内容:
  - 按用户要求继续重试昨天未完成的 Moltbook 外部游戏讨论评论，优先回查 `AI Council - Diplomacy Game for AI Agents`、`Why Games Are Better Testbeds Than Production` 与昨天已成功评论过的 `CrustLand: why recovery arcs matter`。
  - 先走本地 `7897` proxy 回查帖子与评论状态，出现 `curl: (35) OpenSSL SSL_connect: SSL_ERROR_SYSCALL in connection to www.moltbook.com:443`；改用直连 API 再探测，同样在 `15s` 窗口内返回 `curl: (28) Connection timed out after 15001 milliseconds`。
  - 结合昨天已记录的 `AI Council` 连续两次 `500 Internal server error` 和 `MoltMob` 评论超时，这次确认当前阻塞点是 Moltbook 连接面/平台稳定性，而不是选帖或文案问题，因此本轮停止继续补发，避免重复评论和无意义重试。
- 验证:
  - `GET /api/v1/posts/d566995d-78cf-4af3-9ef4-21ddc5c9a366` via proxy
  - `GET /api/v1/posts/d566995d-78cf-4af3-9ef4-21ddc5c9a366/comments?sort=new&limit=10` via proxy
  - `GET /api/v1/posts/1c0c9a7a-de63-4870-a3be-596e17170dd8` via proxy
  - `GET /api/v1/posts/1c0c9a7a-de63-4870-a3be-596e17170dd8/comments?sort=new&limit=10` via proxy
  - `GET /api/v1/posts/d79f7706-1482-4649-a67f-e91c50b78897` via proxy
  - `GET /api/v1/posts/d79f7706-1482-4649-a67f-e91c50b78897/comments?sort=new&limit=10` via proxy
  - direct `GET /api/v1/posts/1c0c9a7a-de63-4870-a3be-596e17170dd8`
  - direct `GET /api/v1/posts/d566995d-78cf-4af3-9ef4-21ddc5c9a366`
- 遗留事项:
  - 等 Moltbook 连接恢复后，优先重试 `AI Council` 那条关于 `permanent reputation / asymmetric victory conditions / costly trust` 的纯玩法评论。
  - `MoltMob` 那条后续重试前先回查楼层，确认昨天那次 timeout 没有慢写入成幽灵评论，再决定是否补发。

## 13:11:35 CST / qa_engineer
- 完成内容:
  - 落地 `TASK-SCRIPTS-014` 的 worktree-isolated harness 实装闭环：新增 `scripts/worktree-harness.sh` / `scripts/worktree-harness-lib.sh`，并把 `run-game-test.sh`、`run-producer-playtest.sh` 接到 worktree-local `output/harness/<worktree-id>/` 目录、端口与元数据契约上。
  - 修复 source-mode 真正阻断项：`run-game-test.sh` 现会先显式构建 `oasis7_game_launcher`、`oasis7_viewer_live`、`oasis7_chain_runtime`，再以绝对路径启动 launcher，并注入 `OASIS7_VIEWER_LIVE_BIN` / `OASIS7_CHAIN_RUNTIME_BIN`，不再依赖 PATH 或偶然已有的 sibling binary。
  - 补强 harness 失败语义：`run-game-test.sh` 启动阶段若 launcher 提前退出会立即失败，不再空等 HTTP/web-bridge timeout；`worktree-harness.sh` 则增加真实存活刷新，避免陈旧 `state.json` 继续冒充 `ready`。
  - 实测 `./scripts/worktree-harness.sh up --no-llm --smoke-timeout 30` 在单次闭环内通过，`agent-browser` 产物落在 `output/harness/wt-781b155c/artifacts/smoke-20260327-131008/`，其中 `state.json` 显示 `connectionStatus=connected`、`controlProfile=live`、`rendererClass=hardware`、`renderer=ANGLE (Intel, Mesa Intel(R) Iris(R) Xe Graphics (RPL-P), OpenGL 4.6)`。
  - 同步修掉本轮验证前暴露出的 viewer wasm 编译阻塞，补齐 `crates/oasis7_viewer/src/egui_right_panel_chat.rs`、`crates/oasis7_viewer/src/egui_right_panel_chat_auth.rs`、`crates/oasis7_viewer/src/viewer_automation.rs`、`crates/oasis7_viewer/src/selection_linking.rs` 与当前协议字段/事件枚举的缺项，使 `env -u RUSTC_WRAPPER cargo check -p oasis7_viewer --target wasm32-unknown-unknown` 重新通过。
- 验证:
  - `bash -n scripts/worktree-harness.sh scripts/worktree-harness-lib.sh scripts/run-game-test.sh scripts/run-producer-playtest.sh`
  - `./scripts/worktree-harness.sh --help`
  - `./scripts/run-game-test.sh --help`
  - `./scripts/run-producer-playtest.sh --help`
  - `./scripts/worktree-harness.sh up --no-llm`
  - `./scripts/worktree-harness.sh url`
  - `./scripts/worktree-harness.sh logs`
  - `./scripts/worktree-harness.sh up --no-llm --smoke-timeout 30`
  - `./scripts/worktree-harness.sh status --json`
  - `./scripts/worktree-harness.sh down`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
  - `env -u RUSTC_WRAPPER cargo check -p oasis7_viewer --target wasm32-unknown-unknown`
- 遗留事项:
  - 当前 Codex 执行环境会在命令返回后回收后台子进程，因此跨命令 `up -> smoke/status` 会被工具环境打断；仓库脚本已补 `nohup` 与状态刷新，但“命令返回后后台栈继续存活”的最终行为仍应在真实开发者 shell 中再做一次人工确认。
  - `status --json` 在本环境里会如实把被回收后的栈刷新成 `stopped`；这不是 harness 协议回归，而是执行器生命周期限制。

## 13:23:34 CST / producer_system_designer
- 完成内容:
  - 按当前团队协作策略更新根 `AGENTS.md`，把“每个新需求默认新开独立 `git worktree`”正式写成开发工作流规则。
  - 明确一需求一 `worktree`、代码/文档/测试/`devlog`/验证产物在对应 `worktree` 内闭环、当前 `worktree` 已绑定未完成任务时必须新开，以及只有用户明确要求时才允许复用现有 `worktree`。
  - 补充本地 Web/launcher/`agent-browser`/smoke 闭环默认绑定需求专属 `worktree` 与隔离 harness，避免后续多任务并行时再次退回共享工作目录。
- 验证:
  - `sed -n '1,120p' AGENTS.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 当前只是把规则写进 `AGENTS.md`；若后续要进一步降低执行成本，可再补一个标准化的新建 `worktree` 脚本或命名模板。

## 13:38:40 CST / qa_engineer
- 完成内容:
  - 新增 `scripts/new-task-worktree.sh`，把“每个新需求默认新开独立 `git worktree`”从口头规范落成标准入口，默认派生 `task/<module>-<task>` 分支名与 `<repo-parent>/worktrees/<repo>-<module>-<task>` 路径。
  - 入口现已补齐三类关键围栏：源 worktree 脏时默认阻断、目标路径已存在时阻断、目标 branch 已被其他 worktree 检出时阻断；恢复已有任务 branch 时则允许 `mode=attach_existing_branch`。
  - `--json` 契约已收口为纯机器可读单对象，并显式输出 `module`、`task`、`branch`、`worktree_path`、`base_ref`、`mode`；同时修正 branch 占用检查改读 `git-common-dir`，避免在非主 worktree 下漏看其他已挂载 worktree。
  - 同步回写 `doc/scripts/{prd,project,prd.index,README}.md`、新增 `doc/scripts/governance/task-worktree-bootstrap-2026-03-27.{prd,design,project}.md`，并把根 `AGENTS.md` 的推荐入口改写为 `./scripts/new-task-worktree.sh <module> <task>`。
- 验证:
  - `bash -n scripts/new-task-worktree.sh`
  - `./scripts/new-task-worktree.sh --help`

## 21:53:32 CST / runtime_engineer
- 完成内容:
  - 在 `crates/oasis7/src/runtime/` 落地 `ClaimAgent / ReleaseAgentClaim` canonical 动作、`AgentClaimState` 持久化结构、claim/release/upkeep/grace/idle reclaim domain events，以及每个治理 epoch 自动结算的 agent claim processor。
  - 把 agent claim 与 main token 账本正式接通：claim 时扣 `activation fee + claim bond + upfront upkeep`，其中 activation fee 拆到 burn + treasury，bond 进入锁定态，后续 upkeep 继续扣 liquid 并降 circulating；release / forced reclaim 则按 refund/slash 规则返还或转入 treasury bucket。
  - 新增 `crates/oasis7/src/runtime/tests/agent_claims.rs`，覆盖“首个 claim 也不免费、重复认领拒绝、release cooldown refund、欠费 grace -> forced reclaim、idle warning -> forced reclaim”五条 required 主链路。
  - runtime v1 暂定 base defaults 为 `activation fee=100`、`claim bond=200`、`upkeep=25`，`activation fee burn split=50%`，并按 `reputation_score < 10 / >= 10 / >= 25` 映射 `tier-0 / tier-1 / tier-2+`；这些仅作为当前实现与测试默认值，后续仍待 balance 复核。
- 验证:
  - `env -u RUSTC_WRAPPER cargo check -p oasis7 --lib`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib --no-run`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib agent_claims`
- 遗留事项:
  - `viewer_engineer` 仍需把 claim quote、cooldown/grace/idle reclaim 倒计时和 tier cap 阻断原因暴露到 pure API / Viewer canonical 字段。
  - `qa_engineer` 还需要补 full tier 的 abuse suite 与经济源汇审计矩阵，尤其是并发、多槽位和 refund/slash 的长链路回归。

## 19:51:28 CST / liveops_community
- 完成内容:
  - 站在 `liveops_community` 视角接收 `TASK-GAME-036 / TASK-GAMEPLAY-LTP-002`，在独立 worktree 中复核 `readme-limited-preview-invite-pack-2026-03-22.md`、closed-beta candidate runbook、反馈/事故模板与 game handoff，确认 round-1 仍应沿用 `LTP-20260322-R1` 的 approved main copy、first comment 与 `T+15m / T+1h / T+4h / T+24h` 巡检窗口。
  - 尝试用 `agent-browser` 启动 Moltbook 主通道执行：先检查 `agent-browser auth list` 与 `agent-browser state list`，确认无可复用 Moltbook auth profile，随后执行 `agent-browser --session-name moltbook-ltp open https://www.moltbook.com/`，得到 `page.goto: net::ERR_CONNECTION_CLOSED`。
  - 因 primary builder channel 在发起窗口前即不可达，明确把本轮执行状态落为 `blocked_before_publish`，没有虚报“已发出 callout”或伪造首批 builder signals；新增 `doc/readme/governance/readme-limited-preview-round1-execution-2026-03-27.md` 记录本轮 approved copy、attempt log、incident row 与 producer summary。
- 验证:
  - `agent-browser auth list`
  - `agent-browser state list`
  - `agent-browser --session-name moltbook-ltp open https://www.moltbook.com/`
  - `rg -n "blocked_before_publish|ERR_CONNECTION_CLOSED|LTP-20260322-R1|Callout Status" doc/readme/governance/readme-limited-preview-round1-execution-2026-03-27.md`
- 遗留事项:
  - `TASK-GAME-036` / `TASK-GAMEPLAY-LTP-002` 仍未完成；待 Moltbook 主通道恢复可达后，必须按同一 approved pack 重跑，而不是临时改写 claim envelope 或换成“已发布待巡检”口径。
  - 若站点持续不可达并跨过下一执行窗口，需要由 `producer_system_designer` 决定是否继续把 Moltbook 作为 round-1 primary channel，或改走其他 builder channel。

## 19:51:28 CST / producer_system_designer
- 完成内容:
  - 基于 `liveops_community` 的实际执行结果，回写 `doc/game/project.md`、`doc/game/gameplay/gameplay-limited-preview-execution-2026-03-22.project.md` 与 `doc/game/gameplay/producer-to-liveops-task-game-036-limited-preview-execution-2026-03-22.md`，把当前真值统一收口为“round-1 已尝试启动，但因 Moltbook `ERR_CONNECTION_CLOSED` 处于 `blocked_before_publish`，`TASK-GAME-036` 继续保持 `in_progress`”。
  - 同步回写 `doc/readme/README.md` 与 `doc/readme/prd.index.md`，把 `readme-limited-preview-round1-execution-2026-03-27.md` 接入治理索引，避免 round-1 执行证据悬空在目录树外。
- 验证:
  - `rg -n "TASK-GAME-036|blocked_before_publish|ERR_CONNECTION_CLOSED|ROUND-051" doc/game/project.md doc/game/gameplay/gameplay-limited-preview-execution-2026-03-22.project.md doc/game/gameplay/producer-to-liveops-task-game-036-limited-preview-execution-2026-03-22.md`
  - `rg -n "readme-limited-preview-round1-execution-2026-03-27.md" doc/readme/README.md doc/readme/prd.index.md`
- 遗留事项:
  - 当前阻断不是 unified gate，而是外部渠道可达性；在 callout 真正发出前，不应提前推进 `TASK-GAME-037/038`。

## 20:28:28 CST / liveops_community
- 完成内容:
  - 按用户最新指令把 `TASK-GAME-036` 的 round-1 primary channel 从 Moltbook 改为 GitHub issue，并直接通过 `gh issue create` 在 `eng-cc/oasis7` 仓库发布 `Limited Preview Round 1: Builder Feedback Thread`。
  - 发布线程地址为 `https://github.com/eng-cc/oasis7/issues/48`；正文继续保持 `limited playable technical preview`、`not a closed beta announcement`、`not a public launch` 边界，并要求 builder 用 `Surface / Type / What you inspected / What broke / Suggested next action` 格式回流。
  - 回写 `doc/readme/governance/readme-limited-preview-round1-execution-2026-03-27.md`，把 round-1 真值从单纯 `blocked_before_publish` 更新为“保留 Moltbook incident 证据，但 primary channel 已改为 GitHub issue 并完成发布，当前状态 `published_waiting_signals`”。
- 验证:
  - `gh issue view 48 --json url,number,title,createdAt,state`
  - `rg -n "Published Thread|published_waiting_signals|eng-cc/oasis7/issues/48" doc/readme/governance/readme-limited-preview-round1-execution-2026-03-27.md`
- 遗留事项:
  - 当前 thread 已发出，但首批有效 builder signals 仍为 `0`；接下来要按 `T+15m / T+1h / T+4h / T+24h` 围绕 issue 评论与 linked issue/PR 做巡检。
  - Moltbook 不再是本轮 primary channel，但其不可达 incident 仍保留为执行背景证据。

## 20:28:28 CST / producer_system_designer
- 完成内容:
  - 基于用户“渠道换成 GitHub issue”的明确裁决，回写 `doc/game/project.md`、`doc/game/gameplay/gameplay-limited-preview-execution-2026-03-22.project.md` 与 `doc/game/gameplay/producer-to-liveops-task-game-036-limited-preview-execution-2026-03-22.md`，将 `TASK-GAME-036` 的当前真值统一切为“callout 已通过 GitHub issue #48 发布，任务仍 in_progress，待收首批 builder signals”。
  - 明确 `ROUND-052`：当前 primary blocker 已从“无法发布”切换为“尚未收到足够外部样本”，因此 `TASK-GAME-037/038` 仍不能提前开始，但 `TASK-GAME-036` 已进入真实监控阶段。
- 验证:
  - `rg -n "eng-cc/oasis7#48|ROUND-052|published_waiting_signals|首批有效 builder signals" doc/game/project.md doc/game/gameplay/gameplay-limited-preview-execution-2026-03-22.project.md doc/game/gameplay/producer-to-liveops-task-game-036-limited-preview-execution-2026-03-22.md`
- 遗留事项:
  - 若 24h 内仍没有 3 条有效 signals，需要再判断是文案太弱、入口不对，还是需要补 maintainer follow-up comment。

## 20:38:47 CST / producer_system_designer
- 完成内容:
  - 继续收口 round-1 渠道切换后的规格一致性：把 `doc/game/prd.md`、`doc/game/project.md`、`doc/game/gameplay/gameplay-limited-preview-execution-2026-03-22.{prd,project}.md`、`doc/game/gameplay/producer-to-liveops-task-game-036-limited-preview-execution-2026-03-22.md` 与 `doc/readme/governance/readme-limited-preview-invite-pack-2026-03-22.md` 中残留的 `invite-only` 执行语义统一改为 `controlled builder-facing`，避免 GitHub issue 公开线程与专题规格互相冲突。
  - 回写 `doc/readme/governance/readme-limited-preview-round1-execution-2026-03-27.md`，明确当前真值是“boundary reminder 已内嵌到 issue body，尚未单独发布 maintainer follow-up comment”，防止执行记录假设了并不存在的首条 comment。
- 验证:
  - `gh issue view 48 --json number,title,url,createdAt,state,body`
  - `rg -n "invite-only builder|invite-only callout|invite-only 外放|Invite-only Builder Callout|发起 invite-only 预览|明确 invite-only" doc/game/prd.md doc/game/project.md doc/game/gameplay/gameplay-limited-preview-execution-2026-03-22.prd.md doc/game/gameplay/gameplay-limited-preview-execution-2026-03-22.project.md doc/game/gameplay/producer-to-liveops-task-game-036-limited-preview-execution-2026-03-22.md doc/readme/governance/readme-limited-preview-invite-pack-2026-03-22.md doc/readme/governance/readme-limited-preview-round1-execution-2026-03-27.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - `TASK-GAME-036` 仍未完成；当前只完成“渠道切换 + thread 发布 + 文档对齐”，还需等待并归档首批有效 builder signals。
  - 若后续需要把 boundary reminder 重新顶到时间线前列，可再决定是否补发 maintainer follow-up comment，但在真实需要出现前不应先把它写成既成事实。

## 17:24:30 CST / runtime_engineer
- 完成内容:
  - 在 `crates/oasis7_client_launcher/src/main.rs`、`app_process.rs`、`app_process_web.rs` 修复 launcher 控制面自动链启动竞态：首次拿到 control-plane snapshot 之前，不再用本地默认配置抢先触发 `/api/chain/start`，避免 ECS 上 `viewer_host/web_bind` 被错误回写成 `127.0.0.1`。
  - 新增/更新 `crates/oasis7_client_launcher/src/main_tests.rs` 覆盖 `should_request_auto_chain_start` 与 snapshot receipt 语义，确认这次修复不会把已有 dirty-config 同步逻辑打坏。
  - 使用 `/home/scc/Documents/keys/aliyun_ecs.txt` 提供的 ECS 凭据，清理旧 `oasis7-web-launcher.service` 运行态，重新上传并部署新的 release bundle；同时排查并确认这轮 `systemd status=203/EXEC` 只是 tar 顶层目录名错误，已修正远端目录和本地发布物结构。
  - 在 ECS 上复验 `oasis7-web-launcher`、`oasis7_game_launcher`、`oasis7_viewer_live`、`oasis7_chain_runtime` 均能启动，`ss -ltnp` 显示 `5410/4173/5011` 已按预期监听，其中 `4173/5011` 已从此前的 loopback 退回到 `0.0.0.0`。
  - 额外确认当前剩余 blocker 已从应用层收敛到云侧网络：ECS 本机 `curl http://127.0.0.1:4173/` 返回 `200`，`ufw` 未启用、`iptables` 默认 ACCEPT，但外网仍无法连入 `39.104.204.172:4173/5011`，说明剩余缺口在阿里云安全组或上游网络 ACL，不在当前仓库或主机内服务配置。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher auto_chain_start_waits_for_initial_control_plane_snapshot -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher apply_web_snapshot_marks_control_plane_snapshot_received -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher apply_web_snapshot_clears_dirty_flag_when_snapshot_matches_local_config -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher -- --nocapture`（本次编译通过；仍存在既有无关失败 `transfer_auth::tests::wasm_transfer_signing_payload_matches_runtime_helper_shape`）
  - `./scripts/build-game-launcher-bundle.sh --out-dir output/release/game-launcher-ecs-shared --profile release`
  - 远端 `systemctl status oasis7-web-launcher --no-pager -l`
  - 远端 `ss -ltnp | grep -E "(:5410|:4173|:5011|:5023|:5121)"`
  - 远端 `curl -sS http://127.0.0.1:5410/api/state`
  - 远端 `curl -sS -D - http://127.0.0.1:4173/`
  - 远端 `sha256sum -c /opt/oasis7/oasis7-checksums.txt`
- 遗留事项:
  - `shared_access` 仍未闭环：阿里云安全组/网络 ACL 还没有放通 `4173` 与 `5011`，当前公网只能访问 `5410`。
  - 当前部署模式仍是 `trusted_local_only`，尚未切到 `hosted_public_join`；因此它恢复了可用的 ECS launcher/runtime 部署链路，但还不能把 shared/public hosted player access 判为完成。
  - `./scripts/new-task-worktree.sh scripts dirty-guard-probe`
  - `./scripts/new-task-worktree.sh scripts worktree-bootstrap-smoke --allow-dirty-source --json`
  - `git -C /home/scc/oasis7 worktree list --porcelain`
  - `git -C /home/scc/worktrees/oasis7-codex-scripts-worktree-bootstrap-smoke status -sb`
  - `git worktree remove -f /home/scc/worktrees/oasis7-codex-scripts-worktree-bootstrap-smoke`
  - `git branch -D task/scripts-worktree-bootstrap-smoke`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 当前脚本只负责创建标准 worktree，不自动生成模块级 `prd.md` / `project.md` 模板；若后续希望把新需求启动进一步模板化，可再开一轮 scaffolding 专题。

## 14:59:27 CST / qa_engineer
- 完成内容:
  - 为 `scripts/new-task-worktree.sh` 补齐 `--init-docs` 与 `--with-harness` followups：新 worktree 创建后可直接检查 `doc/<module>/prd.md`、`doc/<module>/project.md` 与当日 `doc/devlog/YYYY-MM-DD.md`，并可异步预热 `worktree-harness.sh up --no-llm`。
  - 修正 task worktree family 派生逻辑，新增 `oasis7.task-worktree-family-name` 与 `oasis7.task-worktrees-root` 本地配置回写；即使从已有 task worktree 再开子任务，也不会生成错误的 `.../worktrees/worktrees/...` 目录。
  - 收口 `--json` followup 摘要：新增 `repo_name`、`worktrees_root`、`doc_checks` 与 `harness` 字段；`--with-harness` 改为后台预热并仅把状态文件路径、bootstrap log、当前 `status` / `viewer_url` 写入 JSON，不污染 stdout。
- 验证:
  - `bash -n scripts/new-task-worktree.sh`
  - `./scripts/new-task-worktree.sh --help`
  - `./scripts/new-task-worktree.sh scripts followup-docs-smoke --allow-dirty-source --json --init-docs`
  - `./scripts/new-task-worktree.sh scripts followup-harness-smoke --allow-dirty-source --json --init-docs --with-harness >/tmp/new-task-worktree-harness-out.json 2>/tmp/new-task-worktree-harness-err.log`
  - `./scripts/worktree-harness.sh status --json`
  - `./scripts/worktree-harness.sh down`
  - `git -C /home/scc/oasis7 worktree remove -f /home/scc/worktrees/oasis7-scripts-followup-docs-smoke`
  - `git -C /home/scc/oasis7 worktree remove -f /home/scc/worktrees/oasis7-scripts-followup-harness-smoke`
  - `git -C /home/scc/oasis7 worktree prune`
  - `git branch -D task/scripts-followup-docs-smoke`
  - `git branch -D task/scripts-followup-harness-smoke`
- 遗留事项:
  - 当前执行环境可能在命令返回后回收后台子进程；本轮已确认 `state.json` 能在 `booting` 阶段落盘且 JSON stdout 保持纯净，但“后台预热持续存活直到 ready”的最终体验仍建议在真实开发者 shell 再补一次人工确认。

## 15:24:11 CST / producer_system_designer
- 完成内容:
  - 为“每个需求默认独立 worktree”补齐合流闭环：新增 `scripts/land-task-worktree.sh`，统一 task branch 到 `main` 的 landing 策略为“source worktree clean 检查 -> target/main worktree clean 检查 -> source rebase target -> target fast-forward merge source -> 输出 cleanup 建议”。
  - 同步把 landing 规则写入根 `AGENTS.md`、`doc/scripts/prd.md`、`doc/scripts/project.md` 与新的治理专题 `doc/scripts/governance/task-worktree-landing-2026-03-27.*`，明确本轮只覆盖本地 `main` landing，不覆盖 push remote / PR 流程。
  - 在验证中顺手修复 `branch_checkout_path` 解析实现，避免 `new-task-worktree.sh` 与新的 landing 脚本在 branch 已被其他 worktree 检出时漏判；并确认 `--json` 模式下 landing 结果保持纯 JSON，不再混入 `git merge` 人类输出。
- 验证:
  - `bash -n scripts/land-task-worktree.sh scripts/new-task-worktree.sh`
  - `./scripts/land-task-worktree.sh --help`
  - `./scripts/land-task-worktree.sh task/scripts-landing-smoke --target landing-smoke-target --dry-run --json`
  - `./scripts/land-task-worktree.sh task/scripts-landing-smoke-2 --target landing-smoke-target-2 --json`
  - `./scripts/new-task-worktree.sh scripts duplicate-branch-smoke --allow-dirty-source --branch task/scripts-landing-smoke-2 --path /tmp/oasis7-duplicate-branch-smoke`
  - `git -C /home/scc/oasis7 worktree add -b landing-smoke-target /home/scc/worktrees/oasis7-landing-smoke-target HEAD`
  - `./scripts/new-task-worktree.sh scripts landing-smoke --allow-dirty-source --json`
  - `git -C /home/scc/oasis7 worktree add -b landing-smoke-target-2 /home/scc/worktrees/oasis7-landing-smoke-target-2 HEAD`
  - `./scripts/new-task-worktree.sh scripts landing-smoke-2 --allow-dirty-source --json`
  - `git -C /home/scc/oasis7 worktree remove -f /home/scc/worktrees/oasis7-scripts-landing-smoke`
  - `git -C /home/scc/oasis7 worktree remove -f /home/scc/worktrees/oasis7-landing-smoke-target`
  - `git -C /home/scc/oasis7 worktree remove -f /home/scc/worktrees/oasis7-scripts-landing-smoke-2`
  - `git -C /home/scc/oasis7 worktree remove -f /home/scc/worktrees/oasis7-landing-smoke-target-2`
  - `git -C /home/scc/oasis7 worktree prune`
  - `git -C /home/scc/oasis7 branch -D task/scripts-landing-smoke landing-smoke-target task/scripts-landing-smoke-2 landing-smoke-target-2`
- 遗留事项:
  - 当前 landing 入口只标准化本地 `main` 合入，不包含远端 `push`、PR 创建、review gate 与 CI 通过后自动清理；若后续要把“本地 landing -> 远端交付”也收成一条流水线，应另开专题。

## 15:59:28 CST / producer_system_designer
- 完成内容:
  - 按最新口径只收紧文案，不改 landing 行为：把 `scripts/land-task-worktree.sh`、根 `AGENTS.md`、scripts 模块文档与 landing 治理专题里“默认合入 `main`”统一改写成“默认合入本地 `main`”。
  - 保留 `--target` 参数与现有本地 git 流程不变，只在帮助文本和规范描述里明确“这里说的是本地 `main` worktree，而不是远端交付流程”。
- 验证:
  - `./scripts/land-task-worktree.sh --help`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 这轮只改文案，不涉及 `push`、PR、review gate 或任何 landing 逻辑变更。

## 16:07:54 CST / producer_system_designer
- 完成内容:
  - 收紧 task worktree 生命周期规则：把根 `AGENTS.md`、scripts 模块 PRD/project、landing 治理专题与 `scripts/land-task-worktree.sh` 输出统一改成“landing 成功后必须删除已完成 task worktree / branch”。
  - 保留“默认不自动 cleanup”的安全边界，避免脚本在 source worktree 内自删当前目录；但 cleanup 已不再是可选建议，而是 landing 成功后的必做步骤。
- 验证:
  - `./scripts/land-task-worktree.sh --help`
  - `rg -n "必须.*回收|必须.*删除|Required Cleanup" AGENTS.md doc/scripts scripts/land-task-worktree.sh`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 当前仍由人或后续 agent 执行 cleanup 命令；若未来要把“切走当前目录 -> 自动删除 source worktree/branch”也做成安全自动化，需要单开下一轮专题。

## 17:49:17 CST / runtime_engineer
- 完成内容:
  - 继续收尾阿里云 ECS `39.104.204.172` 的 hosted 部署：确认公网 `5410` 上的 operator/control plane 已按预期退回私有边界，外网访问 `/` 与 `/api/state` 均返回 `403 operator_plane_only`，`/api/public/state` 正常 `200`。
  - 直接从公网复验 shared player plane：`4173` 上的 viewer 静态页可打开并重定向到 `software_safe.html`；同源 `/api/public/player-session/admission -> issue -> refresh -> release` 全链路返回 `ok: true`，活跃会话计数可稳定从 `0 -> 1 -> 0` 往返。
  - 为 ECS 的 `oasis7-web-launcher.service` 注入新的 preview-grade hosted strong-auth signer 环境变量 `OASIS7_HOSTED_STRONG_AUTH_{PUBLIC_KEY,PRIVATE_KEY,APPROVAL_CODE}`，重启并重新通过 loopback 控制面拉起 chain/game，确认 `/api/public/state` 的 `hosted_access.action_matrix` 已从 `blocked_until_strong_auth` 升级 `prompt_control_* -> public_player_plane_with_backend_reauth_preview`。
  - 再次从公网验证 strong-auth preview lane：带有效 `player_id + release_token + approval_code` 请求 `/api/public/strong-auth/grant?action_id=prompt_control_apply` 已返回 backend-signed `HostedStrongAuthGrant`；同条件下 `action_id=main_token_transfer` 仍显式返回 `strong_auth_action_not_enabled`，说明 prompt-control preview lane 与资产动作阻断边界保持正确。
- 验证:
  - `curl -i --max-time 10 http://39.104.204.172:5410/`
  - `curl -i --max-time 10 http://39.104.204.172:5410/api/state`
  - `curl -i --max-time 10 http://39.104.204.172:5410/api/public/state`
  - `curl -i --max-time 10 http://39.104.204.172:4173/`
  - `curl -i --max-time 10 http://39.104.204.172:4173/api/public/player-session/admission`
  - `agent-browser --session ecs-viewer-public open 'http://39.104.204.172:4173/?ws=ws://39.104.204.172:5011'`
  - `agent-browser --session ecs-viewer-public screenshot /tmp/oasis7-ecs-viewer-public-2.png`
  - 公网 `issue -> refresh -> release` 直连验证
  - 公网 `prompt_control_apply` / `main_token_transfer` strong-auth grant 对比验证
  - 远端 `systemctl status oasis7-web-launcher --no-pager -l`
  - 远端 `systemctl cat oasis7-web-launcher`
- 遗留事项:
  - 当前 ECS 只完成了 hosted shared/public player access 与 preview-grade backend strong-auth grant；`oasis7_viewer_live` 仍以 `--no-llm` 运行，未在这台线上机上继续追 `prompt_control` 的真实 `preview_ack/apply_ack` success evidence。
  - 新生成的 preview approval code 现仅保存在 ECS 的 `oasis7-web-launcher.service` 环境变量中；若后续要给真实操作者使用，需要结合 runbook 决定如何保管/轮换，而不是把它当成长期固定口令。

## 18:40:04 CST / qa_engineer
- 完成内容:
  - 按 `oasis7` 技能在本机复验真实 OpenClaw gameplay 路径，而不是停留在 4-step smoke：先确认 `127.0.0.1:18789` OpenClaw Gateway 存活、补装 `oasis7_openclaw_agent`，再启动 `oasis7_provider_local_bridge`，确认 `127.0.0.1:5841` 的 `/v1/provider/info` 与 `/v1/provider/health` 正常。
  - 使用现成 release bundle `output/release/game-launcher-ecs-shared` 执行 `oasis7-run.sh play --reuse-bridge --skip-agent-setup --execution-mode headless_agent --no-open-browser`，确认 `run-game.sh --with-llm --agent-provider-mode provider_loopback_http` 可真实拉起 `oasis7_game_launcher -> oasis7_viewer_live -> oasis7_chain_runtime` 主链路，launcher stack ready。
  - 进一步把 real-play 加深到 `player_parity` 长样本：执行 `oasis7-run.sh smoke --execution-mode player_parity --samples 3 --ticks 12 --timeout-ms 30000`，三条 `P0-001` 样本全部 `status=passed`、`goal_completed=true`、`timeout_count=0`，说明 OpenClaw 作为外部 provider 的真实 gameplay 链路已能稳定完成目标。
  - 同时记录出更深回合下的新边界：三条样本都稳定收敛到 `action_kind_counts={move_agent:10, wait:2}`、`invalid_action_count=2`、`error_counts={action_rejected:4}`，末两步会复现 `move_agent -> action_rejected`，说明当前 `oasis7_p0_low_freq_npc` 在长样本里仍存在固定策略偏差，不再是“只看 4 步 smoke 就全绿”的状态。
- 验证:
  - `./.agents/skills/oasis7/scripts/oasis7-run.sh doctor --repo-root /home/scc/worktrees/oasis7-p2p-shared-ecs-deploy --json`
  - `bash scripts/setup-provider-oasis7-runtime.sh`
  - `env -u RUSTC_WRAPPER cargo run -p oasis7 --bin oasis7_provider_local_bridge -- --openclaw-agent oasis7_openclaw_agent`
  - `curl -sS http://127.0.0.1:5841/v1/provider/info`
  - `curl -sS http://127.0.0.1:5841/v1/provider/health`
  - `./.agents/skills/oasis7/scripts/oasis7-run.sh play --repo-root /home/scc/worktrees/oasis7-p2p-shared-ecs-deploy --bundle-dir /home/scc/worktrees/oasis7-p2p-shared-ecs-deploy/output/release/game-launcher-ecs-shared --reuse-bridge --skip-agent-setup --execution-mode headless_agent --no-open-browser`
  - `./.agents/skills/oasis7/scripts/oasis7-run.sh smoke --repo-root /home/scc/worktrees/oasis7-p2p-shared-ecs-deploy --reuse-bridge --skip-agent-setup --execution-mode player_parity --samples 3 --ticks 12 --timeout-ms 30000 --connect-timeout-ms 15000`
  - `jq '{status,goal_completed,decision_steps,invalid_action_count,timeout_count,median_latency_ms,p95_latency_ms,action_kind_counts,error_counts}' output/openclaw_parity/openclaw_parity_20260327_183753/samples/provider_loopback_http/sample_1/summary/P0-001.provider_loopback_http.json`
  - `cat output/openclaw_parity/openclaw_parity_20260327_183753/summary/combined.csv`
- 遗留事项:
  - 本次 deeper play 只证明了本机 OpenClaw gameplay provider 路径；没有把这条本机 provider 通过隧道接回 ECS，因此仍不构成远端 hosted `prompt_control` authority 的补证。
  - `player_parity` 长样本里稳定出现 `action_rejected`，后续若要继续“玩得更深”而不是只做通过判定，应转向 profile/prompt 调整或更长场景样本，查清为什么末段会多出两次无效 `move_agent`。

## 20:48:39 CST / producer_system_designer
- 完成内容:
  - 基于用户“首个 agent 认领也不免费，其他按押金 + upkeep 规则做”的明确裁决，新开独立 worktree 并新增 `PRD-GAME-011`：`doc/game/gameplay/gameplay-agent-claim-token-cost-2026-03-27.{prd,design,project}.md`。
  - 正式冻结 agent claim 的 v1 口径：每次认领都必须支付非零 `activation fee`、锁定 `claim bond`、承担 `upkeep`；同时定义 `tier-0/1/2+ -> cap 1/2/3`、`slot-1/2/3 -> 1.0x/1.5x/2.0x`、`release_cooldown_epochs=3`、`grace_epochs=2`、闲置 `7` epoch 进入回收候选、最晚 `10` epoch 完成回收。
  - 同步回写 `doc/game/prd.md`、`doc/game/project.md`、`doc/game/prd.index.md` 与 `doc/game/README.md`，把新专题接入 game 根入口，并拆出 `TASK-GAME-040/041/042/043` 给 runtime/viewer/qa/producer 后续执行。
- 验证:
  - `rg -n "PRD-GAME-011|TASK-GAME-039|TASK-GAME-040|首个 claim 也不免费|activation fee|claim bond|upkeep" doc/game/prd.md doc/game/project.md doc/game/prd.index.md doc/game/README.md doc/game/gameplay/gameplay-agent-claim-token-cost-2026-03-27.prd.md doc/game/gameplay/gameplay-agent-claim-token-cost-2026-03-27.project.md`
- 遗留事项:
  - 当前只完成规则专题建档与任务拆解，尚未落地 runtime canonical claim 状态机与 viewer/pure API 表达。
  - 绝对 token 定价仍故意留在后续平衡阶段，不应把本次 PRD 误读成最终价格公告。

## 22:37:10 CST / viewer_engineer
- 完成内容:
  - 完成 `TASK-GAMEPLAY-AGC-003`：在 `crates/oasis7/src/viewer/runtime_live/` 新增 agent claim canonical 快照构建，把 `player_gameplay.agent_claim` 接入 compat snapshot / pure API `--player-gameplay-only`，统一输出 claimer、tier/cap、未认领报价、claimed status、release/grace/idle reclaim 倒计时与 cap blocker。
  - 在 `crates/oasis7_viewer/src/ui_text_claims.rs` 落地选中 agent 详情扩展，并挂入 `ui_text.rs`；现在右侧 details 会显示 claim owner、bond/upkeep、release ready / grace remaining / forced reclaim epoch 倒计时，以及未认领目标的报价与 blocker。
  - 顺手补齐因协议字段演进导致的 viewer 测试编译阻塞：为 `PromptControlApplyRequest` 旧测试初始化加上 `strong_auth_grant: None`，保证本轮 viewer 定向测试可正常编译执行。
- 验证:
  - `env -u RUSTC_WRAPPER cargo check -p oasis7 --lib`
  - `env -u RUSTC_WRAPPER cargo check -p oasis7_viewer`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib compat_snapshot_exposes_player_agent_claim_overview -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer update_ui_populates_agent_selection_details_with_claim_state -- --nocapture`
- 遗留事项:
  - 目前 viewer 侧只做了 details 文案接入，未新增独立 claim 面板；若后续需要把认领经济做成独立 UI 流程，应另开 viewer 任务，不在本轮继续扩展布局。
  - `TASK-GAMEPLAY-AGC-004` 仍需由 `qa_engineer` 补齐 claim 并发、欠费、闲置、cap 与 refund/slash 的系统级矩阵，当前只覆盖了 viewer / API 所需的最小 required 闭环。

## 22:55:18 CST / qa_engineer
- 完成内容:
  - 完成 `TASK-GAMEPLAY-AGC-004`：在 `crates/oasis7/src/runtime/tests/agent_claims.rs` 补齐 claim QA 矩阵，新增并发争抢仅 winner 扣费、`tier-1` 第二槽位涨价且第三槽位超 cap 被拒、grace 内补款恢复、release 全额退回 locked bond、欠费/闲置 forced reclaim 的 treasury/slash/refund 对账断言。
  - 新增 `doc/testing/evidence/game-agent-claim-abuse-matrix-2026-03-27.md`，把 required/full 真值、分类矩阵、执行命令和 QA 结论固定下来，便于后续 producer 做参数复核时直接引用。
  - 同步把 claim 专题项目状态推进到 `TASK-GAMEPLAY-AGC-005`，并在项目文档里记录当前 claim QA 已 `pass`，剩余工作收敛为平衡参数复核。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib --features test_tier_required runtime::tests::agent_claims:: -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib --features test_tier_full runtime::tests::agent_claims:: -- --nocapture`
- 遗留事项:
  - 直接运行不带 `--lib` 的 `cargo test -p oasis7 --features test_tier_required runtime::tests::agent_claims:: -- --nocapture` 目前会命中与本专题无关的 `crates/oasis7/src/bin/oasis7_hosted_access.rs` 编译错误；该问题不影响当前 claim runtime suite 结论，但需要由对应 owner 另行处理。
  - 当前 QA 已确认结构性 abuse / accounting 行为闭环，不代表默认数值一定最优；是否继续维持 `activation fee / bond / upkeep / penalty_bps / tier cap` 仍需 `producer_system_designer` 在 `TASK-GAMEPLAY-AGC-005` 中复核。

## 23:03:40 CST / producer_system_designer
- 完成内容:
  - 完成 `TASK-GAMEPLAY-AGC-005` 首轮平衡复核，并把结论回写到 `PRD-GAME-011`：当前继续维持 agent claim 的 v1 默认参数，不新开调参专题。
  - 维持项包括 `slot multiplier=1.0/1.5/2.0`、`grace_epochs=2`、`forced_reclaim_penalty_bps=2000`、`tier cap=1/2/3`；理由是 runtime/viewer/QA 闭环已验证结构正确，但还没有真实 claim 分布、grace 命中率、idle reclaim 占比和组织扩张样本支持改价。
  - 同步把项目状态标记为 `completed`，明确本专题下一次重开只由真实 liveops / QA / 持有数据异常触发，而不是继续围绕空样本改默认值。
- 验证:
  - `rg -n "DEC-AGC-006|首轮平衡复核结论|TASK-GAMEPLAY-AGC-005|当前状态: completed|下一任务: 无" doc/game/gameplay/gameplay-agent-claim-token-cost-2026-03-27.prd.md doc/game/gameplay/gameplay-agent-claim-token-cost-2026-03-27.design.md doc/game/gameplay/gameplay-agent-claim-token-cost-2026-03-27.project.md doc/game/project.md`
- 遗留事项:
  - 当前“维持默认值”不等于这些数值已经最优，只表示在没有真实 claim 行为样本前，不继续制造第二套未经验证的参数。
  - 若后续 `liveops_community`、`qa_engineer` 或真实组织扩张数据表明 `slot-2/3`、`grace`、`penalty`、`tier cap` 有系统性偏轻/偏重，再重新开调参专题。

## 00:10:45 CST / runtime_engineer
- 完成内容:
  - 修复 `TASK-P2P-042` compile/CI 主阻断：把误落在 `src/bin/` 的 `oasis7_hosted_access` 提升为共享模块 `crates/oasis7/src/hosted_access.rs`，收口 `--bins` 编译错误与额外 bin 缺 `main` 问题。
  - 调整 builtin wasm materializer 优先走 host-native `nightly-2025-12-11 + wasm32-unknown-unknown` 构建、保留 Docker fallback，并同步刷新 `m1/m4/m5` builtin wasm identity/hash 产物与对应测试常量，消除 required CI 中的 Docker 拉取失败与 identity 漂移。
  - 重写 `crates/oasis7_viewer/src/ui_text_claims.rs` 的 claim 详情读取路径，改为兼容 `runtime_snapshot` JSON 与 `player_gameplay` 快照，恢复 `oasis7_viewer` 在 `wasm32-unknown-unknown` 下的编译。
  - 为 `oasis7_chain_runtime` transfer API tests 增加串行共享状态锁与每测重置，消除 `TransferTracker/NEXT_TRANSFER_ACTION_ID` 并行串扰；同时把 `oasis7_game_launcher` strong-auth 测试的 env 锁改为 poison-safe，避免单测失败级联污染后续用例。
  - 重新跑通整套 `./scripts/ci-tests.sh required`，确认 compile/required CI 已恢复全绿。
- 验证:
  - `env -u RUSTC_WRAPPER cargo check -p oasis7 --bins`
  - `env -u RUSTC_WRAPPER cargo check -p oasis7_viewer --target wasm32-unknown-unknown`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 builtin_identity_manifest_resolves_ -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer tests_selection_details::update_ui_populates_agent_selection_details_with_claim_state -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `./scripts/ci-tests.sh required`
- 遗留事项:
  - 当前 required 已恢复通过，但仓内仍存在若干既有 warning，后续若要收口发布面洁净度可再单开 warning cleanup 任务。
  - `.distfs/builtin_wasm` 本地构建缓存已在本任务中复用，当前不作为提交产物；若后续 identity 再漂移，优先复查 toolchain/metadata 环境与 sync 脚本输入。
