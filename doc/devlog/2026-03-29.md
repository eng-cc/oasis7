# 2026-03-29

## 20:15:50 CST / producer_system_designer
- 完成内容:
  - 回写 `doc/game/gameplay/gameplay-agent-claim-token-cost-2026-03-27.{prd,design,project}.md`，将 `PRD-GAME-011` 从“claim 只消费 liquid main token”升级为“首个 claim 仍保持非零 canonical 成本，但 `slot-1` 可消费 `restricted starter claim balance`”，并明确 transfer guard、refund provenance、slot-2/3 blocker 与审计字段。
  - 同步更新 `doc/game/prd.md` 与 `doc/game/project.md`，把 `restricted starter claim balance` 挂入 game 根入口，并新增 `TASK-GAME-044/045/046/047/048` 与对应 `TASK-GAMEPLAY-AGC-006/007/008/009/010` 的追踪链。
  - 维持 `TASK-GAME-036` 作为 game 模块当前 next task，不打断现有 limited preview callout；新 claim 规则变更以“已登记待排任务”方式并入 project。
- 遗留事项:
  - `runtime_engineer` 仍需落地受限 bucket、slot-1 claim/upkeep 专用扣费、bond provenance、refund 拆分与 transfer guard。
  - `viewer_engineer` / `qa_engineer` 仍需补齐 restricted/liquid 余额表达、pure API 字段、allowlist/QA seed 发放与 provenance/过期矩阵回归。

## 21:04:05 CST / runtime_engineer
- 完成内容:
  - 在 `crates/oasis7/src/runtime/` 为 `MainTokenAccountBalance` 增加 `restricted_starter_claim_balance`，并把 `AgentClaimed / AgentClaimUpkeepSettled / AgentClaimReleased / AgentClaimReclaimed` 事件与 `AgentClaimState` 扩展为 provenance-aware 结构：显式记录 upfront restricted/liquid spend、bond restricted/liquid lock 与 refund split。
  - 运行时 claim 结算现改为 `slot-1` restricted-first、liquid-fallback，`slot-2/3` 继续 liquid-only；release / forced reclaim 的 bond refund 现按原 funding source 回 restricted/liquid bucket，公开转账预检继续只认 transferable liquid，并在 restricted-only 情况下返回结构化 `transferable_balance=0` 提示。
  - 同步补齐 `player_gameplay.agent_claim`、viewer claim 文案、transfer account API、explorer address/assets 输出中的 restricted/liquid/eligible/funding mix 字段，并新增定向测试覆盖 slot-1 restricted claim/upkeep、mixed funding refund provenance、slot-2 guard、restricted-only transfer reject 与 viewer snapshot 真值。
- 遗留事项:
  - `qa_engineer` 仍需执行 `TASK-GAME-047`，补齐 allowlist/QA seed 发放、过期/回收与经济审计矩阵。
  - `producer_system_designer` 仍需在 `TASK-GAME-048` 基于首轮真实 preview/liveops/QA 数据复核 starter balance 的额度、过期与发放边界。

## 19:59:59 CST / liveops_community
- 完成内容:
  - 新增小红书第六篇素材包 `readme-xiaohongshu-spring-recruit-post-pack-2026-03-29.md`，固定标题《作为游戏工作室主理人，今年春招我会看什么人》、正文、关键词、互动问题与热点借势边界。
  - 同步回写 `doc/readme/prd.md`、`doc/readme/project.md`、`doc/readme/README.md`、`doc/readme/prd.index.md` 与小红书 runbook 源文档列表，保持第六篇素材包可追踪、可回查。
  - 新增 `site/social/xiaohongshu-spring-recruit-cover.html` 与导出图 `site/social/xiaohongshu-spring-recruit-cover.png`，视觉方向为“主理人判断板 / 招人筛选视角 / 暖纸面工作台”，并完成 1080x1440 校验。
- 遗留事项:
  - 发布后需按小红书 runbook 执行 `T+15m / T+1h / T+4h / T+24h` 巡检，并观察评论区是更关心“AI 怎么写进作品集”还是“玩家感觉”。

## 20:25:00 CST / liveops_community
- 完成内容:
  - 新增第六篇轮播版素材包 `readme-xiaohongshu-spring-recruit-carousel-pack-2026-03-29.md`，把长文版压成 6 页手机端判断卡，并固定逐页文案与评论区收束问题。
  - 新增 `site/social/xiaohongshu-spring-recruit-carousel.html` 与 `site/social/xiaohongshu-spring-recruit-carousel-slide1.png` 到 `slide6.png`，完成 6 张 `1080x1440` 轮播图导出与抽样目检。
  - 同步补齐 `doc/readme/prd.md`、`doc/readme/project.md`、`doc/readme/README.md`、`doc/readme/prd.index.md` 与 runbook 源文档列表，保持轮播版可追踪。
- 遗留事项:
  - 发布时需在长文版和轮播版中二选一，不建议同一时间重复发两套。

## 20:36:06 CST / viewer_engineer
- 完成内容:
  - 将 `site/social/xiaohongshu-spring-recruit-cover.html` 与 `site/social/xiaohongshu-spring-recruit-carousel.html` 的视觉主题从“中性工作台”刷新为更贴春招语境的 `春季招聘季刊 / 校园公告栏 / 简历筛选板`。
  - 强化封面与轮播的季节感、层次和版面记忆点：引入浅绿、花瓣粉、暖杏色，增加票据、胶带、筛选卡和轻花瓣装饰，避免俗套招聘会海报感。
  - 重新导出 `site/social/xiaohongshu-spring-recruit-cover.png` 与 `site/social/xiaohongshu-spring-recruit-carousel-slide1.png` 到 `slide6.png`，并完成关键页目检。
  - 同步回写长文版与轮播版素材包的视觉说明，并在 `doc/readme/project.md` 记录视觉刷新任务。
- 遗留事项:
  - 如果后续评论区更偏“校招面试”语境，可以再把第 1 页和第 6 页的提问词继续往“面试自我判断”上压一层。

## 20:52:10 CST / viewer_engineer
- 完成内容:
  - 将 `site/social/xiaohongshu-spring-recruit-cover.html` 与 `site/social/xiaohongshu-spring-recruit-carousel.html` 的主色板从薄荷粉杏系改成海盐蓝 / 奶油黄 / 校园红的冷暖对比方案，更接近春招公告栏而不是春日手账。
  - 同步调整封面与轮播里的背景渐变、装饰贴片、筛选卡、收束条和统计卡底色，保持版式不变但整体气质更利落。
  - 回写第六篇长文版与轮播版素材包的视觉说明，并在 `doc/readme/project.md` 追加配色换版追踪。
- 遗留事项:
  - 如果还想继续往“更克制”或“更锐一点”的方向走，下一轮可以只在强调色和阴影密度上再做一档收窄。

## 21:16:17 CST / liveops_community
- 完成内容:
  - 按小红书 20 字标题上限，将第六篇最终发布标题统一收口为《作为游戏工作室主理人，今年的春招视角》。
  - 同步更新长文版与轮播版素材包标题建议、`doc/readme/prd.md` 的第六篇口径和 `doc/readme/project.md` 任务追踪，避免后续继续沿用超长标题。
- 遗留事项:
  - 封面图主文案仍可保留当前短句结构，不需要为了平台标题限制再去压缩画面层文字。

## 21:30:00 CST / qa_engineer
- 完成内容:
  - 新增 `doc/testing/evidence/game-agent-claim-restricted-starter-balance-matrix-2026-03-29.md`，把 restricted starter balance 的 QA 守门面拆成 slot-1 专用消费、mixed funding refund provenance、slot-2 blocker、transfer guard、explorer 非转账显示、grant lifecycle / audit blocker 六类矩阵。
  - 补齐并复跑定向真值：`runtime::tests::agent_claims` required/full、`viewer::runtime_live::tests::compat_snapshot_*`、`oasis7_chain_runtime` 的 restricted-only transfer reject、transfer accounts 分离展示与 explorer address/assets 非转账口径。
  - QA 结论正式收口为 `block`：当前 claim/upkeep/refund/transfer guard/viewer parity 全部通过，但 restricted grant 的 `issuance_reason / issuer_id / expires_at_epoch` 发放与过期回收、以及对应经济审计链仍未实现。
- 遗留事项:
  - `producer_system_designer` 需要在 `TASK-GAME-048` 决定是收窄 `PRD-GAME-011` 的 grant lifecycle 范围，还是重新打开 runtime/liveops 实现任务补齐 grant issue/expire/revoke/audit。

## 21:33:54 CST / liveops_community
- 完成内容:
  - 按用户确认的“moltbook 已恢复”继续执行 `2026-03-27` 挂起的外部游戏讨论 follow-up，先用 `agent-browser --session-name moltbook-ltp open https://www.moltbook.com/` 确认首页已恢复可达，不再是 `ERR_CONNECTION_CLOSED`。
  - 复核 shell 侧 API 链路：本地 `7897` proxy 继续复现 `curl: (35) OpenSSL SSL_connect: SSL_ERROR_SYSCALL`，直连 proxy 读评论也仍会超时；因此本轮改用浏览器上下文 `fetch` 继续执行，而不是把站点恢复误判为“shell proxy 也已恢复”。
  - 回查 `AI Council - Diplomacy Game for AI Agents (7 Factions Open!)`（`post_id=1c0c9a7a-de63-4870-a3be-596e17170dd8`）与 `Why Games Are Better Testbeds Than Production: Observable Failure Without Consequences`（`post_id=d566995d-78cf-4af3-9ef4-21ddc5c9a366`），确认前者仍无 `oasis7` 评论、后者没有因 `2026-03-26` timeout 留下幽灵重复评论。
  - 在 `AI Council` 下新发纯玩法评论 `comment_id=8d8e5dd3-7bca-4b5b-86e0-48f898e34565`，继续把讨论收在 `permanent reputation / asymmetric victory conditions / trust debt`，随后用 `POST /api/v1/verify` 提交 `verification_code=moltbook_verify_900a525e5f80f6a936bf4dfbf8ba779d` 与 `answer=115.00`，接口返回 `Verification successful!`。
  - 在 `Why Games Are Better Testbeds Than Production` 下补发评论 `comment_id=81bd3255-f967-4720-8fdf-50ecb03d0b33`，把楼层收在 `failure leaves inspectable residue`、`legible trace` 与 `repeated social game with memory`，随后用 `POST /api/v1/verify` 提交 `verification_code=moltbook_verify_a0dee3ac97f0556ad3a9df1b33abb5b2` 与 `answer=32.00`，接口返回 `Verification successful!`。
  - 回查评论读接口时，`81bd3255-f967-4720-8fdf-50ecb03d0b33` 已刷成 `verification_status=verified`；`8d8e5dd3-7bca-4b5b-86e0-48f898e34565` 虽然验证接口已成功，但评论读取接口暂时仍回 `verification_status=pending`，判断为评论读缓存延迟而不是验证失败。
- 遗留事项:
  - 下次 Moltbook 巡检先优先回查 `comment_id=8d8e5dd3-7bca-4b5b-86e0-48f898e34565` 是否从 `pending` 刷成 `verified`；若长时间不刷新，再考虑是否需要补做一次读取确认或平台侧人工排查。
  - `GET /api/v1/home` 在浏览器上下文下仍返回 `500 Internal server error`，因此当前不依赖 home/notifications 做运营闭环，继续以目标帖子级读取为主。
  - 本地 `7897` proxy 目前仍未恢复到稳定可用，后续 shell 侧若还要批量处理 Moltbook，优先先修 proxy，再恢复 CLI 批量动作。

## 21:36:47 CST / producer_system_designer
- 完成内容:
  - 完成 `TASK-GAME-048` producer review，并正式决定不收窄 `PRD-GAME-011`：restricted starter balance 继续维持 `slot-1` 专用、不可转账、refund 保留 provenance 的受限余额边界，不能把当前“只有数值 bucket”的实现视为专题完成。
  - 回写 `doc/game/gameplay/gameplay-agent-claim-token-cost-2026-03-27.{prd,project}.md`、`doc/game/{prd,project}.md`，把 restricted grant 缺失项拆成 `TASK-GAME-049/050/051` 与 `TASK-GAMEPLAY-AGC-011/012/013`，明确后续需补齐 `issuance_reason / issuer_id / expires_at_epoch`、issue/expire/revoke 事件、issuer runbook 与 QA lifecycle/audit matrix。
  - 将当前产品裁决收口为“继续保持 blocker，而不是静默降级需求范围”：在 grant lifecycle 与经济审计链落地前，restricted starter balance 仍不得宣称完整放行。
- 遗留事项:
  - `runtime_engineer` 需先执行 `TASK-GAME-049` / `TASK-GAMEPLAY-AGC-011`，补齐 restricted grant metadata、事件与审计链。
  - `liveops_community` 与 `qa_engineer` 需随后完成 `TASK-GAME-050/051`，冻结 issuer runbook 并复跑 lifecycle/audit 守门矩阵。

## 21:38:30 CST / liveops_community
- 完成内容:
  - 按用户要求把 `human.md` 中仅对 Moltbook 日常运营有用的两条本机约定移出，避免继续把渠道执行前提放在非正式开发者便笺里。
  - 在 `doc/readme/governance/readme-moltbook-liveops-runbook-2026-03-21.md` 新增 `1.1 本地执行环境`，固定 `~/.config/moltbook/token` 默认路径、`127.0.0.1:7897` proxy 的优先使用方式，以及 proxy 不稳时退回浏览器上下文的执行回退。
  - 同步删除 `human.md` 里的 Moltbook token / proxy 两条，收口渠道执行信息的单一真值。
- 遗留事项:
  - 这次只补 runbook，没有额外把本机路径写进模块级 PRD/project；若后续需要把“operator environment assumptions”扩成跨渠道统一规范，再单开治理任务。

## 21:51:44 CST / liveops_community
- 完成内容:
  - 按用户确认的最终文案，在 `m/general` 发布新帖 `When should an agent world refuse to forgive?`，正文聚焦 `recovery vs forgiveness`、`trust debt`、`lost access` 与 `obligations that still bind after the breach`，继续沿用已验证更稳的 `general + builder question` 路线，不回到 `gaming`。
  - 主贴发布成功后拿到 `post_id=89a2f99d-c909-4bfa-a630-3c36533ae8b1`，并通过 `POST /api/v1/verify` 提交 `verification_code=moltbook_verify_2f3900152053312397056e5fb22cc31c` 与 `answer=50.00`；接口返回 `Verification successful! Your post is now published.`。
  - 本轮继续不补首评，避免再次踩中“新帖刚发出 + 官方账号立刻自评”的 spam 风险组合；先看自然评论会不会把话题收进 `forgiveness boundary / durable trust debt / irreversible consequences`。
  - 回查 `GET /api/v1/posts/89a2f99d-c909-4bfa-a630-3c36533ae8b1` 时，帖子已可读，当前 `submolt=general`、`comment_count=0`、`score=0`；读接口暂时仍回 `verification_status=pending`，判断为发布后短时缓存延迟，而不是验证失败。
- 遗留事项:
  - 下一检查窗口按 runbook 走 `T+15m / T+1h / T+4h / T+24h`，重点看评论是否自然落到 `trust debt / loss of access / obligations after breach / what should stay hard to get back`。
  - 如果楼层开始滑向抽象道德讨论，回复时优先把讨论重新压回 `world rule / memory / consequence / recovery boundary`，不要让它散成泛伦理表态。

## 22:20:10 CST / runtime_engineer
- 完成内容:
  - 在 `crates/oasis7/src/runtime/` 为 restricted starter funding 增加正式 `grant` 生命周期模型：新增 `RestrictedStarterClaimGrantState/Status`、`IssueRestrictedStarterClaimGrant / RevokeRestrictedStarterClaimGrant` 动作、`RestrictedStarterClaimGrantIssued / Expired / Revoked` canonical 事件，以及 `restricted_starter_claim_grants` 持久化状态。
  - 将 grant source-sink 审计接入 ecosystem treasury：grant 发放从 `MAIN_TOKEN_TREASURY_BUCKET_ECOSYSTEM_POOL` 出账，grant 到期/撤销时把 beneficiary 剩余 restricted spendable 退回同一 treasury bucket；同时在 claim `release / reclaim` 事件里显式记录 restricted refund sink，当 grant 已 `expired/revoked` 时，后续 restricted bond refund 会直接回 treasury，而不再返还到 beneficiary restricted bucket。
  - 补齐 epoch 自动过期处理、issuer-scope 发放/撤销校验与定向回归；已通过 `env -u RUSTC_WRAPPER cargo check -p oasis7 --lib`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --lib --features test_tier_required runtime::tests::agent_claims:: -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --lib --features test_tier_full runtime::tests::agent_claims:: -- --nocapture`。
- 遗留事项:
  - `liveops_community` 仍需执行 `TASK-GAME-050 / TASK-GAMEPLAY-AGC-012`，冻结 restricted grant 的 issuer runbook、过期策略、撤销条件与 incident fallback。
  - `qa_engineer` 仍需执行 `TASK-GAME-051 / TASK-GAMEPLAY-AGC-013`，建立 lifecycle / source-sink audit matrix，并把当前 runtime 新增的 expiry/revoke/refund-sink 真值升级为正式 QA 结论。

## 22:30:55 CST / runtime_engineer
- 完成内容:
  - 定位 `GitHub Actions` 失败根因为 `m5` builtin wasm tracked manifest 与 GitHub runner 当前 canonical 构建产物漂移，导致 `Wasm Determinism Gate` 和 `Rust required` 同时失败。
  - 将 `crates/oasis7/src/runtime/world/artifacts/m5_builtin_modules.sha256` 与 `m5_builtin_modules.identity.json` 对齐到 `2026-03-29` 失败流水线中 GitHub runner 实际产出的 canonical `linux-x86_64` hash。
  - 调整 `crates/oasis7/src/runtime/builtin_wasm_materializer.rs`：builtin wasm fallback 在 `CI=true` 时不再优先走 host-native 快路；若本地快路构建出的 hash 不在 expected manifest 中，则自动重试 canonical docker 构建。
  - 同时收紧 builtin wasm 缓存命中条件，只有 cached hash 仍在 expected manifest 内时才允许复用，避免旧 `.distfs` 缓存把 runtime 拉回过期产物。
  - 本地已完成 `env -u RUSTC_WRAPPER cargo fmt --all`、`git diff --check` 与 `env -u RUSTC_WRAPPER cargo test -p oasis7 builtin_wasm_materializer --lib --features test_tier_required`。
- 遗留事项:
  - 当前机器拉取 `docker.io/library/debian:bookworm-slim` 会超时，无法在本地完整重放 `sync-m5-builtin-wasm-artifacts.sh --check` 或 GitHub 的 canonical wasm gate；需要依赖下一次 GitHub CI 复核最终结果。

## 23:20:37 CST / runtime_engineer
- 完成内容:
  - 继续跟进 `fix(ci): align m5 builtin wasm manifests` 触发后的新一轮 GitHub CI，确认 `m5` 已不再是首个 blocker，新的 `Wasm Determinism Gate` 失败点已前移到 `m1.rule.move` canonical hash mismatch；同一轮 `Rust required` 还暴露了 `builtin_wasm_identity.rs` 里残留的旧 `m5.gameplay.war.core` hardcoded hash。
  - 按 GitHub-hosted `linux-x86_64` runner 日志回写 `crates/oasis7/src/runtime/world/artifacts/m1_builtin_modules.sha256` 与 `m1_builtin_modules.identity.json`，将 `m1.rule.move / visibility / transfer / body / sensor / mobility / memory / storage / power.*` 的 tracked canonical hash token 全部对齐到当前 CI 真值。
  - 同步修正 `crates/oasis7/src/runtime/tests/builtin_wasm_identity.rs` 中的 `m1.rule.move` 与 `m5.gameplay.war.core` hardcoded canonical hash，避免 identity manifest smoke test 继续拿旧 token 误报。
  - 本地已完成 `./scripts/doc-governance-check.sh`、`git diff --check`、`env -u RUSTC_WRAPPER cargo fmt --all -- --check`、`env -u RUSTC_WRAPPER cargo test -p oasis7 builtin_wasm_identity --lib --features test_tier_required` 与 `env -u RUSTC_WRAPPER cargo test -p oasis7 builtin_wasm_materializer --lib --features test_tier_required`。
- 遗留事项:
  - 当前机器对 `docker.io/library/debian:bookworm-slim` 的 metadata 拉取仍然超时，`env -u RUSTC_WRAPPER cargo test -p oasis7 --tests --features test_tier_required` 在需要 canonical-docker fallback 的 builtin wasm 用例上仍会本地失败；本轮 `m1/m5` canonical hash 修复只能依赖 GitHub-hosted runner 复核，而不能在本机完整重放 full required gate。
