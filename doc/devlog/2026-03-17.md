# 2026-03-17

## 00:21:47 CST / agent_engineer
- 完成内容：为 OpenClaw 双轨模式落地真实 `player_parity` 执行 lane，新增 `ProviderExecutionMode::parse()`，把 execution mode 接到 runtime live、`oasis7_game_launcher`、`oasis7_provider_parity_bench`、`scripts/openclaw-parity-p0.sh` 与 `.agents/skills/oasis7/scripts/oasis7-run.sh`。
- 验证方式：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- oasis7_game_launcher_tests`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_provider_parity_bench -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 runtime_openclaw -- --nocapture`、`env -u RUSTC_WRAPPER cargo check -p oasis7`。
- 遗留事项：`qa_engineer` / `producer_system_designer` 仍需基于真实双模式样本完成 `PRD-WORLD_SIMULATOR-040` T4 正式对照结论。

## 00:24:31 CST / agent_engineer
- 完成内容：用 `.agents/skills/oasis7/scripts/oasis7-run.sh smoke --execution-mode <mode>` 分别完成 `headless_agent` 与 `player_parity` 的真实 OpenClaw smoke，确认两条链路都能在 `P0-001` / `4 ticks` 下通过。
- 关键产物：`output/openclaw_parity/openclaw_parity_20260317_002147/summary/P0-001.provider_loopback_http.json`、`output/openclaw_parity/openclaw_parity_20260317_002217/summary/P0-001.provider_loopback_http.json`。
- 遗留事项：需要由 `qa_engineer` / `producer_system_designer` 基于上述产物回写 T4 默认模式与偏差结论。

## 01:12:11 CST / qa_engineer
- 完成内容：基于 `output/openclaw_parity/openclaw_parity_20260317_002147` 与 `output/openclaw_parity/openclaw_parity_20260317_002217` 的真实双样本，完成 `PRD-WORLD_SIMULATOR-040` T4 的 `player_parity` vs `headless_agent` 对照采证，确认两条模式在 `P0-001` 单样本上无阻断级偏差。
- 结论：T4 `pass`；默认回归模式维持 `headless_agent`，`player_parity` 保留为体验对照 lane；本轮不替代 `PRD-WORLD_SIMULATOR-038` 的 builtin/OpenClaw parity 扩面统计。
- 遗留事项：继续推进 builtin/OpenClaw 真实 parity 双边样本与评分卡双签。

## 01:12:11 CST / producer_system_designer
- 完成内容：审阅 QA 对照结果并冻结双轨模式默认策略：`headless_agent` 作为 CI / server / 自动回归默认模式，`player_parity` 作为制作人 / QA 体验对照与准入门禁，`debug_viewer` 保持 observer-only。
- 结论：`PRD-WORLD_SIMULATOR-040` T4 收口；后续若要宣称“玩家体验等价”，仍需以 `PRD-WORLD_SIMULATOR-038` 的 builtin/OpenClaw parity 结果为准。
- 遗留事项：安排下一轮 `PRD-WORLD_SIMULATOR-038` 扩面采证与评分卡汇总。

## 01:40:00 CST / qa_engineer
- 完成内容：执行真实 builtin/OpenClaw `P0-001` parity 批次 `openclaw_builtin_parity_20260317_t4d`，输出聚合对比、失败签名与 QA 结论；确认当前 parity 不成立。
- 结论：builtin `completion_rate=0%`、OpenClaw `completion_rate=100%`，当前 `PRD-WORLD_SIMULATOR-038` T4 结论为 `failed`，必须保持 `experimental`；另记录 `scripts/openclaw-parity-p0.sh` 默认 `--openclaw-connect-timeout-ms=3000` 会制造假性 `provider_unreachable`。
- 遗留事项：修复 builtin 基线退化与 OpenClaw parity 默认 timeout 风险后，重跑 `P0` parity。

## 01:40:00 CST / producer_system_designer
- 完成内容：审阅 `PRD-WORLD_SIMULATOR-038` T4 parity 证据并冻结当前放行口径：OpenClaw 保持 `experimental`，不得默认启用。
- 结论：`PRD-WORLD_SIMULATOR-040` 的双轨默认策略不变，但 builtin/OpenClaw 体验等价门禁尚未通过，后续必须先修复基线与 timeout 风险。
- 遗留事项：等待工程侧修复后重跑 `P0`，再决定是否进入下一层 parity。


## 10:59:16 CST / agent_engineer
- 完成内容：在 `crates/oasis7/src/bin/oasis7_provider_parity_bench.rs` 为 builtin parity lane 增加 `P0-001` patrol guardrail，确保 builtin 在 parity 场景下不会执行偏离 phase-1 巡游目标的动作；同时将 bench / `scripts/openclaw-parity-p0.sh` 默认 connect-timeout 统一提升到 `15000ms`。
- 验证：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_provider_parity_bench -- --nocapture` 通过；真实批次 `openclaw_builtin_parity_20260317_fix2` 中 builtin 样本恢复为 `status=passed`、`move_agent=4`。
- 遗留事项：真实 OpenClaw 样本在 `fix2` 中仍出现 `timeout=4`，需继续排查 local_http / runtime agent 请求链路。

## 10:59:16 CST / qa_engineer
- 完成内容：复验 `openclaw_builtin_parity_20260317_fix2`，确认 builtin 基线退化已收口，但 OpenClaw lane 仍为 `wait=4` / `timeout=4`。
- 结论：`PRD-WORLD_SIMULATOR-038` 的正式 T4 结论暂不改写，当前阻断从“builtin 基线 + timeout 风险”收敛为“真实 OpenClaw 连续 timeout”；继续保持 `experimental`。
- 遗留事项：在 OpenClaw timeout 根因修复后，发起新的 builtin/OpenClaw 同批次 parity 结论。

## 10:59:16 CST / producer_system_designer
- 完成内容：确认 `fix2` 后产品口径调整为“builtin parity 基线已恢复，但 OpenClaw 仍未达到可宣称等价的稳定性”，并回写专题状态。
- 结论：默认/放行策略不变；在真实 OpenClaw lane 不再连续 timeout 前，不进入 `P1/P2` 扩面，也不允许对外宣称 parity 达标。
- 遗留事项：等待工程侧给出 OpenClaw timeout 收敛证据，再决定是否发起下一轮 QA/producer 双签。

## 11:06:54 CST / producer_system_designer
- 完成内容：
  - 基于仓库现状完成 WASM 构建链路盘点，确认 oasis7 已具备 deterministic guard、canonical packaging、keyed hash manifest、identity manifest、DistFS hydrate 与 multi-runner 对账等基础设施。
  - 新增 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.prd.md`，明确 oasis7 的 WASM 信任模型采用 binary-first：共识层与 runtime 只认 canonical wasm binary 的 hash 与 identity，源码验证属于社会层。
  - 回写 `doc/world-runtime/prd.md`，新增 `PRD-WORLD_RUNTIME-020/021/022` 与根级验收口径、验证映射。
- 遗留事项：
  - 需要 `wasm_platform_engineer` 继续执行 `WDBP-1 ~ WDBP-4`，把 build recipe / metadata / release evidence / source compile 审计字段进一步统一到代码与门禁。
  - 需要 `qa_engineer` 后续补 deterministic pipeline 专题回归矩阵与失败签名固化。

## 11:08:09 CST / wasm_platform_engineer
- 完成内容：
  - 新增 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.design.md` 与 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md`，把构建入口、canonicalizer、manifest/identity、DistFS、release manifest 与 runtime binary-only 消费串成统一详细设计。
  - 回写 `doc/world-runtime/project.md`、`doc/world-runtime/prd.index.md`、`doc/world-runtime/README.md`，登记 `TASK-WORLD_RUNTIME-041` 及后续 `TASK-WORLD_RUNTIME-042/043/044`。
  - 执行 `./scripts/doc-governance-check.sh`，结果通过：`doc-governance-check: OK`。
- 遗留事项：
  - 当前 canonicalizer 仍是 `strip-custom-sections-v1`，尚不是 semantic-equivalence hash；后续若扩展 canonicalizer，必须显式版本化迁移。
  - `compile_module_artifact_from_source` 虽已复用 deterministic builder，但 metadata/identity 字段仍未与 builtin 发布链路完全对齐，这是后续实现优先级最高的缺口之一。

## 11:25:51 CST / producer_system_designer
- 完成内容：
  - 根据新澄清的目标回收前一版口径：本专题真正要解决的是“不同宿主平台 hash 漂移”，因此不能继续把多平台 keyed 发布 hash 当作长期方案。
  - 将 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.prd.md` 改写为 Docker-first：明确 pinned builder image、single canonical publish hash、CI 只比较容器输出、runtime 只认容器产物。
  - 同步回写 `doc/world-runtime/prd.md` 与 `doc/world-runtime/project.md`，把 `TASK-WORLD_RUNTIME-042/043/044` 调整为 builder image、Docker canonical hash 与 source compile 外移路线。
- 遗留事项：
  - 需要 `wasm_platform_engineer` 下一步定义 builder image 路径、镜像 digest 管理方式与 wrapper script contract。
  - 需要 `runtime_engineer` 评估 `compile_module_artifact_from_source` 外移到 external builder 后的动作协议与治理边界。

## 11:25:51 CST / wasm_platform_engineer
- 完成内容：
  - 将 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.design.md` 修正为 Docker canonical builder 详细设计，明确当前仓库尚无 `Dockerfile`、manifest 迁移目标为单 canonical token `linux-x86_64=<sha256>`、production source compile 不应继续在 runtime 进程内 host 直编。
  - 更新 `doc/world-runtime/README.md`，把专题维护口径改为 `WASM Docker 确定性构建`。
- 遗留事项：
  - 现有 CI 与 manifest 代码仍是 keyed 平台模型，后续实现需要兼容读路径并切换写路径。
  - 当前仓库还没有 builder image 与 wrapper，实现阶段前需要先确定 `docker/wasm-builder/Dockerfile` 与 receipt schema。

## 11:08:00 CST / agent_engineer
- 完成内容：将 `oasis7_provider_parity_bench` 与 `scripts/openclaw-parity-p0.sh` 的默认产物根目录从 `artifacts/` 收口到已忽略的 `output/openclaw_parity/`，并把 `artifacts/` 与 `tools/openclaw/oasis7_openclaw_workspace/.openclaw/` 加入 `.gitignore`，避免本地运行产物持续污染 `git status`。
- 验证：检查 `.gitignore` 与脚本默认值已生效；后续 parity 批次默认落到 `output/openclaw_parity/<run_id>`。
- 遗留事项：历史已生成的 `artifacts/*` 证据路径暂不迁移，避免影响既有文档中的本地证据引用。


## 11:14:00 CST / agent_engineer
- 完成内容：将当前本地 OpenClaw parity 产物目录从仓库根下的 `artifacts/` 迁移到 `output/openclaw_parity/`，并同步回写近期 OpenClaw 相关项目/测试/devlog 文档中的本地证据路径。
- 验证：当前本地批次已位于 `output/openclaw_parity/openclaw_builtin_parity_20260317_fix2` 等目录；仓库内不再残留旧的 OpenClaw parity 本地证据路径引用。
- 遗留事项：更早或未在本机生成的历史证据仍按文档路径约定查找，但后续新批次统一使用 `output/openclaw_parity/`。


## 11:22:00 CST / agent_engineer
- 完成内容：移除 `.gitignore` 中仓库根 `artifacts/` 的兜底忽略，仅保留 `output/` 与 OpenClaw 本地工作目录忽略；当前 parity 默认输出已统一到 `output/openclaw_parity/`。
- 验证：仓库默认脚本/bench 仍写入 `output/openclaw_parity/`，`tools/openclaw/oasis7_openclaw_workspace/.openclaw/` 继续保持忽略。
- 遗留事项：若后续仍有旧入口把运行产物写回仓库根 `artifacts/`，需要逐个迁移入口，而不是再扩大忽略范围。


## 11:49:12 CST / agent_engineer
- 完成内容：定位到 `OpenClaw(Local HTTP)` timeout 根因不在上游模型，而在 `openclaw gateway call agent` 链路；已为 `oasis7_provider_local_bridge` 增加 gateway timeout → `openclaw agent --local` fallback，并用 `sha256(session_key)` 生成稳定 `session_id` 保持会话隔离。
- 验证：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_provider_local_bridge -- --nocapture` 通过；真实批次 `output/openclaw_parity/openclaw_timeout_probe_20260317_fixlocal4` 与 `output/openclaw_parity/openclaw_builtin_parity_20260317_fix3` 已确认 OpenClaw `timeout_count=0`、`move_agent=4`。
- 遗留事项：虽然 timeout 已修复，但 `fix3` 中 builtin/OpenClaw 的 `median_extra_wait_ms` 仍分别约 `9900/13957`，明显高于 `P0` 通过线，下一步需要继续压缩绝对等待时延。

## 11:49:12 CST / qa_engineer
- 完成内容：复验 `openclaw_builtin_parity_20260317_fix3`，确认 builtin/OpenClaw 在 `P0-001` 上都已恢复 `completion_rate=100%`、`timeout_rate=0%`。
- 结论：timeout 与 completion gap 阻断已收口，但 `median/p95 extra wait` 仍显著超线，因此 `PRD-WORLD_SIMULATOR-038` 继续保持 `failed / experimental`，暂不推进 `P1/P2`。
- 遗留事项：等待工程侧继续优化 prompt / 调用链延迟后，再发起新的 T4/T5 结论批次。

## 11:49:12 CST / producer_system_designer
- 完成内容：确认产品口径更新为“OpenClaw 已恢复可行动作与 completion parity，但绝对等待时延仍不达 P0 门槛”。
- 结论：依旧不允许默认启用，也不进入下一层 parity；后续优化目标从“修 timeout”转为“把等待体感拉回 P0 通过线”。
- 遗留事项：等待 latency 收敛证据后，再决定是否重签 `PRD-WORLD_SIMULATOR-038` T4/T5 结论。

## 12:03:18 CST / wasm_platform_engineer
- 完成内容：
  - 新增 `docker/wasm-builder/Dockerfile` 与 `docker/wasm-builder/README.md`，固化 canonical builder image：固定 nightly toolchain、`rust-src`、`wasm32-unknown-unknown` 与内置 `wasm_build_suite`，并约定 canonical container platform 为 `linux-x86_64`（Docker `linux/amd64`）。
  - 重写 `scripts/build-wasm-module.sh`：宿主入口改为 Docker-only wrapper，自动构建默认 builder image、把源码 workspace 挂载到容器内 `/workspace`，并在容器内执行同一条 deterministic build path；不再保留 host-native fallback。
  - 回写 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.prd.md`、`design.md`、`project.md` 与 `doc/world-runtime/project.md`，将 `WDBP-1` / `TASK-WORLD_RUNTIME-042` 标记为完成，并把下一步收口切换到 build receipt、canonical token 和 CI/release evidence 迁移。
- 遗留事项：
  - 当前 builder 仍未输出 `build receipt`；`builder_image_digest/source_hash/build_manifest_hash` 绑定要在下一阶段补进 identity / release evidence。
  - 现有 keyed manifest、CI summary 和 runtime source compile 仍未迁移，后续需要分别由 `wasm_platform_engineer` / `runtime_engineer` 完成 `WDBP-2~4`。

## 13:24:01 CST / producer_system_designer
- 完成内容：回写 `PRD-WORLD_SIMULATOR-038` 的分层 latency gate 文档，包括 parity PRD、场景矩阵、评分卡、聚合模板、专题 project、模块 project 与 T4 结论文档；正式把真实在线 LLM provider 的等待口径拆为“行为等价硬门禁 + rollout latency_class”。
- 结论：`openclaw_builtin_parity_20260317_fix3` 在新口径下记为 `behavior_parity_pass / latency_class B / keep experimental`；允许受限试点，但不得默认启用。
- 遗留事项：继续由 `agent_engineer` / `viewer_engineer` 收敛 OpenClaw absolute wait latency，达到 `latency_class A` 后再重签默认启用与扩面结论。

## 14:16:01 CST / wasm_platform_engineer
- 完成内容：
  - 完成 `WDBP-2` 代码落地：`tools/wasm_build_suite` 现已在 Docker canonical path 下输出 `build receipt`，并把 `source_hash`、`build_manifest_hash`、`builder_image_ref/digest`、`container_platform` 写入 metadata/receipt。
  - 完成 builtin 工件治理迁移：`crates/oasis7_distfs/src/bin/sync_builtin_wasm_identity.rs` 改为 receipt 驱动 identity 生成；`scripts/sync-m1-builtin-wasm-artifacts.sh` 写路径收敛为单 canonical token `linux-x86_64=<sha256>`，同时保留 legacy multi-token 读兼容；`m1/m4/m5` manifest 与 identity 已全部重写。
  - 完成 CI 摘要口径切换的基础改造：`scripts/ci-m1-wasm-summary.sh` 现在显式区分 `host_platform` 与 `canonical_platform`，不同宿主只比较 Docker canonical 输出，不再把 host-native hash 当发布依据。
  - 回写 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md` 与 `doc/world-runtime/project.md`，将专题状态推进到 `WDBP-3 / TASK-WORLD_RUNTIME-043` 的 release evidence 收口阶段。
  - 执行验证：
    - `env -u RUSTC_WRAPPER cargo test --manifest-path tools/wasm_build_suite/Cargo.toml`
    - `env -u RUSTC_WRAPPER cargo check -p oasis7_distfs --bin sync_builtin_wasm_identity`
    - `./scripts/sync-m1-builtin-wasm-artifacts.sh --check`
    - `./scripts/sync-m4-builtin-wasm-artifacts.sh --check`
    - `./scripts/sync-m5-builtin-wasm-artifacts.sh --check`
    - `./scripts/doc-governance-check.sh`
    - `git diff --check`
- 遗留事项：
  - `TASK-WORLD_RUNTIME-043` 还没有把 release evidence / release gate 全量切到 receipt 驱动，当前只完成了 manifest / identity / CI summary 子切片。
  - `TASK-WORLD_RUNTIME-044` 尚未开始，`compile_module_artifact_from_source` 的生产路径仍需外移到 external Docker builder 或在 production 默认禁用。

## 14:35:02 CST / runtime_engineer
- 完成内容：修复 `crates/oasis7/src/bin/oasis7_provider_local_bridge.rs` 与 `crates/oasis7/src/bin/oasis7_provider_parity_bench.rs` 的 Rust 格式问题，执行 `env -u RUSTC_WRAPPER cargo fmt --all` 后确认 CI `fmt` diff 已清零。
- 验证方式：`env -u RUSTC_WRAPPER cargo fmt --all -- --check` 已通过；`CI_VERBOSE=1 ./scripts/ci-tests.sh required` 进入完整 required gate 验证。
- 遗留事项：required gate 未再卡在 `fmt`，但后续被现存 runtime 用例阻断：`runtime::tests::gameplay_protocol::step_with_modules_applies_gameplay_directive_emits_to_domain_events` 与 `runtime::tests::gameplay_protocol::step_with_modules_uses_gameplay_tick_modules_without_fallback` 因 `m5_builtin_modules.identity.json` 缺少 `m5.gameplay.crisis.cycle` 的 hash token 失败，本次未扩修该问题。

## 14:35:02 CST / qa_engineer
- 完成内容：复验本轮 CI 目标问题，确认 `scripts/ci-tests.sh required` 中的 `cargo fmt --all -- --check` 已通过，不再构成阻断项。
- 结论：当前 required gate 的剩余失败签名已收敛为 runtime 测试数据/清单问题，而非格式问题；失败位置为 `crates/oasis7/src/runtime/tests/gameplay_protocol_split_part2.rs:460` 与 `crates/oasis7/src/runtime/tests/gameplay_protocol_split_part2.rs:483`。
- 遗留事项：若要恢复 required gate 全绿，需要由对应 owner 继续处理 builtin identity manifest `m5_builtin_modules.identity.json` 缺 hash token 的问题并回归相关 gameplay protocol 用例。

## 14:38:22 CST / wasm_platform_engineer
- 完成内容：
  - 推进 `WDBP-3 / TASK-WORLD_RUNTIME-043` 的 release evidence / release gate 子切片：runtime `ModuleReleaseSubmitAttestation`、`DomainEvent::ModuleReleaseAttested` 与持久化 state 现已显式携带 `builder_image_digest`、`container_platform`、`canonicalizer_version`，发布 attestation 不再只绑定 `source_hash/build_manifest_hash/wasm_hash`。
  - 收紧 release gate：`ModuleReleaseApply` 现要求阈值内 attestation 的 receipt evidence 完全一致，并要求 attestation 的 `source_hash/build_manifest_hash/wasm_hash` 与 manifest `artifact_identity` 对齐；新增拒绝回归覆盖“builder digest 漂移导致 apply 被阻断”。
  - 扩展 CI 对账证据：`scripts/ci-m1-wasm-summary.sh` 现会导出 `receipt_evidence + identity_build_recipe`，`scripts/ci-verify-m1-wasm-summaries.py` 会跨 runner 校验 receipt/build recipe 一致，而不只比较 wasm hash / identity hash。
  - 回写 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md` 与 `doc/world-runtime/project.md`，把 `TASK-WORLD_RUNTIME-043` 当前阶段明确为“receipt-aware release gate 已落地，剩余 full-tier 跨宿主证据收口”。
  - 执行验证：
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 module_release_ --features test_tier_required -- --nocapture`
    - `./scripts/ci-m1-wasm-summary.sh --module-set m1 --runner-label linux-x86_64 --out <tmp>`
    - `python3 ./scripts/ci-verify-m1-wasm-summaries.py --module-set m1 --summary-dir <tmp> --expected-runners linux-x86_64`
    - `bash -n scripts/ci-m1-wasm-summary.sh`
    - `python3 -m py_compile scripts/ci-verify-m1-wasm-summaries.py`
- 遗留事项：
  - 还没有补真实多宿主（至少 macOS + Linux）full-tier summary 证据，当前只在本机 runner 上验证了新的 receipt-aware summary/verify 路径。
  - `TASK-WORLD_RUNTIME-044` 仍未开始，runtime 内源码编译路径还没有外移或 production 默认禁用。

## 14:43:58 CST / qa_engineer
- 完成内容：复验 GitHub issue `#39` 当前是否仍存在。按原始 real-play 路径执行 `.agents/skills/oasis7/scripts/oasis7-run.sh play --no-open-browser` 后，用默认 headless `agent-browser` 打开 `http://127.0.0.1:4173/?ws=ws://127.0.0.1:5011`，再次命中 `SwiftShader` 与 `copy_deferred_lighting_id_pipeline` fatal，页面保持空白；随后检查发现源码已有 `software_safe`/`render_mode=auto` 逻辑，但当时被服务的 `crates/oasis7_viewer/dist` 产物不含相关标记。使用 `cd crates/oasis7_viewer && env -u NO_COLOR trunk build --dist dist` 重建后，再次打开同一 URL，页面自动跳转到 `software_safe.html?...software_safe_reason=software_renderer_detected`，不再出现黑屏 fatal。
- 验证：1) 原始复现控制台再次出现 `ANGLE ... SwiftShader`、`wgpu error: Validation Error`、`copy_deferred_lighting_id_pipeline`、`CONTEXT_LOST_WEBGL`；2) 重建后 `agent-browser snapshot -i` 可见 `Filter targets`、`Play/Pause/Step x1` 等交互元素，`window.__AW_TEST__.getState()` 返回 `renderMode=software_safe`、`rendererClass=software`、`softwareSafeReason=software_renderer_detected`。
- 结论：issue `#39` 对“当前本地被服务的旧 `dist` 产物”仍可复现；若使用最新源码重建 Viewer Web 产物，则黑屏 crash 已收口为 `software_safe` 自动降级。当前更准确的问题形态是“发布/运行路径仍可能拿到过期 viewer dist，导致 `#39` 表象复发”。
- 遗留事项：建议由 `viewer_engineer` / `qa_engineer` 继续确认 `oasis7_game_launcher` / release bundle / `oasis7` 默认入口是否总是消费最新 Viewer dist，并补一条防止 stale dist 重新放出 `#39` 的门禁验证。

## 14:57:07 CST / wasm_platform_engineer
- 完成内容：
  - 继续推进 `WDBP-3 / TASK-WORLD_RUNTIME-043` 的 release evidence 落盘：`ModuleReleaseManifestMappingState` 新增 release evidence 摘要字段，现会持久化 `release_wasm_hash`、`release_source_hash`、`release_build_manifest_hash`、`release_builder_image_digest`、`release_container_platform`、`release_canonicalizer_version`、`attestation_platforms`、`attestation_proof_cids` 与 `receipt_evidence_conflict`，用于回放与审计。
  - 扩展 runtime 回归断言：`module_release_state_machine_runs_submit_shadow_approve_apply`、attestation persistence / duplicate / receipt mismatch 用例现在会验证 mapping 中的 release evidence 摘要与冲突位，确保发布证据不只存在于事件流和 request.attestations。
  - 更新节点侧验收入口 `scripts/module-release-node-acceptance.sh`：required 步骤新增 `module_release_apply_rejects_when_attestation_receipt_evidence_mismatches`，分诊 grep 也纳入 `attestation receipt evidence mismatch` 失败签名。
  - 执行验证：
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 module_release_ --features test_tier_required -- --nocapture`
    - `./scripts/module-release-node-acceptance.sh --dry-run`
    - `bash -n scripts/module-release-node-acceptance.sh`
- 遗留事项：
  - 还没有补节点侧真实执行版 `module-release-node-acceptance.sh` 产物归档，本轮只验证了 dry-run 命令编排与定向 runtime 套件。
  - 真实跨宿主 full-tier receipt evidence 归档仍未补齐，`TASK-WORLD_RUNTIME-043` 还不能标记完成。

## 15:00:00 CST / wasm_platform_engineer
- 完成内容：
  - 新增 `scripts/wasm-release-evidence-report.sh`，作为 `WDBP-3 / TASK-WORLD_RUNTIME-043` 的多 runner 固定证据入口：可统一收集当前 runner 的 `m1/m4/m5` summary，调用现有 verify 脚本完成 per-module-set 对账，并产出 `summary.md`、`summary.json`、`module_sets.tsv`、verify log 与 per-runner summary json。
  - 回写 `testing-manual.md` 的 CI / S11 章节，把 `wasm-release-evidence-report.sh` 纳入 builtin wasm 多 runner 与去中心化模块发布证据手册；同时补充 receipt mismatch 分诊信号与等价拆分命令。
- 执行验证：
  - `./scripts/wasm-release-evidence-report.sh --expected-runners linux-x86_64`
  - `bash -n scripts/wasm-release-evidence-report.sh`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - 还需要在真实 Linux + macOS runner 上各跑一次该脚本，再把同一 `summary-root` 的双 runner 结果做最终 full-tier 归档。

## 15:21:58 CST / viewer_engineer
- 完成内容：为 source-tree Viewer Web 入口补齐 stale dist freshness gate。`scripts/agent-browser-lib.sh` 现会把 `crates/oasis7_viewer/index.html`、`software_safe.html`、`software_safe.js`、`assets/`、`favicon.ico` 与 `Cargo.lock`/proto 输入纳入 freshness 计算；`.agents/skills/oasis7/scripts/oasis7-run.sh play` 在 repo source 模式下会先解析 fresh Viewer static dir，再把 `--viewer-static-dir <fresh-dir>` 传给 `oasis7_game_launcher`，避免继续直接消费仓库里的旧 `crates/oasis7_viewer/dist`。
- 验证：`bash -n scripts/agent-browser-lib.sh scripts/bundle-freshness-lib.sh .agents/skills/oasis7/scripts/oasis7-run.sh scripts/agent-browser-viewer-dist-freshness-test.sh`、`bash ./scripts/agent-browser-viewer-dist-freshness-test.sh`、`bash ./.agents/skills/oasis7/scripts/oasis7-run-path-test.sh`、`bash ./.agents/skills/oasis7/scripts/oasis7-run-bootstrap-test.sh` 均通过。
- 遗留事项：当前已补 source-tree / bundle freshness scope，但尚未把同类 root-level Viewer 入口漂移回归接到更高层 required gate；若后续再次出现 stale dist 事故，建议由 `qa_engineer` 再补一条更贴近 real-play 的端到端 smoke。

## 15:21:58 CST / qa_engineer
- 完成内容：为 Viewer Web freshness gate 新增针对性回归脚本 `scripts/agent-browser-viewer-dist-freshness-test.sh`，显式覆盖“`software_safe.js` 新于 `dist/index.html` 时必须触发 trunk rebuild”这一失败签名，避免 issue `#39` 因 stale dist 重新表象复发。
- 结论：本轮门禁已把“源码已有 software-safe fallback，但旧 dist 仍被继续服务”的问题从隐性风险收口为可回归检测项。
- 遗留事项：若要把该问题纳入发布前强门禁，可再决定是否把新脚本并入更高层测试入口。

## 15:43:35 CST / agent_engineer
- 完成内容：修复 `oasis7-run.sh download` 在 release bundle 检测失败时仍可能继续执行并把宿主 `/.` 误拷进缓存 `bundle/` 的问题。现在脚本会在解压前校验 `archive_path` 存在，并在 `find_extracted_bundle_dir` 失败、返回空路径、返回非绝对路径或缺少 `run-game.sh` 时立刻非零退出，拒绝创建/填充缓存 bundle 目录。
- 验证：`bash -n .agents/skills/oasis7/scripts/oasis7-run.sh .agents/skills/oasis7/scripts/oasis7-run-download-test.sh`、`bash ./.agents/skills/oasis7/scripts/oasis7-run-download-test.sh`、`bash ./.agents/skills/oasis7/scripts/oasis7-run-path-test.sh`、`bash ./.agents/skills/oasis7/scripts/oasis7-run-bootstrap-test.sh` 均通过；新增的 download 负向回归已覆盖“解压目录缺少 `run-game.sh` 时必须失败且不得生成伪 bundle”路径。
- 遗留事项：后续若要进一步强化 operator 体验，可继续考虑把 bundle 失败签名透出到更高层 launcher/UI，但本轮宿主根目录误拷风险已收口。

## 15:43:35 CST / qa_engineer
- 完成内容：为 `#41` 补充负向回归：`oasis7-run-download-test.sh` 现在会显式模拟“archive 下载成功、解压成功、但 extracted bundle 缺少 `run-game.sh`”的失败路径，并检查脚本必须报错退出、stdout 不得输出 bundle 路径、缓存目录中不得出现 `dev/usr/lib` 等宿主根路径污染。
- 结论：本轮已把 `#41` 从高风险宿主文件系统误操作，收敛为可自动回归检测的 bundle 安全门禁问题。
- 遗留事项：如需把该门禁提升到更高层发布收口，可再评估是否并入更广的脚本组合测试。

## 15:47:32 CST / wasm_platform_engineer
- 完成内容：
  - 完成 `WDBP-4 / TASK-WORLD_RUNTIME-044`：`ReleaseSecurityPolicy` 新增 `allow_runtime_source_compile`，production policy 默认关闭 runtime 内源码编译路径。
  - `CompileModuleArtifactFromSource` 在 production 下现会直接拒绝，并给出“改走 external Docker builder + deploy binary + receipt”的明确失败签名；runtime 热路径不再隐式依赖 Docker daemon。
  - 回写 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.design.md`、`doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md` 与 `doc/world-runtime/project.md`，将 `WDBP-4 / TASK-WORLD_RUNTIME-044` 标记完成。
- 执行验证：
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 compile_module_artifact_from_source_ --features test_tier_required -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 production_policy_ --features test_tier_required -- --nocapture`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - external builder worker 仍未实现；当前方案是 production default-disable，而不是把源码包发布完全迁移到独立构建服务。
  - `TASK-WORLD_RUNTIME-043` 还剩真实 Linux + macOS full-tier release evidence 归档，专题整体仍未完全收口。

## 16:05:11 CST / wasm_platform_engineer
- 完成内容：
  - 继续推进 `TASK-WORLD_RUNTIME-043` 的 CI evidence 收口：`scripts/wasm-release-evidence-report.sh` 新增 `--summary-import-dir`，支持 verify job 直接消费下载下来的 runner summary json，再统一产出 `summary.md/json + logs + module_sets.tsv`。
  - 更新 `.github/workflows/builtin-wasm-m1-multi-runner.yml` 与 `.github/workflows/builtin-wasm-m4-m5-multi-runner.yml`：verify job 不再只做 compare，而是调用 `wasm-release-evidence-report.sh --skip-collect --summary-import-dir ...`，并上传 release evidence report artifact。
  - 回写 `testing-manual.md`、`doc/world-runtime/project.md` 与 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md`，把 CI verify/归档入口与新参数同步到专题口径。
- 执行验证：
  - `./scripts/wasm-release-evidence-report.sh --module-sets m1 --skip-collect --summary-import-dir .tmp/wasm_release_evidence_report/20260317-151847/summaries/m1 --expected-runners linux-x86_64 --out-dir .tmp/wasm_release_evidence_report_import_check`
  - `bash -n scripts/wasm-release-evidence-report.sh`
  - `python3 - <<'PY' ... yaml parse smoke for builtin wasm workflows ... PY`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - workflow 已能自动归档 release evidence report，但真实 `linux-x86_64 + darwin-arm64` 双 runner 的正式 artifact 仍需等待下一次 CI 实跑。

## 16:18:04 CST / wasm_platform_engineer
- 完成内容：
  - 继续收口 CI 入口：删除分散的 `.github/workflows/builtin-wasm-m1-multi-runner.yml` 与 `.github/workflows/builtin-wasm-m4-m5-multi-runner.yml`，新增统一入口 `.github/workflows/wasm-determinism-gate.yml`。
  - 新 workflow 统一覆盖 `(m1|m4|m5) x (linux-x86_64|darwin-arm64)` summary 收集，以及按 `module_set` 下载 summaries、生成并上传 release evidence report artifact。
  - 回写 `testing-manual.md`、`doc/world-runtime/project.md` 与 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md`，把 builtin wasm 的独立 gate 名称收敛为 `wasm-determinism-gate`。
- 执行验证：
  - `python3 - <<'PY' ... yaml parse smoke for .github/workflows/wasm-determinism-gate.yml ... PY`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - `wasm-determinism-gate` 只是统一了 CI 入口；真正的 `linux-x86_64 + darwin-arm64` 正式 evidence artifact 仍需等待 GitHub Actions 下一次实跑生成。

## 19:45:20 CST / wasm_platform_engineer
- 完成内容：
  - 复盘作者分支上的 `wasm-determinism-gate`：确认 `scripts/build-wasm-module.sh` 已切为 Docker-only path 后，workflow 里的 `macos-14` summary job 会在 `require_docker` 阶段失败，因为 GitHub-hosted macOS runner 不提供 Docker daemon。
  - 修正 `.github/workflows/wasm-determinism-gate.yml`：先移除 GitHub-hosted `macos-14` matrix，仅保留 `(m1|m4|m5) x ubuntu-24.04/linux-x86_64` 的稳定 gate，并把 verify job 的 `--expected-runners` 收敛为 `linux-x86_64`。
  - 回写 `testing-manual.md`、`doc/world-runtime/project.md` 与 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md`：明确当前 GitHub-hosted gate 为 Linux-only，跨宿主 full-tier 证据改为后续导入 Docker-capable macOS summary 完成。
- 执行验证：
  - `python3 - <<'PY' ... yaml parse smoke for .github/workflows/wasm-determinism-gate.yml ... PY`
  - `bash -n scripts/wasm-release-evidence-report.sh scripts/ci-m1-wasm-summary.sh scripts/build-wasm-module.sh`
  - `python3 -m py_compile scripts/ci-verify-m1-wasm-summaries.py`
  - `./scripts/wasm-release-evidence-report.sh --module-sets m1 --dry-run --out-dir /tmp/pr43-wasm-evidence-dryrun`
  - `git diff --check`
- 遗留事项：
  - `WDBP-3 / TASK-WORLD_RUNTIME-043` 仍未完成；需要补一条带 Docker daemon 的 macOS runner 或外部构建节点，把 `darwin-arm64` summary 导入现有 report 流程后再做跨宿主 full-tier evidence 归档。

## 20:34:06 CST / wasm_platform_engineer
- 完成内容：
  - 根据 PR CI 新暴露的失败链路继续排查 builtin wasm materialize：确认 `crates/oasis7/src/runtime/builtin_wasm_materializer.rs` 的 fallback 临时输出目录原先落在系统 `/tmp`，与新版 `scripts/build-wasm-module.sh` 的“`manifest-path` / `out-dir` 必须位于同一 workspace root 下”约束冲突，导致 `scripts/build-builtin-wasm-modules.sh` 在 CI 中以 `exit status: 2` 失败。
  - 修正 fallback 输出目录：`temp_build_dir()` 现改为仓库内 `.tmp/`，并补充定向单测，保证 builtin wasm fallback 不再把 `--out-dir` 指到 workspace 外。
  - 同步修复 `crates/oasis7/src/runtime/tests/builtin_wasm_identity.rs` 中仍指向旧 builtin 清单的硬编码 hash，并为 `crates/oasis7/src/viewer/runtime_live/tests.rs` 的 `runtime_prompt_control_apply_updates_snapshot_and_bindings` 补上 LLM/OpenClaw env lock，避免并行测试环境串扰。
  - 为 Docker canonical builder 增加稳定 `builder_image_digest` 默认值，避免 CI 每次本地 build 出来的 image id 漂移导致 receipt/build manifest 与已跟踪 identity manifest 不一致。
- 执行验证：
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 builtin_wasm_identity -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 temp_build_dir_stays_under_repo_tmp_root -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 runtime_prompt_control_apply_updates_snapshot_and_bindings -- --nocapture`
- 遗留事项：
  - `wasm-determinism-gate` 的 `m1/m5` summary 失败日志仍待 workflow `23193048908` 完整结束后回收；若后续仍有红灯，需要根据最终日志继续收敛 build receipt / identity manifest 对账链。

## 21:41:28 CST / wasm_platform_engineer
- 完成内容：
  - 针对“WASM 打包已切到 Docker，但仓库仍有旧口径残留”的收口需求，复核现状后确认代码入口与 artifact manifest 已切到 Docker canonical / `linux-x86_64` 单 token，残留主要在 CI required-check 默认上下文与 testing / scripts / p2p 文档叙事。
  - 更新 `scripts/ci-ensure-required-checks.py`：默认 required checks 由旧 `Builtin Wasm * Multi Runner / verify-*` 切换为 `Wasm Determinism Gate / verify-wasm-determinism (m1|m4|m5)`。
  - 回写 `doc/testing/prd.md`、`doc/testing/prd.index.md`、`doc/testing/project.md` 与三个 testing CI 专题 PRD / project，把现行口径统一为 `.github/workflows/wasm-determinism-gate.yml`、Docker canonical summary / release evidence、GitHub-hosted Linux gate + 外部 Docker-capable macOS full-tier 补证。
  - 回写 `doc/scripts/wasm/builtin-wasm-nightly-build-std.prd.md` 与 `doc/p2p/consensus/builtin-wasm-identity-consensus.prd.md`，移除“明确不采用容器化 / 不依赖 Docker”这类与现行发布口径冲突的表述，并改为指向 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.prd.md`。
- 执行验证：
  - `python3 -m py_compile scripts/ci-ensure-required-checks.py`
  - `rg -n "Wasm Determinism Gate / verify-wasm-determinism|wasm-determinism-gate|Docker canonical build|single canonical token|Docker canonical gate" doc/testing doc/world-runtime doc/scripts doc/p2p scripts/ci-ensure-required-checks.py --glob '!doc/devlog/**'`
- 遗留事项：
  - 非现行入口的 review / audit 历史记录仍会保留旧文件名与历史路径，这是追溯记录，不作为当前工程口径。
  - `TASK-WORLD_RUNTIME-043` 的真实跨宿主 full-tier evidence 仍待外部 Docker-capable macOS summary 补齐，本轮未改变该阻塞。

## 23:18:12 CST / wasm_platform_engineer
- 完成内容：
  - 继续收口 builtin wasm Docker 化后的历史命名残留：新增当前活跃专题文档路径 `ci-builtin-wasm-determinism-gate-m1.*`、`ci-builtin-wasm-docker-canonical-gate.*`、`ci-builtin-wasm-determinism-gate-required-check-protection.*`。
  - 将旧 `ci-builtin-wasm-m1-multi-runner.*`、`ci-builtin-wasm-m4-m5-hash-drift-hardening.*`、`ci-m1-multi-runner-required-check-protection.*` 路径降级为原地归档提示，保留历史追溯但不再作为活跃入口。
  - 回写 `doc/testing/prd.index.md`、`doc/testing/prd.md`、`doc/testing/project.md`，把 testing 模块的活跃入口统一切到新路径集合。
- 执行验证：
  - `rg -n "ci-builtin-wasm-m1-multi-runner|ci-m1-multi-runner-required-check-protection|ci-builtin-wasm-m4-m5-hash-drift-hardening" doc/testing doc/world-runtime doc/scripts doc/p2p testing-manual.md README.md AGENTS.md --glob '!doc/devlog/**' --glob '!doc/core/reviews/**' --glob '!doc/engineering/prd-review/**'`
- 遗留事项：
  - `doc/core/reviews/**`、`doc/engineering/prd-review/**` 与 `doc/devlog/**` 仍保留旧文件名，这是历史审计记录，本轮不改写。

## 23:34:55 CST / wasm_platform_engineer
- 完成内容：
  - 继续清理历史审计与评审残留：回写 `doc/engineering/prd-review/checklists/active-testing.md`、`doc/core/reviews/round-001/003/004/006/007/008-*` 与 `doc/site/github-pages/github-pages-release-download-pipeline-2026-03-01.project.md`，将 review/checklist/索引中的旧专题名统一切到新活跃入口。
  - 保留历史结论与时间戳，只替换当前应引用的专题路径与 gate 名称，避免后续治理检索被旧命名噪音干扰。
- 执行验证：
  - `rg -n "ci-builtin-wasm-m1-multi-runner|ci-m1-multi-runner-required-check-protection|ci-builtin-wasm-m4-m5-hash-drift-hardening|Builtin Wasm m1 Multi Runner|verify-m1-multi-runner-summary|builtin-wasm-m1-multi-runner\\.yml|builtin-wasm-m4-m5-multi-runner\\.yml" doc/core/reviews doc/engineering/prd-review doc/site --glob '!doc/devlog/**'`
- 遗留事项：
  - 旧专题名仍会保留在 `doc/testing/ci/ci-*-历史文件名归档提示` 与 `doc/devlog/**` 中，这是有意保留的追溯层，不再视为活跃残留。

## 23:56:40 CST / viewer_engineer
- 完成内容：
  - 复盘 `v0.0.32` 的 `release-gate-web` 失败，定位到 `scripts/viewer-release-qa-loop.sh` 的截图阶段并非 Viewer 语义失败，而是 `agent-browser 0.17.1` 会把以 `.tmp/...` 开头的相对截图路径误判成 CSS selector，导致 zoom/screenshot artifact 全量失败。
  - 修正 `scripts/agent-browser-lib.sh`：`ab_screenshot` 现统一把输出路径解析为绝对路径并确保父目录存在，避免 `agent-browser screenshot` 再把点号前缀路径当成 selector。
  - 回写 `scripts/run-game-test-ab.sh`：把直接 `ab_cmd ... screenshot` 调用统一收敛到 `ab_screenshot`，避免同类回归在其它 web 闭环脚本复发。
- 执行验证：
  - `bash -n scripts/agent-browser-lib.sh scripts/run-game-test-ab.sh scripts/viewer-release-qa-loop.sh`
  - `./scripts/viewer-release-qa-loop.sh --scenario llm_bootstrap --out-dir .tmp/local-web-strict-fixed --headed`
- 遗留事项：
  - `v0.0.32` 对应的 GitHub release run `23199003783` 已结束且结论为 `cancelled`，需要基于本次修复提交后重新打新 tag 触发下一轮 release。

## 23:58:40 CST / viewer_engineer
- 完成内容：
  - 复盘 `v0.0.33` 的 `release-gate-web` 新失败：截图路径问题修复后，CI 进入 `oasis7 Viewer (Software-Safe)`，`renderMode=software_safe` / `softwareSafeReason=software_renderer_detected`，而 `viewer-release-qa-loop.sh` 仍沿用标准 Viewer 的 `mode=3d;focus=...;zoom=...` DSL，导致 `runSteps` 返回 `payload must be non-empty step string or count`，随后把不支持的 zoom 检查误判成发布阻断。
  - 修正 `scripts/viewer-release-qa-loop.sh`：新增 `renderMode` / `softwareSafeReason` 识别；当命中 `software_safe` 时，改用最小闭环校验（连接、选择目标、`runSteps(4)` 推进 tick、主截图），并将 zoom gate 明确标记为 `skipped (software_safe: ...)`；标准 `renderMode=standard` 仍保持原 3D zoom gate。
  - 回写 summary 字段，把 `Render mode` 与 `Software-safe reason` 直接输出到 QA 摘要，便于后续 release 证据直接区分是标准 3D 还是 software-safe 兜底链路。
- 执行验证：
  - `bash -n scripts/viewer-release-qa-loop.sh scripts/agent-browser-lib.sh`
  - `./scripts/viewer-release-qa-loop.sh --scenario llm_bootstrap --out-dir .tmp/local-web-strict-standard-postsafe --headed`
  - `AGENT_BROWSER_ARGS='--use-angle=swiftshader-webgl,--ignore-gpu-blocklist' ./scripts/viewer-release-qa-loop.sh --scenario llm_bootstrap --out-dir .tmp/local-web-strict-softsafe --headed --skip-visual-baseline`
- 遗留事项：
  - 需要基于本次修复重新提交并打新 tag，重新触发 release；当前 `v0.0.33` run `23200596552` 中的 `release-gate-web` 失败属于修复前产物，不再作为最新结论。
