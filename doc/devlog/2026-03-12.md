# 2026-03-12 Devlog

## 00:26:51 CST / producer_system_designer
- 完成内容：延续昨夜制作人试玩，继续以 GUI Agent 复核 launcher 链模式；确认首个失败根因之一为本地 bundle 混版（旧 `oasis7_chain_runtime` 不支持 `--storage-profile`），重建 `output/release/game-launcher-local` 后消除该阻断。进一步验证发现：默认 `chain_node_id=viewer-live-node` 命中旧 `execution_world_dir` 时会触发 `DistributedValidationFailed/latest state root mismatch`，导致链状态退化为 `unreachable`；改用 fresh `chain_node_id=viewer-live-node-fresh-20260312` 后，链可进入 `ready`，`query_explorer_overview` 成功，随后 `start_game` 也可通过。同步新增 launcher 专题三件套并回写 `doc/world-simulator/prd.md`、`doc/world-simulator/project.md` 与 `doc/world-simulator/prd.index.md`，将该问题登记为 `PRD-WORLD_SIMULATOR-034` / `TASK-WORLD_SIMULATOR-103/104`。
- 遗留事项：由 `viewer_engineer` 牵头实现 stale execution world 的结构化识别与 fresh node id 恢复链路；`runtime_engineer` 联审恢复边界，避免破坏历史执行世界数据。

## 09:45:00 CST / viewer_engineer
- 完成内容：完成 `TASK-WORLD_SIMULATOR-104` 实现闭环：`oasis7_web_launcher` 新增 stale execution world 错误识别，命中 `DistributedValidationFailed + latest state root mismatch` 时将链状态提升为 `stale_execution_world`，并在 `/api/state`、`/api/chain/start`、GUI Agent 状态/动作返回中暴露结构化恢复建议（含 `fresh_node_id`、`fresh_chain_status_bind`、`suggested_config`）；新增 GUI Agent `recover_chain` 动作；`oasis7_client_launcher` 同步接入 stale 状态映射、恢复提示与“一键使用 fresh node 恢复”CTA。补齐定向回归测试，验证 stale 状态序列化、GUI Agent 能力暴露以及客户端恢复快照映射。
- 遗留事项：尚未补做新的 GUI Agent 实机截图与 `query_explorer_overview -> start_game` 现场证据；如需正式验收，可在当前实现上继续跑一轮 Web/GUI Agent 闭环复测并补图。

## 10:00:19 CST / qa_engineer
- 完成内容：使用最新本地 `oasis7_web_launcher` + GUI Agent + `agent-browser` 完成一轮真实闭环复验。控制面监听 `127.0.0.1:5413`，默认 `viewer-live-node` 启链后稳定复现 `stale_execution_world`，结构化返回包含 `fresh_node_id=viewer-live-node-fresh-1773280564145` 与 `fresh_chain_status_bind=127.0.0.1:5123`；随后 `recover_chain -> query_explorer_overview -> start_game -> agent-browser 打开 launcher/viewer 页面 -> stop_game -> stop_chain` 全链路成功。现场证据已保存到 `output/manual-playtest/2026-03-12/launcher-console-ready-5413.png`、`output/manual-playtest/2026-03-12/viewer-running-4173.png` 及对应 JSON/快照文件。
- 遗留事项：`start_game` 后日志中仍出现一次 `[game stderr] viewer web bridge error: WebSocket(Protocol(HandshakeIncomplete))`，本次未阻断 Viewer 页面加载与闭环通过，但建议后续单独评估是否为探针/浏览器握手噪音。

## 10:14:00 CST / producer_system_designer
- 完成内容：继续以制作人视角亲自试玩最新提交版本。通过 `oasis7_web_launcher` + GUI Agent 在 `127.0.0.1:5414` 上复现默认链 stale world，并完成 `recover_chain -> start_game`。进入 Web Viewer 后，发现 `agent-browser` 环境下页面标题与钩子正常，但运行在 SwiftShader/WebGL 软件渲染时触发 `wgpu` shader validation panic，浏览器控制台报 `copy_deferred_lighting_id_pipeline` 相关致命错误，导致 Viewer 一直停留在 `connectionStatus=connecting`，无法真正推进 `play/step` 控制；随后按手册切换到 native fallback，执行 `scripts/capture-viewer-frame.sh --scenario llm_bootstrap --addr 127.0.0.1:5131 --auto-focus-target first_agent --automation-steps 'mode=3d;focus=first_agent;zoom=0.8;select=first_agent'`，抓帧状态显示 `connection_status=connected`、`selection_kind=agent`、`selection_id=agent-0`、`snapshot_ready=1`，确认 native 链路下至少能进入场景并聚焦到 agent。
- 遗留事项：当前真正阻断“制作人能在 Web 里继续玩”的不是 stale recovery，而是 Web Viewer 在软件渲染环境的 shader/pipeline 崩溃；建议新开专题，把 `agent-browser` / SwiftShader 下的 Web Viewer 可运行性作为高优先级试玩阻断处理。

## 11:12:00 CST / viewer_engineer
- 完成内容：继续追 producer Web 试玩阻断，补齐 `PRD-WORLD_SIMULATOR-035` / `TASK-WORLD_SIMULATOR-105/106`：1) `crates/oasis7_viewer/src/web_test_api.rs` 新增浏览器 runtime fatal 透出（`window.error` / `unhandledrejection` / 已知渲染 fatal `console.error` / `webglcontextlost`），将致命错误汇总到 `window.__AW_TEST__.getState().lastError`，避免页面长期假停在 `connecting`；2) `scripts/run-game-test-ab.sh` 增加 `lastError` 快失败分支；3) `doc/world-simulator/viewer/viewer-manual.md`、`testing-manual.md` 与模块主文档/索引同步补齐 `test_api=1`、software renderer / SwiftShader 判定与 native fallback 口径。随后做两轮实机分流验证：A. 历史 `4174/5012` 旧进程在当前 headed `agent-browser` 下已不再是 SwiftShader，而是硬件 `ANGLE (Intel/Mesa)`；其失败根因为 `decode error: unknown variant authoritative_batch`，对应旧静态资源/协议漂移。B. 新起 `oasis7_viewer_live --no-llm` + `run-viewer-web.sh`（`127.0.0.1:4176/?ws=ws://127.0.0.1:5014&test_api=1`）后，`agent-browser` 可稳定连上，`sendControl("play")` 与 `sendControl("step", {count:8})` 均返回 `accepted=true`，最终 `getState()` 达到 `connectionStatus=connected`、`logicalTime=17`、`eventSeq=23`、`cameraMode=3d`、`lastError=null`，并产出 `output/playwright/playability/manual-20260312-direct/final.png`。
- 遗留事项：`oasis7_game_launcher` 一键链模式仍可能因旧 `viewer-live-node` execution world 或历史 bundle 混版直接退出；当前“agent-browser 能不能玩”的答案已经明确：在 fresh Web build + direct viewer 链路下可以正常玩，之前卡死不是 agent-browser 误用，而是环境边界（SwiftShader/software renderer）与历史静态资源/链状态混杂导致的复合阻断，后续若要收敛成单命令稳定试玩，还需继续治理 launcher 启动栈的一致性。

## 11:24:30 CST / viewer_engineer
- 完成内容：继续收敛一键试玩栈。确认 `run-game-test.sh` 之前没有把 `--chain-node-id/--chain-status-bind/--chain-disable` 透传给 `oasis7_game_launcher`，导致脚本虽会 fresh build Web 包，但 chain-enabled 场景仍可能复用固定 `viewer-live-node` 命中旧 execution world。已为 `scripts/run-game-test.sh` 增加 fresh `chain_node_id` 默认值（`viewer-live-node-playtest-<RUN_ID>`）、显式 `chain_status_bind` 透传与 `--chain-disable/--chain-node-id/--chain-status-bind` 参数支持，并回写 `PRD-WORLD_SIMULATOR-034` / `TASK-WORLD_SIMULATOR-107/108`。同时修复 `scripts/run-game-test-ab.sh` 在 `set -e` 下对 `wait_for_connected` 结果赋值导致的静默提前退出。随后执行 `./scripts/run-game-test-ab.sh --no-llm` 完成 chain-enabled 实机闭环：产物位于 `output/playwright/playability/20260312-112146/`，`run-game-test.log` 显示 fresh `chain_node_id=viewer-live-node-playtest-20260312-112146`；`ab_metrics.json` 显示 `connectionStatus=connected`、`lastError=null`、`tick 1 -> 21`、`eventSeq 0 -> 27`、`TTFC=18ms`、有效控制命中率 `3/3=100%`，A/B 两阶段均 PASS，证明 stale execution world 已不再阻断 chain-enabled 一键试玩栈。
- 遗留事项：`agent-browser record start` 仍会短暂打断连接，当前脚本已通过 stop/reopen 自动恢复并继续完成闭环，但若后续要把录屏过程也收敛到完全无重连，需要单独排查 `agent-browser` 录屏启动对当前 Viewer WebSocket 会话的影响。

## 11:55:00 CST / qa_engineer
- 完成内容：继续收敛 `agent-browser` 录屏边界。通过稳定直连链路（`oasis7_viewer_live --no-llm` + `run-viewer-web.sh`）验证 `agent-browser record start` 的真实语义是“创建 fresh browser context”；对 Viewer Web 来说，这会把页面重置回冷启动态，并且在当前环境下不会自行恢复连接（25 秒内始终停留 `connectionStatus=connecting`、`tick=0`、`lastError=null`）。进一步确认：为了恢复连接而执行的 `close/open` 也会同时结束该次录屏，因此当前 `playthrough.webm` 只能作为 best-effort pre-sync 片段，不能被当作完整 gameplay 录像。已据此把 `scripts/run-game-test-ab.sh` 改为：录屏一启动就立即 reopen 页面同步会话，不再误等 15 秒，也不再在流程末尾误报“无录屏在进行”；同时输出 `recording_status.txt` 明确说明 `playthrough.webm` 的证据边界。复验 `./scripts/run-game-test-ab.sh --no-llm`（`output/playwright/playability/20260312-115337/`）通过，A/B 两阶段仍为 PASS，`TTFC=19ms`、`tick 1 -> 21`、`eventSeq 0 -> 27`、`lastError=null`。
- 遗留事项：若发布/评审必须要求“不中断的完整视频”，当前需要切换到 `agent-browser` 之外的屏幕录制方案；就现有工具边界而言，截图、`__AW_TEST__` 状态与量化指标仍应作为主证据，`playthrough.webm` 只能作为辅助片段。

## 13:45:00 CST / producer_system_designer
- 完成内容：将启动器试玩入口正式收敛为 bundle-first 口径：新增 `doc/testing/launcher/launcher-bundle-first-playtest-entry-2026-03-12.prd.md` / `.design.md` / `.project.md`，并同步 `testing-manual.md`、`doc/testing/launcher/launcher-manual-test-checklist-2026-03-10.prd.md`、`doc/testing/README.md`、`doc/testing/prd.index.md`、`doc/testing/project.md`。同时为 `scripts/run-game-test.sh` 增加 `--bundle-dir`，使现有 `run-game-test-ab.sh` 可直接透传 bundle 产物入口，不再把“源码态 bootstrap”和“制作人/发布前验收入口”混为一谈。
- 验证记录：`bash -n scripts/run-game-test.sh scripts/run-game-test-ab.sh`、`./scripts/run-game-test.sh --help`、`./scripts/run-game-test-ab.sh --help` 通过；已构建三组对照 bundle：`output/release/game-launcher-bundle-first-20260312`（release fresh web）、`output/release/game-launcher-bundle-first-dev-20260312`（dev fresh web）、`output/release/game-launcher-bundle-first-distreuse-20260312`（dev + 复用 `crates/oasis7_viewer/dist`），并分别执行 `env -u NO_COLOR ./scripts/run-game-test-ab.sh --bundle-dir <bundle> --no-llm --headless`。三组产物均成功走到新 bundle 入口并输出 `Mode: bundle` / `Launcher: <bundle>/run-game.sh`，证据位于 `output/playwright/playability/20260312-132647/`、`output/playwright/playability/20260312-133426/`、`output/playwright/playability/20260312-133853/`；但浏览器内 `window.__AW_TEST__.getState()` 一直停在 `connectionStatus=connecting`、`tick=0`、`lastError=null`，source 模式最新复验 `output/playwright/playability/20260312-134013/` 也复现同一阻断。
- 遗留事项：当前“bundle-first 入口已具备”与“fresh Web 闭环可实际进入游戏”是两件事；前者已完成，后者仍被 fresh/fresh-like Web 构建链路阻断，需由 `viewer_engineer` 继续排查当前 trunk / dist 产物为何在 agent-browser 下持续停在 `connecting`。

## 14:12:00 CST / qa_engineer
- 完成内容：继续追查 bundle/source fresh Web 在 `agent-browser` 下长期停在 `connectionStatus=connecting` 的阻断。通过同一套 `run-game-test-ab.sh` 分别执行 headed 与 headless 对照，确认根因不是 fresh Web 构建，也不是 bundle 入口本身，而是 `headless` 浏览器环境退化到 `SwiftShader` software renderer。实测：`./scripts/run-game-test-ab.sh --no-llm`（headed）与 `./scripts/run-game-test-ab.sh --bundle-dir output/release/game-launcher-bundle-first-20260312 --no-llm` 均通过，产物分别为 `output/playwright/playability/20260312-140409/`、`output/playwright/playability/20260312-140918/`；对应 `browser_env.json` 显示 renderer 为 `ANGLE (Intel, Mesa Intel(R) Iris(R) Xe Graphics (RPL-P), OpenGL 4.6)` 且 `connectionStatus=connected`。而 `./scripts/run-game-test-ab.sh --no-llm --headless` 则稳定命中 `output/playwright/playability/20260312-140856/browser_env.json`，renderer 为 `ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero) (0x0000C0DE)), SwiftShader driver)`，状态停在 `connectionStatus=connecting`、`tick=0`。已据此为 `scripts/run-game-test-ab.sh` 增加环境 guardrail：命中 `headless + SwiftShader` 时立即快失败并落 `browser_env.json`，不再把环境阻断误记成 fresh Web 构建或玩法回归。
- 遗留事项：当前默认建议仍应保持 Viewer Web 使用 headed 模式；若后续要正式支持 headless 门禁，需要单独确认 CI/agent-browser 是否能提供硬件加速或非 SwiftShader 的 headless WebGL 环境。

## 14:24:00 CST / producer_system_designer
- 完成内容：继续把制作人 bundle-first 试玩流程收短为单命令入口。新增 `scripts/run-producer-playtest.sh`：默认复用或自动构建 `output/release/game-launcher-producer-local`，随后直接执行 `run-game-test.sh --bundle-dir <bundle>`，避免再手动记忆“先 build bundle、再传 `--bundle-dir`”两步。同步更新 `doc/testing/launcher/launcher-bundle-first-playtest-entry-2026-03-12.prd.md` / `.design.md` / `.project.md`、`testing-manual.md`、`doc/testing/launcher/launcher-manual-test-checklist-2026-03-10.prd.md`、`doc/testing/project.md`，将 `./scripts/run-producer-playtest.sh` 收敛为制作人试玩默认入口。
- 验证记录：`bash -n scripts/run-producer-playtest.sh`、`./scripts/run-producer-playtest.sh --help` 通过；执行 `./scripts/build-game-launcher-bundle.sh --profile dev --out-dir output/release/game-launcher-producer-local` 预热默认 bundle；随后 `timeout 20s ./scripts/run-producer-playtest.sh --profile dev --no-llm` 成功复用现成 bundle，并输出 `Mode: bundle`、`Launcher: /home/scc/oasis7/output/release/game-launcher-producer-local/run-game.sh` 与可访问 URL，证明制作人现在可用单命令进入 bundle-first 试玩栈。
- 遗留事项：当前单命令入口只负责“准备 bundle + 起栈”，不替代后续 headed `agent-browser` 实际游玩或 `run-game-test-ab.sh` 自动化采样；若后续要再缩一步，可考虑补一条“起栈后自动打开 headed 浏览器”的可选模式。

## 14:36:00 CST / producer_system_designer
- 完成内容：继续把制作人试玩路径再缩一步，为 `scripts/run-producer-playtest.sh` 增加 `--open-headed`、`--session` 与 `--startup-timeout`。开启 `--open-headed` 后，脚本不再直接 `exec run-game-test.sh`，而是后台起 bundle 栈、从启动日志提取 URL，再自动执行 `agent-browser --headed open <url>`；这样制作人可以一条命令同时完成“准备 bundle + 起栈 + 打开 headed 浏览器”。同步更新 `testing-manual.md`、`doc/testing/launcher/launcher-manual-test-checklist-2026-03-10.prd.md`、`doc/testing/launcher/launcher-bundle-first-playtest-entry-2026-03-12.*` 与 `doc/testing/project.md`。
- 验证记录：`bash -n scripts/run-producer-playtest.sh`、`./scripts/run-producer-playtest.sh --help` 通过；执行 `timeout 20s ./scripts/run-producer-playtest.sh --profile dev --no-llm --open-headed --session producer-open-headed-test` 成功复用本地 bundle，自动输出 headed 浏览器 session 与启动日志路径，并完成 URL 提取与浏览器打开。
- 遗留事项：当前 `--open-headed` 只负责打开浏览器，不自动执行页面内的玩法操作；若后续要进一步缩短到“自动打开并切到 3D / 自动点 play”，应单独设计制作人试玩宏，不与当前起栈脚本混在一起。

## 14:55:00 CST / producer_system_designer
- 完成内容：处理制作人单命令试玩的收尾缺口。先清理当前残留的 `agent-browser` 会话，随后为 `scripts/run-producer-playtest.sh --open-headed` 增加退出自动 `agent-browser close` 的 cleanup；这样脚本在 `Ctrl+C`、超时或异常退出后，会主动关闭自己拉起的 headed 浏览器，不再留下残余窗口。同步更新 `testing-manual.md`、`doc/testing/launcher/launcher-manual-test-checklist-2026-03-10.prd.md`、`doc/testing/launcher/launcher-bundle-first-playtest-entry-2026-03-12.*` 与 `doc/testing/project.md`。
- 验证记录：`agent-browser session list` 清理前确认存在多组残留会话，逐一执行 `agent-browser --session <name> close` 后已归零；随后执行 `bash -n scripts/run-producer-playtest.sh`、`./scripts/run-producer-playtest.sh --help`，并通过后台启动 + `TERM` 收尾的方式复验 `./scripts/run-producer-playtest.sh --profile dev --no-llm --open-headed --session producer-auto-close-test`，确认脚本可起 bundle 栈、自动打开 headed 浏览器，且退出后不再残留对应 headed 浏览器进程/窗口。
- 遗留事项：当前脚本只负责“自动开窗 + 自动关窗”，不负责页面内进一步玩法点击；若后续要做制作人一键宏，应独立设计页面动作脚本。

## 15:29:33 CST / producer_system_designer
- 完成内容：按“制作人亲自玩”要求，实际执行一轮 bundle-first / launcher GUI Agent 闭环试玩。先尝试 `./scripts/run-game-test.sh --bundle-dir output/release/game-launcher-producer-local --no-llm` 直进 Viewer，观察到页面长期停在 `connectionStatus=connecting`；随后切换到真实产品路径 `output/release/game-launcher-producer-local/run-web-launcher.sh --listen-bind 127.0.0.1:5411`，通过 `/api/gui-agent/capabilities|state|action` 读取能力、用 `start_game` 覆盖 `chain_enabled=false` 启动游戏，再用 `agent-browser` 打开 Viewer，完成 `play -> 点击画面中心选中 agent-0 -> step(10) -> stop_game` 最小可玩闭环。补充产出 `doc/playability_test_result/card_2026_03_12_15_29_33.md`、`output/playwright/playability/20260312-producer-play/`、`output/manual-playtest/gui-agent-launcher-5411-20260312.png`。
- 验证记录：`window.__AW_TEST__.getState()` 最终达到 `connectionStatus=connected`、`logicalTime=57`、`eventSeq=70`、`selectedKind=agent`、`selectedId=agent-0`；`sendControl("play", {})` 与 `sendControl("step", {count:10})` 均返回 `accepted=true` 并完成推进；录屏与 trace 已落 `output/playwright/playability/20260312-producer-play/playthrough.webm`、`output/playwright/playability/20260312-producer-play/trace.zip`。
- 遗留事项：默认链启用路径仍会命中 `stale_execution_world`，当前只能靠 `recover_chain` 或 `chain_enabled=false` 绕过；此外 Viewer 首连仍有 `ERR_CONNECTION_RESET/ERR_CONNECTION_REFUSED` 与两条 `closure invoked recursively or after being dropped` 噪音，虽未阻断本轮微循环，但仍需继续收口正式体验。

## 16:08:00 CST / viewer_engineer
- 完成内容：继续收口“默认链入口仍命中 stale execution world”问题。将 `oasis7_web_launcher`、`oasis7_client_launcher` 与 `oasis7_game_launcher` 的默认链配置从固定 `viewer-live-node` 改为自动生成 fresh `chain_node_id`（`viewer-live-node-fresh-<pid>-<unix_ms>`），同时保持用户显式输入的 `chain_node_id` 不变；`self_guided` 演示模式安全默认值也同步改为 fresh node。补齐 `oasis7_web_launcher`、`oasis7_game_launcher`、`oasis7_client_launcher` 三组默认值测试，锁定“默认 fresh node id”行为。
- 验证记录：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher -- --nocapture` 全通过；随后重建 `output/release/game-launcher-producer-local` 并以 `output/release/game-launcher-producer-local/run-web-launcher.sh --listen-bind 127.0.0.1:5415` 实机复验，`/api/gui-agent/state` 默认返回 `chain_node_id=viewer-live-node-fresh-949052-1773306492552`，按默认配置直接执行 `start_chain -> query_explorer_overview` 成功进入 `chain_status=ready`，不再命中旧 `viewer-live-node` 的 stale execution world。
- 遗留事项：默认链入口的 stale 目录冲突已从“高频默认阻断”降为“仅在用户显式指定旧 node id 时可能复现”；后续仍需继续观察 Viewer 首连的短暂 WS reset/refused 噪音，但它已不再与 stale execution world 混在同一默认入口问题里。

## 17:23:30 CST / viewer_engineer
- 完成内容：专项追查 Viewer 首连噪音根因，区分“真实默认入口阻断”与“会话/录屏时序噪音”。对照复验表明：1) 在 fresh 会话、无录屏条件下，直接打开 `http://127.0.0.1:4173/?ws=ws://127.0.0.1:5011&test_api=1`，Viewer 可在约 6 秒内从 `connecting` 进入 `connected`，控制台与页面错误均为空；2) `viewer web bridge error: WebSocket(Protocol(HandshakeIncomplete))` 仍会在 launcher 停止或浏览器中断早期握手时写入 `oasis7_viewer_live` 日志，但它更像 WebSocket 握手被半途取消的服务端噪音，而不是稳定玩法阻断；3) `ERR_CONNECTION_RESET/ERR_CONNECTION_REFUSED` 与两条 `closure invoked recursively or after being dropped` 未在干净首连中复现，说明其更接近“同一浏览器会话复用旧 5011 连接 / `agent-browser record start` 触发 fresh context / 后端切换时短暂重连”的复合时序问题。
- 验证记录：使用 `oasis7_web_launcher` 无链默认路径在 `127.0.0.1:5416` 启动游戏后，`AGENT_BROWSER_SESSION=no-record-probe agent-browser --headed open ...` 6 秒内达到 `connectionStatus=connected`、`logicalTime=1`，`agent-browser errors` 与 `agent-browser console` 无 `ERR_CONNECTION_*`、无 `closure invoked recursively...`；而停服再起服时，页面会短暂进入 `connectionStatus=error` 且 `lastError=connection closed (code 1006), retrying...`，佐证同端口重启/重连时序确会制造噪音。结合实现排查，`crates/oasis7/src/viewer/web_bridge.rs` 会对未完成握手的 TCP/WS 连接直接打印 `HandshakeIncomplete`；`crates/oasis7_viewer/src/web_test_api.rs` 的 runtime diagnostic hook 会把 `console.error` 代理到 `window.__AW_TEST__.reportFatalError`，因此当浏览器控制台在上下文切换或旧闭包失效阶段报错时，最容易放大成 `closure invoked recursively or after being dropped`。
- 遗留事项：当前更像两个低层次时序噪音叠加，而不是新的单点产品阻断。若要进一步收敛，优先候选修法是：A) `web_bridge` 对 `HandshakeIncomplete` / 早断连降级为 debug/忽略，不再刷 stderr；B) `web_test_api` 的 runtime diagnostic hook 不要 capture 固定 `aw` 对象，而应在每次 `report()` 时动态读取 `window.__AW_TEST__`，减少旧闭包/旧上下文导致的 dropped closure 噪音。

## 17:38:49 CST / viewer_engineer
- 完成内容：按上条调查结论直接做 Viewer 首连降噪修复。1) 更新 `crates/oasis7/src/viewer/web_bridge.rs`：为 `HandshakeIncomplete`、`ConnectionReset`、`BrokenPipe`、`UnexpectedEof` 等预期断连增加 `is_expected_bridge_disconnect()` 分类，不再把这类早期握手中断统一打印成 `viewer web bridge error`；同时补了对应单测。2) 更新 `crates/oasis7_viewer/src/web_test_api.rs`：runtime diagnostic hook 不再 capture 固定 `const aw = window.__AW_TEST__`，改为每次 `report()` 时动态读取当前 `window.__AW_TEST__`，降低浏览器上下文切换、旧闭包失效时触发 `closure invoked recursively or after being dropped` 的概率。
- 验证记录：`env -u RUSTC_WRAPPER cargo test -p oasis7 --lib viewer::web_bridge -- --nocapture` 通过（含新增 `expected_bridge_disconnect_classifies_handshake_and_reset_noise`）；`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer -- --nocapture` 通过；`env -u RUSTC_WRAPPER cargo check -p oasis7_viewer --target wasm32-unknown-unknown` 通过。另已清理本轮残留 `agent-browser` 会话，当前 `agent-browser session list` 归零，先前未关闭的三个游戏浏览器窗口已处理。
- 遗留事项：本轮先收口日志/闭包噪音，不改变 Viewer 协议或重连策略；如后续仍观察到零星 `ERR_CONNECTION_*`，下一步应评估是否需要在前端控制台层面对“服务重启窗口内的瞬时网络错误”再做更细粒度的去重或降级展示。


## 18:00:30 CST / producer_system_designer
- 完成内容：按制作人真实产品链路亲自试玩。复用 `oasis7_web_launcher` 默认链入口（fresh `chain_node_id`），依次完成 `start_chain -> query_explorer_overview -> start_game`，随后用 `agent-browser` 打开 Viewer 并执行 `reopen Viewer -> play -> select(first:agent) -> focus(first:agent) -> step(10)` 的最小可玩循环；回写新卡 `doc/playability_test_result/card_2026_03_12_18_00_30.md`，并把证据沉淀到 `output/playwright/playability/20260312-producer-play-2/`。
- 验证记录：`curl http://127.0.0.1:5417/api/gui-agent/state` 显示 `chain_status=ready`、fresh `chain_node_id=viewer-live-node-fresh-1171981-1773308986020`；Viewer `__AW_TEST__.getState()` 最终为 `connectionStatus=connected`、`selectedId=agent-0`、`logicalTime=19`、`eventSeq=25`、`errorCount=0`、`lastError=null`；控制反馈显示 `play` 与 `step(10)` 均 `completed_advanced`。
- 遗留事项：默认链产品路径主阻断已解除，但同一浏览器会话首开 Viewer 仍可能停在 `connecting`，需要手动 reopen 一次；下一步应继续收口首连时序，目标是“第一次打开即 connected”，避免把恢复动作留给制作人或玩家。

## 18:18:40 CST / viewer_engineer
- 完成内容：专项定位“Viewer 同会话首开卡在 connecting、reopen 后恢复 connected”的根因。复现表明：首开 8 秒内 `__AW_TEST__.getState()` 持续 `connectionStatus=connecting`、`tick=0`、`lastError=null`；同一会话 reopen 后约 3 秒进入 `connected`。结合浏览器 console 证据，首开阶段实际先触发 `wgpu` Validation Error（`Device::create_render_pipeline label = copy_deferred_lighting_id_pipeline`）与 `CONTEXT_LOST_WEBGL`，导致 wasm runtime 在连接握手完成前就被图形 fatal 打断，因此表面症状才会表现为“卡在 connecting”。
- 验证记录：`output/release/game-launcher-producer-local/run-web-launcher.sh --listen-bind 127.0.0.1:5418` + GUI Agent `start_chain -> start_game` 后，使用 `agent-browser` 首开 `http://127.0.0.1:4173/?ws=ws://127.0.0.1:5011&test_api=1` 采集到 `output/playwright/playability/20260312-first-open-debug-2/state-open-8s.json`、`console-first-open.txt`、`errors-first-open.txt`；console 中明确出现 `copy_deferred_lighting_id_pipeline` 与 `WebGL: CONTEXT_LOST_WEBGL`。同时核对实现发现：仓库当前仅在 `crates/oasis7_viewer/src/app_bootstrap.rs` 关闭了 `add_default_deferred_lighting_plugin`，但 Bevy `CorePipelinePlugin` 仍会无条件注册 `CopyDeferredLightingIdPlugin`，说明 2026-02-25 的 wasm 兼容降级方案只关掉了 deferred lighting plugin，本身不足以杜绝这条 copy pipeline 初始化。
- 遗留事项：下一步修法应面向“彻底避免 wasm/WebGL2 首开初始化 `copy_deferred_lighting_id_pipeline`”，而不是继续把症状当成纯 WebSocket/重连问题；同时 `web_test_api` 当前未把这次 runtime fatal 可靠反映到 `lastError`，也建议顺手补上，以免再次被误读为 `connecting` 卡死。


## 18:46:30 CST / viewer_engineer
- 完成内容：修复“Viewer 同会话首开卡在 connecting、需要手动 reopen”问题的产品侧恢复链路。保留既有 `__AW_TEST__` runtime fatal 透出，并在 `crates/oasis7_viewer/src/web_test_api.rs` 的 runtime diagnostic hook 增加“已知图形 fatal（`copy_deferred_lighting_id_pipeline` / `CONTEXT_LOST_WEBGL` / `wgpu error`）仅自动 reload 一次”的 bounded recovery；同时让 `record_runtime_fatal_error()` 立即把 `connectionStatus=error`、`lastError`、`errorCount` 回写到快照，避免 wasm panic 后状态停在旧的 `connecting`。回写 `viewer-web-runtime-fatal-surfacing-2026-03-12` 专题 PRD/project 与 `doc/world-simulator/project.md`、`doc/playability_test_result/game-test.project.md`。
- 验证记录：`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer default_pbr_plugin_for_runtime_tracks_target_compat_mode -- --nocapture` 通过；`env -u RUSTC_WRAPPER cargo check -p oasis7_viewer --target wasm32-unknown-unknown` 通过；重打 `output/release/game-launcher-producer-local` 后，用 `oasis7_web_launcher` + GUI Agent 在 `127.0.0.1:5422` 启动产品栈，`agent-browser` 首开 Viewer 9 秒内达到 `connectionStatus=connected`、`tick=1`，不再需要手动 reopen；同页注入 `window.__AW_TEST__.reportFatalError("manual verification fatal", "qa.inject")` 后，快照立即变为 `connectionStatus=error`、`lastError="qa.inject: manual verification fatal"`。
- 遗留事项：当前自动恢复依赖“第一次 fatal 后第二次加载可成功”这一产品级 workaround；SwiftShader/WebGL2 环境本身仍会在 console 留下首轮 fatal 记录，因此后续若要彻底根治，仍需继续追 `copy_deferred_lighting_id_pipeline` 的底层兼容路径，而不是只停在 auto-reload。

## 18:58:10 CST / viewer_engineer
- 完成内容：完成“Viewer 首开卡住”的根本原因归因。结论分两层：1) 产品表层症状是 `__AW_TEST__` 长时间停在 `connecting/tick=0`；2) 真正根因是 SwiftShader/WebGL2 环境下，Bevy `CorePipelinePlugin` 仍无条件注册 `CopyDeferredLightingIdPlugin`，即便我们已在 wasm 路径关闭默认 deferred lighting plugin，该 copy pass 仍会在启动时为 `copy_deferred_lighting_id_pipeline` 建立一条读取 `texture_2d<u32>`、只写 `frag_depth` 的 depth-only fullscreen pipeline；这条 pipeline 在当前 `wgpu` GLES/SwiftShader 后端上触发 shader translation / validation fatal，导致 wasm runtime 在 Viewer 完成 HelloAck/首帧前就被图形层打断。
- 验证记录：源码对照显示 `crates/oasis7_viewer/src/app_bootstrap.rs` 仅关闭 `add_default_deferred_lighting_plugin`；而 `/home/scc/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/bevy_core_pipeline-0.18.0/src/lib.rs` 仍把 `CopyDeferredLightingIdPlugin` 作为 `CorePipelinePlugin` 的固定组成部分。对应 shader 与 pipeline 定义见 `/home/scc/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/bevy_core_pipeline-0.18.0/src/deferred/copy_deferred_lighting_id.wgsl` 与 `/home/scc/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/bevy_core_pipeline-0.18.0/src/deferred/copy_lighting_id.rs`；实机证据仍可见 `output/playwright/playability/20260312-first-open-fix-final/console-open-9s.txt` 里的 `Device::create_render_pipeline label = copy_deferred_lighting_id_pipeline` 与 `CONTEXT_LOST_WEBGL`。当前自动 reload 只是把“用户需要手动 reopen”降成产品内一次自恢复，没有改变底层 shader 兼容问题本身。
- 遗留事项：若要真正根治，后续需要针对 `CopyDeferredLightingIdPlugin` / deferred prepass 在 wasm+SwiftShader/WebGL2 的兼容路径做专项修复（如上游/本地分流禁用该 pass、或换掉这条 depth-only uint-texture copy 方案），而不是继续在 Viewer 连接层做补丁。

## 19:05:20 CST / viewer_engineer
- 完成内容：确认“有头模式是否仍不兼容”的环境根因。结论是：当前机器并非没有 GPU，也不是 Chrome 完全拿不到 `/dev/dri`；默认 `agent-browser` 会拉起 `google-chrome` 的 GPU process，并实际打开 `/dev/dri/renderD128`，但默认 WebGL renderer 仍是 `ANGLE (... SwiftShader Device ..., SwiftShader driver)`，说明 Chrome 在当前 X11 会话里把 WebGL/ANGLE 默认后端回退到了 SwiftShader。进一步用 `agent-browser --args '--use-angle=gl,--ignore-gpu-blocklist'` 强制改走 ANGLE OpenGL 后，浏览器 renderer 变为 `ANGLE (Intel, Mesa Intel(R) Iris(R) Xe Graphics (RPL-P), OpenGL 4.6)`，Viewer 首开可直接 `connected` 且未再出现 `copy_deferred_lighting_id_pipeline` fatal。
- 验证记录：系统侧确认存在 Intel Iris Xe + NVIDIA RTX 3050，且 `google-chrome` GPU process 已打开 `/dev/dri/renderD128`；`agent-browser eval` 在默认模式下返回 `renderer=ANGLE (... SwiftShader ...)`，而在 `--use-angle=gl,--ignore-gpu-blocklist` 下返回 `renderer=ANGLE (Intel, Mesa Intel(R) Iris(R) Xe Graphics (RPL-P), OpenGL 4.6)`。在 `127.0.0.1:5423` 产品栈上，用该 flags 打开 Viewer 后 `__AW_TEST__.getState()` 为 `connectionStatus=connected`、`errorCount=0`、`lastError=null`，console 也未再命中 `copy_deferred_lighting_id_pipeline`。
- 遗留事项：当前默认 `agent-browser`/Chrome 仍会选到 SwiftShader，因此“有头模式”本身不足以作为通过条件；后续若要稳定 Web 验收，应把 `--use-angle=gl`（或等价的硬件后端固定策略）纳入手册/脚本，或进一步查清为何默认 ANGLE Vulkan 在本机会回退到 SwiftShader。

## 18:52:00 CST / viewer_engineer
- 完成内容：把“headed 仍可能回退 SwiftShader”的环境结论正式落到 Web 试玩入口。`scripts/agent-browser-lib.sh` 现默认对 `ab_open` 注入 `--use-angle=gl,--ignore-gpu-blocklist`（可用 `AGENT_BROWSER_ARGS` 覆盖），并在同名 session 重新 `open` 前先 `close`，减少残留窗口/脏会话；`scripts/run-producer-playtest.sh --open-headed` 也已改走统一封装。
- 完成内容：`scripts/run-game-test-ab.sh` 不再只阻断 `headless + SwiftShader`；现在 headed/headless 只要 renderer 命中 `SwiftShader` / `llvmpipe` / `Software Rasterizer` / `Basic Render Driver`，都会落 `browser_env.json` 并按环境阻断，避免把“有头但仍是软件渲染”误判成兼容。
- 完成内容：同步回写 `testing-manual.md`、`doc/testing/manual/web-ui-agent-browser-closure-manual.prd.md`、`doc/testing/launcher/launcher-bundle-first-playtest-entry-2026-03-12.prd.md` 与启动器人工清单，明确 headed 默认固定硬件 WebGL 参数、若仍落 software renderer 则继续阻断。
- 遗留事项：当前默认参数已在本机 Intel/Mesa OpenGL 路径上验证有效，但仍未解释为何 Chrome 默认 ANGLE/Vulkan 会回退到 SwiftShader；若后续要彻底免配置，还需继续追默认后端回退原因。

## 20:16:30 CST / qa_engineer
- 完成内容：对 headed 硬件 WebGL 默认参数补丁做针对性回归。`bash -n scripts/agent-browser-lib.sh scripts/run-producer-playtest.sh scripts/run-game-test-ab.sh` 与两个 `--help` 帮助口径均通过；执行 `./scripts/run-game-test-ab.sh --bundle-dir output/release/game-launcher-local --no-llm --out-dir output/playwright/playability/20260312-angle-gl-patch-check` 后，`browser_env.json` 已返回 `renderer=ANGLE (Intel, Mesa Intel(R) Iris(R) Xe Graphics (RPL-P), OpenGL 4.6)`，证明默认 `--use-angle=gl,--ignore-gpu-blocklist` 已成功把 headed Viewer Web 从 SwiftShader 切到硬件路径。
- 完成内容：额外用 `timeout 25s ./scripts/run-producer-playtest.sh --bundle-dir output/release/game-launcher-local --no-llm --open-headed --session producer-angle-patch-test` 抽样 producer 入口，日志明确打印 `agent-browser args: --use-angle=gl,--ignore-gpu-blocklist` 且成功开出 headed Viewer 页面，说明制作人入口也已走统一封装。
- 遗留事项：本轮 `run-game-test-ab.sh` 后续仍被新的协议错误阻断：Viewer `lastError=decode error: unknown variant authoritative_batch`。这已与 GPU/SwiftShader 问题分离，属于另一条 runtime/viewer 协议兼容链路，需后续单独追。

## 20:28:30 CST / viewer_engineer
- 完成内容：查明 `decode error: unknown variant authoritative_batch` 的根因不是当前 Viewer 源码缺协议，而是 **bundle 内 Web Viewer 静态产物过旧**。证据链如下：1) 源码协议已支持 `AuthoritativeBatch`，`crates/oasis7_proto/src/viewer.rs` 定义了 `ViewerResponse::AuthoritativeBatch`，`crates/oasis7_viewer/src/main_connection.rs` 也已显式匹配该分支；2) 旧 bundle `output/release/game-launcher-local/web/oasis7_viewer-f2a6268d18f394d_bg.wasm` 的字符串表里只含 `hello_ack/event/decision_trace/control_completion_ack/prompt_control_ack/prompt_control_error/agent_chat_ack/agent_chat_error`，缺少 `authoritative_batch` / `authoritative_challenge_*` / `authoritative_recovery_*`；3) 当前源码 fresh trunk 产物 `crates/oasis7_viewer/dist/oasis7_viewer-771ab2b9a778d948_bg.wasm` 与 fresh bundle `output/release/game-launcher-protocol-check/web/oasis7_viewer-449d0579dcf8bc30_bg.wasm` 都已包含这些新 variant。
- 验证记录：旧 bundle 上执行 `./scripts/run-game-test-ab.sh --bundle-dir output/release/game-launcher-local --no-llm --out-dir output/playwright/playability/20260312-angle-gl-patch-check` 时，`browser_env.json` 显示 renderer 已是 Intel OpenGL，但 `lastError=decode error: unknown variant authoritative_batch`；随后 fresh rebuild `./scripts/build-game-launcher-bundle.sh --profile dev --out-dir output/release/game-launcher-protocol-check`，再执行 `./scripts/run-game-test-ab.sh --bundle-dir output/release/game-launcher-protocol-check --no-llm --out-dir output/playwright/playability/20260312-protocol-fresh-bundle-check`，A/B playability 直接通过，`ab_metrics.json` 中 `initial/final.connectionStatus=connected`、`lastError=null`。
- 遗留事项：当前 bundle-first 入口默认会复用已存在 bundle（如 `run-producer-playtest.sh` 未传 `--rebuild`），因此只要 runtime/viewer 协议前进而本地 bundle 未重建，就仍可能复现同类“源码已兼容、bundle 仍旧协议”的产物漂移问题；后续若要彻底消除这类误判，应考虑为 bundle 增加 viewer wasm/hash/version freshness 校验或协议版本守卫。

## 20:38:30 CST / qa_engineer
- 完成内容：为 bundle-first 试玩入口补上 stale bundle 根治守卫。新增 `scripts/bundle-freshness-lib.sh`：以工作区相关源码集合（`oasis7/src/viewer*`、`oasis7_viewer/src`、`oasis7_proto/src`、launcher/client launcher 入口与 `Cargo/Trunk/index.html`）计算 `sourceFingerprint`，并由 `scripts/build-game-launcher-bundle.sh` 在 bundle 根目录写入 `.oasis7-bundle-manifest.json`，同时记录 viewer/launcher Web 产物路径与 sha256。
- 完成内容：`scripts/run-game-test.sh --bundle-dir` 现在默认校验 freshness manifest；manifest 缺失或 fingerprint 漂移时直接 fail-fast，并给出“重建或显式 `--allow-stale-bundle`”提示。`scripts/run-producer-playtest.sh` 则改为在复用前先校验 freshness；命中 stale bundle 时自动重建，不再默默沿用旧 Viewer Web 产物。
- 验证记录：1) 旧 bundle `output/release/game-launcher-local` 先被 `./scripts/run-game-test.sh --bundle-dir output/release/game-launcher-local --no-llm` 正确阻断，报 `bundle freshness manifest missing`；2) `timeout 35s ./scripts/run-producer-playtest.sh --bundle-dir output/release/game-launcher-local --profile dev --no-llm` 自动识别 stale bundle 并重建，日志明确输出 `stale producer bundle detected`；3) 重建后 manifest 已生成，且 `./scripts/run-game-test-ab.sh --bundle-dir output/release/game-launcher-local --no-llm --out-dir output/playwright/playability/20260312-freshness-guard-pass` 再次通过，A/B 指标正常。
- 遗留事项：当前 freshness guard 依赖工作区源码 fingerprint，因此若执行者确实要拿“历史 bundle / 外部交付 artifact”做刻意回归，需显式使用 `--allow-stale-bundle` 作为越权声明；后续若要支持更细的“跨 commit artifact 审计”，可以再补 bundle 来源 commit / release metadata。

## 21:40:34 CST / producer_system_designer
- 完成内容：围绕“是否抽象一层标准接口，让 `OpenClaw` 替代/承接 agent 决策层参与模拟游戏”完成专题建模。新增 `doc/world-simulator/llm/llm-decision-provider-standard-openclaw-feasibility-2026-03-12.{prd,design,project}.md`，将需求登记为 `PRD-WORLD_SIMULATOR-036`，明确采用 `Decision Provider` 标准层而非直接让外部框架替代 runtime/kernel；同步回写 `doc/world-simulator/prd.md`、`doc/world-simulator/project.md` 与 `doc/world-simulator/prd.index.md`，新增 `TASK-WORLD_SIMULATOR-112` 追踪“可行性建模 + 模块入口更新”。
- 遗留事项：后续需由 `agent_engineer` 牵头进入 `T1/T2`，先冻结 provider contract 与 golden fixture，再决定是否推进 `OpenClawAdapter` PoC；`runtime_engineer` / `viewer_engineer` 需联审 trace、memory 与失败语义映射边界。

## 22:07:32 CST / producer_system_designer
- 完成内容：在 `PRD-WORLD_SIMULATOR-036` 的可行性专题基础上，继续补齐“安装在用户机器上的 `OpenClaw` 怎么玩这个游戏”的首期工程方案。新增 `doc/world-simulator/llm/llm-provider-loopback-http-integration-2026-03-12.{prd,design,project}.md`，登记为 `PRD-WORLD_SIMULATOR-037` / `TASK-WORLD_SIMULATOR-113`，明确首期采用 `OpenClaw(Local HTTP)`：本地独立进程仅监听 `127.0.0.1`，通过 `GET /v1/provider/info|health` 与 `POST /v1/world-simulator/decision|feedback` 完成发现、握手、决策与反馈；同步回写模块主 PRD / project / index。
- 遗留事项：后续需由 `agent_engineer` 先冻结 mock local HTTP contract 与 launcher provider 配置，再决定是否进入真实 `OpenClawAdapter` PoC；`viewer_engineer` 需补 provider 状态与 trace 摘要展示，`runtime_engineer` 联审超时/非法动作的降级边界。

## 22:24:00 CST / producer_system_designer
- 完成内容：根据“目标是体验能等价”的新增口径，补建 `doc/world-simulator/llm/llm-provider-agent-experience-parity-2026-03-12.{prd,design,project}.md`，登记为 `PRD-WORLD_SIMULATOR-038` / `TASK-WORLD_SIMULATOR-114`。专题把 `OpenClaw` 接入目标从“可接入/可玩”升级为“在纳入范围场景下与内置 agent 达到用户可感知等价”，并补齐 `P0/P1/P2` 场景层级、完成率/延迟/无效动作率/trace 完整度/恢复率等指标阈值，以及“未通过 parity 不得默认启用”的发布门禁；同步回写 `doc/world-simulator/prd.md`、`doc/world-simulator/project.md` 与 `doc/world-simulator/prd.index.md`。
- 遗留事项：后续需由 `agent_engineer` 与 `qa_engineer` 先冻结 parity 场景集与评分卡，再把 `Decision Provider` / `OpenClaw(Local HTTP)` 实施任务正式改挂到 parity 通过线；在 `P0` 未通过前，`OpenClaw` 仅能保持 `experimental`。

## 22:41:37 CST / producer_system_designer
- 完成内容：承接根 `AGENTS.md` 未提交改动，按 engineering 治理流程将“需要其他伙伴协作时”的执行语义正式收口到现有角色协作体系：更新 `doc/engineering/prd.md`，将该变更并入 `PRD-ENGINEERING-018` 的成功标准、关键流程、规格矩阵、验收、NFR、验证映射与决策记录；更新 `doc/engineering/project.md`，登记 `TASK-ENGINEERING-049` 并回写状态摘要，明确此类协作应通过切换到 `.agents/roles/*.md` 标准角色视角并加载职责卡完成，而非依赖未定义的 `sub agent` 机制表述。
- 遗留事项：已执行 `scripts/doc-governance-check.sh` 作为 `test_tier_required` 验证；当前门禁仍被既有文档问题阻断：1) `doc/testing/launcher/launcher-bundle-first-playtest-entry-2026-03-12.prd.md` 缺少 paired project 双向链接与 `目标/范围/接口 / 数据/里程碑/风险` 必需章节；2) `doc/playability_test_result/card_2026_03_12_15_29_33.md`、`doc/playability_test_result/card_2026_03_12_18_00_30.md` 尚未纳入 `module-root-md-allowlist`。以上均与本次 engineering/`AGENTS.md` 改动无直接关系，后续需由对应 owner 单独收口。

## 22:59:47 CST / qa_engineer
- 完成内容：修复 `scripts/doc-governance-check.sh` 暴露的两类文档门禁问题：1) 为 `doc/testing/launcher/launcher-bundle-first-playtest-entry-2026-03-12.prd.md` 补齐 legacy 兼容头部章节 `目标/范围/接口 / 数据/里程碑/风险`，并显式加入 paired project 路径 `doc/testing/launcher/launcher-bundle-first-playtest-entry-2026-03-12.project.md`，恢复专题 PRD 与项目文档双向追溯；2) 回写 `doc/.governance/module-root-md-allowlist.txt`，纳入 `doc/playability_test_result/card_2026_03_12_15_29_33.md` 与 `doc/playability_test_result/card_2026_03_12_18_00_30.md` 两张当天测试卡，消除模块根目录 allowlist 漏项。
- 遗留事项：需复跑 `scripts/doc-governance-check.sh` 确认本轮门禁已完全通过；若后续再新增测试卡，仍需同步回写 allowlist 以避免同类阻断重复出现。


## 22:46:00 CST / producer_system_designer
- 完成内容：继续推进 `PRD-WORLD_SIMULATOR-038` 的 T1，新增 `doc/world-simulator/prd/acceptance/openclaw-agent-parity-scenario-matrix-2026-03-12.md` 与 `doc/world-simulator/prd/acceptance/openclaw-agent-parity-score-card-2026-03-12.md`，冻结 `P0/P1/P2` 场景层级、样本数、通过线/阻断线以及 QA/producer 共用评分卡；同步将 parity 专题 project 的 T1 标记完成，回写 `doc/world-simulator/prd.md`、`doc/world-simulator/project.md` 与 `doc/world-simulator/prd.index.md`，登记 `TASK-WORLD_SIMULATOR-115`。
- 遗留事项：下一步由 `agent_engineer` 牵头进入 parity 专题 T2，给 builtin/OpenClaw 补统一 fixture benchmark、trace 汇总与分数聚合产物；在 T2 完成前，当前评分卡仍是人工/半自动模板。

## 23:08:00 CST / producer_system_designer
- 完成内容：继续推进 `PRD-WORLD_SIMULATOR-038` 的 T2，新增 `doc/world-simulator/prd/acceptance/openclaw-agent-parity-benchmark-protocol-2026-03-12.md` 与 `doc/world-simulator/prd/acceptance/openclaw-agent-parity-aggregation-template-2026-03-12.md`，冻结 builtin/OpenClaw 的统一 fixture benchmark 运行字段、trace 汇总字段、错误码分类、JSON/CSV 产物结构以及聚合结论模板；同步将 parity 专题 project 的 T2 标记完成，回写 `doc/world-simulator/prd.md`、`doc/world-simulator/project.md` 与 `doc/world-simulator/prd.index.md`，登记 `TASK-WORLD_SIMULATOR-116`。
- 遗留事项：下一步进入 T3，需要把 `Decision Provider` 与 `OpenClaw(Local HTTP)` 两个实施专题的后续任务，显式改挂到 parity 门禁而不是“接通”门禁。

## 23:47:01 CST / agent_engineer
- 完成内容：推进 `PRD-WORLD_SIMULATOR-036` 的 `T1/T2`，在 `crates/oasis7/src/simulator/decision_provider.rs` 新增 `DecisionProvider` 标准层最小实现，冻结 `ObservationEnvelope / DecisionRequest / DecisionResponse / FeedbackEnvelope / ProviderTraceEnvelope / MemoryWriteIntent` 等 contract 类型，并补建 `golden_decision_provider_fixtures()` 作为 provider-agnostic golden fixture；同时新增 `ProviderBackedAgentBehavior` 与 `MockDecisionProvider`，完成 `AgentBehavior facade -> DecisionProvider -> runtime -> trace` 的离线闭环。补充 `crates/oasis7/src/simulator/tests/decision_provider.rs` 定向回归，覆盖 fixture round-trip、正常动作执行、provider timeout 降级与非法 action_ref 降级。同步回写 `doc/world-simulator/llm/*openclaw*.project.md` 与 `doc/world-simulator/project.md`，将 `Decision Provider` / `OpenClaw(Local HTTP)` 后续实施任务显式改挂到 `PRD-WORLD_SIMULATOR-038` parity 门禁。
- 遗留事项：下一步继续 `TASK-WORLD_SIMULATOR-119`，在 launcher 落地 `provider mode / base URL / token / auto-discover / localhost health-check`；随后再进入 `PRD-WORLD_SIMULATOR-037 / T2` 的 mock local HTTP contract tests 与 `PRD-WORLD_SIMULATOR-036 / T3` 的 `OpenClawAdapter` PoC。
- 验证：已执行 `env -u RUSTC_WRAPPER cargo test -p oasis7 decision_provider -- --nocapture`，4 项新增定向测试全部通过。

## 23:50:48 CST / agent_engineer
- 完成内容：补充文档门禁复验；执行 `timeout 30 ./scripts/doc-governance-check.sh`。
- 遗留事项：文档门禁脚本在 30s 内未返回结果（`EXIT_CODE=124`），本轮先以定向代码回归为准；若后续继续推进 launcher / OpenClaw 文档回写，建议单独排查脚本慢点或阻塞点。


## 00:18:22 CST / viewer_engineer
- 完成内容：收口 `TASK-WORLD_SIMULATOR-119`，在 `oasis7_client_launcher` 落地 `OpenClaw(Local HTTP)` 首期产品入口：新增 `provider_mode=provider_loopback_http`、`openclaw_base_url`、`openclaw_auth_token`、`openclaw_connect_timeout_ms`、`openclaw_agent_profile`、`openclaw_auto_discover` 配置项，补充 loopback-only 校验、默认发现地址 `http://127.0.0.1:5841`、本地 `/v1/provider/info` + `/v1/provider/health` 探测与 UI/反馈窗口脱敏展示；同步补齐 launcher 定向回归测试。
- 遗留事项：下一步切回 `agent_engineer` 继续 `TASK-WORLD_SIMULATOR-121`，把本地 HTTP contract 绑定到 simulator `DecisionProvider`/`OpenClawAdapter`，随后再进入 `T4` 的 viewer/launcher trace 状态展示细化。
- 验证：已执行 `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher openclaw -- --nocapture`，4 项 OpenClaw 定向测试全部通过。

## 00:18:22 CST / agent_engineer
- 完成内容：收口 `TASK-WORLD_SIMULATOR-120`，在 `crates/oasis7/src/simulator/provider_loopback_http.rs` 新增 `OpenClawLocalHttpClient` 与 `OpenClawProviderInfo/OpenClawProviderHealth/OpenClawFeedbackAck` contract 类型，统一封装 loopback-only `http://` 校验、Bearer token、`/v1/provider/info`、`/v1/provider/health`、`/v1/world-simulator/decision`、`/v1/world-simulator/feedback` 调用；新增 `crates/oasis7/src/simulator/tests/provider_loopback_http.rs`，用 mock local HTTP server 冻结四个端点的 request/response 协议、401 失败签名与非 loopback 拒绝路径。
- 遗留事项：下一步继续 `TASK-WORLD_SIMULATOR-121`，实现 `OpenClawAdapter` 与 `DecisionProvider` 绑定，并把 phase-1 低频动作白名单与 provider error/fallback 映射固化到 simulator contract tests。
- 验证：已执行 `env -u RUSTC_WRAPPER cargo test -p oasis7 provider_loopback_http -- --nocapture`，3 项本地 HTTP contract tests 全部通过。


## 00:23:40 CST / agent_engineer
- 完成内容：继续推进 `TASK-WORLD_SIMULATOR-121`，在 `crates/oasis7/src/simulator/openclaw_adapter.rs` 新增 `OpenClawAdapter`，把 `OpenClawLocalHttpClient` 绑定到 `DecisionProvider`；补充 phase-1 白名单校验、`action_catalog` 对齐校验、`provider_error`/HTTP 错误到 `DecisionProviderError` 的映射，以及 feedback ack 拒绝路径。新增 `crates/oasis7/src/simulator/tests/openclaw_adapter.rs`，用 mock local HTTP server 验证 `decision -> feedback` 绑定链路、超出白名单/不在 catalog 的拒绝策略，以及 provider error envelope 到 simulator fallback 错误的映射。
- 遗留事项：`TASK-WORLD_SIMULATOR-121` 暂不标完成；当前 adapter 仅锁住 `wait` / `wait_ticks` / `move_agent` 子集，`speak_to_nearby` / `inspect_target` / `simple_interact` 仍待 runtime 动作语义与 adapter 映射补齐，且最终完成定义仍受 `PRD-WORLD_SIMULATOR-038` parity 门禁约束。
- 验证：已执行 `env -u RUSTC_WRAPPER cargo test -p oasis7 openclaw_adapter -- --nocapture`，3 项 adapter binding tests 全部通过。
