# 2026-03-30

## 09:54:21 CST / runtime_engineer
- 完成内容: 在 `crates/oasis7/src/runtime` 为 `governance_main_token_controller_registry` 增加 `restricted_starter_claim_admin_account_ids`，要求 admin account 同时绑定既有 `controller_signer_policies`；`IssueRestrictedStarterClaimGrant / RevokeRestrictedStarterClaimGrant` 现在会在进入 grant 业务校验前先拒绝 registry 缺失、allowlist 为空或非 admin issuer。
- 完成内容: 补齐 runtime 定向测试与治理 registry 构造点，覆盖 `registry missing`、`non-admin issuer`、`revoke early reject` 和 admin policy 缺失校验；并同步回写 `gameplay-agent-claim-token-cost-2026-03-27.{prd,project}.md` 与 restricted grant liveops runbook。
- 遗留事项: 继续完成本轮 `env -u RUSTC_WRAPPER cargo test/check`、`./scripts/doc-governance-check.sh`、`git diff --check`，确认无编译/文档治理残留后再提交与 landing。

## 11:23:39 CST / runtime_engineer
- 完成 `TASK-ENGINEERING-054`：将 `crates/oasis7/src/bin/oasis7_chain_runtime.rs` 压到 1200 行内，抽出 `cli.rs`；将 `execution_bridge.rs` 拆为目录模块与独立测试文件，保持 `cargo check -p oasis7 --bin oasis7_chain_runtime --tests` 通过。
- 继续清理门禁新增 runtime 超限：`crates/oasis7/src/runtime/events.rs`、`crates/oasis7/src/runtime/state.rs`、`crates/oasis7/src/runtime/state/apply_domain_event_core.rs`、`crates/oasis7/src/runtime/state/apply_domain_event_gameplay.rs`、`crates/oasis7/src/runtime/state/apply_domain_event_main_token.rs`、`crates/oasis7/src/runtime/world/event_processing.rs` 已全部拆回 1200 行内。
- 刷新 `doc/.governance/rust-oversized-file-baseline.tsv`，退休本批已完成治理的旧超限基线。
- 遗留事项：`TASK-ENGINEERING-056` 仍需处理 `oasis7_viewer` 侧 8 个生产超限文件；`TASK-ENGINEERING-057` 仍需收口 13 个超限测试文件。

## 11:23:39 CST / viewer_engineer
- 完成 `TASK-ENGINEERING-055`：`crates/oasis7/src/viewer/runtime_live.rs` 与 `crates/oasis7/src/viewer/runtime_live/tests.rs` 拆为目录模块和按行为域分文件测试，旧超限基线已退休。
- 验证 `env -u RUSTC_WRAPPER cargo check -p oasis7 --lib --tests` 通过，`scripts/check-rust-file-size.sh` 已不再报告本批 `runtime_live` / runtime 新增超限。
- 遗留事项：`oasis7_viewer` 的 `player guide`、`web_test_api`、`viewer_automation` 仍是下一批 viewer 超限治理重点。

## 11:36:42 CST / runtime_engineer
- 完成内容: 为 restricted grant admin registry 增加正式 runtime governance 热更新动作 `UpdateRestrictedStarterClaimAdminRegistry`，并通过 `GovernanceEvent::RestrictedStarterClaimAdminRegistryUpdated` 落到 world-state；更新前要求 passed proposal 与 proposer 匹配，更新后仍强制命中现有 controller signer allowlist policy，不允许把 registry 退回成仅靠离线 import / 启动注入维护。
- 完成内容: 修正实现分层错误，移除误加到 `DomainEvent` 的 registry update 事件，开放 sibling module 所需的治理校验 helper 可见性，补齐 governance / agent_claims 定向测试，并回写 PRD / project / liveops runbook，把本轮任务登记为 `TASK-GAMEPLAY-AGC-015` / `TASK-GAME-053`。
- 遗留事项: 继续等待并确认本轮定向 `cargo test`、`./scripts/doc-governance-check.sh`、`git diff --check` 结果；通过后执行提交与 landing。

## 11:40:12 CST / runtime_engineer
- 完成内容: 将 `UpdateRestrictedStarterClaimAdminRegistry` 从“模拟内 passed proposal proposer 授权”重构为“主链 `ecosystem_pool` treasury controller account 授权”；`Action`/`GovernanceEvent` 去掉 `operator_agent_id + proposal_key`，统一改为 `controller_account_id`，并在 world apply 时直接绑定当前 controller slot 真值。
- 完成内容: 在 `ConsensusActionPayloadEnvelope` 与 `NodeRuntime` 为该动作接入正式 main-token auth proof，新增独立 canonical 签名前缀、controller slot account binding 和 signer allowlist / threshold policy 校验；补齐 `oasis7_node` payload 回归与 `oasis7` governance/agent_claims 定向测试，确认 admin registry update 已完全收口到钱包级治理路径。
- 完成内容: 同步回写 `gameplay-agent-claim-token-cost-2026-03-27.{prd,project}.md` 与 `mainchain-token-signed-transaction-authorization-2026-03-23.{prd,project}.md`，明确 restricted grant admin roster 属于 main-token controller 面，而不是模拟内 agent/proposal 面。
- 遗留事项: 继续执行 `./scripts/doc-governance-check.sh`、`git diff --check`、提交与 landing。

## 12:00:21 CST / producer_system_designer
- 完成内容: 在独立 task worktree 内启动 ROUND-009“文档消费入口与手册语义收口轮”，沿用既有 round 治理模式，新增 `consistency-review-round-009.md`、`round-009-reviewed-files.md`、`round-009-kickoff-worklist.md` 与 `round-009-audit-progress-log.md` 四件套。
- 完成内容: 冻结 ROUND-009 focused scope 为 23 个高频入口/手册/静态镜像对象，并登记 `I9-001~I9-005` 五类问题：入口可消费性、手册语义漂移、`readme` 模块边界膨胀、`ui_review_result` 例外目录定位、以及高体量索引可读性下降。
- 完成内容: 同步回写 `doc/core/project.md`、`doc/engineering/project.md` 与 `doc/core/README.md`，将 ROUND-009 挂靠到 core / engineering 双侧台账。
- 遗留事项: 后续按 ROUND-009 清单继续完成首批 `keep/migrate/split/defer` 决议与高优先级入口/手册回写；完成后补跑 `./scripts/doc-governance-check.sh`、`git diff --check` 并执行提交。

## 12:10:28 CST / runtime_engineer
- 完成内容: 新增 `crates/oasis7/src/bin/oasis7_liveops_grant_cli.rs`，为 restricted grant 提供 `issue / revoke / status` 三个运营 CLI 子命令，默认 `issuer_id=liveops`，并继续复用 `World::submit_action -> step -> save_to_dir` 的 canonical runtime 路径，不再要求运营手工拼 action JSON。
- 完成内容: CLI 明确只覆盖日常运营动作，不开放 admin roster 直改；`UpdateRestrictedStarterClaimAdminRegistry` 仍保留给 `ecosystem_pool` controller-governed 钱包路径。同步回写 `gameplay-agent-claim-token-cost-2026-03-27.{prd,project}.md`、`doc/game/project.md` 与 restricted grant liveops runbook。
- 遗留事项: 继续完成 `cargo check/test`、`doc-governance-check`、`git diff --check`，通过后提交与 landing。

## 12:48:11 CST / runtime_engineer
- 完成内容: 新增 `scripts/oasis7-liveops-grant.sh` 作为 restricted grant 运营 wrapper，把 `status / issue / revoke` 收口为更短的位置参数命令，并支持 `OASIS7_WORLD_DIR` / `OASIS7_LIVEOPS_ISSUER_ID` 缺省、`--print-cmd` 与 `--cli-bin` smoke；底层仍只转发到 `oasis7_liveops_grant_cli`，不复制 runtime 规则。
- 完成内容: 同步回写 `gameplay-agent-claim-token-cost-2026-03-27.{prd,project}.md`、`doc/game/project.md` 与 restricted grant liveops runbook，明确“正式 CLI + 运营 wrapper”双层入口和不新增 admin/world-file 旁路的边界。
- 遗留事项: 继续执行 wrapper smoke、`./scripts/doc-governance-check.sh`、`git diff --check`、提交与 landing。

## 13:42:53 CST / runtime_engineer
- 完成内容: 将 `oasis7_governance_registry_import` / `oasis7_governance_registry_audit` 从“全局一个 threshold”扩成“manifest 可按 slot 显式声明 threshold，缺省仍走默认值”；由此正式支持 `liveops` 这类低权限 restricted grant admin slot 使用 `1-of-2`，同时保持 finality 与 treasury/controller 主槽位继续默认 `2-of-3`。
- 完成内容: 同步放宽 `scripts/governance-registry-{drill,live-drill}.sh` 对 slot signer count/threshold 的硬编码假设，并回写 gameplay claim PRD/project、game 根 project、restricted grant runbook 与 p2p governance signer externalization project，明确 `liveops` 是特例 slot 而非全局降阈值。
- 遗留事项: 继续执行 import/audit 定向测试、shell syntax check、文档治理检查、提交与 landing。

## 15:56:51 CST / runtime_engineer
- 完成内容: 确认 `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_governance_registry_import -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_governance_registry_audit -- --nocapture`、`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_governance_registry_import --bin oasis7_governance_registry_audit`、`bash -n scripts/governance-registry-{drill,live-drill}.sh` 与 `./scripts/doc-governance-check.sh` 全部通过。
- 完成内容: 将运营侧 `/home/scc/Documents/keys/oasis7-liveops-batch-20260330-01/public_manifest.json` 正式补成 `liveops threshold=1` 的 `1-of-2` bundle，和本轮 runtime/import/audit 能力保持一致。
- 遗留事项: 执行单 commit、标准化 landing 到本地 `main`，然后回收 task worktree / branch。

## 13:22:24 CST / runtime_engineer
- 完成 `TASK-ENGINEERING-056` 收尾：将 `crates/oasis7/src/runtime/world/module_actions_impl_part2.rs` 的 module release 分支抽到 `crates/oasis7/src/runtime/world/module_actions_impl_part2/release_support.rs`，并确认根文件降到 931 行、支撑文件控制在 1194 行；同时退休 `oasis7_openclaw_parity_bench.rs`、`oasis7_pure_api_client.rs`、`oasis7_openclaw_local_bridge.rs`、`oasis7_web_launcher/control_plane.rs`、`runtime/world/persistence.rs`、`viewer/live_split_part1.rs`、`runtime/world/governance.rs`、`runtime/world/module_actions_impl_part2.rs` 的生产超限基线。
- 修复同轮拆分遗留：`crates/oasis7/src/runtime/world/governance_internal.rs` 中需要跨 sibling 访问的 helper 恢复为 `pub(crate)`，避免 `governance.rs`、`event_processing.rs`、`release_support.rs` 在编译期访问失败。
- 遗留事项：生产代码超限已清零，但测试侧仍保留 13 个冻结尾债，下一批 burn-down 需转向测试文件。

## 13:22:24 CST / qa_engineer
- 验证通过：`env -u RUSTC_WRAPPER cargo check -p oasis7 --lib --tests`、`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_web_launcher --tests`、`./scripts/check-rust-file-size.sh`；当前门禁结果为 `oversized code files=0, test files=13`。
- 与最新 `main` 对齐时，补录 `crates/oasis7_node/src/tests_action_payload.rs` 的冻结测试基线从 1497 刷新到 1622，并将 `RestrictedStarterClaimAdminRegistryUpdated` 的 apply 逻辑补入 `governance_internal.rs`，避免 rebase 后出现基线漂移与 governance event 非穷尽匹配。
- 观察到的非阻断项：`oasis7_pure_api_client.rs`、`oasis7_chain_runtime/execution_bridge/mod.rs`、`oasis7_viewer_live.rs`、`oasis7_web_launcher.rs` 仍有未使用 import / `mut` 警告；`crates/oasis7_viewer --tests` 仍有 `AgentClaimState` 初始化缺字段的独立测试债，与本轮生产文件超限治理无直接耦合。
- 遗留事项：按 `TASK-ENGINEERING-057` 的冻结清单继续处理 13 个超限测试文件，并单独安排 `oasis7_viewer --tests` 编译债修复。

## 13:24:52 CST / producer_system_designer
- 完成内容: 执行 ROUND-009 首批手册载体规范化，新增 `doc/world-simulator/viewer/viewer-manual.manual.md` 与 `doc/testing/manual/web-ui-agent-browser-closure-manual.manual.md` 两份 canonical `*.manual.md`，分别承接 Viewer 使用说明与 Web UI 闭环操作步骤。
- 完成内容: 将 `doc/world-simulator/viewer/viewer-manual.md` 降级为兼容入口，并回写 `README.md`、`doc/README.md`、`doc/world-simulator/README.md`、`doc/testing/README.md`、`testing-manual.md` 与 `site/doc/{cn,en}/index.html` 的高频入口引用。
- 完成内容: 同步更新 `doc/core/project.md`、`doc/engineering/project.md`、`doc/testing/project.md`、`doc/world-simulator/project.md` 与 ROUND-009 台账，明确 manual / PRD / project 三层职责与当前收口结论。
- 遗留事项: 继续执行 `./scripts/doc-governance-check.sh`、`git diff --check`，若通过则提交、landing，并视 ROUND-009 后续批次再决定是否继续替换更深层历史引用。

## 15:39:58 CST / producer_system_designer
- 完成内容: 执行 ROUND-009 第二批入口层治理，在 `README.md` 增加“从这里开始”矩阵，在 `doc/README.md` 增加“按目标进入”矩阵，把预览、验证、开发、追溯四类阅读起点显式化。
- 完成内容: 在 `site/doc/cn/index.html` 与 `site/doc/en/index.html` 增加中英文对齐的按目标分流区块，使公开 docs hub 与仓库入口共享同一套消费层分流逻辑。
- 完成内容: 回写 `doc/core/project.md`、`doc/core/reviews/round-009-reviewed-files.md` 与 `doc/core/reviews/round-009-audit-progress-log.md`，将 `README.md`、`doc/README.md`、`site/doc/{cn,en}/index.html` 的 ROUND-009 状态更新为 `aligned`。
- 遗留事项: 继续执行 `./scripts/doc-governance-check.sh`、`git diff --check`，通过后提交并 landing；ROUND-009 后续仍需继续处理 `doc/readme/**` 边界与 `ui_review_result/**` 定位问题。

## 16:03:00 CST / liveops_community
- 完成内容: 执行 ROUND-009 第三批 `readme` 模块边界治理，在 `doc/readme/README.md` 增加 `canonical / runbook / material / execution_log` 四层消费语义与推荐入口，避免对外口径、渠道 SOP、素材包和真实执行记录混写成同一层。
- 完成内容: 在 `doc/readme/prd.index.md` 中将索引拆分为 `Canonical / 三件套专题` 与 `Material / Execution / SOP 补充入口` 两层，并为每类文档补充用途说明。
- 完成内容: 回写 `doc/readme/project.md`、`doc/core/project.md`、`doc/engineering/project.md` 与 ROUND-009 台账，将 `doc/readme/README.md`、`doc/readme/prd.index.md` 的状态更新为 `aligned`。
- 遗留事项: 继续执行 `./scripts/doc-governance-check.sh`、`git diff --check`，通过后提交并 landing；ROUND-009 仍剩 `ui_review_result/**` 与更高体量模块索引的后续治理。

## 16:19:00 CST / viewer_engineer
- 完成内容: 执行 ROUND-009 第四批根级例外目录治理，将 `doc/ui_review_result/README.md` 明确为“活跃评审样本池说明”，不再让该目录承担正式模块语义。
- 完成内容: 为 `doc/ui_review_result/README.md` 补齐根级例外目录保留理由、进入条件、退出条件，并在 `ui_review_list.md` 标注其仅是活跃样本索引。
- 完成内容: 回写 `doc/core/project.md`、`doc/engineering/project.md` 与 ROUND-009 台账，将 `doc/ui_review_result/README.md` 的状态更新为 `aligned`。
- 遗留事项: 继续执行 `./scripts/doc-governance-check.sh`、`git diff --check`；ROUND-009 仍剩 `world-simulator` / `site` 一侧的高体量索引与静态镜像一致性收口。

## 16:05:43 CST / viewer_engineer
- 完成内容: 执行 ROUND-009 第五批 `world-simulator` 入口治理，将 `doc/world-simulator/README.md` 从纯模块索引改为带“从这里开始”的读者分流入口。
- 完成内容: 在模块入口中明确 `README.md`、`prd.md`、`project.md`、`prd.index.md`、`viewer-manual.manual.md` 与 `site/doc/{cn,en}/viewer-manual.html` 的职责边界，避免长表索引、仓库权威手册与公开静态镜像互相替代。
- 完成内容: 回写 `doc/world-simulator/project.md`、`doc/core/project.md`、`doc/engineering/project.md` 与 ROUND-009 台账，将 `doc/world-simulator/README.md` 状态更新为 `aligned`，并把静态镜像页显式登记为 `deferred`。
- 遗留事项: ROUND-009 仍剩 `doc/site/README.md` 与最终复审收口；继续执行 `./scripts/doc-governance-check.sh`、`git diff --check` 后提交并 landing。

## 16:14:33 CST / producer_system_designer
- 完成内容: 执行 ROUND-009 第六批 `site` 入口映射治理，将 `doc/site/README.md` 明确为 repo 内 site 模块治理入口，而不是对外 docs hub 的替代页。
- 完成内容: 在 site 模块入口中明确 `doc/site/README.md`、`site/doc/{cn,en}/index.html`、`doc/site/manual/`、`site/doc/{cn,en}/viewer-manual.html` 与仓库内 canonical Viewer 手册之间的职责边界。
- 完成内容: 回写 `doc/site/project.md`、`doc/core/project.md`、`doc/engineering/project.md` 与 ROUND-009 台账，将 `doc/site/README.md` 状态更新为 `aligned`。
- 遗留事项: ROUND-009 仅剩最终 `qa_engineer` 复审与关轮动作；继续执行 `./scripts/doc-governance-check.sh`、`git diff --check`，通过后提交并 landing。
