# Devlog

## 09:54:21 CST / runtime_engineer
- 完成内容: 在 `crates/oasis7/src/runtime` 为 `governance_main_token_controller_registry` 增加 `restricted_starter_claim_admin_account_ids`，要求 admin account 同时绑定既有 `controller_signer_policies`；`IssueRestrictedStarterClaimGrant / RevokeRestrictedStarterClaimGrant` 现在会在进入 grant 业务校验前先拒绝 registry 缺失、allowlist 为空或非 admin issuer。
- 完成内容: 补齐 runtime 定向测试与治理 registry 构造点，覆盖 `registry missing`、`non-admin issuer`、`revoke early reject` 和 admin policy 缺失校验；并同步回写 `gameplay-agent-claim-token-cost-2026-03-27.{prd,project}.md` 与 restricted grant liveops runbook。
- 遗留事项: 继续完成本轮 `env -u RUSTC_WRAPPER cargo test/check`、`./scripts/doc-governance-check.sh`、`git diff --check`，确认无编译/文档治理残留后再提交与 landing。

## 11:36:42 CST / runtime_engineer
- 完成内容: 为 restricted grant admin registry 增加正式 runtime governance 热更新动作 `UpdateRestrictedStarterClaimAdminRegistry`，并通过 `GovernanceEvent::RestrictedStarterClaimAdminRegistryUpdated` 落到 world-state；更新前要求 passed proposal 与 proposer 匹配，更新后仍强制命中现有 controller signer allowlist policy，不允许把 registry 退回成仅靠离线 import / 启动注入维护。
- 完成内容: 修正实现分层错误，移除误加到 `DomainEvent` 的 registry update 事件，开放 sibling module 所需的治理校验 helper 可见性，补齐 governance / agent_claims 定向测试，并回写 PRD / project / liveops runbook，把本轮任务登记为 `TASK-GAMEPLAY-AGC-015` / `TASK-GAME-053`。
- 遗留事项: 继续等待并确认本轮定向 `cargo test`、`./scripts/doc-governance-check.sh`、`git diff --check` 结果；通过后执行提交与 landing。
