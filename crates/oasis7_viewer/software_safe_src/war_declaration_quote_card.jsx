import { createSignal } from "solid-js";
const display = (value) => value == null || value === "" ? "—" : String(value);
const Metric = (props) => <div class="metric"><div class="metric__label">{props.label}</div><div class="metric__value">{props.value}</div></div>;
export function WarDeclarationQuoteCard(props) { const q = () => props.quote || {}; const tr = props.tr; const locale = () => props.locale; return <section class="panel panel--nested" data-testid="war-declaration-quote" data-quote-kind="preflight" data-state-fingerprint={display(q().state_fingerprint)}><div class="panel__header"><div class="panel__eyebrow">{tr(locale(), "提交前估价", "Before You Commit")}</div><div class="panel__title">{tr(locale(), "战争结果预估", "War Outcome Quote")}</div><div class="panel__meta-copy">{tr(locale(), "这是已签名的只读预估；不会宣战、预留资源、推进时间或创建战争。", "This is a signed read-only quote. It does not declare war, reserve resources, advance time, or create a conflict.")}</div></div><div class="panel__body stack"><div class="summary-grid"><Metric label={tr(locale(), "最小胜利强度", "Minimum winning intensity")} value={display(q().minimum_winning_intensity)} /><Metric label={tr(locale(), "预计持续", "Projected duration")} value={`${display(q().war_duration_ticks)} ${tr(locale(), "步", "ticks")}`} /><Metric label={tr(locale(), "预计胜者", "Projected winner")} value={display(q().likely_winner_before_action)} /><Metric label={tr(locale(), "胜负差", "Projected margin")} value={display(q().victory_margin_estimate)} /></div><div class="feedback-summary" data-testid="war-declaration-blocker">{`${tr(locale(), "冲突窗口", "Conflict window")}: ${display(q().reentry_cooldown_or_active_conflict_blocker)}`}</div><div class="feedback-summary feedback-summary--warn" data-testid="war-declaration-risk">{`${tr(locale(), "风险", "Risk")}: ${display(q().settlement_risk)}`}</div><div class="feedback-summary" data-testid="war-declaration-recommendation">{`${tr(locale(), "建议", "Recommended")}: ${display(q().why_this_war_is_worth_or_risky)}`}</div><button class="button button--secondary" type="button" disabled data-testid="war-declaration-submit-disabled">{tr(locale(), "宣战提交尚不可用", "War declaration submission unavailable")}</button></div></section>; }
export function WarDeclarationQuotePanel(props) { const [a, setA] = createSignal("alliance.red"); const [d, setD] = createSignal("alliance.blue"); const [i, setI] = createSignal("3"); const [busy, setBusy] = createSignal(false); const remote = () => props.requestState || {}; const stale = () => props.quote && (String(props.quote.actor_alliance_id) !== a().trim() || String(props.quote.target_alliance_id) !== d().trim() || String(props.quote.intensity) !== i().trim() || Number(props.quote.quoted_at_tick) !== Number(props.logicalTime)); async function request(event) { event.preventDefault(); setBusy(true); await props.requestWarDeclarationQuote(a(), d(), i()); setBusy(false); } return <section class="panel panel--nested" data-testid="war-declaration-quote-panel"><div class="panel__header"><div class="panel__title">{props.tr(props.locale, "战争结果预估", "War Outcome Quote")}</div></div><div class="panel__body stack"><form class="stack stack--compact" data-testid="war-declaration-quote-request-form" onSubmit={request}><label><span>{props.tr(props.locale, "进攻联盟", "Aggressor alliance")}</span><input aria-label={props.tr(props.locale, "进攻联盟", "Aggressor alliance")} value={a()} onInput={(e) => setA(e.currentTarget.value)} /></label><label><span>{props.tr(props.locale, "防守联盟", "Defender alliance")}</span><input aria-label={props.tr(props.locale, "防守联盟", "Defender alliance")} value={d()} onInput={(e) => setD(e.currentTarget.value)} /></label><label><span>{props.tr(props.locale, "强度", "Intensity")}</span><input aria-label={props.tr(props.locale, "强度", "Intensity")} type="number" min="1" max="10" value={i()} onInput={(e) => setI(e.currentTarget.value)} /></label><button class="button button--secondary" type="submit" disabled={busy() || remote().status === "pending"}>{props.tr(props.locale, "请求战争预估", "Request war quote")}</button></form>{remote().status === "error" ? <div role="alert" class="feedback-summary feedback-summary--warn" data-testid="war-declaration-unavailable">{String(remote().error).includes("unavailable") ? props.tr(props.locale, "M5 结算路径未提供权威只读预估。", "The M5 settlement path has no authoritative read-only quote.") : props.tr(props.locale, "无法获取战争预估。", "Could not get the war quote.")}</div> : null}{stale() ? <div role="status" class="feedback-summary feedback-summary--warn" data-testid="war-declaration-quote-stale">{props.tr(props.locale, "世界状态或输入已变化；当前预估已过期。", "World state or inputs changed; this quote is stale.")}</div> : null}{props.quote && remote().status !== "pending" ? <WarDeclarationQuoteCard quote={props.quote} locale={props.locale} tr={props.tr} /> : null}</div></section>; }
