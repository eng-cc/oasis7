# 时间线

本目录存放小说历史年表、分卷事件顺序和修订记录。当前可引用 `TL-001` 到 `TL-198` 事件锚点：第一至第三卷及第三卷后续覆盖 `TL-001` 到 `TL-158`，第四卷 scaffold 覆盖 `TL-159` 到 `TL-198`。

- `timeline.md`: 时间线主文件，已记录 `TL-001` 到 `TL-198`；第四卷部分仍为 scaffold，不等同已完成正文。
