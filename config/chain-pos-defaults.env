# Repo-owned default PoS timing profile.
# Rust binaries embed this file at compile time; shell scripts source it at runtime.

POS_SLOT_DURATION_MS=8000
POS_TICKS_PER_SLOT=10
POS_PROPOSAL_TICK_PHASE=9
POS_MAX_PAST_SLOT_LAG=256
