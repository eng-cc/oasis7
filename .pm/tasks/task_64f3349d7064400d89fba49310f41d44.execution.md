# task_64f3349d7064400d89fba49310f41d44 Execution Log

- task_uid: task_64f3349d7064400d89fba49310f41d44
- title: Follow up remaining workspace compile issues
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-engineering-workspace-compile-followup

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-16 23:10:00 CST / runtime_engineer
- 完成内容:
  - 在独立 worktree `/home/scc/worktrees/oasis7-engineering-workspace-compile-followup` 建立并启动 `task_64f3349d7064400d89fba49310f41d44`，以 `./scripts/cargo-dev.sh check --workspace --all-targets` 重新采集当前 workspace compile 状态。
  - 因当前分支基于 `main`，先重放两条前置本地修复：`crates/oasis7_proto/src/distributed_dht.rs` 的 7 个 `PeerRecord` 测试初始化补 `source_operator/source_asn = None`，以及 `crates/oasis7_net/src/libp2p_net/discovery.rs` 将 `CachedPeerRecord.ask_peer` 纳入失败日志 `via=<peer_id>` 诊断上下文。
  - 在前置修复之后，workspace 新的首个 blocker 变为 crates.io 依赖 `egui-winit 0.33.3`：其 `handle_platform_output` 对 `egui::PlatformOutput` 的模式匹配未容忍新增字段 `accesskit_update`。本任务在仓内 vendor 一份 `vendor-egui-winit-0.33.3/`，并通过 workspace 根 `Cargo.toml` 的 `[patch.crates-io]` 指向本地修补版，在解构末尾补 `..` 以恢复编译兼容。
  - 同步清理 `crates/oasis7` 下三处低成本 warning：把 `oasis7_pure_api_client`、`oasis7_web_launcher`、`oasis7_chain_runtime` 中仅供 tests 使用的导入/包装函数改为 `#[cfg(test)]` 暴露，去掉运行态未使用噪音。
  - 回写 `doc/engineering/project.md`，新增 `workspace-compile-followup (PRD-ENGINEERING-001)` 稳定任务项并绑定本 task trace。
- 验证结果:
  - `env -u RUSTC_WRAPPER cargo fmt --all`
  - `env -u RUSTC_WRAPPER cargo fmt --all -- --check`
  - `cargo tree -p egui-winit --edges normal,no-proc-macro` 显示当前解析到 `/home/scc/worktrees/oasis7-engineering-workspace-compile-followup/vendor-egui-winit-0.33.3`
  - `./scripts/cargo-dev.sh check --workspace --all-targets` 通过，workspace 不再报 compile error
- 遗留事项:
  - workspace 虽已无 compile error，但仍残留较多 warning，当前主要集中在 `crates/oasis7_client_launcher/**`（约 29 条）与 `crates/oasis7_viewer/**`（约 48 条）。这些多为测试专用 helper、反序列化响应字段或暂未接线的 UI 组件，未在本切片继续扩 scope 清理
