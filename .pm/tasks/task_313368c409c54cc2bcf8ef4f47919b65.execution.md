# task_313368c409c54cc2bcf8ef4f47919b65 Execution Log

- task_uid: task_313368c409c54cc2bcf8ef4f47919b65
- title: auto first-claim starter grant when pool funded
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-runtime-first-agent-claim-auto-grant

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-12 16:18:00 CST / runtime_engineer
- 完成内容: 先更新 `doc/world-runtime` 与 `doc/game` 规格/项目文档，把首笔 `slot-1` 启动 OC 从“运营审批后发放”改成“若 `restricted_starter_claim_liveops_pool` 有余额则在 `ClaimAgent` 时自动补足短缺”，并保留旧审批链路仅作兼容路径。
- 完成内容: 运行时实现已落地到 `ClaimAgent` 事件生成与应用链路；新增 `auto_restricted_starter_claim_amount(...)`、`AgentClaimed.auto_issued_restricted_amount`、claim 级 `claim_bond_restricted_source_treasury_bucket_id`，使首笔短缺可直接从 dedicated pool 自动补足，并在 bond 释放/回收时把 auto-funded restricted 部分退回原 treasury bucket。
- 完成内容: 快照/API/Viewer 已同步对齐；`player_gameplay.agent_claim` 与 `agent_claim/submit` preview 暴露 auto-funding 数额，`software_safe` 证据与契约测试改成“默认直领、无 pending approval 卡片”的新真值。
- 完成内容: 验证通过 `cargo check -p oasis7 --bin oasis7_chain_runtime --bin oasis7_viewer_live`、`cargo test -p oasis7 --lib first_slot_claim_auto_funds_shortfall_from_dedicated_pool_without_approval -- --nocapture`、`cargo test -p oasis7 --lib auto_funded_restricted_bond_refund_returns_to_dedicated_pool_on_release -- --nocapture`、`cargo test -p oasis7 --lib compat_snapshot_exposes_slot_1_auto_funding_from_dedicated_pool -- --nocapture`、`cargo test -p oasis7 --bin oasis7_chain_runtime agent_claim_submit_handler_previews_slot_1_auto_funding -- --nocapture`、`cargo test -p oasis7 --lib first_agent_claim_approval_issues_restricted_grant_and_enables_slot_1_claim -- --nocapture`、`node crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs`、`npm --prefix crates/oasis7_viewer run build:software-safe`、`cargo fmt --all`、`git diff --check`。
- 遗留事项: 旧的 approval request submit/approve/reject API 与运行时状态机仍保留兼容；如果后续要彻底下线运营审批面板与相关端点，需要另开专题清理。
