# task_e81e085cf2614cfd82e6c7bb411144d6 Execution Log

- task_uid: task_e81e085cf2614cfd82e6c7bb411144d6
- title: launcher 内置官方 bootstrap anchors 默认值
- owner_role: viewer_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-simulator-launcher-default-bootstrap-anchors

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-28 16:53:06 CST / viewer_engineer
- 完成内容: 将 launcher 默认 bootstrap anchors 收敛到 `crates/oasis7/src/launcher_bootstrap_peers.rs` 共享入口，内置 `/dns4/bootstrap1.oasis7.tech/tcp/5611` 与 `/dns4/bootstrap2.oasis7.tech/tcp/5612`，并同步接入 `oasis7_game_launcher`、`oasis7_web_launcher`、`oasis7_client_launcher` 的默认配置。
- 完成内容: 回写 `doc/world-simulator/launcher/game-client-launcher-native-web-control-plane-unification-2026-03-04.prd.md` 与 `.project.md`，明确“新用户零手填也有默认入网锚点，高级用户仍可覆盖”的产品边界。
- 完成内容: 验证通过 `env -u RUSTC_WRAPPER cargo fmt --all`、`env -u RUSTC_WRAPPER cargo test -p oasis7 default_chain_replication_bootstrap_peers_match_official_anchor_domains -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher parse_options_defaults -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher parse_options_defaults -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher launch_config_defaults_enable_llm -- --nocapture`、`git diff --check`。
- 遗留事项: 当前官方默认 anchors 先以域名+端口 multiaddr 形态内置，后续若稳定 peer id 固定下来，可再升级为带 `/p2p/<peer_id>` 的更强锚点格式。

## 2026-04-28 17:04:12 CST / viewer_engineer
- 完成内容: 在 required 门禁回归中发现 `oasis7_game_launcher` CLI 对 `--chain-replication-network-peer` 仍沿用“在默认 anchors 后追加”的旧语义；已修正为“首次显式传入时清空默认列表，后续 repeat flag 只累加显式值”，满足高级用户覆盖默认 peers 的约束。
- 完成内容: 补跑 `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher parse_options_ -- --nocapture`，并重新通过 `OASIS7_CI_RUN_OASIS7_REQUIRED_TESTS=true OASIS7_CI_RUN_CONSENSUS_TESTS=false OASIS7_CI_RUN_DISTFS_TESTS=false OASIS7_CI_RUN_VIEWER_TESTS=false OASIS7_CI_RUN_VIEWER_CONTRACT_TESTS=false OASIS7_CI_RUN_VIEWER_WASM_CHECK=false OASIS7_CI_RUN_LAUNCHER_WEB_BUILD=true ./scripts/ci-tests.sh required`。
- 遗留事项: 无新增功能遗留；若后续把官方 anchors 升级为带 `/p2p/<peer_id>` 的强锚点格式，需要同步验证 CLI 覆盖语义仍保持“显式值优先”。

## 2026-04-28 20:44:12 CST / viewer_engineer
- 完成内容: 按 PR #172 review comments 收口 bootstrap peers 文案与 API 稳定性：将 `DEFAULT_CHAIN_REPLICATION_BOOTSTRAP_PEERS` 从固定长度数组改为 slice，避免后续 anchor 数量变动时暴露破坏性 public type 变化。
- 完成内容: 在 `oasis7_game_launcher` flag 处理附近和 `print_help()` 中明确 `--chain-replication-network-peer` 的覆盖语义，即首次显式传参会替换 bundled defaults，而不是追加。
- 完成内容: 将 `launcher_bootstrap_peers`、web launcher config 校验、client launcher UI/错误提示统一改成 `/dns4/.../tcp/...` 与 `/ip4/.../tcp/.../p2p/<peer-id>` 都合法的表述，避免把 `/p2p/...` 误导成必填。
- 完成内容: 补跑 `env -u RUSTC_WRAPPER cargo test -p oasis7 launcher_bootstrap_peers::tests::default_chain_replication_bootstrap_peers_match_official_anchor_domains -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher parse_options_ -- --nocapture`、`git diff --check`。
- 遗留事项: 仍未把官方 anchors 升级为带 `/p2p/<peer_id>` 的强锚点格式；若后续补齐 peer id，需要再核对 UI/帮助文案与默认值保持一致。
