# task_81b8b3dfddd3487e9733600aad78806d Execution Log

- task_uid: task_81b8b3dfddd3487e9733600aad78806d
- title: fix release web strict gate script execute bit regression
- owner_role: qa_engineer
- worktree_hint: /home/scc/worktrees/oasis7-testing-release-gate-web-strict-permission-fix

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-22 09:26:34 CST / qa_engineer
- 完成内容: 复盘 GitHub Actions `Release Packages` run `26228413232`，确认唯一失败点为 `release-gate-web` 在执行 `./scripts/release-gate-web-strict.sh` 时因 tracked mode 漂移为 `100644` 触发 `Permission denied`。已恢复 `scripts/release-gate-web-strict.sh` 执行位，并新增 `scripts/check-script-executable-bits.sh`，把 release 关键 shell 脚本 mode/worktree 可执行性前移到 `scripts/ci-tests.sh` 的 commit/required gate；同步回写 `doc/testing/prd.md`、`doc/testing/project.md`、`testing-manual.md` 的 release 门禁口径。
- 完成内容: 验证通过 `./scripts/check-script-executable-bits.sh`、`bash -n scripts/check-script-executable-bits.sh scripts/ci-tests.sh scripts/release-gate-web-strict.sh`、`./scripts/release-gate-web-strict.sh --help`、`./scripts/ci-tests.sh commit`；其中首次 `commit` gate 因新 worktree 缺少 `crates/oasis7_viewer/node_modules` 报 `vitest: not found`，补跑 `npm ci --prefix crates/oasis7_viewer` 后重跑通过。
- 遗留事项: 修复尚未提交/开 PR/合入 `main`，因此 `v0.0.49` 的 release 失败状态不会自动恢复；需要提交并走 PR 合流后，基于新 `main` 重新触发下一次 release tag。
