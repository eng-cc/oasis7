# task_ec27ba7d019549d4b4692bd648a1cf09 Execution Log

- task_uid: task_ec27ba7d019549d4b4692bd648a1cf09
- title: required gate runtime support coverage
- owner_role: qa_engineer
- worktree_hint: /home/scc/worktrees/oasis7-engineering-required-gate-runtime-support-coverage

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-05-20 18:45:21 CST / qa_engineer
- 完成内容: 扩展 `scripts/plan-rust-required-scope.sh`、`scripts/ci-tests.sh`、`.github/workflows/rust.yml` 与 `scripts/prepare-task-pr.sh`，让 GitHub `required-gate` 在 changed-path planner 命中 `crates/oasis7_node/**` / `crates/oasis7_net/**` 时，除了基础 required shard，还会按 scope 注入 `oasis7_node --lib`、`oasis7_net --lib` 与 `oasis7_net --features libp2p --lib` support-crate 测试；同时保持裸跑 `./scripts/ci-tests.sh required` 仍是本地基础 required 语义，并把该口径同步回写到 `testing-manual.md` 与 `doc/engineering/project.md`。
- 完成内容: 新增 required-gate 覆盖后，修复并收敛了一组被新 shard 暴露出来的 support-crate 回归与脆弱测试假设：`runtime_network_replication_respects_topic_isolation` 改为显式关闭 local proposal 并只验证 peer-head topic 隔离；`runtime_network_replication_gap_sync_fetch_commit_success_cache_expires` 改为 TTL 过期后单次再取，避免循环刷新缓存；`runtime_gossip_replication_with_signature_applies_files` 为 sequencer 打开 `auto_attest_all_validators` 以消除全套件时序抖动；`libp2p_replication_network_request_round_robins_across_connected_peers` 改为验证短窗口内会覆盖两端 peer，而不是强依赖前两次请求必然不同。
- 完成内容: 修复 `crates/oasis7_net/src/libp2p_net/discovery.rs` 中 cached peer-record `NotFound` 场景的 60 秒 cooldown 卡死问题：当所有已连接 proxy peer 都已尝试完毕后，会清除该 target 的 cooldown，避免 rendezvous / cached discovery 在早期 miss 后被整段冷却期阻塞；对应测试补充了 cooldown 被清空的断言。与此同时，把 `oasis7_net` 两条 libp2p discovery 集成测试拆到新文件 `tests_libp2p_discovery.rs`，将稳定 contract 收敛为 “DHT 可读到 peer record” 与 “rendezvous 会产生日志/事件”，不再要求 10 秒内自动建立直接连接。
- 完成内容: 已完成 `rtk git diff --check`、`rtk env -u RUSTC_WRAPPER cargo test -p oasis7_node --lib`、`rtk env -u RUSTC_WRAPPER cargo test -p oasis7_net --lib`，以及最终 required-tier 验收命令：`rtk env OASIS7_CI_RUN_OASIS7_REQUIRED_TESTS=false OASIS7_CI_RUN_CONSENSUS_TESTS=false OASIS7_CI_RUN_DISTFS_TESTS=false OASIS7_CI_RUN_OASIS7_NODE_TESTS=true OASIS7_CI_RUN_OASIS7_NET_TESTS=true OASIS7_CI_RUN_OASIS7_NET_LIBP2P_TESTS=true OASIS7_CI_RUN_VIEWER_CONTRACT_TESTS=false OASIS7_CI_RUN_VIEWER_WASM_CHECK=false OASIS7_CI_RUN_LAUNCHER_WEB_BUILD=false ./scripts/ci-tests.sh required`；结果包含 doc governance、Windows path、Rust file size、fmt、`oasis7_node --lib`、`oasis7_net --lib` 与 `oasis7_net --features libp2p --lib` 全部绿色。
- 遗留事项: 继续执行 task closeout、commit 与 PR 标准收口。
