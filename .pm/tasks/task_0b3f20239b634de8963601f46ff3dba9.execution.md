# task_0b3f20239b634de8963601f46ff3dba9 Execution Log

- task_uid: task_0b3f20239b634de8963601f46ff3dba9
- title: Reduce next client-launcher compile warnings
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-engineering-workspace-compile-followup

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-16 23:20:00 CST / runtime_engineer
- 完成内容:
  - 为保持上一切片恢复的 workspace buildability，本任务复用当前未提交的 compile-cleanup worktree `/home/scc/worktrees/oasis7-engineering-workspace-compile-followup`，在其上新建 `task_0b3f20239b634de8963601f46ff3dba9`，继续收缩残余 warning。
  - 清理 `oasis7_client_launcher` 根模块下仅供 tests / wasm 使用的编译面：`launcher_core.rs` 中 `build_launcher_args`、`build_chain_runtime_args`、`probe_chain_status_endpoint` 改为 `#[cfg(test)]`；`self_guided.rs` 中 `resolve_primary_disabled_cta` 改为 `#[cfg(test)]`、`save_launcher_ux_state` 改为 `#[cfg(not(test))]`；`main.rs` 与 `main_ui_helpers.rs` 中 `FeedbackSubmit` request-domain / inflight 状态按 wasm 条件收窄。
  - 为 `explorer_window.rs` 与 `explorer_window_p1.rs` 中只作反序列化载体、当前 UI 不消费全部字段的响应 DTO 补最小 `#[allow(dead_code)]`，消除 `observed_at_unix_ms` 等字段级 warning。
  - 将 `transfer_entry.rs` 中除 `TransferDraft` 之外的历史 native transfer helper 全部收进 `#[cfg(test)]`，把运行态未接线的 helper 从主编译面移除。
  - 回写 `doc/engineering/project.md`，新增 `client-launcher-warning-followup (PRD-ENGINEERING-001)` 稳定任务项并绑定本 task trace。
- 验证结果:
  - `env -u RUSTC_WRAPPER cargo fmt --all`
  - `./scripts/cargo-dev.sh check --workspace --all-targets` 通过
  - 本轮结束后，workspace 日志里不再出现 `oasis7_client_launcher` warning；剩余 warning 已收敛到 `oasis7_viewer` 未接线 UI/文案 helper 集群
- 遗留事项:
  - 当前 workspace 剩余 warning 主要来自 `crates/oasis7_viewer/**`，包括 `player_hud.rs`、`timeline_panel.rs`、`world_overlay.rs`、`web_test_api/mod.rs` 等模块的未接线控件/辅助函数；若继续清理，应新开 viewer 切片单独处理
