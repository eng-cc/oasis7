# task_0abc42e0aa7c47d39972300f9ef9027b Execution Log

- task_uid: task_0abc42e0aa7c47d39972300f9ef9027b
- title: Repair release S10 soak gate failure
- owner_role: runtime_engineer
- worktree_hint: oasis7-testing-release-s10-soak-gate

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-13 21:37:54 CST / runtime_engineer
- 完成内容: 复盘 GitHub `Release Packages` run `24342863968`，确认 `release-gate-soak` 已从上一轮的 `s9` 失败切换为 `s10` 失败，且 `v0.0.40` 未发布；下载远端 summary/log 后锁定 canonical 失败命令为 `./scripts/s10-five-node-game-soak.sh --duration-secs 300 --no-prewarm --max-stall-secs 240 --max-lag-p95 50 --out-dir .tmp/release_gate_s10`。
- 完成内容: 先修复 `scripts/s10-five-node-game-soak.sh` 的五节点 replication 拓扑缺失，为每个节点显式注入 `--replication-network-listen` / `--replication-network-peer`；修复前本地 300 秒复现为 `last_error_samples=212; known_peer_heads_zero_samples=346`，observer 持续报 `libp2p-replication handler missing: /aw/node/replication/fetch-commit/1.0.0`，修复后该持续性签名消失。
- 完成内容: 首轮 snapshot review 指出两条问题：不能在 runtime 里把所有 `fetch-commit` `NetworkProtocolUnavailable` 统一降级为 waitable，也不能靠 startup 采样窗口吞掉所有启动期 `last_error`。已按 review 回退过宽 runtime 容错与宽采样窗口，改成脚本侧窄化判定：仅对 `s10-sequencer` 上精确匹配 `/aw/node/replication/fetch-commit/1.0.0` 的 bootstrap protocol-unavailable 额外计数，并设置 `>2` 仍然 fail；其他 `last_error` 全部维持原门禁语义。
- 完成内容: 最终本地验证通过：
  - `bash -n scripts/s10-five-node-game-soak.sh`
  - `env -u RUSTC_WRAPPER cargo build -p oasis7 --bin oasis7_chain_runtime`
  - `./scripts/s10-five-node-game-soak.sh --duration-secs 60 --base-port 7410 --no-prewarm --max-stall-secs 240 --max-lag-p95 50 --out-dir .tmp/release_gate_s10_fix_probe_v6` -> `run_dir=.tmp/release_gate_s10_fix_probe_v6/20260413-220149`, `status=ok`, `metric_gate=pass`, `last_error_samples=0`
  - `./scripts/s10-five-node-game-soak.sh --duration-secs 300 --base-port 7510 --no-prewarm --max-stall-secs 240 --max-lag-p95 50 --out-dir .tmp/release_gate_s10_fix_full_v5` -> `run_dir=.tmp/release_gate_s10_fix_full_v5/20260413-220304`, `status=ok`, `metric_gate=pass`, `last_error_samples=0`, `tolerated_bootstrap_fetch_commit_unavailable_samples=1`
- 遗留事项: 需要执行第二次 snapshot review、提交改动，并在合入后重新触发 GitHub `Release Packages` 验证远端 `release-gate-soak` 可越过 S10。
