# task_45e60b871cb942c59c382a6a9a57eb8d Execution Log

- task_uid: task_45e60b871cb942c59c382a6a9a57eb8d
- title: Implement P2PARCH-5 peer manager quarantine enforcement
- owner_role: runtime_engineer
- worktree_hint: oasis7-p2p-p2parch-5-peer-manager-quarantine

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-03 11:17:33 CST / runtime_engineer
- 完成内容: 已为 `P2PARCH-5` 新开独立 worktree `task/p2p-p2parch-5-peer-manager-quarantine`（`/home/scc/worktrees/oasis7-p2p-p2parch-5-peer-manager-quarantine`），并创建/启动 `task_45e60b871cb942c59c382a6a9a57eb8d`，把本轮目标收敛为“在现有 peer-manager health substrate 上补 active-set quarantine enforcement”，不与上一轮首切片复用 task。
- 完成内容: 已在 `crates/oasis7_net/src/libp2p_net.rs` 新增 quarantine helper，把 `suspect/blocked` health 真正接入连接状态机：health 刷新后会主动 `disconnect_peer_id` 已连接的 quarantined peer，且 `ConnectionClosed` / `OutgoingConnectionError` 会抑制这些 peer 的 failover / retry，避免 transport 状态机把被隔离 peer 立刻拉回 active set。
- 完成内容: 已补 `peer_requires_quarantine_matches_suspect_and_blocked` 与 `collect_quarantined_active_peers_only_returns_quarantined_active_set` 两条纯逻辑回归测试，并复跑既有 discovery dedupe 回归，确认本轮 quarantine 不破坏上一轮 `process_discovered_peer_record` 的首拨 gate 语义。
- 完成内容: 增量验证已通过 `env -u RUSTC_WRAPPER cargo test -p oasis7_net --features libp2p libp2p_net::tests::peer_requires_quarantine_matches_suspect_and_blocked -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net --features libp2p libp2p_net::tests::collect_quarantined_active_peers_only_returns_quarantined_active_set -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net --features libp2p libp2p_net::tests::process_discovered_peer_record_does_not_poison_dial_dedupe_for_suspect_peer -- --nocapture`、`env -u RUSTC_WRAPPER cargo check -p oasis7_net --features libp2p`。
- 遗留事项: 仍未接 operator / ASN 外部情报，也未把 `blocked` 升级成持久 banlist 或 release-gate artifact；本轮若继续扩展，优先补完整 `libp2p_net::tests` 套件与 event-flow 级回归，再决定是否把该切片收口成独立 commit。

## 2026-04-03 11:17:33 CST / runtime_engineer
- 完成内容: 已处理 subagent review finding，修正“坏连接先进入 active set 再污染同轮健康统计”的事件顺序问题。health refresh 现在先从原始 active set 识别 quarantine peer，再用剔除未准入连接后的 admitted active-set 重新计算对外发布的 health snapshot，避免 `/24`、relay-domain、relay budget 规则瞬时误伤其他健康 peer。
- 完成内容: active connection quarantine 现已收紧为“`suspect` 与已验证 hard-`blocked` 才主动断连”；`MissingPeerRecord` 这类待校验连接不再被立即踢掉，但也不会参与 admitted active-set 健康统计。
- 完成内容: 已新增 `peer_requires_active_quarantine_skips_missing_peer_record_block` 与 `admitted_active_transport_paths_excludes_non_active_peers_from_health_recompute` 回归测试，并复跑 `env -u RUSTC_WRAPPER cargo test -p oasis7_net --features libp2p libp2p_net::tests -- --nocapture` 与 `env -u RUSTC_WRAPPER cargo check -p oasis7_net --features libp2p`，确认 review 暴露的 active-set poisoning 风险已闭环。
- 遗留事项: 当前切片已具备再次进入 review/commit 流程的条件；后续若继续扩展 `P2PARCH-5`，优先方向仍是 operator / ASN 输入、持久 banlist/block artifact，以及更高层 mixed-topology fail-signature 套件。

## 2026-04-03 11:31:53 CST / runtime_engineer
- 完成内容: 已处理第二次 subagent review finding，修正“未准入 active peer 从 admitted active-set health view 中消失后，被 request-path 当成默认 candidate”的缺口。health refresh 现在保留未准入 active peer 的 raw `Blocked/Suspect` 状态给 request 选择与 quarantine 逻辑使用，同时只把 admitted active-set 用于其他 peer 的健康统计。
- 完成内容: 已把 active-set admission 改成“上一轮已准入 active-set + 新连接逐个试入”的模型。新连接若会让已准入 active peer 从 `Active` 退化，就不会被纳入 admitted active-set；这样坏 peer 被挡在门外时，不会把已准入健康 peer 一起拖成 `suspect`。
- 完成内容: 已新增 `refresh_peer_manager_healths_keeps_blocked_unverified_peer_out_of_admitted_set_but_in_health_map` 回归测试，并复跑 `env -u RUSTC_WRAPPER cargo test -p oasis7_net --features libp2p libp2p_net::tests -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net --features libp2p peer_manager -- --nocapture`、`env -u RUSTC_WRAPPER cargo check -p oasis7_net --features libp2p`，确认两个 subagent review finding 都已闭环。
- 遗留事项: 当前切片已具备最终 PM close / commit / landing 条件；后续若继续扩展 `P2PARCH-5`，优先方向仍是 operator / ASN 外部情报、持久 banlist/block artifact，以及更高层 mixed-topology fail-signature required/full 套件。
