# task_b518025590ae4e998726066eb862e17c Execution Log

- task_uid: task_b518025590ae4e998726066eb862e17c
- title: p2p subscribe ack and udp gossip hardening
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-subscribe-ack-udp-gossip-hardening

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-22 21:00:52 CST / runtime_engineer
- 完成内容: 修复 `libp2p` `subscribe()` 假成功路径，改为等待 runtime loop 的 gossipsub ack 后再返回 `NetworkSubscription`，并在订阅失败时同步向调用方回传 `WorldError`；同时收口 UDP gossip datagram 语义，新增超大 replication datagram 显式拒绝、把接收缓冲提升到完整 UDP payload 上限，并让 fan-out 在遇到单个 `send_to` 失败时继续尝试剩余 peer 后返回汇总 partial-failure。
- 完成内容: 补齐 `handle_command_subscribe_acknowledges_success`、`libp2p_smoke_request_response_and_pubsub_work_between_peers`、`gossip_endpoint_continues_broadcast_after_intermediate_send_error`、`gossip_endpoint_rejects_oversized_replication_datagram_before_send` 等定向回归，并回写 `doc/p2p/prd.md` / `doc/p2p/project.md` 的追踪条目。
- 遗留事项: 静态审查里提到的 peer-manager 全量重算与 clone-heavy debug snapshot 未在本任务处理，后续需独立 task 继续收口。

## 2026-05-22 21:22:10 CST / runtime_engineer
- 完成内容: 响应 PR #266 review comment，将 `gossip_udp::broadcast_bytes` 的失败聚合从“为每个失败 peer 分配 `Vec<String>`”收紧为 `failure_count + first_error`，避免 unreachable peer 多时的额外分配与格式化开销，同时保持 partial-failure 错误语义不变。
- 完成内容: 重跑 `env -u RUSTC_WRAPPER cargo test -p oasis7_node gossip_endpoint_ -- --nocapture`，确认 review 修复未破坏 UDP gossip 路径。
- 遗留事项: 修复提交后仍需 rebase/push 并显式 resolve review thread，再复查 PR 的 `mergeStateStatus` / `reviewDecision`。
