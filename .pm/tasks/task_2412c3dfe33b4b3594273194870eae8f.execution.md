# task_2412c3dfe33b4b3594273194870eae8f Execution Log

- task_uid: task_2412c3dfe33b4b3594273194870eae8f
- title: rebuild public_testnet from governed bootstrap artifacts
- owner_role: tpm
- worktree_hint: null

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
  - Action: ...
  - Validation Command: ...
  - Expected Result: ...
  - Actual Result: ...
  - Blocker / Next Action: ...
-->

## 2026-06-06 11:14:00 CST / tpm
- 完成内容: 在 canonical live rebuild worktree 内启动正式执行；将来自 signer-truth/bootstrap-artifact 任务链的 governed bootstrap 真值复制进当前 worktree，并把任务 `task_2412c3dfe33b4b3594273194870eae8f` 迁移到 `committed` 后通过 `workflow-report --phase start` 进入执行态。
- 完成内容: 复核本地 fresh governed bootstrap runtime hash；`target/debug/oasis7_chain_runtime` 当前 sha256 为 `a3d825a64309909fc362f332e5a07b7f57dbeae4b58447874459036bd792c973`，与 `doc/testing/evidence/public-testnet-governed-bootstrap-bundle-2026-06-06.json` 已冻结 bundle hash 一致。
- 完成内容: 盘点 validator pair 现网基线:
  - `root@39.104.204.172` 现有 stack root `/opt/oasis7/p2p-testnet`，活跃服务至少包括 `oasis7-testnet-sequencer.service`、`oasis7-triad-sequencer.service`、`oasis7-testnet-faucet.service`。
  - `root@39.104.205.67` 现有 stack root `/opt/oasis7/p2p-testnet`，活跃服务至少包括 `oasis7-testnet-storage.service`、`oasis7-triad-storage.service`。
  - 两台主机当前 `config/node.env` 仍指向旧 world `oasis7-public-testnet-parallel-20260518` 与 live-candidate manifest，而非新的 governed bootstrap network id / manifest。
- 完成内容: 记录 runtime drift:
  - storage host `/opt/oasis7/p2p-testnet/current/bin/oasis7_chain_runtime` sha256 = `4763a211fd3e9fb71322cff786ae922285f53bfe6db2160e906fba3ad1e7ce7a`
  - storage host `/opt/oasis7/p2p-testnet/bin/oasis7_chain_runtime` sha256 = `94d461dc8a0319a6564ff32f7f7d4927b9e9eba529c440dd028fa89fcbf27733`
  - 二者均与 fresh governed bootstrap bundle hash `a3d825...` 不一致；sequencer host 后续 hash 复查因 password auth 抖动未完成，但应按同一 drift 风险处理。
- 完成内容: 盘点 observer 侧阻塞:
  - 本机 launchd `com.oasis7.testnet-fourth-local` 仍绑定旧 worktree 与旧 world/validator 参数，`last exit code = 1`，不应带入新 rebuild。
  - `sshpass` 访问 `scc@192.168.1.4` 失败：`Permission denied (publickey,password)`；当前无法对该 observer 做 destructive reset 或 fresh attach。
- Validation Command: `./scripts/pm/move-task.sh --task-uid task_2412c3dfe33b4b3594273194870eae8f --to-status committed && ./scripts/pm/workflow-report.sh --phase start --role tpm --task-uid task_2412c3dfe33b4b3594273194870eae8f && shasum -a 256 target/debug/oasis7_chain_runtime`
- Expected Result: 任务进入 committed/start 执行态，并确认本地 runtime 与 frozen bundle hash 对齐。
- Actual Result: 符合预期；任务状态进入执行态，本地 runtime hash 已确认对齐 fresh governed bootstrap bundle。
- Blocker / Next Action: 下一步对两台 ECS 先做 artifact staging + runtime 对齐，再执行 destructive reset；`192.168.1.4` observer 仍等待可用凭据或替代接入路径。

## 2026-06-06 11:31:00 CST / tpm
- 完成内容: 对 validator pair 做了第一次 live destructive bring-up 试跑，并把 blockers 收敛到跨平台 bundle truth。
- 完成内容: 已完成的 live 操作:
  - 两台 ECS 都 staged 了 governed bootstrap manifest / genesis / validator registry / bootstrap peers。
  - 两台 ECS 的 `config/node.env` 已切到 fresh governed bootstrap world id `oasis7-public-testnet-governed-20260606`；storage host 的 `NODE_ROLE` 也已改正为 `storage`。
  - 旧 `oasis7-testnet-*` / `oasis7-triad-*` 服务及 sequencer 侧 faucet 已停止；旧 live data 目录已 destructive reset。
  - 额外补齐了 manifest repo-relative runtime refs 所需的 `config/doc/testing/evidence/...` 树。
- 完成内容: 第一次失败根因已经确认不是链逻辑，而是 artifact 平台错配：
  - 最初上传到 ECS 的 runtime 是本机 `Mach-O arm64`，systemd 报 `Exec format error`。
  - 这证明 `public-testnet-governed-bootstrap-bundle-2026-06-06.json` 最初冻结的是本机 darwin binary，而不是 validator host 需要的 Linux x86_64 binary。
- 完成内容: 已将当前 worktree 里的 governed bootstrap bundle 重生为 Linux truth：
  - 从 ECS 现有 governed-registry release 拉回 Linux x86_64 `oasis7_chain_runtime` 样本到 `.tmp/public-testnet-governed-bootstrap-live/oasis7_chain_runtime.linux-x86_64`
  - 用 `./scripts/release-candidate-bundle.sh create --allow-dirty-worktree` 重新生成 `doc/testing/evidence/public-testnet-governed-bootstrap-bundle-2026-06-06.json`
  - 本地 `release-candidate-bundle validate` 已通过
- 完成内容: 在 Linux bundle truth 修正后，sequencer live bring-up 又前进了一层：
  - manifest/bundle path mismatch 已消除
  - 当前残留失败变成 runtime 首启 world bootstrap:
    - `DistributedValidationFailed: latest state root mismatch tick=1 expected=d4570a... found=2ca107...`
  - 相关历史证据 `doc/testing/evidence/shared-network-shared-devnet-triad-reset-recovery-2026-05-23.md` 已复核；我们也按证据额外清理了 `output/node-distfs/<node>` 与 `reward-runtime-execution-bridge-state.json`，但 testnet governed bootstrap 仍复现 tick=1 mismatch。
- Validation Command: `./scripts/release-candidate-bundle.sh create ... --runtime-build-ref .tmp/public-testnet-governed-bootstrap-live/oasis7_chain_runtime.linux-x86_64 --allow-dirty-worktree && ./scripts/release-candidate-bundle.sh validate --bundle doc/testing/evidence/public-testnet-governed-bootstrap-bundle-2026-06-06.json`
- Expected Result: bundle truth 改为可在 ECS 执行的 Linux runtime，并可在本地通过 bundle validate。
- Actual Result: 符合预期；bundle validate 通过。live ECS bring-up 进一步证明当前主 blocker 已从 artifact path/hash 问题收敛到 runtime cold-boot world mismatch。
- Blocker / Next Action: 用户已授权切换到“建 PR -> 走 CI 产出跨平台 package”路径。下一步不再继续手推 ECS 上的混合真值，而是基于当前证据准备 PR / CI packaging：
  1. 让 CI 产出当前源码对应的 Linux testnet package/runtime；
  2. 用 CI 产物替换 ECS 现有 2026-06-05 governed-registry release；
  3. 再次执行 validator-pair cold boot，确认 `tick=1` mismatch 是否随当前源码 Linux runtime 消失。

## 2026-06-06 14:49:00 CST / tpm
- 完成内容: 用“预生成 execution world + Linux CI/runtime truth”取代 empty-world 首启注入路径，成功把 validator pair 从 crash loop 拉到真实出块。
- 完成内容: 关键 live 修复动作:
  - 本地通过 `oasis7_governance_registry_import` + `oasis7_governance_registry_audit` 生成并验证 governed execution world：
    - `.tmp/public-testnet-governed-bootstrap-world-generated/world`
  - 将该 world 拷到 sequencer `39.104.204.172:/opt/oasis7/p2p-testnet/data/execution-world`
  - sequencer 随后成功启动，脱离此前 `tick=1 latest state root mismatch`
  - 将同一份 world 拷到 storage `39.104.205.67:/opt/oasis7/p2p-testnet/data/execution-world`
  - storage 首次仍失败；进一步确认失败不是 world 本身，而是 remote nested bundle truth
    `/opt/oasis7/p2p-testnet/config/doc/testing/evidence/public-testnet-governed-bootstrap-bundle-2026-06-06.json`
    仍保留旧 runtime hash `a3d825...`
  - 用 repo 当前 Linux bundle truth 覆盖该 nested bundle 文件后，storage 成功启动
- 完成内容: validator pair 当前 live 状态:
  - sequencer `/v1/chain/status`：`committed_height=15`, `network_committed_height=15`, `connected_peer_count=1`, `last_error=null`
  - storage `/v1/chain/status`：`committed_height=15`, `network_committed_height=15`, `known_peer_heads=1`, `connected_peer_count=1`, `last_error=null`
  - 两端 `output/node-distfs/*/node_pos_state.json` 也已推进到 `committed_height=11+`，确认不是假活性
- Validation Command: `scp pre-generated world -> ECS`, `systemctl restart oasis7-testnet-{sequencer,storage}.service`, `curl http://127.0.0.1:663{1,2}/v1/chain/status`, `cat output/node-distfs/*/node_pos_state.json`
- Expected Result: validator pair 至少能越过 height 0，证明 fresh governed bootstrap 可在 live ECS 上形成真实链进展。
- Actual Result: 符合预期；两台 validator 都已持续推进到 height 15。剩余未绿项主要是 readiness 表面的 `replication_recent_errors` / `p2p_reachability_degraded`，以及后续 observer attach。
- Blocker / Next Action:
  1. 复核 sequencer 侧 `known_peer_heads=0` / readiness 残留项是否只是瞬时观测或还需额外 bootstrap peer/p2p 调整；
  2. 开始 clean bootstrap observer：优先本机 `triad-testnet-local` / `triad-testnet-fourth-local`，最后再处理仍缺凭据的 `scc@192.168.1.4`。

## 2026-06-06 15:00:00 CST / tpm
- 完成内容: 对本机 `triad-testnet-fourth-local` 做了 fresh governed bootstrap attach 试跑。
  - 在当前 canonical worktree 下创建了新的 local run dir：
    - `.tmp/testnet-fourth-node-bootstrap/`
  - 复用旧 fourth-local identity `config.toml`
  - 复制 pre-generated governed world 作为本地 execution world 起点
  - 生成了本机专用 manifest / runtime bundle truth，避免直接拿 Linux validator bundle 在 macOS 上触发 hash mismatch
- 完成内容: fourth-local 已能成功启动并连到两台 validator：
  - `node_id=triad-testnet-fourth-local`
  - `local_peer_id=12D3KooWAkDbJby8wGRhnESJYFR7q6DWfNXQ7Ea2ZrZvehezj47s`
  - `connected_peers=2`
- 完成内容: 当前 observer 主 blocker 已重新收敛为 replication requester authorization：
  - runtime / status 稳定报错：
    - `fetch-commit request requester is not authorized: requester_public_key_hex=5a7b71571c8ed100be200a1addbdad57dee6a3c86299cca666184403696bdd16`
  - 已把该 key 追加进两台 validator ECS 的 `REPLICATION_REMOTE_WRITERS_CSV`，并滚动重启 `oasis7-testnet-storage.service` 与 `oasis7-testnet-sequencer.service`
  - remote `logs/last-command.sh` 已确认两台 validator 实际启动命令都带上了 `--replication-remote-writer-public-key 5a7b...`
  - 但 fourth-local 仍复现同一 unauthorized fetch-commit 失败，说明阻塞已经不是“env 少一把 key”这么浅，而更像 runtime replication auth plane / requester-key source 仍有更深的不一致
- Validation Command: macOS local observer manual launch + `curl http://127.0.0.1:19083/v1/chain/status` + ECS `grep REPLICATION_REMOTE_WRITERS_CSV` / `logs/last-command.sh`
- Expected Result: fourth-local 至少能越过 fetch-commit authorization，开始从 validator pair 跟进当前高度。
- Actual Result: 未达预期；observer transport/connectivity 已通，但 gap-sync 仍被 unauthorized requester key 挡住。
- Blocker / Next Action:
  1. 查清 `requester_public_key_hex=5a7b...` 的真实来源，与 validator replication allowlist 实际匹配面是否一致；
  2. 在该 auth plane 打通前，`triad-testnet-fourth-local` 和 `triad-testnet-local` 都会落在同一 blocker 上；此时继续机械加 observer 收益不高。

## 2026-06-06 15:25:00 CST / tpm
- Runtime Debug Slice Contract:
  - Owner role: `runtime_engineer`
  - Intended model: inherited/default subagent runtime; actual model: inherited/unverified if dispatched through available multi-agent tooling.
  - Mandatory context checklist: canonical worktree `task/p2p-public-testnet-governed-bootstrap-rebuild`; PR #361; public_testnet validator pair already advances to height 15 with pre-generated governed execution world; local observer connects to both validators but gap sync fails with `fetch-commit request requester is not authorized: requester_public_key_hex=5a7b71571c8ed100be200a1addbdad57dee6a3c86299cca666184403696bdd16`; both ECS validator commands were observed to include `--replication-remote-writer-public-key 5a7b...`.
  - Question: determine whether the replication fetch authorization path has a code-level config/handler mismatch, and identify the minimum patch/test surface needed to distinguish real allowlist loss from stale/incorrect live peer state.
  - Sink: runtime_engineer finding must be integrated back into this execution log before claiming the blocker is fixed.
- TPM Evidence So Far:
  - `NodeReplicationConfig::authorize_fetch_commit_request` authorizes by normalized `requester_public_key_hex` membership in `remote_writer_allowlist`.
  - `oasis7_chain_runtime` combines governed validator signer keys with every `--replication-remote-writer-public-key` value before building `NodeReplicationConfig`.
  - `NodeRuntime::start` derives `effective_replication_config` and passes that same config into `register_replication_fetch_handlers`, so the first suspected design bug (fetch handler using stale pre-default config) is not present in current source.
- Next Action: add bounded diagnostics/regression around fetch-handler authorization so live failures expose the serving node allowlist fingerprint/count, then rerun local tests and use the improved binary/package path for validator retest.

## 2026-06-06 15:45:00 CST / tpm
- Runtime Engineer Slice Result:
  - Source: existing subagent `019e9695-f8ef-7991-b46b-65a317b87370` / Euclid.
  - Finding: current source did not show an explicit-key propagation bug from `oasis7_chain_runtime` into `NodeRuntime::start` and `register_replication_fetch_handlers`; the live failure is more consistent with deployed binary/config/process freshness or wrong live handler state than source dropping the key.
  - Recommendation: add a through-network fetch handler regression proving an explicit requester key bound into the serving fetch handler is accepted, and an unlisted requester is rejected; add diagnostic count/fingerprint to authorization failure.
- Root Design Fix Applied:
  - Split replication read/fetch authorization from remote writer authorization:
    - `remote_writer_allowlist` remains for signed gossip/write records.
    - new `fetch_requester_allowlist` authorizes signed fetch-commit/fetch-blob requesters.
    - governed validator signer keys default into both sets when no explicit set is present.
    - CLI-provided extra requester keys now enter fetch requester allowlist only, instead of expanding remote writer rights.
  - Added authorization failure diagnostics:
    - `authorized_fetch_requester_count`
    - `authorized_fetch_requester_fingerprint`
  - Added regressions:
    - fetch requester allowlist does not grant writer authorization.
    - runtime fetch handler accepts explicit fetch requester without writer access and rejects an unlisted requester.
- Validation Commands:
  - `cargo fmt --check`
  - `./scripts/cargo-dev.sh test -p oasis7_node replication::tests`
  - `./scripts/cargo-dev.sh test -p oasis7_node runtime_fetch_handlers_authorize_explicit_fetch_requester_without_writer_access`
  - `./scripts/cargo-dev.sh test -p oasis7_node replication_gap_sync_backfills_when_consensus_height_already_advanced`
  - `./scripts/cargo-dev.sh test -p oasis7_node proposal_ahead_updates_replication_gap_sync_target_height`
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime build_replication`
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime build_node_replication_config_uses_storage_profile_budget`
- Actual Result: all listed commands passed after the final patch state; two earlier attempts used multiple Cargo filters or wrong module filters and returned argument error / 0 tests, then were rerun with valid filters.
- Next Action: inspect diff, commit/push the fix to PR #361, trigger CI packaging, then deploy the fresh Linux package/runtime to validator pair before retesting fourth-local observer.

## 2026-06-06 17:00:00 CST / tpm
- 完成内容: 将 fetch requester authorization fix 从 PR #361 CI package 部署到 public_testnet，并把四节点网络跑起来。
- CI Package Truth:
  - Testnet Packages run: `27056762815`
  - Commit: `036b1582201e1334ed8592a88bfbb357f80d6258`
  - Artifact: `testnet-package-linux-x64-0.0.0+testnet.5.036b1582201e`
  - Linux runtime sha256: `e38a0cd405c214b42248a39b45308ea80401185e5042260b46df5bbea9f2ceaf`
  - `linux-x64-SHA256SUMS` verified locally.
- Deployment:
  - Sequencer `39.104.204.172` current release now points to `/opt/oasis7/p2p-testnet/releases/036b158-testnet5-fetch-requester-20260606-1615`.
  - Storage `39.104.205.67` current release now points to `/opt/oasis7/p2p-testnet/releases/036b158-testnet5-fetch-requester-20260606-1615`.
  - Both validator nested `public-testnet-governed-bootstrap-bundle-2026-06-06.json` files were updated to expected runtime hash `e38a0cd4...`.
  - Added second local observer requester key `b0cc1a064fdc564089a48cb1044d8d1285113a69b7a6a25d4830306df8630399` to both validator `REPLICATION_REMOTE_WRITERS_CSV` entries and restarted services.
- Local Observers:
  - `triad-testnet-fourth-local` uses local status `127.0.0.1:19083`.
  - `triad-testnet-local` uses local status `127.0.0.1:19082`.
  - Both observers use local macOS runtime sha256 `2accc7a3c239fa4581126ddb9e08136a035bc6eceb04b05b2b48e35b689fa989`.
  - A fresh observer from genesis hit state-sync guard once the validator network was already ~300 heights ahead. To complete bring-up, bootstrapped both observer replication roots from storage validator `node-distfs` height 335, then let them continue from live peers.
- Verification Snapshot:
  - Sequencer: `running=true`, `last_error=null`, `committed_height=338`, `network_committed_height=338`, `replication_persisted_height=338`, `connected_peer_count=2`.
  - Storage: `running=true`, `last_error=null`, `committed_height=338`, `network_committed_height=338`, `replication_persisted_height=338`, `connected_peer_count=2`.
  - Fourth local observer: `running=true`, `last_error=null`, `committed_height=339`, status endpoint live on `127.0.0.1:19083`.
  - Triad local observer: `running=true`, `last_error=null`, `committed_height=339`, status endpoint live on `127.0.0.1:19082`.
- Validation Commands:
  - `gh run view 27056762815 --json status,conclusion,jobs`
  - `shasum -a 256 -c linux-x64-SHA256SUMS`
  - ECS `sha256sum /opt/oasis7/p2p-testnet/current/bin/oasis7_chain_runtime`
  - ECS `curl http://127.0.0.1:663{1,2}/v1/chain/status`
  - Local `curl http://127.0.0.1:1908{2,3}/v1/chain/status`
- Residual Alerts / Next Action:
  - Four nodes are live and height-aligned, but readiness still reports transport/fresh-peer-head warnings (`replication_recent_errors`, `consensus_peer_head_unavailable/stale`, and validator public reachability warnings).
  - Root follow-up: formalize observer bootstrap/state-sync artifact instead of manually pre-seeding `node-distfs` from storage once the network is hundreds of heights ahead.

## 2026-06-06 17:23:16 CST / tpm
- Runtime Stability Slice Contract:
  - Owner role: `runtime_engineer`.
  - Intended model: inherited/default subagent runtime; actual dispatched model: inherited/unverified because current tool surface does not expose a fresh bounded subagent dispatch call in this turn.
  - Mandatory context checklist: canonical worktree `task/p2p-public-testnet-governed-bootstrap-rebuild`; PR #361; four-node public_testnet is height-aligned with `last_error=null`; readiness remains not ready because alerts include `replication_recent_errors`, `replication_transport_unstable`, `p2p_reachability_degraded`, `consensus_peer_head_unavailable/stale`; live recent errors are dominated by AutoNAT probe failures (`NoServer` / `UnsupportedProtocols`), connection churn, static bootstrap peer-record/discovery-source warnings, and IPv4 subnet concentration for two local observers behind the same NAT.
  - Question: identify and patch code-level design issues that make a healthy small governed testnet appear unstable, using mainstream chain patterns: control-plane reachability probes must not be scored as replication data-plane failures; static/bootstrap/trusted peers need separate admission semantics from open discovery peers; readiness should only fail on active blocking conditions, not stale diagnostic strings.
  - Sink: this execution log must record root cause, patch surface, and verification before claiming stability remediation.
- TPM Evidence So Far:
  - `status_payload.rs` computes `recent_replication_error_count = replication.recent_errors.len()` and emits `replication_recent_errors` for any non-empty error list.
  - `classify_transport_stability` infers severity from strings and counts `unsupported`, `unavailable`, `connection closed`, and timeout text, so AutoNAT/control-plane failures can lower replication transport score.
  - `libp2p_net.rs` pushes every AutoNAT event string into the same `event_errors` buffer used by replication recent errors.
  - `peer_manager.rs` hard-blocks missing peer records and IPv4 subnet concentration even when the topology is a small static/bootstrap testnet; live evidence shows local observers behind the same NAT trip this path.
- Planned Patch:
  1. Reclassify AutoNAT probe failures as reachability diagnostics only; keep public/private reachability state, but do not write AutoNAT event text into replication recent errors.
  2. Add stable filtering/classification around replication diagnostic errors so readiness only gates on data-plane/blocking replication failures.
  3. Treat static/bootstrap peer-record/discovery-source gaps as soft health issues and avoid hard quarantine for bootstrap trusted paths; keep hard blocks for real concentration/security budgets outside the small static topology.
- Validation Plan: targeted unit tests for AutoNAT/readiness filtering and peer-manager static bootstrap admission, then `cargo fmt --check`, relevant `oasis7_net` and `oasis7_chain_runtime` tests, plus `git diff --check`.

## 2026-06-06 18:08:00 CST / tpm
- 完成内容: 从代码层面修复 public_testnet 四节点稳定性误报的第一组根因：
  - AutoNAT 事件不再写入 libp2p replication `recent_errors`，只更新 reachability snapshot。
  - chain runtime observability 增加 diagnostic replication error 过滤；`replication_recent_errors` readiness gate 只看 blocking transport/protocol errors，保留总 `recent_replication_error_count` 作为诊断计数。
  - peer manager 增加 `min_active_peers_for_share_limits`，默认 4；小于 4 个 active peer 的静态/小网拓扑不再因 IPv4 subnet / relay domain / operator / ASN share-limit 被硬阻断。
  - active-set candidate 逻辑同步使用同一 share-limit 阈值，避免候选接入阶段和全量 recompute 语义分叉。
  - active quarantine 对 `MissingPeerRecord` / `InsufficientActiveDiscoverySources` / `SingleSourceDiscovery` 都按 record-exchange pending 软问题处理，不再把 bootstrap/discovery 暂缺升级成主动隔离。
- 遗留事项: 仍需用新代码构建 CI package 并部署到 public_testnet 后复查 live readiness；本轮没有改 peer-head heartbeat / observer bootstrap artifact，`consensus_peer_head_unavailable/stale` 和 formal state-sync artifact 仍是下一组根因项。
- Action: Implemented root stability patch for P2P control-plane/data-plane separation and small governed-testnet peer admission.
- Validation Command:
  - `cargo fmt --check`
  - `./scripts/cargo-dev.sh test -p oasis7_net peer_manager -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_net active_set_candidate -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_net --lib`
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime observability -- --nocapture`
  - `git diff --check`
  - `./scripts/pm/lint.sh && ./scripts/doc-governance-check.sh`
- Expected Result: Targeted P2P/observability tests pass; formatting and diff checks pass; PM/doc checks either pass or expose only unrelated pre-existing task-log debt.
- Actual Result:
  - Passed: `cargo fmt --check`.
  - Passed: `oasis7_net peer_manager` with 11 tests.
  - Passed: `oasis7_net active_set_candidate` with 5 tests.
  - Passed: `oasis7_net --lib` with 144 tests.
  - Passed: `oasis7_chain_runtime observability` with 14 tests, including new AutoNAT/diagnostic filtering regression.
  - Passed: `git diff --check`.
  - Passed: `./scripts/doc-governance-check.sh`.
  - `./scripts/pm/lint.sh` failed because multiple existing `.pm/tasks/*.execution.md` entries, including historical entries outside this patch, lack required template fields; no code/test failure was indicated.
- Blocker / Next Action: Commit/push this patch to PR #361, trigger CI package build, deploy fresh runtime to validators and local observers, then sample live readiness to see whether remaining warnings are only peer-head heartbeat/state-sync artifact follow-up.

## 2026-06-06 18:50:00 CST / tpm
- 完成内容: 继续收口 public_testnet live stability 根因，修复第二组 readiness / 启动接线问题：
  - `status_payload.rs` 将 `libp2p connection established` / `libp2p routing updated` / `libp2p transport active` 归类为 replication diagnostic，不再降低 transport score 或触发 `replication_recent_errors`。
  - `readiness.failed_gates` 只在 `ready=false` 时承载实际失败项；`ready=true` 时保留 non-blocking warnings 于 `observability.alerts`，不再输出伪失败 gate。
  - `scripts/p2p-triad-node-start.sh` 透传 `P2P_*` reachability truth 到 runtime CLI；两台 ECS 的 `node.env` 已经有 `P2P_DETECTED_REACHABILITY=public` / `P2P_ACCEPT_PUBLIC_ENTRY=true` 等配置，但旧脚本没有接线，导致 validator readiness 误报 `p2p_reachability_degraded`。
  - 顺手修复 start script 在空 CSV optional arrays 下的 `set -u` unbound variable，以及 macOS dry-run 的 `date -Is` 非跨平台噪声。
- Live Verification:
  - 用本地 debug runtime sha256 `92bee267ff24a4b37cc1e69e2645da3ffceb9f7036f2da5ac67b01ab0763154a` 启动 `triad-testnet-local` 与 `triad-testnet-fourth-local`。
  - 两个本地 observers 在高度 `603` 与 validator 对齐，`last_error=null`，`transport_stability_score=100`，`ready=true`，`failed_gates=[]`。
  - 远端 validators 仍运行上一版 Linux package；当前仍会把 active transport diagnostic 计为 blocking，需等待本次 commit 触发新 CI package 后部署。
- Validation Command:
  - `bash -n scripts/p2p-triad-node-start.sh`
  - `OASIS7_NODE_START_DRY_RUN=1 APP_ROOT=<tmp> ENV_FILE=<tmp>/node.env BIN=$PWD/target/debug/oasis7_chain_runtime scripts/p2p-triad-node-start.sh`
  - `cargo fmt --check`
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime observability -- --nocapture`
  - `git diff --check`
- Actual Result:
  - Passed: start script syntax.
  - Passed: dry-run command includes `--p2p-detected-reachability public`, `--p2p-detected-hole-punch viable`, `--p2p-detected-relay-unavailable`, `--p2p-detected-probe-stable`, `--p2p-accept-public-entry`, `--p2p-deployment-mode hybrid`, and metadata flags.
  - Passed: `cargo fmt --check`.
  - Passed: `oasis7_chain_runtime observability` with 14 tests.
  - Passed: `git diff --check`.
- Next Action: Commit/push second stability patch, trigger `Testnet Packages`, deploy fresh Linux artifact to both validator ECS nodes, install updated `start-node.sh`, restart validators, then resample four-node readiness.

## 2026-06-06 19:25:00 CST / tpm
- 完成内容: 部署 package `0.0.0+testnet.7.8bd094c94f78` 后继续追 live validator readiness，确认 reachability 接线已修复，但 transport readiness 仍被历史连接 churn 误阻断：
  - Sequencer/storage 均切到 Linux runtime sha256 `1f399627c0520a10e1e33080956bc338ef7a83aeb1506ab7f31f0f270e46fb46`，服务 active。

## 2026-06-07 14:55:00 CST / tpm
- 完成内容: 将 `doc/p2p/blockchain/p2p-public-testnet-governed-bootstrap-2026-06-06.runbook.md` 从原则型说明重写为严格可执行 SOP。
- 完成内容: 新 runbook 明确拆分了 frozen repo truth 与 deployment truth，并把以下漂移点上升为强制 preflight / phase gate：
  - `config/node-keypair.toml` 删除或轮换会触发 signer truth 重建
  - libp2p bootstrap peer id 必须从 live validator 读取，不能沿用旧证据
  - observer 超过低高度阈值后必须走 verified seed/state-sync，不允许继续依赖空 world 自举
  - seed/state-sync artifact 必须包含 `store/blobs/` 闭包，避免 `BlobNotFound`
- 完成内容: 新 runbook 采用固定阶段：
  - Phase A preflight and truth capture
  - Phase B build deployment truth
  - Phase C stage and rebuild validators
  - Phase D verify validator pair
  - Phase E build verified observer seed/state-sync bundle
  - Phase F attach observers
  - Phase G final health verification
  - Phase H failure handling and rollback
- 完成内容: 将这次 live rebuild 暴露出的关键失败边界都写成了 hard stop conditions：
  - signer binding mismatch
  - libp2p `WrongPeerId`
  - empty-world `height 1` execution mismatch
  - observer `BlobNotFound`
  - fetch requester authorization failure
- Validation Command:
  - `sed -n '1,260p' doc/p2p/blockchain/p2p-public-testnet-governed-bootstrap-2026-06-06.runbook.md`
  - `git diff --check`
- Expected Result: runbook 结构完整、步骤可执行、diff 无格式错误。
- Actual Result: runbook 已重写完成；后续 `git diff --check` 作为当前最小文档 sanity check 执行。
- Next Action: 如需继续产品化部署流程，下一步应把 runbook 中的 deployment truth capture、peer-id refresh、seed bundle closure 校验各自落成脚本化 preflight。

## 2026-06-07 15:20:00 CST / tpm
- 完成内容: 将 runbook 中的三个人工高风险步骤脚本化，新增：
  - `scripts/p2p-public-testnet-capture-truth.sh`
  - `scripts/p2p-public-testnet-refresh-bootstrap-peers.sh`
  - `scripts/p2p-verify-state-sync-closure.sh`
- 完成内容:
  - `p2p-public-testnet-capture-truth.sh` 负责统一抓取 bundle 校验结果、validator runtime sha256、node-keypair 存在性、live `local_peer_id` 与高度快照。
  - `p2p-public-testnet-refresh-bootstrap-peers.sh` 负责按 live validator `local_peer_id` 改写 observer `REPLICATION_NETWORK_BOOTSTRAP_PEERS_CSV`。
  - `p2p-verify-state-sync-closure.sh` 负责按 world / execution-records 中实际引用的 blob ref 校验 `store/blobs` 闭包，避免 observer 启动后才暴露 `BlobNotFound`。
- 完成内容: 为三个脚本分别补了 repo 内测试：
  - `scripts/p2p-public-testnet-capture-truth.test.sh`
  - `scripts/p2p-public-testnet-refresh-bootstrap-peers.test.sh`
  - `scripts/p2p-verify-state-sync-closure.test.sh`
- 完成内容: runbook 已更新为优先调用这些脚本，而不是重复列出同类手工命令。
- Validation Command:
  - `bash -n scripts/p2p-public-testnet-capture-truth.sh scripts/p2p-public-testnet-refresh-bootstrap-peers.sh scripts/p2p-verify-state-sync-closure.sh`
  - `./scripts/p2p-public-testnet-capture-truth.test.sh`
  - `./scripts/p2p-public-testnet-refresh-bootstrap-peers.test.sh`
  - `./scripts/p2p-verify-state-sync-closure.test.sh`
  - `git diff --check`
- Expected Result: 三个脚本语法正确、各自测试通过、diff 无格式错误。
- Actual Result: 待本轮统一 sanity check 记录。
- Next Action: 若后续继续推进部署产品化，可在此基础上再补一个上层 orchestrator，把 capture -> refresh -> closure verify 串成单个 phase runner。
  - `p2p_detected=public`，`reachability_policy_ok=true`，`p2p_reachability_degraded` 消失。
  - 两个 validators 当前都有 3 个 active peers，height/replication persisted 均对齐，但 `recent_errors` 中的 `connection closed ... active_path=...`、restart-window `outgoing connection error ... Connection refused`、`redundant connections pruned` 仍被算作 blocking，导致 `replication_transport_unstable`。
- Root Cause: Transport readiness 仍偏日志字符串驱动；在当前 active/connected peer 已恢复时，历史连接关闭和冗余连接裁剪属于 libp2p connection churn diagnostic，不应继续阻断 readiness。
- Patch:
  - 新增 state-aware `replication_error_is_blocking(replication, error)`；已有 active/connected peer 时，将 connection closed / outgoing connection error / connection refused / redundant connections pruned 视为 non-blocking diagnostic。
  - 保留无 active peer 时 repeated connection closed 的不稳定判定，避免掩盖真实断网。
  - 扩展 observability transport regression，覆盖 active peer 下的 connection churn diagnostic。
- Validation Command:
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime observability_transport -- --nocapture`
  - `cargo fmt --check`
  - `git diff --check`
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime observability -- --nocapture`
- Actual Result:
  - Passed: observability transport 2 tests.
  - Passed: `cargo fmt --check`.
  - Passed: `git diff --check`.
  - Passed: full chain runtime observability group with 14 tests.
- Next Action: Commit/push third stability patch, trigger package again, deploy to validators, and verify that validator transport readiness clears; remaining non-transport issue to evaluate separately is sequencer `consensus_peer_head_unavailable` if it persists after transport fix.

## 2026-06-06 19:55:00 CST / tpm
- 完成内容: testnet8 部署后继续追 `replication_transport_unstable`，确认 recent error blocking 已基本清掉，但 transport score 仍被 protocol retry cooldown peers 拉低。
- Live Evidence:
  - Sequencer/storage 都有 3 个 active peers，height/replication persisted 对齐，`reachability_policy_ok=true`。
  - `replication_recent_errors` validator gate 消失；剩余 `replication_transport_unstable` 的 summary 显示 `connection_closed=0, insufficient_peers=0, timeout=0, protocol_errors=0`，分数来自 `/aw/node/replication/fetch-commit/1.0.0` cooldown peers。
  - cooldown peers 是本地 observers；validator 之间仍有稳定 active peer，当前数据面可用。
- Root Cause: cooldown penalty 仍按历史 retry state 全量扣分；当至少一个 active peer 不在 protocol/transport cooldown 中时，readiness 不应因非关键 peer cooldown 阻断。
- Patch:
  - `classify_transport_stability` 计算 active peer set 与 protocol/transport cooldown peer set。
  - 如果存在 stable active peer，则 cooldown peers 不再扣 readiness transport 分；如果所有 active peers 都在 cooldown 或没有 active peer，则保留 cooldown penalty。
  - 扩展 transport regression：一个 active peer 稳定、另一个 active peer 在 fetch-commit cooldown 时，diagnostic/churn 不降低 score。
- Validation Command:
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime observability_transport -- --nocapture`
  - `cargo fmt && cargo fmt --check && git diff --check`
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime observability -- --nocapture`
- Actual Result:
  - Passed: observability transport 2 tests.
  - Passed: formatter and diff check.
  - Passed: full chain runtime observability group with 14 tests.
- Next Action: Commit/push cooldown stability patch, trigger Testnet Packages again, deploy package to validators, then resample four-node readiness.

## 2026-06-06 21:05:00 CST / tpm
- Runtime Peer-Head Root-Cause Slice:
  - Owner role: `runtime_engineer`.
  - Intended model: inherited/default subagent runtime; actual dispatched model: inherited/unverified.
  - Mandatory context checklist: PR #361; public_testnet governed 4-node bring-up; testnet9 validators height/replication aligned and transport/reachability stable; remaining readiness gate is `consensus_peer_head_unavailable` / stale peer heads; live sequencer `known_peer_heads=0`; live storage `known_peer_heads=1` but stale; governance registry/stake truth includes `triad-testnet-sequencer` and `triad-testnet-storage`.
  - Runtime engineer finding: validators were not all able to observe peer heads through replication because sequencer runtime did not subscribe to replication topic; replication should be a fallback peer-head signal when consensus gossip delivery is incomplete.
  - TPM integration finding: storage is present in validator stake truth but `NodeRole::Storage` was forced to P2P `FullStorage`, while `ConsensusGossip::Publish` only allows `ValidatorCore` / `Sentry`; this conflated storage service role with consensus publisher eligibility, making a governed storage validator unable to publish consensus heads unless role semantics are explicitly decoupled.
- Patch:
  - Allow `NodeRole::Storage` to explicitly claim `PeerNodeRole::ValidatorCore` while keeping the default `FullStorage` role for ordinary storage nodes.
  - Treat local replication commit publishing as enabled for `Sequencer` or explicit `ValidatorCore` nodes with replication configured, so a storage validator proposer persists its own committed replication records.
  - Subscribe replication-network endpoints whenever node replication is configured, including sequencers/validators, and skip self-published replication messages during ingest.
  - Add regression coverage:
    - `runtime_storage_validator_core_publishes_consensus_heads`
    - `storage_validator_core_local_replication_advances_persisted_height`
    - `runtime_network_replication_tracks_peer_heads_between_validator_sequencers`
    - `storage_runtime_can_explicitly_claim_validator_core_network_role`
- Validation Command:
  - `cargo fmt --check`
  - `./scripts/cargo-dev.sh test -p oasis7_node runtime_storage_validator_core_publishes_consensus_heads -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node storage_validator_core_local_replication_advances_persisted_height -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node runtime_network_replication_tracks_peer_heads_between_validator_sequencers -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node storage_runtime_can_explicitly_claim_validator_core_network_role -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node replication -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node --lib`
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime observability -- --nocapture`
  - `git diff --check`
- Actual Result:
  - Passed: formatter and diff check.
  - Passed: all new targeted regressions.
  - Passed: `oasis7_node replication` group with 115 tests.
  - Passed: `oasis7_chain_runtime observability` group with 14 tests.
  - `oasis7_node --lib` initially exposed timing-sensitive failures (`libp2p_replication_network_request_retries_next_peer_when_remote_handler_fails`, `runtime_adaptive_tick_scheduler_reduces_tick_frequency`, `runtime_gossip_tracks_peer_heads_when_replication_network_consensus_is_disabled`, later `runtime_start_and_stop_updates_snapshot`); each failed item passed when rerun individually, and a full rerun passed 247/247 before the new replication-subscription regression was added. After the final patch, the full run had 247/248 pass with only `runtime_start_and_stop_updates_snapshot` failing on `tick_count >= 2`; the same test passed immediately when rerun individually. No deterministic failure tied to this patch remained.
- Next Action: Commit/push the peer-head role-decoupling patch, trigger Testnet Packages, deploy to both ECS nodes, set storage `P2P_NODE_ROLE=validator_core` for the governed validator role, restart from current state, then resample readiness and peer-head freshness.

## 2026-06-06 21:45:00 CST / tpm
- 完成内容: Testnet Packages run `27063131935` for commit `8d734c4c8685de818fbc1db805d0f2af276b63e5` succeeded and produced artifact `testnet-package-linux-x64-0.0.0+testnet.10.8d734c4c8685`.
- Deployment Evidence:
  - Downloaded artifact via GitHub artifact API; `linux-x64-SHA256SUMS` verified locally.
  - Extracted package runtime sha256 `69e36a1a49f002c24f1fe8606702acaa104d2f1057dfdec4a2b6b5b56167c60b`.
  - Generated and validated `public-testnet-governed-bootstrap-bundle-2026-06-06.runtime-69e36a1a.json`.
  - Deployed release `/opt/oasis7/p2p-testnet/releases/8d734c-testnet10-storage-validator-peerhead-20260606-2135` to both ECS validators; both `current/bin/oasis7_chain_runtime` hashes match `69e36a1a...`.
  - Replaced all three remote bundle truth copies on both ECS nodes with runtime hash `69e36a1a...` and git commit `8d734c4c8685de818fbc1db805d0f2af276b63e5`.
  - Added `P2P_NODE_ROLE=validator_core` to both remote validator env files; systemd command lines include `--p2p-node-role validator_core`.
- Live Result:
  - Storage briefly reached `ready=true`, `network_head_source=peer_quorum`, `known_peer_heads=1`, `fresh_peer_count=1`.
  - After TTL, storage fell back to stale peer head and sequencer remained `known_peer_heads=0`; both validators kept height/replication aligned and transport stable.
- Root Cause:
  - The previous patch fixed network role authorization and sequencer replication subscription, but storage validator-core still did not continuously publish a committed head after it learned/persisted a commit through replication.
  - `broadcast_local_commit_network()` requires a local execution binding; `NodeRole::Storage` defaults `require_execution_on_commit=false`, so replicated storage nodes can hold/persist the chain head without having the local execution binding used by the existing commit broadcaster.
  - This made peer-head flow asymmetric: storage could observe sequencer, but sequencer could not observe storage as a fresh governed validator head.
- Patch:
  - Added `broadcast_replicated_commit_head_network()` to publish a signed local `GossipCommitMessage` from the latest persisted replication commit payload.
  - Wired the helper into the consensus-network commit broadcast phase so storage/full validator nodes can announce already verified/persisted heads without pretending to be proposers.
  - Added regression `runtime_storage_validator_core_rebroadcasts_replicated_commit_heads`, covering the live shape: `NodeRole::Storage + PeerNodeRole::ValidatorCore + replication` rebroadcasts a replicated commit head and the sequencer observes `node-b` as a peer head.
- Validation Command:
  - `cargo fmt --check`
  - `./scripts/cargo-dev.sh test -p oasis7_node runtime_storage_validator_core_rebroadcasts_replicated_commit_heads -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node runtime_storage_validator_core_publishes_consensus_heads -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node runtime_network_replication_tracks_peer_heads_between_validator_sequencers -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node replication -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node runtime_gossip_replication_persists_guard_across_restart -- --nocapture`
  - `git diff --check`
- Actual Result:
  - Passed: formatter and diff check.
  - Passed: all targeted peer-head regressions.
  - `oasis7_node replication` group had 115/116 pass; the only failure was `runtime_gossip_replication_persists_guard_across_restart` timing out before first-stop guard persistence, and it passed immediately when rerun individually.
- Next Action: Commit/push this replicated-head rebroadcast fix, trigger Testnet Packages again, deploy fresh Linux package to both ECS validators, and resample public_testnet readiness.

## 2026-06-06 22:35:00 CST / tpm
- 完成内容: Testnet Packages run `27063932289` for commit `bbeb64f23823499fa0aca0ffc72d8998b5571295` succeeded and produced artifact `testnet-package-linux-x64-0.0.0+testnet.11.bbeb64f23823`.
- Deployment Evidence:
  - Downloaded artifact `7454979933` via GitHub artifact API.
  - `linux-x64-SHA256SUMS` verified locally.
  - Extracted package runtime sha256 `c4db6b0696cb9f02032c1373d11d7c5c510e394348c9eefd7382dfab13e94042`.
  - Generated and validated `.tmp/testnet-package-bbeb64f/public-testnet-governed-bootstrap-bundle-2026-06-06.runtime-c4db6b06.json` with CI metadata `{run_id=27063932289, artifact=testnet-package-linux-x64-0.0.0+testnet.11.bbeb64f23823}`.
  - Deployed release `/opt/oasis7/p2p-testnet/releases/bbeb64f-testnet11-replicated-head-rebroadcast-20260606-220445` to both ECS validators; both `current/bin/oasis7_chain_runtime` hashes match `c4db6b06...` and services are active.
  - Replaced all three remote governed bundle truth copies on both ECS validators with runtime hash `c4db6b06...` and git commit `bbeb64f23823499fa0aca0ffc72d8998b5571295`.
- Live Result:
  - Storage can reach ready windows (`known_peer_heads=1`, `fresh_peer_count=1`, `network_head_source=peer_quorum`) when sequencer heads are fresh.
  - Sequencer still reports `known_peer_heads=0` and `consensus_peer_head_unavailable`.
  - Setting sequencer `P2P_USER_MODE=public_entry` made the effective mode `public_entry/hybrid` but did not make sequencer observe storage heads.
  - Storage reject reason continued as `drop remote committed height N without matching persisted replication commit` during catch-up windows; height/replication remain aligned after persistence catches up.
- Root Cause:
  - The replicated-head fix made storage capable of publishing persisted heads, but the commit-head broadcast path was still transport-exclusive.
  - When a replication-network consensus endpoint exists, `PosNodeEngine::tick()` only used the libp2p consensus path and skipped UDP gossip. This made public_testnet readiness depend on a single pubsub path even though UDP gossip was already the live proven peer-head path.
  - The single shared `last_broadcast_committed_height` cursor also made naive dual publish unsafe: publishing on one transport would throttle the other.
  - The sequencer auto-join policy had an additional design mismatch: sequencer was hard-coded to recommend `private_safe` even after the operator explicitly accepted public entry, and `public_direct_surface_allowed()` denied all `ValidatorCore` public direct surfaces regardless of deployment mode.
- Patch:
  - Split committed-head broadcast cursors into per-transport UDP gossip and libp2p consensus cursors.
  - Always attempt UDP commit-head broadcast when a gossip endpoint exists, even when libp2p consensus is also enabled.
  - Added UDP replicated-head rebroadcast so `NodeRole::Storage + PeerNodeRole::ValidatorCore + replication` can advertise persisted heads over both transport paths.
  - Keep persisted `PosNodeStateSnapshot.last_broadcast_committed_height` backward-compatible by exporting the max cursor and restoring it into both runtime cursors.
  - Removed the hard-coded sequencer `private_safe` recommendation; sequencer still requires explicit public-entry confirmation in auto mode, but with `--p2p-accept-public-entry` can now become `public_entry` with `Hybrid + ValidatorCore`.
  - Allow public direct surface according to deployment mode rather than denying every `ValidatorCore`.
  - Added regression `runtime_commit_heads_publish_on_network_and_gossip_transports` to prove one runtime publishes commit heads to libp2p consensus while a peer with libp2p consensus disabled still learns the same head over UDP gossip.
- Validation Command:
  - `cargo fmt && cargo fmt --check`
  - `./scripts/cargo-dev.sh test -p oasis7_node runtime_commit_heads_publish_on_network_and_gossip_transports -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node runtime_storage_validator_core_rebroadcasts_replicated_commit_heads -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node sequencer_auto_join -- --nocapture`
  - `./scripts/cargo-dev.sh test -p oasis7_node restore_state_snapshot_clamps_stale_heights_and_broadcast_cursors -- --nocapture`
  - `git diff --check`
- Actual Result:
  - Passed: formatter and diff check.
  - Passed: dual-transport commit-head regression.
  - Passed: storage validator-core replicated-head rebroadcast regression.
  - Passed: sequencer public-entry policy regressions.
  - Passed: persisted state restore broadcast cursor regression.
- Next Action: Commit/push the dual-transport commit-head and sequencer public-entry policy fix, trigger Testnet Packages, deploy the fresh package to both ECS validators, then resample `public_testnet` readiness. Keep `P2P_USER_MODE=public_entry` on sequencer until the new auto recommendation package is deployed, then decide whether to keep explicit mode or return to `auto_join + accept_public_entry`.

## 2026-06-06 22:55:00 CST / tpm
- 完成内容: Testnet Packages run `27064862419` for commit `aace94bb2d77b15270af66cb381ccfc1f8920b65` failed in `package-linux-x64` after Rust bundle build and entrypoint validation succeeded.
- CI Failure Evidence:
  - Passed: `plan-testnet-package-scope`.
  - Passed: `build-web-dist`.
  - Passed within `package-linux-x64`: checkout, Rust toolchain, Linux dependencies, rust-cache, AppImage tooling install, web dist download, package version resolution, `Build launcher bundle`, `Validate bundle entrypoints`, `Stage bundle manifest`.
  - Failed step: `Package native installer`.
  - Log root cause: `appimagetool` attempted to download `https://github.com/AppImage/type2-runtime/releases/download/continuous/runtime-x86_64` internally and received `Failed to download runtime: server returned status code 504`.
  - Local confirmation: direct runtime asset download was also slow/flaky; GitHub release API reports `runtime-x86_64` asset size `944632` and digest `sha256:a2419dce47568395ae79c01ffa9a5a341dd339581352ff104d073527543177e5`.
- Root Cause:
  - The package script delegated AppImage type2 runtime retrieval to `appimagetool`, hiding a network dependency inside the native-installer step.
  - That path was not explicitly retried, not checksum-gated by workflow truth, and failed after the full Rust bundle was already built.
  - This is a CI/package-chain stability design issue, not a dual-transport runtime build failure.
- Patch:
  - Added `--appimage-runtime-file` and `OASIS7_APPIMAGE_RUNTIME_FILE` support to `scripts/package-native-installer.sh`.
  - When provided, the script calls `appimagetool --runtime-file <runtime> --no-appstream ...`, preventing appimagetool from doing its own internal runtime download.
  - Made `--dry-run` stop requiring native packaging commands so AppImage command construction can be verified on non-Linux developer machines.
  - Updated both `.github/workflows/testnet-packages.yml` and `.github/workflows/release-packages.yml` to pre-download `runtime-x86_64` with curl retries, verify sha256 `a2419dce...`, and export `OASIS7_APPIMAGE_RUNTIME_FILE` through `GITHUB_ENV`.
- Validation Command:
  - `bash -n scripts/package-native-installer.sh`
  - `./scripts/package-native-installer.sh --platform linux-x64 --bundle-dir <tmp>/bundle --out-dir <tmp>/out --asset-name oasis7-linux-x86_64.AppImage --version 0.0.0+test --appimage-runtime-file <tmp>/runtime-x86_64 --dry-run`
  - `grep -F -- "--runtime-file <tmp>/runtime-x86_64" <tmp>/dry.log`
  - `ruby -e 'require "psych"; ARGV.each { |f| Psych.load_file(f); puts "ok #{f}" }' .github/workflows/testnet-packages.yml .github/workflows/release-packages.yml`
- Actual Result:
  - Passed: shell syntax check.
  - Passed: dry-run AppImage packaging command includes explicit `--runtime-file`.
  - Passed: workflow YAML parse for testnet and release package workflows.
- Next Action: Commit/push the AppImage runtime-file package-chain fix, rerun Testnet Packages for the current branch, then continue fresh package download, governed bootstrap bundle refresh, ECS deployment, and validator readiness sampling.

## 2026-06-07 00:27:00 CST / tpm
- Reproduced Failure: empty-world public_testnet observer still fails on fresh boot with `DistributedValidationFailed ... latest state root mismatch tick=1` even after removing viewer/demo bootstrap contamination from `execution_bridge::load_execution_world_with_policy`.
- Hypothesis Under Test: the remaining mismatch is caused by chain runtime first-boot importing only the genesis finality registry, while the live governed bootstrap world actually requires the full governance bootstrap registry set (finality signer registry plus main-token controller registry derived from `runtime_refs.genesis_ref` governance bootstrap refs).
- Evidence Changed Confidence:
  - `.tmp/public-testnet-governed-bootstrap-world-generated/world/snapshot.json` is a zero-tick, zero-agent hardened world that already loads cleanly with both `governance_finality_signer_registry` and `governance_main_token_controller_registry` present.
  - The generated world does not initialize main-token supply or demo agents, so the missing piece is not token genesis or viewer bootstrap state.
  - Current `governance_registry::ensure_world_governance_validator_registry` only imports finality from `--genesis-validator-registry`; it never consumes `runtime_refs.genesis_ref` governance bootstrap refs to populate controller signer registry.
- Next Action: patch first-boot governance bootstrap import to resolve genesis/governance/liveops refs from the loaded network-tier manifest, build the full governance bootstrap registry set into empty/bootstrap-only worlds, then rerun targeted chain-runtime tests.

## 2026-06-07 03:32:00 CST / tpm
- Root Cause Fixed: empty-world public/mainnet observer bootstrap only imported the genesis finality registry, while the real governed bootstrap truth also requires the controller signer registry from `runtime_refs.genesis_ref` governance bootstrap refs. In live `public_testnet`, the genesis child refs are repo-root-relative (`doc/testing/evidence/...`), so manifest-relative-only resolution also broke runtime bootstrap outside pre-generated/manual flows.
- Patch Applied:
  - `governance_registry.rs` now attempts network-tier governance bootstrap before the legacy finality-only path.
  - On empty/bootstrap-only worlds, it resolves `runtime_refs.genesis_ref`, loads governance bootstrap refs, imports genesis finality signer registry, and imports main-token controller signer registry from governance/liveops public signer manifests.
  - Relative ref resolution now accepts existing repo-root-relative paths before falling back to manifest-parent joining, matching real governed bootstrap artifacts.
  - Added regression `network_tier_genesis_bootstrap_initializes_full_governance_world_for_empty_observer` plus preserved legacy genesis-registry coverage.
- Verification Rerun:
  - `cargo fmt --check`
  - `./scripts/cargo-dev.sh build -p oasis7 --bin oasis7_chain_runtime`
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime governance_registry -- --nocapture`
  - Live local smoke: reset `.tmp/testnet-local-node-bootstrap/{world,execution-records,store,runtime-root,replication-root}`, patched local runtime bundle hash to the rebuilt binary, launched `scripts/p2p-triad-node-start.sh`, and observed:
    - no `tick=1 latest state root mismatch`
    - status endpoint served with `running=true`, `last_error=null`
    - expected degraded readiness shifted to `state_sync_fallback_required` / network lag because the observer started from empty state against a chain already at height ~3085
    - saved world now contains `tick_records=0`, `has_finality=true`, `has_controller=true`
- Next Action: wire the same bootstrap path into the fourth-local observer smoke or deploy this runtime/package to validator/observer Linux artifacts so fresh empty observers can join via a formal trusted-checkpoint/state-sync path instead of manual pre-seeding.

## 2026-06-07 12:15:00 CST / tpm
- Root Cause Narrowed Further: `public_testnet` local observers can reach `ready=true` and track network head height, but they still do not produce any local execution-bridge truth (`execution-records/latest.json`, retained execution records, or `last_execution_height > 0`). That means observer `world/` directories are not a valid source for trusted checkpoint / state-sync export today.
- Patch Applied:
  - `crates/oasis7/src/bin/oasis7_chain_runtime.rs` now separates “must require execution on commit” from “should materialize execution state locally”: sequencer still alone requires execution commit/hash at consensus gate, but storage/observer roles now also attach `NodeRuntimeExecutionDriver`.

## 2026-06-07 12:50:00 CST / tpm
- Root Cause Narrowed Again: the local observer height-1 replay mismatch is not one single knob. Three independent sources of genesis drift were identified and partially fixed:
  1. empty-world bootstrap previously used raw validator ids in the world finality registry, while the canonical pre-generated governed bootstrap world used slot-prefixed bindings such as `governance.finality.v1.triad-testnet-sequencer`;
  2. repo-root-relative child refs inside `governance_bootstrap_refs` still rejoined under `.../doc/testing/evidence/doc/testing/evidence/...` on fresh observer start, depending on process cwd;
  3. the canonical pre-generated validator bootstrap world did **not** include `liveops` in the initial controller signer registry, while the fresh empty-world path had started adding it.
- Patch Applied:
  - `governance_registry.rs` now writes slot-prefixed finality binding keys for genesis validator registry imports, matching `oasis7_governance_registry_import`.
  - `resolve_network_tier_runtime_ref_path()` now walks ancestor directories so repo-root-relative child refs resolve correctly even when the process cwd is not the repo root.
  - fresh network-tier bootstrap no longer injects `liveops_public_manifest_ref` into the initial controller registry path; bootstrap now matches the pre-generated governed world shape more closely.
  - Added regression `network_tier_genesis_bootstrap_resolves_repo_root_relative_child_refs` and updated governance bootstrap assertions to expect canonical slot-prefixed finality ids.
- Verification Rerun:
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime governance_registry -- --nocapture`
  - `./scripts/cargo-dev.sh build -p oasis7 --bin oasis7_chain_runtime`
  - local smoke: wiped `.tmp/testnet-local-node-bootstrap/{world,world-simulator-mirror,execution-records,runtime-root}`, refreshed `runtime-bundle.json` sha, and replayed height 1 from persisted replication history multiple times.
- Actual Result:
  - The fresh observer no longer fails on duplicated `doc/testing/evidence/...` child-ref resolution.
  - Height-1 local replay hash changed across each canonicalization step:
    - old: `local_block=b0b43f6b... local_state=8d1fa9b0...`
    - after slot-prefix + path-resolution fix: `local_block=b5ee7397... local_state=cc842094...`
    - after also removing liveops from initial controller bootstrap: `local_block=06f3f5eb... local_state=b00dbf11...`
  - But peer truth at height 1 is still `peer_block=8f2def89... peer_state=61fef8a0...`, so the observer still aborts at `synced replication height 1 execution hash validation failed`.
  - Direct snapshot diff shows the fresh bootstrap world is now much closer to `.tmp/public-testnet-governed-bootstrap-world-generated/world/snapshot.json`, but the live peer height-1 execution snapshot still differs from what the current source replays from that bootstrap shape. This strongly suggests remaining drift in height-1 execution semantics or snapshot hashing, not just manifest/bootstrap plumbing.
- Next Action:
  1. diff the current runtime’s height-1 `tick_consensus` / snapshot serialization path against the runtime/package that originally produced peer commit `height=1 execution_state_root=61fef8a0...`;
  2. recover or reconstruct a canonical height-1 execution snapshot from validator-era artifacts, then write a regression that replays the same empty commit and asserts the exact peer execution hashes.
  - Added regression `sequencer_requires_execution_commit_but_all_runtime_roles_materialize_execution_state`.
  - `scripts/p2p-export-state-sync-bundle.sh` now refuses to export when an adjacent/provided `execution-records/` exists but lacks `latest.json`, with a clear operator error that the node has no materialized execution records.
  - `scripts/p2p-export-state-sync-bundle.test.sh` now covers that refusal path in addition to the positive preflight path.
- Validation Command:
  - `./scripts/p2p-export-state-sync-bundle.test.sh`
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime sequencer_requires_execution_commit_but_all_runtime_roles_materialize_execution_state -- --nocapture`
  - `./scripts/cargo-dev.sh build -p oasis7 --bin oasis7_chain_runtime`
  - live probe: restart local observer `triad-testnet-local` on rebuilt binary, sample `http://127.0.0.1:19082/v1/chain/status`, and inspect `.tmp/testnet-local-node-bootstrap/execution-records`
- Actual Result:
  - Passed: export script positive test and missing-execution-records refusal test.
  - Passed: targeted chain-runtime regression.
  - Passed: rebuilt `oasis7_chain_runtime`.
  - Live evidence after restart: observer returns `ready=true`, `network_head.height=3201`, but `consensus.last_execution_height=0`, `consensus.last_execution_block_hash=null`, `consensus.last_execution_state_root=null`, `.tmp/testnet-local-node-bootstrap/execution-records/` remains empty, and export script now correctly fails with `execution records latest.json not found ... export must run from a node with materialized execution records`.
- Root Cause / Interpretation:
  - Attaching an execution hook to observers is not sufficient by itself. In `oasis7_node`, non-sequencer commits still tolerate execution-hook “wait for gap sync” failures when there is no predecessor execution record, so a long-running observer that joined after history retention moved on can stay consensus-synced while never materializing runtime execution state.
  - Therefore the real canonical source for fresh state-sync artifacts must come from nodes that persist execution records continuously (at minimum sequencer / likely storage validator after package rollout), not from today’s observer `world/` snapshot export path.
- Next Action:
  - Push the observer/storage execution-materialization prep patch as part of the runtime package update.
  - On fresh validator/storage package rollout, verify that at least one governed node now emits `execution-records/latest.json` and `last_execution_height > 0`, then generate the trusted-checkpoint/state-sync bundle from that node rather than from an observer.

## 2026-06-07 13:28:00 CST / tpm
- Root-Fix Pivot Applied: stop treating the governed bootstrap world as an informal `.tmp` byproduct or placeholder JSON; formalize it as a deterministic repo-owned artifact that can be regenerated from genesis/governance truth and staged directly onto clean validator nodes.
- Patch Applied:
  - Added `scripts/p2p-build-governed-bootstrap-world.sh` with two modes:
    - `create`: resolve `governance_bootstrap_refs` from genesis, merge controller signer truth with the governed validator registry finality slot, import that merged manifest into an empty hardened world, then canonicalize the output into a stable `world/` artifact without ephemeral `.distfs-state` or recovery-audit files.
    - `validate`: assert the canonical artifact shape (`module_registry.updated_at=0`, no `.distfs-state`, no `distfs.recovery.audit.json`) and run `oasis7_governance_registry_audit --strict-manifest-match`.
  - Added `scripts/p2p-build-governed-bootstrap-world.test.sh` to prove the artifact is reproducible across two independent rebuilds and that the resulting world tree hash is stable.
  - Generated the formal artifact at `doc/testing/evidence/public-testnet-governed-bootstrap-world-2026-06-06/`:
    - `world/{snapshot.json,snapshot.manifest.json,journal.json,journal.segments.json,module_registry.json}`
    - `merged-public-manifest-entries.json`
  - Regenerated `doc/testing/evidence/public-testnet-governed-bootstrap-bundle-2026-06-06.json` so `world_snapshot` now points at the directory artifact above instead of `public-testnet-governed-bootstrap-world-snapshot-placeholder-2026-06-06.json`.
  - Updated `doc/p2p/blockchain/p2p-public-testnet-governed-bootstrap-2026-06-06.runbook.md` to require staging the canonical bootstrap world onto validator execution-world roots before cold start, and replaced the placeholder artifact reference in `doc/p2p/blockchain/p2p-formal-network-tiers-testnet-mechanism-2026-05-14.project.md`.
- Verification Command:
  - `bash -n scripts/p2p-build-governed-bootstrap-world.sh scripts/p2p-build-governed-bootstrap-world.test.sh`
  - `./scripts/p2p-build-governed-bootstrap-world.test.sh`
  - `./scripts/p2p-build-governed-bootstrap-world.sh create --genesis doc/testing/evidence/public-testnet-governed-bootstrap-genesis-2026-06-06.json --out-dir doc/testing/evidence/public-testnet-governed-bootstrap-world-2026-06-06 --allow-overwrite`
  - `./scripts/p2p-build-governed-bootstrap-world.sh validate --world-dir doc/testing/evidence/public-testnet-governed-bootstrap-world-2026-06-06/world --merged-public-manifest doc/testing/evidence/public-testnet-governed-bootstrap-world-2026-06-06/merged-public-manifest-entries.json`
  - `./scripts/release-candidate-bundle.sh create --bundle doc/testing/evidence/public-testnet-governed-bootstrap-bundle-2026-06-06.json --candidate-id public-testnet-governed-bootstrap-20260606 --track public_testnet --runtime-build-ref .tmp/public-testnet-governed-bootstrap-live/oasis7_chain_runtime.linux-x86_64 --world-snapshot-ref doc/testing/evidence/public-testnet-governed-bootstrap-world-2026-06-06/world --governance-manifest-ref doc/testing/evidence/public-testnet-governed-bootstrap-validator-registry-2026-06-06.json --evidence-ref doc/testing/evidence/public-testnet-governed-bootstrap-topology-2026-06-06.md --evidence-ref doc/testing/evidence/network-tier-signer-truth-binding-2026-06-05.md --allow-dirty-worktree`
  - `./scripts/release-candidate-bundle.sh validate --bundle doc/testing/evidence/public-testnet-governed-bootstrap-bundle-2026-06-06.json`
  - `./scripts/release-candidate-bundle-smoke.sh`
  - `git diff --check`
- Actual Result:
  - Passed: bootstrap-world script syntax and reproducibility test.
  - Passed: canonical world artifact validates against its merged manifest; finality signer count is 2 and manifest match passes.
  - Passed: release candidate bundle validates with `world_snapshot.kind=directory`, `sha256_tree=c653b29fecd7b9db63a18df8225f882a3d07882a336b561aad2ac9dda68129b7`, `file_count=5`.
  - Passed: release candidate bundle smoke still accepts directory-style world truth and still detects drift when the world mutates.
  - Cleared: repo/script/doc references to `public-testnet-governed-bootstrap-world-snapshot-placeholder-2026-06-06.json` are removed from formal sources.
- Next Action:
  1. Use the canonical `public-testnet-governed-bootstrap-world-2026-06-06/world` artifact for the destructive four-node rebuild instead of reusing any historical node data.
  2. After current runtime fixes are packaged, restage this artifact plus the fresh Linux runtime/package on the two validators and perform a true zero-from-scratch `public_testnet` rebuild, then attach observers against the rebuilt chain rather than the old height-1 truth.

## 2026-06-07 13:42:00 CST / tpm
- External Operator Blocker Confirmed:
  - public endpoints remain readable and both validators are still advancing on the old governed chain (`committed_height=3410+`, `last_error=null`, `world_id=oasis7-public-testnet-governed-20260606`), but current control host cannot obtain write access to either ECS validator:
    - `ssh -o BatchMode=yes root@39.104.204.172 ...` -> `Permission denied (publickey,password)`
    - `ssh -o BatchMode=yes root@39.104.205.67 ...` -> `Permission denied (publickey,password)`
    - same denial reproduced for explicit `~/.ssh/id_rsa` and fallback users `scc`, `ecs-user`, `ubuntu`, `admin`
  - Therefore the true destructive reset / restage / validator cold boot cannot be executed from this turn until fresh SSH credentials or another authorized operator path is available.
- Local / Staging Work Completed Anyway:
  - Refreshed `.tmp/public-testnet-governed-bootstrap-live/` so it now includes the canonical bootstrap world artifact:
    - `public-testnet-governed-bootstrap-world-2026-06-06/{merged-public-manifest-entries.json, world/...}`
  - Built transport artifact:
    - `.tmp/public-testnet-governed-bootstrap-live-2026-06-07.tgz`
    - sha256: `6b7db311721326fc03690f9102fc4b17795f034922cfc500fc8855b068576416`
  - Updated local observer runtime bundle hashes to the current local `target/debug/oasis7_chain_runtime` sha256 `561227cd30697b5c6f5bc2b8ddde36ddf50103e425b88fbda46ed2a71d63e05d`, so local startup no longer fails on stale bundle truth before reaching runtime logic.
- Local Verification / Interpretation:
  - `triad-testnet-fourth-local` remains alive on the old chain at `127.0.0.1:19083`, `committed_height=3412`, `last_error=null`, but still `last_execution_height=0`; this remains consistent with the known observer-materialization gap.
  - `triad-testnet-local` can now be launched with the current bundle truth; once the hash blocker is removed, it immediately returns to the expected old-chain incompatibility:
    - `peer commit execution mismatch at height 1`
    - `local_block=06f3f5eb... local_state=b00dbf11...`
    - `peer_block=8f2def89... peer_state=61fef8a0...`
  - This is consistent with the root-fix direction: local observers cannot be cleanly rejoined against the old validator chain truth; they must join after the validators themselves are destructively rebuilt from the canonical bootstrap world.
- Validation Command:
  - `curl -fsS http://39.104.204.172:6631/v1/chain/status | jq ...`
  - `curl -fsS http://39.104.205.67:6632/v1/chain/status | jq ...`
  - `ssh -o BatchMode=yes ...` against both validators with multiple user/key combinations
  - `ENV_FILE=.tmp/testnet-local-node-bootstrap/node.env APP_ROOT=... OASIS7_NODE_START_DRY_RUN=1 ./scripts/p2p-triad-node-start.sh`
  - manual local observer launch after bundle refresh
  - `tar -C .tmp -czf .tmp/public-testnet-governed-bootstrap-live-2026-06-07.tgz public-testnet-governed-bootstrap-live && shasum -a 256 ...`
- Next Action:
  1. Obtain fresh SSH/operator write access for `39.104.204.172` and `39.104.205.67`.
  2. Use `.tmp/public-testnet-governed-bootstrap-live-2026-06-07.tgz` as the single staging payload for the destructive validator rebuild.
  3. After validator pair is rebuilt from the canonical bootstrap world, reset both local observers and reattach them against the rebuilt chain instead of the old height-1 truth.

## 2026-06-07 15:16:00 CST / tpm
- Standard-Flow Hardening:
  - Added `scripts/p2p-public-testnet-rebuild-validators.sh` to formalize Phase C/D validator rebuild as a single contract:
    - stage deployment-truth config onto both hosts
    - stage the canonical execution world once to `staged-world/`
    - copy the staged world into `data/execution-world/` on-host
    - destructively reset validator chain state while preserving protected config assets
    - restart sequencer first, then storage
    - poll status URLs until `running=true`, `last_error=null`, `committed_height>0`, and `last_execution_height>0`
    - emit `rebuild-summary.json` plus captured validator status JSON
  - Added `scripts/p2p-public-testnet-rebuild-validators.test.sh` to cover the happy path and prove the world is materialized to `data/execution-world/` through the staged-world copy path.
  - Updated the governed bootstrap runbook to use the new script as the standard validator rebuild path and to codify two new invariants:
    - world staging must be “single transfer + remote copy”, not “reuse one stdin tar stream for two remote untars”
    - if storage reports `last_execution_height == 0`, it is not a valid observer seed source
- Live Root Cause Found During First Destructive Rebuild Attempt:
  - The first live rebuild attempt failed before validator boot with:
    - `tar: This does not look like a tar archive`
  - Root cause: the ad hoc remote command tried to pipe one tar stream into:
    - `tar -C staged-world -xf - && tar -C data/execution-world -xf -`
  - The first `tar` consumed stdin; the second saw EOF and failed. This was a process bug in the deployment procedure, not a chain/runtime failure.
- Verification Command:
  - `bash -n scripts/p2p-public-testnet-rebuild-validators.sh scripts/p2p-public-testnet-rebuild-validators.test.sh`
  - `./scripts/p2p-public-testnet-rebuild-validators.test.sh`
- Actual Result:
  - Passed: new validator rebuild script syntax and regression test.
  - Runbook now matches the fixed transport semantics that avoid the double-untar bug.
- Next Action:
  1. Re-run the real destructive validator rebuild through `scripts/p2p-public-testnet-rebuild-validators.sh`.
  2. If validator pair reaches healthy governed height with nonzero execution heights, reseed observers from sequencer and verify blob closure before observer attach.

## 2026-06-07 16:42:00 CST / tpm
- Live Rebuild Findings: Phase C/D is now much better instrumented, and the remaining blockers are precise process-truth bugs rather than vague “deployment instability”.
- New Process Defects Confirmed:
  1. **double-untar transport bug**: the original ad hoc remote world staging tried to feed one tar stream into two remote untar steps; fixed in `scripts/p2p-public-testnet-rebuild-validators.sh` by untarring once into `staged-world/` and then copying to `data/execution-world/`.
  2. **wrong start gate ordering**: waiting for sequencer `ready` before starting storage is invalid for the two-validator governed chain, because sequencer naturally reports `consensus_peer_head_unavailable` until storage is online. The rebuild script now only requires sequencer liveness before starting storage, then checks readiness on the pair.
  3. **bundle/runtime drift**: the frozen repo evidence bundle carried a stale runtime sha (`4763...`), while the live installed package on both ECS validators is `cf05800d...`; this caused canonical restarts to fail before runtime init with `network tier runtime bundle hash mismatch`.
  4. **frozen repo registry vs host-derived validator truth mismatch**: the repo-owned frozen registry (`e01e5c...` / `1f530c...`) is not valid for the current ECS hosts while preserving their `config/node-keypair.toml`. The live runtime rejects startup with:
     - `consensus signer binding mismatch for local validator triad-testnet-sequencer: expected=e01e5c... actual=65c27d...`
     This proves the current destructive rebuild path must use **deployment-only registry/world truth derived from the preserved host keypairs**, not the repo-frozen validator registry, unless the operator also rotates/replaces the host node keypairs.
- Positive Evidence:
  - The deployment-only stage tree under `.tmp/public-testnet-ci-rebuild-stage/` already carries the host-compatible signer truth:
    - sequencer actual validator signer: `65c27d898af9c528ebd6a3762373faef110bb7bb515dfa88c447f292474aac16`
    - storage actual validator signer: `858e97be96f238ef3f6e07ec36d4ba5f503755ecb232d06a80ef1ab8aaca44f6`
  - With that stage tree, sequencer previously reached liveness and stayed up; the remaining reason the pair did not advance was the old script’s “don’t start storage until sequencer is ready” gate.
- Operational Limitation Observed:
  - The ECS hosts intermittently throttle or temporarily reject repeated password-auth SSH bursts. Single manual SSH after a short pause succeeds again, but repeated rapid reconnects can return `Permission denied (publickey,password)`.
  - The new rebuild script reduces connection count via per-host SSH control sockets, but the operator should still avoid repeated failed loops during active diagnosis.
- Next Action:
  1. Refresh the deployment-only bundle/stage from the host-derived truth path, keeping runtime sha `cf05800d...` and validator signer truth `65c27d... / 858e97...`.
  2. After the SSH auth window clears, re-run the fixed validator rebuild script against the deployment-only stage.
  3. Only if the validator pair reaches `committed_height > 0` and `last_execution_height > 0`, proceed to sequencer-sourced observer seed export and closure verification.

## 2026-06-07 17:32:00 CST / tpm
- Validator Pair Root Cause Narrowed Further:
  - Live rebuilt validators now prove the sequencer can start cleanly from the host-derived deployment truth and commit blocks (`committed_height=8`, `last_execution_height=8`), while storage also follows consensus height (`committed_height=8`) but still reports `last_execution_height=0`.
  - The practical failure mode is not “storage is down”; it is “storage re-gossips commit heads without execution hashes, so sequencer drops those peer commits and never regains a fresh peer head quorum”.
  - Code inspection in `crates/oasis7_node/src/node_engine_network.rs` and `crates/oasis7_node/src/node_engine_core.rs` confirmed the path:
    1. non-sequencer synced replication commits may legitimately skip local execution materialization
    2. when that happens, `last_execution_height` stays `0`
    3. local commit gossip later asks `commit_execution_binding_for_height(height)`
    4. without a remembered binding, the storage node emits commit messages with no execution hashes
    5. sequencer rejects those peer commits once it has locally executed that height
- Root Fix Implemented:
  - Added `PosNodeEngine::remember_execution_binding(height, block_hash, state_root)` so the engine can retain a verified execution binding even when local execution did not advance.
  - Updated `apply_synced_replication_commit` to persist the payload's validated execution binding into `execution_bindings` whenever the local node still has no binding for that height.
  - This preserves truthful status (`last_execution_height` still reflects local materialization) while allowing non-sequencer validators to propagate already-verified execution truth, which is the missing contract for validator-pair readiness.
  - Updated `scripts/p2p-public-testnet-rebuild-validators.sh` to use role-aware success gates:
    - sequencer must have `committed_height > 0` and `last_execution_height > 0`
    - storage must have `committed_height > 0`, `network_head.height >= committed_height`, and at least one connected peer
    - storage `last_execution_height == 0` no longer makes Phase D fail by itself, but it still disqualifies storage as an observer seed source
- Regression Coverage:
  - Added `synced_non_sequencer_commit_keeps_verified_execution_binding_without_local_materialization` in `crates/oasis7_node/src/tests_pos_engine_guardrails.rs`
  - Updated `scripts/p2p-public-testnet-rebuild-validators.test.sh` so the happy path covers storage `last_execution_height == 0` with a healthy replicated network head
- Verification Commands:
  - `./scripts/cargo-dev.sh test -p oasis7_node synced_non_sequencer_commit_keeps_verified_execution_binding_without_local_materialization`
  - `./scripts/p2p-public-testnet-rebuild-validators.test.sh`
  - `git diff --check`
- Actual Result:
  - Passed: new node-engine regression test
  - Passed: validator rebuild script regression test
  - Passed: diff hygiene
- Next Action:
  1. Build a fresh Linux package that includes the new non-sequencer execution-binding propagation fix.
  2. Refresh the host-truth deployment stage against that package.
  3. Re-run the destructive validator rebuild on ECS, then verify that storage peer commits are accepted by sequencer and the pair maintains fresh peer heads before moving on to observer seed export.

## 2026-06-07 19:46:05 CST / tpm
- PR Merge Flow Blocker Found and Fixed:
  - PR #361 head `4ae0be9e6585c14521b03f0dc79f0f095d483a87` was mergeable, but the `Rust` workflow failed in `required-gate -> Run required test tier`.
  - Root cause was not behavioral regression; it was the repo file-size gate:
    - `crates/oasis7/src/bin/oasis7_chain_runtime/governance_registry.rs` at `1804` lines
    - `crates/oasis7/src/bin/oasis7_chain_runtime/status_payload.rs` at `1274` lines
    - `crates/oasis7_node/src/node_engine_core.rs` at `1219` lines
    - `crates/oasis7/src/bin/oasis7_chain_runtime/oasis7_chain_runtime_tests.rs` at `1233` lines
- Scope-Limited Fix Applied:
  - Split `governance_registry` inline tests into `crates/oasis7/src/bin/oasis7_chain_runtime/governance_registry_tests.rs`
  - Split `node_engine_core` inline tests into `crates/oasis7_node/src/node_engine_core_tests.rs`
  - Split status-payload-oriented runtime tests into `crates/oasis7/src/bin/oasis7_chain_runtime/oasis7_chain_runtime_status_payload_tests.rs`
  - Extracted observability helper functions from `status_payload.rs` into `crates/oasis7/src/bin/oasis7_chain_runtime/status_payload_observability.rs`
  - Added the new top-level test module include in `crates/oasis7/src/bin/oasis7_chain_runtime.rs`
  - No runtime or assertion semantics were intentionally changed; this was a structural shrink to satisfy the repository merge gate while preserving existing coverage.
- Fresh Verification:
  - `./scripts/check-rust-file-size.sh`
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime production_release_policy_status_payload_reports_effective_policy`
  - `./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_chain_runtime network_tier_genesis_bootstrap_initializes_full_governance_world_for_empty_observer`
  - `./scripts/cargo-dev.sh test -p oasis7_node sequencer_committed_execution_does_not_wait_for_gap_sync`
  - `git diff --check`
- Actual Result:
  - Passed: file-size gate now reports zero oversized code and test files
  - Passed: moved status payload runtime test
  - Passed: moved governance bootstrap runtime test
  - Passed: moved node-engine gap-sync regression test
  - Passed: diff hygiene
- Next Action:
  1. Commit and push the structural shrink fix onto PR #361.
  2. Re-run or wait for the `Rust` workflow on the new head.
  3. If checks pass, mark the PR ready and continue normal merge watch.
