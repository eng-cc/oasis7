# task_2412c3dfe33b4b3594273194870eae8f Execution Log

- task_uid: task_2412c3dfe33b4b3594273194870eae8f
- title: rebuild public_testnet from governed bootstrap artifacts
- owner_role: tpm
- worktree_hint: null

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
  - Action: ...
  - Validation Command: ...
  - Expected Result: ...
  - Actual Result: ...
  - Blocker / Next Action: ...
-->

## 2026-06-06 11:14:00 CST / tpm
- 完成内容: 在 canonical live rebuild worktree 内启动正式执行；将来自 signer-truth/bootstrap-artifact 任务链的 governed bootstrap 真值复制进当前 worktree，并把任务 `task_2412c3dfe33b4b3594273194870eae8f` 迁移到 `committed` 后通过 `workflow-report --phase start` 进入执行态。
- 完成内容: 复核本地 fresh governed bootstrap runtime hash；`target/debug/oasis7_chain_runtime` 当前 sha256 为 `a3d825a64309909fc362f332e5a07b7f57dbeae4b58447874459036bd792c973`，与 `doc/testing/evidence/public-testnet-governed-bootstrap-bundle-2026-06-06.json` 已冻结 bundle hash 一致。
- 完成内容: 盘点 validator pair 现网基线:
  - `root@39.104.204.172` 现有 stack root `/opt/oasis7/p2p-testnet`，活跃服务至少包括 `oasis7-testnet-sequencer.service`、`oasis7-triad-sequencer.service`、`oasis7-testnet-faucet.service`。
  - `root@39.104.205.67` 现有 stack root `/opt/oasis7/p2p-testnet`，活跃服务至少包括 `oasis7-testnet-storage.service`、`oasis7-triad-storage.service`。
  - 两台主机当前 `config/node.env` 仍指向旧 world `oasis7-public-testnet-parallel-20260518` 与 live-candidate manifest，而非新的 governed bootstrap network id / manifest。
- 完成内容: 记录 runtime drift:
  - storage host `/opt/oasis7/p2p-testnet/current/bin/oasis7_chain_runtime` sha256 = `4763a211fd3e9fb71322cff786ae922285f53bfe6db2160e906fba3ad1e7ce7a`
  - storage host `/opt/oasis7/p2p-testnet/bin/oasis7_chain_runtime` sha256 = `94d461dc8a0319a6564ff32f7f7d4927b9e9eba529c440dd028fa89fcbf27733`
  - 二者均与 fresh governed bootstrap bundle hash `a3d825...` 不一致；sequencer host 后续 hash 复查因 password auth 抖动未完成，但应按同一 drift 风险处理。
- 完成内容: 盘点 observer 侧阻塞:
  - 本机 launchd `com.oasis7.testnet-fourth-local` 仍绑定旧 worktree 与旧 world/validator 参数，`last exit code = 1`，不应带入新 rebuild。
  - `sshpass` 访问 `scc@192.168.1.4` 失败：`Permission denied (publickey,password)`；当前无法对该 observer 做 destructive reset 或 fresh attach。
- Validation Command: `./scripts/pm/move-task.sh --task-uid task_2412c3dfe33b4b3594273194870eae8f --to-status committed && ./scripts/pm/workflow-report.sh --phase start --role tpm --task-uid task_2412c3dfe33b4b3594273194870eae8f && shasum -a 256 target/debug/oasis7_chain_runtime`
- Expected Result: 任务进入 committed/start 执行态，并确认本地 runtime 与 frozen bundle hash 对齐。
- Actual Result: 符合预期；任务状态进入执行态，本地 runtime hash 已确认对齐 fresh governed bootstrap bundle。
- Blocker / Next Action: 下一步对两台 ECS 先做 artifact staging + runtime 对齐，再执行 destructive reset；`192.168.1.4` observer 仍等待可用凭据或替代接入路径。

## 2026-06-06 11:31:00 CST / tpm
- 完成内容: 对 validator pair 做了第一次 live destructive bring-up 试跑，并把 blockers 收敛到跨平台 bundle truth。
- 完成内容: 已完成的 live 操作:
  - 两台 ECS 都 staged 了 governed bootstrap manifest / genesis / validator registry / bootstrap peers。
  - 两台 ECS 的 `config/node.env` 已切到 fresh governed bootstrap world id `oasis7-public-testnet-governed-20260606`；storage host 的 `NODE_ROLE` 也已改正为 `storage`。
  - 旧 `oasis7-testnet-*` / `oasis7-triad-*` 服务及 sequencer 侧 faucet 已停止；旧 live data 目录已 destructive reset。
  - 额外补齐了 manifest repo-relative runtime refs 所需的 `config/doc/testing/evidence/...` 树。
- 完成内容: 第一次失败根因已经确认不是链逻辑，而是 artifact 平台错配：
  - 最初上传到 ECS 的 runtime 是本机 `Mach-O arm64`，systemd 报 `Exec format error`。
  - 这证明 `public-testnet-governed-bootstrap-bundle-2026-06-06.json` 最初冻结的是本机 darwin binary，而不是 validator host 需要的 Linux x86_64 binary。
- 完成内容: 已将当前 worktree 里的 governed bootstrap bundle 重生为 Linux truth：
  - 从 ECS 现有 governed-registry release 拉回 Linux x86_64 `oasis7_chain_runtime` 样本到 `.tmp/public-testnet-governed-bootstrap-live/oasis7_chain_runtime.linux-x86_64`
  - 用 `./scripts/release-candidate-bundle.sh create --allow-dirty-worktree` 重新生成 `doc/testing/evidence/public-testnet-governed-bootstrap-bundle-2026-06-06.json`
  - 本地 `release-candidate-bundle validate` 已通过
- 完成内容: 在 Linux bundle truth 修正后，sequencer live bring-up 又前进了一层：
  - manifest/bundle path mismatch 已消除
  - 当前残留失败变成 runtime 首启 world bootstrap:
    - `DistributedValidationFailed: latest state root mismatch tick=1 expected=d4570a... found=2ca107...`
  - 相关历史证据 `doc/testing/evidence/shared-network-shared-devnet-triad-reset-recovery-2026-05-23.md` 已复核；我们也按证据额外清理了 `output/node-distfs/<node>` 与 `reward-runtime-execution-bridge-state.json`，但 testnet governed bootstrap 仍复现 tick=1 mismatch。
- Validation Command: `./scripts/release-candidate-bundle.sh create ... --runtime-build-ref .tmp/public-testnet-governed-bootstrap-live/oasis7_chain_runtime.linux-x86_64 --allow-dirty-worktree && ./scripts/release-candidate-bundle.sh validate --bundle doc/testing/evidence/public-testnet-governed-bootstrap-bundle-2026-06-06.json`
- Expected Result: bundle truth 改为可在 ECS 执行的 Linux runtime，并可在本地通过 bundle validate。
- Actual Result: 符合预期；bundle validate 通过。live ECS bring-up 进一步证明当前主 blocker 已从 artifact path/hash 问题收敛到 runtime cold-boot world mismatch。
- Blocker / Next Action: 用户已授权切换到“建 PR -> 走 CI 产出跨平台 package”路径。下一步不再继续手推 ECS 上的混合真值，而是基于当前证据准备 PR / CI packaging：
  1. 让 CI 产出当前源码对应的 Linux testnet package/runtime；
  2. 用 CI 产物替换 ECS 现有 2026-06-05 governed-registry release；
  3. 再次执行 validator-pair cold boot，确认 `tick=1` mismatch 是否随当前源码 Linux runtime 消失。
