# task_3778b0e747b249bc85b92b942a32b3fd Execution Log

- task_uid: task_3778b0e747b249bc85b92b942a32b3fd
- title: add on-demand launcher web build check to required gate
- owner_role: qa_engineer
- worktree_hint: /home/scc/worktrees/oasis7-testing-required-gate-launcher-web-build

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-22 22:56:54 CST / qa_engineer
- 完成内容: 为 `scripts/plan-rust-required-scope.sh` 新增 `run_launcher_web_build` / `needs_trunk` 规划输出，并把 `crates/oasis7_client_launcher/**`、`crates/oasis7_launcher_ui/**`、`crates/oasis7_proto/**`、`crates/oasis7_wasm_abi/**` 以及 `crates/oasis7/**` 的 launcher shared runtime 改动纳入 required-gate 命中范围。
- 完成内容: 为 `scripts/ci-tests.sh required` 新增 env-gated `launcher web build` 组件，为 `.github/workflows/rust.yml` 接通 planner 输出、按需 `cargo install trunk --locked` 与 `OASIS7_CI_RUN_LAUNCHER_WEB_BUILD` 透传；保持本地显式 `./scripts/ci-tests.sh required` 默认语义不变。
- 完成内容: 回写 `doc/testing/prd.md`、`doc/testing/project.md`、`doc/testing/ci/ci-tiered-execution.prd.md` 与 `doc/testing/ci/ci-tiered-execution.project.md`，明确 required-gate 的 launcher Web build 只在相关 changed paths 命中时执行，避免把 release-only `build-web-dist` 成本扩散到无关 PR。
- 完成内容: 执行 `bash -n scripts/plan-rust-required-scope.sh scripts/ci-tests.sh`、`python -c "import pathlib, yaml; yaml.safe_load(pathlib.Path('.github/workflows/rust.yml').read_text())"`、planner hit/miss 样本、`./scripts/doc-governance-check.sh`、`git diff --check`，以及 `OASIS7_CI_RUN_* = false + OASIS7_CI_RUN_LAUNCHER_WEB_BUILD=true ./scripts/ci-tests.sh required`；launcher-only required 实测跑通并完成 `trunk build --release --dist ../../output/release/web-launcher-dist`。
- 遗留事项: 当前 launcher Web build 仍会暴露既有 `dead_code` / `unused_imports` warning，但本任务范围内无新的编译阻断；若后续需要收紧 warning policy，应单独建题。

## 2026-04-23 09:47:43 CST / qa_engineer
- 完成内容: 处理 PR `#144` review comment，修正 `scripts/ci-tests.sh required` 中 launcher Web build 的默认值为 `OASIS7_CI_RUN_LAUNCHER_WEB_BUILD:-false`，避免本地显式 `./scripts/ci-tests.sh required` 在 env 未设置时默认跑 `trunk build`。
- 完成内容: 修正 `scripts/plan-rust-required-scope.sh` 的 `mark_full()`，让 `workflow_dispatch` / diff base 不可解析等 fallback-to-full 场景也会同步打开 `run_launcher_web_build`，保持 planner 不确定时的 full safety net 完整。
- 完成内容: 验证 `./scripts/plan-rust-required-scope.sh --event-name workflow_dispatch` 现在输出 `run_launcher_web_build=true` / `needs_trunk=true`；验证在 launcher env 未设置且其他 CI 组件显式关闭时，`./scripts/ci-tests.sh required` 会打印 `skip: launcher web build disabled by CI scope planner`，确认本地显式 `required` 默认语义恢复。
- 遗留事项: 无。
