# task_075b812172914487a06a93bda125bc9f Execution Log

- task_uid: task_075b812172914487a06a93bda125bc9f
- title: close wasm source hash local dependency coverage
- owner_role: wasm_platform_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-runtime-wasm-source-hash-dependency-closure

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-22 22:48:00 CST / wasm_platform_engineer
- 完成内容: 新增共享 crate `crates/oasis7_wasm_build`，用 `cargo metadata --filter-platform wasm32-unknown-unknown` 计算模块源码与本地 `path` 依赖闭包的 `source_hash`；`tools/wasm_build_suite` 与 `crates/oasis7_distfs/src/bin/sync_builtin_wasm_identity.rs` 已切到同一实现。
- 完成内容: 为共享 helper、build suite、distfs sync 各补回归，覆盖“修改本地 path 依赖源码时 source_hash 必须变化”。
- 完成内容: 回写 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.prd.md`、对应 project，以及 root `doc/world-runtime/project.md`，明确 `source_hash` 边界已从模块目录白名单收紧到依赖闭包。
- 遗留事项: 尚未跑格式化与 doc governance；若后续要进一步压缩技术债，可继续把 build identity 相关 shared helpers 从脚本/工具层再向 crate 边界内收拢。

## 2026-05-22 22:55:00 CST / wasm_platform_engineer
- 完成内容: 已执行 `cargo fmt --all`、`cargo fmt --manifest-path tools/wasm_build_suite/Cargo.toml --all`、`./scripts/doc-governance-check.sh`、`git diff --check`，并复跑 `cargo test -p oasis7_wasm_build`、`cargo test -p oasis7_distfs --bin sync_builtin_wasm_identity`、`cargo test --manifest-path tools/wasm_build_suite/Cargo.toml`。
- 遗留事项: 若继续收口，可把 build identity 相关 shared helper 进一步从单一 `source_hash` 扩展到 receipt/build-manifest 侧的重复逻辑；本轮 `source_hash` 闭包修复已完成。

## 2026-05-22 23:34:40 CST / wasm_platform_engineer
- 完成内容: 已将分支 rebase 到 `origin/main`，重新确认 `./scripts/prepare-task-pr.sh` 通过；`./scripts/ci-tests.sh required` 最终退出码 `0`，PR `#269` 已创建并推送。
- 遗留事项: `gh pr view 269` 当前仍为 `REVIEW_REQUIRED` / `BLOCKED`，等待 GitHub required checks 跑完后再做后续 review/merge 动作。

## 2026-05-23 11:00:00 CST / wasm_platform_engineer
- 完成内容: 复盘 PR `#269` 的 GitHub Actions 失败日志，确认 `collect-wasm-summaries` 三个分片都卡在 `docker/wasm-builder/Dockerfile` 的 `cargo install --path /opt/oasis7/tools/wasm_build_suite`，原因是镜像上下文未复制新引入的 `crates/oasis7_wasm_build`。
- 完成内容: 已在 `docker/wasm-builder/Dockerfile` 补充 `COPY crates/oasis7_wasm_build /opt/oasis7/crates/oasis7_wasm_build`；用最小临时上下文复跑 `cargo metadata --manifest-path tools/wasm_build_suite/Cargo.toml`，已确认 path 依赖能解析到 `oasis7_wasm_build/Cargo.toml`。
- 遗留事项: 本地完整 `./scripts/ci-m1-wasm-summary.sh --module-set m1` 回归已跨过原始 manifest 缺失点，但后续在 crates.io 下载 `syn` 时遇到网络超时；这属于环境噪音，不是本次 CI 根因。

## 2026-05-23 11:08:00 CST / wasm_platform_engineer
- 完成内容: 跟进最新一轮 PR `#269` 红灯，确认失败已前移到 Docker build context 阶段：`.dockerignore` 采用全排除 allowlist 策略，但未放行 `crates/oasis7_wasm_build/**`，导致 `COPY crates/oasis7_wasm_build /opt/oasis7/crates/oasis7_wasm_build` 在 GitHub runner 上直接报 `not found`。
- 完成内容: 已在 `.dockerignore` 补充 `!crates/`、`!crates/oasis7_wasm_build/`、`!crates/oasis7_wasm_build/**`；本地 `docker build -f docker/wasm-builder/Dockerfile .` 已确认 build context 从 `78.71kB` 扩到 `93.68kB`，并成功执行 `COPY crates/oasis7_wasm_build ...`。
- 遗留事项: builder image 后续仍可能受 crates.io 网络波动影响，但这已经越过当前 GitHub failed step 的 `context missing` 根因。

## 2026-05-23 11:42:00 CST / wasm_platform_engineer
- 完成内容: 跟进下一轮 PR `#269` 红灯，确认 `collect-wasm-summaries` 已跨过 Docker context 阶段，但 `oasis7_wasm_build::compute_source_hash` 仍把 builtin 模块自身目录当作 `workspace_root`，导致依赖 `crates/oasis7_wasm_sdk` 时在 CI 上报 `path ... is outside workspace root ...`。
- 完成内容: 已调整 `crates/oasis7_wasm_build/src/lib.rs`：若 `cargo metadata.workspace_root` 不能覆盖全部本地 path package，则回退到这些 package 的公共祖先目录；并新增回归测试覆盖“metadata workspace root 过窄但 path 依赖合法”的布局。
- 完成内容: 已执行 `cargo test -p oasis7_wasm_build`；并做针对性 `./scripts/ci-m1-wasm-summary.sh --module-set m1` 回归，确认 `m1.rule.move`、`m1.rule.visibility` 均成功产出 metadata/receipt，不再出现 `outside workspace root` 失败。
- 遗留事项: 完整 m1 summary 长跑回归已在确认修复命中后手动中止，避免继续占用本地 Docker/编译资源；最终门禁结果以 GitHub 新一轮重跑为准。

## 2026-05-23 14:10:00 CST / wasm_platform_engineer
- 完成内容: 继续收口 PR `#269` 的 wasm determinism 红灯，先重建本地 `oasis7/wasm-builder:nightly-2025-12-11`，排除旧镜像内置 `wasm_build_suite` 造成的本地复现偏差。
- 完成内容: 重新执行 `env OASIS7_WASM_SYNC_WRITE_ALLOW=local-dev ./scripts/sync-m{1,4,5}-builtin-wasm-artifacts.sh`，把当前 `source_hash` 语义对应的 `m1/m4/m5` builtin wasm identity manifests 同步到仓库基线。
- 完成内容: 已并行复跑 `./scripts/ci-m1-wasm-summary.sh --module-set m1|m4|m5`，三组均通过 `check ok: identity manifest is in sync` 与 `check ok: hash manifest is in sync with built wasm`，产物分别写到 `/tmp/oasis7-ci-wasm-m1`、`/tmp/oasis7-ci-wasm-m4`、`/tmp/oasis7-ci-wasm-m5`。
- 遗留事项: 仍需提交并推送这批 identity manifest 修正，再观察 GitHub Actions 新一轮 required checks 是否全绿。

## 2026-05-23 14:35:00 CST / wasm_platform_engineer
- 完成内容: 处理 PR `#269` 的 review thread，修正 `crates/oasis7_wasm_build::read_cargo_metadata` 对 `Cargo.lock` 的探测逻辑，改为向上搜索 workspace 祖先目录，避免成员 crate 误跳过 `cargo metadata --locked`。
- 完成内容: 为上述行为补充单测 `find_lockfile_for_metadata_searches_workspace_ancestors`，并执行 `env -u RUSTC_WRAPPER cargo test -p oasis7_wasm_build` 通过。
- 遗留事项: 仍需将 comment-fix commit 推送到 PR，并显式 resolve 当前 unresolved review threads。
