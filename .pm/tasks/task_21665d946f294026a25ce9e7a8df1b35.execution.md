# task_21665d946f294026a25ce9e7a8df1b35 Execution Log

- task_uid: task_21665d946f294026a25ce9e7a8df1b35
- title: Implement P2P auto-selected user deployment mode recommender
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-p2parch-auto-mode-recommender

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-03 14:32:01 CST / runtime_engineer
- 完成内容: 在 `oasis7_node` 落地 P2P 用户层模式推荐 substrate，新增 `auto_join / private_safe / public_entry`、reachability auto-detection 与 recommendation/effective-policy contract，并把 `public_entry` 自动升级改成显式确认门；同时把 chain runtime CLI、status payload、game launcher、web launcher、launcher UI schema 全部接上用户模式与确认位，默认路径在未显式给出原始 `deployment_mode/node_role` 时走自动推荐。
- 完成内容: 修复实现期回归，统一 `oasis7_game_launcher` 的 chain runtime 构参 helper 与实际 spawn 参数，补齐 `oasis7_web_launcher` 对新字段的测试样例，确保 launcher 面不会出现 helper 和真实启动参数不一致。
- 完成内容: 已验证 `env -u RUSTC_WRAPPER cargo test -p oasis7_node --lib auto_join_requires_confirmation_before_public_entry_upgrade`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime parse_options_reads_user_mode_detection_hints`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime build_chain_status_payload_includes_storage_metrics`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher parse_options_accepts_overrides`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher parse_options_rejects_invalid_chain_p2p_user_mode`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher build_oasis7_chain_runtime_args_includes_storage_profile`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher parse_options_accepts_overrides`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher build_chain_runtime_args_includes_chain_overrides_when_on`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher validate_game_config_reports_missing_required_fields`。
- 完成内容: commit 前独立 review 已检查当前 diff；已补修 `/v1/chain/status` 在 raw `deployment_mode/node_role` override 下误报 effective policy 的问题，并补状态测试覆盖该路径。
- 遗留事项: 当前 recommender 仍主要消费 CLI 注入的 detection hint，尚未直接接上 AutoNAT / relay reservation / hole-punch live snapshot；这条缺口已回写 `project.md`，后续需要单独网络观测切片补齐。
- 遗留事项: `P2PARCH-9` 的 viewer UX 仍未完成，当前只完成 runtime + launcher substrate 与 status surface；后续需要 `viewer_engineer` 把推荐说明、探测依据和 `公网入口` 风险确认做成用户可见交互，并补对应 `test_tier_full` 证据。
