# task_160a5a96b32945999f7ef30a84e244d3 Execution Log

- task_uid: task_160a5a96b32945999f7ef30a84e244d3
- title: upgrade live triad to current version with three full game nodes
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-triad-current-version-full-game-nodes

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-16 21:49:00 CST / runtime_engineer
- 完成内容:
  - 在 task worktree `HEAD=d104864026bb` 上构建当前 release `d104864026bb-triad-full-game-nodes-20260516-213138`，统一产出 `oasis7_chain_runtime sha256=a7f5f8d44aee4e8c0384ac3d63f3528bcadce2ae28c03976197d07ea281c3f8a`；本地补建 `oasis7_viewer_live` 并把旧 full-stack release 的 `web/` 静态目录带到新 release，使 `triad-observer-local` 继续满足“完整游戏节点”所需的 player-entry 组件。
  - 将本机 `/opt/oasis7/p2p-triad-local/current` 与两台 ECS `/opt/oasis7/p2p-triad/current` 全部切到该 release；两台 ECS `systemd` 服务与本机 `oasis7-triad-observer.service` 最终都恢复为 `active`。
  - 第一次热加入时，本机因另一套本地 `oasis7_viewer_live` 占用 `127.0.0.1:5011/5023` 以及残留 replication/runtime 状态导致本地 validator 出现 `BlobNotFound` / `peer commit execution mismatch`；已把本机 player-entry 端口改到 `5111/5123/4273`，并确认真正的阻断面是本地 `output/node-distfs/triad-observer-local` 等复制状态残留，而不是新 binary 本身。
  - 随后对本机 + 两台 ECS 同步执行三端冷重置，统一迁出 live `execution-world`、`execution-records`、`storage`、`output/node-distfs/<node>`、`output/chain-runtime/<node>` 到 `20260516-214642-triad-synchronous-reset-*` 备份，再同步拉起三端。
  - 最终复跑 `P2PARCH6_SEQ_SSH_PASSWORD='***' P2PARCH6_STORAGE_SSH_PASSWORD='***' ./scripts/p2p-real-env-triad-snapshot.sh --samples 4 --interval-secs 5 --out-dir .tmp/p2p_real_env_triad_sync_reset`，产物 `.tmp/p2p_real_env_triad_sync_reset/20260516-214745/summary.md` 给出 `claim_status=pass_candidate`、`claim_mode=three_equal_validator`、`failure_signatures=(none)`；三节点都在 same-window 内以 `role=sequencer` 推进且 `last_error=null`。
- 遗留事项:
  - 为了不和另一套本地 launcher/viewer 进程抢占 `5011/5023/4173`，本机 triad 的 player-entry 端口目前临时改为 `PLAYER_ENTRY_VIEWER_BIND=127.0.0.1:5123`、`PLAYER_ENTRY_WEB_BIND=127.0.0.1:5111`、`PLAYER_ENTRY_HTTP_PORT=4273`；若后续需要恢复原端口，应先停掉冲突中的本地 `oasis7_game_launcher/oasis7_viewer_live` 进程，再回切配置。

## 2026-05-17 18:06:18 CST / runtime_engineer
- 完成内容:
  - 继续追查 triad 停滞时，确认 2026-05-16 那轮 `pass_candidate` 只覆盖冷重置后的短窗恢复，不代表问题已经根除；其后本机 player-entry 端口已切回默认 `4173/5011/5023/5633`。
  - 先前卡死根因已落到 execution 持久态矛盾：local 与 storage 都曾出现 `execution-records/latest.json` 停在 `435`、缺少 `436.json`，但 `execution-world/snapshot.manifest.json` 已指向错误的 `epoch=436`；sequencer 真值 `436` 的 `state_root` 不同，导致两坏节点在收到更高高度时触发 `execution driver missing predecessor record for non-contiguous committed height`。
  - 为恢复服务，已对本机与两台 ECS 再次执行同步冷重置并重启，新增备份目录：
    - 本机：`/opt/oasis7/p2p-triad-local/backups/20260517-144932-triad-synchronous-reset-local`
    - ECS sequencer：`/opt/oasis7/p2p-triad/backups/20260517-144932-triad-synchronous-reset-sequencer`
    - ECS storage：`/opt/oasis7/p2p-triad/backups/20260517-144932-triad-synchronous-reset-storage`
  - 冷重置后短窗直连复核曾恢复正常：本机 / sequencer / storage 先对齐到 `committed_height=10` 且 `last_error=null`，随后继续推进到 `13/13/12`，再追平到 `14/14/14`。
  - 但 2026-05-17 18:06 CST 再次直连复核时，triad 已重新复发：
    - 本机 `triad-observer-local`: `committed_height=617`, `network_committed_height=617`, `last_error=\"node execution error: execution driver missing predecessor record for non-contiguous committed height: last_applied=617 incoming=642 predecessor=641\"`
    - ECS `triad-sequencer-a`: `committed_height=753`, `network_committed_height=753`, `last_error=null`
    - ECS `triad-storage-b`: `committed_height=617`, `network_committed_height=618`, `last_error=\"node execution error: execution driver missing predecessor record for non-contiguous committed height: last_applied=617 incoming=753 predecessor=752\"`
  - 这说明 2026-05-17 的同步冷重置只带来了阶段性恢复，问题仍会在链继续前进后再次把 local + storage 拉回同类缺前驱记录故障；当前真实环境结论应回退为 `blocked`，不能继续宣称三节点稳定健康。
  - 另外确认 `.tmp/p2p_real_env_triad_post_reset/20260517-145305/summary.md` 所对应的 repo snapshot 脚本仍会误报云节点不可达；云端健康/停滞判断应以直连 SSH + `curl /v1/chain/status` 为准。
- 遗留事项:
  - 需要继续追 `execution-world` 与 `execution-records` 在长窗推进中为何再次分叉，重点应放在 restore snapshot / execution bridge / replication 历史落盘一致性，而不是单纯重复冷重置。

## 2026-05-17 18:39:59 CST / runtime_engineer
- 完成内容:
  - 继续沿代码和现网双向收敛后，确认 `oasis7_node` 里存在一个可直接导致“execution 失败后跳过缺失高度、随后长期报 missing predecessor record”的游标推进缺陷：
    - `crates/oasis7_node/src/node_engine_replication.rs`
    - `crates/oasis7_node/src/replication_probe_gate.rs`
  - 原问题是 gap-sync / successor-probe 在 `apply_synced_replication_commit(...)` 成功前，就先把 `replication_persisted_height` 推到目标高度；一旦该高度的 execution hook 失败，下一轮就会跳过该高度继续向上，最终把节点锁死在 `missing predecessor record`。
  - 已将上述两处游标推进时序改为“只有 execution commit 成功后才推进游标”，并新增两条回归测试锁住该行为：
    - `successor_probe_does_not_advance_replication_cursor_when_execution_fails`
    - `gap_sync_does_not_advance_replication_cursor_when_execution_fails`
  - 同时复跑既有基线 `runtime_network_replication_gap_sync_fetches_missing_commits`，确认正常 gap-sync 没被打坏。
  - 新 patched release `target/release/oasis7_chain_runtime` 已编译完成，`sha256=268c8d22cc8a0599239c3d9f84433daa99e78d6bab16166a9e6c2ad1befc72a4`，并已 rollout 到：
    - 本机 `/opt/oasis7/p2p-triad-local/current/bin/oasis7_chain_runtime`
    - ECS `/opt/oasis7/p2p-triad/current/bin/oasis7_chain_runtime`
  - 随后再次对三节点执行同步冷重置并重启，新增备份目录：
    - 本机：`/opt/oasis7/p2p-triad-local/backups/20260517-183959-triad-patched-reset-local`
    - ECS sequencer：`/opt/oasis7/p2p-triad/backups/20260517-183959-triad-patched-reset-sequencer`
    - ECS storage：`/opt/oasis7/p2p-triad/backups/20260517-183959-triad-patched-reset-storage`
  - patched rollout 后的直连复核目前为止保持健康推进：
    - 第一轮：`2 / 2 / 2`
    - 第二轮：`4 / 4 / 4`
    - 第三轮：`7 / 7 / 7`
    - 第四轮：`12 / 12 / 12`
    - 四轮中 local / sequencer / storage 的 `last_execution_height` 均与 `committed_height` 对齐，`last_error` 持续为 `null`
- 遗留事项:
  - 当前只确认 patched rollout 后的短窗恢复稳定，仍需继续拉长观察窗口，确认是否还能复发到此前的 `617 -> 619` 跳高故障。

## 2026-05-17 18:53:29 CST / runtime_engineer
- 完成内容:
  - 在 patched rollout 完成后继续用直连 `curl /v1/chain/status` 复核三端，本机 `5633`、ECS sequencer `5631`、ECS storage `5632` 当时都推进到 `committed_height=57`，且 `network_committed_height=57`、`last_execution_height=57`、`last_error=null`。
  - 这说明修补后的 triad 不只停在最初的 `2/4/7/12` 短样本，而是又继续向前推进了一段观察窗，当前可以把 live evidence 从“已知复发阻断”收敛为“patched rollout 观察窗内恢复为 pass_candidate”。
  - 已同步把本轮根因、回归测试、patched binary sha、三端 `20260517-183959-triad-patched-reset-*` 备份目录和 `57/57/57` 复核结果回写到 `doc/testing/evidence/p2p-real-env-triad-current-version-full-game-nodes-2026-05-16.md`。
- 遗留事项:
  - 长窗不复发仍未完全证明；如果后续还要把 `pass_candidate` 升格为长期稳定结论，需要继续观察是否会再次出现 `missing predecessor record`。

## 2026-05-17 18:56:06 CST / runtime_engineer
- 完成内容:
  - 已执行 `./scripts/pm/task-closeout.sh --role runtime_engineer --task-uid task_160a5a96b32945999f7ef30a84e244d3`，当前 task yaml 已写入 `status=done` 与 `last_closed_at=2026-05-17T18:56:06+08:00`。
- 遗留事项:
  - closeout 尾部的 repo 级 `pm lint` 仍失败，但失败原因是无关 task `task_7aaccc0bf0094d64ab0e155087610eae` 缺 `source_ref`，不是本轮 triad 修复引入的新问题。
