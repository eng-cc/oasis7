# task_7021c28970ef4f40b0367563df7f1e32 Execution Log

- task_uid: task_7021c28970ef4f40b0367563df7f1e32
- title: public-chain-style testnet mechanism skeleton
- owner_role: producer_system_designer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-public-chain-testnet-mechanism-2026-05-14

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-05-14 10:21:12 CST / producer_system_designer
- 完成内容: 新增 `doc/p2p/blockchain/p2p-formal-network-tiers-testnet-mechanism-2026-05-14.{prd,design,project}.md`，把公共主链式 `local_devnet -> shared_devnet -> public_testnet -> mainnet` 四层模型、`network_tier_manifest` schema、`public_testnet` 的 public RPC/explorer/faucet/reset 语义与 `mainnet` 的 `no faucet + frozen reset + MAINNET-1~4` gate 冻结为正式专题。
- 完成内容: 回写 `doc/p2p/prd.md`、`doc/p2p/project.md`、`doc/p2p/prd.index.md` 与 `testing-manual.md`，将本轮变更接入 `PRD-P2P-028` 主追踪，并按项目治理把模块项目页主任务命名收敛到稳定 slug `formal-network-tiers-testnet-mechanism`。
- 完成内容: 新增 repo-owned skeleton `scripts/network-tier-manifest.sh`、`scripts/network-tier-manifest-smoke.sh` 以及 `doc/testing/templates/network-tier-{shared-devnet,public-testnet,mainnet}.example.json`，用于后续 runtime / QA / liveops 接线前的 manifest create/validate 真值固定。
- 完成内容: 已验证 `bash -n scripts/network-tier-manifest.sh scripts/network-tier-manifest-smoke.sh`、`./scripts/network-tier-manifest-smoke.sh`、三份 example manifest 的 `validate`、`./scripts/doc-governance-check.sh` 与 `git diff --check` 全部通过。
- 遗留事项: 当前只完成 spec + config/script skeleton；仓库仍未建立 live `public_testnet` 或 live `mainnet`，后续还需要 `runtime_engineer` 把 tier manifest 接到 network profile / genesis / bootstrap 入口，再由 `qa_engineer` 与 `liveops_community` 建 first public testnet rehearsal 与 exit review。

## 2026-05-14 11:38:00 CST / runtime_engineer
- 完成内容: 新增共享 Rust 模块 `crates/oasis7/src/network_tier_manifest.rs`，将 `oasis7.network_tier_manifest.v1` 的 schema、bootstrap peer ref 装载和基础校验收进 repo 内共享真值。
- 完成内容: `oasis7_chain_runtime` 已接入 `--network-tier-manifest`，支持从 manifest 读取 `chain_id/world_id` 默认值、bootstrap peer file 与 status 面的 network tier 暴露；`/v1/chain/status` 现可区分当前运行层是否绑定 `shared_devnet/public_testnet/mainnet` formal manifest。
- 遗留事项: 正在继续补 `oasis7_game_launcher` / `oasis7_web_launcher` 的 manifest 透传测试和编译收口；本轮仍不宣称 live public testnet/mainnet 已存在。

## 2026-05-14 11:41:00 CST / viewer_engineer
- 完成内容: `oasis7_game_launcher` 与 `oasis7_web_launcher` 已开始接入 `chain_network_tier_manifest` 配置面；当该字段存在时，launcher 会优先透传 formal manifest，而不再强制要求本地重复输入 storage profile / node role。
- 遗留事项: 仍需跑完 launcher 定向测试与 web control plane 编译，确认 manifest 模式和 legacy 显式参数模式都可兼容。

## 2026-05-14 12:18:00 CST / qa_engineer
- 完成内容: 新增 `doc/testing/templates/{shared-devnet,public-testnet,mainnet}-{genesis.example.json,bootstrap.example.txt}`，并把 `network-tier-manifest.sh validate` 收紧到会校验 `genesis_ref/bootstrap_peer_ref` 引用文件存在、claim/gate 列表条目非空，以及 `public_testnet/mainnet/shared_devnet` 的关键 claims 语义不越界。
- 完成内容: 新增 `scripts/network-tier-exit-review.sh` 与 `doc/testing/templates/public-testnet-{rehearsal,exit-review}-template.md`，让 `public_testnet` rehearsal review 与 `mainnet` gate review 至少有一套 repo-owned skeleton 输入。
- 完成内容: `scripts/network-tier-manifest-smoke.sh` 已覆盖三份 example manifest 校验以及 `public_testnet/mainnet` 的 exit-review 脚本冒烟。
- 遗留事项: 当前仍只有 schema/runtime/QA skeleton；仓库里还没有 live `public_testnet` 的 public RPC/explorer/faucet/reset 实证，也没有 live `mainnet` gate 通过证据。

## 2026-05-14 12:18:00 CST / liveops_community
- 完成内容: 正式补齐 `public_testnet` rehearsal / exit-review 模板，明确对外口径只能基于 public RPC/explorer/faucet/reset 实证；在这些 live evidence 建立前，不得把当前 shared release-train 或示例 manifest 对外称为 live public testnet。
- 完成内容: `mainnet` 对外口径继续限制为 `specified_skeleton_only`，只有 `MAINNET-1~4` 实证完成、无 faucet、frozen reset 后才允许进入 mainnet claims。
- 遗留事项: 后续仍需在真实 public 节点窗口生成 rehearsal 记录、exit-review 结论与对外公告草案。

## 2026-05-14 12:18:00 CST / producer_system_designer
- 完成内容: 回写项目文档与 `testing-manual.md`，把本专题口径更新为“formal mechanism 已补齐，runtime/launcher 已接线，public_testnet/mainnet 仍非 live 网络”；避免继续保留“runtime 尚未接线”的过时描述。
- 遗留事项: 若后续真的建立 live `public_testnet` 或进入 `MAINNET-1~4` 实证，需要新任务沿正式 runbook/evidence 路径推进，而不是复用本专题 skeleton 直接对外宣称已上线。
