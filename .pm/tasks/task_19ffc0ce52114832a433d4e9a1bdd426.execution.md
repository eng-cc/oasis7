# task_19ffc0ce52114832a433d4e9a1bdd426 Execution Log

- task_uid: task_19ffc0ce52114832a433d4e9a1bdd426
- title: align software_safe hosted auth surface with implemented session/recovery truth
- owner_role: viewer_engineer
- worktree_hint: /home/scc/worktrees/oasis7-viewer-hosted-auth-surface-truth-alignment

## 2026-05-14 23:08:09 CST / viewer_engineer
- 完成内容: 在 `crates/oasis7_viewer/software_safe_src/legacy_core.js` 对齐 hosted auth truth surface，避免继续把已实现的 hosted `player_session` issue / reconnect / release 说成“未实现”。具体收口为：`player_session` 注册成功态改标为 `active_hosted_session`，`strong_auth` 梯度改标为 `preview_only`，`reconnect` 文案区分 legacy preview bootstrap 与 hosted browser session 的真实恢复路径，并把 hosted guest lane 的 `strong_auth_required` 原因改成“先获取 player_session，再进入 backend re-authorization”。
- 完成内容: 在 `crates/oasis7_viewer/software_safe_src/main.jsx` 扩展 `tierBadgeClass`，让 `active_hosted_session` 与 `preview_only` / `issued_pending_register` 在 UI 上分别落到正确的 good/accent 语义，不再把 preview-grade 或 pending hosted state 一律渲染成 warn。
- 完成内容: 为 repo-owned 验证补两层断言。其一，在 `crates/oasis7_viewer/software_safe_src/main.test.jsx` 新增 hosted public-join 场景，覆盖 `Session Ladder`、`Release Player Session` 按钮、`active_hosted_session` / `preview_only` badge、`reconnect_sync` 文案，以及资产 lane 继续保持 stronger hosted custody blocker 的真值。其二，在 `crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs` 新增 `buildAuthSurfaceModel()` 的 hosted truth contract，直接锁定 tier status、reconnect summary、`prompt_control` preview enable 与 `main_token_transfer` 仍为 `strong_auth_required`。
- 完成内容: 重新构建 `crates/oasis7_viewer/software_safe.js`，保持 source 与发布 bundle 同步。
- 验证结果: `npm --prefix crates/oasis7_viewer run test:ui` 通过；`node crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs` 通过；`npm --prefix crates/oasis7_viewer run build:software-safe` 通过；`./scripts/doc-governance-check.sh` 通过；`git diff --check` 通过。
- 遗留事项: 当前切片只收 hosted auth/session/recovery 的 player-facing truth surface，不新增 hosted-ready 资产/治理能力，也不改变 `main_token_transfer` 继续停留在 stronger custody lane 的产品边界。
