# task_91949e5879a8434db3a55d3012b76950 Execution Log

- task_uid: task_91949e5879a8434db3a55d3012b76950
- title: Resolve P2PARCH-6 real-env observer peering blocker
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-p2parch-6-real-env-observer-peering-followup

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-08 10:40:20 CST / runtime_engineer
- 完成内容: 复核当前真实环境下的本机 observer 真值。直接读取 `/opt/oasis7/p2p-triad-local/config/node.env`、`systemctl is-active oasis7-triad-observer.service` 与 `http://127.0.0.1:5633/v1/chain/status` 后确认：本机 observer 当前已不再复现 `known_peer_heads=0 / network_committed_height=0`，而是处于 `known_peer_heads=1 / network_committed_height=61399 / committed_height=3638`；当前最稳定的本机 blocker 已收敛为 `gap sync height ... blob not found`。
- 完成内容: 用 `./scripts/p2p-real-env-triad-snapshot.sh --samples 2 --interval-secs 3 --out-dir .tmp/p2p_real_env_triad` 补跑当前环境采样，产物位于 `.tmp/p2p_real_env_triad/20260408-102126/`。本轮因未提供远端 ECS SSH 凭据，只完整采到本机 observer，summary 因云端不可采样而记为 `blocked`；已将这一限制 truthfully 回写到新 evidence 文档，避免把“本轮缺远端认证”误写成“云端服务真实失效”。
- 完成内容: 为 `oasis7_node` 的 `sync_replication_height_once` 补齐 provider-aware gap-sync blob fetch：当前路径会先按 DHT provider lookup 对 `fetch-blob` 做 provider-restricted 请求；若遇到 `NetworkProtocolUnavailable`、无 connected providers 或 provider-route 暂时不可用，再回退到普通 lane-aware request，而不是继续盲打任意已连接 peer 或直接把该高度判死。
- 完成内容: 新增并通过两条定向回归：`runtime_network_replication_gap_sync_prefers_dht_blob_providers` 与 `runtime_network_replication_gap_sync_falls_back_after_provider_route_unavailable`。同时复跑 `env -u RUSTC_WRAPPER cargo test -p oasis7_node runtime_network_replication_gap_sync_ -- --nocapture`，确认现有 `fetches_missing_commits / not_found_is_non_fatal / reports_error_after_retries_exhausted` 语义未被破坏；`env -u RUSTC_WRAPPER cargo check -p oasis7_node` 也已通过。
- 完成内容: 新增正式 evidence `doc/testing/evidence/p2p-real-env-observer-gap-sync-followup-2026-04-08.md`，并回写 `doc/p2p/network/p2p-mainnet-private-reachability-architecture-2026-04-01.project.md`，把 `P2PARCH-6` 当前真实环境 blocker 从“observer peer-head-zero”更新为“observer gap-sync blob not found + 尚缺带远端认证的 same-window triad reconfirmation”。
- 完成内容: 提交前已完成独立 subagent review；review 无 findings，残余风险仅保留“同窗 triad 仍缺远端认证复核”与“当前 diff 没有重新证明 consensus gossip + gap sync 的联合场景”，两者均已在 evidence / review 结论中显式记录。
- 遗留事项: 本轮尚未在带远端 ECS 认证的 same-window triad 上重跑完整 real-env 证据，因此不能把 `P2PARCH-6` 升级为 `pass`；下一步应继续沿 `content_hash -> provider publish freshness -> remote fetch-blob availability` 追查，并在带认证的真实三节点窗口里重采 `sequencer/storage` 同步状态。
