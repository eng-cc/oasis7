# task_0deb46f616874e40b147407f6b1e510b Execution Log

- task_uid: task_0deb46f616874e40b147407f6b1e510b
- title: Audit wasm code and Rust dependency pruning opportunities
- owner_role: tpm
- worktree_hint: /Users/scc/ccwork/worktrees/oasis7-engineering-wasm-prune-audit

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
  - Action: ...
  - Validation Command: ...
  - Expected Result: ...
  - Actual Result: ...
  - Blocker / Next Action: ...
-->

## 2026-06-14 13:37:40 CST / tpm
- 完成内容: WORKFLOW BOOTSTRAP DECIDED
  - Repository State Impact: read-only professional repository governance audit; no code deletion requested yet.
  - Isolation Decision: created dedicated task worktree from `origin/main` at `/Users/scc/ccwork/worktrees/oasis7-engineering-wasm-prune-audit`; source main was behind and not used for edits.
  - Task Truth: owner role `tpm`; `.pm` task `task_0deb46f616874e40b147407f6b1e510b`; source ref `doc/engineering/project.md`; workflow doc ref `doc/engineering/source-of-truth.md`.
  - Routed Next Phase: repo-owned router step 0, read-only professional/domain judgment, because user asked engineering governance to assess wasm-related deletable code/Rust dependencies.
- 遗留事项: none for bootstrap; implementation was deferred until the user explicitly said `做`.
- Action: WORKFLOW ROUTE DECIDED
  - Selected workflow surface: bounded professional subagent slices; no implementation/PR closeout unless user asks to apply pruning.
  - Skipped: TDD and execution patches skipped because this task is audit-only; verification-before-completion will be used only for final evidence sanity, not a code completion claim.
- Subagent Slice Plan:
  - Slice 1 role: `repository_health_engineer`
  - Slice 1 type: bounded read-only repository-health audit
  - Slice 1 intended model configuration: workflow source-of-truth Default subagent runtime
  - Slice 1 actual dispatched model/reasoning: inherited/unverified; current subagent tool inherits parent by default and does not expose verified model after dispatch
  - Slice 1 context delivery mode: full-thread/full-history fork plus this explicit checklist
  - Slice 1 mandatory context checklist/packet: identity and authority from `AGENTS.md`; workflow governance from `doc/engineering/workflow/source-of-truth.md`; task truth from `.pm/tasks/task_0deb46f616874e40b147407f6b1e510b.*`; user intent is to identify wasm-related code or Rust dependency pruning opportunities; scoped repo context includes Cargo workspace, `crates/*wasm*`, feature edges that pull `wasmtime`/WASM stacks, docs under `doc/world-runtime/wasm/*` if present, and previous known risk around `oasis7_client_launcher` default features; collaboration boundary is findings only, no edits.
  - Slice 1 write scope: append findings to this execution log only; no code/docs edits.
  - Slice 1 return contract: ranked deletion/pruning candidates with file/dependency evidence, risk level, owner recommendation, validation command, and whether wasm_platform_engineer confirmation is needed.
  - Slice 2 role: `wasm_platform_engineer`
  - Slice 2 type: bounded read-only WASM platform semantic review
  - Slice 2 intended model configuration: workflow source-of-truth Default subagent runtime
  - Slice 2 actual dispatched model/reasoning: inherited/unverified; current subagent tool inherits parent by default and does not expose verified model after dispatch
  - Slice 2 context delivery mode: full-thread/full-history fork plus this explicit checklist
  - Slice 2 mandatory context checklist/packet: same task/workflow/user intent; focus on whether candidate removals would violate ABI, manifest/hash, module lifecycle, permissions, execution fallback, compatibility, or future platform surfaces.
  - Slice 2 write scope: append findings to this execution log only; no code/docs edits.
  - Slice 2 return contract: confirmed safe/unsafe/unknown pruning candidates, wasm semantic risk notes, required validation commands, and any candidate that must not be removed without a follow-up task.
  - Formal sink: `.pm/tasks/task_0deb46f616874e40b147407f6b1e510b.execution.md`
  - Integration owner/order: `tpm` gathers local inventory while slices run, then integrates repository-health findings with wasm-platform confirmations into a user-facing audit summary.
- Validation Command: task/worktree bootstrap via `./scripts/new-task-worktree.sh ... --json`
- Expected Result: dedicated task worktree, `.pm` task, and execution-log sink exist before professional slice dispatch.
- Actual Result: created branch `task/engineering-wasm-prune-audit`; worktree and `.pm` task created successfully.
- Blocker / Next Action: dispatch bounded slices and collect current wasm dependency/code inventory.

## 2026-06-14 13:55:06 CST / tpm
- 完成内容: Integrated `repository_health_engineer` and `wasm_platform_engineer` read-only findings.
  - Consensus: no core wasm crate or heavy runtime dependency is safe to directly delete from current platform semantics.
  - Best pruning candidate: `oasis7_wasm_build` should not be deleted, but can be investigated for dependency-closure isolation because it appears tied to `sync_builtin_wasm_identity`/build identity tooling while entering broader `oasis7_distfs` closure.
  - Safe small cleanup candidate: remove unused imports in `tools/wasm_build_suite/src/lib.rs` (`oasis7_wasm_build::DEFAULT_WASM_TARGET`, `std::io`) in a follow-up patch task.
  - Not recommended for deletion: `wasmtime`, `wasmparser`, `wasm-encoder`, `wat`, module-level builtin wasm `Cargo.lock` files, `oasis7_wasm_abi/sdk/executor/router/store/build`.
  - Boundary: `wasm-bindgen*` belongs to browser/viewer bridge rather than wasm runtime platform; needs viewer/platform owner if separately audited.
- 遗留事项: implementation not started in this audit entry; candidate patch scope is dependency isolation plus small import cleanup.
- Action: integrate bounded professional findings and define patchable follow-up scope.
- Validation Command: `env -u RUSTC_WRAPPER cargo tree -p oasis7 -i wasmtime`
- Expected Result: audit distinguishes real runtime wasm execution dependencies from non-executor consumer closure leaks.
- Actual Result: `wasmtime -> oasis7_wasm_executor -> oasis7`, confirming default `oasis7` feature still pulls real executor backend.
- Validation Command: `env -u RUSTC_WRAPPER cargo tree -p oasis7 --no-default-features -i wasmtime`
- Actual Result: failed with package ID not found, confirming `wasmtime` is absent when `oasis7` defaults are disabled.
- Validation Command: `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i wasmtime`
- Actual Result: failed with package ID not found, confirming launcher remains protected by `default-features = false`.
- Validation Command: `env -u RUSTC_WRAPPER cargo check -p oasis7_wasm_abi -p oasis7_wasm_build -p oasis7_wasm_executor -p oasis7_wasm_router -p oasis7_wasm_sdk -p oasis7_wasm_store`
- Actual Result: passed; existing `oasis7_wasm_executor` unused metric observer warnings observed.
- Validation Command: `env -u RUSTC_WRAPPER cargo test -p oasis7_wasm_executor --no-default-features --lib`
- Actual Result: passed, 8 tests.
- Validation Command: `env -u RUSTC_WRAPPER cargo check -p oasis7 --no-default-features`
- Actual Result: passed; unrelated existing warnings observed in non-wasm binaries.
- Blocker / Next Action: audit can be reported to user. If user wants implementation, create/continue a patch scope for `oasis7_wasm_build` dependency isolation and/or small unused-import cleanup with wasm/repository-health ownership.

## 2026-06-14 13:53:53 CST / wasm_platform_engineer
- 完成内容: Read-only WASM platform pruning audit completed for ABI / manifest / hash / module lifecycle / permission / execution compatibility.
  - Role boundary: acting as bounded `wasm_platform_engineer` slice; no code or docs changed beyond this execution-log sink.
  - Context inspected: `AGENTS.md`; `.agents/roles/wasm_platform_engineer.md`; `doc/engineering/workflow/source-of-truth.md` sections 0-5 enough for sink/worktree boundaries; `.pm/tasks/task_0deb46f616874e40b147407f6b1e510b.{yaml,execution.md}`; Cargo manifests; `crates/oasis7_wasm_*`; `tools/wasm_build_suite`; `tools/wasm_module_observe`; `crates/oasis7/src/runtime/{manifest.rs,builtin_wasm_identity_manifest.rs,builtin_wasm_materializer.rs}`; runtime module routing/persistence/permission tests.
- 遗留事项: no direct wasm core deletion approved; any default-feature policy change needs separate runtime/QA validation.
- Action: classify pruning candidates for wasm ABI/lifecycle/execution safety.
- Findings:
  - safe-to-consider:
    - `tools/wasm_build_suite/src/lib.rs` has two non-semantic unused imports reported by `cargo check --manifest-path tools/wasm_build_suite/Cargo.toml`: `oasis7_wasm_build::DEFAULT_WASM_TARGET` and `std::io`. Removing those imports is safe from WASM ABI/manifest/hash/lifecycle semantics because they are compile-time lint-only cleanup and not part of module wire format or build manifest hashing.
    - Tightening native consumers to `oasis7 = { default-features = false }` remains safe where the consumer does not execute modules. Existing evidence: `crates/oasis7_client_launcher/Cargo.toml` already does this, and `cargo tree -p oasis7_client_launcher -i wasmtime` reports no `wasmtime` package. This is a consumer-boundary pruning pattern, not permission to remove the executor from runtime/node paths.
  - unsafe-to-remove:
    - `crates/oasis7_wasm_abi` is the shared ABI and lifecycle contract surface. It defines `ModuleManifest`, `ModuleAbiContract`, `ModuleChangeSet`, registry records, `ModuleCallRequest`, `ModuleContext`, `ModuleOutput`, error codes including permission/policy failures, and `ModuleSandbox`. Removing or flattening it would break canonical CBOR inputs, manifest hash context, module lifecycle events, and tests under `crates/oasis7/src/runtime/tests/*`.
    - `crates/oasis7_wasm_sdk` is the no_std module-side lifecycle/wire helper used by built-in modules through `oasis7_wasm_sdk = { features = ["wire"] }` in `crates/oasis7_builtin_wasm_modules/*/Cargo.toml`. Removing it would break module export/dispatch and CBOR wire compatibility.
    - `crates/oasis7_wasm_executor` and its optional `wasmtime` dependency are required for real native module execution, limits, fuel/timeout, compiled cache, and structured failure mapping. Evidence: `cargo tree -p oasis7 -i wasmtime` shows `wasmtime -> oasis7_wasm_executor -> oasis7`; `tools/wasm_module_observe/Cargo.toml` explicitly enables `oasis7_wasm_executor/features = ["wasmtime"]`; `doc/world-runtime/wasm/wasm-executor.prd.md` says default real execution avoids `SandboxUnavailable`. Removing `wasmtime` from runtime would be a behavior/compatibility change, not a safe prune.
    - `crates/oasis7_wasm_router` is runtime routing and validation for subscription stages/filters. Evidence: `crates/oasis7/src/runtime/world/module_runtime.rs` calls `prepare_subscriptions`, `prepared_module_subscribes_to_event`, and `prepared_module_subscribes_to_action`; `crates/oasis7/src/runtime/world/base_layer.rs` validates subscription stage/filter shape before install/activation. Removing it risks routing, permission-stage, and lifecycle compatibility.
    - `crates/oasis7_wasm_store` is runtime persistence for registry/meta/artifact bytes. Evidence: `crates/oasis7/src/runtime/world/persistence.rs` calls `save_module_store_to_dir` / `load_module_store_from_dir`, verifies manifest equality and sha256 artifact hashes, and revalidates artifact identity on load. Removing it would break module persistence and hash integrity.
    - `crates/oasis7_wasm_build` is source-hash/build-manifest support. Evidence: `tools/wasm_build_suite/src/lib.rs` and `crates/oasis7_distfs/src/bin/sync_builtin_wasm_identity.rs` call `compute_source_hash`; `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md` records it as the shared dependency-closure source hash helper. Removing it risks identity/attestation drift.
    - `wasmparser` and `wasm-encoder` in `tools/wasm_build_suite/Cargo.toml` are used by canonicalization / deterministic build tooling; removing them would invalidate deterministic artifact/hash pipeline unless a replacement canonicalizer is designed and verified.
    - `wat` in `crates/oasis7_wasm_executor/Cargo.toml` is dev-only but supports executor tests; removing it is not a production shrink and would reduce regression coverage of trap/output/fuel/cache behavior.
  - unknown-needs-follow-up:
    - Changing `crates/oasis7/Cargo.toml` default feature from `default = ["wasmtime"]` to no-default may reduce compile closure, but from WASM platform semantics it is unsafe without a follow-up owner decision because native runtime/module execution would default to `SandboxUnavailable`; docs explicitly describe default real execution as desired for production/readme gap closure.
    - `wasm-bindgen` / `wasm-bindgen-test` in `crates/pixel_world_bridge/Cargo.toml` are browser bridge dependencies rather than core module platform dependencies. WASM-platform slice has no deletion approval here; viewer/platform owners should confirm browser bridge test/runtime needs before pruning.
    - WIT-related packages appear in `Cargo.lock` as transitives of `wasmtime`/tool stacks, not direct repo dependencies. They are only removable by changing the parent dependency boundary; no direct delete candidate was found.
    - `oasis7_wasm_executor` no-default check reports unused metric observer functions (`observe_wasm_executor_compile`, `observe_wasm_executor_instantiate`, `observe_wasm_executor_entrypoint_call`, `observe_wasm_executor_decode`). They are used by the `wasmtime` feature path, so direct deletion is unsafe; a future cleanup could consider cfg-gating or warning suppression if repository-health wants a warning-only task.
- Validation Command:
  - `rg -n "wasmtime|wasmparser|wasm-encoder|wasm-smith|\\bwat\\b|wit-|wasm-bindgen|oasis7_wasm|wasm32|default-features|features|optional" Cargo.toml crates/*/Cargo.toml tools/*/Cargo.toml crates/oasis7_builtin_wasm_modules/*/Cargo.toml`
  - `rg -n "oasis7_wasm_store|ModuleStore|write_artifact\\(|read_artifact\\(|write_meta\\(|read_meta\\(|save_registry\\(|load_registry\\(" crates tools scripts doc -g '!target'`
  - `rg -n "oasis7_wasm_build|compute_source_hash|DEFAULT_WASM_TARGET|SourceHashError" crates tools scripts doc -g '!target'`
  - `rg -n "oasis7_wasm_router|prepare_subscriptions|module_subscribes_to|validate_subscription|prepared_module" crates tools scripts doc -g '!target'`
  - `rg -n "oasis7_wasm_executor|WasmExecutor|FixedSandbox|ModuleSandbox|execute_module_call|step_with_modules|SandboxUnavailable" crates tools scripts doc -g '!target'`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7 -i wasmtime`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i wasmtime`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7_wasm_executor -i wasmtime --features wasmtime`
  - `env -u RUSTC_WRAPPER cargo tree --manifest-path tools/wasm_build_suite/Cargo.toml -i wasmparser && env -u RUSTC_WRAPPER cargo tree --manifest-path tools/wasm_build_suite/Cargo.toml -i wasm-encoder`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7_wasm_executor -i wat`
  - `env -u RUSTC_WRAPPER cargo check -p oasis7_wasm_abi -p oasis7_wasm_build -p oasis7_wasm_executor -p oasis7_wasm_router -p oasis7_wasm_sdk -p oasis7_wasm_store`
  - `env -u RUSTC_WRAPPER cargo check -p oasis7 --no-default-features`
  - `env -u RUSTC_WRAPPER cargo check --manifest-path tools/wasm_build_suite/Cargo.toml`
  - `env -u RUSTC_WRAPPER cargo check --manifest-path tools/wasm_module_observe/Cargo.toml`
- Expected Result: safe-to-consider candidates are limited to non-semantic cleanup or consumer-boundary pruning; core ABI/executor/router/store/build dependencies remain protected unless replacement design exists.
- Actual Result:
  - Dependency tree commands confirmed `wasmtime` is present through `oasis7 -> oasis7_wasm_executor` defaults, absent from `oasis7_client_launcher`, enabled by `oasis7_wasm_executor --features wasmtime`, and enabled by `tools/wasm_module_observe`.
  - `wasmparser` and `wasm-encoder` reverse trees are limited to `tools/wasm_build_suite`.
  - `wat` reverse tree is dev-only under `oasis7_wasm_executor`.
  - `cargo check --manifest-path tools/wasm_build_suite/Cargo.toml` passed with warnings for two unused imports.
  - `cargo check --manifest-path tools/wasm_module_observe/Cargo.toml` passed.
  - `cargo check -p oasis7_wasm_abi -p oasis7_wasm_build -p oasis7_wasm_executor -p oasis7_wasm_router -p oasis7_wasm_sdk -p oasis7_wasm_store` passed with no-feature `oasis7_wasm_executor` dead-code warnings for metric observer functions.
  - `cargo check -p oasis7 --no-default-features` passed with existing unrelated warning noise plus the same no-feature executor metric warnings.
- Blocker / Next Action:
  - No WASM-platform-approved direct deletion of core wasm crates/dependencies found.
  - Recommended next task, if pruning proceeds: repository-health may remove the two `tools/wasm_build_suite/src/lib.rs` unused imports and separately evaluate warning-only cfg cleanup for no-feature executor metrics; any change to `oasis7` default `wasmtime` feature must be treated as a behavior/packaging policy task with runtime/QA validation.

## 2026-06-14 13:47:01 CST / repository_health_engineer
- 完成内容: BOUNDED READ-ONLY REPOSITORY HEALTH WASM PRUNING AUDIT
  - Role boundary: repository health findings only; no code/docs edits. ABI/runtime/platform correctness and actual removal safety require `wasm_platform_engineer` confirmation before implementation.
  - Worktree/task truth inspected: `AGENTS.md`, `.agents/roles/repository_health_engineer.md`, `doc/engineering/workflow/source-of-truth.md`, `.pm/tasks/task_0deb46f616874e40b147407f6b1e510b.yaml`, and this execution log.
  - Inventory summary: top-level Cargo workspace includes `crates/oasis7_wasm_build`, `crates/oasis7_wasm_abi`, `crates/oasis7_wasm_executor`, `crates/oasis7_wasm_sdk`, `crates/oasis7_wasm_router`, and `crates/oasis7_wasm_store`. Builtin wasm module crates live under `crates/oasis7_builtin_wasm_modules/*` but are not top-level workspace members; they are addressed by `crates/oasis7/src/runtime/world/artifacts/builtin_module_manifest_map.txt`.
- 遗留事项: no code changes in this slice; implementation candidates require TPM integration and wasm-platform confirmation.
- Action: rank repository-health pruning/deletion candidates with evidence and validation commands.
- Ranked pruning/deletion candidates:
  1. P1 / medium risk / owner recommendation: `repository_health_engineer` + `wasm_platform_engineer` should consider moving `oasis7_wasm_build` out of the normal `oasis7_distfs` package dependency closure, likely by splitting `sync_builtin_wasm_identity` into a dedicated tool crate or gating it behind a narrow feature. Evidence: `rg -n "oasis7_wasm_build" crates/oasis7_distfs/src crates/oasis7_distfs/Cargo.toml` shows only `crates/oasis7_distfs/Cargo.toml:11` and `crates/oasis7_distfs/src/bin/sync_builtin_wasm_identity.rs:{1,302}`. `cargo tree -p oasis7_client_launcher -i oasis7_wasm_build` shows `oasis7_wasm_build -> oasis7_distfs -> oasis7 -> oasis7_client_launcher`, plus `oasis7_distfs -> oasis7_node/consensus` paths. This is a package-architecture dependency leak: source-hash helper code needed by a sync tool is pulled into normal distfs consumers.
     - Risk notes: do not delete `oasis7_wasm_build`; docs show it is the shared source-hash implementation for `tools/wasm_build_suite` and `sync_builtin_wasm_identity`. Candidate is dependency-boundary pruning, not semantic removal.
     - Validation commands for a follow-up patch: `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i oasis7_wasm_build` should become no-match; `env -u RUSTC_WRAPPER cargo test -p oasis7_distfs --lib`; `env -u RUSTC_WRAPPER cargo run --quiet -p <new-or-gated-tool> --bin sync_builtin_wasm_identity -- --help` or equivalent; `env -u RUSTC_WRAPPER cargo test --manifest-path tools/wasm_build_suite/Cargo.toml -- --nocapture`.
  2. P2 / medium-high risk / owner recommendation: `wasm_platform_engineer` should decide whether `oasis7` can feature-gate non-essential runtime wasm support so non-runtime binaries like `oasis7_client_launcher` do not compile `oasis7_wasm_executor`, `oasis7_wasm_router`, and `oasis7_wasm_store` at all. Evidence: `crates/oasis7_client_launcher/Cargo.toml` already uses `oasis7 = { path = "../oasis7", default-features = false }`, and `cargo tree -p oasis7_client_launcher -i wasmtime` correctly returns no `wasmtime` package. However `cargo tree -p oasis7_client_launcher -i oasis7_wasm_executor`, `... -i oasis7_wasm_router`, and `... -i oasis7_wasm_store` still show `oasis7 -> oasis7_client_launcher`; `cargo tree -p oasis7_client_launcher --prefix none | sort -u | wc -l` reports 606 packages and the tree still includes the small wasm runtime crates.
     - Risk notes: this is not a deletion recommendation by repository health alone. These crates are actively used by runtime/module lifecycle, chain runtime status, tests, and module execution paths. The pruning opportunity is to split feature edges or API surfaces so launcher-only builds avoid unused runtime module machinery. Prior known `wasmtime` regression remains fixed: current no-match for launcher inverse `wasmtime` is a guard to preserve.
     - Validation commands for a follow-up patch: `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i wasmtime` remains no-match; `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i oasis7_wasm_executor` ideally becomes no-match if feature split is accepted; `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher --bin oasis7_client_launcher`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --tests --features test_tier_required`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --features wasmtime --lib --bins`.
  3. P2 / medium risk / owner recommendation: `repository_health_engineer` + `wasm_platform_engineer` should assess whether the wasm build/observer tools can converge or isolate their parser/encoder dependencies. Evidence: `tools/wasm_build_suite/Cargo.toml` directly uses `wasm-encoder = "0.243"` and `wasmparser = "0.243"`; `cargo tree --manifest-path tools/wasm_build_suite/Cargo.toml -i wasmparser` and `... -i wasm-encoder` confirm those are direct tool-only dependencies. Workspace lock also contains multiple `wasmparser`/`wasm-encoder` versions via `wasmtime`/`wat`/WIT-related packages, though workspace `cargo tree -i wasmparser@0.244.0` and `@0.251.0` printed no active default host path in this audit.
     - Risk notes: do not remove parser/encoder blindly. `tools/wasm_build_suite/src/lib.rs` uses them for canonicalization/validation (`use wasm_encoder::{Module, RawSection}; use wasmparser::Parser;`), and deterministic build docs depend on the tool. The candidate is version convergence or clearer isolation, not deletion.
     - Validation commands for a follow-up patch: `env -u RUSTC_WRAPPER cargo test --manifest-path tools/wasm_build_suite/Cargo.toml -- --nocapture`; representative `scripts/build-builtin-wasm-modules.sh --module-id m1.rule.move --dry-run`; if versions change, run a real deterministic build/hash comparison through the wasm platform gate.
  4. P3 / low-medium risk / owner recommendation: keep `crates/oasis7_builtin_wasm_modules/*/Cargo.lock` unless `wasm_platform_engineer` redesigns deterministic identity inputs. Evidence: there are 37 module lockfiles with 4,958 total lines. Docs explicitly say source hash is based on module-level lockfile rather than workspace root lock: `doc/testing/ci/ci-builtin-wasm-docker-canonical-gate.prd.md`, `doc/testing/ci/ci-builtin-wasm-m4-m5-hash-drift-hardening.prd.md`, and `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md`. `crates/oasis7_wasm_build/src/lib.rs` includes `Cargo.lock` in source-hash input handling.
     - Risk notes: this looks deletion-attractive by file count, but current repository contract makes these lockfiles evidence inputs. Treat as "not deletable now"; possible future debt is a generated/centralized lockfile strategy with source-of-truth update first.
     - Validation commands if redesigned later: update workflow/design docs first, then run `env -u RUSTC_WRAPPER cargo test -p oasis7_wasm_build`, `env -u RUSTC_WRAPPER cargo test --manifest-path tools/wasm_build_suite/Cargo.toml -- --nocapture`, and canonical builtin wasm hash gate scripts.
  5. P3 / low risk / owner recommendation: do not prune `oasis7_wasm_sdk` merely because `cargo tree -i oasis7_wasm_sdk` is empty from the top-level workspace perspective. Evidence: builtin wasm modules depend on it via path dependency with `features = ["wire"]`; `scripts/ci-tests.sh` also tests `-p oasis7_wasm_sdk --lib`. It is a shared SDK surface for out-of-workspace builtin module crates.
     - Risk notes: current inverse tree emptiness is a workspace topology artifact, not proof of dead code.
     - Validation command if touched: `env -u RUSTC_WRAPPER cargo test -p oasis7_wasm_sdk --lib` plus `env -u RUSTC_WRAPPER cargo check --manifest-path crates/oasis7_builtin_wasm_modules/m1_rule_move/Cargo.toml`.
- Non-candidates / current healthy edges:
  - Launcher heavy `wasmtime` regression is not present in this audit. Command `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i wasmtime` returned package no-match; `env -u RUSTC_WRAPPER cargo tree -p oasis7 -i wasmtime` shows the expected default path `wasmtime -> oasis7_wasm_executor -> oasis7`.
  - `wasm-bindgen` for `oasis7_client_launcher --target wasm32-unknown-unknown` is web UI/browser plumbing through `eframe`, `gloo-net`, `js-sys`, `web-sys`, `web-time`, and direct launcher wasm target dependencies. It is not evidence of the Rust runtime wasm execution stack leaking into the web build.
  - `wat` is a dev dependency used by `crates/oasis7_wasm_executor/src/tests.rs`; it is also pulled by `wasmtime`. No standalone deletion candidate found.
- Evidence commands run:
  - `rg --files | rg '(^|/)(Cargo.toml|Cargo.lock)$|wasm|wit|wat'`
  - `rg -n "wasm|wasmtime|wasm-bindgen|wit|wasmparser|wasm-encoder|wasm-smith|wat" Cargo.toml Cargo.lock crates -g 'Cargo.toml'`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i wasmtime`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7 -i wasmtime`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i oasis7_wasm_build`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i oasis7_wasm_executor`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i oasis7_wasm_router`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i oasis7_wasm_store`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher --target wasm32-unknown-unknown -i oasis7_wasm_executor`
  - `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher --target wasm32-unknown-unknown -i wasm-bindgen`
  - `env -u RUSTC_WRAPPER cargo tree --manifest-path tools/wasm_build_suite/Cargo.toml -i wasmparser`
  - `env -u RUSTC_WRAPPER cargo tree --manifest-path tools/wasm_build_suite/Cargo.toml -i wasm-encoder`
  - `env -u RUSTC_WRAPPER cargo tree --manifest-path tools/wasm_module_observe/Cargo.toml -i wasmtime`
  - `find crates/oasis7_builtin_wasm_modules -mindepth 2 -maxdepth 2 -name Cargo.lock -print0 | xargs -0 wc -l | tail -n 1`
  - `rg -n "source_hash|module-level lock|模块级 lock|Cargo.lock" doc/testing/ci/ci-builtin-wasm-docker-canonical-gate.prd.md doc/testing/ci/ci-builtin-wasm-m4-m5-hash-drift-hardening.prd.md doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md crates/oasis7_wasm_build/src/lib.rs`
- Validation Command: evidence commands listed above.
- Expected Result: identify real dependency closure leaks while not misclassifying deterministic wasm lockfiles or module SDK usage as dead code.
- Actual Result: found one actionable closure-isolation candidate (`oasis7_wasm_build` behind `sync_builtin_wasm_identity`) plus lower-risk tool import cleanup; no direct wasm core crate deletion candidate found.
- Blocker / Next Action: implement only after wasm-platform confirmation; do not delete core wasm crates.
- Residual risk / required follow-up:
  - `wasm_platform_engineer` must confirm whether candidate 1 can be split without breaking source-hash/identity semantics and whether candidate 2 can be feature-gated without breaking runtime/module ABI expectations.
  - If TPM decides to implement pruning, create a separate implementation step/task section; this slice intentionally made no code changes.
- Memory extraction check:
  - Durable workflow default? no.
  - Repeated failure signature? no; package-cache lock waits were transient and commands completed.
  - Cross-task technical debt worth recording? yes, already recorded here as candidate findings for TPM integration; no separate memory/signal created by this bounded subagent slice because write scope is execution-log only.

## 2026-06-14 14:29:26 CST / tpm
- 完成内容: Implemented the approved pruning patch scope.
  - `crates/oasis7_distfs/Cargo.toml`: made `oasis7_wasm_build` optional, added `sync-builtin-wasm-identity` feature, and bound `sync_builtin_wasm_identity` bin to that feature.
  - `scripts/sync-m1-builtin-wasm-artifacts.sh`: enabled `sync-builtin-wasm-identity` when invoking the identity sync bin; m4/m5 wrapper scripts continue to route through m1 sync script.
  - `tools/wasm_build_suite/src/lib.rs`: removed unused `std::io` import and made `DEFAULT_WASM_TARGET` import test-only because it is used only by lib tests.
- 遗留事项: broader feature-gating of `oasis7_wasm_executor/router/store` out of launcher remains a separate, higher-risk runtime/API split and was not attempted in this patch.
- Action: isolate `oasis7_wasm_build` from default `oasis7_distfs`/launcher dependency closure and apply the safe import cleanup.
- Validation Command: `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i oasis7_wasm_build`
- Expected Result: command should fail with package ID no-match, proving launcher no longer carries `oasis7_wasm_build`.
- Actual Result: failed with `package ID specification oasis7_wasm_build did not match any packages`, as expected.
- Validation Command: `env -u RUSTC_WRAPPER cargo tree -p oasis7_distfs -i oasis7_wasm_build`
- Expected Result: command should fail with package ID no-match for default `oasis7_distfs`.
- Actual Result: failed with `package ID specification oasis7_wasm_build did not match any packages`, as expected.
- Validation Command: `env -u RUSTC_WRAPPER cargo tree -p oasis7_distfs --features sync-builtin-wasm-identity -i oasis7_wasm_build`
- Expected Result: feature-enabled identity sync path should still include `oasis7_wasm_build`.
- Actual Result: passed; tree shows `oasis7_wasm_build -> oasis7_distfs`.
- Validation Command: `env -u RUSTC_WRAPPER cargo check -p oasis7_distfs`
- Expected Result: default distfs lib/package check passes without the optional wasm build helper.
- Actual Result: passed.
- Validation Command: `env -u RUSTC_WRAPPER cargo check -p oasis7_distfs --features sync-builtin-wasm-identity --bin sync_builtin_wasm_identity`
- Expected Result: feature-enabled identity sync bin check passes.
- Actual Result: passed.
- Validation Command: `env -u RUSTC_WRAPPER cargo check --manifest-path tools/wasm_build_suite/Cargo.toml`
- Expected Result: wasm build suite check passes without unused import warnings from the removed/gated imports.
- Actual Result: passed.
- Validation Command: `env -u RUSTC_WRAPPER cargo check -p oasis7_client_launcher`
- Expected Result: launcher check still passes after dependency closure pruning.
- Actual Result: passed with existing unrelated warnings in `oasis7_wasm_executor`, `oasis7_node`, and `oasis7_client_launcher`.
- Validation Command: `env -u RUSTC_WRAPPER cargo test --manifest-path tools/wasm_build_suite/Cargo.toml --lib`
- Expected Result: lib tests pass, including source-hash test that still uses `DEFAULT_WASM_TARGET`.
- Actual Result: passed, 15 tests.
- Validation Command: `env -u RUSTC_WRAPPER cargo test -p oasis7_distfs --features sync-builtin-wasm-identity --bin sync_builtin_wasm_identity`
- Expected Result: identity sync bin test passes with the feature enabled.
- Actual Result: passed, 1 test.
- Validation Command: `bash -n scripts/sync-m1-builtin-wasm-artifacts.sh scripts/sync-m4-builtin-wasm-artifacts.sh scripts/sync-m5-builtin-wasm-artifacts.sh`
- Expected Result: sync shell wrappers remain syntactically valid.
- Actual Result: passed.
- Validation Command: `git diff --check`
- Expected Result: no whitespace errors.
- Actual Result: passed.
- Validation Command: `./scripts/pm/lint.sh`
- Expected Result: repo-wide PM lint may still expose unrelated historical `.pm` debt; current task fields should be brought into shape separately with task-scoped lint.
- Actual Result: failed on this task's earlier audit entries plus unrelated historical execution-log debt; this entry was added to address current-task field gaps, unrelated historical debt left untouched.
- Blocker / Next Action: run task-scoped workflow lint and doc governance after this log update; if task-scoped lint is clean, remaining repo-wide PM lint failures are unrelated existing debt.

## 2026-06-14 14:35:39 CST / tpm
- 完成内容: Pre-PR local role review requested after implementation commit.
- 遗留事项: integrate role review results; address valid findings before PR creation.
- Action: dispatch fresh bounded local review slices for `repository_health_engineer`, `wasm_platform_engineer`, and `qa_engineer`.
- Review Trigger: pre-PR local role review
- Review Scope: `.pm/tasks/task_0deb46f616874e40b147407f6b1e510b.*`; `crates/oasis7_distfs/Cargo.toml`; `scripts/sync-m1-builtin-wasm-artifacts.sh`; `tools/wasm_build_suite/src/lib.rs`
- Review Roles: repository_health_engineer, wasm_platform_engineer, qa_engineer
- Review Question: confirm that the optional `oasis7_wasm_build` feature boundary safely removes the helper from default distfs/launcher closures while preserving identity sync/build-suite behavior and that verification is sufficient for PR creation.
- Evidence Available: implementation commit `4db4e043368176982e909a9fa27fd855cf9d0d3d`; task closeout verified with fresh combined cargo checks/tests, task-scoped workflow lint, doc governance, and diff check; repo-wide PM lint skipped due unrelated historical `.pm` debt.
- Expected Return Contract: `findings` | `no_findings` | `residual_risk`
- Formal Sink: `.pm/tasks/task_0deb46f616874e40b147407f6b1e510b.execution.md`
- Validation Command: local role review dispatch record only.
- Expected Result: all required involved roles return findings/no-findings with residual risk before PR creation.
- Actual Result: pending.
- Blocker / Next Action: wait for local role review results and integrate dispositions.

## 2026-06-14 14:45:39 CST / tpm
- 完成内容: Integrated pre-PR local role review results.
  - `qa_engineer`: no_findings; merge risk low; residual risk is that no real artifact sync/deterministic rebuild was run, not a blocker for this feature-boundary patch.
  - `wasm_platform_engineer`: no_findings; current dependency-boundary narrowing is safe for ABI/source-hash/canonical build semantics; no approval to delete `oasis7_wasm_build`, `wasmtime`, or wasm ABI/executor/router/store/sdk.
  - `repository_health_engineer`: one P1 scope finding and one P3 usage finding.
- 遗留事项: none blocking after disposition; GitHub CI/review still required after PR creation.
- Action: resolve review findings and record passed local role review packet.
- Finding Disposition:
  - P1 scope finding rejected as stale/misread: current `git diff --name-only origin/main...HEAD` contains only this task's `.pm` files plus `crates/oasis7_distfs/Cargo.toml`, `scripts/sync-m1-builtin-wasm-artifacts.sh`, and `tools/wasm_build_suite/src/lib.rs`; `git diff origin/main...HEAD -- crates/oasis7_node/src/node_engine_replication.rs crates/oasis7_node/src/tests_storage_replication_recovery.rs .pm/tasks/task_96c772c830e043f9b1e40b03e6f73d38.execution.md` produced no diff.
  - P3 usage finding addressed: updated `crates/oasis7_distfs/src/bin/sync_builtin_wasm_identity.rs` usage text to include `--features sync-builtin-wasm-identity`.
- Pre-PR Local Role Review: passed
- Task UID: task_0deb46f616874e40b147407f6b1e510b
- Source Worktree: /Users/scc/ccwork/worktrees/oasis7-engineering-wasm-prune-audit
- Source Branch: task/engineering-wasm-prune-audit
- Source Head: 4db4e043368176982e909a9fa27fd855cf9d0d3d
- Comparison Ref: origin/main
- Reviewed Changed Paths: .pm/tasks/task_0deb46f616874e40b147407f6b1e510b.execution.md; .pm/tasks/task_0deb46f616874e40b147407f6b1e510b.yaml; crates/oasis7_distfs/Cargo.toml; scripts/sync-m1-builtin-wasm-artifacts.sh; tools/wasm_build_suite/src/lib.rs; later review-fix-only path crates/oasis7_distfs/src/bin/sync_builtin_wasm_identity.rs
- Role Selection Basis: dependency-boundary and wasm deterministic identity tooling change requires repository_health_engineer and wasm_platform_engineer; PR readiness and verification claim requires qa_engineer.
- Review Roles: repository_health_engineer, wasm_platform_engineer, qa_engineer
- Review Evidence: qa_engineer no_findings appended by subagent; wasm_platform_engineer no_findings appended by subagent; repository_health_engineer findings appended by subagent and disposition recorded here.
- Review Findings Disposition: addressed
- Finding Disposition Evidence: P1 rejected by current diff evidence listed above; P3 fixed in `crates/oasis7_distfs/src/bin/sync_builtin_wasm_identity.rs` and reverified.
- Residual Risk: real artifact sync/deterministic rebuild deferred as non-blocking because this patch changes Cargo feature boundaries and invocation wiring, with feature-enabled bin check/test covering the affected path.
- Validation Command: `env -u RUSTC_WRAPPER cargo check -p oasis7_distfs --features sync-builtin-wasm-identity --bin sync_builtin_wasm_identity`
- Expected Result: usage-text fix does not break feature-enabled identity sync bin.
- Actual Result: passed.
- Validation Command: `env -u RUSTC_WRAPPER cargo test -p oasis7_distfs --features sync-builtin-wasm-identity --bin sync_builtin_wasm_identity`
- Expected Result: identity sync bin test still passes.
- Actual Result: passed, 1 test.
- Validation Command: `bash -n scripts/sync-m1-builtin-wasm-artifacts.sh scripts/sync-m4-builtin-wasm-artifacts.sh scripts/sync-m5-builtin-wasm-artifacts.sh && ./scripts/pm/workflow-lint.sh --task-uid task_0deb46f616874e40b147407f6b1e510b --phase current && git diff --check`
- Expected Result: wrapper syntax, current task workflow lint, and whitespace checks pass after review evidence/fix.
- Actual Result: passed.
- Blocker / Next Action: commit review evidence/fix, run prepare-task-pr preflight/create, then continue normal PR CI/comment/merge watch.

## 2026-06-14 14:43:37 CST / wasm_platform_engineer
- 完成内容: Pre-PR local role review completed for WASM platform / deterministic identity / module lifecycle semantics at HEAD `4db4e043368176982e909a9fa27fd855cf9d0d3d` on branch `task/engineering-wasm-prune-audit`.
  - Role boundary: acting as required bounded `wasm_platform_engineer` review slice; no code/docs edited beyond this execution-log review sink.
  - Scope reviewed: `crates/oasis7_distfs/Cargo.toml`; `scripts/sync-m1-builtin-wasm-artifacts.sh`; `tools/wasm_build_suite/src/lib.rs`; task execution evidence.
  - Decision: no_findings. The patch is safe from WASM ABI/hash/identity/canonical build/module lifecycle semantics.
- 遗留事项: broader feature-gating of `oasis7_wasm_executor/router/store` out of non-executing consumers remains separate higher-risk work and is not implied by this approval.
- Action: review whether making `oasis7_wasm_build` optional and required only by `sync-builtin-wasm-identity` preserves deterministic identity sync behavior, and whether test-only `DEFAULT_WASM_TARGET` preserves source-hash tests.
- Findings:
  - no_findings.
  - Severity / merge risk: none blocking; merge risk low for this patch scope.
  - Evidence: `crates/oasis7_distfs/Cargo.toml` now keeps `oasis7_wasm_build` as an optional dependency and exposes it through only `sync-builtin-wasm-identity`; the `sync_builtin_wasm_identity` bin has `required-features = ["sync-builtin-wasm-identity"]`.
  - Evidence: `scripts/sync-m1-builtin-wasm-artifacts.sh` invokes `cargo run -p oasis7_distfs --features sync-builtin-wasm-identity --bin sync_builtin_wasm_identity`, so the canonical sync path still enables the source-hash helper explicitly.
  - Evidence: `crates/oasis7_distfs/src/bin/sync_builtin_wasm_identity.rs` still imports `DEFAULT_WASM_TARGET` and calls `oasis7_wasm_build::compute_source_hash`; the feature boundary changes compile reachability, not source-hash semantics.
  - Evidence: `tools/wasm_build_suite/src/lib.rs` uses `DEFAULT_WASM_TARGET` only in tests, and the test-only import preserves `source_hash_tracks_local_path_dependency_changes`.
- Validation Command: `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i oasis7_wasm_build`
- Expected Result: launcher default dependency closure should not include `oasis7_wasm_build`.
- Actual Result: failed as expected with `package ID specification oasis7_wasm_build did not match any packages`.
- Validation Command: `env -u RUSTC_WRAPPER cargo tree -p oasis7_distfs -i oasis7_wasm_build`
- Expected Result: default `oasis7_distfs` dependency closure should not include `oasis7_wasm_build`.
- Actual Result: failed as expected with `package ID specification oasis7_wasm_build did not match any packages`.
- Validation Command: `env -u RUSTC_WRAPPER cargo tree -p oasis7_distfs --features sync-builtin-wasm-identity -i oasis7_wasm_build`
- Expected Result: feature-enabled identity sync closure should include `oasis7_wasm_build`.
- Actual Result: passed; tree shows `oasis7_wasm_build -> oasis7_distfs`.
- Validation Command: `env -u RUSTC_WRAPPER cargo check -p oasis7_distfs --features sync-builtin-wasm-identity --bin sync_builtin_wasm_identity`
- Expected Result: feature-enabled identity sync bin compiles.
- Actual Result: passed. Command initially waited for the shared Cargo build-directory lock, then completed successfully.
- Validation Command: `env -u RUSTC_WRAPPER cargo test --manifest-path tools/wasm_build_suite/Cargo.toml --lib`
- Expected Result: build suite lib tests pass, including source-hash test coverage that references `DEFAULT_WASM_TARGET`.
- Actual Result: passed; 15 tests including `source_hash_tracks_local_path_dependency_changes`.
- Validation Command: `env -u RUSTC_WRAPPER cargo test -p oasis7_distfs --features sync-builtin-wasm-identity --bin sync_builtin_wasm_identity`
- Expected Result: identity sync bin test passes with the new feature enabled.
- Actual Result: passed; 1 test `sync_source_hash_tracks_local_path_dependency_changes`.
- Validation Command: `bash -n scripts/sync-m1-builtin-wasm-artifacts.sh scripts/sync-m4-builtin-wasm-artifacts.sh scripts/sync-m5-builtin-wasm-artifacts.sh`
- Expected Result: sync wrapper scripts remain syntactically valid.
- Actual Result: passed.
- Residual Risk:
  - This review does not approve deleting `oasis7_wasm_build`; it approves narrowing the dependency edge to the identity sync feature/bin.
  - This review does not approve changing default native runtime execution or removing `wasmtime`, wasm ABI, executor, router, store, or SDK crates.
  - No ABI/hash/identity/canonical build blocker remains for PR creation from the WASM platform perspective.
- Blocker / Next Action: none from `wasm_platform_engineer`; TPM may integrate this with other local role reviews.

## 2026-06-14 14:39:33 CST / repository_health_engineer
- Review Trigger: pre-PR local role review.
- Review Scope: current branch `task/engineering-wasm-prune-audit` at HEAD `4db4e043368176982e909a9fa27fd855cf9d0d3d`; requested paths `.pm/tasks/task_0deb46f616874e40b147407f6b1e510b.*`, `crates/oasis7_distfs/Cargo.toml`, `scripts/sync-m1-builtin-wasm-artifacts.sh`, and `tools/wasm_build_suite/src/lib.rs`; branch-level changed-path audit also inspected `git diff --name-status main...HEAD`.
- Review Question: does the patch correctly isolate `oasis7_wasm_build` from default distfs/launcher dependency closure while preserving the explicit sync identity tool path, and is the task evidence sufficient for PR creation?
- Evidence Reviewed:
  - `crates/oasis7_distfs/Cargo.toml:11` makes `oasis7_wasm_build` optional; `crates/oasis7_distfs/Cargo.toml:21-27` adds the narrow `sync-builtin-wasm-identity` feature and requires it for the `sync_builtin_wasm_identity` bin.
  - `scripts/sync-m1-builtin-wasm-artifacts.sh:252` enables `--features sync-builtin-wasm-identity` for the identity sync path.
  - `tools/wasm_build_suite/src/lib.rs:9-10` limits `DEFAULT_WASM_TARGET` import to tests; `std::io` import is gone.
  - Fresh spot checks during review: `env -u RUSTC_WRAPPER cargo tree -p oasis7_client_launcher -i oasis7_wasm_build` returned package no-match as expected; `env -u RUSTC_WRAPPER cargo tree -p oasis7_distfs --features sync-builtin-wasm-identity -i oasis7_wasm_build` showed `oasis7_wasm_build -> oasis7_distfs`.
  - Task log evidence includes prior fresh checks/tests for default distfs, feature-enabled identity sync bin, wasm build suite, task-scoped workflow lint, doc governance, and diff check.
- Findings:
  - P1 / PR-scope blocker / repository-health: current branch HEAD contains non-task, non-review-scope replication changes: `.pm/tasks/task_96c772c830e043f9b1e40b03e6f73d38.execution.md:747-774`, `crates/oasis7_node/src/node_engine_replication.rs:1068-1093`, and `crates/oasis7_node/src/tests_storage_replication_recovery.rs:325-369`. These changes are not part of the wasm dependency-pruning task, are not included in the requested changed-path review scope, and require different domain ownership (`runtime_engineer` / `blockchain_ops_engineer`) and task evidence. Creating this wasm PR from the current branch would mix unrelated production replication behavior into the wasm pruning PR.
    - Suggested fix: before PR creation, remove or split the replication/task_96 changes from `task/engineering-wasm-prune-audit` so `git diff --name-status main...HEAD` contains only this task's `.pm` files and wasm pruning files, or formally move the replication changes into their own canonical task/worktree/PR with the required owner-role review.
  - P3 / low merge risk / operator-facing text: `crates/oasis7_distfs/src/bin/sync_builtin_wasm_identity.rs:77` still says `Usage: cargo run -p oasis7_distfs --bin sync_builtin_wasm_identity -- ...`, which is stale after the bin gained `required-features = ["sync-builtin-wasm-identity"]`. The main wrapper script is correct, so this does not invalidate the dependency-boundary patch, but the direct cargo usage hint should include `--features sync-builtin-wasm-identity` to avoid future operator confusion.
    - Suggested fix: update the usage string to `cargo run -p oasis7_distfs --features sync-builtin-wasm-identity --bin sync_builtin_wasm_identity -- ...`.
- no_findings:
  - No dependency-boundary issue found in the wasm pruning patch itself. Default `oasis7_distfs` and `oasis7_client_launcher` no longer carry `oasis7_wasm_build`; the explicit identity sync path still opts into and reaches `oasis7_wasm_build`.
  - No missed m4/m5 wrapper script reference found; both wrappers continue to delegate through `scripts/sync-m1-builtin-wasm-artifacts.sh`, where the feature flag is now applied.
- Severity / Merge Risk: high until the branch scope is cleaned because the PR would carry unrelated runtime behavior changes; low for the stale usage string if fixed before or with PR cleanup.
- Residual Risk: after removing/splitting unrelated node replication changes and updating the direct usage hint, remaining residual risk is low and limited to Cargo feature-boundary regressions covered by the recorded cargo tree/check/test evidence. GitHub CI and PR review still remain required.
- Blocker / Next Action: do not create the wasm pruning PR from the current HEAD as-is. TPM should first restore single-task PR scope, address the stale usage text, rerun task-scoped verification, then integrate remaining role reviews and write the passed pre-PR local role review packet.

## 2026-06-14 14:39:30 CST / qa_engineer
- 完成内容: Required pre-PR local role review completed for verification sufficiency.
  - Role boundary: acting as bounded `qa_engineer` slice; no code/docs edits beyond this execution-log review sink.
  - Scope inspected: task execution log and task yaml; implementation commit `4db4e043368176982e909a9fa27fd855cf9d0d3d`; changed paths `crates/oasis7_distfs/Cargo.toml`, `scripts/sync-m1-builtin-wasm-artifacts.sh`, and `tools/wasm_build_suite/src/lib.rs`.
  - Verification evidence reviewed: default `oasis7_distfs` check; feature-enabled `sync_builtin_wasm_identity` check/test; `tools/wasm_build_suite` check/lib tests; sync wrapper shell syntax; launcher/default-distfs inverse dependency closure for `oasis7_wasm_build`; task-scoped workflow lint; doc governance check; `git diff --check`.
- 遗留事项: repo-wide `./scripts/pm/lint.sh` still has unrelated historical `.pm` debt, already bounded away by task-scoped workflow lint; no QA blocker for this PR scope.
- Action: assess whether verification evidence is sufficient to take dependency-boundary/tooling change to PR.
- Findings: no_findings.
- Severity / merge risk: low. The patch changes Cargo feature boundaries and one script invocation; post-change evidence covers compile/test behavior for both default and feature-enabled paths plus the dependency-closure goal.
- Validation Command: inspect recorded closeout evidence in `.pm/tasks/task_0deb46f616874e40b147407f6b1e510b.{yaml,execution.md}` and implementation diff at HEAD.
- Expected Result: required PR-prep evidence should cover default distfs, feature-enabled identity sync bin, wasm build suite, shell syntax, launcher dependency closure, task-scoped workflow lint, doc governance, and diff hygiene.
- Actual Result:
  - Covered by final verified command in task yaml: `cargo check -p oasis7_distfs`; `cargo check -p oasis7_distfs --features sync-builtin-wasm-identity --bin sync_builtin_wasm_identity`; `cargo test --manifest-path tools/wasm_build_suite/Cargo.toml --lib`; `cargo test -p oasis7_distfs --features sync-builtin-wasm-identity --bin sync_builtin_wasm_identity`; `./scripts/pm/workflow-lint.sh --task-uid task_0deb46f616874e40b147407f6b1e510b --phase current`; `./scripts/doc-governance-check.sh`; `git diff --check`; exit code 0 at `2026-06-14T14:34:57+08:00`.
  - Covered by same post-change implementation evidence in execution log: `cargo tree -p oasis7_client_launcher -i oasis7_wasm_build` no-match; `cargo tree -p oasis7_distfs -i oasis7_wasm_build` no-match; `cargo tree -p oasis7_distfs --features sync-builtin-wasm-identity -i oasis7_wasm_build` includes `oasis7_wasm_build -> oasis7_distfs`; `cargo check --manifest-path tools/wasm_build_suite/Cargo.toml`; `cargo check -p oasis7_client_launcher`; `bash -n scripts/sync-m1-builtin-wasm-artifacts.sh scripts/sync-m4-builtin-wasm-artifacts.sh scripts/sync-m5-builtin-wasm-artifacts.sh`; all recorded passed or expected no-match.
- Missing required or high-value regression check: none for this PR scope.
- Residual risk:
  - The verification does not run a real artifact sync or deterministic wasm rebuild; acceptable residual risk because this patch only adds the feature flag to the existing cargo invocation and separately checks the feature-enabled bin/test path.
  - Broader launcher pruning of `oasis7_wasm_executor/router/store` remains explicitly out of scope and should not be inferred as covered.
- Blocker / Next Action: QA has no blocker; TPM may integrate role review results and continue PR creation after other required local role reviews are resolved.
