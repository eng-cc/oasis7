# task_67de23e6aaf44099b0bf87d3f4d08996 Execution Log

- task_uid: task_67de23e6aaf44099b0bf87d3f4d08996
- title: Stabilize release soak chaos metric gate
- owner_role: qa_engineer
- worktree_hint: /home/scc/worktrees/oasis7-testing-release-soak-chaos-metric-gate

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-05-23 13:25:35 CST / qa_engineer
- 完成内容: 复盘 `Release Packages` run `26323648187` 的 `release-gate-soak` 失败。先从 GitHub artifact `release-gate-soak-summary` 读取 `.tmp/release_gate_soak/20260523-043949/release-gate-summary.md` 与 `s9.log`，确认失败发生在 `./scripts/p2p-longrun-soak.sh --profile soak_release --topologies triad_distributed --duration-secs 300 --no-prewarm --max-stall-secs 240 --max-lag-p95 50 --max-distfs-failure-ratio 0.1 --chaos-continuous-enable --chaos-continuous-interval-secs 30 --chaos-continuous-start-sec 30 --chaos-continuous-max-events 8 --chaos-continuous-actions restart,pause --chaos-continuous-seed 1772284566 --chaos-continuous-restart-down-secs 1 --chaos-continuous-pause-duration-secs 2`，GitHub 失败签名为 `metric_gate=last_error_samples=1; balances_load_error_samples=9; known_peer_heads_zero_samples=136`。
- 完成内容: 在新 task worktree 里先按 CI 预热 `env -u RUSTC_WRAPPER cargo build -p oasis7 --bin oasis7_chain_runtime`，然后用独立端口段 `--base-port 17610` 运行同款 300 秒 soak，避免本机常驻 triad 占用 `561x/563x/565x` 端口造成伪失败。基线 run `/tmp/release-gate-p2p-local-26323648187-base17610/20260523-131055` 最终通过，但 summary 仍记录了 `balances_load_error_samples=10; known_peer_heads_zero_samples=137`；同时 `sequencer/stdout.log` 两次出现 `node replication error: replication network error: NetworkRequestFailed { code: ErrUnsupported, message: "/aw/node/replication/fetch-commit/1.0.0", retryable: false }`，说明这类瞬时 fetch-commit unsupported 噪音本地也会发生，只是未被采样成 `last_error_samples`。
- 完成内容: 对照 `scripts/s10-five-node-game-soak.sh` 已有的 `is_tolerable_bootstrap_fetch_commit_unavailable` 规则，在 `scripts/p2p-longrun-soak.sh` 补入同类容忍逻辑：当 `sequencer` 在 chaos/restart 期间瞬时上报 `/aw/node/replication/fetch-commit/1.0.0` 的 `NetworkRequestFailed + NetworkProtocolUnavailable|ErrUnsupported` 时，不再记为硬失败样本，而是单独计入 `analysis_tolerated_bootstrap_fetch_commit_unavailable_samples` warning。补丁后通过 `bash -n scripts/p2p-longrun-soak.sh`，并再次用同款 300 秒 soak（`/tmp/release-gate-p2p-local-26323648187-base17610-patched/20260523-132006`）复验通过。
- 遗留事项: 需要提交该 soak gate 修复、走 PR 合入 `main`，重新触发 `release-packages.yml` 的 `v0.0.52`，并再次盯到 GitHub release 真正发布完成。

## 2026-05-23 17:38:36 CST / qa_engineer
- 完成内容: 继续排查 merge 后的 `Release Packages` run `26324820114`。从 `release-gate-soak-summary` artifact 的 `release-gate-summary.md` 确认 `s9` 已通过，新的 blocker 已切换为 `s10`：`./scripts/s10-five-node-game-soak.sh --duration-secs 300 --no-prewarm --max-stall-secs 240 --max-lag-p95 50 --out-dir .tmp/release_gate_s10`。
- 完成内容: 在 task worktree 复现 `s10`。第一次基线 run `/tmp/release-gate-s10-local-26324820114-base5810/20260523-172008` 通过；随后在隔离端口 `28610+` 复现到硬失败，`/tmp/release-gate-s10-local-26324820114-base28610-patched/20260523-172641/failures.md` 明确记录唯一 runtime error：`s10-storage-b` 上报 `node replication error: replication network error: NetworkRequestFailed { code: ErrUnsupported, message: "/aw/node/replication/fetch-commit/1.0.0", retryable: false }`，对应 gate 签名 `last_error_samples=1; balances_load_error_samples=9; known_peer_heads_zero_samples=149`。
- 完成内容: 在 `scripts/s10-five-node-game-soak.sh` 扩展 `is_tolerable_bootstrap_fetch_commit_unavailable`，把 bootstrap 期间任意节点出现的精确 `fetch-commit` unsupported 签名（`NetworkRequestFailed` 且 `NetworkProtocolUnavailable|ErrUnsupported`，协议路径固定为 `/aw/node/replication/fetch-commit/1.0.0`）从 hard failure 改为 tolerated warning。补丁后通过 `bash -n scripts/s10-five-node-game-soak.sh`，并在隔离端口 `29610+` 复跑 300 秒 soak：`/tmp/release-gate-s10-local-26324820114-base29610-patched/20260523-173220/summary.md` 显示 `metric_gate=pass`、`last_error_samples=0`、`tolerated_bootstrap_fetch_commit_unavailable_samples=2`。
- 遗留事项: 提交 `s10` 补丁并合入 `main`，重新触发 `release-packages.yml` 的 `v0.0.52`，最终确认 `gh release view v0.0.52` 已存在且 assets 已发布。
