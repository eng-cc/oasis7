# task_9c3d38de653840ea9177af3e67449016 Execution Log

- task_uid: task_9c3d38de653840ea9177af3e67449016
- title: Clear stale relay reservation evidence and split live recommendation from effective runtime mode
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-p2parch-live-reachability-followup

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-06 23:18:59 CST / runtime_engineer
- 完成内容: 为 live reachability snapshot 补齐 relay reservation 失效收口，改用 relayed listen addr 的 `NewListenAddr/ExpiredListenAddr/ListenerClosed` 生命周期同步 `relay_reservation_active` 并回刷 peer record/external address；新增对应 snapshot 单测，避免 reservation 证据在 status/recommender 中永久滞留。
- 完成内容: 收口 `/v1/chain/status.p2p` 语义分离，保留 `effective_user_mode` 表示基于实时 snapshot 计算的当前有效推荐态，并新增 `applied_effective_user_mode` 表示 runtime 启动时实际应用的用户模式；显式 raw policy override 继续以 `deployment_mode/node_role_claim` 作为实际运行态真值，同步更新状态 payload 定向测试。
- 遗留事项: 仍未接入完整 AutoNAT / port reachability probe；`viewer_engineer` 仍需消费新的 `applied_effective_user_mode` 字段，避免前端继续把“运行态”和“推荐态”混用。

## 2026-04-06 23:40:03 CST / runtime_engineer
- 完成内容: 根据最终独立 review 补齐 `active_transport_kind=RelayReserved` 的 stale-state 收口，relay reservation 消失后会重算 preferred transport kind，避免 `probe_stable` 被旧 relay 状态继续拉高；review 最终结果为 `no findings`。
- 遗留事项: `event_listening_addrs` 仍是 bounded `Vec` 且未去重，极端情况下可能出现 relay listen addr 被提前挤出导致的 false negative；另外现有证据收口仍主要依赖 `NewListenAddr/ExpiredListenAddr/ListenerClosed` 三类事件，后续若发现其他 listener 生命周期路径需要补观测面，应继续加事件级测试。
