# task_4d597c77a31b4411864f998159d8d5ec Execution Log

- task_uid: task_4d597c77a31b4411864f998159d8d5ec
- title: p2p network runtime hardening
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-network-runtime-hardening

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-22 16:38:07 CST / runtime_engineer
- 完成内容: 收口 `libp2p` publish/fallback 两类 runtime 债务。`crates/oasis7_net` 侧将 `Command::Publish` 改为通过 oneshot 向调用方同步回传 `gossipsub.publish()` 结果，失败时不再把消息记入 `published` 或成功流量统计，并向 `event_errors` 写入明确错误；同时在 `error_mapping.rs` 增加共享 availability classifier，把 `missing handler` / `bad request` / `retryable connection gap` 从自由文本判断收敛成统一 helper。
- 完成内容: `crates/oasis7_node` 侧把 replication fallback/cooldown 判定改为消费共享 classifier 与稳定 reason prefix，去掉多处重复 `contains(...)` 错误串猜测；`network_bridge.rs` 现在会显式区分 `replication network availability gap` 与 `replication network route unavailable`，`replication_probe_gate.rs` 只把前者视为可等待 gap。
- 完成内容: 补齐回归。新增 `libp2p_publish_surfaces_gossipsub_failures`，验证无 peer 时 publish 会返回错误且不会被记成已发布；新增 route-unavailable fallback 测试，验证 provider-aware fallback 与 waitable-gap 判定不再混淆。
- 完成内容: 回写 `doc/p2p/prd.md` 与 `doc/p2p/project.md`，把本次 hardening 追踪到 `PRD-P2P-001/003`，同步验收矩阵、edge case、decision log 与项目状态。
- 验证: `env -u RUSTC_WRAPPER cargo test -p oasis7_net --features libp2p --lib`；`env -u RUSTC_WRAPPER cargo test -p oasis7_node --lib`
- 遗留事项: 当前 `network_bridge.rs` 到 `replication_probe_gate.rs` 之间仍通过字符串 prefix 传递 node 侧分类结果，只是前缀已收敛为单一真值；若后续继续演进 replication 控制面，可再把这层 reason 升级为结构化错误枚举，减少跨层文本协议。
