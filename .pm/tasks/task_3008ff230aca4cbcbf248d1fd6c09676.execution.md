# task_3008ff230aca4cbcbf248d1fd6c09676 Execution Log

- task_uid: task_3008ff230aca4cbcbf248d1fd6c09676
- title: Fix peer record direct-surface truth and reachability-driven republish
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-peer-record-surface-truth

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-14 21:00:16 CST / runtime_engineer
- 完成内容: 收紧 libp2p local peer record 的 direct surface 物化逻辑；当模板未显式给出 `direct_addrs` 时，默认仅发布已确认的外部直连地址，或在无确认地址时回退到明确可拨通的 public listen surface，避免继续把 loopback/private/unspecified 监听地址签进 DHT peer record。
- 完成内容: 将 `ExternalAddrConfirmed/Expired`、listen addr 变化和 relay reservation 接到 peer record 重发链路；`RR_GET_LOCAL_PEER_RECORD`、周期性 republish 与 discovery refresh 现在共享同一份 reachability truth，不再出现状态面显示公网可达、peer record 仍发布本地监听地址的分叉。
- 完成内容: 补齐 `build_configured_peer_record` 定向回归，并修复 `transport_retry_tests` / `tests.rs` 中阻断当前验证的测试编译问题。
- 完成内容: 已执行 `env -u RUSTC_WRAPPER cargo test -p oasis7_net build_configured_peer_record -- --nocapture` 与 `env -u RUSTC_WRAPPER cargo test -p oasis7_net --lib -- --nocapture`，均通过。
- 遗留事项: 无。

## 2026-04-14 21:19:30 CST / runtime_engineer
- 完成内容: 为避免 `libp2p_net.rs` 与 `tests.rs` 继续卡在 rust-size 门禁，将 peer-record 回归测试拆到 `tests/peer_record_tests.rs`，并把 local peer-record republish 调用收敛到 helper / macro 组合；同步更新 `doc/.governance/rust-oversized-file-baseline.tsv`，移除已回到阈值内的 `tests.rs` 基线条目，并把 `libp2p_net.rs` 基线刷新到最新 1240 行。
- 完成内容: 已执行 `./scripts/check-rust-file-size.sh`、`./scripts/doc-governance-check.sh` 与 `git diff --check`，均通过。
- 完成内容: 已执行 `./scripts/pm/codex-review-snapshot.sh`；审查过程完成了当前 diff 的快照巡检，已见输出未给出新的 finding，最终终端输出在大 diff 回显后结束，按仓库既有 snapshot-review hang/truncation 约定不将其作为额外 blocker。
- 遗留事项: 无。

## 2026-04-14 21:53:46 CST / runtime_engineer
- 完成内容: 处理 PR #76 review comments。`LocalPeerRecordRepublisher::republish` 现在仅在 `publish_configured_peer_record(...)` 成功时才更新时间戳并重发 discovery provider；若发布失败，则将错误写入 `event_errors`，避免错误地抑制后续 interval republish 并继续暴露未成功刷新的 provider surface。
- 完成内容: 为 `tests/peer_record_tests.rs` 显式补回 `peer_record_enables_rendezvous` import，修复拆分测试模块后的作用域缺失；同时继续将 `crates/oasis7_net/src/libp2p_net.rs` 收缩到 1238 行，并同步 `doc/.governance/rust-oversized-file-baseline.tsv`。
- 完成内容: 已执行 `cargo fmt --check -p oasis7_net`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net --lib -- --nocapture`、`./scripts/check-rust-file-size.sh`、`./scripts/doc-governance-check.sh` 与 `git diff --check`，均通过。
- 遗留事项: 待 commit / push 后在 GitHub resolve 对应 review threads。
