# task_f7e0830c497849a9a9f5f731cbf60b97 Execution Log

- task_uid: task_f7e0830c497849a9a9f5f731cbf60b97
- title: Profile, benchmark, and fix wasm hot paths
- owner_role: wasm_platform_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-runtime-wasm-performance-profile

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-14 21:00:00 CST / wasm_platform_engineer
- 完成内容: 在隔离 worktree 中为 `oasis7_wasm_executor`、`oasis7_wasm_router`、`oasis7_wasm_abi` 增加本地 `#[ignore]` perf probe，并用 `env -u RUSTC_WRAPPER cargo test ... --release -- --ignored --nocapture` 采集热路径数据。
- 完成内容: 确认执行器热路径中每次 `call()` 都会创建并回收 `EpochWatchdog` 线程；probe 结果为 `warm_call_us_per_op=75.169`、`watchdog_us_per_op=45.773`、watchdog 占单次调用约 `60.89%`。
- 完成内容: 确认 `ModuleCache::get()` 的 artifact clone 成本随 wasm 大小线性放大；probe 结果为 `4 KiB -> 0.120 us/get`、`256 KiB -> 6.131 us/get`、`4 MiB -> 277.548 us/get`，说明当前拥有型 `Vec<u8>` API 会把调用成本直接绑定到 wasm 大小。
- 完成内容: 订阅过滤器 probe 显示“重复 JSON parse”在无 regex 规则时放大约 `5.86x`，但含 regex 规则时仅放大约 `1.29x`；说明 parse 确实有成本，但真实热路径更大的问题是 `match_rule()` 每次匹配都重新 `Regex::new(...)`。
- 遗留事项: 还未实施优化修复；若继续推进，优先级建议为 `EpochWatchdog` 线程模型 -> wasm artifact/ModuleCallRequest 借用化或 `Arc<[u8]>` 化 -> 订阅规则预解析并缓存 regex。

## 2026-04-14 21:25:00 CST / wasm_platform_engineer
- 完成内容: 将 `oasis7_wasm_executor` 的 timeout watchdog 改为 executor 级复用线程，通过 arm/disarm 命令复用同一后台线程，不再在每次 `call()` 中 `spawn + join`。
- 完成内容: 将 `ModuleArtifact.bytes`、`World.module_artifact_bytes` 与 `ModuleCallRequest.wasm_bytes` 统一切到共享 `Arc<[u8]>`，去掉 runtime 模块加载与 request 组装热路径上的大块 wasm 字节复制；同步修正 `oasis7_net` 客户端和相关断言。
- 完成内容: 为 `oasis7_wasm_router` 增加 parsed-filter cache 与 regex cache，把订阅过滤器 JSON 解析和 `Regex::new(...)` 编译从每次匹配热路径中移出。
- 完成内容: 复跑验证：
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_wasm_abi -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_wasm_router -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_wasm_executor --features wasmtime -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo check -p oasis7 --tests`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 kernel_wasm_sandbox_bridge -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 persist_and_restore_world_defaults_to_module_store_roundtrip -- --nocapture`
- 完成内容: 复跑 release perf probe：
  - 执行器：`warm_call_us_per_op 75.169 -> 19.034`，watchdog `45.773 -> 0.237 us/op`，share `60.89% -> 1.24%`
  - artifact cache：`4 MiB` get `277.548 -> 0.050 us/op`，线性复制成本基本消失
  - router：`no_regex_ratio 5.86x -> 3.16x`，`regex_ratio 1.29x -> 2.98x`，说明 regex 编译成本已显著下降，parse 成本仍在但总耗时从 `3548ms -> 477ms`
- 遗留事项: 若继续深挖 router，无 regex 场景仍可考虑把 `filters_value.to_string()` 替换为更低成本的稳定 key，以继续压缩 parsed-filter cache 的 lookup 固定成本。

## 2026-04-14 21:43:00 CST / wasm_platform_engineer
- 完成内容: `./scripts/ci-tests.sh commit` 首次复跑命中 Rust 文件体量门禁，发现 `crates/oasis7_wasm_executor/src/lib.rs` 被本轮改动推高到 `1369` 行；已将测试模块拆到新的 `crates/oasis7_wasm_executor/src/tests.rs`，把主实现文件压回 `884` 行。
- 完成内容: 拆分后复跑 `env -u RUSTC_WRAPPER cargo test -p oasis7_wasm_executor --features wasmtime -- --nocapture` 通过，并复跑 `./scripts/ci-tests.sh commit` 通过（文档治理、文件体量、`cargo fmt --check`、`oasis7_consensus`/`oasis7_distfs` 轻量测试、`software-safe-feedback-contract` 均通过）。
- 完成内容: 基于当前稳定 diff 两次重跑 `./scripts/pm/codex-review-snapshot.sh --output-last-message ...`，分别使用 `gpt-5.4` 与 `gpt-5.1-codex-mini`；两次都在快照内持续仓库扫描直到 `timeout`，未输出最终 review message，也未返回具体 finding。
- 遗留事项: 当前代码与本地验证已收敛，但按仓库约束，commit 仍受 snapshot review CLI 超时阻塞；若继续收口，需要先解决 `codex-review-snapshot.sh` 在本 diff 上无法在时限内产出最终消息的问题，或由用户明确授权按人工校验结果继续提交。

## 2026-04-14 21:52:00 CST / wasm_platform_engineer
- 完成内容: 用户已明确授权在 `codex-review-snapshot.sh` 工具级超时未产出最终 message 的前提下继续推进提交与 PR 创建；本轮按该授权继续执行后续 closure。
- 遗留事项: PR 描述与远端 checks 仍待后续 `prepare-task-pr` / GitHub required checks 收口。

## 2026-04-14 22:10:00 CST / wasm_platform_engineer
- 完成内容: 处理 PR #78 review comments：为 `oasis7_wasm_executor` 补回非 `wasmtime` 构建下的 `Clone` 实现兼容；为 `oasis7_wasm_router` 的 parsed-filter / regex cache 增加固定容量上限与 poison-recovery 锁访问，并恢复 filter validation 的 serde 解析错误细节。
- 完成内容: 为 router 新增缓存逐出回归测试，并复跑 `env -u RUSTC_WRAPPER cargo test -p oasis7_wasm_router -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_wasm_executor --features wasmtime -- --nocapture`、`env -u RUSTC_WRAPPER cargo check -p oasis7_wasm_executor --no-default-features`。
- 遗留事项: 仍有一条关于 `filters_value.to_string()` 固定成本的性能建议未继续深挖；本轮先处理编译/兼容/缓存治理类 review finding。
