# task_97820fd5e09a450aadcf988a968faad8 Execution Log

- task_uid: task_97820fd5e09a450aadcf988a968faad8
- title: viewer web single-source build truth and legacy core split
- owner_role: viewer_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-simulator-viewer-web-single-source-build-truth

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-19 18:28:50 CST / viewer_engineer
- 完成内容: 将 `legacy_core.js` 的 world-scale / auth / feedback-gameplay 逻辑拆到 `viewer_world_scale_module.js`、`viewer_auth_surface_module.js`、`viewer_feedback_module.js`，保留 `legacy_core.js` 作为 facade/import assembly；文件行数从 4226 行降到 3604 行。
- 完成内容: 将 Viewer Web generated artifact 真值切到 `crates/oasis7_viewer/viewer.js`，由 `vite.software-safe.config.mjs` + `scripts/finalize-software-safe-build.mjs` 统一产出 canonical bundle，并生成 `software_safe.js` compat wrapper；`software_safe.html` 改为直接引用 `viewer.js`。
- 完成内容: 同步收口 dist / bundle / freshness helper 到 canonical-first 复制方向，覆盖 `scripts/run-viewer-web.sh`、`scripts/agent-browser-lib.sh`、`scripts/build-game-launcher-bundle.sh`、`scripts/bundle-freshness-lib.sh`、`scripts/agent-browser-viewer-dist-freshness-test.sh`，并把 `pixel-world-bridge/` generated runtime 继续绑定在同一条 finalize flow。
- 完成内容: 修复拆分后暴露的 feedback / hosted recovery contract 文案漂移，回跑 `npm --prefix crates/oasis7_viewer run test:ui`、`node crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs`、`bash scripts/agent-browser-viewer-dist-freshness-test.sh`、`npm --prefix crates/oasis7_viewer run build:software-safe`、`./scripts/doc-governance-check.sh`、`git diff --check` 全部通过。
- 遗留事项: `legacy_core.js` 仍保留较多 render/bootstrap 组装逻辑；下一轮若继续拆分，优先切 `render summary / interaction panel / event binding`，但不应再回退 canonical build truth。

## 2026-05-19 19:39:43 CST / viewer_engineer
- 完成内容: rebase `origin/main` 后补齐 hosted-account-login 新契约，把 `legacy_core.js` 的模块化 facade 对齐 `main.jsx` 期望的 `state.hostedLogin`、`startHostedAccountLogin()`、`completeHostedAccountLogin()`，避免拆分后的旧 `issue session` 语义与主入口新登录流程冲突。
- 完成内容: 统一 `viewer_auth_surface_module.js` 的 hosted recovery / backend reauth 文案到 “Hosted Account Login -> fresh player_session -> runtime register/reconnect” 单一路径，去掉 `Re-acquire Hosted Player Session` 这类把 compat 产物误当真值入口的表述。
- 完成内容: 最终回跑 `./scripts/ci-tests.sh required`，其中包含 `cargo fmt --all -- --check`、`cargo test -p oasis7 --tests --features test_tier_required`、`cargo test -p oasis7_consensus --lib`、`cargo test -p oasis7_distfs --lib`、`node crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs`、`npm --prefix crates/oasis7_viewer run test:ui`、`npm --prefix crates/oasis7_viewer run build:software-safe`，全部通过。
- 遗留事项: `legacy_core.js` 已降到单点组装层，但 hosted login、render/bootstrap 和部分 runtime sync 仍共处一文件；后续技术债优先继续拆 `hosted session lifecycle` 与 `runtime live event/render bridge`。

## 2026-05-19 20:07:57 CST / viewer_engineer
- 完成内容: 处理 PR `#254` review comments，把 hosted browser session 的 `localStorage` schema 对齐 `origin/main` 的安全边界：不再持久化 Ed25519 `privateKey/publicKey`，恢复路径只保留 `releaseToken + deviceSessionId`，并在读取旧记录时回写为当前 schema。
- 完成内容: 收口 `persistHostedPlayerSession()` / `resolveStoredHostedPlayerSession()` 的契约不一致，避免 `persist` 允许缺 `releaseToken` 而 `resolve` 把同一记录当损坏数据清掉。
- 完成内容: 修正 `crates/oasis7_viewer/scripts/finalize-software-safe-build.mjs` 的报错文案，从 `software_safe bundle outputs` 改为 `viewer canonical bundle outputs`，与当前 canonical `viewer.js` 单一真值命名保持一致。
- 完成内容: 回跑 `node crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs`、`npm --prefix crates/oasis7_viewer run test:ui`、`npm --prefix crates/oasis7_viewer run build:software-safe` 全部通过。
- 遗留事项: 这次只把 storage schema 安全边界拉齐到主线；如果后续要进一步缩窄恢复面，可以再把 `releaseToken` 的浏览器持久化范围和过期策略单独专题化。
