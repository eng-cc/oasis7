# task_fd49238273e0447d8189df40519a51b0 Execution Log

- task_uid: task_fd49238273e0447d8189df40519a51b0
- title: Shrink near-limit Rust hotspots
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-engineering-shrink-near-limit-rust-hotspots

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-20 23:39:55 CST / runtime_engineer
- 完成内容: 对 5 个逼近 1200 行阈值的 Rust 热点文件执行预防性职责拆分：`llm_sidecar.rs` 抽出 `llm_sidecar_runtime_support.rs`，`oasis7_node/src/lib.rs` 抽出 `consensus_support.rs` 与 `replica_maintenance_support.rs`，`oasis7_provider_parity_bench.rs` 抽出 `src/bin/oasis7_provider_parity_bench/io_support.rs`，`oasis7_wasm_router/src/lib.rs` 抽出 `filtering.rs`，`self_guided.rs` 抽出 `self_guided_storage.rs`；同时修复拆分后 test-only/可见性问题，确保新 helper 不再被 Cargo 误识别为独立 bin。
- 完成内容: 验证通过 `env -u RUSTC_WRAPPER cargo check -p oasis7_wasm_router -p oasis7_client_launcher -p oasis7_node -p oasis7 --bin oasis7_provider_parity_bench`、`cargo test -p oasis7_wasm_router --lib`、`cargo test -p oasis7 --lib bind_agent_player_emits_unbind_before_rebind_for_same_agent -- --nocapture`、`cargo test -p oasis7_client_launcher self_guided -- --nocapture`、`cargo test -p oasis7_node --lib --no-run`、`./scripts/check-rust-file-size.sh` 与 `./scripts/doc-governance-check.sh`；热点主文件现为 `858 / 841 / 981 / 860 / 1131` 行。
- 遗留事项: 无；后续若新增开发再次逼近 1200 行，应继续优先提前抽职责模块，不得回退到 baseline 或机械切片。

## 2026-04-21 00:19:26 CST / runtime_engineer
- 完成内容: 处理 PR `#130` 的 GitHub `required-gate` 失败。根据 CI 日志复现并修复两处由拆分遗漏引起的回归：一是给 `oasis7_provider_parity_bench.rs` 恢复 `#[path = "oasis7_provider_parity_bench/io_support.rs"]`，确保 CI 能正确解析 `io_support` 模块；二是将 `oasis7_node::compute_consensus_action_root` 恢复为公开导出，修复 `oasis7_chain_runtime` required-tier test 编译面。
- 完成内容: 重新跑通 `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_provider_parity_bench --no-run`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --tests --features test_tier_required --no-run` 与完整 `./scripts/ci-tests.sh required`；当前本地 required-tier 已通过。
- 遗留事项: 等待 GitHub 重新执行 `required-gate`；本地未观察到新的阻塞项。

## 2026-04-21 00:19:26 CST / runtime_engineer
- 完成内容: 处理 PR `#130` 的 review comment，补齐 `oasis7_provider_parity_bench` 帮助文本，使 `--provider` 参数显式列出 `provider_local_bridge`，并说明其是 `provider_loopback_http` 的 alias，避免 CLI 文档与 `BenchProviderKind::parse` 实现继续漂移。
- 完成内容: 验证通过 `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_provider_parity_bench --no-run`；随后将回推同一条 PR 并 resolve 已覆盖的 review threads。
- 遗留事项: 无。
