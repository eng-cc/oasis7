# task_494b9cd1a63544bca3c1e69f17af1918 Execution Log

- task_uid: task_494b9cd1a63544bca3c1e69f17af1918
- title: add new windows machine to testnet node inventory
- owner_role: tpm
- worktree_hint: /Users/scc/ccwork/worktrees/oasis7-engineering-add-testnet-windows-node

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
  - Action: ...
  - Validation Command: ...
  - Expected Result: ...
  - Actual Result: ...
  - Blocker / Next Action: ...
-->

## 2026-06-07 21:27:01 CST / tpm
- 完成内容: WORKFLOW BOOTSTRAP DECIDED for user request "最下面又加了一台window机器，也加到testnet节点里面"。
- 遗留事项: 更新仓库内 testnet 节点清单真值并完成最小验证。
- Repository State Impact: 文档真值更新；不改 runtime 代码与远端机器状态。
- Isolation Decision: 从干净的 `main` worktree 创建专用 task worktree `/Users/scc/ccwork/worktrees/oasis7-engineering-add-testnet-windows-node`，分支 `task/engineering-add-testnet-windows-node`。
- Task Truth: `.pm` task `task_494b9cd1a63544bca3c1e69f17af1918` 已创建并 `workflow-report start`；owner role 维持 `tpm` 作为流程协调。
- Routed Next Phase: `executing-project-tasks`，因为当前工作是把已有 repo truth 对齐到最新 testnet 节点 inventory。
- Scope Boundary: 仅依据 `/Users/scc/Documents/keys/aliyun_ecs.txt` 与仓库内现有 inventory/evidence 做客观写回，不把本次改动表述成 live deploy 或运行时拓扑已验证完成。
- Action: 读取 `aliyun_ecs.txt`、项目级环境清单与现有 triad/public-testnet evidence，确认新增机器身份与应落文档。
- Validation Command: `git status --short --branch`; `git worktree list`; `sed -n '1,220p' /Users/scc/Documents/keys/aliyun_ecs.txt`; `rg -n "192\\.168\\.1\\.3|192\\.168\\.1\\.4|testnet|节点清单" doc .pm/tasks -g '!third_party'`
- Expected Result: 找到新增 Windows 机器条目，并锁定当前 repo 中承载 testnet 节点 inventory 的 canonical 文档。
- Actual Result: `aliyun_ecs.txt` 末尾新增 `Administrator@192.168.1.3:220708`；当前与 testnet 节点真值最直接相关的文档是 `doc/engineering/governance/environment-lanes-and-inventory-2026-05-29.md` 与 `doc/testing/evidence/shared-network-ecs-triad-node-inventory-2026-03-30.md`。
- Blocker / Next Action: none.

## 2026-06-07 21:27:01 CST / tpm
- 完成内容: 完成 plan-gap review，决定以“补 inventory 真值”而不是“补部署/健康结论”的口径最小更新文档。
- 遗留事项: 落盘并验证 diff。
- Action: 对照既有证据，确认新增 Windows 机器应作为本地/局域网 testnet 节点入口补入环境清单与 triad inventory，而不改已有 ECS 责任描述。
- Validation Command: `sed -n '1,240p' doc/engineering/governance/environment-lanes-and-inventory-2026-05-29.md`; `sed -n '1,220p' doc/testing/evidence/shared-network-ecs-triad-node-inventory-2026-03-30.md`; `sed -n '1,220p' doc/testing/evidence/p2p-real-env-triad-snapshot-2026-04-07.md`; `sed -n '1,220p' doc/testing/evidence/public-testnet-ecs-freshness-audit-2026-05-22.md`
- Expected Result: 文档间口径保持一致，即阿里云两台 ECS 仍是云上主机，新增 Windows 机器只补充为 testnet 相关本地节点入口/库存信息。
- Actual Result: 现有环境清单只列 204/205 两台 ECS；triad inventory 仍停留在“本机 observer”版本。仓库已有 2026-06-03 证据表明 `192.168.1.4` 上运行第四个本地 testnet 节点，本次新增 `192.168.1.3` 为新的 Windows 机器后，最合理的 repo truth 是把它补成“新增本地/局域网 Windows 节点入口”，并明确尚未附带 live health/readiness 证据。
- Blocker / Next Action: 更新上述两份文档并做文本级验证。

## 2026-06-07 21:32:52 CST / tpm
- 完成内容: 用户将任务推进为“用 CI 编包，部署”；已将当前 task 路线扩展为 `文档写回 -> GitHub Testnet Packages 编包 -> Windows 目标机部署`。
- 遗留事项: 完成本地验证、提交推送、触发 workflow、下载产物并部署到 `192.168.1.3`。
- Action: 核对 GitHub Actions 与 repo 内 deploy 真值，确认当前存在 `Testnet Packages` 手动 workflow，可为当前分支生成 Windows 安装包；同时确认仓库暂无等价的 GitHub testnet deploy workflow，Windows 落地仍需走 operator SSH 路径。
- Validation Command: `sed -n '1,260p' .github/workflows/testnet-packages.yml`; `sed -n '1,260p' .github/workflows/release-packages.yml`; `rg -n "workflow_dispatch|testnet-packages|deploy|192\\.168\\.1\\.3|oasis7-windows-x64\\.exe" .github/workflows scripts doc -g '!third_party'`; `gh auth status`
- Expected Result: 明确“编包”和“部署”分别由哪条仓库主链负责，并避免把不存在的 deploy workflow 当成既有真值。
- Actual Result: `.github/workflows/testnet-packages.yml` 支持对任意 `ref_or_sha` 手动编包并包含 `windows-x64` 产物；部署真值仍是 operator 路径，现有 testnet deploy evidence 主要覆盖 ECS 的 `start-node.sh`/systemd 更新与本机 observer sync，不存在可直接替代的 Windows deploy workflow。
- Blocker / Next Action: none; 进入 closeout/commit/push 与 CI workflow 触发。

## 2026-06-07 22:40:17 CST / tpm
- 完成内容: 已改为使用最新 `origin/main` 编包，并把 `main` 的 Windows 产物部署到目标机准备目录。
- 遗留事项: 在 `192.168.1.3` 上执行安装器并验证安装结果。
- Action: 取消误触发的 task-branch workflow，改为对 `origin/main@cfe3430258fb25d5e55fb1f0c4233f758e62e422` 触发 `Testnet Packages`；等待 `package-windows-x64` 成功后下载 artifact，并通过 password SSH/SCP 传到 `Administrator@192.168.1.3` 的 `C:\\oasis7-deploy`。
- Validation Command: `git fetch origin main`; `gh run cancel 27094093022`; `gh workflow run testnet-packages.yml --ref main -f ref_or_sha=cfe3430258fb25d5e55fb1f0c4233f758e62e422 -f build_profile=release -f package_scope=all_existing`; `gh api repos/eng-cc/oasis7/actions/runs/27094313636/artifacts`; `gh run download 27094313636 -n testnet-package-windows-x64-0.0.0+testnet.16.cfe3430258fb -D .tmp/testnet-package-27094313636/windows`; `sshpass -e scp ... Administrator@192.168.1.3:/C:/oasis7-deploy/`; `sshpass -e ssh ... \"cmd /c dir C:\\oasis7-deploy\"`; `sshpass -e ssh ... \"cmd /c certutil -hashfile ... SHA256\"`
- Expected Result: 使用最新 `main` 的 Windows 安装包完成远端落盘，且远端文件哈希与本地下载产物一致。
- Actual Result: `Testnet Packages` run `27094313636` 对 `main`/`cfe3430258fb25d5e55fb1f0c4233f758e62e422` 成功产出 `testnet-package-windows-x64-0.0.0+testnet.16.cfe3430258fb`；`oasis7-windows-x64.exe` 及 `BUILDINFO/SHA256SUMS/bundle-manifest/entrypoint-validation.log` 已传到 `C:\\oasis7-deploy`。远端 `certutil` 返回的 SHA256 与本地一致，其中安装包哈希为 `78dcf6ebc8ae6d856992a353af0cec2ffa0d294228a60a64bc9b023674dd499e`。
- Blocker / Next Action: none; 静默安装并核对 `%LOCALAPPDATA%\\Programs\\oasis7` 是否落地。

## 2026-06-07 22:48:54 CST / tpm
- 完成内容: 已在 `192.168.1.3` 上完成 NSIS 静默安装并验证安装目录、二进制入口与快捷方式落地。
- 遗留事项: 补一次安装后启动验证，确认客户端实际可拉起。
- Action: 远端执行 `C:\\oasis7-deploy\\oasis7-windows-x64.exe /S`，随后检查卸载注册表项、`%LOCALAPPDATA%\\Programs\\oasis7` 目录、`run-client.cmd`、`Uninstall.exe`、`bin\\*.exe`，以及开始菜单/桌面快捷方式。
- Validation Command: `sshpass -e ssh ... 'cmd /c \"C:\\oasis7-deploy\\oasis7-windows-x64.exe /S\"'`; `sshpass -e ssh ... 'cmd /c reg query HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Uninstall\\oasis7 /s'`; `sshpass -e ssh ... 'cmd /c dir \"%LOCALAPPDATA%\\Programs\"'`; `sshpass -e ssh ... 'cmd /c dir \"%LOCALAPPDATA%\\Programs\\oasis7\" & dir \"%LOCALAPPDATA%\\Programs\\oasis7\\bin\"'`; `sshpass -e ssh ... 'cmd /c if exist \"%APPDATA%\\Microsoft\\Windows\\Start Menu\\Programs\\oasis7\\oasis7 Client Launcher.lnk\" echo STARTMENU_OK & if exist \"%USERPROFILE%\\Desktop\\oasis7 Client Launcher.lnk\" echo DESKTOP_OK'`
- Expected Result: 安装器把 `oasis7` 安装到当前用户目录，并创建可发现的启动/卸载入口。
- Actual Result: 注册表项 `HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Uninstall\\oasis7` 已创建，`DisplayVersion=0.0.0+testnet.16.cfe3430258fb`，`InstallLocation=C:\\Users\\Administrator\\AppData\\Local\\Programs\\oasis7`；安装目录下已存在 `run-client.cmd`、`Uninstall.exe`、`web/`、`web-launcher/` 与 `.oasis7-bundle-manifest.json`；`bin\\` 下已存在 `oasis7_client_launcher.exe`、`oasis7_game_launcher.exe`、`oasis7_chain_runtime.exe`、`oasis7_web_launcher.exe` 等；开始菜单和桌面快捷方式均已创建。
- Blocker / Next Action: 继续做安装后启动验证；当前偶发出现 password SSH 命令行封装抖动，需要用更保守的分步命令复核进程，而不是把它误判成安装失败。

## 2026-06-07 23:04:58 CST / tpm
- 完成内容: 已定位 Windows 启动验证的首个真实阻断不是安装包损坏，而是目标机缺少 VC++ runtime；补装后 `oasis7_web_launcher.exe --help` 已可正常执行。
- 遗留事项: 对 `oasis7_web_launcher` 做一次最小前台运行验证；`oasis7_client_launcher` 的 GUI 启动仍需区分“SSH 非交互图形环境限制”与“桌面会话真实可用性”。
- Action: 用远端 `.cmd` 包装 `oasis7_web_launcher.exe --help` 捕获 stdout/stderr/exit code，先拿到失败码 `-1073741515`；随后检查目标机 `System32`，发现缺少常见 `vcruntime140.dll/msvcp140.dll`；通过微软官方 `https://aka.ms/vc14/vc_redist.x64.exe` 静默安装 `Microsoft Visual C++ 2015-2022 Redistributable (x64) - 14.51.36231`，再复跑 help 验证。另对 `oasis7_client_launcher.exe --help` 进行同样包装复测。
- Validation Command: `cmd /c C:\\oasis7-deploy\\oasis7-help-web.cmd`; `type C:\\oasis7-deploy\\web-help.exit`; `powershell -NoProfile -ExecutionPolicy Bypass -File C:\\oasis7-deploy\\install-vcredist.ps1`; `Get-Content C:\\oasis7-deploy\\vc_redist_install.log -Tail 40`; `cmd /c C:\\oasis7-deploy\\oasis7-help-client.cmd`; `type C:\\oasis7-deploy\\client-help.exit`
- Expected Result: 若是缺少系统运行库，补装 VC++ runtime 后 `web_launcher --help` 应从 DLL 级失败转为正常退出；若 `client_launcher` 仍失败，则错误签名应切换为图形/窗口栈问题，帮助区分“包坏”与“非交互桌面限制”。
- Actual Result: `web_launcher` 初次 `help` 的 `EXITCODE=-1073741515`，无 stdout/stderr，符合 DLL/运行库缺失特征；VC++ runtime 安装日志 `Apply complete, result: 0x0`。补装后 `web_launcher --help` 变为 `EXITCODE=0`，并正常输出 Usage 文本。`client_launcher --help` 则返回 `EXITCODE=1`，stderr 为 `Error: NoGlutinConfigs(... kind: NotFound)`，说明当前失败面已切到图形上下文/窗口配置，而非缺少 VC runtime 或安装文件缺失。
- Blocker / Next Action: 下一步优先用前台/最小参数验证 `oasis7_web_launcher` 能否在 SSH 会话中实际绑定监听；`client_launcher` 暂不应被误判成安装失败，更可能需要真实桌面会话或图形能力验证。

## 2026-06-07 23:19:41 CST / tpm
- 完成内容: 已确认 `oasis7_client_launcher` 可以被注入真实桌面会话；此前失败不是“机器无桌面”，而是 SSH 直接启动把 GUI 进程落到了 `Services` session 0。
- 遗留事项: 清理临时计划任务/诊断脚本；后续若要产品化该路径，可把“桌面 session 注入”整理成 operator runbook 或自动化脚本。
- Action: 先用 `query user` / `query session` / `tasklist explorer.exe` 证明 `Administrator` 已登录 `console` session 1 且 `explorer.exe` 存在；随后通过 Scheduled Task 的 `Interactive` principal 启动 `C:\\Users\\Administrator\\AppData\\Local\\Programs\\oasis7\\run-client.cmd`，并在任务启动后轮询 `oasis7_client_launcher` 的 `SessionId/StartTime/Responding`。
- Validation Command: `query user`; `query session`; `tasklist /FI "IMAGENAME eq explorer.exe"`; `Register-ScheduledTask -TaskName oasis7-client-console-start -Action (New-ScheduledTaskAction -Execute "...\\run-client.cmd") -Principal (New-ScheduledTaskPrincipal -UserId "Administrator" -LogonType Interactive -RunLevel Highest)`; `Start-ScheduledTask -TaskName oasis7-client-console-start`; `Get-Process oasis7_client_launcher | Select Name,Id,SessionId,StartTime,MainWindowHandle,Responding,Path`
- Expected Result: 若真实桌面会话存在，interactive scheduled task 应把 `oasis7_client_launcher` 启动到 `SessionId=1`，并在短暂等待后仍保持存在；这可与此前 `Services` session 0 的错误启动方式形成对照。
- Actual Result: `Administrator` 处于 `console` session 1，`explorer.exe` 也在 `SessionId=1`。直接 `start "" run-client.cmd` 只会把 `oasis7_client_launcher` 拉到 `Services` session 0；改用 `oasis7-client-console-start` 计划任务后，`oasis7_client_launcher` PID `64060` 出现在 `SessionId=1`，`StartTime=2026/6/7 23:16:57`，5 秒后复查仍存在且 `Responding=True`。任务状态显示 `Running`，说明 GUI 客户端已被成功注入当前桌面会话。
- Blocker / Next Action: 无部署 blocker；后续 repo TODO 方向已记录为（1）安装器补 VC++ runtime 自动安装，（2）无图形环境友好报错/fallback。另发现 `.pm/inbox/signals.jsonl` 并发 capture 出现重复 `signal_id=SIG-PM-0056`，建议后续单独修 PM signal id 生成竞态。

## 2026-06-07 23:23:18 CST / tpm
- 完成内容: 已完成临时验证路径收尾：客户端继续保留在桌面 session 1，临时 scheduled task 已撤除，不再作为常驻触发器残留。
- 遗留事项: 如需进一步整洁，可继续手动清理 `C:\\oasis7-deploy` 下保留的诊断日志与 VC runtime 安装日志；当前它们作为部署证据暂时保留。
- Action: 通过单独 cleanup 脚本对 `oasis7-client-console-start` 依次执行 `Disable/Stop/Unregister`，同时保留 `oasis7_client_launcher` 进程不被误杀；随后复查客户端进程与 `C:\\oasis7-deploy` 目录状态。
- Validation Command: `Disable-ScheduledTask/Stop-ScheduledTask/Unregister-ScheduledTask -TaskName oasis7-client-console-start`; `Get-Process oasis7_client_launcher | Select Name,Id,SessionId,Responding`; `Get-ChildItem C:\\oasis7-deploy`
- Expected Result: 临时计划任务被移除，而已启动到桌面会话的 `oasis7_client_launcher` 继续留在 `SessionId=1` 运行；部署与调试证据文件仍可按需保留在 `C:\\oasis7-deploy`。
- Actual Result: cleanup 脚本返回 `TASK_CLEANUP_ATTEMPTED`；后续复查中 `oasis7_client_launcher` 仍为 PID `64060`、`SessionId=1`、`Responding=True`。`C:\\oasis7-deploy\\cleanup-task-state.txt` 为空，表明任务查询已无返回项；目录内仍保留安装器、VC runtime、help/run 输出与 cleanup 证据文件，便于后续审计。
- Blocker / Next Action: 当前无 blocker。本次部署/启动验证已经足够收口；若要继续工程化，下一步应转向修 repo TODO：把 VC runtime 安装和 GUI 无图形提示纳入正式发布链。

## 2026-06-07 23:57:12 CST / tpm
- 完成内容: 已把 Windows observer 从旧 `parallel-20260518` manifest 切到当前 ECS 正在使用的 governed public-testnet 合同，并让 `oasis7_chain_runtime` 在 `127.0.0.1:5121` 上稳定启动。
- 遗留事项: 节点虽然已按 `public_testnet` / `governance_registry` 合同运行，但还没有进入 ready/synced；需要继续排查为何拿不到 peer committed head / replication peer record。
- Action: 先从 `39.104.204.172` / `39.104.205.67` 读取当前 `/opt/oasis7/p2p-testnet/config/node.env`，确认现网 contract 已升级为 `WORLD_ID=oasis7-public-testnet-governed-20260606` 且显式使用 `GENESIS_VALIDATOR_REGISTRY_PATH=/opt/oasis7/p2p-testnet/config/public-testnet-governed-bootstrap-validator-registry-2026-06-06.json`。随后把 ECS 当前 governed manifest、validator registry、bootstrap peers，以及 `genesis` 依赖的 signer/topology evidence 同步到 `C:\\oasis7-deploy\\config`，重写 Windows 本地化 `manifest` / `bundle` / `genesis`，让它们引用 `C:/oasis7-deploy/config/...` 与已安装的 Windows runtime `sha256=d8d3b409fbc9d2090b7ec6c180cb20108579e728c9159d47f0e7f2ae8820eed2`。最后通过 `C:\\oasis7-deploy\\oasis7-chain-governed-run.cmd` 启动 observer。
- Validation Command: `ssh root@39.104.204.172 'sed -n 1,220p /opt/oasis7/p2p-testnet/config/node.env'`; `ssh root@39.104.205.67 'sed -n 1,220p /opt/oasis7/p2p-testnet/config/node.env'`; `ssh Administrator@192.168.1.3 'cmd /c type C:\\oasis7-deploy\\chain-governed-run.out'`; `ssh Administrator@192.168.1.3 'powershell -NoProfile -Command \"(Invoke-WebRequest -UseBasicParsing http://127.0.0.1:5121/v1/chain/status -TimeoutSec 8).Content\"'`
- Expected Result: Windows observer 不再因缺少 `--genesis-validator-registry` 或 Linux 绝对路径引用而启动失败，且 `/v1/chain/status` 明确显示 `network_tier.tier=public_testnet`、`world_id=oasis7-public-testnet-governed-20260606`。
- Actual Result: 启动路径连续前移了三次：（1）旧 live-candidate manifest 下的 `public validator registry required before startup` 已通过补齐 `--genesis-validator-registry` 消除；（2）随后暴露 `genesis` 内嵌 Linux worktree 绝对路径，补齐 `public-testnet-governance-public-signers-2026-06-05.json`、`public-testnet-liveops-public-signers-2026-06-05.json`、`network-tier-signer-truth-binding-2026-06-05.md`、`public-testnet-governed-bootstrap-topology-2026-06-06.md` 并把 `genesis` refs 本地化到 `C:/oasis7-deploy/config/...` 后，runtime 成功进入 `ready`；（3）`http://127.0.0.1:5121/v1/chain/status` 当前返回 `running=true`、`runtime_last_error=null`、`network_tier.tier=public_testnet`、`network_tier.governance_mode=governance_registry`、`network_tier.source_path=\\\\?\\C:\\oasis7-deploy\\config\\public-testnet-governed-bootstrap-manifest-2026-06-06.windows.json`、`bootstrap_peer_count=2`、`role=observer`、`world_id=oasis7-public-testnet-governed-20260606`。这说明 Windows 机已经按现网 testnet 合同启动，不再停留在 hosted/local-dev 模式。
- Blocker / Next Action: 当前主阻断已从“能否启动/是否接入 public_testnet 合同”切换为“为何还未拿到有效 peer head”。`/v1/chain/status` 仍是 `readiness.status=not_ready`，`failed_gates=[consensus_peer_head_unavailable, replication_recent_errors, p2p_reachability_degraded, reward_runtime_degraded]`，`fresh_peer_count=0`，`known_peer_heads=0`，`connected_peers=[]`；但 `recent_errors` 同时记录了 bootstrap peer `12D3KooWMyPapum...` / `12D3KooWAuNCCE...` 的 `connection established` / `routing updated` / `MissingPeerRecord` / `InsufficientActiveDiscoverySources` / `ConnectionClosed`。下一步应继续对照 ECS 侧 peer/discovery/reachability 真值，确认 Windows observer 是否还缺少 peer record/discovery seed 或额外的 P2P operator 配置，而不是再回退到 packaging/deploy 层。

## 2026-06-08 00:18:44 CST / tpm
- 完成内容: 完成“设计/实现问题优先修复”的 plan-gap review，确认当前真正缺口不是再给 Windows observer 手工 seed，而是 repo 内 fresh observer bootstrap 只有 `trusted checkpoint + state-sync bundle` 的校验/恢复骨架，没有稳定的“一致性导出 bundle”主链；现有 `seed-from-remote` 仍在直接复制 live world/files，容易跨保存窗口产生不一致。
- 遗留事项: 修 `scripts/p2p-public-testnet-local-observer-sync.sh`，把 remote seed staging 从裸拷 live 目录推进到“优先复制冻结 sidecar/一致性校验并重试”的路径，然后再清空 Windows 节点数据并按修复后流程重部署。
- Professional Slice Contract: `blockchain_ops_engineer` 责任面=public-testnet observer bootstrap/operator flow，需给出“从健康节点导出一致 bootstrap seed”的最小可落地改动；`runtime_engineer` 责任面=execution world persistence/sidecar/archive 一致性边界，需确认哪些文件组合可被 runtime 作为可信 bootstrap 输入。当前工具限制下由 TPM 在 task log 内记录 slice 结论并据此集成，不能把未验证的经验判断当成专业真值。
- Action: 对照 `scripts/p2p-upgrade-preflight.sh`、`scripts/p2p-public-testnet-local-observer-sync.sh`、`crates/oasis7/src/runtime/world/persistence.rs`、`persistence_support.rs` 和 `doc/testing/evidence/testnet-upgrade-preflight-drill-2026-06-01.md`，核实已有能力与缺失环节。
- Validation Command: `sed -n '520,1120p' scripts/p2p-upgrade-preflight.sh`; `sed -n '240,380p' scripts/p2p-public-testnet-local-observer-sync.sh`; `sed -n '720,860p' scripts/p2p-public-testnet-local-observer-sync.sh`; `sed -n '636,940p' crates/oasis7/src/runtime/world/persistence.rs`; `sed -n '320,560p' crates/oasis7/src/runtime/world/persistence_support.rs`; `sed -n '150,260p' doc/testing/evidence/testnet-upgrade-preflight-drill-2026-06-01.md`
- Expected Result: 明确“恢复侧已有、生成侧缺失”的代码证据，并把修复收敛到 seed staging/export 入口，而不是放宽 runtime 校验或继续 operator 手工拼装目录。
- Actual Result: `p2p-upgrade-preflight.sh` 已具备受信 checkpoint、state-sync bundle、restore/rollback plan 与执行时二次校验，但只消费 bundle，不生成 bundle；runtime 保存路径已为 `execution-world` 写入 `snapshot.manifest.json`、`journal.segments.json` 与 `.distfs-state/sidecar-generations/*`，且 sidecar generation 在保存时会先 stage、再 validate、再 finalize，具备“冻结生成”的一致性基础。当前 `seed-from-remote` 却仍直接从 live `execution-world` 复制 `snapshot.json` / `journal.json` / `tick-consensus.archive.index.json`，还未复制 `.distfs-state`，也未对源文件在 staging 期间是否变化做二次确认。这正好解释了 Windows 上出现的 `tick consensus archive index hot range mismatch`。
- Blocker / Next Action: next action 是先补 `seed-from-remote`：至少把 `.distfs-state` / sidecar root files 一并纳入 staging，并在 remote staging 末尾做源文件稳定性校验；若源文件在 staging 期间变化则放弃并重试。完成后做脚本级验证，再用修复后的流程清空并重部署 Windows 节点。

## 2026-06-08 02:01:22 CST / tpm
- 完成内容: 将“observer 坏本地 world 会阻断启动”从 operator 补救问题收敛为 runtime 启动语义修复，并完成针对性测试验证。
- 遗留事项: 提交当前修复、触发 Windows 打包、替换 `192.168.1.3` 上的 chain runtime，并在清空旧数据后验证 observer 能从干净状态重新导入 governed registry 并进入自动同步。
- Professional Slice Contract: `runtime_engineer` 责任面=observer 启动与 execution world 恢复语义；结论是不要放宽 world validation，而应在 `NodeRole::Observer` 启动前主动检测本地 persisted world，若加载失败则隔离坏 `execution-world` 与 simulator mirror，让 runtime 以 clean bootstrap state 继续完成治理注册表导入与后续网络跟随。`blockchain_ops_engineer` 责任面=Windows observer 重部署路径；结论是部署时应清空现存坏状态，验证节点靠正常 governed public-testnet bootstrap 恢复，而不是保留/回填历史 seed。当前工具限制下由 TPM 在 task log 内记录 slice 结论并据此集成。
- Action: 在 `crates/oasis7/src/bin/oasis7_chain_runtime.rs` 增加 `maybe_recover_observer_execution_state` / `quarantine_observer_execution_state`，仅对 `NodeRole::Observer` 在 runtime init 前执行坏 world 隔离；同步补 `oasis7_chain_runtime_tests.rs` 三条定向测试，并修正同一 bin 中 `TransferMainToken` 新增 `extensions` 字段带来的旧测试/构造漂移，使定向验证可运行。
- Validation Command: `rtk cargo check -p oasis7 --bin oasis7_chain_runtime --message-format=short`; `rtk cargo test -p oasis7 --bin oasis7_chain_runtime observer_invalid_execution_world_is_quarantined_before_runtime_init -- --nocapture`; `rtk cargo test -p oasis7 --bin oasis7_chain_runtime observer_quarantine_allows_genesis_registry_reimport_on_clean_world -- --nocapture`; `rtk cargo test -p oasis7 --bin oasis7_chain_runtime non_observer_invalid_execution_world_is_not_quarantined -- --nocapture`
- Expected Result: `oasis7_chain_runtime` 单 bin 编译通过；observer 坏 world 会被隔离；隔离后空 world 仍可导入 genesis governance registry；非 observer 不应误触发隔离逻辑。
- Actual Result: `cargo check -p oasis7 --bin oasis7_chain_runtime` 最终收敛到 runtime bin 自身通过；三条 observer 定向测试均 `1 passed`。这说明修复已覆盖根因链路的关键行为：严格 world 校验保持不变，但坏本地 persisted world 不再把 observer 永久卡死在启动前。
- Blocker / Next Action: 无代码级 blocker；进入部署主线，产出新的 Windows 包/二进制并在 `192.168.1.3` 上清理旧数据后做冷启动验证。

## 2026-06-08 15:05:00 CST / tpm
- 完成内容: 针对“fresh observer 冷启动后 `committed_height=0` 且 `state_sync_fallback_required=true`”补充新的 execution plan 和专业责任边界，明确当前剩余问题属于自动 bootstrap/state-sync 机制缺口，而不是 packaging、部署或坏本地 world 复发。
- 遗留事项: 落地根修，让 observer 在空本地状态且远端高度明显领先时，能自动建立可信同步基线，而不是停在 `replication gap sync` 的逐高度慢路径起点。
- Professional Slice Contract: `runtime_engineer` 责任面=`NodeRuntime`/`PosNodeEngine` 冷启动与 replication bootstrap 语义，需回答“现有 gap sync 为什么无法把 fresh observer 从 0 推进到 network head，以及最小安全接线点在哪”；`blockchain_ops_engineer` 责任面=governed public-testnet observer 冷启动/回滚操作边界，需坚持“自动链路修复，不引入手工 seed/post-fix 依赖”。当前工具限制下由 TPM 在 task log 中记录 slice 结论并据此集成。
- Integration Order:
  1. 先确认 `oasis7_net` 现有 `bootstrap_world_from_head*` 能力与 `oasis7_node` 当前 observer 冷启动主链之间的断层。
  2. 选取最小代码接点，把“空本地 observer + 远端已知 head”接入自动 bootstrap/建立本地基线流程。
  3. 补 focused tests，覆盖 fresh observer 自动 bootstrap/脱离 `committed_height=0` 的行为。
  4. 重新编包并在 `192.168.1.3` 清空数据后冷启动复验。
- Action: 阅读 `crates/oasis7_node/src/lib.rs`、`node_engine_replication.rs`、`node_engine_network.rs`、`crates/oasis7_net/src/bootstrap.rs`、`head_follow.rs`、`observer.rs` 与现有 gap-sync tests，确认 runtime 目前只有“逐高度 fetch commit”恢复路径，而没有在大幅落后且无 checkpoint 时自动走 peer-head bootstrap 的 wiring。
- Validation Command: `rtk rg -n "bootstrap_world_from_head|HeadFollower|sync_missing_replication_commits|replication_persisted_height|state_sync_fallback_required|network_committed_height" crates/oasis7/src/bin/oasis7_chain_runtime crates/oasis7_node crates/oasis7_net`; `sed -n '220,620p' crates/oasis7_node/src/lib.rs`; `sed -n '260,620p' crates/oasis7_node/src/node_engine_replication.rs`; `sed -n '1,220p' crates/oasis7_net/src/bootstrap.rs`; `sed -n '1,360p' crates/oasis7_net/src/observer.rs`
- Expected Result: 拿到可落地的根因链，即“status 只报告 fallback，需要的 bootstrap 能力存在，但 observer 冷启动主链没有把它接入，因此 fresh observer 会卡在没有本地基线的 gap sync 起点”。
- Actual Result: 已确认 `oasis7_net` 具备 `bootstrap_world_from_head*` / `HeadFollower` 能力，但 `NodeRuntime` 当前冷启动只做 `reconcile_engine_with_persisted_replication` + 运行时 `sync_missing_replication_commits`，恢复路径仅有逐高度 fetch commit；`status_payload_state_sync.rs` 只负责报告 fallback，不执行同步。这与远端 `network_committed_height=3066`、`replication_persisted_height=0`、`checkpoint_count=0` 的状态吻合。
- Blocker / Next Action: next action 是在 runtime/node 主链中补“自动 bootstrap 建立基线”的最小接线，并用 focused tests 锁住行为；不再继续扩展后置脚本修复路径。

## 2026-06-08 16:10:00 CST / tpm
- 完成内容: 根据远端最新 status 与本地代码链路，把本轮根因进一步收敛为“observer 启动期 bootstrap peer 只因 peer-record/discovery 尚未热起来就被请求选路错误清空，导致 `fetch-commit` request 主链自我饿死”，并完成最小修复与针对性测试。
- 遗留事项: 用修复后的版本重新编包、部署到 `192.168.1.3`，清空 observer 数据后复验 `connected_peers`、`fetch-commit` 与 `committed_height` 是否脱离 0。
- Professional Slice Contract: `runtime_engineer` 结论=当前 public-testnet fresh observer 的真实阻塞先于“trusted checkpoint 自动恢复”，发生在 libp2p request candidate 选择：当 connected peer 全部只带 `MissingPeerRecord`/`InsufficientActiveDiscoverySources` 这类启动期软约束时，`admissible_request_peers()` 错误返回空，导致上层 `request_with_providers`/`request` 把仍在线的 bootstrap peer 视为不可请求；`blockchain_ops_engineer` 结论=这是链同步机制实现缺口，应修请求主链而不是继续手工 seed。
- Action: 读取远端 `http://127.0.0.1:5121/v1/chain/status`，确认当前 `network_head.height=3105`、`known_peer_heads=2`、`connected_peers=[]`、`libp2p request_response.outbound_failure=4794`、`recent_errors` 持续含 `MissingPeerRecord` 与 `InsufficientActiveDiscoverySources`；随后在 `crates/oasis7_net/src/libp2p_net/api.rs` 调整 `connected_or_active_transport_peers_from_healths(...)`，当 connected peers 非空但都只是 soft-deprioritized bootstrap peers 时，不再返回空集合，而是回退到这些已连接 peer；同步在 `crates/oasis7_net/src/libp2p_net/tests/admissible_request_peers_tests.rs` 增补 connected soft bootstrap peers 的回归测试。
- Validation Command: `sshpass -p '220708' ssh ... \"powershell -NoProfile -Command \\\"(Invoke-WebRequest -UseBasicParsing http://127.0.0.1:5121/v1/chain/status -TimeoutSec 8).Content\\\"\"`; `rtk cargo test -p oasis7_net admissible_request_peers_keeps_connected_soft_bootstrap_peers_when_no_healthy_choice_exists -- --nocapture`; `rtk cargo test -p oasis7_net admissible_request_peers -- --nocapture`
- Expected Result: 启动期仅因 peer-record 交换未完成而被 soft-deprioritized 的 bootstrap peer，仍应保留在 request 候选集中；新增单测和同模块回归应通过。
- Actual Result: 远端最新 status 显示 `network_committed_height=3105`、`replication_persisted_height=0`、`known_peer_heads=2`、`connected_peers=[]`，同时 `libp2p` 控制面累计 `request_response.outbound_failure=4794`，最近错误反复出现 `MissingPeerRecord` / `InsufficientActiveDiscoverySources` / `ConnectionClosed`。代码修复后，新增定向测试 `admissible_request_peers_keeps_connected_soft_bootstrap_peers_when_no_healthy_choice_exists` 通过；同模块回归 `cargo test -p oasis7_net admissible_request_peers -- --nocapture` 最终为 `5 passed, 139 filtered out`。
- Blocker / Next Action: 代码级回归已通过；next action 是重新编包并部署到 Windows observer，然后清空数据冷启动复验同步是否真正恢复。

## 2026-06-08 16:35:00 CST / tpm
- 完成内容: 修复 CI/web packaging 对 `trunk` 运行时下载 `wasm-bindgen` release 的脆弱依赖，把 launcher web build 接入仓库已锁定的 `wasm-bindgen-cli` 供应链。
- 遗留事项: 提交并推送当前修复，重新触发 `Testnet Packages`；若 workflow 成功，再下载新的 Windows 产物替换 `192.168.1.3` 并做清库冷启动复验。
- Professional Slice Contract: `runtime_engineer` 结论=这一步属于 build/runtime toolchain 接线修复，不改变链同步语义，只消除 CI 在 `build-web-dist` 中对 GitHub release 临时下载 `wasm-bindgen-0.2.122-*.tar.gz` 的外部脆弱点；`blockchain_ops_engineer` 结论=只有打包链稳定后，Windows observer 的重部署/冷启动验证才可连续推进。
- Action: 新增 `scripts/build-client-launcher-web.sh`，复用 `scripts/ensure-wasm-bindgen-cli.sh` 解析并预装锁定版本的 `wasm-bindgen`，然后以 `PATH=<cached-bin>:$PATH` 和 `WASM_BINDGEN_BIN=<cached-bin>` 运行 `trunk build`。同步将 `.github/workflows/testnet-packages.yml`、`.github/workflows/mainnet-packages.yml`、`.github/workflows/release-packages.yml`、`scripts/build-game-launcher-bundle.sh` 与 `scripts/ci-tests.sh` 中对 launcher web dist 的裸 `trunk build` 调用切到该脚本。
- Validation Command: `git diff --check`; `./scripts/pm/lint.sh`; `./scripts/doc-governance-check.sh`; `./scripts/build-client-launcher-web.sh --release --dist-dir "$PWD/.tmp/launcher-web-dist-local"`
- Expected Result: `build-client-launcher-web.sh` 明确使用仓库缓存的 `wasm-bindgen 0.2.122`，不再触发 `trunk` 自行下载 GitHub release；workflow/build/bundle/CI 的 launcher web build 入口统一到同一 pinned path。
- Actual Result: `git diff --check` 通过。`./scripts/build-client-launcher-web.sh --release --dist-dir "$PWD/.tmp/launcher-web-dist-local"` 输出明确显示 `PATH=/Users/scc/.cache/oasis7/wasm-bindgen-cli/0.2.122/bin:$PATH WASM_BINDGEN_BIN=/Users/scc/.cache/oasis7/wasm-bindgen-cli/0.2.122/bin/wasm-bindgen trunk build ...`，随后本地成功产出 `target/wasm32-unknown-unknown/release/oasis7_client_launcher.wasm`，证明 launcher web 构建已走 pinned CLI 而非外部下载。`./scripts/pm/lint.sh` 仍因现存脏文件 `.pm/inbox/signals.jsonl` 中 `duplicate signal_id in inbox: SIG-PM-0056` 失败，这与本次 CI 修复无关且未被擅自改写。
- Blocker / Next Action: 当前 blocker 已从“workflow 504 下载失败”降为“等待重新触发 CI 并拿到新 Windows 包”；next action 是提交/push 当前修复并重跑 `Testnet Packages`。

## 2026-06-08 17:25:00 CST / tpm
- 完成内容: 已在 `testnet.20.dafadbd5898c` 新包上完成“真冷启动复现”，确认问题仍属于链同步机制本体；随后将根因再收敛到 peer-manager warmup quarantine，并完成最小修复与 focused tests。
- 遗留事项: 提交/推送当前 warmup quarantine 修复，重新打包 Windows 包并再次在 `192.168.1.3` 上清库复验，确认 observer 是否脱离 `committed_height=0`。
- Professional Slice Contract: `runtime_engineer` 结论=前一轮修复只保证“已连接 soft bootstrap peer 仍可进入 request 候选集”，但 fresh observer 冷启动时 peer-manager 仍会因为 `MissingPeerRecord + InsufficientActiveDiscoverySources` 将 active bootstrap peers 放入 quarantine 并主动断连，导致连接还没等 peer-record/discovery source 自愈就被本地掐断；这是同一同步机制缺口的下一层根因。`blockchain_ops_engineer` 结论=部署链已经排除，需继续修运行时 peer warmup 语义而非再改 operator 步骤。
- Action: 在 `192.168.1.3` 上确认新包已真正安装为 `DisplayVersion=0.0.0+testnet.20.dafadbd5898c`，并在清空 `windows-observer/state`、`state-simulator-mirror`、`storage`、`replication` 后重新启动 governed observer。复验 status 发现即便是新包 + 清库冷启动，仍然出现 `committed_height=0`、`replication_persisted_height=0`、`network_committed_height=3383`、`known_peer_heads=2`、`connected_peers=[]`、`state_sync_fallback_required=true`，同时 recent errors 继续是 `MissingPeerRecord` / `InsufficientActiveDiscoverySources` / `ConnectionClosed`。随后回到 `crates/oasis7_net/src/libp2p_net/runtime_loop.rs`，把 `peer_requires_active_quarantine(...)` 从“仅 `MissingPeerRecord` 不 quarantine”扩展为“`MissingPeerRecord` 与 `InsufficientActiveDiscoverySources` 这类 warmup-only 自愈问题组合也不 quarantine”，并在 `crates/oasis7_net/src/libp2p_net/tests.rs` 增补 `peer_requires_active_quarantine_skips_bootstrap_warmup_block_combo` 回归测试。
- Validation Command: `sshpass -p '220708' ssh ... 'powershell -NoProfile -Command \"(Invoke-WebRequest -UseBasicParsing http://127.0.0.1:5121/v1/chain/status -TimeoutSec 8).Content\"'`; `sshpass -p '220708' ssh ... 'cmd /c reg query HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Uninstall\\oasis7 /v DisplayVersion'`; `rtk cargo test -p oasis7_net peer_requires_active_quarantine_skips_bootstrap_warmup_block_combo -- --nocapture`; `rtk cargo test -p oasis7_net peer_requires_active_quarantine -- --nocapture`; `rtk cargo test -p oasis7_net admissible_request_peers -- --nocapture`
- Expected Result: 新包安装版本应切到 `testnet.20.dafadbd5898c`；冷启动复现应证明问题不在部署旧状态；peer-manager 不应再因 warmup-only discovery issues 主动 quarantine/断开 active bootstrap peers；新增和相关 focused tests 应通过。
- Actual Result: 远端安装版本最终成功切到 `0.0.0+testnet.20.dafadbd5898c`，清库冷启动后 status 仍重现 `committed_height=0`、`replication_persisted_height=0`、`known_peer_heads=2`、`connected_peers=[]`、`state_sync_fallback_required=true`，从而排除“旧包/旧数据”因素。代码修复后，`peer_requires_active_quarantine_skips_bootstrap_warmup_block_combo` 通过；相关 `peer_requires_active_quarantine` focused suite 为 `2 passed, 143 filtered out`；`admissible_request_peers` focused suite 为 `5 passed, 140 filtered out`。
- Blocker / Next Action: 当前无代码级 blocker；next action 是提交/推送这次 warmup quarantine 修复，重新编包并再次远端冷启动复验同步是否真正恢复。
