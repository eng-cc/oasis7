# task_49d6af52e31d404eb80999993eb71b98 Execution Log

- task_uid: task_49d6af52e31d404eb80999993eb71b98
- title: Harvest ECS-backed public_testnet and shared-access evidence
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-public-testnet-ecs-evidence-harvest

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-22 10:08:19 CST / runtime_engineer
- 完成内容:
  - 新开独立 worktree `task/p2p-public-testnet-ecs-evidence-harvest`，基于最新 `public_testnet` claims 分支继续，避免把新一轮 ECS freshness 采样混入已收口的 claims task。
  - 复核 `~/Documents/keys/aliyun_ecs.txt` 后，直接从控制机重采当前 live `public_testnet` 入口，确认以下公网面仍返回 `ok=true`：
    - `http://39.104.204.172:6631/v1/chain/status`
    - `http://39.104.205.67:6632/v1/chain/explorer/overview`
    - `http://39.104.204.172:6681/`
  - 同时发现 repo 里已有的更乐观 lane 真值已经过期：当前 sequencer status 面明确报 `execution driver missing predecessor record for non-contiguous committed height`，且 `committed_height/network_committed_height` 不再可直接作为健康 truth。
  - 用 `./scripts/p2p-real-env-triad-snapshot.sh` 对当前 testnet 三节点做 same-window snapshot，产物位于 `.tmp/p2p_testnet_reality/20260522-100229/`；summary 给出：
    - `claim_status=partial_with_local_validator_blocker`
    - `local_node` 当前 `network_tier=null`
    - 本机 `oasis7-testnet-observer.service` 仍保留三 validator / 三 signer 合同，并因 `fetch-commit authorization failed` 卡在 `committed_height=0`
    - 两台 ECS 则已收口到两 validator manifest；sequencer 还额外带 `execution driver missing predecessor record` 运行态错误
  - 进一步核对 `/opt/oasis7/p2p-testnet-local/logs/last-command.sh` 与 `/opt/oasis7/p2p-testnet-local/bin/start-node.sh` 后，确认本机 observer 仍走不带 `--network-tier-manifest` 的旧启动链路，这也是 `network_tier=null` 与 local/ECS validator contract 分叉的直接原因。
  - 为避免 readiness 真值继续散落在 dated evidence 与远端路径中，本轮新增以下 repo-owned 产物：
    - `doc/testing/evidence/public-testnet-live-candidate-bundle-2026-05-22.json`
    - `doc/testing/evidence/public-testnet-live-candidate-bootstrap-peers-2026-05-22.txt`
    - `doc/testing/evidence/public-testnet-live-candidate-manifest-2026-05-22.json`
    - `doc/testing/evidence/public-testnet-ecs-freshness-audit-2026-05-22.md`
    - `doc/testing/evidence/public-testnet-live-candidate-lanes-2026-05-22.tsv`
  - 新 lanes TSV 已按当前 fresh truth 收紧：
    - `public_rpc_ready=partial`
    - `explorer_public_ready=partial`
    - `faucet_guard_ready=partial`
    - `runtime_bootstrap=block`
    - 其余 `reset_policy_announced=pass`、`claims_boundary_review=pass`、`shared_devnet_pass=partial`
  - 同步回写 `doc/p2p/blockchain/p2p-formal-network-tiers-testnet-mechanism-2026-05-14.project.md`，把“current repo truth 已有 live endpoints，但 current live runtime/config drift 阻断 readiness”固定成单一口径。
- 遗留事项:
  - 当前还不能把这个 task 延伸成线上修复收口，因为 live runtime 问题已分成两层：
    - 本机 observer 需要先对齐到两 validator + formal manifest 的现网合同
    - ECS sequencer 需要清掉 predecessor-gap runtime error
  - 这两步若继续推进，很可能涉及线上 config 覆盖、service restart，甚至进一步冷重置；本轮先把 freshest repo truth 固化，避免在未审慎处理 live runtime 前继续沿用过期 `pass` 口径。
