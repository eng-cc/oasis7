# task_190808f5488348558aaf7fe3778fac6d Execution Log

- task_uid: task_190808f5488348558aaf7fe3778fac6d
- title: Reduce next viewer compile warnings
- owner_role: viewer_engineer
- worktree_hint: /home/scc/worktrees/oasis7-engineering-workspace-compile-followup

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-16 23:21:16 CST / viewer_engineer
- 完成内容:
  - 为保持前两轮 compile cleanup 已恢复的 workspace buildability，本任务复用当前未提交的 worktree `/home/scc/worktrees/oasis7-engineering-workspace-compile-followup`，在其上新建 `task_190808f5488348558aaf7fe3778fac6d`，继续清理 `oasis7_viewer` 剩余 warning。
  - 将 `egui_right_panel_player_guide/mod.rs` 中仅供测试使用的 minimap / mission HUD helper re-export 收紧到 `#[cfg(test)]`，保留运行态仍在使用的 HUD 入口。
  - 为 `app_bootstrap.rs`、`web_test_api/mod.rs` 中只在 wasm 或 tests 里消费的 locale/control helper 增加条件化 `dead_code` 注解，避免 native `check` 对跨目标辅助函数误报。
  - 将 `egui_right_panel_player_experience.rs` 的测试包装函数收进 `#[cfg(test)]`，并为 `egui_right_panel_player_experience_hud.rs` 中仅供测试消费的 stuck-hint helper 标记 `#[cfg_attr(not(test), allow(dead_code))]`。
  - 为 `diagnosis.rs`、`event_click_list.rs`、`selection_linking.rs`、`timeline_controls.rs`、`world_overlay.rs`、`ui_locale_text.rs`、`i18n.rs`、`tests_ui_text.rs` 中当前有意保留但尚未接线的 viewer 诊断/事件联动/时间线/覆盖层 UI stub 与测试文案 helper 补最小 `dead_code` 注解，不改运行逻辑。
  - 为 `industry_graph_view_model.rs` 的 test-only owned accessors 标记 `#[cfg_attr(not(test), allow(dead_code))]`，消除仅在测试里消费的 owned graph/routes wrapper warning。
- 验证结果:
  - `env -u RUSTC_WRAPPER cargo fmt --all`
  - `./scripts/cargo-dev.sh check --workspace --all-targets` 通过
  - `rg -n "warning:|generated [0-9]+ warnings|error:" /tmp/oasis7-workspace-followup.log` 无输出，说明本轮结束后 workspace 编译 warning 已归零
- 遗留事项:
  - 当前 compile warning 清理链路已完成；后续若再出现新 warning，应以新的 warning 来源为准另开 task，不在本 task 内继续扩 scope
