# task_fb1794ff54184cb3984d09cc61799b30 Execution Log

- task_uid: task_fb1794ff54184cb3984d09cc61799b30
- title: add remote_https provider transport for runtime
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-runtime-remote-provider-transport-remote-https

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-16 23:59:00 CST / runtime_engineer
- 完成内容: 新增 provider transport 最小实现切片，保留 `loopback_http` 默认兼容，同时为 runtime/viewer live/game launcher/client launcher 增加 `remote_https` transport 透传与 URL policy 校验；同步回写 `testing-manual.md`、`doc/core/prd.md`、`doc/world-runtime/project.md` 的迁移口径。
- 完成内容: `env -u RUSTC_WRAPPER cargo check -p oasis7 -p oasis7_client_launcher` 已通过。
- 遗留事项: 仍需串行补齐新增定向测试的稳定执行记录，并决定是否继续把 native launcher 的 provider probe 从“仅 loopback”扩展到远程 HTTPS 检查。

## 2026-05-17 12:54:05 CST / runtime_engineer
- 完成内容: 收口 `remote_https` transport 一版实现，补齐 `ProviderLoopbackHttpClient::new_with_transport(...)` / `ProviderLoopbackAdapter::new_with_transport(...)`，让 runtime live、`oasis7_game_launcher`、`oasis7_client_launcher` 都能按 transport 透传 provider URL、auth token 与 timeout，并继续保留 `loopback_http` 兼容 alias。
- 完成内容: 补齐定向回归并通过：`env -u RUSTC_WRAPPER cargo test -p oasis7 provider_http_client_accepts_remote_https_base_url -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 provider_http_client_rejects_remote_http_for_remote_https_transport -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 provider_settings_from_env_accepts_remote_https_transport -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 parse_options_accepts_remote_https_provider_transport -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher build_launcher_args_accepts_remote_https_provider_transport -- --nocapture`、`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_game_launcher --bin oasis7_viewer_live`、`env -u RUSTC_WRAPPER cargo check -p oasis7_client_launcher`、`./scripts/doc-governance-check.sh`、`git diff --check`。
- 遗留事项: 本轮只落了 transport/runtime contract 与本地定向回归，尚未接真实云上 `https://...` bridge 做端到端 smoke；远端部署的鉴权、证书、超时与可用性策略仍需要后续实机验证。

## 2026-05-17 15:24:00 CST / runtime_engineer
- 完成内容: 为 `remote_https` provider 增加 repo-owned 云端参考装配：新增 `scripts/provider-remote-https/letai_provider_cli.py`，把 OpenAI-compatible `POST /v1/chat/completions` 上游包装成 `oasis7_provider_local_bridge` 兼容的 provider CLI 输出；同步新增 `start-remote-provider-bridge.sh`、`remote-provider-bridge.env.example`、`oasis7-remote-provider-bridge.service`、`t2t.oasis7.tech.nginx.conf` 与 `doc/world-runtime/runtime/provider-remote-https-bridge-operator-runbook.md`。
- 完成内容: 已执行本地验证：`bash -n scripts/provider-remote-https/start-remote-provider-bridge.sh`、`python3 -c "import ast,pathlib; ast.parse(pathlib.Path('scripts/provider-remote-https/letai_provider_cli.py').read_text())"`、wrapper mock smoke、`env -u RUSTC_WRAPPER cargo build -p oasis7 --release --bin oasis7_provider_local_bridge`、`./scripts/doc-governance-check.sh`、`git diff --check`，均通过。
- 完成内容: 已预铺 `39.104.205.67`：安装 `nginx`，上传证书到 `/etc/nginx/ssl/t2t.oasis7.tech.{pem,key}`，上传 release 到 `/opt/oasis7/remote-provider-bridge/releases/20260517-152249`，创建 `current` 符号链接，放置 `/etc/systemd/system/oasis7-remote-provider-bridge.service`、`/etc/oasis7/remote-provider-bridge.env.example` 与 `/etc/nginx/sites-available/t2t.oasis7.tech.conf`，并执行 `systemctl daemon-reload`。
- 遗留事项: ECS 端尚缺真实 `OASIS7_REMOTE_LLM_API_KEY` 与 `OASIS7_REMOTE_LLM_MODEL`，因此服务尚未生成正式 `/etc/oasis7/remote-provider-bridge.env`，也未 enable/start systemd unit 或启用 nginx site；另外公网安全组 `443` 开放状态仍需 operator 在阿里云控制台确认。

## 2026-05-17 18:10:15 CST / runtime_engineer
- 完成内容: 按用户要求继续扩展为“每用户一个 token_key”：`oasis7_provider_local_bridge` 新增 `--auth-route-map <path>`，支持 `bridge bearer token -> route label`；`letai_provider_cli.py` 新增 `OASIS7_REMOTE_LLM_ROUTE_LABEL` + `OASIS7_REMOTE_LLM_ROUTES_PATH`，支持 `route label -> {api_key, model, base_url}`。本地已完成 bash/python 语法检查、`cargo check -p oasis7 --bin oasis7_provider_local_bridge` 与 per-route wrapper mock smoke。
- 完成内容: ECS 已切到新 release `/opt/oasis7/remote-provider-bridge/releases/20260517-180820`，并预铺 `/etc/oasis7/remote-provider-auth-routes.json.example` 与 `/etc/oasis7/remote-provider-llm-routes.json.example` 两份多用户模板。
- 完成内容: 当前单用户 smoke 已确认 `https://t2t.oasis7.tech/v1/provider/info` 可达，`oasis7-remote-provider-bridge.service` 与 `nginx.service` 均为 `active`。
- 遗留事项: 真实 LetAI 推理凭证目前仍未跑通，现有 `pk_...` 直接打 `POST /v1/chat/completions` 返回 `HTTP 403 error code: 1010`；多用户模式也尚未拿到实际“用户 -> bridge bearer token -> token_key”清单，因此现在只是具备 per-user 路由能力，尚未进入真实多用户运营态。

## 2026-05-17 18:13:11 CST / runtime_engineer
- 完成内容: 补齐 per-user operator 入口，新增 `scripts/provider-remote-https/manage-user-route.py`，支持 `upsert-user` 一次性维护 `bridge bearer token -> route label` 与 `route label -> {api_key, model, base_url}` 两份 JSON；默认沿用已有用户 token，必要时可 `--rotate-bridge-token` 轮换。
- 完成内容: 同步回写 `remote-provider-bridge.env.example` 与 operator runbook，明确推荐流程从“手改 JSON”切到“脚本签发用户 bridge token + 输出 curl 样例”。
- 遗留事项: ECS 端仍需真实用户级 LetAI `token_key` 才能把脚本产出的 route 正式写入 `/etc/oasis7/remote-provider-auth-routes.json` 与 `/etc/oasis7/remote-provider-llm-routes.json`；当前 `pk_...` 依旧无法作为已验证可用的推理凭证。

## 2026-05-17 18:24:00 CST / runtime_engineer
- 完成内容: 继续补齐“自动映射”能力：`oasis7_provider_local_bridge` 新增 `--auth-route-from-bearer`，可把请求 Bearer token 直接作为 route label；`letai_provider_cli.py` 新增 `OASIS7_REMOTE_LLM_NEWAPI_BRIDGE_STATE_PATH`，可直接从 `oasis7_newapi_bridge_service` state 自动解析 `newapi_user_ref/bridge_user_id -> token_key`。
- 完成内容: 同步更新 `start-remote-provider-bridge.sh`、`remote-provider-bridge.env.example` 与 operator runbook，补充“client 直接传 `--agent-provider-auth-token <newapi_user_ref>`”的自动映射部署路径。
- 遗留事项: 自动映射模式仍依赖 `oasis7_newapi_bridge_service` state 中已经持久化真实 `token_key`；当前提供的 `pk_...` 仍未通过上游推理验证，因此还不能宣称 LetAI 端到端已跑通。

## 2026-05-17 18:44:00 CST / runtime_engineer
- 完成内容: 已将 ECS `39.104.205.67` 当前 release 切到自动映射模式：更新远端 `oasis7_provider_local_bridge` 二进制与 wrapper，写入 `/etc/oasis7/newapi-bridge/bridge-state.json` 最小 state，并把 `/etc/oasis7/remote-provider-bridge.env` 改为 `OASIS7_PROVIDER_AUTH_ROUTE_FROM_BEARER=true + OASIS7_REMOTE_LLM_NEWAPI_BRIDGE_STATE_PATH=/etc/oasis7/newapi-bridge/bridge-state.json`。
- 完成内容: 兼容迁移方式已验证：继续使用现有 bearer `17d0bc749f79176513da1041b377fe1648179395de5c545f` 访问 `https://t2t.oasis7.tech/v1/provider/info` 仍然成功；同时本机 `bridge_user_id:bridge-user-000001` 也能通过自动映射命中 provider info。
- 遗留事项: 当前 ECS 上的自动映射 state 只是最小引导态，里面仍使用 placeholder `newapi_user_ref` 与用户提供的 `pk_...`；由于该 LetAI 凭证对 `POST /v1/chat/completions` 仍返回 `HTTP 403 error code: 1010`，所以线上只证明了“自动映射到 bridge 合同”已生效，尚未完成可用推理 smoke。

## 2026-05-17 18:55:00 CST / runtime_engineer
- 完成内容: 继续把“管理员 key 动态创建推理 token_key”收成代码真值：`oasis7_newapi_bridge_service` 的 `bind_user(...)` 现在在配置了 LetAI OpenAPI 管理口后，会立即执行 `upsert user + ensure project token`，并把 `platform_user_id/platform_project_id/token_key/token_status` 写入共享 state；新增定向测试 `bind_user_eagerly_provisions_platform_user_project_and_token_when_letai_is_configured` 已通过。
- 完成内容: 新增 repo-owned 部署资产 `scripts/newapi-bridge-service/*` 与 `doc/world-runtime/runtime/newapi-bridge-service-operator-runbook.md`，补齐 `oasis7_newapi_bridge_service` 的启动脚本、env 模板、systemd 模板与最小 operator 路径。
- 遗留事项: 还需把新版 `oasis7_newapi_bridge_service` 二进制与 systemd/env 实际部署到 ECS，并用真实管理员 key 对 `POST /v1/bridge/bind` 做一次 live 绑定验证，确认 LetAI 管理口会返回可用 `token_key`。

## 2026-05-17 19:07:20 CST / runtime_engineer
- 完成内容: 已将 `oasis7_newapi_bridge_service` release 二进制与 systemd/env 部署到 ECS `39.104.205.67`，服务运行于 `127.0.0.1:5852`，共享 state 路径为 `/etc/oasis7/newapi-bridge/bridge-state.json`；`GET /v1/bridge/health` 已通过。
- 完成内容: live `bind` 现已确认能通过管理员 key 打通 LetAI `POST /api/platform/open/users/upsert`，并把 `platform_user_id=224` 回写到本地 state；这证明“管理员 key 只做管理接口、provider bridge 读共享 state”链路前半段成立。
- 遗留事项: live `bind` 随后卡在 LetAI project/token 管理口。当前代码与多组候选路径对 `https://api.letai.run` 的探测都返回 `HTTP 404 Invalid URL`，因此真实阻塞点已收敛为“当前 base URL 下缺少可用的 project/token ensure endpoint 路径或该接口不在此域名暴露”；在拿到正确的 LetAI 管理 endpoint 规范前，无法继续自动生成真实推理 `token_key`。

## 2026-05-17 19:49:51 CST / runtime_engineer
- 完成内容: 根据飞书文档第 16/17/18 节把 LetAI OpenAPI 契约补正到代码与测试：`projects/ensure-token -> POST /api/platform/open/users/{platform_user_id}/projects/upsert`、`project_name -> external_project_name`、project summary 改为 `GET /api/platform/open/projects/{platform_project_id}/summary`、project logs 改为 `GET /api/platform/open/projects/{platform_project_id}/logs`，同时兼容 `success: true/false` 响应与数值型 `token_status`。定向回归 `bind_user_eagerly_provisions_platform_user_project_and_token_when_letai_is_configured`、`reconcile_provisions_letai_user_project_token_and_marks_reconciled` 已通过。
- 完成内容: ECS `39.104.205.67` 已切到新 release `/opt/oasis7/newapi-bridge/releases/20260517-193926`，`oasis7-newapi-bridge.service` 重启后重新对现有 bootstrap 用户执行 `POST /v1/bridge/bind`，共享 state 已成功补正为 `platform_user_id=224`、`platform_project_id=784`、动态 `token_key=wIcX9x...`、`token_status=1`；这证明“管理员 key 动态创建 per-user project token_key -> provider bridge 自动映射读取 state”主链路已真正跑通。
- 完成内容: 继续对 provider bridge 做 live smoke：`/etc/oasis7/remote-provider-bridge.env` 已确认开启 `OASIS7_PROVIDER_AUTH_ROUTE_FROM_BEARER=true` 与 `OASIS7_REMOTE_LLM_NEWAPI_BRIDGE_STATE_PATH=/etc/oasis7/newapi-bridge/bridge-state.json`；带 `Authorization: Bearer 17d0bc749f79176513da1041b377fe1648179395de5c545f` 的 `GET /v1/provider/info` 返回 `200`。
- 遗留事项: 实际推理仍被 LetAI 上游拒绝。直接使用刚生成的动态 `token_key` 请求 `https://api.letai.run/v1/models` 与 `POST /v1/chat/completions` 均返回 `HTTP 403`，错误为 `分组 cc 已被弃用`；当前远端 `OASIS7_NEWAPI_BRIDGE_LETAI_PARENT_CHANNEL_ID` 尚未配置，且你之前明确说过会提供父级渠道。因此下一步需要用户提供可用 `parent_channel_id`，写入 `/etc/oasis7/newapi-bridge-service.env` 后重启 `oasis7-newapi-bridge.service`，并为现有 bootstrap 绑定重新生成新的 project/token（旧的 `platform_project_id=784` 与 `token_key` 属于废弃 `cc` 分组，不宜继续复用）。

## 2026-05-18 13:20:00 CST / runtime_engineer
- 完成内容: 复测 LetAI live 状态后确认平台侧已放开当前 `cc` 组模型路由：正式 bootstrap token `wIcX9x...` 与新 probe token `vUgu5Y...` 现在都能通过 `GET https://api.letai.run/v1/models` 枚举可用模型，返回至少 `gpt-5.2`、`gpt-5.3-codex`、`gpt-5.4`、`gpt-5.4-mini`、`gpt-5.5`、`gpt-5-codex`、`gpt-image-2`；直接 `POST /v1/chat/completions` 也已恢复为 `HTTP 200`。
- 完成内容: 继续收口 provider bridge 侧真实阻塞并修复：定位到 `scripts/provider-remote-https/letai_provider_cli.py` 通过 Python `urllib` 直连 LetAI 时会因默认 `User-Agent` 被上游拦成 `HTTP 403 error code: 1010`，而同 payload 用 `curl` 返回 `200`。已补丁为显式发送 `User-Agent: curl/8.5.0`，并改为 `stream=true` + 解析 LetAI SSE `data:` chunk 流，成功在 ECS 远端直接执行 wrapper 返回 `{\"payloads\":[{\"text\":\"OK\"}],...}`。
- 完成内容: ECS `oasis7-remote-provider-bridge.service` 已同步更新上述 wrapper 并重启；live 手工 wrapper 调用现已可用。
- 遗留事项: provider bridge 的最终 `decision` live smoke 仍未完全通过，但失败签名已从“分组/渠道不可用”收缩为“用户额度不足”：LetAI 返回 `insufficient_user_quota`，提示 `用户剩余额度: $0.005588，需要预扣费额度: $0.005642`。因此当前剩余阻塞已不再是分组或 token，而是需要给 `platform_user_id=224` 再补一点额度后重试。

## 2026-05-18 22:02:00 CST / runtime_engineer
- 完成内容: 针对 PR `#237` 的 GitHub `required-gate` 失败执行 CI 修复，确认唯一失败项为 `./scripts/check-rust-file-size.sh` 报告 `crates/oasis7/src/viewer/runtime_live/tests.rs` 达到 `1207` 行，超过仓库 `1200` 行上限。
- 完成内容: 采用现有 `runtime_live/tests/*` 子模块组织方式，把 provider settings 环境解析测试拆到新增文件 `crates/oasis7/src/viewer/runtime_live/tests/provider_settings.rs`，并把 `removed_old_brand_runtime_live_env(...)` helper 下沉到 `tests_support.rs` 复用；主测试文件现已降到 `1111` 行。
- 完成内容: 已执行并通过 `./scripts/check-rust-file-size.sh` 与 `env -u RUSTC_WRAPPER cargo test -p oasis7 provider_settings_from_env --lib`，确认本次 CI 修复既过行数门禁也保留原测试行为。
- 遗留事项: 仍需把本次修复 commit/push 到 `task/world-runtime-remote-provider-transport-remote-https`，并等待 GitHub PR `#237` 重新跑绿。
