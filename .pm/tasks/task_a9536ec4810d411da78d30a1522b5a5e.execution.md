# task_a9536ec4810d411da78d30a1522b5a5e Execution Log

- task_uid: task_a9536ec4810d411da78d30a1522b5a5e
- title: make live triad a three equal validator topology
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-triad-three-equal-validator-topology

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-29 22:06:20 CST / runtime_engineer
- 完成内容: 在 task worktree 内补齐 `doc/p2p/prd.md` / `doc/p2p/project.md` 的 `PRD-P2P-026`、`AC-35` 与 task trace；将两台 ECS 节点的 `NODE_ROLE` 都改成 `sequencer`，统一三节点 validator set / signer binding；修复 `triad-sequencer-a` 的 stale execution-world 启动问题并确认其已稳定推进到新高度；对 `triad-storage-b` 轮换旧 `execution-world`、`execution-records`、`output/node-distfs/triad-storage-b` 与 `output/chain-runtime/triad-storage-b` 残留，使其从 “writer switch must start at sequence 1” / stale execution-bridge-state 卡死恢复为可继续回放追高；同步更新 `scripts/p2p-real-env-triad-snapshot.sh` 与 `testing-manual.md`，让 triad snapshot claim 改为按实测 runtime role 自动区分 `observer_mixed_topology` 与 `three_equal_validator`，不再把历史 service label 当成角色真值。
- 遗留事项: 本机 `/opt/oasis7/p2p-triad-local` 仍需 root 权限才能把 `triad-observer-local` 从 `observer` 切到第三个 `sequencer`，并同步轮换本机旧 `execution-world` / `execution-records` / `output/node-distfs` / `output/chain-runtime` 状态；`triad-storage-b` 仍在从历史高度回放追高，待云端稳定后需要完成本机切换并跑 same-window triad snapshot / evidence / closeout。

## 2026-04-30 09:13:18 CST / runtime_engineer
- 完成内容: 使用本机 sudo 密码完成本地 `triad-observer-local` 切换到 `sequencer`，并先后对本地与 `triad-storage-b` 做全量冷种；一度把本地与 `triad-sequencer-a` 拉齐到 `74559+` 且 `last_error=null`，随后运行 `.tmp/p2p_real_env_triad/20260429-235934/summary.md`，脚本层面已能识别 `claim_mode=three_equal_validator`。继续迭代后，`triad-storage-b` 通过以 `triad-sequencer-a` 当前冷种包为真值的第二轮全量冷种，已经恢复到 `75768/75768` 且 `last_error=null`；`triad-sequencer-a` 也保持 `75783/75783` 且 `last_error=null`。最终收敛出的唯一 live blocker 落在本地 `triad-observer-local`: 每次把它并回 live triad，都会在 `gap sync height 75774` 附近触发 `persisted commit hash mismatch`，并在此前一次实验中伴随云端看到 `replication writer switch must start at sequence 1`。
- 遗留事项: 当前安全态是两台云节点健康、 本地节点停机：`triad-sequencer-a`=`75783/75783,last_error=null`，`triad-storage-b`=`75768/75768,last_error=null`，本地 `oasis7-triad-observer.service` 已停止以避免继续制造分叉。下次继续时应以 `triad-sequencer-a` 当前冷种包为真值，重点排查本地新 `sequencer` 首次写入前是否需要清空/重建 `node-distfs` writer-switch 相关持久态（尤其是导致 `sequence 1` 缺失与 `persisted commit hash mismatch` 的那组文件），否则无法把 live triad 稳定收口到真正的三节点对等写链。

## 2026-04-30 09:54:41 CST / runtime_engineer
- 完成内容: 重新对本地与两台云节点做 same-window 状态复核。当前 `triad-sequencer-a` 仍是唯一健康真值节点，`STATUS_BIND=127.0.0.1:5631` 上报 `latest_height=75858`、`committed_height=75858`、`last_error=null`，其本地 `node_pos_state` 与 `reward-runtime-execution-bridge-state` 均对齐到 `75858`，writer state 为 `writer_epoch=1777468623760,last_sequence=1415,last_replicated_height=75859`。本地 `triad-observer-local` 仍停机，`systemctl status` 显示服务在 `2026-04-30 09:16:51 CST` 停止；其持久态仍卡在 `node_pos_state.committed_height=75773`、`reward-runtime-execution-bridge-state.last_applied_committed_height=75774`，并保留本地 writer `writer_epoch=1777511257174,last_sequence=7,last_replicated_height=75773`；日志尾部再次确认最后失败签名仍是 `gap sync height 75774 persisted commit hash mismatch expected=64dce005...`。`triad-storage-b` 也不再健康：`STATUS_BIND=127.0.0.1:5632` 上报 `latest_height=75772`、`network_committed_height=75801`，`last_error=node replication error: gap sync height 75773 ... persisted commit hash mismatch expected=b61bdf...`；其持久态保留本地 writer `writer_epoch=1777511485450,last_sequence=5,last_replicated_height=75772`。
- 遗留事项: 截至本次复核，三节点并未成功收口到对等写链。现在是 1 个健康 sequencer (`triad-sequencer-a`) + 2 个带本地 writer 残留且会在追远端高度时触发 persisted commit hash mismatch 的节点。本地问题不是“服务没拉起来”这么简单，而是写入过本地提交后与 `seq-a` 的同高提交执行结果分叉；`storage-b` 已出现同类症状，后续应转向代码级定位/修复 writer-local execution divergence，而不是继续重复 live 重启。

## 2026-04-30 10:18:54 CST / runtime_engineer
- 完成内容: 在 `crates/oasis7/src/bin/oasis7_chain_runtime/execution_bridge/driver.rs` 修复 execution bridge 对 committed-height gap 的错误处理：不再在 `incoming > last_applied + 1` 时“继续带 gap 执行”，而是强制先把 execution head 恢复到 `incoming-1` 的精确 record；若缺少前置 record，则直接返回错误，避免在缺块状态上继续产出新的 execution hash。新增/更新 `crates/oasis7/src/bin/oasis7_chain_runtime/execution_bridge/tests/driver.rs` 回归测试两组：1) 缺少前置 record 时拒绝 gap commit；2) 先回退到旧高度、再直接收到更高高度时，会先恢复 predecessor，再复算出与原始高度完全一致的 execution hash / state root。已执行 `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime node_runtime_execution_driver_restores_predecessor_before_gap_commit` 与 `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime node_runtime_execution_driver_`，均通过。
- 遗留事项: 这次只完成了代码级护栏与回归测试，尚未把新二进制重部署到本地 / ECS 三节点并重跑 same-window triad snapshot；live triad 仍停留在 `1 healthy + 2 divergent/stopped` 的旧运行态，需要下一步编译、发布、冷种/回放后再验证是否真正消除 `persisted commit hash mismatch`。

## 2026-05-05 22:48:31 CST / runtime_engineer
- 完成内容: 在 task worktree 内继续针对 live triad 做代码级修复并完成两轮真实环境回放。第一轮定位到 `broadcast_local_replication()` 会在任意节点进入 `Committed` 后都以本地 `node_id` 落盘同高度 replication commit，导致同一块在不同 validator 上生成不同 `content_hash`；已在 `crates/oasis7_node/src/node_engine_replication.rs` 增加“只有该 slot 的期望 proposer 才允许持久化 replication commit”的护栏，并在 `crates/oasis7_node/src/tests_storage_replication.rs` 增加 `non_proposer_committed_decision_does_not_persist_local_replication` 回归测试。第二轮定位到冷启动时 `seeded_writer_epoch()` 仅以毫秒时间戳做 seed，三节点近同时启动会拿到相同 `writer_epoch`，从而在本地追远端提交时触发 `replication writer conflict requires writer_epoch advance`；已在 `crates/oasis7_node/src/replication.rs` 将初始 writer epoch 改为“当前毫秒 + writer 公钥派生 nonce”，并在 `crates/oasis7_node/src/replication/tests.rs` 增加 `seeded_writer_epoch_from_millis_distinguishes_writers` 回归测试。已验证测试包括：`env -u RUSTC_WRAPPER cargo test -p oasis7_node seeded_writer_epoch_from_millis_distinguishes_writers`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node non_proposer_committed_decision_does_not_persist_local_replication`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node runtime_restart_reconciles_stale_pos_state_from_persisted_replication_height`。两次将新版 `oasis7_chain_runtime` 编译为 release 并分别发布到 `/opt/oasis7/p2p-triad-local/releases/3f43ef911-synced-replay-guard-20260505-211605/bin/oasis7_chain_runtime` 与两台 ECS 对应 release，最新三端一致 `sha256=fa67f6d7a6f795a09e04b08d2a6955b7066032130697b017ed60fb0db1f9232b`。真实环境方面：1) `20260505-222014` 采样窗口先证实三端 `replication_commit_messages` 已收敛掉“同高多份 content_hash”问题，但本地随后暴露 writer-epoch 冲突；2) 应用 writer-epoch seed 修复并再次冷重置后，三节点在短窗口内能从 `5/5` 一起推进到 `11/12` 且 `last_error=null`，说明两层新护栏都在起效。
- 遗留事项: live triad 仍未完成 same-window 收口。最新正式采样产物 `.tmp/p2p_real_env_triad/20260505-223851/summary.md` 仍判定 `claim_status=partial_with_local_validator_blocker`：采样期内本地 `triad-observer-local` 停在 `15/15`，云端 `triad-sequencer-a` 与 `triad-storage-b` 前进到 `17/17`；随后继续观测时，本地复现 `node replication error: gap sync height 25 persisted commit hash mismatch expected=7bd76acb6189362cbc5c3b97b99094f29e08f64ef3a864f050b27a65f880234c`，而 `triad-storage-b` 也出现 `node execution error: execution driver missing predecessor record for non-contiguous committed height: last_applied=23 incoming=25 predecessor=24` / `incoming=26 predecessor=25`。下一步应转向 height `24-26` 的 commit 热/冷索引、gap-sync 真值来源与 storage blob/provider 路径继续定位，而不是再重复冷重置。

## 2026-05-06 10:01:51 CST / runtime_engineer
- 完成内容: 继续收口 fixed-genesis 恢复路径，定位 `restore_state_snapshot()` 在固定 `POS_SLOT_CLOCK_GENESIS_UNIX_MS` 下会无条件保留超前持久态 `next_slot/last_observed_slot/last_observed_tick`，导致重启后三节点继续运行在“未来 slot”。已在 `crates/oasis7_node/src/pos_state_store.rs` 增加“固定 genesis + 启动时当前墙钟”裁剪逻辑，并在 `crates/oasis7_node/src/lib.rs` 的启动恢复入口传入 `now_unix_ms()`；新增回归 `restore_state_snapshot_clamps_future_clock_state_when_fixed_genesis_is_configured`，并复跑 `runtime_restart_reconciles_stale_pos_state_from_persisted_replication_height`、`runtime_pos_state_persists_across_restart`、`pos_engine_observed_slot_does_not_backtrack_on_clock_rewind` 与 `env -u RUSTC_WRAPPER cargo check -p oasis7_node`，均通过。随后构建 release binary `sha256=9722c25ab2a58af17a439d4baf7872713dc5ddbc3fb08300ff84bbf96c78c26f`，发布到本机 `/opt/oasis7/p2p-triad-local/releases/3f43ef911-clock-restore-clamp-20260505-232617` 与两台 ECS `/opt/oasis7/p2p-triad/releases/3f43ef911-clock-restore-clamp-20260505-232617`。第一次带新 binary 重启后观测到三端仍分别停在 `0/1/1022`，确认 live blocker 已从时钟漂移转为旧 execution/replication 历史态分裂；因此执行保留配置的协调冷重置，把三端旧 `execution-world`、`execution-records`、`storage`、对应 `output/node-distfs` 与 `output/chain-runtime` 转存到 `backups/20260505-234000-cold-reset*`。冷重置后正式复跑 `P2PARCH6_SEQ_SSH_PASSWORD='***' P2PARCH6_STORAGE_SSH_PASSWORD='***' ./scripts/p2p-real-env-triad-snapshot.sh --samples 4 --interval-secs 5 --out-dir .tmp/p2p_real_env_triad`，产物 `.tmp/p2p_real_env_triad/20260506-100041/summary.md` / `summary.json` 已给出 `claim_status=pass_candidate`、`claim_mode=three_equal_validator`、`failure_signatures=(none)`；同窗样本里 `triad-observer-local` 从 `6 -> 8`、`triad-sequencer-a` 从 `7 -> 9`、`triad-storage-b` 从 `7 -> 9`，三节点 `role=sequencer`、`service=active`、`last_error=null`。
- 遗留事项: 本任务的 live topology / same-window 目标已达成；若后续要保留旧历史链状态并继续做无冷重置升级，需要另外针对 execution/replication 历史迁移与 blob 可用性开新问题，不应再把它混入本任务的对等 validator 拓扑验收。
