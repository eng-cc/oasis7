# task_d454d71480fa432d8d796e80791126a6 Execution Log

- task_uid: task_d454d71480fa432d8d796e80791126a6
- title: Wire live reachability evidence into P2P auto mode recommender
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-p2parch-live-reachability-recommender

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-06 22:44:47 CST / runtime_engineer
- 完成内容: 为 `oasis7_net` 新增 live reachability snapshot substrate，记录 relay reservation、DCUtR hole-punch success/failure 与 active transport path kind；`oasis7_node` / chain runtime 已可按请求时 live snapshot 重算用户层 P2P recommendation，并在 CLI 未显式覆盖对应探测字段时把 live relay/hole-punch/path evidence 合并进 status payload 的 detection / recommendation / effective policy 输出。
- 完成内容: 补齐 CLI explicit-hint 标记位、runtime/status helper 模块与对应单测；修复 `Libp2pReplicationNetwork` 句柄转型、`libp2p_net` 测试 import/可见性回归与相关 warning。
- 遗留事项: 完整 AutoNAT / port reachability probe 仍未接入 swarm，当前 live fallback 仅覆盖 relay reservation、hole-punch 结果与 active path evidence；`viewer_engineer` 后续仍需消费 status payload，落地推荐说明与 `公网入口` 显式确认 UX。

## 2026-04-06 23:00:18 CST / runtime_engineer
- 完成内容: 根据独立 review 结论修正 live inference 过强的问题：DCUtR 失败不再直接推断本地 `hole_punch_viability=blocked`；默认 CLI detection 改为无证据保守态，`relay_available` 改为由 live relay evidence 明确提升，`probe_stable` 改为只接受 reservation/active-path/成功打洞等稳定证据；新增对应回归单测。
- 遗留事项: `relay_reservation_active` 仍可能因上游 libp2p client event 面缺少显式失效事件而偏粘滞；`/v1/chain/status` 当前输出的是“基于实时 snapshot 的当前推荐态”，并不代表 runtime 在进程内已热切换运行策略，这一语义差异暂保留并记录为后续 contract 明确项。
