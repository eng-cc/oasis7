# task_7fc83a5053f84a8a83624ebebcb719c5 Execution Log

- task_uid: task_7fc83a5053f84a8a83624ebebcb719c5
- title: 彻底清理 OpenClaw public/structural naming 残留
- owner_role: producer_system_designer
- worktree_hint: /home/scc/worktrees/oasis7-core-openclaw-name-purge

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-07 18:01:05 CST / producer_system_designer
- 完成内容: 继续收口活跃结构层 naming，将 canonical provider implementation 固定为 `provider_loopback_http`，并把 repo-owned bridge / parity bench / setup script / runtime workspace / runtime agent 默认入口切到 `oasis7_provider_*`；`openclaw_local_http` 与 `OPENCLAW_OASIS7_*` 仅保留兼容解析，不再作为 operator-facing 当前真值。
- 完成内容: 同步更新 `oasis7` skill 的主文档、references、wrapper/self-test、parity/help 文案与 `software_safe` 提示，使当前默认命名统一指向 `provider_loopback_http` / `oasis7_provider_local_bridge` / `scripts/provider-parity-p0.sh` / `scripts/setup-provider-oasis7-runtime.sh` / `oasis7_provider_agent`，同时保留 OpenClaw 作为 backend 语义说明。
- 完成内容: 回补文档治理约束，将 `doc/world-simulator/project.md` 压回 1000 行门禁以内。
- 完成内容: 处理 commit 前独立 review finding，将 `doc/world-simulator/prd.md` 中仍指向旧 `setup-openclaw-oasis7-runtime` / `oasis7_openclaw_parity_bench` 的 current-truth 条目改回当前 canonical 名称，消除 PRD 与实现漂移。
- 验证: `env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_provider_local_bridge --bin oasis7_provider_parity_bench --bin oasis7_game_launcher -p oasis7_client_launcher`；`bash -n scripts/provider-parity-p0.sh`；`bash -n scripts/setup-provider-oasis7-runtime.sh`；`bash -n .agents/skills/oasis7/scripts/oasis7-run.sh`；`bash -n .agents/skills/oasis7/scripts/oasis7-run-bootstrap-test.sh`；`./.agents/skills/oasis7/scripts/oasis7-run-bootstrap-test.sh`；`./scripts/doc-governance-check.sh`；`git diff --check`。
- 遗留事项: 历史归档文档、历史评审记录与以 OpenClaw 为后端真值的语义描述仍保留旧命名；本任务不改写历史证据，只清理活跃结构层与当前 operator-facing 默认入口。
