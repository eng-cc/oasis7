# task_22a0d58d0d9445b1ad74127c25768d8d Execution Log

- task_uid: task_22a0d58d0d9445b1ad74127c25768d8d
- title: Implement Windows standard release installer
- owner_role: viewer_engineer
- worktree_hint: oasis7-world-simulator-broader-user-release-distribution-design

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-14 23:24:02 CST / viewer_engineer
- 完成内容: 将 Windows 公共 release 包从 7z SFX 自解压 `.exe` 收口为 NSIS 标准安装器实现。`scripts/build-game-launcher-bundle.sh` 现在会在 Windows bundle 中生成 `run-client.cmd`、`run-web-launcher.cmd`、`run-game.cmd`、`run-chain-runtime.cmd`；新增 `scripts/windows-release-installer.nsi` 负责 per-user 安装、开始菜单/桌面快捷方式与卸载入口；`scripts/package-native-installer.sh` 的 Windows 分支改为依赖 `makensis` 生成 installer；`.github/workflows/release-packages.yml` 的 Windows packaging 依赖也从 `7zip` 切到 `nsis`。
- 遗留事项: Windows 代码签名 / Publisher trust chain 仍未闭环；当前实现解决的是“标准安装器 + 可发现入口”，尚未解决普通用户首次安装的安全信任提示。
## 2026-04-14 23:31:06 CST / viewer_engineer
- 完成内容: 验证通过：`bash -n scripts/build-game-launcher-bundle.sh`、`bash -n scripts/package-native-installer.sh`、Windows bundle dry-run（确认生成 `run-client.cmd` / `run-game.cmd` 且平台判定为 `windows-x64`）、Windows installer dry-run（确认 `package-native-installer.sh` 走 `makensis` 而不是旧 SFX 路径）、`./scripts/doc-governance-check.sh`、`./scripts/pm/lint.sh`、`git diff --check` 全部通过；文档同步回写 `doc/world-simulator/{prd.md,project.md}`、`doc/world-simulator/launcher/game-client-launcher-broad-user-release-distribution-2026-04-14.project.md` 与 `doc/site/github-pages/github-pages-release-download-pipeline-2026-03-01.{prd,project}.md`。
- 遗留事项: 当前环境未发现 Windows/macOS 签名相关凭据或配置，`T1A`（Windows codesign）与 `T2`（macOS notarization）暂时不能在本地继续闭环；后续若继续推进普通用户发行路径，优先选择不依赖凭据的 `T3 Linux AppImage` 或先补齐签名 secret/runbook。
