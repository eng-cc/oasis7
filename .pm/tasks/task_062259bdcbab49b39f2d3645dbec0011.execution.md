# task_062259bdcbab49b39f2d3645dbec0011 Execution Log

- task_uid: task_062259bdcbab49b39f2d3645dbec0011
- title: repair release web dist freshness gating
- owner_role: viewer_engineer
- worktree_hint: /home/scc/worktrees/oasis7-viewer-release-web-dist-freshness

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-22 23:04:00 CST / viewer_engineer
- 完成内容: 复盘 GitHub Actions run `26294265688`，确认 `release-gate-web` 在 `viewer-software-safe-step-regression.sh --skip-llm-provider-preflight` 阶段报 `window.error: Uncaught RuntimeError: unreachable`，其余 `runtime-support`、`runtime-core`、`runtime-sync`、`soak` 均通过；定位 `scripts/agent-browser-lib.sh` 的 `resolve_viewer_static_dir_for_web_closure()` 仍依赖 mtime 认定 `crates/oasis7_viewer/dist` 是否新鲜，clean checkout 下会把内容已 stale 的 checked-in dist 误判为 fresh。已改为逐项比对 canonical dist 文件与 `pixel-world-bridge/` 生成目录内容，不一致时强制 rebuild 到隔离 `web-dist`；同步把 `scripts/agent-browser-viewer-dist-freshness-test.sh` 扩成“内容 stale 但 mtime 相同”的回归场景。
- 完成内容: 已验证 `bash -n scripts/agent-browser-lib.sh`、`bash -n scripts/agent-browser-viewer-dist-freshness-test.sh`、`./scripts/agent-browser-viewer-dist-freshness-test.sh` 通过。
- 遗留事项: 新 worktree 首次真实浏览器回归曾受本地环境影响而不稳定：初次缺少 `crates/oasis7_viewer/node_modules` 导致 `vite: not found`；补 `npm ci --prefix crates/oasis7_viewer` 后，完整 `viewer-software-safe-step-regression.sh` 仍在首次大编译/agent-browser 环境链路中消耗较长时间，当前未等待其最终绿灯后再进入 commit/PR。
