# task_c1149e15fef14f12925182a03f37e546 Execution Log

- task_uid: task_c1149e15fef14f12925182a03f37e546
- title: 链上 world 默认被动跟随 committed action，空轮询不推进
- owner_role: runtime_engineer
- worktree_hint: null

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-16 23:14:00 CST / runtime_engineer
- 完成内容: 更新 `doc/world-runtime/prd.md` / `doc/world-runtime/project.md`，新增 PRD-WORLD_RUNTIME-031，明确 chain-linked viewer runtime 默认被动跟随 `/v1/chain/status.consensus.committed_height` 对应 execution world，空轮询不计入逻辑 world progression。
- 完成内容: 为 `ViewerRuntimeLiveServerConfig` 增加 `chain_status_bind` 与 chain poll 配置；`oasis7_viewer_live` 新增 `--chain-status-bind` 入口，`oasis7_game_launcher --chain-enable` 现在会把 chain status 地址透传给 viewer。
- 完成内容: `runtime_live` 空闲循环新增 chain-linked follower，轮询 `/v1/chain/status` 并在 committed height 前进时重载 execution world；只有当新的 execution world 带来新增 logical time 或 event 时才向 viewer 发出 snapshot / events / metrics。
- 完成内容: 补充 runtime live / viewer live / launcher 定向回归，覆盖“不用显式 Play 也会跟 committed world 走”“空轮询不推进”“launcher 透传 chain status 参数”“viewer live CLI 接受 chain status 参数”。
- 遗留事项: 当前修复让 viewer 在 chain-linked 模式下被动跟随 committed execution world，但未把 `runtime_live` 的本地 gameplay action 提交链化；若后续要把 chain-linked 路径收成真正的链上交互主路径，还需要补通 viewer gameplay action -> chain runtime submit 的产品闭环。

## 2026-04-16 23:56:00 CST / runtime_engineer
- 完成内容: 为满足 Rust 1200 行治理门禁，将 `oasis7_game_launcher` 的 CLI/help 解析拆到 `cli.rs`，将 viewer live 启动参数拼装拆到 `viewer_live_command.rs`，并将 `runtime_live` 的 chain follower / control helper 拆到 `chain_link.rs` 与 `control_utils.rs`，把 `oasis7_game_launcher.rs` 压到 911 行、`runtime_live.rs` 压到 1108 行。
- 完成内容: 刷新 `doc/.governance/rust-oversized-file-baseline.tsv`，移除已降回阈值内的 `crates/oasis7/src/bin/oasis7_game_launcher.rs` frozen baseline 条目，并重新通过 `./scripts/check-rust-file-size.sh`。
- 验证结果: `env -u RUSTC_WRAPPER cargo test -p oasis7 chain_linked_runtime_ -- --nocapture`；`env -u RUSTC_WRAPPER cargo test -p oasis7 parse_options_reads_custom_values -- --nocapture`；`env -u RUSTC_WRAPPER cargo test -p oasis7 build_viewer_live_command_wires_llm_timeout_default_into_spawn_path -- --nocapture`；`./scripts/check-rust-file-size.sh`；`./scripts/doc-governance-check.sh`；`git diff --check`；`./scripts/pm/lint.sh`。
- 遗留事项: chain-linked 路径当前仍只解决 committed world 被动跟随，不包含 gameplay action 上链提交；如要让 viewer 在链模式下形成完整交互闭环，还需补 viewer gameplay action -> chain runtime submit。

## 2026-04-17 00:20:00 CST / runtime_engineer
- 完成内容: 复盘 PR #107 `required-gate` 失败日志，根因定位为 `viewer::runtime_live::tests::auth_actions::runtime_step_control_surfaces_runtime_failure_as_blocked_ack`；在拆分 `control_utils.rs` 时把 `Runtime(WorldError::AgentNotFound)` 的细粒度 viewer ack 映射粗化成了通用 `runtime_step_failed`，同时丢失了“restore the missing agent”专用 hint。
- 完成内容: 为 `runtime_control_error_details()` 补回 `RuntimeWorldError::AgentNotFound` 的专用错误码、消息与 hint，其余 runtime world failure 继续保持通用 `runtime_step_failed`。
- 验证结果: `env -u RUSTC_WRAPPER cargo test -p oasis7 runtime_step_control_surfaces_runtime_failure_as_blocked_ack -- --nocapture`。
- 遗留事项: 本条仅修复 CI 失败的 viewer ack 语义回退，不改变 chain-linked gameplay action 尚未上链提交的产品边界。
