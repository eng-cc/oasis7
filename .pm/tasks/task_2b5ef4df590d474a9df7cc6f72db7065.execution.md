# task_2b5ef4df590d474a9df7cc6f72db7065 Execution Log

- task_uid: task_2b5ef4df590d474a9df7cc6f72db7065
- title: Detect public testnet replication gap split brain
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-runtime-replication-gap-health

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
  - Action: ...
  - Validation Command: ...
  - Expected Result: ...
  - Actual Result: ...
  - Blocker / Next Action: ...
-->

## 2026-05-26 22:32:32 CST / runtime_engineer
- 完成内容: Added explicit replication convergence state to `NodeConsensusSnapshot`, including `replication_persisted_height`, `replication_gap_sync_blocked_height`, and `replication_gap_sync_blocked_reason`; gap sync now records the first missing commit height instead of silently leaving status without a recoverability signal.
- 完成内容: Chain runtime observability now treats blocked gap sync and committed-height-ahead-of-contiguous-replication as critical, sets `/v1/chain/status` top-level `ok=false`, and exposes the new replication fields in consensus and observability payloads.
- 遗留事项: Live three-node recovery still needs a fresh release build/deploy plus coordinated state repair or reseed verification; this entry only records the code-level detection fix and tests.
- Action: Implemented focused regression coverage for the public-testnet failure mode where a node sees a higher network commit but cannot fetch the next contiguous commit.
- Validation Command: `env -u RUSTC_WRAPPER cargo test -p oasis7_node network_gap_sync_not_found -- --nocapture`
- Expected Result: gap-sync not-found remains non-fatal but exposes the blocked missing height.
- Actual Result: passed; `runtime_network_replication_gap_sync_not_found_is_non_fatal` reports the missing-height state and keeps the node running.
- Validation Command: `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_tests -- --nocapture`
- Expected Result: status payload marks replication gap blocked as critical and unhealthy.
- Actual Result: passed; 4 observability tests passed, including `build_chain_status_payload_marks_replication_gap_blocked_unhealthy`.
- Validation Command: `cargo fmt --check`
- Expected Result: formatting is clean.
- Actual Result: passed.
- Blocker / Next Action: Continue with repo governance checks and, if green, build/deploy the updated runtime before live three-node recovery verification.

## 2026-05-26 23:01:38 CST / runtime_engineer
- 完成内容: Added startup publication for restored consensus snapshots after disk restore/reconcile, so `/v1/chain/status` reports the restored committed/execution heights before the next runtime tick mutates state.
- 完成内容: This closes the live storage-node observation where `node_pos_state.json` had `committed_height=23702` but the HTTP status remained at height 0 immediately after restart.
- 遗留事项: Rebuild and redeploy the release binary to all three nodes, then recheck status payloads and decide the operational state repair path from current live evidence.
- Action: Re-ran the targeted regression commands after the startup snapshot fix.
- Validation Command: `cargo fmt --check`
- Expected Result: formatting is clean.
- Actual Result: passed.
- Validation Command: `env -u RUSTC_WRAPPER cargo test -p oasis7_node network_gap_sync_not_found -- --nocapture`
- Expected Result: gap-sync not-found remains non-fatal and exposes blocked missing height.
- Actual Result: passed; 1 targeted node replication test passed.
- Validation Command: `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_tests -- --nocapture`
- Expected Result: status payload marks blocked replication gap as critical/unhealthy.
- Actual Result: passed; 4 observability tests passed.
- Blocker / Next Action: Run PM/governance checks, rebuild release, and redeploy this newer binary over the earlier live build.

## 2026-05-27 10:30:10 CST / runtime_engineer
- 完成内容: Rebuilt and deployed the updated `oasis7_chain_runtime` to the public testnet observer, sequencer, and storage nodes.
- 完成内容: Deployed runtime sha256 `2462cf283557e640a72523e94bb1c8b3869e77af02b4303eca16c48713d1065c`, size `96318648`, replacing earlier sha256 `9ef106ee11698615afff5cb7a2333390afa1f8c4c7e331d7c5f5565d863292de`.
- 完成内容: Live status after the detection rollout showed the real unhealthy state instead of a false green: local and storage blocked at missing replication commit height `7207`; sequencer blocked at height `20`.
- 完成内容: Probed live and backup data. The shared replication commit chain had multiple missing heights (`7207`, `7350`, `7728`, `10235`, `10763`, `10817`, `13777`, and later storage gaps), and only `7207` was found in backups. This made gap-sync repair from existing data impossible.
- 完成内容: Performed a coordinated, backed-up testnet state reset by moving old execution, storage, replication, and bridge state into `coordinated-reset-replication-gap-20260527-102531` backup directories on all three nodes, then restarted all nodes with the same updated binary.
- Action: Verified live node health after restart and a follow-up progress window.
- Validation Command: `curl /v1/chain/status` on local `:6633`, sequencer `39.104.204.172:6631`, and storage `39.104.205.67:6632`.
- Expected Result: all three nodes report `ok=true`, no replication gap blocked height, and committed/network/replication/execution heights converge.
- Actual Result: passed; all three nodes reported `ok=true`, `last_error=null`, `blocked=null`, `height=9`, `network=9`, `replicated=9`, `execution=9`, `peers=2`.
- 遗留事项: Status remains `warn` because recent replication transport/protocol error counters include startup/reset window noise; health is green because there is no critical replication gap, no execution lag, and top-level `ok=true`.
- Blocker / Next Action: Land the code-level health detection patch and retain the coordinated-reset backup directories for rollback/audit.
