# task_7f3428a8d35740e8912d1efd3ce4f2ff Execution Log

- task_uid: task_7f3428a8d35740e8912d1efd3ce4f2ff
- title: Verify and optimize gameplay runtime/viewer performance hotspots
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-game-gameplay-performance-verify-optimize

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-15 21:32:35 CST / runtime_engineer
- 完成内容: 先补了 `#[ignore]` perf harness，验证 viewer 侧热点而不是凭静态阅读直接优化。release 基线结果为 `agent_activity_summary avg_ms=0.355`（256 agents / 24,576 events）、`industry_graph zoom avg_ms=0.513`（576 nodes / 64 edges / 6,000 events）、`event_window avg_us_per_push=1.270`，确认前两者值得优先处理，`event_window` 仅记为轻度成本。
- 完成内容: 优化 `crates/oasis7_viewer/src/ui_text.rs`，把 Agent 活动摘要从“每个 agent 逆序扫描整条 events”改成“单次逆序扫描 events，按事件涉及的 agent 写入最新活动”，避免 `agents * events` 级重复扫描。
- 完成内容: 优化 `crates/oasis7_viewer/src/industry_graph_view_model.rs` 及其调用点，在构建期缓存 world/region zoom slice 与 route 过滤结果，新增 `graph_slice_for_zoom` / `routes_for_zoom_ref` 借用接口；`ui_text_economy.rs`、`ui_text_industrial.rs`、`world_overlay.rs` 改为复用借用视图，避免重复 clone/filter/sort。
- 完成内容: 补了回归覆盖，新增 `agent_activity_summary_uses_latest_matching_event_per_agent`、`zoom_cached_views_match_owned_views`，并确认 `collect_flow_segments_extracts_trade_and_power` 继续通过。
- 完成内容: release after 结果为 `agent_activity_summary avg_ms=0.268~0.310`、`industry_graph zoom avg_ms=0.066~0.083`、`industry_graph build avg_ms=2.202~2.294`、`event_window avg_us_per_push=0.873~0.920`。其中工业图 zoom 访问耗时下降约 84%~87%，Agent 活动摘要下降约 13%~25%，构建期 `IndustryGraphViewModel::build` 基本持平，说明这次优化主要命中了 render/read path。
- 验证: `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer agent_activity_summary_uses_latest_matching_event_per_agent --release`
- 验证: `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer zoom_cached_views_match_owned_views --release`
- 验证: `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer collect_flow_segments_extracts_trade_and_power --release`
- 验证: `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer --release perf_ -- --ignored --nocapture`
- 遗留事项: 当前 perf harness 仍主要覆盖 viewer 文本/overlay 路径；运行时 `collect_active_module_invocations` 的模块路由热点还没有同等粒度的基准，需要后续补 runtime 侧 perf harness 再决定是否继续优化。

## 2026-04-15 22:14:42 CST / runtime_engineer
- 完成内容: 补了 runtime 侧 `perf_route_module_subscriptions_with_many_active_manifests`，用 192 个 active manifests、事件/动作各 80 轮路由、每轮 192 次订阅命中来测 `route_event_to_modules` / `route_action_to_modules` 的订阅路由开销，避免只凭 `collect_active_module_invocations` 的实现形态做主观判断。
- 完成内容: release 验证结果为 `event_total_ms=250.07 event_avg_ms=3.126`、`action_total_ms=229.39 action_avg_ms=2.867`、`event_invoked=15360 action_invoked=15360`。这说明 runtime 路由在高 fan-out 合成负载下可测，但当前单次路由成本仍明显低于这轮已经命中的 viewer 工业图 read path，且主要耗时分散在 active invocation 收集、manifest/hash/context/input 序列化与模块调用前准备，暂不适合在没有更细粒度 flamegraph/真实场景数据前继续做高侵入缓存改造。
- 完成内容: 本轮将 runtime 模块订阅路由归类为 `partial`：已验证存在可测成本，但尚未复现为当前最值得优先优化的确认热点；viewer `agent_activity_summary` 与 industry graph zoom/read path 归类为 `confirmed` 并已完成优化，`event_window` 归类为 `not reproduced`。
- 验证: `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib --release perf_route_module_subscriptions_with_many_active_manifests -- --ignored --nocapture`
- 遗留事项: 若后续真实世界配置把 active module 数量继续推高，或 profiling 证明 `collect_active_module_invocations` / `hash_json` / `to_canonical_cbor(ModuleCallInput)` 进入热点前列，再单开任务做 runtime 路由缓存与失效策略设计。
