# task_e7f70a29287e4fe9a23467ba04ebf2ed Execution Log

- task_uid: task_e7f70a29287e4fe9a23467ba04ebf2ed
- title: libp2p control plane cpu optimization
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-libp2p-control-plane-cpu-optimization

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-24 13:25:00 CST / runtime_engineer
- 完成内容: 对齐 `doc/p2p/prd.md`、`doc/p2p/project.md` 与三节点监控样本，确认本任务承接 `libp2p-hotpath-perf (PRD-P2P-002)`，优先收敛 replication request peer 选择反复拉取 `debug_peer_healths()` 的控制面 CPU 热点。
- 遗留事项: 实现轻量 peer 选择接口、补齐定向回归并回写项目状态。

## 2026-04-24 13:48:00 CST / runtime_engineer
- 完成内容: 在 `crates/oasis7_net/src/libp2p_net/api.rs` 新增 `Libp2pNetwork::admissible_request_peers()`，直接基于运行态 `connected_peers + peer_healths` 计算请求候选，保留 blocked / soft-bootstrap fallback 语义；`crates/oasis7_node/src/libp2p_replication_network.rs` 改为复用该接口，避免 replication request wait/provider 选择路径反复构造 `debug_peer_healths()` 全量快照；新增 `admissible_request_peers` 定向测试并把 `libp2p-hotpath-perf` 项目项回写为完成。
- 遗留事项: `cargo test -p oasis7_net --features libp2p --lib` 全量回归两次出现 network-discovery/rendezvous 时序抖动，其中单独重跑 `libp2p_discovery_acquires_peer_from_dht_peer_record`、`libp2p_rendezvous_discovery_acquires_peer_from_bootstrap_registration` 均通过；`cargo test -p oasis7_node libp2p_replication_network -- --nocapture` 全量回归一次出现 `request_retries_next_peer_when_remote_handler_fails` 时序抖动，单独重跑该用例通过；`./scripts/doc-governance-check.sh` 当前在本环境中长时间无输出，仍待确认最终退出状态。

## 2026-04-24 21:14:37 CST / runtime_engineer
- 完成内容: 基于三节点同版本 observability 报告继续下钻，确认上一轮 peer-selection 收敛后，新的控制面热点已经转移到 `sequencer_ecs / replication_error_retry_churn`，主要表现为关闭连接上的 replication 请求反复重试；据此把 `crates/oasis7_node/src/libp2p_replication_network.rs` 的 protocol retry cooldown 判定调整为“所有 replication protocol 的 retryable connection-gap 都进入短冷却”，同时保留 `ErrNotFound` 只对 `fetch_commit` 生效，避免 `fetch_blob/ping` 等协议继续对已关闭 peer 做高频立即重试；新增 `connection_gap_on_ping_enters_short_cooldown` 回归并把断言改成匹配顶层请求语义而非内部重试次数。
- 遗留事项: 仍需基于这轮代码重新构建发布版、更新当前 PR 分支、再次部署本地 + 阿里云三节点，并用同窗口 observability 对比 `recent_error_count/connection_closed/runtime_cpu_core_ratio` 是否下降。

## 2026-04-24 21:30:50 CST / runtime_engineer
- 完成内容: 已执行 `env -u RUSTC_WRAPPER cargo build --release -p oasis7 --bin oasis7_chain_runtime`，得到 release binary `sha256=c94ef34982dcc0f00168f4af8b7bd62c9a2c6b14501d397cd2887d560cd0bc65`，并把本机 observer 与两台阿里云 ECS 的 `current` 全部切到 `f48e17af-retry-cooldown-20260424-212457`，确认 `oasis7-triad-observer.service`、`oasis7-triad-sequencer.service`、`oasis7-triad-storage.service` 均为 `active` 且 `current/bin/oasis7_chain_runtime` hash 一致。随后复跑 `./scripts/p2p-real-env-triad-snapshot.sh --samples 4 --interval-secs 5 --out-dir .tmp/p2p_real_env_triad_cpu_opt_20260424_post_retry_cooldown`，same-window 产物 `.tmp/p2p_real_env_triad_cpu_opt_20260424_post_retry_cooldown/20260424-212659/summary.md` 显示 `claim_status=pass_candidate`、三节点无 `last_error`、链高同窗推进。最后复跑 `./scripts/p2p-real-env-observability-monitor.sh --out-dir .tmp/p2p_real_env_observability_cpu_opt_20260424_post_retry_cooldown`，新报告位于 `.tmp/p2p_real_env_observability_cpu_opt_20260424_post_retry_cooldown/20260424-212810/report/latest_summary.json`。
- 完成内容: 相比基线 `/home/scc/oasis7/.tmp/p2p_real_env_observability_cpu_opt_20260424_post_local/20260424-175914/report/latest_summary.json`，这轮 live 指标出现实质收敛：`sequencer_ecs` host CPU `169.0% -> 163.0%`、core ratio `0.845 -> 0.815`、replication recent errors `77 -> 27`、`connection_closed 56 -> 14`；`storage_ecs` replication recent errors `138 -> 18`；`observer_local` CPU基本持平但 replication errors `9 -> 26`，说明全局重连抖动尚未完全消失。`replication_error_retry_churn` 仍是 `sequencer_ecs` 的 top optimization candidate，但强度已明显下降，说明把 connection-gap cooldown 扩展到全部 replication protocol 对控制面重试风暴是有效的。
- 遗留事项: 下一轮若继续压 CPU，应优先继续收敛 `sequencer_ecs` 上剩余的 `replication_error_retry_churn`，特别是 `other/request_failed` 组与 observer 侧新抬头的 connection-closed 抖动；storage 侧仍有 `storage_gc_failed` 信号，属于独立于本轮的后续治理项。

## 2026-04-24 23:33:18 CST / runtime_engineer
- 完成内容: 继续下钻上一轮 `sequencer_ecs` 剩余的 `replication_error_retry_churn`，发现连接抖动已经不只体现在 request 失败上，还表现为同一 peer 被多个 replication protocol 在短窗内重复复用；据此把 `crates/oasis7_node/src/libp2p_replication_network.rs` 的 retry-cooldown 机制拆成两层：`unsupported` 与 `fetch_commit ErrNotFound` 仍保留 protocol 级冷却，真正的 connection-gap 则进入跨协议复用的 peer-level transport cooldown。同步更新 `crates/oasis7/src/bin/oasis7_chain_runtime/status_server_support.rs` 与 `scripts/p2p-real-env-observability-summary.py`，把 transport cooldown 状态透出到 `/v1/chain/status` 与 triad observability summary；新增跨协议 transport cooldown 回归和状态构造回归，已执行 `env -u RUSTC_WRAPPER cargo test -p oasis7_node libp2p_replication_network -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability -- --nocapture` 与 `python3 -m py_compile scripts/p2p-real-env-observability-summary.py`，全部通过。
- 完成内容: 已执行 `env -u RUSTC_WRAPPER cargo build --release -p oasis7 --bin oasis7_chain_runtime`，得到 release binary `sha256=d2f105f62f04e6c26b6f2c12b291f326d762823bfc608f846145564dc48e0842`，并把本机 observer 与两台阿里云 ECS 的 `current` 全部切到 `9f5fbc33-transport-cooldown-20260424-232503`，确认三节点 `systemd active + current/bin hash` 一致。随后复跑 `./scripts/p2p-real-env-triad-snapshot.sh --samples 4 --interval-secs 5 --out-dir .tmp/p2p_real_env_triad_cpu_opt_20260424_post_transport_cooldown`，same-window 产物 `.tmp/p2p_real_env_triad_cpu_opt_20260424_post_transport_cooldown/20260424-232623/summary.md` 仍为 `claim_status=pass_candidate`、三节点无 `last_error`。
- 完成内容: 新 observability 报告位于 `.tmp/p2p_real_env_observability_cpu_opt_20260424_post_transport_cooldown/20260424-232737/report/latest_summary.json`。相比上一轮 `post_retry_cooldown`，`sequencer_ecs` 再次收敛：host CPU `163.0% -> 151.0%`、core ratio `0.815 -> 0.755`、replication recent errors `27 -> 20`、`connection_closed 14 -> 10`；相对最初 `post_local` 基线则已累计下降到 `169.0% -> 151.0%`、`0.845 -> 0.755`、`77 -> 20`、`56 -> 10`。但本轮也暴露出新的真值：`observer_local` CPU `48.7% -> 105.0%`、`storage_ecs` replication recent errors `18 -> 26`，且三节点 `transport_retry_cooldown_peers` 在采样时均为空，`recent_errors` 的主内容已经转为原始 `libp2p connection closed/established/transport active` 事件，说明剩余 CPU/churn 主因已转向底层重复建连与 active-path 抖动，而不是 replication request 层重试本身。
- 遗留事项: 下一轮应把重点转到 `oasis7_net` 的 `ConnectionEstablished/ConnectionClosed` 与 active transport path 生命周期，优先收敛同一 peer 的重复 direct 连接、重复 `transport active` 刷新和高频 `connection closed` 事件，而不是继续只在 replication request 层加冷却。

## 2026-04-25 11:38:30 CST / runtime_engineer
- 完成内容: 按上一轮 live 结论把优化点下钻到 `crates/oasis7_net/src/libp2p_net.rs` / `utils.rs` 的 `event_errors` 生命周期事件面，先增加短窗生命周期降噪工具，再把 `connection established/closed`、`transport active`、`routing updated` 接入该层。第一版按“完整字符串”做 cooldown 后，虽然 `observer_local` CPU 从 `105.0%` 降到 `85.9%`，但 live 报告 `.tmp/p2p_real_env_observability_cpu_opt_20260425_post_lifecycle_dedupe/20260425-110230/report/latest_summary.json` 也暴露出抖动仍然被 `num_established` 与临时端口差异放大：`sequencer_ecs` recent errors `20 -> 40`、`connection_closed 10 -> 27`，说明 key 取值过细，没有真正跨掉同语义的 direct-connection churn。
- 完成内容: 随后把 cooldown key 收紧为“按 peer + 生命周期语义”而非完整字符串：`connection established` 按 peer 去重，`connection closed` 区分 partial/final 但不再跟随 `num_established` 和临时端口，`transport active` 按 `peer + kind + flavor` 去重，保留 message 本身作为样本。新增 `push_bounded_string_with_keyed_cooldown_suppresses_distinct_messages_for_same_key` 单测，并复跑 `env -u RUSTC_WRAPPER cargo test -p oasis7_net libp2p_net -- --nocapture`；`env -u RUSTC_WRAPPER cargo test -p oasis7_node libp2p_replication_network -- --nocapture` 仍有既有时序抖动 `libp2p_replication_network_request_round_robins_across_connected_peers`，但单独重跑同一用例立即通过，其余 28 个相关用例全部通过。
- 完成内容: 已重新构建 release binary `sha256=0f7b487d7ff9bd38f092563d8f2888bf6980ff4a70aa19dc20615e830a484bf3`，并将本机 observer 与两台 ECS 的 `current` 全部切到 `514ea296-lifecycle-semantic-dedupe-20260425-113359`；三节点 `systemd active + current/bin hash` 一致。随后通过带密码的 wrapper 复跑同窗口监控，得到 `.tmp/p2p_real_env_observability_cpu_opt_20260425_post_semantic_lifecycle_dedupe/20260425-113546/report/latest_summary.json` 与 snapshot `.tmp/p2p_real_env_observability_cpu_opt_20260425_post_semantic_lifecycle_dedupe/20260425-113546/snapshot/20260425-113546/summary.md`，same-window triad 已恢复到 `claim_status=pass_candidate`。
- 完成内容: 相比语义 key 之前的同日第一轮 `.tmp/p2p_real_env_observability_cpu_opt_20260425_post_lifecycle_dedupe/20260425-110230/report/latest_summary.json`，三节点的生命周期抖动明显收敛：`observer_local` recent errors `32 -> 12`、`connection_closed 23 -> 4`；`sequencer_ecs` CPU `160.0% -> 152.0%`、recent errors `40 -> 11`、`connection_closed 27 -> 1`、optimization candidates `2 -> 1`；`storage_ecs` CPU `85.2% -> 80.9%`、recent errors `34 -> 14`、`connection_closed 19 -> 3`。相对更早的 `post_transport_cooldown` 基线，`observer_local` CPU `105.0% -> 90.0%`、`sequencer_ecs` recent errors `20 -> 11` / `connection_closed 10 -> 1`、`storage_ecs` CPU `82.9% -> 80.9%` / `connection_closed 13 -> 3`，说明这轮真正收敛的是“观测/状态面被重复生命周期事件放大”的成本，而底层多连接 churn 本身仍在。
- 遗留事项: 目前剩余高优先级热点仍是 `sequencer_ecs` 的 `replication_error_retry_churn`，但证据已经从“重复错误样本太多”收敛成“底层仍会建立多条 direct 连接，只是状态面不再被它放大”。下一轮若继续压 CPU，应该去 `oasis7_net` 的实际建连/active-path 选择路径收敛并发 direct 连接数量，而不是继续在 status/debug surface 上做字符串级治理。

## 2026-04-25 14:07:33 CST / runtime_engineer
- 完成内容: 按 `2026-04-25 13:02 CST` refresh 报告继续下钻真实热点，确认 sequencer 的控制面抖动已从“状态面被放大”进一步收敛成“同一 peer 同时保留多条已建立连接”。据此在 `crates/oasis7_net/src/libp2p_net/connection_lifecycle.rs` 新增 `redundant_connection_ids_for_peer()`，按 `TransportPath::preference_rank + ConnectionId` 只保留同一 peer 的一条最优连接；并在 `SwarmEvent::ConnectionEstablished` 后立即对其余连接执行 `swarm.close_connection(...)`，同时新增 direct-vs-relay 与 equal-rank 两个单测，复跑 `env -u RUSTC_WRAPPER cargo fmt --check`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net libp2p_net -- --nocapture`，均通过。
- 完成内容: 已重新构建 release binary `sha256=e411f9fe263f59f85e50b55f0efa3ccad2dc5dad0e4dfec681a63e37b0f4348a`，并将本机 observer 与两台 ECS 的 `current` 全部切到 `e411f9fe-redundant-connection-prune-20260425-140252`；`oasis7-triad-observer.service`、`oasis7-triad-sequencer.service`、`oasis7-triad-storage.service` 均为 `active`，三台机器运行态 hash 一致。
- 完成内容: 新 observability 报告位于 `.tmp/p2p_real_env_observability_cpu_opt_20260425_post_redundant_connection_prune/20260425-140418/report/latest_summary.json`。与部署前的同日 refresh `.tmp/p2p_real_env_observability_cpu_opt_20260425_refresh/20260425-130036/report/latest_summary.json` 对比，`sequencer_ecs` runtime CPU `173.0% -> 122.0%`、core ratio `0.865 -> 0.61`、`transport.connection_closed 1518 -> 16`、`transport.connection_established 1547 -> 18`，并且状态面已经出现 `libp2p redundant connections pruned peer=... count=1` 证据；`storage_ecs` CPU `85.5% -> 83.8%` 基本持平，`observer_local` `105.0% -> 110.0%` 仍偏高，但 sequencer 的真实热点已明显压下。
- 完成内容: 首轮 snapshot 因 observer 刚重启后的短窗未推进，报告为 `partial_with_observer_blocker`；随后补跑 `.tmp/p2p_real_env_triad_cpu_opt_20260425_post_redundant_connection_prune_resnapshot/20260425-140629/summary.md`，结果已恢复 `claim_status=pass_candidate`，三节点链高同窗推进且无 `last_errors`，确认该 blocker 不是持久回归。
- 遗留事项: sequencer 的多连接抖动已从千级事件压到两位数，但 `known_peer_heads` 仍为 `0 -> 0`，且 observer CPU 仍高于 `post_semantic_lifecycle_dedupe`；下一轮若继续压 CPU，应继续沿 `kad`/active-path 侧检查为什么仍会产生少量 `num_established=1` 的 direct close，而不是回到字符串级观测面治理。

## 2026-04-25 19:15:17 CST / runtime_engineer
- 完成内容: 继续沿 `kad` / active-path 下钻后，尝试把 `MissingPeerRecord`、`SingleSourceDiscovery`、`InsufficientActiveDiscoverySources` 这类“发现信息暂未补齐”的 issue 从 `peer_requires_active_quarantine()` 的 active disconnect 条件里剥离，仅保留真实拓扑/集中度/relay budget 风险继续触发 quarantine；同步新增 `soft_suspect_peer` 回归，确认 soft suspect 不会被立即断连。相关代码先通过 `env -u RUSTC_WRAPPER cargo fmt --check`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net libp2p_net -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node libp2p_replication_network -- --nocapture` 与 `./scripts/check-rust-file-size.sh`。
- 完成内容: 已构建 release binary `sha256=b780d2f5c8c909249471adc9d3fda738396e18f6e351bb6df80c3ed95d83456d`，短暂部署到本机 observer 与两台 ECS，release 目录为 `b780d2f5-soft-suspect-discovery-20260425-190456`；三节点一度都切到了该 hash 并完成了两窗 live observability。
- 完成内容: 第一窗报告位于 `.tmp/p2p_real_env_observability_cpu_opt_20260425_post_soft_suspect_discovery/20260425-190918/report/latest_summary.json`，相对当前线上最优基线 `.tmp/p2p_real_env_observability_cpu_opt_20260425_post_redundant_connection_prune/20260425-140418/report/latest_summary.json` 已出现退化：`sequencer_ecs` CPU `122.0% -> 130.0%`、`transport.connection_closed 4 -> 15`、`transport.connection_established 4 -> 15`、`kademlia.outbound_query_progressed 15 -> 51`；`observer_local` 与 `storage_ecs` 的 transport/kad 事件也同步上升。
- 完成内容: 第二窗报告位于 `.tmp/p2p_real_env_observability_cpu_opt_20260425_post_soft_suspect_discovery_resample/20260425-191140/report/latest_summary.json`，退化继续放大：`sequencer_ecs` CPU `122.0% -> 153.0%`、recent replication errors `11 -> 21`、`transport.connection_closed 4 -> 15`、`kademlia.outbound_query_progressed 15 -> 53`；`storage_ecs` recent replication errors `15 -> 34`、`transport.connection_closed 5 -> 16`。两窗都没有优于 `e411f9fe...`，因此确认该方向会重新放大连接 churn 与 Kademlia 控制面，而不是降低 CPU。
- 完成内容: 已立即将本机 observer 与两台 ECS 的 `current` 全部回滚到 `e411f9fe-redundant-connection-prune-20260425-140252`，并确认三台 `systemd active + current/bin sha256=e411f9fe263f59f85e50b55f0efa3ccad2dc5dad0e4dfec681a63e37b0f4348a`；同时把本地 worktree 中本次 soft-suspect quarantine 试验代码回退，不把退化方案留在当前 PR。
- 遗留事项: 下一轮不要继续扩 discovery-incomplete quarantine 例外；更值得继续追的是 `peer record request failure/retry` 节奏与 `kad` 查询放大链，而不是放宽 active quarantine。

## 2026-04-25 22:59:28 CST / runtime_engineer
- 完成内容: 继续沿 `replication request` 热路径下钻，确认当前更值得收敛的是上层对同一 `fetch-commit` height / `fetch-blob` hash 的 retryable gap 高频重入，而不是继续改 discovery。于是把短窗 request-level backoff 收进 `crates/oasis7_node/src/network_bridge.rs` 与 `crates/oasis7_node/src/replication_probe_gate.rs`：对命中 `replication_request_waitable_connection_gap()` 的 `fetch-commit` / `fetch-blob` 请求按同一 height/hash 做 1s 默认 backoff，backoff 窗口内直接返回上次 retryable error，不再重新打网络。
- 完成内容: 在 `crates/oasis7_node/src/tests_network_gap_sync.rs` 新增两个定向回归：`gap_sync_fetch_commit_retryable_backoff_suppresses_identical_height_retries` 与 `gap_sync_fetch_blob_retryable_backoff_suppresses_identical_hash_retries`，分别验证相同 height/hash 的立即重复请求会被抑制，而在 backoff 过后会重新放行；同时保留既有 `runtime_network_replication_gap_sync_fetch_commit_success_cache_reuses_validated_response` 通过，确认成功路径缓存没有被破坏。已执行 `cargo fmt --all`、`./scripts/check-rust-file-size.sh`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node gap_sync_fetch_commit_retryable_backoff_suppresses_identical_height_retries -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node gap_sync_fetch_blob_retryable_backoff_suppresses_identical_hash_retries -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node runtime_network_replication_gap_sync_fetch_commit_success_cache_reuses_validated_response -- --nocapture`。
- 遗留事项: 还需等更广的 `network_gap_sync` 定向回归完成，再决定是否直接构建 release 并部署三节点验证两窗 CPU/traffic 变化。

## 2026-04-26 10:07:00 CST / runtime_engineer
- 完成内容: 已补跑 `env -u RUSTC_WRAPPER cargo test -p oasis7_node network_gap_sync -- --nocapture`（11 passed）与 `env -u RUSTC_WRAPPER cargo test -p oasis7_node storage_challenge -- --nocapture`（14 passed），随后构建 release binary `sha256=76eeeff7c1569b1a621efbfa5bc20624cadf7d18d692884fc93f489c5e4423d3`，短暂部署到本机 observer 与两台 ECS，release 目录为 `76eeeff7-replication-request-backoff-20260426-095714`；三台运行态 hash 在切换时一致。
- 完成内容: live 第一窗很快暴露退化/异常，不满足继续留在线上的条件。证据一：`.tmp/p2p_real_env_triad_cpu_opt_20260426_post_replication_request_backoff/20260426-095846/summary.md` 直接给出 `claim_status=blocked`，observer `committed_height 0 -> 0`、`network_committed_height 0 -> 0`、`known_peer_heads 0 -> 0`；本机实时 `/v1/chain/status` 也确认 `connected_peers=0`。证据二：observability monitor 在生成最终 report 前因 wasm 输入缺少 `observed_at_unix_ms` 退出，但保留下来的 `host/latest_summary.json` 与 `traffic/latest_summary.json` 已足够说明风险：observer 在不到 1 分钟窗口里出现 `fetch-commit=52`、`fetch-blob=50` 的高频拉取，而最优基线 `.tmp/p2p_real_env_observability_cpu_opt_20260425_post_redundant_connection_prune/20260425-140418/traffic/latest_summary.json` 只有个位数同类请求且 observer 同窗可推进。
- 完成内容: 已立即把本机 observer 与两台 ECS 的 `current` 全部回滚到 `e411f9fe-redundant-connection-prune-20260425-140252`，并确认三台 `current/bin/oasis7_chain_runtime` 均恢复为 `sha256=e411f9fe263f59f85e50b55f0efa3ccad2dc5dad0e4dfec681a63e37b0f4348a`、对应 systemd service 为 `active`。同时把本次 request-level backoff 代码从当前 worktree 回退，不把这轮 live 退化方案留在当前 PR diff。
- 遗留事项: 线上坏版本已撤下，但 observer 在回滚后仍未恢复到基线前的连通状态；最新核验 `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_rollback_verify_resnapshot/20260426-100554/summary.md` 仍是 `claim_status=blocked`，表现为 observer `0 -> 0`、`connected_peers=0`。下一轮应先把 observer 的 baseline 连通/追高恢复到 `pass_candidate`，再继续新的 CPU 优化切点。

## 2026-04-26 11:30:18 CST / runtime_engineer
- 完成内容: 放弃全局 request-level backoff 后，改为只在 `crates/oasis7_node/src/replication_probe_gate.rs` 的 `maybe_hold_proposal_for_replication_successor_probe()` 上收敛“同一 successor height 且 `peer_heads` 为空”的重复探测：新增 `REPLICATION_SUCCESSOR_PROBE_COOLDOWN_MS=1000`，在 `PosNodeEngine` 里记录最近 probe 的 `height/at_ms`，并在 `NotFound` 或 waitable connection-gap 命中时进入短冷却；同时补上 `successor_probe_cooldown_suppresses_same_height_not_found_retry` 回归。相关本地验证已完成：`cargo fmt --all`、`./scripts/check-rust-file-size.sh`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node successor_probe_cooldown_suppresses_same_height_not_found_retry -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node network_gap_sync -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node storage_challenge -- --nocapture`。
- 完成内容: 已重新构建并部署 release binary `sha256=69043a85ec9477c0e4a78748cd237646ef46b530fd010138136524aa22f38d98`，release 目录为 `69043a85-successor-probe-cooldown-20260426-105936`，本机 observer 与两台 ECS 在切换后均为 `active` 且 hash 一致。部署后的第一份 triad snapshot `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_successor_probe_cooldown/20260426-110058/summary.md` 受采样窗里的 `cloud_pair_service_unhealthy` 干扰暂时给出 `claim_status=blocked`，但 observer 已恢复基本连通；随后补跑 `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_successor_probe_cooldown_resnapshot/20260426-110346/summary.md`，结果恢复为 `claim_status=pass_candidate`，三节点 `committed_height/network_committed_height` 同窗推进。
- 完成内容: 第一窗 observability 产物位于 `.tmp/p2p_real_env_observability_cpu_opt_20260426_post_successor_probe_cooldown/20260426-110058/{host,traffic}/latest_summary.json`。对比当前线上最优基线 `.tmp/p2p_real_env_observability_cpu_opt_20260425_post_redundant_connection_prune/20260425-140418/traffic/latest_summary.json`，按窗口长度归一化后已出现收敛：`observer_local` 每分钟 `fetch_commit outbound 13.04 -> 12.94`、`fetch_blob outbound 8.70 -> 2.35`、`kad 43.48 -> 32.94`；`sequencer_ecs` 每分钟 `fetch_commit outbound 34.78 -> 32.94`、`fetch_blob outbound 21.74 -> 4.71`、`kad 65.22 -> 49.41`，且 host CPU `122.0% -> 75.0%`；`storage_ecs` 每分钟 `fetch_commit outbound 34.78 -> 24.71`、`fetch_blob outbound 17.39 -> 3.53`。
- 完成内容: 为避免只看短窗，又追加第二窗完整 observability `.tmp/p2p_real_env_observability_cpu_opt_20260426_post_successor_probe_cooldown_resample2/20260426-110655/report/latest_summary.json`。第二窗 triad 仍为 `pass_candidate`，并且相对 `e411f9fe...` 基线继续保持更低的控制面速率：`observer_local` 每分钟 `fetch_commit outbound 13.04 -> 4.17`、`fetch_blob outbound 8.70 -> 0.74`、`kad 43.48 -> 18.63`；`sequencer_ecs` 每分钟 `fetch_commit outbound 34.78 -> 18.87`、`fetch_blob outbound 21.74 -> 3.19`、`kad 65.22 -> 44.12`，host CPU `122.0% -> 118.0%`；`storage_ecs` 每分钟 `fetch_commit outbound 34.78 -> 20.83`、`fetch_blob outbound 17.39 -> 3.43`、`kad 43.48 -> 37.25`。虽然第二窗原始累计 `control_plane_events` 更大，但窗口覆盖时长也从基线的 `0.23m` 拉长到 `4.08m`，归一化后没有反弹。
- 完成内容: 基于两窗 live 证据，当前决定是不回滚，继续保留 `69043a85-successor-probe-cooldown-20260426-105936` 在线。它没有复现 `76eeeff7...` 那种 observer 掉线/空转拉取退化，且在 `fetch_commit/fetch_blob/kad` 三个直接对应控制面 CPU 的热点上，窗口化速率均优于 `e411f9fe...` 基线。
- 遗留事项: `observer_local` CPU 仍在 `~106%`，`sequencer_ecs` 第二窗也仍有 `recent_replication_errors_high` 与较高 `control_plane_events`，说明这轮解决的是“空 `peer_heads` successor probe 重入”，还没打到剩余 `transport.connection_{closed,established}` 与 replication recent-errors 根因；下一轮应继续沿 `known_peer_heads=0` 时的发现/建连抖动去压 sequencer 与 storage 的长窗控制面事件。

## 2026-04-26 12:26:45 CST / runtime_engineer
- 完成内容: 继续尝试收敛 `known_peer_heads=0` 窗口里的 discovery/request 抖动，先在 `crates/oasis7_net/src/libp2p_net/connection_lifecycle.rs` 试验“peer 断连时保留 `connected/cached/discovery` 三类 peer-record cooldown，不再立即清空”，对应把 `clear_disconnected_peer_state_removes_peer_record_cooldowns` 改为保留 cooldown 语义。相关本地验证先通过：`cargo fmt --all`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net clear_disconnected_peer_state_preserves_peer_record_cooldowns -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net discovery_peer_record -- --nocapture`、`./scripts/check-rust-file-size.sh`。
- 完成内容: 已构建 release binary `sha256=5a83bee2e198aca850f1b40a88f4f7175b50d116d6e00a11208e9d53013ccf9d`，release 目录为 `5a83bee2-peer-record-cooldown-retain-20260426-121336`，并切换本机 observer 与两台 ECS；三台 `current/bin/oasis7_chain_runtime` hash 一致、对应 systemd service 为 `active`。部署后先跑 triad snapshot `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_peer_record_cooldown_retain/20260426-121702/summary.md`，结果为 `claim_status=pass_candidate`，说明这版不会像 `76eeeff7...` 那样立即把 triad 打挂。
- 完成内容: 第一窗 observability 产物位于 `.tmp/p2p_real_env_observability_cpu_opt_20260426_post_peer_record_cooldown_retain/20260426-121825/report/latest_summary.json`。相对当前线上 `69043a85...` 第二窗 `.tmp/p2p_real_env_observability_cpu_opt_20260426_post_successor_probe_cooldown_resample2/20260426-110655/report/latest_summary.json`，出现混合信号：`sequencer_ecs` host CPU `118.0% -> 106.0%`、`fetch_commit outbound/min 18.87 -> 16.30`，但同时 `kad/min 44.12 -> 56.52`、`fetch_blob outbound/min 3.19 -> 6.52`、`get_local_peer_record outbound/min 2.45 -> 4.35`，窗口仅 `0.92m`，不足以直接定生死。
- 完成内容: 为避免误判，又补了第二窗 `.tmp/p2p_real_env_observability_cpu_opt_20260426_post_peer_record_cooldown_retain_resample2/20260426-122133/report/latest_summary.json`，结果已经触发回滚门槛：snapshot 直接给出 `claim_status=blocked`，失败签名为 `cloud_pair_no_recent_progress_signal, observer_committed_height_not_advancing`，三节点 `71670 -> 71670` 无推进；控制面也没有出现可辩护的净收益，`sequencer_ecs` 虽维持 `106.0%` CPU，但 `control_plane_events/min 83.33 -> 90.80`、`kad/min 44.12 -> 49.43`、`get_local_peer_record outbound/min 2.45 -> 4.60`，observer CPU 还从 `106.0%` 抬到 `113.0%`。据此确认“断连后保留 peer-record cooldown”会伤害 live 推进稳定性，不适合保留在线上。
- 完成内容: 已立即把本机 observer 与两台 ECS 的 `current` 全部回滚到 `69043a85-successor-probe-cooldown-20260426-105936`，并确认三台 `current/bin/oasis7_chain_runtime` 均恢复为 `sha256=69043a85ec9477c0e4a78748cd237646ef46b530fd010138136524aa22f38d98`、对应 systemd service 为 `active`。回滚后首窗 `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_peer_record_cooldown_rollback_verify/20260426-122548/summary.md` 仍是重启后短窗 `partial_with_observer_blocker`，随后补跑 `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_peer_record_cooldown_rollback_verify_resnapshot/20260426-122704/summary.md`，已恢复 `claim_status=pass_candidate`，三节点 `71678 -> 71679` 同窗推进。
- 完成内容: 已把本次 peer-record cooldown 保留试验代码从当前 worktree 回退，不把退化方案留在当前 PR diff；当前 PR 仍只保留已经通过 live 两窗验证的 `successor_probe cooldown` 改动。
- 遗留事项: 不要继续沿“保留 peer-record cooldown across disconnect”方向优化；更值得继续追的是 `69043a85...` 仍残留的 `known_peer_heads=0` 期间 `kad / connection_closed / connection_established / recent_replication_errors` 根因，而不是继续挤 peer-record request cooldown 语义。

## 2026-04-26 13:25:18 CST / runtime_engineer
- 完成内容: 继续沿 discovery fan-out 下钻，在 `crates/oasis7_net/src/libp2p_net/runtime_loop.rs` 试验把 `RefreshPeerDiscovery` 的 backfill 从“对全部 connected peers 发 `get_cached_discovery_peers`”收敛成“每个周期只选一个 peer 发请求”，优先减少 `get_cached_discovery_peers / get_local_peer_record / kad` 的重复 fan-out；同时在 `transport_retry_tests.rs` 补了“优先选缺 record peer”和“单周期只发一个 cached discovery request”的定向回归。相关本地验证均通过：`cargo fmt --all`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net select_backfill_peer_discovery_peer_prefers_missing_connected_peer_record -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net refresh_peer_discovery_requests_cached_discovery_from_single_connected_peer -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net transport_retry -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net discovery_peer_record -- --nocapture`、`./scripts/check-rust-file-size.sh`。
- 完成内容: 已构建 release binary `sha256=6da1345ae198ba579078ca02051328932fc94cf61fbcfe59ce95262af5487d41`，release 目录为 `6da1345a-single-peer-discovery-backfill-20260426-131338`，并切换本机 observer 与两台 ECS；三台 `current/bin/oasis7_chain_runtime` hash 一致、对应 systemd service 为 `active`。
- 完成内容: live 首窗 `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_single_peer_discovery_backfill/20260426-131525/summary.md` 直接给出 `claim_status=blocked`，首个 blocker 里已经有 `observer_committed_height_not_advancing`；为排除重启短窗噪声，又补跑 `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_single_peer_discovery_backfill_resnapshot/20260426-131632/summary.md`，结果仍然是 `claim_status=blocked`，失败签名收敛成 `cloud_pair_no_recent_progress_signal, observer_committed_height_not_advancing`，三节点窗口内全部停在 `71755 -> 71755`，说明这条线会伤害 live 推进稳定性，不值得继续保留到 observability 第二阶段。
- 完成内容: 已立即把本机 observer 与两台 ECS 的 `current` 全部回滚到 `69043a85-successor-probe-cooldown-20260426-105936`，并确认三台 `current/bin/oasis7_chain_runtime` 均恢复为 `sha256=69043a85ec9477c0e4a78748cd237646ef46b530fd010138136524aa22f38d98`、对应 systemd service 为 `active`。回滚后首窗 `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_single_peer_discovery_backfill_rollback_verify/20260426-131832/summary.md` 仍是短窗 `blocked`，随后补跑 `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_single_peer_discovery_backfill_rollback_verify_resnapshot/20260426-131936/summary.md`，已恢复 `claim_status=pass_candidate`，三节点 `71757 -> 71758` 同窗推进。
- 完成内容: 已把 single-peer discovery backfill 试验代码和对应测试从当前 worktree 回退，不把这轮退化方案留在当前 PR diff；`oasis7_net` 回归已重新恢复到原始基线：`env -u RUSTC_WRAPPER cargo test -p oasis7_net transport_retry -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net discovery_peer_record -- --nocapture` 均通过。
- 遗留事项: 不要继续沿“把 discovery backfill fan-out 收成单 peer”这条线深挖；虽然它理论上能减少 `get_cached_discovery_peers`，但 live 里会放大 `observer_committed_height_not_advancing` 风险。下一轮仍应回到 `69043a85...` 的已知稳定线上版本上，继续找 `known_peer_heads=0` 窗口里更直接的 `kad / connection_closed / connection_established / recent_replication_errors` 根因。

## 2026-04-26 14:22:34 CST / runtime_engineer
- 完成内容: 继续沿 `known_peer_heads=0` 的 `kad` 热点下钻，这轮没有再碰 request backoff / peer-record disconnect cooldown / single-peer backfill，而是把 `crates/oasis7_net/src/libp2p_net/discovery.rs` 的 `maybe_queue_discovery_peer_record()` 收紧为“若该 peer 已有 `discovered_peer_records` 缓存，则不再重复排入 DHT `get_record`”；对应更新 `crates/oasis7_net/src/libp2p_net.rs` 调用点，并新增 `maybe_queue_discovery_peer_record_skips_peer_with_cached_record` 回归，目标是直接削掉 `RoutingUpdated / ConnectionEstablished / DiscoverPeers` 对同一 peer record 的重复 Kademlia 查询。
- 完成内容: 本地验证通过：`cargo fmt --all`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net discovery_peer_record -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net transport_retry -- --nocapture`、`./scripts/check-rust-file-size.sh`；期间将 `crates/oasis7_net/src/libp2p_net.rs` 从 `1202` 行压回 `1197` 行，保持 Rust 文件尺寸门禁通过。
- 完成内容: 已构建 release binary `sha256=e514726025f41ec5c9c2823d53be29eb61af8f09f751ac2162e8f9142e7a1433`，release 目录为 `e5147260-discovery-peer-record-dht-dedupe-20260426-141408`，并把本机 observer 与两台 ECS 的 `current` 全部切到该 release；三台 `current/bin/oasis7_chain_runtime` hash 一致、对应 `oasis7-triad-observer.service` / `oasis7-triad-sequencer.service` / `oasis7-triad-storage.service` 均为 `active`。
- 完成内容: 部署后首窗 triad snapshot `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_discovery_peer_record_dedupe/20260426-141652/summary.md` 因 `sequencer` 重启短窗触发 `cloud_pair_service_unhealthy`，表现为 `claim_status=blocked`；按既定规则补跑 resnapshot `.tmp/p2p_real_env_triad_cpu_opt_20260426_post_discovery_peer_record_dedupe_resnapshot/20260426-141754/summary.md` 后恢复 `claim_status=pass_candidate`，三节点 `71840 -> 71842` 同窗推进，说明这版没有伤到 live 推进稳定性。
- 完成内容: 基线 observability 取自 `.tmp/p2p_real_env_observability_cpu_opt_current_precheck/20260426-141058/report/latest_summary.json`，新窗 observability 取自 `.tmp/p2p_real_env_observability_cpu_opt_20260426_post_discovery_peer_record_dedupe/20260426-141902/report/latest_summary.json`。相对当前线上 `69043a85...` 基线，这轮出现明确净收益：`sequencer_ecs` host CPU `117.0% -> 90.8%`、core ratio `0.585 -> 0.454`、recent replication errors `247 -> 37`、control-plane events/min `90.8 -> 49.4`；`observer_local` CPU `120.0% -> 110.0%`、recent errors `17 -> 15`、events/min `45.98 -> 31.33`；`storage_ecs` CPU `88.3% -> 86.3%`、recent errors `311 -> 19`、events/min `62.07 -> 46.99`。流量 top kinds 也直接命中目标：`sequencer_ecs` 的 `kademlia.outbound_query_progressed` 从 `43` 掉到 `9`，`observer_local` 从 `20` 掉到 `4`，`storage_ecs` 从 `22` 掉到 `7`；`transport.connection_established/closed` 仍在，但 `kad` 已不再是第一热点。
- 完成内容: 基于 `pass_candidate + observability` 的组合证据，这轮决定不回滚，继续把 `e5147260-discovery-peer-record-dht-dedupe-20260426-141408` 保留在线上，作为当前 PR 在 `69043a85...` 基础上的下一版稳定优化。
- 遗留事项: 这轮主要打掉的是“已知 peer record 的重复 DHT 查询”，`transport.connection_established/closed` 仍是剩余第一热点，后续若继续压 CPU，应优先查同一 peer 的建连/断连抖动与 `recent_error_groups.other` 的来源，而不是回到已证伪的 fan-out / cooldown 方向。
