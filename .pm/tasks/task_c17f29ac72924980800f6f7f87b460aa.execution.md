# task_c17f29ac72924980800f6f7f87b460aa Execution Log

- task_uid: task_c17f29ac72924980800f6f7f87b460aa
- title: public_testnet three-node deploy readiness audit
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-public-testnet-three-node-deploy-readiness

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-18 22:33:00 CST / runtime_engineer
- 完成内容: 读取 formal network-tier / readiness 文档与脚本后，确认当前 repo 只有 `public_testnet` skeleton，没有现成 live candidate；因此本轮按“本地三节点 rehearsal 部署 + live status 取证”推进，而不是把 placeholder scaffold 误报为可部署结论。
- 完成内容: 构建 `oasis7_chain_runtime`，生成本地 `public_testnet` rehearsal candidate bundle / manifest，并修正 manifest `runtime_refs.genesis_ref` 为 runtime 可加载的绝对路径。
- 完成内容: 用同一份 `--network-tier-manifest output/public-testnet-local-triad/network-tier-public-testnet-local-triad.json` 起 `pt-sequencer` / `pt-storage` / `pt-observer` 三节点，固定 `status-bind=17631/17632/17633`、`node-gossip-bind=17611/17612/17613`、`replication-network-listen=17651/17652/17653`。
- 完成内容: live `/v1/chain/status` 证明三节点都成功加载 formal `public_testnet` manifest，且都暴露 `network_tier.tier=public_testnet`、`status=rehearsal`、`bootstrap_peer_count=3`、`last_error=null`；因此可把 `runtime_bootstrap` 记为 `pass`。
- 完成内容: 同时确认本轮 manifest `rpc_ref/explorer_ref/faucet_ref` 都仍是 `127.0.0.1` local-only ref，因此 `public_rpc_ready` / `explorer_public_ready` / `faucet_guard_ready` 不能记 `pass`；`shared_devnet_pass`、`reset_policy_announced`、`claims_boundary_review` 也没有被这次本地部署覆盖。
- 遗留事项: 若要继续把 readiness lane 往上推，需要补真实 public RPC/explorer/faucet 与正式 reset/claims review 证据；若要推进 overall gate，还必须先拿到 formal `shared_devnet_pass`。
