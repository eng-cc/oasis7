# task_b7097f476e674a429469a98d6ae36794 Execution Log

- task_uid: task_b7097f476e674a429469a98d6ae36794
- title: Fix release web gate LLM preflight config
- owner_role: qa_engineer
- worktree_hint: /home/scc/worktrees/oasis7-testing-release-web-llm-preflight

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-22 21:48:19 CST / qa_engineer
- 完成内容: 复盘 `Release Packages` run `26290255692`；确认 aggregate `release-gate` 仅因 `release-gate-web` 失败而阻断，失败点位于 `Run web strict release gate`。进一步提取 job `77388302846` 失败日志，确认 `viewer-primary-web-entry-regression.sh` 跑完后，同一 workflow 的 `viewer-software-safe-step-regression.sh` 在 `run-game-test` active LLM provider preflight 处报 `missing env variable: OASIS7_LLM_MODEL`。
- 完成内容: 修复 `scripts/release-gate-web-strict.sh`，让 software-safe phase 显式传 `--skip-llm-provider-preflight`，把 release web strict gate 收口为 stack bootstrap 后的 UI/blocker contract 验证，不再错误依赖 GitHub release runner 的本机/secret LLM 配置。
- 完成内容: 修复 `scripts/viewer-primary-web-entry-regression.sh`，记录 root `config.toml` 起跑前是否存在；若原本不存在，则在退出时清理 launcher/chain runtime 自动生成的 node-only `config.toml`，避免污染同一 worktree / workflow 的后续步骤。
- 完成内容: 同步 `doc/testing/prd.md` 与 `doc/testing/project.md`，把 release web 的 node-only `config.toml` 污染 + preflight drift 记为正式 failure signature，并将 `release-web-preflight-config-shadow` 任务收口到已完成状态。
- 完成内容: 本地验证通过：
  - `bash -n scripts/release-gate-web-strict.sh scripts/viewer-primary-web-entry-regression.sh`
  - `./scripts/viewer-primary-web-entry-regression.sh --help >/dev/null`
  - 临时移走 ignored `config.toml` 后执行 `./scripts/release-gate-web-strict.sh --scenario llm_bootstrap --out-dir .tmp/release_gate_web_preflight_shadow_local`，整条 strict gate 返回 `ok: artifacts written .../software-safe-step/...`
  - 临时移走 ignored `config.toml` 后单跑 `./scripts/viewer-primary-web-entry-regression.sh --scenario llm_bootstrap --out-dir .tmp/primary_entry_config_shadow_local`，退出后观察到 `CONFIG_ABSENT`，确认 primary-entry 已清理自己生成的 node-only `config.toml`
- 完成内容: PR `#267` reviewer 提出 `scripts/release-gate-web-strict.sh` 的 echo 命令未同步打印 `--skip-llm-provider-preflight`。已补齐日志输出，使 `summary/log` 中的命令回显与真实执行参数一致。
- 遗留事项: 需要 commit -> prepare PR -> merge 到 `main` 后，重新触发 `release-packages.yml`（建议继续使用未占用 tag `v0.0.52` 或更高新 tag），并监控新的 GitHub release run 直到 `gh release view <tag>` 可见。
