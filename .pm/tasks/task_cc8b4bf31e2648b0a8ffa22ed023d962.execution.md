# task_cc8b4bf31e2648b0a8ffa22ed023d962 Execution Log

- task_uid: task_cc8b4bf31e2648b0a8ffa22ed023d962
- title: software_safe bilingual UI and explicit viewer entry
- owner_role: viewer_engineer
- worktree_hint: /home/scc/worktrees/oasis7-viewer-software-safe-bilingual-entry

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-16 12:37:10 CST / viewer_engineer
- 完成内容: 为 `software_safe` 正式 Web 主入口补齐中英文 locale 支持与页面级语言切换；在 `software_safe` 页面显式暴露当前语言的标准 Viewer 入口；为标准 Viewer 增加 `locale` / `language` URL 初始化支持；同步更新 `scripts/run-game-test.sh` 默认中文试玩 URL 与标准 Viewer 中英双链接输出；回写 viewer PRD/project/manual。
- 完成内容: 更新 `crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs`，覆盖中文语义反馈与 `setSoftwareSafeLocale("zh")` 行为。
- 完成内容: 完成验证闭环：`node crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs` 通过；`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer parse_ui_locale_from_search` 通过；`npm run build:software-safe` 通过；`bash ./scripts/viewer-primary-web-entry-regression.sh` 通过，摘要位于 `output/playwright/viewer-primary-web-entry/viewer-primary-web-entry-20260416-122942/summary.md`；额外通过 `agent-browser` 静态 smoke 确认 `software_safe.html?locale=zh` 首屏中文、切到 English 后 URL 变为 `?locale=en`，且页面展示的标准 Viewer 链接分别为 `/?locale=zh&render_mode=standard` 与 `/?locale=en&render_mode=standard`。
- 遗留事项: 未执行 commit / PR，按用户当前指令保留为本地改动待后续收口。

## 2026-04-16 14:44:27 CST / viewer_engineer
- 完成内容: 将 `main` 本地 checkout 的 repo-local LLM 配置同步到当前 worktree 的本地 `config.toml`，仅补齐 `[llm]` 字段，保留当前 worktree 自己的 `[node]` keypair，不覆盖节点身份。
- 完成内容: 停掉旧的 `run-game-test.sh` 栈后，使用 `4273/5111/5123/5221` 端口重新启动试玩；`agent-browser` 验证中文主入口不再出现 `missing env variable: OASIS7_LLM_MODEL` / `llm_required`，`stage=active`，`Advance 1 step` 与 `Resume live play` 均为 `ready`；执行一次 `单步 x1` 后页面反馈为 `completed_advanced`；切换到 English 后 URL 正确变为 `locale=en`，标准 Viewer 链接仍保持可用。
- 遗留事项: 当前 LLM 配置属于本地 ignored state，不进入 Git；试玩栈仍在运行，需停止时对当前 `run-game-test.sh` 会话发送 `Ctrl+C`。
