# task_fb2de7d6d5c943a08857d6ec659ff8c6 Execution Log

- task_uid: task_fb2de7d6d5c943a08857d6ec659ff8c6
- title: Deploy live candidate public-testnet endpoints and reassess lanes
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-public-testnet-live-candidate-endpoint-deploy

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-19 09:18:19 CST / runtime_engineer
- 完成内容:
  - 核对 `~/Documents/keys/aliyun_ecs.txt` 后，确认两台 ECS 上已存在 `oasis7-testnet-sequencer.service` / `oasis7-testnet-storage.service` 与本机 `oasis7-testnet-observer.service`，其 `world_id` 为 `oasis7-public-testnet-parallel-20260518`。
  - 发现现网 testnet 虽为三节点 full runtime，但最初未加载 formal `network-tier-manifest`，且 `t2t.oasis7.tech` 仅代理 provider 路由，不足以直接确认 `public_rpc_ready` / `explorer_public_ready`。
  - 更新 repo-owned `scripts/p2p-triad-node-start.sh`，新增 `NETWORK_TIER_MANIFEST_PATH` 支持；同时更新 `scripts/provider-remote-https/t2t.oasis7.tech.nginx.conf`，补 `/healthz`、`/v1/chain/status`、`/v1/chain/explorer/` 的代理入口。
  - 生成 live-candidate bundle / manifest / bootstrap peers，并下发到两台 ECS 的 `/opt/oasis7/p2p-testnet/config/`。
  - 替换两台 ECS 的 `/opt/oasis7/p2p-testnet/bin/start-node.sh`，写入 `NETWORK_TIER_MANIFEST_PATH=/opt/oasis7/p2p-testnet/config/network-tier-public-testnet-live-candidate.json` 后重启 `oasis7-testnet-sequencer.service` 与 `oasis7-testnet-storage.service`。
  - 确认两台 ECS 重启后 `/v1/chain/status` 都返回 `network_tier.tier=public_testnet`、`status=rehearsal`、`bootstrap_peer_count=3`、`last_error=null`。
  - 更新 `39.104.205.67` 上的 `t2t.oasis7.tech` nginx 配置并 reload；从 storage 主机本机可确认 `https://t2t.oasis7.tech/v1/chain/status` 返回 `HTTP/2 200`。
  - 新增证据文档 `doc/testing/evidence/public-testnet-live-candidate-endpoint-deploy-2026-05-19.md`，明确这次推进后的 lane 边界。
- 遗留事项:
  - 当前控制机对 `https://t2t.oasis7.tech/v1/chain/status` 与 `/v1/chain/explorer/overview` 仍出现 TLS `connection reset by peer`，因此 `public_rpc_ready` / `explorer_public_ready` 仍不能确认 `pass`。
  - `faucet_guard_ready`、`reset_policy_announced`、`claims_boundary_review`、`shared_devnet_pass` 仍未被这次部署补齐。

## 2026-05-19 09:34:25 CST / runtime_engineer
- 完成内容:
  - 根据用户更正，确认 `t2t.oasis7.tech` 不是 testnet 专用域名，本轮已撤回所有把 `t2t` 当作 testnet endpoint 的口径。
  - 回滚 repo 内 `scripts/provider-remote-https/t2t.oasis7.tech.nginx.conf` 的 testnet 链路代理改动，恢复为 provider-only 模板。
  - 将两台 ECS `/opt/oasis7/p2p-testnet/config/network-tier-public-testnet-live-candidate.json` 的 endpoint refs 改回 placeholder：
    - `https://public-testnet.example.invalid/rpc`
    - `https://public-testnet.example.invalid/explorer`
    - `https://public-testnet.example.invalid/faucet`
  - 重启两台 `oasis7-testnet-*` service，确认 `network_tier.tier=public_testnet` 仍正常加载，但 live status 已不再引用 `t2t`。
  - 重跑 `network-tier-public-testnet-readiness.sh`，最新结果回到：
    - `gate_result=block`
    - `readiness_verdict=block`
    - `live_candidate_allowed=false`
- 遗留事项:
  - 当前仍无 testnet 专用公网域名或公网 endpoint，因此 `public_rpc_ready` / `explorer_public_ready` 只能保持 `partial`。
  - 后续若要继续推进公网 lane，需要先分配正确的 testnet 专用入口，再重新布置 manifest / 代理 / 验证证据。

## 2026-05-19 09:53:54 CST / runtime_engineer
- 完成内容:
  - 进一步采用公网 `IP:port` 直连方式推进 testnet 入口，而不复用非 testnet 专用域名。
  - 将两台 ECS `config/node.env` 的 `STATUS_BIND` 分别调整为 `0.0.0.0:6631` 与 `0.0.0.0:6632`，保留 `NETWORK_TIER_MANIFEST_PATH=/opt/oasis7/p2p-testnet/config/network-tier-public-testnet-live-candidate.json`。
  - 将 formal manifest endpoint refs 收口为：
    - `rpc_ref=http://39.104.204.172:6631/v1/chain/status`
    - `explorer_ref=http://39.104.205.67:6632/v1/chain/explorer/overview`
    - `faucet_ref=https://public-testnet.example.invalid/faucet`
  - 从当前控制机直接验证：
    - `curl http://39.104.204.172:6631/v1/chain/status` 返回 `ok=true`
    - `curl http://39.104.205.67:6632/v1/chain/status` 返回 `ok=true`
    - `curl http://39.104.205.67:6632/v1/chain/explorer/overview` 返回 `ok=true`
  - 因此将 `public_rpc_ready` 与 `explorer_public_ready` 两条 lane 提升为 `pass`，并同步更新 evidence / readiness lanes。
- 遗留事项:
  - `faucet_guard_ready` 仍缺 guarded faucet 与 abuse control 验证。
  - `reset_policy_announced`、`claims_boundary_review`、`shared_devnet_pass` 仍未被这次部署补齐，因此整体 readiness 仍不能放行。
