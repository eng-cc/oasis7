# task_4261de9e42ac422c9ecc63525740fbb9 Execution Log

- task_uid: task_4261de9e42ac422c9ecc63525740fbb9
- title: close issue 160 first capability gate after PostOnboarding
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-game-issue-160-first-capability-gate

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-16 21:24:00 CST / runtime_engineer
- 完成内容: 已为 `#160` 新建独立 worktree `/home/scc/worktrees/oasis7-game-issue-160-first-capability-gate`，并原子创建 `.pm` task `task_4261de9e42ac422c9ecc63525740fbb9`；同时补读 `PRD-GAME-012` retention 专题 PRD/project、`doc/game/project.md` 与相关 runtime follow-up execution logs，将本次任务入口收敛为“在当前 main truth 上复跑 active-LLM formal lane，并继续追 `first capability gate` 的现行 blocker”。
- 完成内容: 首轮 worktree harness 预热未进入 Viewer ready。已从 `output/harness/wt-feb7343d/startup.log` 确认启动失败签名为 `crates/oasis7_viewer` 缺少前端依赖，`npm run build:software-safe` 调起 `vite` 时返回 `sh: 1: vite: not found`；该失败属于新 worktree 环境未闭环，不是 `#160` gameplay blocker。
- 完成内容: 已在目标 worktree 的 `crates/oasis7_viewer/` 执行 `npm ci` 补齐前端依赖，并重启 `./scripts/worktree-harness.sh up --with-llm`，当前正等待隔离栈进入 ready 后执行 fresh active-LLM formal sample。
- 遗留事项: 等待 worktree harness 启动完成并拿到 `viewer_url`；随后需先刷新当前 `trust gate` 真值，再按结果决定是否直接进入 `30` 分钟 capability follow-up 样本或继续定位 repo-side runtime blocker。

## 2026-05-16 22:18:00 CST / runtime_engineer
- 完成内容: 已补 viewer live 链同步回归测试，并先确认两个狭义修复：其一，`chain_link.rs` 现在在链状态声明 `dev_local` policy 时对读回来的 execution world 执行与 chain runtime 一致的 main-token 配置归一化；其二，`chain_status.consensus.committed_height == 0` 时，viewer 不再把 bootstrap/reward execution world 当成 authoritative committed world 去加载。对应新增/通过的定向测试包括 `chain_linked_runtime_dev_local_policy_normalizes_main_token_config_before_verifying_tick_consensus`、`chain_linked_runtime_committed_height_zero_skips_bootstrap_execution_world_validation`，并复核 `chain_linked_runtime_sync_advances_without_play`、`chain_linked_runtime_empty_poll_does_not_advance_world` 仍通过。
- 完成内容: 用 fresh formal sample `output/playwright/retention-active-llm-formal/issue160-trust-refresh-fix2/` 重新验证 `#160` 当前真值。结果显示旧的伪 blocker 已被拆除：`state-post-onboarding-entry.json` 不再进入 `post_onboarding.recover_capability / 68% / latest state root mismatch tick=1`，而是正常回到 `post_onboarding.establish_first_capability / 20% / executing`；`startup-issue160-trust-refresh-fix2/oasis7_viewer_live.log` 在 `committed_height=0` 的 bootstrap 阶段也不再出现 `chain sync skipped: latest state root mismatch`。
- 完成内容: same fresh sample 的中后段已暴露新的真实 capability blocker：从 `state-play-samples.jsonl` 第 9 条样本开始，stage 进入 `post_onboarding.recover_capability / 68%` 时的 `blockerDetail` 已切换成外部 LLM provider 并发限流，签名为 `decode response failed: responses sdk decode failed (primary request): {"error":{"type":"rate_limit_error","message":"Concurrency limit exceeded for account, please retry later"}}`；该签名也在 `startup-issue160-trust-refresh-fix2/oasis7_viewer_live.log` 中落地。
- 遗留事项: `#160` 仍未闭环，但当前 repo-side runtime truth 已从“bootstrap 零高度 chain-sync 误判”收敛到“active-LLM capability 路径被外部 provider concurrency limit 打断”。下一步需决定是继续在本任务内做 provider/backpressure/retry 降并发修复，还是将本次 issue 真值先回写后再开下一个更窄实现切片。

## 2026-05-17 06:05:00 CST / runtime_engineer
- 完成内容: 已继续在同一任务内完成两层 repo-side capability 路径修复。第一层在 `viewer/runtime_live` 补齐 simulator 工业动作桥接：`BuildFactory`/`ScheduleRecipe` 不再落入 `_ => None`，而是复用 `viewer/gameplay_actions.rs` 的 canonical factory spec / recipe plan 生成 runtime action；同时把 `gameplay_actions` 重构成 crate 内 helper，并新增/通过 `runtime_simulator_action_mapping_includes_industrial_actions`，确认自定义 `factory_id`、`site_id` 与 `batches` 缩放都能正确映射。第二层在 provider-backed lane 补齐 capability 输入：`llm_sidecar_provider.rs` 的 `provider_phase1_action_catalog()` 新增 `harvest_radiation`、`mine_compound`、`refine_compound`、`build_factory`、`schedule_recipe`，并给 runtime_live provider 行为注入持久 `post_onboarding.establish_first_capability` 工业 guidance；`decision_provider.rs` 同步改成保留 base memory summary，不再被第一条 action feedback 全量覆盖。
- 完成内容: 已补/通过与上述修复对应的回归：`runtime_step_control_requests_llm_decision_and_advances_with_provider_backed_loopback` 现在会解码真实发往 provider 的 `DecisionRequest`，断言 action catalog 已暴露工业/恢复动作且 `memory_summary` 含 `build_factory(factory.smelter.mk1)` 与 `schedule_recipe(...)` 指引；`provider_backed_agent_behavior_executes_mocked_move_and_records_feedback` 与 `runtime_simulator_action_mapping_includes_industrial_actions` 也继续通过。
- 完成内容: 已连续复跑三轮 fresh formal sample 作为真值验证。`fix5`（仅桥接修复，`output/playwright/retention-active-llm-formal/issue160-trust-refresh-fix5/`）仍为 `trust_gate=pass / firstCapability=watch / progress=20% / maxLogicalTime=78 / maxEventSeq=16`；`fix6`（加 provider industrial catalog，`.../issue160-trust-refresh-fix6/`）仍为 `watch / 20% / maxLogicalTime=86 / maxEventSeq=18`；`fix7`（再加持久工业 guidance，`.../issue160-trust-refresh-fix7/`）仍为 `watch / 20% / maxLogicalTime=84 / maxEventSeq=18`。三轮 artefact 中都未出现 `BuildFactory`、`ScheduleRecipe`、`FactoryBuilt`、`RecipeScheduled` 等实际工业执行信号，只有 UI/action-matrix 中的 `build_factory_smelter_mk1` 可用入口文本。
- 遗留事项: `#160` 的 repo-side blocker 已从“桥接缺失/能力 catalog 缺失”进一步收敛到“provider-backed active-LLM 在 formal lane 中仍未产出工业决策或未暴露可追踪的工业决策 trace”。当前代码已经保证 runtime 能接工业动作、provider 请求也已携带工业动作与 post-onboarding guidance，但 formal sample 仍长期停在 `post_onboarding.establish_first_capability / 20%`。下一步更像需要补 provider-backed 决策 trace 落盘/观测，或继续核查 provider contract/prompt 是否仍缺少比 builtin `LlmAgentBehavior` 更关键的工业阶段上下文。

## 2026-05-17 08:08:00 CST / runtime_engineer
- 完成内容: 已在 `crates/oasis7/src/simulator/llm_agent/behavior_runtime_helpers.rs` 把 `environment.current_observation` 从“只回原始 observation”扩成“原始 observation + build-ready 派生层”。新增字段覆盖 `current_location_id/current_location_name`、默认首个 smelter 建造成本、`can_build_factory_smelter_mk1_now`、`missing_build_prerequisites`、`build_ready_summary`、`recommended_build_factory_action`、`recommended_schedule_recipe_action` 与已知工厂摘要，目的是把 active-LLM 在 `20%` 阶段缺失的“当前位置可建造上下文”直接显式化给模型。
- 完成内容: 已在 `crates/oasis7/src/simulator/llm_agent/tests_part3_module_lifecycle.rs` 新增 `llm_agent_current_observation_module_exposes_build_ready_context`，定向锁定 colocated location、`can_build_factory_smelter_mk1_now=true`、空 `missing_build_prerequisites` 与推荐 `build_factory` 模板这几项关键输出；并已通过 `rtk cargo fmt --all` 与 `rtk cargo test -p oasis7 llm_agent_current_observation_module_exposes_build_ready_context --features test_tier_required`。
- 完成内容: 已用短样本 `output/playwright/retention-active-llm-formal/issue160-trust-refresh-fix10-build-context/` 验证新上下文层的行为变化：formal lane 不再只停在 `post_onboarding.establish_first_capability / 20%`，而是首次出现真实 `BuildFactory` / `ScheduleRecipe` trace，并在约 `181s` 内推进到 `post_onboarding.stabilize_first_line_after_output / 80%`；对应 summary 仍为 `firstCapability=watch`，但真值已经从“工业动作未发生”切换成“首条产线已建立，尚未跑到更长 capability 窗口终点”。
- 完成内容: 已继续跑 `900s` capability 窗口样本 `output/playwright/retention-active-llm-formal/issue160-trust-refresh-fix11-capability-window/`，并拿到当前任务最关键的 fresh formal truth：`trustGateResult=pass`、`firstCapabilityResult=pass`、`playDurationMs=907415`、`finalGoalId=post_onboarding.choose_first_expansion_tradeoff`、`finalProgressPercent=92`、`maxLogicalTime=76`、`maxEventSeq=103`。同一 artefact 的 `state-play-samples.jsonl` 证明 active-LLM lane 已能经由真实 `build_factory(factory.smelter.mk1)` 与后续 `schedule_recipe(...)` 从 `20%` capability entry 推进到 `92%` 扩产取舍阶段，不再停留在旧的 `20%` blocker。
- 遗留事项: runtime-side `#160` blocker 已拿到 fresh pass evidence；若要正式 close issue/提交，需要继续把这轮新真值同步到对应 GitHub issue / 项目文档 / task closeout 流，并决定是否补一条更正式的 capability evidence 文档而不是仅保留 artefact 与 execution log。

## 2026-05-17 18:43:00 CST / runtime_engineer
- 完成内容: 已处理 PR `#231` 的两条 review comments。`crates/oasis7/src/runtime/tests/persistence_recovery_tests.rs` 中的 issue160 样本恢复测试不再静默依赖 `output/` 目录存在，而是改成显式 `OASIS7_VERIFY_ISSUE160_SAMPLE_WORLD=1` gate；只有操作者明确开启该 gate 时才要求样本 execution world 必须存在，从而把非 CI 固化样本的跳过语义变成有意行为。
- 完成内容: 已在 `crates/oasis7/src/viewer/runtime_live/llm_sidecar.rs` 恢复 runtime-live 与 `viewer_live` 一致的默认 `30s` LLM timeout ceiling，避免未配置环境变量时 silently 放大到 provider 全局 budget；如需 formal retention lane 的更长预算，则由 `scripts/collect-active-llm-retention-sample.sh` 在自举隔离栈时显式导出 `OASIS7_RUNTIME_LIVE_LLM_TIMEOUT_MS=180000` opt-in。
- 完成内容: 已通过针对 comment 的定向验证：`env -u RUSTC_WRAPPER cargo test -p oasis7 runtime_live_llm_timeout --features test_tier_required`、`env -u RUSTC_WRAPPER cargo test -p oasis7 sample_issue160_execution_world_matches_tick_consensus_after_distfs_restore --features test_tier_required`、`bash -n scripts/collect-active-llm-retention-sample.sh`。
- 遗留事项: 需要继续完成本次 follow-up 的 commit / push / thread resolve，并复核 PR `#231` 的 `reviewDecision` 与 `mergeStateStatus` 是否从 `REVIEW_REQUIRED / BLOCKED` 进入下一状态。
