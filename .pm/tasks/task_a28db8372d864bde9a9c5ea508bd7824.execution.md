# task_a28db8372d864bde9a9c5ea508bd7824 Execution Log

- task_uid: task_a28db8372d864bde9a9c5ea508bd7824
- title: Reduce peer-record request traffic waste
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-runtime-peer-record-request-backoff

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-16 16:20:00 CST / runtime_engineer
- 完成内容: 为 `libp2p_net` 的 `get_local_peer_record`、`get_cached_peer_record`、`get_cached_discovery_peers` 增加 peer-scoped 短时协议冷却；在断连清理中同步移除对应 peer 冷却状态，避免连接恢复后被旧窗口误抑制；保留 cached-peer-record 单次请求链里的多 proxy fallback。补充 `discovery_peer_record_tests.rs` 定向回归，覆盖三类请求的窗口内抑制、窗口过后恢复，以及既有 fallback 语义不回退。同步回写 `doc/world-runtime/prd.md`、`doc/world-runtime/project.md`，新增 `PRD-WORLD_RUNTIME-030` 与项目追踪条目。
- 验证命令: `env -u RUSTC_WRAPPER cargo fmt --all`；`env -u RUSTC_WRAPPER cargo test -p oasis7_net discovery_peer_record_ -- --nocapture`；`env -u RUSTC_WRAPPER cargo test -p oasis7_net maybe_request_ -- --nocapture`；`env -u RUSTC_WRAPPER cargo check -p oasis7_net --tests`；`./scripts/doc-governance-check.sh`；`git diff --check`
- 遗留事项: 完成本地 task close / lint / commit / PR 准备后，再决定是否继续把当前分支部署到本机与两台 ECS 节点做最近 10 分钟流量对比。

## 2026-04-16 23:22:00 CST / runtime_engineer
- 完成内容: 按 PR #102 review comment 为 `clear_disconnected_peer_state()` 补充定向回归，显式覆盖三类 peer-record/discovery cooldown 在断连清理前会抑制立即重试、清理后会立刻移除 suppression 并允许重新发起请求。
- 验证命令: `env -u RUSTC_WRAPPER cargo test -p oasis7_net clear_disconnected_peer_state_removes_peer_record_cooldowns -- --nocapture`；`env -u RUSTC_WRAPPER cargo test -p oasis7_net discovery_peer_record_ -- --nocapture`
- 遗留事项: 待补一次 `cargo check -p oasis7_net --tests`、`./scripts/pm/lint.sh` 并推送后，再回看 PR #102 的 mergeability / review thread 状态。
