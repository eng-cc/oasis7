# task_d741e28cd0344ae6a8b8a3fd72e97976 Execution Log

- task_uid: task_d741e28cd0344ae6a8b8a3fd72e97976
- title: extend rust-size gate coverage beyond crates-only scan
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-engineering-rust-size-tools-gate-coverage

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-20 16:21:57 CST / runtime_engineer
- 完成内容: 将 `scripts/check-rust-file-size.sh` 的零扫描范围从 `crates/**` 扩到 tracked 的首方 `tools/**` Rust 文件，并保持 `third_party/**`、`vendor-*`、`target/**` 等 vendored/生成目录不纳入治理；同步回写 `doc/engineering/project.md` 与 `doc/engineering/rust-governance/rust-1200-line-root-cause-governance-2026-03-29.{prd,project}.md`，补齐本次 follow-up task trace 与当前状态。
- 完成内容: 针对新纳入范围后暴露出的 `tools/wasm_build_suite/src/lib.rs` 1218 行热点，将 compile-time/env prefix 相关测试抽到新文件 `tools/wasm_build_suite/src/env_prefix_tests.rs`，把主文件压回 1109 行，恢复仓库在扩大扫描范围后的零扫描状态。
- 测试: `env -u RUSTC_WRAPPER cargo test --manifest-path tools/wasm_build_suite/Cargo.toml --lib`；`env -u RUSTC_WRAPPER cargo fmt --manifest-path tools/wasm_build_suite/Cargo.toml --all -- --check`；`./scripts/check-rust-file-size.sh`；`./scripts/doc-governance-check.sh`；`bash -n scripts/check-rust-file-size.sh`；临时 git repo smoke 验证 `tools/**/*.rs` 超限会被门禁拦截且 `vendor-*` 不会被误报。
- 遗留事项: 若后续新增首方 Rust 顶层目录超出当前 `crates/**` / `tools/**` 范围，需要同步扩展 rust-size gate 与 rust-governance 文档，而不是默认为自动覆盖。
