# task_5ba09996375b49ceb2bc22512a5ee48d Execution Log

- task_uid: task_5ba09996375b49ceb2bc22512a5ee48d
- title: mvp playtest readiness hardening
- owner_role: producer_system_designer
- worktree_hint: /Users/scc/ccwork/worktrees/oasis7-world-simulator-mvp-playtest-readiness-hardening

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
  - Action: ...
  - Validation Command: ...
  - Expected Result: ...
  - Actual Result: ...
  - Blocker / Next Action: ...
-->

## 2026-06-01 11:05:00 CST / producer_system_designer
- 完成内容: 基于 `run-producer-playtest.sh` / `run-game-test.sh` / `software_safe-playability-unblock` 与 `simulator-kernel-persistence-state-hardening` 现状完成 execution gap review，确认当前“必须做”优先收口两类问题：一是 producer 试玩入口把“HTTP/bridge 已起”误当成“可玩 ready”；二是 `doc/world-simulator/project.md` 仍把两个已完成 `.pm` 任务留在未勾选状态，造成 MVP 试玩缺口判断漂移。
- 遗留事项: 需要验证新的 ready gate 是否能在“非空 snapshot”时放行、在空实体快照时阻断，并补齐 project truth 回写后的基础脚本验证。
- Action: 先读 `doc/world-simulator/project.md`、相关 `.pm` task 与执行日志，确认 `task_1c5ac527bed54e969b737137fc998ab8`、`task_6780f8bf31a042dea2c929673ef8db40` 均已 `done`；同时检查 `run-game-test.sh`，定位到它当前只等待 viewer HTTP 与 web bridge listener，就会把 stack 标成 ready。
- Validation Command: `sed -n '1,80p' doc/world-simulator/project.md`；`sed -n '1,220p' .pm/tasks/task_1c5ac527bed54e969b737137fc998ab8.{yaml,execution.md}`；`sed -n '1,220p' .pm/tasks/task_6780f8bf31a042dea2c929673ef8db40.{yaml,execution.md}`；`sed -n '520,760p' scripts/run-game-test.sh`
- Expected Result: 明确 must-do 缺口已从“功能未做”收敛为“ready gate 不够严格 + project 汇总文档未同步”。
- Actual Result: 两个 `.pm` 任务均已完成并各自带有验证记录，但 `doc/world-simulator/project.md` 仍保留未勾选条目；`run-game-test.sh` 的 ready 逻辑确实只检查 HTTP 与 TCP listener，没有等待 viewer 连接后的非空快照。
- Blocker / Next Action: 无硬 blocker；下一步直接补脚本 ready gate，并同步 project truth 勾选状态。

## 2026-06-01 11:18:00 CST / producer_system_designer
- 完成内容: 已为 producer/playtest 主链补齐“可玩 snapshot ready gate”，并同步 `world-simulator` project truth，把两个已完成 must-do 任务改回 `[x]`。producer 入口现在默认透传 `--require-playable-snapshot`，`run-game-test.sh` 会在报 ready 前确认 `__AW_TEST__` 已连接且 snapshot 中同时存在 `agents` 与 `locations`；若超时仍为空，则直接把 `runtime_snapshot_empty_entities` 状态落盘并失败退出。
- 遗留事项: 当前环境没有现成 LLM/provider 配置，未在真实 launcher+runtime+LLM 栈上补跑完整 L4B 试玩；但已用 mocked bundle/agent-browser readiness harness 验证成功态与失败态分支。
- Action: 修改 `scripts/run-game-test.sh`、`scripts/run-producer-playtest.sh` 与 `doc/world-simulator/project.md`；新增 `--require-playable-snapshot` / `--playable-snapshot-timeout`，并把最后一次 readiness state 写入 `playable-snapshot-state.json`。
- Validation Command: `bash -n scripts/run-game-test.sh && bash -n scripts/run-producer-playtest.sh`；`./scripts/run-game-test.sh --help | sed -n '1,120p'`；`./scripts/run-producer-playtest.sh --help | sed -n '1,120p'`；`PATH=\"$PWD/.tmp/mvp_playtest_ready_mock/fakebin:$PATH\" FAKE_AGENT_BROWSER_STATE_JSON='{\"hasTestApi\":true,\"connectionStatus\":\"connected\",\"agents\":1,\"locations\":1,\"blockerKind\":null,\"lastError\":null}' ./scripts/run-game-test.sh --bundle-dir \"$PWD/.tmp/mvp_playtest_ready_mock/bundle\" --allow-stale-bundle --skip-llm-provider-preflight --require-playable-snapshot --chain-disable --viewer-port 43173 --web-bind 127.0.0.1:43111 --live-bind 127.0.0.1:43223 --json-ready`；`PATH=\"$PWD/.tmp/mvp_playtest_ready_mock/fakebin:$PATH\" FAKE_AGENT_BROWSER_STATE_JSON='{\"hasTestApi\":true,\"connectionStatus\":\"connected\",\"agents\":0,\"locations\":0,\"blockerKind\":\"runtime_snapshot_empty_entities\",\"lastError\":null}' ./scripts/run-game-test.sh --bundle-dir \"$PWD/.tmp/mvp_playtest_ready_mock/bundle\" --allow-stale-bundle --skip-llm-provider-preflight --require-playable-snapshot --playable-snapshot-timeout 2 --chain-disable --viewer-port 43174 --web-bind 127.0.0.1:43112 --live-bind 127.0.0.1:43224`
- Expected Result: 脚本语法正常、帮助文本显式暴露新 gate；mock 成功态会输出 `Game test stack is ready`，mock 失败态会以非零退出并打印 `runtime_snapshot_empty_entities` 的 readiness state，而不是继续误报 ready。
- Actual Result: 语法检查与帮助文本均通过；mock 成功态正常输出 ready JSON 与 `Game test stack is ready`；mock 失败态以退出码 `1` 终止，并打印 `viewer stack reached HTTP/bridge ready, but did not expose a non-empty playable snapshot in time`，同时落出 `agents=0`、`locations=0`、`blockerKind=runtime_snapshot_empty_entities` 的 readiness state。
- Blocker / Next Action: 无仓库内 blocker；若要拿真实 L4B 试玩 PASS，还需在有有效 LLM/provider 配置的环境里重跑 producer playtest 主链。

## 2026-06-01 12:15:00 CST / producer_system_designer
- 完成内容: 按用户要求删除正式 direct LLM 使用流程，把 launcher、runtime live、client launcher、producer/playtest 脚本与 Viewer 错误提示收敛到 `provider_backed + remote_https`。`run-game-test.sh` 不再调用 removed direct LLM provider probe / `oasis7_llm_provider_probe`，改为 remote provider contract preflight；正式入口需要 `--newapi-user-ref`、`--bridge-user-id` 或 `--agent-provider-auth-token`，并自动下发 `newapi_user_ref:<ref>` 到 provider bridge。已删除 direct LLM probe 脚本与 Rust probe bin。
- 遗留事项: 已被后续 12:50 清理替代；游戏端 direct LLM 基础实现不再保留。
- Action: 修改 `oasis7_game_launcher` 默认 decision source、provider env 注入与测试；修改 `runtime_live` 只解析 provider-backed settings；修改 `oasis7_client_launcher` 默认 remote provider；修改 `run-game-test.sh`/producer/playtest/runbook/docs；同步 Viewer feedback 文案；移除旧 direct provider probe 脚本与 Rust probe bin。
- Validation Command: `bash -n scripts/run-game-test.sh && bash -n scripts/run-producer-playtest.sh && bash -n scripts/release-gate-web-strict.sh && bash -n scripts/viewer-pixel-world-wasm-regression.sh && bash -n scripts/prepare-playability-l4-review.sh && bash -n scripts/provider-parity-p0.sh`; `node crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib provider_settings_from_env -- --nocapture --test-threads=1`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_provider_parity_bench -- --nocapture`; `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher build_launcher_args_contains_llm_and_no_open_switches -- --nocapture`; `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher launch_config_defaults_enable_llm -- --nocapture`; mock remote-provider argument assertion; `./scripts/doc-governance-check.sh`; `./scripts/pm/lint.sh`; `git diff --check`.
- Expected Result: Direct LLM preflight/launcher decision source 不再是正式入口；remote provider selector 缺失时 fail-fast；有 NewAPI selector 时 `run-game-test` 透传 `provider_backed remote_https` 与 `newapi_user_ref:<ref>`；相关 Rust/JS/script/doc checks 通过。
- Actual Result: 上述验证均通过。`oasis7_game_launcher` 98 passed / 1 ignored；runtime provider settings 4 passed；provider parity bench 15 passed；client launcher 两个 focused tests 均 passed；Viewer feedback contract passed；脚本语法、doc governance、pm lint、diff check 均通过；mock 验证确认无 selector fail-fast 且 `--newapi-user-ref user-1` 转成 `--agent-provider-auth-token newapi_user_ref:user-1`。
- Blocker / Next Action: 无仓库内 blocker；真实云端试玩仍需要操作者提供有效 `newapi_user_ref` 或等价 bearer selector，并确保 `https://t2t.oasis7.tech/v1/provider/info` 可访问。

## 2026-06-01 12:50:00 CST / producer_system_designer
- 完成内容: 按用户确认“对游戏端彻底废弃 direct model env”，删除 direct LLM agent 模块、direct demo bin、direct stress/smoke/diff 脚本，并把 provider parity bench、viewer live direct runner 与相关测试改为 provider-backed/removed 口径。
- 遗留事项: 云端 bridge 服务内部如仍需上游模型配置，应使用服务侧命名与部署配置，不再由游戏端暴露 direct model env。
- Action: 删除 `simulator` direct LLM 模块与 demo/stress 入口；移除 provider parity builtin runner；viewer live direct LLM mode 现在返回 removed 错误；更新 `ci-tests.sh`、`testing-manual.md`、world-simulator PRD/project 与旧 direct LLM 专题文档。
- Validation Command: `env -u RUSTC_WRAPPER cargo check -p oasis7 --lib --bins`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib --no-run`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_provider_parity_bench -- --nocapture`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`; `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher build_launcher_args_contains_llm_and_no_open_switches -- --nocapture`; `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher launch_config_defaults_enable_llm -- --nocapture`; `node crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs`; script `bash -n` bundle; `./scripts/doc-governance-check.sh`; `./scripts/pm/lint.sh`; `git diff --check`; direct LLM keyword sweep.
- Expected Result: 游戏端源码/脚本不再提供 direct LLM 配置或执行入口；lib/bins 与 lib tests 可编译；provider-backed launcher/provider/viewer tests 和治理检查通过。
- Actual Result: 全部通过；provider parity bench 11 passed，game launcher 98 passed / 1 ignored，client launcher focused tests passed，Viewer feedback contract passed，doc governance OK，pm lint OK，diff check OK，direct LLM keyword sweep无结果。
- Blocker / Next Action: 无仓库内 blocker；真实云端试玩仍需有效 NewAPI/provider selector。

## 2026-06-01 17:02:46 CST / producer_system_designer
- 完成内容: 按用户确认的架构边界继续收口：OpenAI-compatible client 能力保留在 NewAPI/remote provider bridge 服务侧，游戏端不再保留 direct OpenAI SDK 依赖。已从 `crates/oasis7` 移除 `async-openai`，刷新 `Cargo.lock`，并把历史 direct client design/project 文档改为 tombstone，明确不得作为恢复游戏端直连客户端的依据。
- 遗留事项: NewAPI bridge / remote provider bridge 仍需要服务侧上游调用能力；该能力当前由 provider bridge HTTP adapter 调用 OpenAI-compatible `/chat/completions` 承接，不属于游戏端 runtime/viewer/launcher。
- Action: 修改 `crates/oasis7/Cargo.toml` 与 `Cargo.lock`；更新 `doc/world-simulator/llm/llm-async-openai-responses.{design,project}.md` 为 retired tombstone；补 `scripts/provider-remote-https/letai_provider_cli.py` 的 annotations future import，让 provider CLI tests 在当前 Python 解释器上可导入验证。
- Validation Command: `env -u RUSTC_WRAPPER cargo check -p oasis7 --lib --bins`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_newapi_bridge_service -- --nocapture --test-threads=1`; `python3 scripts/provider-remote-https/letai_provider_cli.test.py`; `bash scripts/provider-remote-https/letai-provider-cli.test.sh`; direct client dependency keyword sweep; `./scripts/doc-governance-check.sh`; `./scripts/pm/lint.sh`; `git diff --check`.
- Expected Result: 游戏端 crate 与 lockfile 不再依赖 direct OpenAI SDK；NewAPI bridge 和 remote provider CLI 服务侧链路测试仍通过；关键词验证不再发现游戏端 direct client dependency。
- Actual Result: `cargo check` 通过，仅保留既有 `oasis7_testnet_faucet` unused import 与 `oasis7_game_launcher` dead-code warning；NewAPI bridge 单线程测试 28 passed；provider CLI Python 与 shell tests 各 3 passed；`async-openai` 已从 `Cargo.lock` 依赖图移除；doc governance、pm lint 与 diff check 均 OK。
- Blocker / Next Action: 并发运行 `oasis7_newapi_bridge_service` tests 时观察到 mock HTTP 非阻塞 socket 抖动，单线程重跑通过；如要把该 test bin 纳入并发 CI，应另开测试 harness 稳定化任务。

## 2026-06-01 18:16:29 CST / producer_system_designer
- 完成内容: 修正“direct LLM 删除过宽”的 PR 范围：恢复 `simulator::llm_agent` 作为游戏端 LLM agent 语义入口，但不恢复 direct OpenAI SDK、`OASIS7_LLM_*`、direct probe/demo/stress 或游戏端直连模型配置。runtime live 现在通过 `LlmAgentProviderConfig` 与 provider-backed builder 创建 agent，模型调用出口仍是 NewAPI/remote provider bridge。
- 遗留事项: 旧 `llm_agent` 中更重的 prompt/guardrail/parser/memory 实现仍未整包恢复；当前修复先把 agent assembly 层和 P0 provider action/memory policy 收回到 simulator 语义边界。后续若要恢复更多“大脑”能力，应继续保持 provider-neutral，并禁止恢复 direct client/env。
- Action: 新增 `crates/oasis7/src/simulator/llm_agent.rs`，提供 `LlmAgentProviderConfig`、`provider_phase1_action_catalog`、`build_provider_backed_llm_agent_behavior` 与 remote provider builder；`simulator/mod.rs` 重新导出该 agent 层；`viewer/runtime_live/llm_sidecar.rs` 改用 agent builder；删除 sidecar 内重复的 provider action catalog / memory summary。
- Validation Command: `env -u RUSTC_WRAPPER cargo fmt --all -- --check`; `env -u RUSTC_WRAPPER cargo check -p oasis7 --lib`; `env -u RUSTC_WRAPPER cargo test -p oasis7 llm_agent --lib -- --nocapture`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib -- --nocapture`; `rg "OASIS7_LLM_|async-openai|OpenAiChatCompletionClient|OpenAIConfig|AsyncOpenAiClient" crates/oasis7/src crates/oasis7/Cargo.toml Cargo.lock -n`
- Expected Result: `llm_agent` 作为 provider-backed agent 层编译并测试通过；全库测试不回退；关键词扫描不发现游戏端 direct LLM env、direct OpenAI SDK 或 direct OpenAI client。
- Actual Result: 格式检查通过；`cargo check -p oasis7 --lib` 通过；focused `llm_agent` tests 2 passed；full oasis7 lib tests 871 passed / 1 ignored；关键词扫描无结果。
- Blocker / Next Action: 无 blocker；下一步可提交并推送 PR 更新。
