# task_595a7f4d49f0440eab0ff9466b8b9be6 Execution Log

- task_uid: task_595a7f4d49f0440eab0ff9466b8b9be6
- title: upgrade stable Rust toolchain to 1.96.0 and align CI/script references
- owner_role: wasm_platform_engineer
- worktree_hint: /Users/scc/ccwork/worktrees/oasis7-scripts-rust-toolchain-upgrade

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
  - Action: ...
  - Validation Command: ...
  - Expected Result: ...
  - Actual Result: ...
  - Blocker / Next Action: ...
-->

## 2026-06-01 10:47:00 CST / wasm_platform_engineer
- 完成内容: 完成 workflow bootstrap 与路由确认；识别当前位于禁止直接改动的 `main` worktree，因此新建 `task/scripts-rust-toolchain-upgrade` worktree，并绑定 `.pm` 任务 `task_595a7f4d49f0440eab0ff9466b8b9be6`。
- 遗留事项: 需要把 stable Rust 版本从 `1.92.0` 升级到已验证的当前 stable `1.96.0`，并对齐所有 GitHub workflow / 本地脚本中的稳定版引用，同时避免误动 canonical wasm nightly `nightly-2025-12-11`。
- Action: 检查仓库内 Rust 工具链散点、读取 scripts/wasm project truth，并用 `rustup check` 验证当前 stable 升级目标。
- Validation Command: `pwd && git branch --show-current && git status --short --branch && rg -n "1\\.92\\.0|nightly-2025-12-11|RUST_TOOLCHAIN|rust-toolchain.toml" -S . --glob '!third_party/**' --glob '!target/**' && rustup check`
- Expected Result: 识别所有 stable pin 位置；确认任务必须在独立 worktree 中执行；验证可升级目标版本，并区分 stable pin 与 pinned nightly。
- Actual Result: 当前 source worktree 为 `main`；已创建 `/Users/scc/ccwork/worktrees/oasis7-scripts-rust-toolchain-upgrade` 并绑定 owner=`wasm_platform_engineer` 的 `.pm` task。仓库 stable pin 位于 `rust-toolchain.toml` 与 5 个 GitHub workflows；`rustup check` 确认 stable 可从 `1.92.0` 升级到 `1.96.0`。canonical wasm nightly 仍固定在 `nightly-2025-12-11`。
- Blocker / Next Action: 无 blocker；下一步修改 stable toolchain 引用，随后安装 `1.96.0` 并执行针对本次改动面的 fresh verification。

## 2026-06-01 11:59:57 CST / wasm_platform_engineer
- 完成内容: 将项目 stable Rust toolchain pin 升级到 `1.96.0`，同步 `rust-toolchain.toml`、CI workflow `RUST_TOOLCHAIN` 引用与 runtime 注释示例；保留 canonical wasm nightly `nightly-2025-12-11` 不变。按最终 required 链路重新生成 viewer pixel-world bridge bindgen artifacts。升级后完整 required 验证暴露两个本地测试脆弱点，已最小修复：mock newapi bridge accepted stream 恢复 blocking read，liveops grant CLI test temp dir 使用 pid/timestamp/atomic sequence 并真实创建目录。
- 遗留事项: 无已知 blocker；后续 CI 仍需在 GitHub runner 上复验 Rust 1.96.0 环境与 regenerated wasm artifacts。
- Action: 本机 `rustup default 1.96.0`；执行 targeted flake reproduction；安装 viewer 依赖并运行完整本地 required gate；补充 PM lint 与 diff whitespace 检查。
- Validation Command: `./scripts/ci-tests.sh required`
- Expected Result: 项目基础测试链路在 Rust `1.96.0` 下通过，且 generated viewer bridge artifacts 与当前 toolchain 输出一致。
- Actual Result: 通过；scope planner 跳过 hosted account local smoke 与 disabled launcher web build，`software_safe build finalized` 并更新 tracked viewer bridge bindgen outputs。
- Validation Command: `for i in {1..10}; do ./scripts/cargo-dev.sh test -p oasis7 --test newapi_reconcile_to_http -- reconcile_moves_to_manual_review_when_verification_logs_missing --exact || exit 1; done`
- Expected Result: newapi reconcile flake 在重复运行下不再出现 `WouldBlock` request read 失败。
- Actual Result: 通过 10/10。
- Validation Command: `for i in {1..10}; do ./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_liveops_grant_cli -- status_command_reports_admin_and_beneficiary_state --exact || exit 1; done && ./scripts/cargo-dev.sh test -p oasis7 --bin oasis7_liveops_grant_cli`
- Expected Result: liveops grant CLI temp dir 相关测试在重复运行与整 binary 运行下稳定通过。
- Actual Result: 通过。
- Validation Command: `git diff --check && ./scripts/pm/lint.sh`
- Expected Result: diff 无 whitespace 错误，`.pm` task metadata 与 execution log 通过 lint。
- Actual Result: 通过，`pm-lint: OK`。
- Blocker / Next Action: 准备提交并创建 draft PR。

## 2026-06-01 12:46:52 CST / wasm_platform_engineer
- 完成内容: 跟进 PR #329 手动 `Rust / full_regression` 失败。GitHub full run `26734843419` 的 `Run full test tier` 在 `oasis7 --lib` 出现 20 个 builtin wasm module timeout，失败形态集中为 `ModuleCallFailed { code: Timeout, detail: "execution exceeded max_call_ms" }`；另有 `materializer_reuses_cached_module_hash_when_manifest_hashes_do_not_match` 在 fallback compiler 不支持 `m9.experimental.cross_platform` 时未回退到 module hash index cache。已添加 full-tier 测试专用 wasm executor config，将测试 helper 的 `max_call_ms` 放宽到 15000ms，并让 materializer 仅在 fetch/compile 都失败后使用 module hash index verified cache 兜底。
- 遗留事项: 本机缺少 canonical Docker/distfs 条件，无法完整执行 builtin wasm module action 单测；需要推送后重新触发 GitHub manual full regression 复验。
- Action: 提取 failed GitHub job log；验证 materializer exact test；验证 full-tier test target 编译装配。
- Validation Command: `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib --features "test_tier_full,wasmtime" runtime::tests::builtin_wasm_materializer::materializer_reuses_cached_module_hash_when_manifest_hashes_do_not_match -- --exact --nocapture`
- Expected Result: cached module hash index 在 fallback build 不支持测试 module id 时可兜底读取 verified blob。
- Actual Result: 通过；测试仍打印 `unsupported module id: m9.experimental.cross_platform`，随后命中 cache fallback 并 pass。
- Validation Command: `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib --features "test_tier_full,wasmtime" runtime::tests::agent_default_modules::default_sensor_module_emits_observation -- --exact --nocapture`
- Expected Result: 本机可直接复现/验证 full-tier wasm module execution。
- Actual Result: 本机阻塞于缺少 Docker/canonical distfs artifact：`docker is required; canonical wasm builds no longer support host-native fallback`。该失败不同于 GitHub full 的 timeout，不能作为本地执行验证。
- Validation Command: `env -u RUSTC_WRAPPER cargo test -p oasis7 --tests --features "test_tier_full,wasmtime,viewer_live_integration" --no-run`
- Expected Result: full-tier oasis7 test targets under the same feature set compile successfully after helper refactor.
- Actual Result: 通过。
- Blocker / Next Action: 提交补丁、推送 PR 分支并重新触发 manual full regression。
