# task_ecaf912a05ff4de199539ac68b035609 Execution Log

- task_uid: task_ecaf912a05ff4de199539ac68b035609
- title: propagate main config into new task worktrees
- owner_role: producer_system_designer
- worktree_hint: /home/scc/worktrees/oasis7-engineering-task-worktree-main-config-bootstrap

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-12 10:17:14 CST / producer_system_designer
- 完成内容: 将 `scripts/new-task-worktree.sh` 调整为在 canonical `main` worktree 根存在本地 `config.toml` 时，自动复制到新建 task worktree，并在 JSON/人类摘要里暴露 source/target/copy 状态。
- 完成内容: 扩展 `scripts/pm/new-task-worktree-bootstrap-smoke.sh`，验证 canonical config 复制、目标 worktree 中 `config.toml` 与 source 内容一致、该文件仍保持 git-ignored，以及 source worktree `git status --short` 前后不变。
- 完成内容: 同步回写 `doc/engineering/prd.md` 与 `doc/engineering/project.md`，把 `new-task-worktree` 自动携带 canonical `main` `config.toml` 的工作流约束纳入 `PRD-ENGINEERING-021` 正式追踪。
- 完成内容: 已验证 `bash -n scripts/new-task-worktree.sh scripts/pm/new-task-worktree-bootstrap-smoke.sh`、`bash scripts/pm/new-task-worktree-bootstrap-smoke.sh --json`、`./scripts/doc-governance-check.sh` 与 `git diff --check`，结果通过；其中 smoke 返回 `config_copied=true`。
- 遗留事项: 若后续需要覆盖“canonical config copy 失败时自动回滚”的显式失败注入场景，可再补独立故障注入 smoke；本轮仅验证成功路径与 source-clean / git-ignored 合同。
