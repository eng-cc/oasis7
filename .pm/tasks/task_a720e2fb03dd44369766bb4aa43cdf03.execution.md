# task_a720e2fb03dd44369766bb4aa43cdf03 Execution Log

- task_uid: task_a720e2fb03dd44369766bb4aa43cdf03
- title: harden release web entry text assertions against case transforms
- owner_role: qa_engineer
- worktree_hint: /home/scc/worktrees/oasis7-testing-release-web-entry-text-casefold

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-22 12:01:21 CST / qa_engineer
- 完成内容: 复盘 GitHub Actions `Release Packages` run `26264366239`，确认唯一失败 job 为 `release-gate-web`。失败签名不是页面内容缺失，而是 `scripts/viewer-primary-web-entry-regression.sh` 对默认入口抓取的 `default_body.txt` 做了区分大小写的 `grep`；artifact 真值里实际存在 `FORMAL GAMEPLAY SUMMARY` 与 `ACTIONS NOT EXPOSED ON THIS PAGE`，说明 `agent-browser get text body` 受渲染层/CSS `text-transform` 影响返回了全大写文本，导致 gate 误报。
- 完成内容: 已把 primary entry 的两条文案断言改成大小写不敏感，并同步回写 `doc/testing/prd.md` / `doc/testing/project.md`，把这类 release Web 文案断言大小写漂移记为 testing 模块的正式 edge case 与任务追踪。
- 完成内容: 本地最小验证已通过：`bash -n scripts/viewer-primary-web-entry-regression.sh`、`./scripts/viewer-primary-web-entry-regression.sh --help`，以及直接对失败 artifact `default_body.txt` 运行新的大小写不敏感匹配，均确认修复覆盖 `FORMAL GAMEPLAY SUMMARY` / `ACTIONS NOT EXPOSED ON THIS PAGE` 的假阴性场景。
- 遗留事项: 需执行 `task-closeout`、提交、PR 合入，并在合入后重新触发 release。
