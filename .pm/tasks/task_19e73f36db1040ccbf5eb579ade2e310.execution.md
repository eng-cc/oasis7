# task_19e73f36db1040ccbf5eb579ade2e310 Execution Log

- task_uid: task_19e73f36db1040ccbf5eb579ade2e310
- title: Recover rust size gate and burn down mid high-risk files
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-engineering-rust-gate-batch-a

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-09 22:04:00 CST / runtime_engineer
- 完成内容: 确认 oversized Rust gate 当前失真源头为 `doc/.governance/rust-oversized-file-baseline.tsv` 为空；修复 `scripts/check-rust-file-size.sh` 在“空 baseline 首次回填 + 同提交更新 baseline”场景下误判全部现存超限文件为“新增超限文件”的 bootstrap 逻辑；随后回填当前仓库的 oversized baseline 真值，并把 `crates/oasis7/src/viewer/runtime_live/tests/auth_actions.rs` 从 1465 行进一步压到 1463 行，避免 `cargo fmt` 触发新的 touch-and-shrink 违规。
- 测试: `./scripts/check-rust-file-size.sh --write-baseline`；`./scripts/check-rust-file-size.sh`；`bash -n scripts/check-rust-file-size.sh`；`env -u RUSTC_WRAPPER cargo fmt --all -- --check`；`env -u RUSTC_WRAPPER cargo test -p oasis7 runtime_gameplay_action_ --features test_tier_required -- --nocapture`
- 遗留事项: 继续按 `PRD-ENGINEERING-R1200` burn-down 顺序处理剩余超限入口文件，优先 launcher / runtime_live / node 热点；本轮 `./scripts/pm/codex-review-snapshot.sh` 已完成快照 diff 与 gate 采样，但未返回最终摘要，需按工具运行环境阻断记录。

## 2026-04-09 22:05:00 CST / viewer_engineer
- 完成内容: 将 `crates/oasis7_viewer/src/egui_right_panel_player_experience.rs` 底部纯 `#[cfg(test)]` 访问器转移到独立的 `egui_right_panel_player_experience_test_api.rs`，保留原测试可见 API，但把主实现文件从 1204 行降到 1118 行，移出 oversized baseline。
- 测试: `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer egui_right_panel_player -- --nocapture`；`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer player_achievement -- --nocapture`；`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer sync_agent_chatter_bubbles -- --nocapture`；`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer first_session_summary -- --nocapture`；`env -u RUSTC_WRAPPER cargo fmt --all -- --check`
- 遗留事项: 继续寻找下一批最小成本 shrink 点，优先清理 launcher 与 runtime_live 的生产热点文件；本轮 `cargo fmt --all` 同步收掉了仓库中两处现存格式漂移（`auth_actions.rs`、`post_onboarding.rs`），以恢复 required gate。

## 2026-04-17 21:09:19 CST / runtime_engineer
- 完成内容: 将 `crates/oasis7/src/runtime/world/module_actions` 从 `include!/impl_part` 结构改为目录模块，重命名并落位为 `artifact_actions.rs`、`release_actions.rs`、`release_normalization.rs`、`release_support.rs`，移除旧 `module_actions_impl_part1.rs` / `module_actions_impl_part2.rs` 与 `module_actions.rs` 的 `include!` 入口。
- 完成内容: 处理真实子模块后的可见性收紧问题，将跨子模块共用 helper 收敛为 `pub(super)` 或 `pub(in crate::runtime::world)`；同时把 release 归一化与 shadow-hash helper 独立到 `release_normalization.rs`，将 `artifact_actions.rs` 压到 1019 行。
- 测试: `env -u RUSTC_WRAPPER cargo check -p oasis7`；`env -u RUSTC_WRAPPER cargo test -p oasis7 runtime::tests::module_action_loop:: -- --nocapture`；`./scripts/check-rust-file-size.sh`；`git diff --check`
- 遗留事项: 下一批 structural slicing / near-threshold 债建议继续处理 `crates/oasis7_node/src/lib.rs`、`crates/oasis7_net/src/libp2p_net.rs` 与 `crates/oasis7_client_launcher/src/self_guided.rs`。

## 2026-04-17 21:27:59 CST / runtime_engineer
- 完成内容: 将 `crates/oasis7_node/src/lib.rs` 从 `include!/impl_part` 切换为正常 `mod` 入口，迁移为 `node_engine_core.rs`、`node_engine_network.rs`、`node_engine_storage_challenge.rs`，退役 `lib_impl_part1.rs` / `lib_impl_part2.rs` / `lib_impl_storage_challenge.rs` 三个旧分片文件名。
- 完成内容: 针对真实子模块后的可见性收敛，将根模块、`pos_engine_gossip`、`replication_probe_gate`、`replication_state_reconcile` 与测试所需的 `PosNodeEngine` helper 明确收敛到 `pub(super)`，避免把原本默认同模块可见的 helper 扩散成更大范围公开接口。
- 完成内容: 复跑 `cargo test -p oasis7_node --lib -- --nocapture`，确认当前仍存在 3 个现存失败签名：`tests::non_sequencer_followers::runtime_network_replication_respects_topic_isolation`、`tests_hardening::runtime_fetch_handlers_reject_unsigned_fetch_request_in_signed_mode`、`tests::runtime_gossip_replication_persists_guard_across_restart`；本轮未修改其行为。
- 测试: `env -u RUSTC_WRAPPER cargo check -p oasis7_node`；`./scripts/check-rust-file-size.sh`；`git diff --check`；4 条 node engine 精确用例
- 遗留事项: 若要把 `oasis7_node --lib` 拉到全绿，需要单独开 task 处理上述 3 个失败签名。

## 2026-04-17 21:39:22 CST / runtime_engineer
- 完成内容: 将 `Libp2pNetwork::new` 内重复的 bootstrap / periodic 调度样板抽到新文件 `crates/oasis7_net/src/libp2p_net/constructor_support.rs`，新增 `schedule_periodic_republish`、`schedule_periodic_discovery_refresh`、`schedule_bootstrap_redial` 与 `enqueue_initial_bootstrap_dials`，把 `crates/oasis7_net/src/libp2p_net.rs` 从 1199 行压到 1156 行，未引入新的 structural slicing / allowlist 债。
- 测试: `env -u RUSTC_WRAPPER cargo fmt --all`；`env -u RUSTC_WRAPPER cargo check -p oasis7_net`；`cargo test -p oasis7_net --lib -- --nocapture`；`./scripts/check-rust-file-size.sh`；`./scripts/doc-governance-check.sh`；`git diff --check`
- 遗留事项: 下一批 Rust 1200 行治理继续优先处理 `viewer/runtime_live/llm_sidecar.rs`、`oasis7_web_launcher/control_plane.rs`、`oasis7_client_launcher/src/launcher_core.rs`、`oasis7_node/src/replication.rs` 与剩余高体量测试文件。

## 2026-04-17 21:53:46 CST / runtime_engineer
- 完成内容: 将 `crates/oasis7_node/src/types.rs` 内的 main-token controller binding 常量、类型、默认值、builder 与校验 helper 抽到新文件 `crates/oasis7_node/src/types/main_token_controller_binding.rs`，并在 `types.rs` 中改为模块重导出；根文件已从 1277 行降到 1069 行，未引入新的 structural slicing / allowlist 债，同时已从 `doc/.governance/rust-oversized-file-baseline.tsv` 退休对应冻结条目。
- 测试: `env -u RUSTC_WRAPPER cargo fmt --all`；`env -u RUSTC_WRAPPER cargo check -p oasis7_node`；`cargo test -p oasis7_node types::tests:: -- --nocapture`；`cargo test -p oasis7_node submit_consensus_action_payload_accepts_signed_main_token_genesis_action -- --nocapture`；`cargo test -p oasis7_node submit_consensus_action_payload_accepts_signed_main_token_treasury_action -- --nocapture`
- 遗留事项: `cargo test -p oasis7_node --lib -- --nocapture` 当前环境仍有 3 个非定向失败签名：`runtime_network_replication_respects_topic_isolation`、`runtime_gossip_replication_persists_guard_across_restart`、`runtime_fetch_handlers_reject_unsigned_fetch_request_in_signed_mode`；本轮未触碰对应 replication/network 行为。

## 2026-04-20 16:39:12 CST / runtime_engineer
- 完成内容: 将 2026-04-17 的 4 个中段 Rust-size burn-down task 合并回本 task 作为唯一的中段聚合真值，保留 execution log 逐条留痕，删除重复的 `.pm/tasks/task_{d8ad79d7...,a2ec08aa...,fbffb4f8...,5c651f03...}.{yaml,execution.md}` 文件，并同步把 project 层条目压缩成 1 个聚合 row。
- 测试: `./scripts/doc-governance-check.sh`；`git diff --check`
- 遗留事项: 中段治理已并档；`.pm/tasks` 下仅保留最终 baseline 退役 task `task_d2e428f00e5047e581061c8cb75963ef` 作为本 PR 的新增 task。
