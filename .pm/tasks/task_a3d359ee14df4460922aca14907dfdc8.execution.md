# task_a3d359ee14df4460922aca14907dfdc8 Execution Log

- task_uid: task_a3d359ee14df4460922aca14907dfdc8
- title: fix release windows invalid path blocker
- owner_role: qa_engineer
- worktree_hint: /home/scc/worktrees/oasis7-testing-release-windows-invalid-path-blocker

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-20 17:08:34 CST / qa_engineer
- 完成内容: 复盘 `Release Packages` run `24653091498` 的 Windows 失败签名，确认阻断并非打包脚本或 NSIS 缺依赖，而是 `windows-2022` 在 `actions/checkout@v6` 阶段直接报 `invalid path '.github/instructions/*.instructions.md'`，导致 `git.exe` 以 exit code `128` 退出、`publish-release` 被跳过。
- 完成内容: 将 `.github/instructions/*.instructions.md` 改名为合法路径 `.github/instructions/pr-review.instructions.md`，并顺手修正文案错字；新增 `scripts/check-windows-paths.sh`，按当前工作树最终路径集扫描 Windows 非法字符、保留名、尾随空格/点，再把该检查接入 `scripts/ci-tests.sh` 的 `run_required_gate_checks`，让 `commit` / `required` / `release-gate-runtime-core` 都能在进入 Windows `package-native` 之前阻断同类问题。
- 完成内容: 回写 `doc/testing/prd.md` 与 `doc/testing/project.md`，把“Windows 路径兼容校验”纳入 release/testing 正式口径，并记录任务 `release-windows-invalid-path-blocker (PRD-TESTING-002)` 及其验收命令。
- 验证: `bash -n scripts/check-windows-paths.sh scripts/ci-tests.sh`；`./scripts/check-windows-paths.sh`；`git diff --check`；`./scripts/ci-tests.sh commit`（通过，且日志中已实际执行新的 `./scripts/check-windows-paths.sh` gate）。
- 遗留事项: 需要在本 task 提交并通过 PR review 后，基于合入后的 `main` 重新触发 `release-packages.yml`（建议新 tag `v0.0.44` 或更高），才能真正恢复 GitHub release 发布。

## 2026-04-20 17:27:30 CST / qa_engineer
- 完成内容: 处理 PR `#127` review comments，对 `scripts/check-windows-paths.sh` 进一步收口两点：一是移除 untracked worktree 路径扫描，保持 gate 只覆盖 tracked 路径及 staged/unstaged rename/delete 后的最终集合，避免本地 `commit|required` 因无关未跟踪文件误阻断；二是补充 Windows 控制字符（0x00-0x1f）校验，避免仍有会让 Windows checkout 失败的非法路径漏过。
- 验证: `bash -n scripts/check-windows-paths.sh`；`./scripts/check-windows-paths.sh`；`git diff --check`；`./scripts/ci-tests.sh commit`（通过）。
- 遗留事项: 推送 comment fix 后需要 resolve PR review threads，并观察 `required-gate` / mergeability 状态是否恢复到仅待 review。
