# task_5eddd21920854c20a769915ac37df977 Execution Log

- task_uid: task_5eddd21920854c20a769915ac37df977
- title: Re-run software_safe formal Web evidence
- owner_role: qa_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-simulator-software-safe-formal-web-evidence

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-07 23:53:46 CST / qa_engineer
- 完成内容: 新增 `scripts/viewer-primary-web-entry-regression.sh`，补齐默认 `/`、`render_mode=auto`、显式 `render_mode=standard` 三路 browser QA 证据；修复 isolated rerun 下遗漏 `--chain-status-bind` 导致的端口冲突，并修正 `ws` 查询参数编码处的 shell/python quoting 问题。
- 完成内容: 在隔离端口上重跑主入口回归，得到 `output/playwright/viewer-primary-web-entry/viewer-primary-web-entry-20260407-235000`，确认 `/` 与 `render_mode=auto` 都落到 `software_safe`，`render_mode=standard` 保持标准 visual QA surface。
- 完成内容: 复核 `output/playwright/viewer-software-safe-step/20260407-232845`，确认 `software_safe` formal gameplay 当前不是入口 contract 回归，而是 runtime live 缺少可用 LLM provider 配置；`final_state.json` 明确给出 `blockerKind=llm_required` 与 `missing env variable: OASIS7_LLM_MODEL`。
- 完成内容: 新增 `doc/testing/evidence/software-safe-primary-web-entry-evidence-2026-04-07.md`，并把 `doc/world-simulator/viewer/viewer-web-software-safe-mode-2026-03-16.project.md`、`doc/world-simulator/project.md` 回写为“主入口 contract 证据已完成，formal gameplay 当前 blocked，release claim 继续 gated”。
- 完成内容: 按仓库流程启动独立 `spawn_agent` review 当前 diff；当前运行环境未把子 agent findings 回传到主会话，因此 owner 额外手工复核变更与验证结果，未发现新的代码/文档阻断项。
- 遗留事项: 若要把 release/current-entry 口径改成“formal Web gameplay PASS”，必须先在 QA/runtime 环境补齐可达的 `OASIS7_LLM_MODEL` 与 provider 配置，再重跑 `./scripts/viewer-software-safe-step-regression.sh` 获取 PASS 证据。
