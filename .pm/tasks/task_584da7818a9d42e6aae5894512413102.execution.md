# task_584da7818a9d42e6aae5894512413102 Execution Log

- task_uid: task_584da7818a9d42e6aae5894512413102
- title: remove hosted browser private-key persistence and add device-session contract
- owner_role: viewer_engineer
- worktree_hint: null

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-05-18 22:45:00 CST / viewer_engineer
- 完成内容: 已在 `hosted_public_join` viewer/launcher 路径落地第一刀托管身份实现，清退浏览器对 hosted player `privateKey` 的长期持久化。`hosted_player_session.rs` 现在会在 grant 中返回 `device_session_id`；`legacy_core.js` 只把 `playerId/deviceSessionId/releaseToken/sessionEpoch/issuedAtUnixMs` 写入 `localStorage`，旧版残留 `privateKey` 会在读取时被清洗掉，不再恢复到运行态。
- 完成内容: viewer 新增按需恢复逻辑：如果 hosted auth 只剩 device session handle，就在页内重新生成临时 Ed25519 keypair，再用它完成 `player_session` 注册与 `reconnect_sync`；对应 UI/feedback contract 已同步改成“hosted device session + in-memory browser session key”表述，生成产物 `software_safe.js` 也已重建。
- 完成内容: 本轮验证已通过 `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_ -- --nocapture`、`node crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs`、`npm --prefix crates/oasis7_viewer run test:ui`、`./scripts/doc-governance-check.sh` 与 `git diff --check`。
- 遗留事项: 这次只解决了“浏览器长期持有 hosted player 私钥”的 preview debt，还没有实现 hosted account、手机号/邮箱登录、KMS/HSM 托管签名后端、`signer_ref` 或 `main_token_transfer` 的正式托管执行链路；下一步应优先推进 `hosted-account-identity-broker` 与 `managed-custody-sign-api`。
