# task_bdb48338fac544849d8c681e9a7dd441 Execution Log

- task_uid: task_bdb48338fac544849d8c681e9a7dd441
- title: Check testnet health status
- owner_role: tpm
- worktree_hint: /Users/scc/ccwork/worktrees/oasis7-world-runtime-testnet-health-status

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
  - Action: ...
  - Validation Command: ...
  - Expected Result: ...
  - Actual Result: ...
  - Blocker / Next Action: ...
-->

## 2026-06-23 09:35:42 CST / tpm
- 完成内容: WORKFLOW BOOTSTRAP DECIDED. Created canonical task worktree for read-only testnet health inspection.
- 遗留事项: none.
- Repository State Impact: no intended repository product/code change; only task execution-log evidence writeback.
- Isolation Decision: source worktree `/Users/scc/ccwork/oasis7` was clean on `main`; created `/Users/scc/ccwork/worktrees/oasis7-world-runtime-testnet-health-status` on branch `task/world-runtime-testnet-health-status`.
- Task Truth: owner role `tpm`; `.pm` task `task_bdb48338fac544849d8c681e9a7dd441`; source ref `doc/world-runtime/project.md`.
- Routed Next Phase: repo-owned workflow router -> read-only professional/domain judgment with `blockchain_ops_engineer` bounded slice.
- Required Writeback: `.pm/tasks/task_bdb48338fac544849d8c681e9a7dd441.execution.md` is mandatory sink; no PRD/project edit planned unless inspection finds a repo-state follow-up.
- Action: Run bootstrap with `./scripts/new-task-worktree.sh world-runtime testnet-health-status --base main --pm-owner-role tpm ...`.
- Validation Command: `git status --short --branch`; `./scripts/new-task-worktree.sh ... --json`.
- Expected Result: dedicated worktree and committed `.pm` task exist before substantive health inspection.
- Actual Result: task/worktree created; `target` linked to shared cargo cache; task status `committed`.
- Blocker / Next Action: record route/slice contract, then collect testnet health evidence.

## 2026-06-23 09:35:42 CST / tpm
- 完成内容: WORKFLOW ROUTE DECIDED for read-only testnet health check.
- 遗留事项: none.
- Task Phase: read-only professional/domain judgment; user asked "看一下testnet的健康状态".
- Selected Workflow Skills: `repo-owned-workflow-router` for phase selection; professional bounded slice through `blockchain_ops_engineer` because chain/node health baseline and ECS/testnet operations are owned by that role.
- Skipped Workflow Skills: `bounded-brainstorming` skipped because the scope is concrete; `tdd-test-writer` skipped because no behavior change is planned; `executing-project-tasks` limited to evidence collection; `verification-before-completion` will be used only before final completion claim if needed.
- Specialist Skills Considered: no optional specialist skill selected; role card `.agents/roles/blockchain_ops_engineer.md` is the authoritative professional role input.
- Subagent Slice Plan:
  - role: `blockchain_ops_engineer`
  - slice type: `read_only_analysis`
  - intended model configuration: `.codex/config.toml` default subagent runtime `gpt-5.5-medium`
  - actual dispatched model/reasoning: inherited/unverified unless subagent tool reports exact model; tool policy exposes spawn but not guaranteed verification
  - context delivery mode: full-thread/full-history fork requested; mandatory context checklist recorded below
  - mandatory context checklist/packet: AGENTS.md workflow contract; `doc/engineering/workflow/source-of-truth.md`; `.agents/roles/blockchain_ops_engineer.md`; task yaml/execution log; canonical worktree `/Users/scc/ccwork/worktrees/oasis7-world-runtime-testnet-health-status`; branch `task/world-runtime-testnet-health-status`; user intent current testnet health status; relevant scripts `scripts/p2p-real-env-triad-snapshot.sh`, `scripts/p2p-public-testnet-capture-truth.sh`, `scripts/oasis7-node-observability-report.sh`; secret boundary for `/Users/scc/Documents/keys/aliyun_ecs.txt`
  - write scope: no code/product writes; append professional conclusion to this execution log only
  - return contract: health verdict, per-node evidence, uncertainty, blockers, recommended next actions; do not reveal secrets
  - formal sink / writeback surface: `.pm/tasks/task_bdb48338fac544849d8c681e9a7dd441.execution.md`
  - integration owner: `tpm`
  - integration order: TPM collects mechanical evidence, role slice judges health, TPM summarizes with attribution
  - context exemption: none
- Action: route after bootstrap and prepare professional slice.
- Validation Command: read workflow router, source-of-truth, role card, task yaml/execution log.
- Expected Result: task-bound professional health judgment path is documented before delegated execution.
- Actual Result: route and slice contract recorded.
- Blocker / Next Action: spawn `blockchain_ops_engineer` slice and continue non-overlapping evidence collection.

## 2026-06-23 09:42:34 CST / blockchain_ops_engineer
- 完成内容: READ-ONLY TESTNET HEALTH VERDICT from public status evidence and TPM-collected snapshot paths.
- 遗留事项: ECS auth and host-level evidence were not yet available in this early verdict; later entries supersede that uncertainty.
- Action: Review TPM-collected public health/status evidence and produce role-owned health verdict.
- Validation Command: read `.tmp/testnet-health-20260623/20260623-093705/summary.{json,md}` and `.tmp/testnet-health-20260623/public-two-sample/*.json`.
- Expected Result: classify current public testnet health without exposing secrets or mutating production state.
- Actual Result: role verdict classified the network as degraded but not fully down; sequencer readiness and faucet remained unhealthy.
- Blocker / Next Action: refresh ECS credential usage and host-level service/log evidence.
- Evidence Paths: `.tmp/testnet-health-20260623/20260623-093705/summary.md`; `.tmp/testnet-health-20260623/20260623-093705/summary.json`; `.tmp/testnet-health-20260623/public-two-sample/*.json`.
- Overall Verdict: `degraded`. Public HTTP evidence shows the sequencer/storage chain is reachable, running, liveness-ok, and at aligned committed height `700` in the TPM two/three-sample window; this is not a full chain outage. However, the sequencer remains `readiness.status=not_ready` with `replication_recent_errors` and `replication_transport_unstable`, observability is `warn`, commit age is growing, and the faucet endpoint on `39.104.204.172:6681` refuses connections.
- Per-Node Evidence: sequencer `39.104.204.172:6631` has `/healthz ok`, `running=true`, `liveness ok`, `committed_height=700`, `sync=synced`, but `ok=false`/`readiness=not_ready` across samples; failed gates shrink to replication transport/recent-error warnings. storage `39.104.205.67:6632` has `/healthz ok`, `running=true`, `liveness ok`, `committed_height=700`, `sync=synced`, `readiness=ready` in later samples, `ok=true`, but observability remains `warn` due to recent replication errors. Explorer overview on storage is `ok=true latest_height=700`.
- Uncertainty / Blockers: formal triad snapshot is blocked by collection environment/auth issues (`SSH key invalid format`, `sshpass denied`, no local `systemctl`) and must not be treated as proof that the chain itself is down. SSH host-level service/log/disk evidence could not be refreshed without valid ECS auth. Public samples show commit age increasing, so short-window chain progress beyond height `700` was not confirmed in the TPM evidence set.
- Recommended Next Actions: restore ECS SSH authentication or provide the valid key/password path, then inspect systemd service state and recent runtime logs on both hosts; continue a longer public status watch to confirm whether height advances and whether sequencer readiness clears; investigate sequencer replication transport instability; check/restart/remediate the faucet service on `39.104.204.172:6681`; only after code/ops owner approval consider service restart or testnet reseed actions.
- Secret Boundary: `/Users/scc/Documents/keys/aliyun_ecs.txt` was treated only as an authentication artifact; no secret contents are recorded.

## 2026-06-23 09:42:03 CST / tpm
- 完成内容: Collected current testnet mechanical evidence without mutating production state.
- 遗留事项: initial SSH/auth assumptions were incomplete and were corrected after the user clarified the credential file format.
- Action: Ran formal triad snapshot with a temporary 0600 copy of `/Users/scc/Documents/keys/aliyun_ecs.txt` and a temporary SSH wrapper; then sampled public status endpoints in a 3-sample window.
- Validation Command: `./scripts/p2p-real-env-triad-snapshot.sh --samples 2 --interval-secs 2 --ssh-timeout-secs 8 --out-dir .tmp/testnet-health-20260623`; public curls for `http://39.104.204.172:6631/{healthz,v1/chain/status}`, `http://39.104.205.67:6632/{healthz,v1/chain/status,v1/chain/explorer/overview}`, and `http://39.104.204.172:6681/`.
- Expected Result: same-window health/status samples identify current node readiness, height progress, and access blockers without writing secrets to artifacts or chat.
- Actual Result: formal triad snapshot wrote `.tmp/testnet-health-20260623/20260623-093705/summary.{json,md}` but remote SSH failed because the provided file is not accepted as an OpenSSH key and sshpass auth was denied; local `systemctl` is unavailable on this host. Public endpoint sampling wrote `.tmp/testnet-health-20260623/public-two-sample/*.json`. Sequencer public status: `healthz ok`, `running=true`, `liveness=ok`, `committed_height=700`, `readiness=not_ready`, latest failed gates `replication_recent_errors,replication_transport_unstable`, `sync=synced`, `observability=warn`, `ok=false`, `last_commit_age_ms` increased from about `82335` to `107191`. Storage public status: `healthz ok`, `running=true`, `liveness=ok`, `committed_height=700`, later samples `readiness=ready`, `sync=synced`, `observability=warn`, `ok=true`, `last_commit_age_ms` increased from about `82476` to `107330`. Explorer overview: `ok=true`, `latest_height=700` in all public samples. Faucet endpoint `http://39.104.204.172:6681/` refused connection.
- Blocker / Next Action: wait for `blockchain_ops_engineer` bounded slice verdict; if unavailable, final answer must label the professional-attribution limitation and avoid presenting TPM synthesis as role-owned conclusion.

## 2026-06-23 09:49:42 CST / tpm
- 完成内容: Re-read `/Users/scc/Documents/keys/aliyun_ecs.txt` as a credential inventory after user clarified the file must be read to know usage; no secret values recorded.
- 遗留事项: `p2p-real-env-triad-snapshot.sh` remote SSH stdin handling can omit later explicit nodes; manual two-node evidence was used to compensate.
- Action: Parsed the credential file structurally and used the first two redacted `target:password` entries for ECS SSH; reran status collection against actual live ports `6631/6632`; inspected systemd status read-only.
- Validation Command: redacted parser for `/Users/scc/Documents/keys/aliyun_ecs.txt`; SSH via `sshpass` to `root@39.104.204.172` and `root@39.104.205.67`; remote `curl` of `127.0.0.1:6631/6632`; remote `systemctl show/status`; no production mutation.
- Expected Result: determine the correct credential usage and replace earlier auth-blocked uncertainty with host-level evidence.
- Actual Result: credential file shape is `target:password`, not a private key. Public ECS entries are `root@39.104.204.172:<redacted len=8>` and `root@39.104.205.67:<redacted len=8>`; two additional LAN entries were not used for public testnet. Corrected evidence paths: `.tmp/testnet-health-20260623-ecs-auth/20260623-094657/summary.{json,md}`, `.tmp/testnet-health-20260623-ecs-auth/manual-20260623-094846/*`, and `.tmp/testnet-health-20260623-ecs-auth/systemd-20260623-094929/*`. Sequencer: process `oasis7_chain_runtime` listens on `0.0.0.0:6631`, height `718`, sync `synced`, network head `ready`, but `readiness=not_ready`, `ok=false`, failed gates `replication_recent_errors,replication_transport_unstable`, transport score `45`, `oasis7-triad-sequencer.service=failed` since `2026-06-06 11:15:41 CST`. Storage: process `oasis7_chain_runtime` listens on `0.0.0.0:6632`, height `718`, `readiness=ready`, `ok=true`, sync `synced`, transport stable score `85`, but `oasis7-triad-storage.service=inactive/dead` since `2026-06-06 11:15:42 CST`. Faucet: `oasis7-faucet.service` not found and public `39.104.204.172:6681` refused connection.
- Blocker / Next Action: get updated `blockchain_ops_engineer` verdict using corrected ECS-auth evidence; note script limitation that `p2p-real-env-triad-snapshot.sh` remote ssh can consume stdin and omit later explicit remote nodes in this invocation.

## 2026-06-23 10:25:36 CST / tpm
- 完成内容: Routed follow-up request "replication_recent_errors 和 replication_transport_unstable 查一下根因" into `systematic-debugging`.
- 遗留事项: none.
- Task Phase: runtime/node operations debugging within existing canonical task/worktree.
- Selected Workflow Skills: `systematic-debugging` because the request is to reproduce and narrow live runtime failure signatures before recommending any fix.
- Subagent Slice Plan:
  - role: `blockchain_ops_engineer`
  - slice type: `read_only_debugging`
  - intended model configuration: `.codex/config.toml` default subagent runtime `gpt-5.5-medium`
  - actual dispatched model/reasoning: inherited/unverified unless tool reports exact model
  - context delivery mode: full-thread/full-history fork requested
  - mandatory context checklist/packet: current task truth; prior degraded verdict; corrected credential structure without secrets; live ECS evidence paths under `.tmp/testnet-health-20260623-ecs-auth`; exact failure signatures `replication_recent_errors`, `replication_transport_unstable`; no mutation without explicit operator approval
  - write scope: no production changes; append root-cause verdict/evidence only
  - return contract: reproduced signature, narrowed root cause hypothesis, confirming/falsifying evidence, residual uncertainty, no-fix vs safe-next-action recommendation
  - formal sink / writeback surface: `.pm/tasks/task_bdb48338fac544849d8c681e9a7dd441.execution.md`
  - integration owner: `tpm`
  - integration order: TPM collects raw logs/status; role slice provides professional root-cause judgment; TPM summarizes.
- Action: Start read-only live debugging; do not restart services or modify ECS.
- Validation Command: `systematic-debugging` skill read; `git status --short --branch`.
- Expected Result: root-cause investigation proceeds from reproduced failure signatures and read-only evidence.
- Actual Result: debugging route recorded; current worktree is task branch with only task-truth files changed/untracked.
- Blocker / Next Action: collect replication and transport evidence from both ECS nodes.

## 2026-06-23 10:33:05 CST / tpm
- 完成内容: Completed read-only root-cause evidence collection for `replication_recent_errors` and `replication_transport_unstable`.
- 遗留事项: exact owner/process for source `111.199.105.123` and peer `12D3KooWBTPMund1jKKydc1sQvTjEgBPTqCsn6vvDneciMB4wxsj` remained unmapped at this point.
- Action: Collected live status, proc fd/log paths, sockets, peer mapping, code-level observability scoring rules, and a short 3-sample watch window.
- Validation Command: remote `curl` for `127.0.0.1:6631/6632/v1/chain/status`; remote `ss -tanp`; remote `/proc/<pid>/fd` inspection; repo searches for peer IDs and observability scoring; status watch under `.tmp/testnet-replication-rootcause-watch-20260623-103013`.
- Expected Result: reproduce the warning signatures and narrow whether they come from validator-pair replication, external peer churn, stale historical logs, or observability scoring.
- Actual Result: Evidence paths: `.tmp/testnet-replication-rootcause-20260623-102641/`, `.tmp/testnet-replication-rootcause-peer-files-20260623-102747/`, `.tmp/testnet-replication-rootcause-logs-20260623-102827/`, `.tmp/testnet-replication-rootcause-watch-20260623-103013/`. Current validator pair is not disconnected: both nodes report fresh peer heads, `sync=synced`, `network_head.decision=ready`, and height advanced to `760+` during collection. Sequencer alert maps exactly to `timeout=4` and `protocol_errors=1`; its `recent_errors` contain four `libp2p inbound failure ... Timeout` entries plus one `ProtocolError(InvalidMessage)`. Status and sockets show more than the two ECS validators connected, including multiple inbound connections from `111.199.105.123`; repo topology maps local observer/fourth-local peer IDs as allowed post-bootstrap joiners, and one active peer remains unmapped. Code inspection shows connection-established/closed/redundant-prune events are mostly diagnostic when active peers exist, so the blocking signal is the inbound timeout/protocol-error set rather than ordinary validator-pair churn. Short watch showed counts stable (`sequencer recent_replication_error_count=173`, storage `940->939`) while sequencer readiness remained not ready.
- Professional Slice Verdict (`blockchain_ops_engineer`): likely root cause is external observer/client peer churn, especially source `111.199.105.123`, causing sequencer-side libp2p inbound timeouts and one invalid-message protocol error that lower transport score; this is not evidence that the validator pair replication path is broken.
- Confidence: medium-high.
- Blocker / Next Action: identify whether `111.199.105.123` is our observer/client; if yes, stop or rate-limit that observer/client and watch 5-10 minutes for sequencer blocking errors to age out and score recover to `>=70`; if not ours, firewall/rate-limit the source. Also map active peer `12D3KooWBTPMund1jKKydc1sQvTjEgBPTqCsn6vvDneciMB4wxsj`. Do not restart validators first based on current evidence.
- Recommended Next Action: identify whether `111.199.105.123` is our observer/client; if yes, stop or rate-limit that observer/client and watch 5-10 minutes for sequencer blocking errors to age out and score recover to `>=70`; if not ours, firewall/rate-limit the source. Also map active peer `12D3KooWBTPMund1jKKydc1sQvTjEgBPTqCsn6vvDneciMB4wxsj`. Do not restart validators first based on current evidence.

## 2026-06-23 10:51:11 CST / tpm
- 完成内容: Routed user request "做策略调整，参考主流公链，改设计和实现" from read-only diagnosis into behavior-changing implementation.
- 遗留事项: none.
- Task Phase: design + runtime implementation.
- Selected Workflow Skills: `tdd-test-writer` because runtime readiness behavior has stable Rust unit-test surface; `executing-project-tasks` because this task has `.pm` truth and implementation should proceed stepwise.
- Required Role Slices:
  - `runtime_engineer`: owns runtime readiness/observability implementation and protocol semantics.
  - `blockchain_ops_engineer`: owns public testnet operations policy, noisy peer tolerance, and operational interpretation.
  - `qa_engineer`: owns regression and release-blocking interpretation after implementation.
- Acceptance Target:
  - external/noisy public peers remain visible as warnings and diagnostics;
  - validator readiness must not be hard-blocked solely by external observer/client churn when core validator path is healthy: synced, fresh network head, no lag, reachable, and at least one healthy requestable validator peer exists;
  - replication transport stability should distinguish core validator/request path from non-core public peer noise;
  - docs should describe public-chain-style noisy peer tolerance and when noisy peers become blocking.
- Initial Implementation Plan:
  1. Add failing Rust tests for noisy external peer errors with a healthy validator/request path.
  2. Update observability classification/readiness behavior narrowly.
  3. Update world-runtime docs/project notes with the strategy.
  4. Run targeted cargo tests and record evidence.
- Action: Begin RED phase and dispatch professional role slices.
- Validation Command: read `tdd-test-writer`, `executing-project-tasks`, role cards.
- Expected Result: behavior change starts from task-bound route with explicit professional role ownership.
- Actual Result: route recorded; implementation not yet started.
- Blocker / Next Action: inspect existing tests and implementation surfaces, then create RED coverage.

## 2026-06-23 09:51:54 CST / blockchain_ops_engineer
- 完成内容: UPDATED READ-ONLY TESTNET HEALTH VERDICT after corrected ECS credential usage and host-level evidence.
- 遗留事项: faucet service remained unavailable and service-manager truth needed reconciliation.
- Action: Re-evaluate health after corrected credential usage and host-level ECS evidence.
- Validation Command: read `.tmp/testnet-health-20260623-ecs-auth/20260623-094657/summary.{json,md}`, manual status samples, and systemd evidence under `.tmp/testnet-health-20260623-ecs-auth/`.
- Expected Result: replace earlier auth-blocked uncertainty with a role-owned health verdict based on public and host-level evidence.
- Actual Result: role verdict remained degraded, with sequencer readiness blocked by replication gates and systemd/service ownership drift confirmed.
- Blocker / Next Action: reconcile systemd truth, inspect replication churn, and repair or intentionally disable faucet.
- Evidence Paths: `.tmp/testnet-health-20260623-ecs-auth/20260623-094657/summary.md`; `.tmp/testnet-health-20260623-ecs-auth/20260623-094657/summary.json`; `.tmp/testnet-health-20260623-ecs-auth/manual-20260623-094846/*`; `.tmp/testnet-health-20260623-ecs-auth/systemd-20260623-094929/*`.
- Overall Verdict: `degraded`, unchanged from the previous role verdict. The prior verdict remains valid but the uncertainty changes materially: ECS auth is now understood as password inventory and host-level inspection succeeded. The chain is not fully down because both node processes are running, status endpoints respond, liveness is ok, both nodes are synced at aligned committed/network height `718`, and the formal rerun observed height progress `715 -> 716`. The network is still degraded because the sequencer remains `ok=false`/`readiness=not_ready`, replication transport is unstable, both nodes carry observability warnings, systemd ownership is unhealthy/drifted, and the faucet endpoint is down.
- Sequencer Evidence: `triad-testnet-sequencer` on `39.104.204.172:6631` reports `running=true`, liveness `ok`, sync `synced`, fresh peer count `1`, stale `0`, committed/network height `718`, and network head decision `ready`; however readiness is `not_ready`, failed gates are `replication_recent_errors,replication_transport_unstable`, observability is `warn`/`ready=false`, transport stable is `false`, transport score is `45`, recent replication error count is `170`, and `last_commit_age_ms` was about `56706` in the manual sample. Host evidence shows `oasis7-triad-sequencer.service=failed` while an `oasis7_chain_runtime` process still listens on `0.0.0.0:6631`; storage/faucet units on that host are inactive/not serving the public faucet role.
- Storage Evidence: `triad-testnet-storage` on `39.104.205.67:6632` reports `ok=true`, readiness `ready`, sync `synced`, fresh peer count `1`, stale `0`, committed/network height `718`, network head decision `ready`, observability `warn` but `ready=true`, transport stable `true`, transport score `85`, reachability ok, recent replication error count `941`, and `last_commit_age_ms` about `58646`. Host evidence shows the runtime process listening on `0.0.0.0:6632` but the triad systemd units are inactive, so storage is functionally serving but not cleanly managed by the expected service unit.
- Collection Limitation: the formal snapshot summary omitted storage because the remote SSH invocation can consume stdin while iterating explicit remote nodes; this is a collection-tool limitation, not storage failure. Manual two-node evidence supersedes that omission for health judgment.
- Concrete Blockers: sequencer readiness gate blocked by replication recent errors and unstable transport; sequencer systemd unit failed while the runtime process continues independently; storage systemd unit inactive despite a live runtime process; faucet public endpoint `39.104.204.172:6681` still refuses connections and `oasis7-faucet.service` is absent/inactive from inspected service state; commit age was around one minute in the manual samples, so a longer watch is needed to distinguish low-traffic idle from stalled proposal cadence.
- Recommended Next Actions: restore service-manager truth first by reconciling the live runtime processes with the intended `oasis7-triad-*` systemd units before any restart; inspect recent sequencer logs around replication timeouts/protocol errors and peer transport churn; run a 5-10 minute public plus SSH watch for height progress, last commit age, readiness, and replication error deltas; repair or intentionally disable/replace the faucet service and update the manifest/runbook accordingly; after remediation, rerun the two-node triad snapshot with the SSH stdin bug fixed so storage is included in the formal summary.
- Secret Boundary: ECS credentials were used only as authentication input; passwords and LAN-only entries are not recorded.

## 2026-06-23 10:31:13 CST / blockchain_ops_engineer
- 完成内容: READ-ONLY ROOT-CAUSE DEBUGGING for `replication_recent_errors` and `replication_transport_unstable`; no code changes and no service restarts/stops.
- 遗留事项: source host/process ownership for the noisy external peer remained unverified.
- Action: Diagnose the reproduced replication readiness signatures using live status, sockets, logs, and source-code scoring rules.
- Validation Command: read `.tmp/testnet-replication-rootcause-20260623-102641/`, `.tmp/testnet-replication-rootcause-20260623/live-watch.tsv`, live validator status samples, and observability source references.
- Expected Result: distinguish validator-pair failure from external peer churn without mutating production state.
- Actual Result: role verdict identified external observer/client churn as the likely root cause while validator pair progress remained healthy.
- Blocker / Next Action: identify or rate-limit the noisy external peer, then watch for transport score/readiness recovery.
- Evidence Paths: `.tmp/testnet-replication-rootcause-20260623-102641/`; `.tmp/testnet-replication-rootcause-20260623/live-watch.tsv`; current `/v1/chain/status` samples from sequencer `39.104.204.172:6631` and storage `39.104.205.67:6632`; source references `crates/oasis7/src/bin/oasis7_chain_runtime/status_payload_observability.rs` and `status_payload.rs`.
- Reproduced Signature: sequencer remains `ok=false`, `readiness=not_ready`, failed gates `replication_recent_errors,replication_transport_unstable`, transport score `45`, alert breakdown `timeout=4, protocol_errors=1`, and `recent_replication_error_count=173`; storage is mostly `ok=true`/`ready`, transport score `85`, with only `replication_recent_errors` for one blocking error. Fresh watch at `10:29:57-10:30:51 CST` observed height progress `766 -> 767` on both validators with `sync=synced`, so this is not a consensus/finality stall.
- Most Likely Root Cause: external observer/client peer churn is injecting inbound libp2p request/handshake noise into both validators, with the sequencer readiness gate more sensitive because its recent blocking window contains four inbound timeouts plus one invalid-message protocol error. The strongest host/network clue is repeated established inbound sockets from `111.199.105.123` to both validator ECS nodes while the validator pair itself remains connected and advancing. Status peer inventory also shows four active peers, not only the two configured validator ECS peers, consistent with allowed observer/fourth-local joins after the validator pair is live.
- Confirming Evidence: sequencer recent errors include four `libp2p inbound failure ... Timeout` entries, one per connected peer, plus one `libp2p incoming connection error ... ProtocolError(InvalidMessage)`; those five errors exactly match the alert counts. Socket evidence shows validator-to-validator connectivity plus several inbound connections from `111.199.105.123`. Source inspection confirms `connection_closed`, `redundant connections pruned`, and other recovered-path diagnostics are filtered when an active request path exists, so the blocking gate is driven by timeout/protocol-error entries rather than ordinary duplicate connection close/prune noise. Live samples show the storage validator remains stable (`transport_stable=true`, score `85`) while the sequencer stays at score `45`.
- Ruled-Out Causes: not a chain halt or consensus finality stall because heights advance and both validators report fresh heads/synced state; not a validator-pair reachability outage because sequencer and storage remain mutually connected and requestable; not a storage persistence gap because current committed height matches replication persisted height in status evidence; not primarily old gap-sync mismatch logs because current status has no height lag and fresh peer heads; not secret/auth failure because ECS access was already corrected and no production mutation was attempted.
- Residual Uncertainty: peer `12D3KooWBTPMund1jKKydc1sQvTjEgBPTqCsn6vvDneciMB4wxsj` is still unmapped by current repo topology search, and the exact process/user behind `111.199.105.123` has not been verified. It is also not yet proven whether the invalid-message protocol error is a stale-version observer, malformed client, duplicate local instance, or a transient libp2p negotiation edge.
- Safe Next Actions: identify and either stop/throttle the observer/client at `111.199.105.123` if it is ours, or firewall/rate-limit it if it is unintended; map `12D3KooWBTPMund1jKKydc1sQvTjEgBPTqCsn6vvDneciMB4wxsj` to an owner/host; run a 5-10 minute read-only watch after removing/throttling that peer and expect sequencer blocking errors to age out, transport score to recover above `70`, and readiness to become `ready`; separately reconcile stale systemd ownership and faucet state after the live replication noise is understood. Do not restart validator processes until the noisy peer/source is identified or isolated.
- Secret Boundary: ECS credentials were used only as authentication input; no password, LAN-only entry, or secret material is recorded.

## 2026-06-23 11:12:20 CST / tpm
- 完成内容: Integrated professional role advice for public-testnet noisy-peer tolerance and started implementation.
- 遗留事项: implementation and verification still pending in this entry.
- Action: Convert role advice into a concrete runtime/docs implementation plan.
- Validation Command: read role slice outputs and current observability test/implementation surfaces.
- Expected Result: establish a bounded patch plan that preserves diagnostics while narrowing readiness blockers.
- Actual Result: plan recorded; follow-up entry records implementation and tests.
- Blocker / Next Action: implement RED/GREEN coverage and runtime classifier changes.
- Runtime Slice Verdict (`runtime_engineer`): split core validator/request-path readiness from public inbound peer noise; external inbound timeout/invalid-message churn must remain visible in diagnostics but should not hard-fail readiness while a healthy requestable core path exists.
- Ops Slice Verdict (`blockchain_ops_engineer`): public testnet/mainnet style networks should expect faulty/noisy external peers; those peers become readiness blockers only when they exhaust resources or affect validator head/state/replication progress.
- QA Slice Contract (`qa_engineer`): keep classifier coverage plus add status/readiness payload coverage; verify that raw recent error count remains visible while `replication_recent_errors` and `replication_transport_unstable` are absent from failed readiness gates under a healthy validator path.
- Implementation Plan Update:
  1. Keep the existing RED classifier test for noisy observer inbound timeout plus invalid-message churn.
  2. Add a status-layer test covering ready validator/public-testnet semantics.
  3. Narrowly update replication transport classification to treat inbound-only public peer noise as non-blocking only when a recovered request path exists.
  4. Run targeted Rust tests and record the results.
- Attribution Boundary: TPM is coordinating and applying the integrated patch; professional semantics above are attributed to the role slices.

## 2026-06-23 11:28:45 CST / tpm
- 完成内容: Implemented public-chain-style noisy external peer tolerance for replication transport readiness.
- 遗留事项: live ECS redeploy was not yet performed in this implementation pass.
- Action: Add tests, update readiness classification, and document the public-peer noise boundary.
- Validation Command: `env -u RUSTC_WRAPPER cargo fmt --check`; targeted `oasis7_chain_runtime` observability tests listed below.
- Expected Result: noisy external inbound peer churn remains diagnostic-only when core validator/request path is healthy, while request-path failures remain blocking.
- Actual Result: targeted formatter and Rust tests passed; unchanged warnings remained in existing `oasis7_node` code.
- Blocker / Next Action: commit/push and proceed to CI package build plus deployment per user request.
- Code Changes:
  - `status_payload_observability.rs`: added recovered-request-path-gated classification for inbound-only public peer noise; request-path/fetch failures remain blocking.
  - `oasis7_chain_runtime_observability_transport_tests.rs`: added classifier coverage for healthy validator path plus noisy observer inbound timeout/invalid-message churn.
  - `oasis7_chain_runtime_observability_tests.rs`: added status/readiness coverage proving raw `recent_replication_error_count=5` stays visible while readiness remains `ready` and failed gates omit `replication_recent_errors` / `replication_transport_unstable`.
- Design Changes:
  - `doc/p2p/node/node-triad-observability-stack-2026-04-23.design.md`: documented public peer noise vs core readiness boundaries.
  - `doc/testing/templates/public-testnet-rehearsal-template.md`: added runtime verification item for diagnostic-only noisy external peers when core validator path is healthy.
- Verification Commands:
  - `env -u RUSTC_WRAPPER cargo fmt --check`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime tolerates_noisy_external_peer_with_healthy_validator_path -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_transport_tests -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_tests -- --nocapture`
- Verification Result: all commands passed. Cargo briefly waited for shared package/build locks during the final test rerun, then completed normally. Existing `oasis7_node` unused/dead-code warnings remained unchanged.
- Residual Risk: no live ECS restart or redeploy was performed in this implementation pass; runtime behavior is verified at unit/status payload level only.

## 2026-06-23 11:43:16 CST / tpm
- 完成内容: Started CI package + five-node redeploy path per user request "走ci编译，重新部署五节点".
- 遗留事项: CI package and deployment were pending in this entry.
- Deployment Strategy:
  - Use GitHub `Testnet Packages` workflow to produce the Linux `oasis7-linux-x64-bundle.tar.gz` artifact.
  - Prefer package upgrade/restart over destructive validator rebuild so existing chain state is not reset unless a later health check requires it.
  - After CI package succeeds, use the package rollout/upgrade helpers and then verify all five intended testnet nodes.
- Local Preflight:
  - `env -u RUSTC_WRAPPER cargo build -p oasis7 --bin oasis7_chain_runtime`
  - Result: passed in dev profile; only existing `oasis7_node` unused/dead-code warnings were reported.
- Action: Start the package-and-redeploy phase with local Rust preflight.
- Validation Command: `env -u RUSTC_WRAPPER cargo build -p oasis7 --bin oasis7_chain_runtime`.
- Expected Result: local runtime build passes before committing and requesting CI packages.
- Actual Result: dev-profile build passed with unchanged existing warnings.
- Blocker / Next Action: commit and push the task branch, dispatch `Testnet Packages` with `build_profile=release` and `package_scope=linux_only`, then download and deploy the Linux bundle.

## 2026-06-23 13:24:00 CST / tpm
- 完成内容: Completed CI package build, deployed Linux package to the ECS validator pair, repaired service-manager truth, and rebuilt validator chain state after storage execution mismatch.
- 遗留事项: later user clarification expanded scope from the initially confirmed managed installs to five machines including two additional document-listed observers.
- Professional Attribution: deployment recovery decisions incorporated `blockchain_ops_engineer` and `runtime_engineer` role guidance; TPM integrated and executed the rollout.
- Action: Build CI package, deploy to ECS validators, repair service-manager truth, and perform validator rebuild after execution mismatch.
- Validation Command: GitHub Actions run checks, package SHA checks, ECS systemd/status checks, and public status endpoint curls listed below.
- Expected Result: validators run the CI package under correct triad systemd units and return to ready/height-aligned state.
- Actual Result: CI package builds passed; both validators were cleanly managed by triad services and public endpoints were ready at aligned height.
- Blocker / Next Action: clarify and update any additional managed nodes outside the initially confirmed topology.
- CI Evidence:
  - Commit: `04ca775181aa52f006ec95720d23136aac39b664`.
  - Linux-only package run: `27999787500`, conclusion `success`, URL `https://github.com/eng-cc/oasis7/actions/runs/27999787500`.
  - Linux package version: `0.0.0+testnet.120.04ca775181aa`.
  - Linux bundle SHA256: `3309c4d600485736f3c4e4839d51ce3f21ee84cc785f3da4527a87e296f493e3  oasis7-linux-x64-bundle.tar.gz`.
  - Runtime binary SHA256 after install: `b545121f7a3f451f6ac75ba0714e7c059efebae3ce37ac6fede7e1e2717a79a7`.
  - Combined Linux/macOS package run: `27999830167`, conclusion `success`, URL `https://github.com/eng-cc/oasis7/actions/runs/27999830167`.
- Deployment Actions:
  - Uploaded CI Linux bundle and `scripts/p2p-public-testnet-package-node-upgrade.sh` to both ECS validators.
  - Upgraded both `/opt/oasis7/p2p-testnet` roots to `CURRENT_VERSION=0.0.0+testnet.120.04ca775181aa`.
  - Rewrote triad systemd units to `WorkingDirectory=/opt/oasis7/p2p-testnet` and `ExecStart=/opt/oasis7/p2p-testnet/bin/start-node.sh`.
  - Disabled stale legacy units `oasis7-testnet-sequencer.service` and `oasis7-testnet-storage.service`; final active units are `oasis7-triad-sequencer.service` and `oasis7-triad-storage.service`, both `active/running` with `NRestarts=0`.
- Recovery Actions:
  - Initial post-upgrade service cleanup exposed stale orphan runtime processes and old legacy units owning the validator ports.
  - Storage then reported `node execution error: execution driver peer mismatch at height 871`, while sequencer had advanced; professional role slice recommended not rolling back the package and not applying observer seed scripts to validators.
  - Built deployment truth stage from the CI runtime, current ECS signer public keys, and frozen bootstrap peers using `scripts/p2p-public-testnet-build-deployment-stage.sh --track public_testnet_rehearsal`.
  - Ran the standard destructive validator pair rebuild path via `scripts/p2p-public-testnet-rebuild-validators.sh`, staging config/world to both validators and resetting validator chain state while preserving `config/node-keypair.toml`.
- Final External Health:
  - Sequencer `http://39.104.204.172:6631/v1/chain/status`: `running=true`, `last_error=null`, `readiness.status=ready`, failed gates `[]`, `committed_height=1073`, `last_execution_height=1073`, `network_head=1073`, storage challenge degraded height `null`.
  - Storage `http://39.104.205.67:6632/v1/chain/status`: `running=true`, `last_error=null`, `readiness.status=ready`, failed gates `[]`, `committed_height=1073`, `last_execution_height=1073`, `network_head=1073`, storage challenge degraded height `null`.
  - Both validators report four connected replication peers; recent raw replication error counters remain visible but no longer block readiness after the validator pair is healthy.
- Five-Node Boundary:
  - Professional slice confirmed the repo-known frozen topology is `2 validators + 2 observers` (`triad-testnet-sequencer`, `triad-testnet-storage`, `triad-testnet-local`, `triad-testnet-fourth-local`), not five formal managed nodes.
  - Current live replication peer inventory shows four connected peers on each validator. Any fifth external node is outside the repo formal governed bootstrap topology and should be treated as an external/observer peer unless separately registered.
- Verification Commands:
  - `gh run view 27999787500 --json status,conclusion,jobs,url,headSha`
  - `gh run view 27999830167 --json status,conclusion,jobs,url,headSha`
  - `shasum -a 256 .tmp/testnet-packages-27999787500/testnet-package-linux-x64-0.0.0+testnet.120.04ca775181aa/oasis7-linux-x64-bundle.tar.gz`
  - ECS systemd/status checks on both validators.
  - External `curl` checks against both public status endpoints.
- Verification Result: CI package builds passed; both ECS validator services are cleanly managed by triad systemd units; both public status endpoints are ready and height-aligned.
- Secret Boundary: ECS passwords were read from `/Users/scc/Documents/keys/aliyun_ecs.txt` only for authentication and were not recorded.

## 2026-06-23 15:08:00 CST / tpm
- 完成内容: Reconciled the user's "文档里有四个，再加上本机，一共五个，都更新" correction, upgraded all confirmed managed installs to the combined Linux/macOS CI package, and restored health after strict validator reset plus local observer reseed.
- 遗留事项: the two old `.tmp` bootstrap directories were still present and later deleted after explicit user approval.
- Action: Upgrade the confirmed ECS and local macOS managed installs, perform strict validator reset when needed, and reseed the local observer.
- Validation Command: GitHub Actions artifact checks, package SHA checks, script syntax/tests, ECS/local version/hash/service/status checks listed below.
- Expected Result: all then-confirmed managed installs run the new package and report ready with aligned height.
- Actual Result: two ECS validators plus local macOS observer reported ready on package `0.0.0+testnet.121.04ca775181aa`.
- Blocker / Next Action: determine whether stale bootstrap directories count as managed installs and delete them only with explicit approval.
- CI Artifact Evidence:
  - Combined Linux/macOS package run: `27999830167`, conclusion `success`, URL `https://github.com/eng-cc/oasis7/actions/runs/27999830167`.
  - Linux package version: `0.0.0+testnet.121.04ca775181aa`; Linux bundle SHA256 `138500046eb62c76855f00ed5007043e63aba78e6c6ce3e80419f79b22ca8dae`; installed Linux runtime SHA256 `b545121f7a3f451f6ac75ba0714e7c059efebae3ce37ac6fede7e1e2717a79a7`.
  - macOS package version: `0.0.0+testnet.121.04ca775181aa`; installed local macOS runtime SHA256 `735a80ce03a61cbb29adf255b55a03d7b9e6948253bb9c123e4696469ccf534e`.
- Deployment Actions:
  - Upgraded `triad-testnet-sequencer` on `root@39.104.204.172` from `0.0.0+testnet.120.04ca775181aa` to `0.0.0+testnet.121.04ca775181aa`.
  - Upgraded `triad-testnet-storage` on `root@39.104.205.67` from `0.0.0+testnet.120.04ca775181aa` to `0.0.0+testnet.121.04ca775181aa`.
  - Upgraded the managed local launchd node `/Users/scc/ccwork/oasis7-testnet-nodes/fourth-local` / `triad-testnet-fourth-local` from `0.0.0+testnet.118.36daf05143bd` to `0.0.0+testnet.121.04ca775181aa`.
- Recovery Actions:
  - Storage remained on an execution mismatch after the package upgrade (`execution driver peer mismatch at height 1223`), so a validator state rebuild was required again.
  - Built `.tmp/public-testnet-ci121-rebuild-stage` from the 121 Linux runtime, frozen bootstrap peers, and current validator signer public keys using `--track public_testnet_rehearsal`.
  - The repo rebuild helper reset legacy `output/node-distfs` paths but not current `data/replication-root`, so storage replayed stale replication commits and stayed mismatched. A stricter reset stopped both validator services, backed up old state under `/opt/oasis7/p2p-testnet/backups/ci121-strict-reset-*`, cleared `data/execution-world`, `data/execution-records`, `data/storage`, `data/replication-root`, `data/runtime-root`, `output/chain-runtime`, and `output/node-distfs`, then restored the staged 121 world.
  - Updated `scripts/p2p-public-testnet-rebuild-validators.sh` so future validator rebuilds also clear current `data/replication-root` and `data/runtime-root`, not only legacy `output/node-distfs`.
  - Updated `scripts/p2p-public-testnet-local-observer-sync.sh` so local observer seeding reads the remote `REPLICATION_ROOT` from env instead of assuming old `output/node-distfs/$NODE_ID`, and treats fresh-node simulator mirror / bridge-state files as optional.
  - Reseeded the managed local observer from storage with `seed-from-remote` and restarted `oasis7.testnet.fourth` via launchd.
- Final Health:
  - Sequencer `39.104.204.172:6631`: `CURRENT_VERSION=0.0.0+testnet.121.04ca775181aa`, `running=true`, `last_error=null`, `readiness.status=ready`, failed gates `[]`, `committed_height=7`, `last_execution_height=7`, `network_head=7`.
  - Storage `39.104.205.67:6632`: `CURRENT_VERSION=0.0.0+testnet.121.04ca775181aa`, `running=true`, `last_error=null`, `readiness.status=ready`, failed gates `[]`, `committed_height=7`, `last_execution_height=7`, `network_head=7`.
  - Local `triad-testnet-fourth-local` `127.0.0.1:19083`: `CURRENT_VERSION=0.0.0+testnet.121.04ca775181aa`, launchd `oasis7.testnet.fourth` running, `running=true`, `last_error=null`, `readiness.status=ready`, failed gates `[]`, `committed_height=7`, `last_execution_height=7`, `network_head=7`.
- Five-Node Inventory Boundary:
  - The runbook's old local observer directories under `/Users/scc/ccwork/worktrees/oasis7-p2p-public-testnet-governed-bootstrap-rebuild/.tmp/testnet-local-node-bootstrap` and `.tmp/testnet-fourth-node-bootstrap` still exist, but they have no `bin/oasis7_chain_runtime`, no `CURRENT_VERSION`, and no `DEPLOYED_BUILDINFO`; they are stale bootstrap state directories, not currently managed installs.
  - `/opt/oasis7/p2p-testnet-local` is absent, and `launchctl list` shows only the managed local `oasis7.testnet.fourth` testnet service.
  - Therefore the confirmed updated managed installs are the two ECS validators plus the one managed local launchd observer. The two stale `.tmp` bootstrap directories cannot be "version upgraded" as installs without first re-promoting them into managed node roots with runtime binaries and service definitions.
- Verification Commands:
  - `gh api repos/eng-cc/oasis7/actions/runs/27999830167/artifacts`
  - `shasum -a 256 .tmp/testnet-linux-27999830167-api/oasis7-linux-x64-bundle.tar.gz`
  - `bash -n scripts/p2p-public-testnet-rebuild-validators.sh scripts/p2p-public-testnet-local-observer-sync.sh scripts/p2p-public-testnet-rebuild-validators.test.sh scripts/p2p-public-testnet-local-observer-sync.test.sh`
  - `bash scripts/p2p-public-testnet-local-observer-sync.test.sh`
  - `bash scripts/p2p-public-testnet-rebuild-validators.test.sh`
  - ECS `CURRENT_VERSION`, `readlink -f current`, runtime `shasum -a 256`, systemd, and local status checks.
  - Local launchd and `curl http://127.0.0.1:19083/v1/chain/status` checks.
- Verification Result: all confirmed managed installs are on CI package `0.0.0+testnet.121.04ca775181aa` and report ready. Raw recent replication errors remain visible as diagnostics but no longer block readiness with healthy validator paths.
- Secret Boundary: ECS passwords were used only through `sshpass` environment variables for authentication and were not recorded.

## 2026-06-23 15:23:00 CST / tpm
- 完成内容: Deleted the two stale local bootstrap directories after user explicitly approved cleanup.
- 遗留事项: none; later user clarification identified two additional real machines from the credential document, not these stale directories.
- Action: Remove the two stale `.tmp` bootstrap directories and verify the managed local observer remained healthy.
- Validation Command: pre-delete file/runtime checks, `rm -rf` for the two approved paths, post-delete existence checks, launchd/TCP/status checks.
- Expected Result: stale bootstrap directories are gone and no managed local service is disrupted.
- Actual Result: both paths were deleted; managed local `triad-testnet-fourth-local` stayed ready and TCP `19082` remained unused.
- Blocker / Next Action: continue with any newly identified real node inventory.
- Deleted Paths:
  - `/Users/scc/ccwork/worktrees/oasis7-p2p-public-testnet-governed-bootstrap-rebuild/.tmp/testnet-local-node-bootstrap`
  - `/Users/scc/ccwork/worktrees/oasis7-p2p-public-testnet-governed-bootstrap-rebuild/.tmp/testnet-fourth-node-bootstrap`
- Pre-Delete Evidence:
  - `testnet-local-node-bootstrap` was about `115M`, had no `bin/oasis7_chain_runtime`, and had no `CURRENT_VERSION`.
  - `testnet-fourth-node-bootstrap` was about `113M`, had no `bin/oasis7_chain_runtime`, and had no `CURRENT_VERSION`.
  - `launchctl list` showed only the managed local testnet node `oasis7.testnet.fourth`; TCP `19082` was not listening.
- Post-Delete Verification:
  - Both deleted paths no longer exist.
  - Managed local `triad-testnet-fourth-local` on `127.0.0.1:19083` remains `running=true`, `last_error=null`, `readiness.status=ready`, failed gates `[]`, and height `61`.
  - TCP `19082` remains not serving.

## 2026-06-23 17:43:00 CST / tpm
- 完成内容: Completed user-requested "补更" for the two additional machines listed in `/Users/scc/Documents/keys/aliyun_ecs.txt` and restored all five nodes to ready after a transient height-178 divergence.
- Professional Attribution: `blockchain_ops_engineer` provided the package-scope and recovery guidance; TPM integrated and executed the update.
- Action: Upgrade Linux LAN and Windows observers, reseed affected observers, recover validator pair after transient divergence, and verify five-node health.
- Validation Command: GitHub Actions Windows-capable package run/artifact checks, remote version/hash/service/status checks, and final five-node status checks listed below.
- Expected Result: all five requested nodes are updated or intentionally version-bounded by platform package availability and report ready at aligned height.
- Actual Result: five nodes reported ready at height `198`; Windows used later package `122` because Windows artifact support required `all_existing`.
- Blocker / Next Action: document the package-scope/version boundary and update reusable runbooks.
- Scope Correction:
  - Earlier redeploy covered the two ECS validators plus the managed local macOS observer. It missed the two additional document-listed observer machines: `scc@192.168.1.4` and `Administrator@192.168.1.3`.
  - `blockchain_ops_engineer` slice recommended upgrading the Linux LAN observer from the Linux CI package and using a Windows artifact from an `all_existing` CI package run because the earlier Linux/macOS CI run did not include Windows.
- CI Artifact Evidence:
  - Prior Linux/macOS package run: `27999830167`, version `0.0.0+testnet.121.04ca775181aa`.
  - Windows-capable `all_existing` package run: `28012764394`, conclusion `success`, head `78baa539320002172fc83b551cc8566ff93c063f`, Windows package version `0.0.0+testnet.122.78baa5393200`.
  - Windows installer SHA256 from artifact: `87685f527007b8ade6ccbd7e33195f94adf78b8bdcf0aeab39345cdc60cc5062`; installed Windows runtime SHA256 `7bad71e25f692c41d31dfd08d309616cb78f003eb6464df00d6e8aed755afadf`.
- Deployment Actions:
  - Linux LAN observer `triad-testnet-local` on `scc@192.168.1.4`: upgraded `/opt/oasis7/p2p-testnet-local` from `0.0.0+testnet.118.36daf05143bd` to `0.0.0+testnet.121.04ca775181aa`, then reseeded from storage.
  - Windows observer `triad-testnet-windows-observer` on `Administrator@192.168.1.3`: installed `0.0.0+testnet.122.78baa5393200`, updated deploy metadata/config runtime build fields, reset observer state, and reseeded from storage.
- Recovery Actions:
  - After observer reseeds, both ECS validators temporarily reported stale peer-head readiness. Sequential service restart exposed a storage execution mismatch at height `178`.
  - Recovered validator pair by reseeding storage from the ready sequencer state, then restarted storage. Both validators returned to `ready` at height `186` and later advanced together.
  - Because Windows, Linux LAN, and local macOS observers had already seeded from the pre-recovery height `177` state, all affected observers were reseeded again from the recovered storage state.
  - Backups were left in the respective node backup directories before every destructive state replacement.
- Final Five-Node Health:
  - ECS sequencer `triad-testnet-sequencer` / `root@39.104.204.172`: `CURRENT_VERSION=0.0.0+testnet.121.04ca775181aa`, `running=true`, `last_error=null`, `readiness.status=ready`, failed gates `[]`, `committed_height=198`, `last_execution_height=198`, `network_head=198`.
  - ECS storage `triad-testnet-storage` / `root@39.104.205.67`: `CURRENT_VERSION=0.0.0+testnet.121.04ca775181aa`, `running=true`, `last_error=null`, `readiness.status=ready`, failed gates `[]`, `committed_height=198`, `last_execution_height=198`, `network_head=198`.
  - Linux LAN observer `triad-testnet-local` / `scc@192.168.1.4`: `CURRENT_VERSION=0.0.0+testnet.121.04ca775181aa`, `running=true`, `last_error=null`, `readiness.status=ready`, failed gates `[]`, `committed_height=198`, `last_execution_height=198`, `network_head=198`.
  - Windows observer `triad-testnet-windows-observer` / `Administrator@192.168.1.3`: `CURRENT_VERSION=0.0.0+testnet.122.78baa5393200`, `running=true`, `last_error=null`, `readiness.status=ready`, failed gates `[]`, `committed_height=198`, `last_execution_height=198`, `network_head=198`.
  - Local macOS observer `triad-testnet-fourth-local` / `/Users/scc/ccwork/oasis7-testnet-nodes/fourth-local`: `CURRENT_VERSION=0.0.0+testnet.121.04ca775181aa`, `running=true`, `last_error=null`, `readiness.status=ready`, failed gates `[]`, `committed_height=198`, `last_execution_height=198`, `network_head=198`.
- Verification Commands:
  - `gh run view 28012764394` and artifact download for Windows package.
  - Remote `CURRENT_VERSION`, runtime hash, systemd/scheduled-task, `netstat`, and status endpoint checks.
  - Fresh storage/sequencer seed packaging plus backup/reseed/restart commands on Windows, Linux LAN, storage, and local macOS observer.
  - Final status checks against all five `/v1/chain/status` endpoints.
- Verification Result: all five requested nodes are updated and health-aligned at height `198`; readiness failed gates are empty across the fleet. Windows is on package `122` because the required Windows artifact came from the later `all_existing` CI run; the other four remain on the Linux/macOS `121` package with the same strategy patch lineage.
- Secret Boundary: credentials from `/Users/scc/Documents/keys/aliyun_ecs.txt` were used only for SSH/SCP authentication and were not recorded.
- 遗留事项: Windows observer is on the later `122` package because Windows artifact support required the later `all_existing` CI run; future uniform package-version rollout should use `all_existing` for all platforms in one run.

## 2026-06-23 18:18:00 CST / tpm
- 完成内容: Updated reusable repo documentation so node inventory, package update scope,补更 order, and reseed recovery are no longer trapped only in this task log.
- Professional Attribution: `blockchain_ops_engineer` provided the documentation policy verdict; TPM integrated the doc edits.
- Action: Update the public testnet runbook, environment inventory, and evidence docs with the current five-node operator inventory and rollout/reseed procedure.
- Action:
  - Updated `doc/p2p/blockchain/p2p-public-testnet-governed-bootstrap-2026-06-06.runbook.md` from a four-node-only rebuild runbook into a deploy/update/rebuild/recovery runbook with current five-node operator inventory, CI `package_scope` guidance, safe update order, platform-specific entrypoints, observer reseed order, and fleet health acceptance fields.
  - Updated `doc/engineering/governance/environment-lanes-and-inventory-2026-05-29.md` with the current managed `public_testnet` five-node inventory and rules that old `.tmp` bootstrap staging directories do not count as managed installs.
  - Added `doc/testing/evidence/public-testnet-five-node-inventory-2026-06-23.md` as current live inventory evidence, without rewriting the frozen 2026-06-06 governed bootstrap topology evidence.
- Professional Slice Verdict (`blockchain_ops_engineer`): the right documentation fix is to preserve the historical governed bootstrap topology evidence, add current five-node live inventory evidence, and make the main runbook explicitly cover `linux_only` / `linux_macos_x64` / `all_existing` package scope, Windows artifact requirements, validator-first recovery, observer reseed triggers, and required health fields.
- Validation Command: `./scripts/doc-governance-check.sh`; `git diff --check`; targeted `rg`; `./scripts/pm/lint.sh`.
- Validation Command:
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
  - `rg -n "/Users/scc|最终四节点|四节点真正|四节点已跑|两个 observer" doc/engineering/governance/environment-lanes-and-inventory-2026-05-29.md doc/p2p/blockchain/p2p-public-testnet-governed-bootstrap-2026-06-06.runbook.md doc/testing/evidence/public-testnet-five-node-inventory-2026-06-23.md`
  - `./scripts/pm/lint.sh`
- Expected Result: reusable docs contain no hard-coded user-home paths, no stale four-node health wording for the current operator fleet, and no diff whitespace errors; PM lint either passes or reports only unrelated historical debt.
- Expected Result Details:
  - Reusable docs contain no hard-coded user-home paths, no stale four-node health wording for the current operator fleet, and no diff whitespace errors.
  - PM lint either passes or reports only pre-existing repo-wide task-log debt unrelated to this task.
- Actual Result: doc governance and diff checks passed; targeted stale-wording search returned no matches; `./scripts/pm/lint.sh` failed only on then-observed repo-wide historical formatting debt.
- Actual Result Details:
  - `doc-governance-check: OK`.
  - `git diff --check` passed.
  - Targeted `rg` returned no matches for hard-coded user-home paths or stale current-fleet four-node wording in the edited docs.
  - `./scripts/pm/lint.sh` failed on pre-existing repo-wide historical `.pm/tasks/*` formatting debt; the output did not identify the current task log as the blocker.
- Blocker / Next Action: no blocker for the documentation update. A separate repository-health cleanup task is needed if repo-wide `.pm` historical lint debt must be cleared.
- Secret Boundary: documentation records node identities, paths, package scopes, and status fields only; it does not record credentials or secret values.
- 遗留事项: Windows observer reset/reseed remains documented as SOP but not yet fully productized as a repo-owned reusable PowerShell script.

## 2026-06-23 18:27:00 CST / tpm
- 完成内容: Dispatched pre-PR local role review for the task branch.
- 遗留事项: review package was later found stale by QA/repository-health review and must be regenerated after the current fixes are committed.
- Action: Request bounded pre-PR review from blockchain ops, runtime, QA, and repository health roles.
- Validation Command: `./scripts/pm/review-package.sh --base origin/main --head HEAD --task-uid task_bdb48338fac544849d8c681e9a7dd441`; `./scripts/pm/slice-ledger.sh --task-uid task_bdb48338fac544849d8c681e9a7dd441 --print`.
- Expected Result: reviewers receive the branch diff and task evidence before PR creation.
- Actual Result: review slices were dispatched; follow-up findings required runbook, PR.md, task YAML, review package, and PM log fixes.
- Blocker / Next Action: address findings, regenerate review package from the correct merge base after committing fixes, and record final `Pre-PR Local Role Review: passed`.
- Review Trigger: pre-PR local role review
- Review Scope: runtime readiness classifier/tests; public testnet rebuild/local observer scripts; public testnet operator runbooks/inventory docs; task truth files.
- Review Package: `.pm/scratch/task_bdb48338fac544849d8c681e9a7dd441/review-packages/review-b3bb0e78e..d0fddcda3.diff`
- Review Roles: `blockchain_ops_engineer`, `runtime_engineer`, `qa_engineer`, `repository_health_engineer`
- Review Question: confirm the branch is safe to open as a PR: public-chain noisy-peer readiness semantics are correct, tests cover the intended behavior, operator docs/scripts match the five-node deployment/reseed reality, and the diff remains within task scope.
- Evidence Available: `doc-governance-check: OK`; `git diff --check` passed; targeted runtime tests and script tests recorded above; live five-node health evidence recorded above; current task workflow lint passed.
- Expected Return Contract: findings | no_findings; scope/spec compliance verdict; role quality/risk verdict; residual_risk.
- Slice Ledger: `.pm/scratch/task_bdb48338fac544849d8c681e9a7dd441/slice-ledger.jsonl`
- Formal Sink: `.pm/tasks/task_bdb48338fac544849d8c681e9a7dd441.execution.md`
- 遗留事项: none for dispatch; integrate valid findings before recording `Pre-PR Local Role Review: passed`.

## 2026-06-23 18:56:00 CST / tpm
- 完成内容: Addressed initial pre-PR review blockers before regenerating the review package.
- 遗留事项: repo-wide `./scripts/pm/lint.sh` still fails on unrelated historical task-log debt, but the current task no longer appears in that lint output.
- Action: Fixed runbook service names and five-node health checks, replaced stale `PR.md`, synchronized task/backlog metadata, normalized current task execution-log fields, and reran fresh pre-PR verification.
- Validation Command: `env -u RUSTC_WRAPPER cargo fmt --check`; `bash -n scripts/p2p-public-testnet-rebuild-validators.sh scripts/p2p-public-testnet-local-observer-sync.sh scripts/p2p-public-testnet-rebuild-validators.test.sh scripts/p2p-public-testnet-local-observer-sync.test.sh`; `bash scripts/p2p-public-testnet-local-observer-sync.test.sh`; `bash scripts/p2p-public-testnet-rebuild-validators.test.sh`; `./scripts/doc-governance-check.sh`; `git diff --check`; `./scripts/pm/workflow-lint.sh --task-uid task_bdb48338fac544849d8c681e9a7dd441 --phase current`; `./scripts/pm/lint.sh > .tmp/pm-lint-current-run.log 2>&1; rg task_bdb48338fac544849d8c681e9a7dd441 .tmp/pm-lint-current-run.log`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_transport_tests -- --nocapture`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_tests -- --nocapture`; two module-qualified noisy-external-peer targeted tests.
- Expected Result: pre-PR review blockers are addressed, current task workflow lint is clean, and runtime/script/doc verification passes before the branch is committed.
- Actual Result: all listed verification passed except repo-wide PM lint, which still returned nonzero for unrelated historical debt and no longer listed the current task; runtime tests passed with unchanged existing `oasis7_node` warnings.
- Blocker / Next Action: commit and push the review-fix updates, regenerate a merge-base review package, request focused follow-up review, then record `Pre-PR Local Role Review: passed` if no blockers remain.

## 2026-06-23 19:05:00 CST / tpm
- 完成内容: Addressed focused `blockchain_ops_engineer` P3 follow-up finding in the validator rebuild script test fixture.
- 遗留事项: none for the ops P3; Windows reset/reseed remains documented as SOP but not fully productized as a reusable PowerShell script.
- Action: Updated `scripts/p2p-public-testnet-rebuild-validators.test.sh` to use `oasis7-triad-sequencer.service` and `oasis7-triad-storage.service` in its fixture instead of stale `oasis7-testnet-*` names.
- Validation Command: `bash -n scripts/p2p-public-testnet-rebuild-validators.sh scripts/p2p-public-testnet-local-observer-sync.sh scripts/p2p-public-testnet-rebuild-validators.test.sh scripts/p2p-public-testnet-local-observer-sync.test.sh`; `bash scripts/p2p-public-testnet-local-observer-sync.test.sh`; `bash scripts/p2p-public-testnet-rebuild-validators.test.sh`; `git diff --check`; `./scripts/pm/workflow-lint.sh --task-uid task_bdb48338fac544849d8c681e9a7dd441 --phase current`.
- Expected Result: stale service names are removed from the regression fixture and the script/doc/task gates remain clean.
- Actual Result: script syntax/tests passed, `git diff --check` passed, and current task workflow lint passed.
- Blocker / Next Action: commit/push this P3 fix, regenerate final review package, then complete local role review closeout.

## 2026-06-23 20:23:42 CST / tpm
- 完成内容: Ran task closeout for the completed public testnet readiness and rollout task.
- 遗留事项: `task-closeout.sh` returned nonzero after writing current task closeout because repo-wide `./scripts/pm/lint.sh` still reports unrelated historical task-log debt; current task workflow lint is handled separately.
- Action: Executed `./scripts/pm/task-closeout.sh --role tpm --task-uid task_bdb48338fac544849d8c681e9a7dd441 --verify-command '<full pre-PR verification command>'`.
- Validation Command: closeout verify command covered `env -u RUSTC_WRAPPER cargo fmt --check`, rebuild/local-observer script syntax and tests, `./scripts/doc-governance-check.sh`, `git diff --check`, current task workflow lint, `observability_transport_tests`, and `observability_tests`.
- Expected Result: task YAML/backlog record `done`, `last_closed_at`, and successful verification metadata for PR-ready closeout.
- Actual Result: `.pm/tasks/task_bdb48338fac544849d8c681e9a7dd441.yaml` now records `status: done`, `last_verification_status: verified`, `last_verification_exit_code: 0`, and `last_closed_at: 2026-06-23T20:23:42+08:00`; backlog committed entry was removed. Helper ended nonzero only at repo-wide historical PM lint.
- Blocker / Next Action: run `claim-ready.sh --claim-type ready_for_pr`, record local role review packet, then rerun PR-ready workflow lint.

## 2026-06-23 20:27:00 CST / tpm
- 完成内容: Completed PR-ready gate reconciliation after closeout.
- 遗留事项: `claim-ready.sh --claim-type ready_for_pr` cannot mutate a closed task after closeout and reported immutable closed-task claim evidence; PR-ready workflow lint accepts the existing verified/closed task metadata.
- Action: Ran `./scripts/pm/claim-ready.sh --claim-type ready_for_pr --task-uid task_bdb48338fac544849d8c681e9a7dd441 --verify-command '<full pre-PR verification command>'`, then reran PR-ready workflow lint.
- Validation Command: `./scripts/pm/workflow-lint.sh --task-uid task_bdb48338fac544849d8c681e9a7dd441 --phase pr-ready --allow-unbound`.
- Expected Result: PR-ready workflow gate is satisfied before `prepare-task-pr`.
- Actual Result: `claim-ready.sh` returned `closed task claim evidence is immutable for non-completion claims`; `workflow-lint: OK (task_bdb48338fac544849d8c681e9a7dd441, phase=pr-ready)` with `PR.md` evidence.
- Blocker / Next Action: record `Pre-PR Local Role Review: passed`, commit/push closeout evidence, then run `prepare-task-pr.sh --create`.

## 2026-06-23 20:28:00 CST / tpm
- 完成内容: Pre-PR Local Role Review: passed.
- 遗留事项: Windows observer reset/reseed remains documented as SOP but is not yet a reusable repo-owned PowerShell automation script; repo-wide `.pm` lint historical debt remains outside this task.
- Action: Integrated local role review results for `blockchain_ops_engineer`, `runtime_engineer`, `qa_engineer`, and `repository_health_engineer`.
- Validation Command: focused review package `.pm/scratch/task_bdb48338fac544849d8c681e9a7dd441/review-packages/review-b5b0fee23..06055aa64.diff`; runtime review targeted tests; final PR-ready workflow lint.
- Expected Result: all actionable review findings are fixed or dispositioned before PR creation.
- Actual Result: `runtime_engineer` returned no findings for readiness semantics/tests; `repository_health_engineer` returned no findings after review package, PR.md, task YAML, and project trace fixes; `blockchain_ops_engineer` returned one P3 stale service-name fixture finding, fixed in `06055aa` and then confirmed `no_findings`; `qa_engineer` confirmed test coverage and prior blockers fixed, with only PR-ready closeout/claim evidence remaining, which is now satisfied by `workflow-lint --phase pr-ready`.
- Blocker / Next Action: create PR and enter normal PR CI/comments/merge watch.

## 2026-06-23 20:31:00 CST / tpm
- 完成内容: Recorded PR creation helper fallback.
- 遗留事项: none; use `gh pr create` fallback because the repository does not contain `scripts/pm/prepare-task-pr.sh` in this worktree.
- Action: Attempted `./scripts/pm/prepare-task-pr.sh --create --task-uid task_bdb48338fac544849d8c681e9a7dd441`, then searched `scripts/pm` for available PR helpers and checked for an existing PR.
- Validation Command: `rg --files scripts/pm | rg 'prepare|pr|task'`; `gh pr list --head task/world-runtime-testnet-health-status --json number,title,url,state`; `git status --short --branch`.
- Expected Result: if the repo helper exists, use it; otherwise create the PR through GitHub CLI with the already-verified `PR.md` packet.
- Actual Result: `prepare-task-pr.sh` is absent; no existing PR was found for the branch; worktree was clean before recording this fallback.
- Blocker / Next Action: commit this fallback note, create PR with `gh pr create`, then watch required checks/comments/mergeability.

## 2026-06-23 20:42:00 CST / tpm
- 完成内容: Fixed PR #581 required-gate file-size failure after merging latest `origin/main`.
- 遗留事项: existing `oasis7_node` unused/dead-code warnings remain unrelated to this task.
- Action: Split `build_chain_status_payload_zeroes_replication_gap_when_replication_disabled` out of `oasis7_chain_runtime_observability_tests.rs` into `oasis7_chain_runtime_observability_replication_tests.rs` and registered the new test module.
- Validation Command: `env -u RUSTC_WRAPPER cargo fmt --check`; `./scripts/check-rust-file-size.sh`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_tests -- --nocapture`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_replication_tests -- --nocapture`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_transport_tests -- --nocapture`; `./scripts/pm/workflow-lint.sh --task-uid task_bdb48338fac544849d8c681e9a7dd441 --phase pr-ready --allow-unbound`; `git diff --check`.
- Expected Result: CI `check-rust-file-size.sh` blocker is removed without changing runtime behavior.
- Actual Result: file-size check passed with zero oversized files; observability tests passed as 8 + 1 + 5 tests; PR-ready lint and diff check passed.
- Blocker / Next Action: commit/push the CI fix and continue PR checks/comments/merge watch.

## 2026-06-23 20:52:00 CST / tpm
- 完成内容: Addressed PR #581 automated review comments.
- 遗留事项: existing `oasis7_node` unused/dead-code warnings remain unrelated; Windows reset/reseed is still documented SOP rather than full PowerShell automation.
- Action: Required non-core/public peer identity before suppressing inbound timeout/protocol errors, restored remote `REPLICATION_ROOT` fallback for seed-from-remote, and made runtime-root bridge-state copy required while preserving legacy optional bridge-state behavior.
- Validation Command: `env -u RUSTC_WRAPPER cargo fmt --check`; `bash -n scripts/p2p-public-testnet-local-observer-sync.sh scripts/p2p-public-testnet-local-observer-sync.test.sh`; `bash scripts/p2p-public-testnet-local-observer-sync.test.sh`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_transport_tests -- --nocapture`; `./scripts/check-rust-file-size.sh`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_tests -- --nocapture`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime observability_replication_tests -- --nocapture`; rebuild script syntax/test; `./scripts/doc-governance-check.sh`; `git diff --check`; `./scripts/pm/workflow-lint.sh --task-uid task_bdb48338fac544849d8c681e9a7dd441 --phase pr-ready --allow-unbound`.
- Expected Result: review comments are fixed with targeted regression coverage and CI file-size gate remains clean.
- Actual Result: all listed checks passed; transport tests now cover degraded core peer inbound errors as blocking; local observer sync test now guards remote replication fallback and runtime-root bridge-state requirements.
- Blocker / Next Action: commit/push review fixes, resolve addressed review threads, and continue CI/merge watch.
