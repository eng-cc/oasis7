# task_dfb4d70a28884617b1506fa6570b34fc Execution Log

- task_uid: task_dfb4d70a28884617b1506fa6570b34fc
- title: align local public_testnet observer deploy contract
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-public-testnet-ecs-evidence-harvest

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-05-22 12:06:44 CST / runtime_engineer
- 完成内容:
  - 基于 `public-testnet-ecs-freshness-audit-2026-05-22.md` 的 drift 结论，把“下一步”收敛成 local observer deploy contract 修复，而不是继续泛化成“再加 ECS”。
  - 新增 repo-owned `scripts/p2p-public-testnet-local-observer-sync.sh`，提供 `render/apply` 两种模式：
    - 从 fresh ECS `node.env` 推导本机 observer 应使用的 two-validator `NODE_VALIDATORS_CSV`、`NODE_VALIDATOR_SIGNERS_CSV`、`NODE_GOSSIP_PEERS_CSV`、`REPLICATION_NETWORK_BOOTSTRAP_PEERS_CSV`、`REPLICATION_REMOTE_WRITERS_CSV`
    - 保留本机 `STATUS_BIND`、player-entry、data 路径与 `NODE_ID`
    - 写入 `NETWORK_TIER_MANIFEST_PATH`
    - 可选复制 live manifest 并安装 repo-owned `scripts/p2p-triad-node-start.sh`
  - 用 `.tmp/p2p_testnet_reality/20260522-100229/` 的 fresh snapshot fixture 验证：
    - `bash -n scripts/p2p-public-testnet-local-observer-sync.sh`
    - `render` 输出已去掉 `triad-testnet-local` validator / signer / writer，仅保留 `triad-testnet-sequencer` 与 `triad-testnet-storage`
    - `apply` 离线落在临时 app root 成功，确认 `node.env`、manifest、`start-node.sh` 与备份目录都能按预期写出
  - 新增证据 `doc/testing/evidence/public-testnet-local-observer-contract-sync-2026-05-22.md`，正式记录：
    - 2026-05-19 endpoint deploy 只覆盖 ECS，未把 local host 收进同一条 repo-owned deploy 路径
    - 当前会话对 `/opt/oasis7/p2p-testnet-local/**` 只有读权限，真实 live apply / restart 仍待具备写权限的 operator 执行
  - 同步回写 `doc/p2p/blockchain/p2p-formal-network-tiers-testnet-mechanism-2026-05-14.project.md`、`doc/p2p/project.md` 与 `doc/testing/evidence/public-testnet-live-candidate-endpoint-deploy-2026-05-19.md`，把 local observer remediation 与 historical deploy correction 固定成单一 repo 真值。
- 遗留事项:
  - 这次只补齐了 local observer 的 repo-owned sync 路径，还没有实际改写 root-owned `/opt/oasis7/p2p-testnet-local/**` 并重启 `oasis7-testnet-observer.service`。
  - ECS sequencer 的 `execution driver missing predecessor record for non-contiguous committed height` 仍是独立 blocker；在 local apply + sequencer runtime error 都清掉前，`public_testnet` aggregate readiness 继续保持 `block`。

## 2026-05-22 13:25:36 CST / runtime_engineer
- 完成内容:
  - 用户提供本机 sudo 密码后，已实际执行：
    - `scripts/p2p-public-testnet-local-observer-sync.sh apply` 到 `/opt/oasis7/p2p-testnet-local/config/node.env`
    - 安装 `/opt/oasis7/p2p-testnet-local/config/network-tier-public-testnet-live-candidate.json`
    - 安装 repo-owned `/opt/oasis7/p2p-testnet-local/bin/start-node.sh`
    - `systemctl restart oasis7-testnet-observer.service`
  - live apply 过程中又补了两处真实修正，并已同步回写 repo 脚本：
    - manifest install 不再原样拷贝 repo refs，而是把 `release_candidate_bundle_ref`、`genesis_ref`、`bootstrap_peer_ref` 本地化到 `/opt/.../config/`
    - `REPLICATION_REMOTE_WRITERS_CSV` 改为复用 ECS `node.env` 中的纯 hex allowlist，而不是误用 `NODE_VALIDATOR_SIGNERS_CSV` 的 `id:pubkey` 形式
  - 最终本机服务已恢复 `active`，`/v1/chain/status` 当前真值显示：
    - `network_tier.tier=public_testnet`
    - `network_tier.status=rehearsal`
    - `network_tier.bootstrap_peer_count=2`
    - `network_tier.rpc_ref=http://39.104.204.172:6631/v1/chain/status`
    - `network_tier.explorer_ref=http://39.104.205.67:6632/v1/chain/explorer/overview`
  - 这说明“本机 observer 仍未加载 formal manifest / 仍保留三 validator 合同”的旧 drift 已被实际清掉。
- 遗留事项:
  - local observer 现在仍停在 `committed_height=0`，新阻断已收敛为：
    - `fetch-commit authorization failed: requester_public_key_hex=8971af...`
  - 也就是说，下一层真实问题不再是 manifest/validator drift，而是两台 ECS 当前的 fetch-commit 授权面没有接受本机 `triad-testnet-local` 的 requester key。
  - ECS sequencer 先前的 `execution driver missing predecessor record for non-contiguous committed height` 也仍未被这一步处理，因此 `public_testnet` aggregate readiness 继续保持 `block`。

## 2026-05-22 16:46:00 CST / runtime_engineer
- 完成内容:
  - 基于本机 `/v1/chain/status` 与 `chain-runtime.log` 再次核对 live truth，确认 13:25 之后的现网阻断已继续前移：
    - `fetch-commit authorization failed` 与 `replication writer switch must start at sequence 1` 不再是当前主错误
    - 本机当前稳定停在 `committed_height=14` / `network_committed_height=14`
    - 当前主错误为 `gap sync height 15 execution hash validation failed`
  - 为了把 stale execution state 清理入口也纳入 repo-owned operator 路径，继续扩展 `scripts/p2p-public-testnet-local-observer-sync.sh`：
    - 新增 `reset-state` mode
    - 支持解析 `node.env` 中的 `$STACK_ROOT/...` 值
    - 会备份并清空 `EXECUTION_WORLD_DIR`、`EXECUTION_RECORDS_DIR`、`output/node-distfs/$NODE_ID`、`reward-runtime-execution-bridge-state.json`
  - 本机会话已多次真实执行 `reset-state`，备份目录落在 `/opt/oasis7/p2p-testnet-local/backups/local-observer-state-reset-*`。
  - 随后核对 runtime binary parity：
    - local current `oasis7_chain_runtime` `sha256=2f836980834da470882fef4ca7ab0598c984acfc42565d574acf2cd19c474cfe`
    - ECS sequencer current binary 同为 `2f836980834da470882fef4ca7ab0598c984acfc42565d574acf2cd19c474cfe`
    - ECS storage current binary 同为 `2f836980834da470882fef4ca7ab0598c984acfc42565d574acf2cd19c474cfe`
  - 即使 binary parity 已建立，本机仍在 `height 15` 失败；repo mirror 的 bundle 文件 `/opt/oasis7/p2p-testnet-local/config/public-testnet-live-candidate-bundle-2026-05-22.json` 还声明 `runtime_build.sha256=d1046485ae71a794cf0f5fb78561bd6068363ca53aee3ccac384d831829c07e8`，已与 live current binary hash 分叉。
  - 已同步回写：
    - `doc/testing/evidence/public-testnet-local-observer-contract-sync-2026-05-22.md`
    - `doc/testing/evidence/public-testnet-live-candidate-endpoint-deploy-2026-05-19.md`
    - `doc/testing/evidence/public-testnet-ecs-freshness-audit-2026-05-22.md`
    - `doc/testing/evidence/public-testnet-live-candidate-lanes-2026-05-22.tsv`
    - `doc/p2p/blockchain/p2p-formal-network-tiers-testnet-mechanism-2026-05-14.project.md`
    - `doc/p2p/project.md`
- 遗留事项:
  - 现网剩余 blocker 已不再是 local contract drift 或 binary 不一致，而是 release/runtime input drift：本机与 peer 在 `height 15` 的 execution block/state 真值仍分叉。
  - `shared_devnet_pass` 仍未满足，因此即使后续修平 height-15 分叉，aggregate `public_testnet` readiness 也仍需要回到 shared release-train gate 重新确认。

## 2026-05-22 16:44:10 CST / runtime_engineer
- 完成内容:
  - 继续按 sidecar 假设做 live 隔离：把本机 `STORAGE_ROOT` 迁出到 `/opt/oasis7/p2p-testnet-local/backups/storage-reset-20260522-164319/storage`，然后重启 `oasis7-testnet-observer.service`。
  - 复核结果表明这一步没有清掉当前分叉：
    - local `/v1/chain/status` 重新从 `committed_height=0` 起步
    - `storage.bytes_by_dir.execution_store_root` 已接近空白
    - 但 `last_error` 仍立即回到同一条 `gap sync height 15 execution hash validation failed`
  - 同窗确认两台 ECS 当前仍明显分叉：
    - `39.104.204.172` `committed_height=7209`，`last_error=execution driver missing predecessor record for non-contiguous committed height: last_applied=7209 incoming=13751 predecessor=13750`
    - `39.104.205.67` `committed_height=13750`，`last_error=null`
  - 已继续回写 `public-testnet-local-observer-contract-sync-2026-05-22.md`、`public-testnet-ecs-freshness-audit-2026-05-22.md` 与 formal network-tier project，避免 repo 继续误判成“只要再清本机 storage 就能恢复”。
- 遗留事项:
  - “本机 stale CAS/blob” 不是单独根因；下一刀应优先做 healthy peer 单源复现，或先修 `39.104.204.172` 的 predecessor-gap 再重试 local observer。
  - 上述新增 live 证据尚未提交到分支，需要在本轮收口时补一个 follow-up commit。
