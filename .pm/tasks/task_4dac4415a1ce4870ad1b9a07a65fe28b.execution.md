# task_4dac4415a1ce4870ad1b9a07a65fe28b Execution Log

- task_uid: task_4dac4415a1ce4870ad1b9a07a65fe28b
- title: Close platform-native release installer entrypoints
- owner_role: viewer_engineer
- worktree_hint: oasis7-world-simulator-launcher-release-platform-packages

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-14 22:35:52 CST / viewer_engineer
- 完成内容: 收口 release 平台原生入口任务。`scripts/build-game-launcher-bundle.sh` 新增平台识别与 bundle 级入口校验，Windows bundle 现在生成 `run-client.cmd`、`run-web-launcher.cmd`、`run-game.cmd`、`run-chain-runtime.cmd`，macOS bundle 现在生成 `oasis7 Client Launcher.app`（含 `Info.plist` 与 `Contents/MacOS/oasis7-client-launcher`）；`scripts/package-native-installer.sh` 在打包前强制执行 bundle 校验，macOS DMG staging 增加 `/Applications` symlink，Windows SFX 默认 `RunProgram=run-client.cmd`；新增 `scripts/validate-release-platform-entrypoints.sh` 作为平台入口治理脚本；同步回写 `doc/world-simulator/prd.md`、`doc/world-simulator/project.md`、`doc/site/github-pages/github-pages-release-download-pipeline-2026-03-01.{prd,project}.md`，并压缩 `doc/world-simulator/project.md` 到 995 行，解除 doc governance 阻塞。
- 完成内容: 验证完成：`bash -n scripts/build-game-launcher-bundle.sh`、`bash -n scripts/package-native-installer.sh`、`bash -n scripts/validate-release-platform-entrypoints.sh` 通过；macOS/Windows dry-run 证明 `.app` 与 `.cmd` 入口被生成；合成 bundle 通过 `scripts/validate-release-platform-entrypoints.sh --platform macos-x64|windows-x64`；真实 Linux release 路径完成 bundle 构建、入口校验、`.deb` 打包，并用 `dpkg-deb --contents` 验证 `/opt/oasis7/README.txt`、`/opt/oasis7/run-client.sh`、`/usr/bin/oasis7-client`；`./scripts/doc-governance-check.sh` 与 `git diff --check` 通过。
- 遗留事项: 当前变更已满足任务闭环，但尚未执行 snapshot review、commit、PR preflight/create；如需继续 landing，按仓库流程进入 review -> commit -> PR。
## 2026-04-14 22:41:11 CST / viewer_engineer
- 完成内容: 额外执行 `./scripts/pm/lint.sh`，结果 `pm-lint: OK`。
- 遗留事项: 两次执行 `./scripts/pm/codex-review-snapshot.sh` 都出现 reviewer 工具侧长时间不退出、无法给出稳定最终结论的问题；期间已确认 reviewer 能读取当前 diff、release workflow、bundle builder、packager 与相关文档，但 formal review 仍未形成可接受的 pass/fail 收敛结果，因此按仓库门禁暂不继续 commit/PR。
## 2026-04-14 22:46:00 CST / viewer_engineer
- 完成内容: 按用户明确指令跳过 formal snapshot review 阻断，继续执行 commit 与 PR 创建流程。
- 遗留事项: PR 创建后仍需在远端补看 required checks 与人工 review；本地 formal snapshot review 仍保持“工具侧未稳定完成”的已知 caveat。
## 2026-04-15 15:38:45 CST / viewer_engineer
- 完成内容: 处理 PR #79 review comment，修复 `scripts/validate-release-platform-entrypoints.sh` 仅用 `-e` 导致目录/文件/可执行位校验过松的问题；Linux/macOS 入口现在按 `dir/file/executable` 分类校验，Windows `.exe` / `.cmd` 维持文件存在性校验。
- 完成内容: 修复 `scripts/build-game-launcher-bundle.sh` 在显式 `--target-triple` 下错误回退宿主 `uname` 的平台识别逻辑；只有 `native` 才读取宿主平台，显式 triple 统一按 target 归类，避免 cross-build Linux bundle 时误生成 macOS/Windows 平台入口。
- 完成内容: 修复 `scripts/package-native-installer.sh --dry-run` 在 Linux/macOS 仍创建临时目录的问题，改为仅打印基于合成 dry-run 路径的命令，不再产生真实文件系统副作用；同时把 Linux AppImage tooling 从 GitHub `continuous` 改为固定 `appimagetool 1.9.1` 并校验 SHA256。
- 完成内容: 同步回写 `doc/world-simulator/prd.md`、`doc/world-simulator/project.md` 与 `doc/site/github-pages/github-pages-release-download-pipeline-2026-03-01.prd.md`，将 Windows 公开资产口径统一改为 NSIS 安装器，并修正 `AC-111` 重号。
- 完成内容: 验证通过：`bash -n scripts/{build-game-launcher-bundle.sh,package-native-installer.sh,validate-release-platform-entrypoints.sh}`、`./scripts/doc-governance-check.sh`、`git diff --check`；定向验证覆盖 release entrypoint 严格性负例、macOS `package-native --dry-run` 无副作用、以及显式 Linux target 在 Darwin `uname` 下不再生成 host-specific bundle 文件。
- 遗留事项: 需继续完成本次 comment follow-up 的 commit、push、review thread resolve，并复核 PR 当前 `mergeStateStatus` / `reviewDecision`。
