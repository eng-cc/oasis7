# task_9a300593735d419ba7de7f95bd67bead Execution Log

- task_uid: task_9a300593735d419ba7de7f95bd67bead
- title: fix release build-web-dist launcher wasm regression
- owner_role: viewer_engineer
- worktree_hint: /home/scc/worktrees/oasis7-testing-release-build-web-dist-launcher-wasm-regression

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-21 17:56:00 CST / viewer_engineer
- 完成内容: 复盘 GitHub Actions `Release Packages` run `24706176623`，确认 `release-gate-*` 已全部通过，新的阻断点迁移到 `build-web-dist`，具体失败签名为 `crates/oasis7_client_launcher/src/launcher_core.rs` 在 wasm32 构建时找不到 `parse_http_base_url`、`normalize_host_for_url`、`host_for_url`，导致 `trunk build --release --dist ../../output/release/web-launcher-dist` 退出 101。
- 完成内容: 检查 `crates/oasis7_client_launcher/src/launcher_core.rs` 与 `launcher_core_http.rs` 后确认 helper 本身并未缺失，根因是 `pub(crate) use self::http_support::{...}` 被整体包在 `#[cfg(not(target_arch = "wasm32"))]`，把跨目标可用的 URL helper 一并错误屏蔽。已将 `host_for_url`、`normalize_host_for_url`、`parse_http_base_url` 拆为跨目标 re-export，仅保留 `check_provider_loopback_http_provider` / `normalize_host_for_connect` 在 native-only 分支。
- 完成内容: 验证通过 `env -u RUSTC_WRAPPER cargo build --target wasm32-unknown-unknown --manifest-path crates/oasis7_client_launcher/Cargo.toml --release --bin oasis7_client_launcher`，并复跑 workflow 同款 `trunk build --release` 两条链路：`crates/oasis7_viewer -> output/release/web-dist`、`crates/oasis7_client_launcher -> output/release/web-launcher-dist`；launcher dist 已生成 `index.html`、`oasis7_client_launcher-*.js`、`oasis7_client_launcher-*_bg.wasm`。
- 遗留事项: 仍需完成 PM close / signal 收口、commit、PR 合流，然后基于合入后的最新 `main` 再次触发 GitHub release，确认 `build-web-dist` 线上恢复。
