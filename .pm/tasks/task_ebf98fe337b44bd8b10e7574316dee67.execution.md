# task_ebf98fe337b44bd8b10e7574316dee67 Execution Log

- task_uid: task_ebf98fe337b44bd8b10e7574316dee67
- title: Close issue 163 shared gameplay semantics parity across Web and pure API
- owner_role: qa_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-simulator-gameplay-parity-shared-player-gameplay-contract

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-28 21:32:00 CST / qa_engineer
- 完成内容: 关闭 GitHub issue `#166`；为 issue `#163` 新建独立 worktree 与 `.pm` task；回写 `doc/world-simulator/project.md`、`doc/world-simulator/viewer/viewer-web-software-safe-mode-2026-03-16.{prd,project}.md`、`doc/testing/{prd,project}.md` 与 `testing-manual.md`，将 `software_safe` / `pure_api` 的 formal 玩家可读语义明确收口为共享 `snapshot.player_gameplay` contract，并明确 `parity_verified` 只适用于 active LLM access。
- 完成内容: 更新 `scripts/oasis7-pure-api-parity-smoke.sh`，禁止 `--no-llm` 被误用为正式 parity 样本，同时在 summary 中直接输出“当前阶段/当前目标/进度/阻塞/下一步/最近关键世界变化”六个共享玩家问题的答案。
- 完成内容: 新增 `doc/testing/evidence/pure-api-shared-player-gameplay-parity-2026-04-28.md`，将旧 `2026-03-19` no-LLM parity 样本降级为历史输入，并给出现行 QA verdict。
- 完成内容: 已验证 `bash -n scripts/oasis7-pure-api-parity-smoke.sh`、`node crates/oasis7_viewer/scripts/software-safe-feedback-contract.test.mjs`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_pure_api_client -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --lib compat_snapshot_exposes_player_gameplay_snapshot -- --nocapture`、`git diff --check` 全部通过。
- 完成内容: 后续已确认 `main` 的 `config.toml` 可复用为当前 task worktree 的 active-LLM 预检配置；`./scripts/check-active-llm-provider.sh --pretty` 在注入该配置后返回 `status=ok`，hello/tool-call 均通过。
- 完成内容: 已尝试补跑 active-LLM `./scripts/oasis7-pure-api-parity-smoke.sh --tier required --with-llm`，并进一步手动复现 `snapshot -> gameplay-action -> step`。
- 完成内容: 当前 active-LLM fresh parity blocker 已收敛为 `pure_api_client live_control.step` 失败签名：`waiting for live_control step ack: connection closed`；对应 stack 日志为 `viewer runtime live server error: Broken pipe`。`snapshot --player-gameplay-only` 与 `gameplay-action --with-snapshot` 均可成功，不是 LLM 配置缺失，也不是 canonical `player_gameplay` 分叉。
- 遗留事项: 若要直接关闭 issue `#163`，需要先修复 active-LLM `pure_api` 的 `live_control.step` 链路，再补 fresh required/full evidence bundle。

## 2026-04-29 09:15:00 CST / qa_engineer
- 完成内容: 修复 `oasis7_pure_api_client` 将 socket 读超时误判成 `connection closed` 的问题；现在 `read timeout` 与真正 EOF 分离，CLI 默认超时提高到 `20s`，不再把服务端后续 ack 误写成假性断链。
- 完成内容: 调整 `scripts/oasis7-pure-api-parity-smoke.sh` 的 active-LLM required-tier 采证口径：命令级超时提高到 `60s`；当 canonical `snapshot.player_gameplay` 已经进入 `post_onboarding` 能力阶段时，允许直接以该 snapshot 作为 followup 证据；跟进目标集合纳入 `post_onboarding.establish_first_capability`；pre-session `gameplay_action` 的结构化 `session_not_found` 响应视为有效协议响应，不再误判为 contract 失败。
- 完成内容: 新鲜 active-LLM required rerun 已通过：`./scripts/oasis7-pure-api-parity-smoke.sh --tier required --with-llm`，产物位于 `output/playwright/playability/pure-api-required-20260429-091118/`，摘要 `pure-api-summary.md` 结论为 `pass`，显示 `post_onboarding.establish_first_capability` / `progress_percent=20` / `recent_feedback=completed_advanced` / `recovery_status=catch_up_ready`。
- 完成内容: 已复跑 `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_pure_api_client -- --nocapture` 与 `bash -n scripts/oasis7-pure-api-parity-smoke.sh`，并将 fresh verdict 回写 `doc/testing/evidence/pure-api-shared-player-gameplay-parity-2026-04-28.md`。
- 遗留事项: `test_tier_required` blocker 已解除；若后续需要 release-candidate 压力样本，可再补 `--tier full`，但不再阻断 issue `#163` 当前收口。
