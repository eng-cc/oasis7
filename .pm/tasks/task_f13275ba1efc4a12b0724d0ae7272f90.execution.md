# task_f13275ba1efc4a12b0724d0ae7272f90 Execution Log

- task_uid: task_f13275ba1efc4a12b0724d0ae7272f90
- title: Implement dual-mode observation adapter and fixture diff validation
- owner_role: agent_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-simulator-agent-dual-mode-observation-adapter-fixture-diff

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-07 09:58:09 CST / agent_engineer
- 完成内容: 为 `TASK-WORLD_SIMULATOR-296` 在 `crates/oasis7/src/simulator/decision_provider.rs` 引入 provider-facing observation payload，把 provider request 的 observation 从原始 `Observation` 切到 contract-aware 结构；`player_parity` 与 `headless_agent` 现已在 payload 层真实分叉，前者不再携带 `local_navigation_graph` / `interaction_targets` 等 headless-only 辅助字段。
- 完成内容: 为 `DecisionRequest` 增加 contract validation，bridge 在 `crates/oasis7/src/bin/oasis7_openclaw_local_bridge.rs` 现会对 `unsupported_schema_version`（含 observation/action schema）与 `mode_observation_mismatch` 返回结构化 `provider_error`，不再让 schema/mode mismatch 静默穿透到后续 prompt 构建。
- 完成内容: 更新 bridge prompt/build guardrail helper 以消费新 observation schema，并新增 fixture diff / schema mismatch / mode mismatch 定向测试，验证 dual-mode 不再只是 metadata 标签。
- 完成内容: 为 provider-facing `nearby_entities` / `local_navigation_graph` / `interaction_targets` 增加按距离与 ID 的稳定排序，避免原始 `HashMap` 迭代顺序把 “nearest reachable” 提示与 fixture diff 做成非确定性结果。
- 遗留事项: `TASK-WORLD_SIMULATOR-297 / T4.4` 仍需由 `viewer_engineer` / `runtime_engineer` 为 launcher probe、runtime live snapshot、viewer debug context 与 parity summary 补 `capabilities/supported_action_sets` 兼容校验和 `fallback_reason` 透传。
