# task_5215d92421494ebda9281cb3a9f40557 Execution Log

- task_uid: task_5215d92421494ebda9281cb3a9f40557
- title: Implement P2PARCH-4 lane/QoS substrate
- owner_role: runtime_engineer
- worktree_hint: oasis7-p2p-p2parch-4-lane-qos-substrate
## 2026-04-02 17:20:34 CST / runtime_engineer
- 完成内容: 为 `P2PARCH-4` 新建独立 worktree `task/p2p-p2parch-4-lane-qos-substrate`（`/home/scc/worktrees/oasis7-p2p-p2parch-4-lane-qos-substrate`），并创建/启动 `TASK-PM-0029`，将该任务绑定到 `doc/p2p/network/p2p-mainnet-private-reachability-architecture-2026-04-01.prd.md` 与 `.project.md` 的 lane/QoS substrate 范围。
- 完成内容: 已读取 `runtime_engineer` 角色卡、专题 PRD/project、当前 task 文件与 role backlog，确认本轮 owner 为 `runtime_engineer`，输入为统一 transport substrate，目标输出为 lane registry、consensus/sync/blob-state/control QoS policy 与 role-aware peer subset。
- 遗留事项: 下一步进入实现与文档回写，收敛 traffic lane registry、QoS policy、链适配器绑定边界及对应 required/full 验证；收口前仍需补 subagent review、PM close、测试与 commit/landing。

## 2026-04-02 18:11:49 CST / runtime_engineer
- 完成内容: 已在 `crates/oasis7_proto` 冻结 `NetworkLane` / `NetworkLaneQosClass` / `NetworkLaneOperation` 与 topic+protocol classifier，并把 `PeerRecord` 提升为显式 `capability_lanes` schema；legacy record 若未声明 lane capability，会按 canonical `node_role` 自动回填默认值，保持 discovery/selection 向后兼容。
- 完成内容: 已在 `crates/oasis7_net` 把 lane substrate 接进 libp2p runtime：gossipsub subscription inbox 现在按 lane 区分配额，req/resp 在 `fetch-commit` / `fetch-blob` 等路径会优先按 peer record 的 lane capability 过滤 candidate peers；同时补了 `filter_request_peers_by_lane` 定向测试，验证 blob lane 会优先选择声明 `blob_state` capability 的 peer。
- 完成内容: 已在 `crates/oasis7_node` / `crates/oasis7` 落 role-aware lane registry：replication/consensus topic 绑定提升成 `TrafficLaneRegistry`，`NodeNetworkPolicy` 新增 lane operation gate，runtime 会拒绝 `observer_light` publish consensus lane、`relay` 请求或服务 data lane 等明显越权路径；默认 peer record 也会显式携带从 `node_role_claim` 派生的 `capability_lanes`。
- 完成内容: 已回写 `doc/p2p/network/p2p-mainnet-private-reachability-architecture-2026-04-01.{prd,design,project}.md`，把 peer record 的 `capability_lanes`、lane taxonomy/QoS substrate 与 `P2PARCH-4` 已完成状态写回专题文档。
- 完成内容: 验证已通过 `env -u RUSTC_WRAPPER cargo test -p oasis7_proto distributed_net -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net --features libp2p libp2p_net::tests -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node types::tests -- --nocapture`、`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_chain_runtime`、`cargo fmt --all`、`./scripts/doc-governance-check.sh` 与 `git diff --check`。
- 遗留事项: 仍需执行 subagent review、PM close、commit 与标准化 landing；更细的 peer scoring / anti-eclipse / relay budget / mixed-topology evidence 继续留在 `P2PARCH-5/6`。

## 2026-04-02 21:20:31 CST / runtime_engineer
- 完成内容: 已处理 commit 前 subagent review 反馈，收紧 `observer_light` 的 data-lane 行为：`observer_light` 不再默认宣称 `sync/blob_state` 服务能力，peer record 显式声明这两条服务 capability 时也会被拒绝；lane gate 现允许 observer 主动 request data lane，但禁止其 serve 对应协议。
- 完成内容: 已调整 `register_replication_fetch_handlers`，改为仅按 `node_role_claim` 注册允许 serve 的协议 handler，不再因为 observer/轻量节点未开放 data-lane serve 权限而导致启动失败；同时新增节点级回归测试，验证 observer runtime 启动成功但不会注册 `fetch-commit` / `fetch-blob` handler。
- 完成内容: 已同步回写专题 design/project 文档，明确 `observer-light` 默认只声明 `consensus_gossip + control`，并注明其可 request 但不 serve `sync/blob_state`。
- 完成内容: 本轮增量验证已通过 `env -u RUSTC_WRAPPER cargo test -p oasis7_proto distributed_net -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_proto distributed_dht -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net --features libp2p libp2p_net::tests -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node observer_replication_runtime_starts_without_registering_data_service_handlers -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node runtime_network_replication_gap_sync_reports_error_after_retries_exhausted -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node types::tests -- --nocapture`、`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_chain_runtime` 与 `./scripts/doc-governance-check.sh`。
- 遗留事项: 剩余收口仅包含 PM close、任务状态切换、最终 `git diff --check` / commit 与标准化 landing。

## 2026-04-02 22:03:43 CST / runtime_engineer
- 完成内容: 已处理第二轮 subagent review 暴露的 `feedback_p2p` 绕过问题：runtime 初始化 `FeedbackAnnounceBridge` 前会显式检查 `blob_state` lane 的 publish+subscribe 权限，`observer_light` 若启用 `feedback_p2p` 现在会在启动时直接收到 `InvalidConfig`，不能再绕过 lane gate 订阅或发布 `feedback.announce`。
- 完成内容: 已把反馈同步 hardening 用例改成 `full_storage` 侧验证，并新增 observer 启动拒绝回归测试，确保 `feedback_p2p` 仍能在允许的 blob/state 角色上工作，同时 observer 默认配置会被阻断。
- 完成内容: 已同步回写专题 design/project 文档，明确 `feedback_p2p` 同样受 `blob_state` lane 的 publish/subscribe gate 约束。
- 完成内容: 本轮增量验证已通过 `env -u RUSTC_WRAPPER cargo test -p oasis7_node runtime_start_rejects_observer_feedback_p2p_without_blob_state_lane_access -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node runtime_feedback_submit_publishes_and_peer_ingests -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node observer_replication_runtime_starts_without_registering_data_service_handlers -- --nocapture`、`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_chain_runtime`、`./scripts/doc-governance-check.sh` 与 `git diff --check`。
- 遗留事项: review 仍提示 `oasis7_net` 的真实 worker-loop/request-with-providers 级覆盖可以继续加强；本任务先将其记录为 residual risk，后续可在 `P2PARCH-5` 补充更贴近真实 discovery/worker loop 的 lane-aware request 集成回归。
