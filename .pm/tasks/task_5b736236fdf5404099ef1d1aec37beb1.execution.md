# task_5b736236fdf5404099ef1d1aec37beb1 Execution Log

- task_uid: task_5b736236fdf5404099ef1d1aec37beb1
- title: fetch-commit success dedupe after long validation
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-runtime-fetch-commit-success-dedupe

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-17 18:17:33 CST / runtime_engineer
- 完成内容: 为 `doc/world-runtime/prd.md` 与 `doc/world-runtime/project.md` 新增 `PRD-WORLD_RUNTIME-034` / `fetch-commit-success-dedupe` 追踪，明确这轮优化只覆盖 `fetch-commit` 成功响应的短窗口、本地、payload-scoped success cache，不扩大到 `found=false`、协议错误或上层 gap-sync 语义改写。
- 完成内容: 在 `crates/oasis7_node/src/libp2p_replication_network.rs` 增加 `fetch_commit_success_cache_after` 配置与 `recent_fetch_commit_successes` 本地缓存，只在 `REPLICATION_FETCH_COMMIT_PROTOCOL` 命中且响应可解析为 `found=true` 时复用成功结果；缓存查询前会按时间窗口清理过期项，窗口过后恢复真实发网请求。
- 完成内容: 在 `crates/oasis7_node/src/libp2p_replication_network/tests.rs` 补齐 `libp2p_replication_network_fetch_commit_success_response_enters_short_cache` 与 `libp2p_replication_network_fetch_commit_not_found_does_not_enter_success_cache` 回归，并顺手收敛既有 `libp2p_replication_network_request_retries_next_peer_when_remote_handler_fails` 的并发时序抖动，避免整组 `libp2p_replication_network` 回归在全套并发执行时被瞬时 peer 抖动打红。
- 完成内容: 已完成验证 `env -u RUSTC_WRAPPER cargo test -p oasis7_node libp2p_replication_network_fetch_commit_ -- --nocapture`、`env -u RUSTC_WRAPPER cargo check -p oasis7_node --tests`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node libp2p_replication_network -- --nocapture`、`./scripts/doc-governance-check.sh`、`git diff --check`。
- 遗留事项: 这轮只收口了“同 payload 的已成功 `fetch-commit` 重复拉取”噪音；若三节点实流里 `fetch-commit` 仍是热点，下一优先面应继续看 gap-sync 调度频率、批量高度请求或 peer-record/discovery 残余噪音，而不是直接拉长 success cache 窗口。
