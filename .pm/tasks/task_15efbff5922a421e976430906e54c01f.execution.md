## 2026-05-13 22:30:10 CST / viewer_engineer
- 完成内容:
  - 去掉 `pixel_world_runtime_loader.js` 在 wasm bridge 导入失败时退回 JS renderer 的路径，改为返回结构化 runtime-unavailable fatal，并让 Host 走显式 fallback surface。
  - 更新 `pixel_world_host.jsx` 文案，明确当前嵌入式世界舞台以 wasm bridge 为唯一 renderer runtime，bridge 缺失时只退 host fallback。
  - 新增 `pixel_world_runtime_loader.test.js` 与 `pixel_world_host.test.jsx`，覆盖“wasm bridge 成功加载”和“wasm bridge 缺失时显式 fallback UI”两条分支。
  - 回写 `viewer-web-entry-visual-redesign-2026-05-12.prd.md` / `.project.md`，补齐 wasm-only renderer contract 与本任务追踪。
- 验证:
  - `npm --prefix crates/oasis7_viewer run test:ui`
  - `npm --prefix crates/oasis7_viewer run build:software-safe`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 未执行 headed browser / agent-browser 回归；本轮变更集中在 repo-owned loader/host 契约与显式失败 surface。
