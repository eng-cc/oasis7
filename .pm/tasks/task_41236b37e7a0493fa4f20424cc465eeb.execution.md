# task_41236b37e7a0493fa4f20424cc465eeb Execution Log

- task_uid: task_41236b37e7a0493fa4f20424cc465eeb
- title: Switch GitHub Release downloads to direct installers
- owner_role: viewer_engineer
- worktree_hint: oasis7-site-release-direct-installers

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-14 11:18:40 CST / viewer_engineer
- 完成内容: 将 `.github/workflows/release-packages.yml` 的公开产物名从 `oasis7-linux-x64.tar.gz` / `oasis7-macos-x64.tar.gz` / `oasis7-windows-x64.zip` 切换为 `oasis7-linux-x64.deb` / `oasis7-macos-x64.dmg` / `oasis7-windows-x64.exe`；`package-native` 现先落标准 `oasis7-<platform>` bundle 目录，再调用新增的 `scripts/package-native-installer.sh` 输出平台原生安装器，`publish-release` 和 `oasis7-checksums.txt` 已同步切到新资产名。
- 完成内容: 新增 `scripts/package-native-installer.sh`，Linux 使用 `dpkg-deb` 生成真正的 `.deb` 并将 bundle 安装到 `/opt/oasis7`，同时提供 `/usr/bin/oasis7-*` 启动入口；macOS 使用 `hdiutil` 生成 `.dmg`；Windows 使用 `7z` SFX 模块生成自解压 `.exe`，避免公开下载层继续暴露 `.tar.gz` / `.zip` 包装。
- 完成内容: 同步 `site/index.html`、`site/en/index.html`、`scripts/site-download-check.sh`、`doc/site/prd.md`、`doc/site/project.md` 与 `doc/site/github-pages/github-pages-release-download-pipeline-2026-03-01.prd.md/.project.md`，将公开下载入口、站点门禁和文档真值统一切到 `.deb` / `.dmg` / `.exe`。
- 完成内容: 本地验证通过：
  - `bash -n scripts/package-native-installer.sh`
  - `bash -n scripts/release-prepare-bundle.sh`
  - `bash -n scripts/build-game-launcher-bundle.sh`
  - `./scripts/site-download-check.sh`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
  - 以临时 bundle 目录实测 `./scripts/package-native-installer.sh --platform linux-x64 --bundle-dir <tmp>/oasis7-linux-x64 --out-dir <tmp>/out --asset-name oasis7-linux-x64.deb --version 0.0.0`，`dpkg-deb --info/--contents` 已确认生成的 `.deb` 内含 `/opt/oasis7` bundle 与 `/usr/bin/oasis7-*` 入口。
- 遗留事项: 尚未触发远端 GitHub `Release Packages` run 验证 `.dmg` / `.exe` 在对应 runner 上的最终落盘行为；下一步应在提交后通过新 tag 或手动触发复核三平台产物与 `latest/download` 直链。

## 2026-04-14 11:24:33 CST / viewer_engineer
- 完成内容: 按提交前门禁执行 `./scripts/pm/codex-review-snapshot.sh`；snapshot 内的 `codex exec review --uncommitted` 已成功读取当前 diff，并完成文档/脚本/workflow 多轮扫描，但在未输出 findings/结论的情况下持续无新输出，多次轮询后仍未收敛。
- 完成内容: 为避免把未完成的 formal review 误记为通过，已在终端发送中断信号结束本轮 review；当前失败签名为 `Review was interrupted. Please re-run /review and wait for it to complete.`，需要后续在稳定环境下重跑快照 review 后再提交。
- 遗留事项: commit/PR 仍被 formal snapshot review 未稳定收敛所阻断；本地实现、文档与验证已完成，但当前 worktree 仅保留未提交改动。

## 2026-04-14 13:17:08 CST / viewer_engineer
- 完成内容: 再次重跑多轮 `./scripts/pm/codex-review-snapshot.sh`（`gpt-5.4` / `gpt-5.3-codex`、TTY / non-TTY），review 过程可稳定读取并扫描当前 diff，但仍未产出 final message；期间 reviewer 额外指出 `.agents/skills/oasis7` 仍残留旧 `.tar.gz` / `.zip` release 资产口径。已据此同步修复 `SKILL.md`、`references/real-play-config.md`、`scripts/oasis7-run.sh` 与 `scripts/oasis7-run-download-test.sh`，将 skill 下载辅助链路改为 `.deb` / `.dmg` / `.exe` 当前真值，并回归通过 `bash ./.agents/skills/oasis7/scripts/oasis7-run-download-test.sh`。
- 完成内容: 用户已明确授权“跳过”本轮 formal snapshot review 门禁；据此解除 task 的临时 blocked 状态，继续执行 commit 与标准化 PR 创建。
- 遗留事项: 虽然用户已授权跳过 formal review，但 reviewer CLI 持续无法输出 final message 的工具异常仍建议后续单独跟进，不应视为已根治。
