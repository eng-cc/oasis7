# task_382f3d2cc062445ca2f07294f17daf0e Execution Log

- task_uid: task_382f3d2cc062445ca2f07294f17daf0e
- title: Resolve P2PARCH-6 proxy failure signatures
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-p2p-p2parch-6-proxy-failure-signatures

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-04-07 14:16:30 CST / runtime_engineer
- 完成内容: 继续消化 `P2PARCH-6` triad distributed proxy soak 的 live blocker，并把修复推进到更深一层的 replication request path。先为 `oasis7_chain_runtime` 增加显式 `--replication-network-listen/--replication-network-peer` 拓扑接线、bootstrap redial、node-scoped libp2p identity、private/validator-core peer record materialization 修正，确认 latest blocker 已不再是本地 fallback / shared peer id / invalid peer record。随后为 `oasis7_net` 把 rendezvous register/discover 改成显式 opt-in，并把 rendezvous client/server behaviour 改成可关闭；用真实 bin 重跑 triad distributed soak 后确认 observer 上的 `libp2p command queue disconnected` / rendezvous server panic 已消失。继续把 `Libp2pReplicationNetwork` 的 wrapper request 改成“先保留 inner lane-aware provider 过滤，再在 `no healthy provider` 上 fallback 到真正的 `request_to_peer` 直发”，避免 wrapper 层再次把 observer 这类不提供 data handler 的 peer 强塞给 `fetch-commit/fetch-blob`。另为 peer manager 增加 loopback `/24` 豁免，确认本机 triad harness 不再因为 `127.0.0.1` subnet concentration 被 quarantine 自己打断。
- 完成内容: 已新增/通过的回归包括 `oasis7_net::libp2p_net::tests::peer_record_enables_rendezvous_only_when_source_is_declared`、`oasis7_net::tests::libp2p_rendezvous_discovery_acquires_peer_from_bootstrap_registration`、`oasis7_net::peer_manager::tests::recompute_ignores_loopback_subnet_concentration`、`oasis7_node::libp2p_replication_network::tests::libp2p_replication_network_request_retries_next_peer_when_remote_handler_fails`，以及 `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime -- --nocapture` 与多次 `cargo build -p oasis7 --bin oasis7_chain_runtime`。
- 完成内容: 已固定新的 live evidence 路径，便于后续继续比对：`.tmp/p2p_longrun_debug_after_bin_rebuild/20260407-135747` 证明 rendezvous panic 已清，但仍有 `ConnectionClosed/no connected peers/ErrNotFound`；`.tmp/p2p_longrun_debug_after_request_to_peer/20260407-141125` 证明 request wrapper 修正后 `last_error_samples` 和 `known_peer_heads_zero_samples` 继续下降；`.tmp/p2p_longrun_debug_after_loopback_exempt/20260407-141501` 证明 loopback quarantine 豁免后 early-window `ConnectionClosed` 基本清空，当前 triad distributed live blocker 已收敛成 sequencer 的 storage challenge 对 `fetch-blob` 连续拿到 `ErrNotFound`。
- 遗留事项: `P2PARCH-6` 仍未完成；当前 primary blocker 已从 rendezvous/discovery 崩溃收敛为 replication data service 选择边界。需要继续定位为什么 storage challenge 的 `endpoint.request_json(REPLICATION_FETCH_BLOB_PROTOCOL, ...)` 仍会命中不提供 blob handler 的 peer，或为什么 live peer record 选择在该路径上没有把 observer 排除干净。
- 遗留事项: 本轮还没进入 PM close / review / commit / landing；在拿到更干净的 triad distributed soak 之前，不应把 `full_proxy_ready` 从 `false` 提升。

## 2026-04-07 14:52:52 CST / runtime_engineer
- 完成内容: 把 storage challenge gate 从盲选 `request_json(fetch-blob)` 改成“先查 DHT content providers，再用 `request_json_with_providers` 定向请求”的路径，并在 `NodeReplicationNetworkHandle` / `ReplicationNetworkEndpoint` 中接入可选 DHT 句柄与 `local_provider_id` 过滤，避免把本机 provider 回流成远端候选。同步为 live 路径补齐 `Libp2pReplicationNetwork: DistributedDht` 委托，以及此前缺失的 `request_with_providers` 实现，使 libp2p wrapper 真正尊重 provider subset，而不是继续静默退回无差别 peer 轮询。
- 完成内容: 新增/通过回归：`tests::runtime_replication_storage_challenge_gate_prefers_dht_blob_providers`、`libp2p_replication_network::tests::libp2p_replication_network_request_with_providers_honors_provider_subset`、`cargo test -p oasis7_node runtime_replication_storage_challenge_gate_ -- --nocapture`、`cargo test -p oasis7 --bin oasis7_chain_runtime -- --nocapture`。其中 provider-aware gate 回归证明 storage challenge 已会把 DHT provider 列表传入 fetch-blob；wrapper subset 回归证明 libp2p wrapper 不再忽略 providers。
- 完成内容: 新证据显示 triad distributed live blocker 继续收敛。基线 `.tmp/p2p_longrun_debug_after_unsupported_cache/20260407-142040` 为 `last_error_samples=6; consensus_hash_divergence count=3 heights=1,3; known_peer_heads_zero_samples=41`；第一次接入 DHT provider 但 live wrapper 尚未尊重 subset 时，`.tmp/p2p_longrun_debug_after_blob_provider_dht_filter/20260407-143728` 改善到 `last_error_samples=4; consensus_hash_divergence count=2 heights=5; known_peer_heads_zero_samples=43`；补齐 live `request_with_providers` 后，`.tmp/p2p_longrun_debug_after_live_provider_subset/20260407-144237` 进一步改善到当前最好结果 `last_error_samples=2; consensus_hash_divergence count=1 heights=5; known_peer_heads_zero_samples=40`。
- 完成内容: 试验性追加了“按 peer record node_role 过滤 blob providers”的版本，并用 `.tmp/p2p_longrun_debug_after_provider_role_filter/20260407-144953` 验证。结果 `last_error_samples` 反弹到 `7` 且 `known_peer_heads_zero_samples=43`，说明该过滤在当前 triad 样本下会把候选削得过狠，已经回退，不纳入有效修复集合。
- 遗留事项: `P2PARCH-6` 仍未完成，但 live failure signature 已从“盲打 observer/command queue/rendezvous panic”压缩为更纯粹的 `fetch-blob` 服务可用性问题。当前剩余样本主要是 `NetworkRequestFailed { ErrNotFound, NetworkProtocolUnavailable(fetch-blob) }` 与短时 `libp2p-replication no connected peers`，需要继续追 `content_hash -> provider publish / provider freshness / blob真正落盘可服务` 的时序边界。
- 遗留事项: 本轮仍未进入 PM close / review / commit / landing；除非后续再拿到更稳的 mixed-topology/live evidence，否则不应宣称 `P2PARCH-6` 收口。

## 2026-04-07 15:18:20 CST / runtime_engineer
- 完成内容: 已把 replication blob provider publish 补进 live 主路径，而不再只依赖后续 replica maintenance。具体包括：本地 `build_local_commit_message` 成功后通过 replication endpoint 发布本地 provider；远端 replication ingest / gap sync 成功落盘后，若本节点具备 `BlobState::Serve` 资格，也立即对相同 `content_hash` 发布 provider。新增回归 `runtime_local_replication_publishes_blob_provider_to_dht` 与 `runtime_remote_replication_ingest_publishes_blob_provider_to_dht`，两条均已通过。
- 完成内容: 已对“跳过最新 hash”“等待 peer head 再启 gate”“按 peer record role 再过滤 provider”做了小范围 live 试探。结果显示 role 过滤与 peer-head gate 都会让整体 metric gate 回退，因此相关试探均已回退；`recency lag` 试探也已回退，不保留在代码中。
- 完成内容: 当前代码仍保留的有效修复集合是：provider-aware fetch-blob request、live `request_with_providers` 真正生效、self-provider 过滤、replication blob provider publish。最新一次仅含有效修复集合的 soak 基线仍以 `.tmp/p2p_longrun_debug_after_provider_publish/20260407-150845` 与更优的 `.tmp/p2p_longrun_debug_after_live_provider_subset/20260407-144237` 为主，后者仍是当前最佳综合证据。
- 遗留事项: provider publish 已经消掉了早期 `network blob not found` 样本，但未能彻底清除 `ErrNotFound -> NetworkProtocolUnavailable(fetch-blob)` 与 `no connected peers`。下一步需要继续定位“provider 已命中 sequencer/storage peer_id，但对端仍回 handler unavailable”的真实成因，重点看 provider freshness 与 libp2p peer connectivity / request routing 的交叉窗口。

## 2026-04-07 15:50:40 CST / runtime_engineer
- 完成内容: 继续沿 `P2PARCH-6` 剩余 live blocker 往下压，补了两组 runtime 侧缓解。其一把 `Libp2pNetwork::register_handler` 改成同步 ack，确保 `NodeRuntime::start()` 返回前 libp2p 后台线程已经真正安装好 fetch handler；其二把 storage challenge gate 的 provider-aware fetch-blob 改成“优先 provider subset、遇到 `NetworkProtocolUnavailable`/provider-only-empty 时回退到普通 lane-aware request”，并新增回归 `runtime_replication_storage_challenge_gate_falls_back_after_provider_route_unavailable`。同时为 `oasis7_net` 的基础 smoke 补齐 peer record 模板，使其不再被 peer manager 的 `MissingPeerRecord` quarantine 门禁误伤。
- 完成内容: 针对 live 中仍反复出现的 `no connected peers for protocol`，在 `Libp2pReplicationNetwork` wrapper 上增加了短窗口 connected-peer/provider wait（12 x 150ms），让 request/request_with_providers 在 bootstrap redial 收敛窗口内先等待再判失败；并新增回归 `libp2p_replication_network_request_waits_for_delayed_bootstrap_connection`。相关定向回归均已通过：`env -u RUSTC_WRAPPER cargo test -p oasis7_node libp2p_replication_network_ -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node runtime_replication_storage_challenge_gate_ -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net libp2p_ -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime -- --nocapture`。
- 完成内容: 追加了两轮 90s triad distributed soak 证据。`.tmp/p2p_longrun_debug_after_provider_unavailable_fallback/20260407-154153` 说明 provider fallback 单独上线后并未改善整体 gate，反而退到 `last_error_samples=6; consensus_hash_divergence count=2 heights=2,5; known_peer_heads_zero_samples=42`；`.tmp/p2p_longrun_debug_after_connected_peer_wait/20260407-154815` 在 connected-peer wait 后恢复到 `last_error_samples=3; consensus_hash_divergence count=2 heights=5; known_peer_heads_zero_samples=43`，证明 `no connected peers` 样本有所收敛，但仍未优于当前最佳 `.tmp/p2p_longrun_debug_after_live_provider_subset/20260407-144237`。
- 遗留事项: 当前最顽固的 live failure signature 已进一步聚焦为“explicit bootstrap triad 下 replication peer 集会掉空”，对应 sequencer 的 `fetch-blob no connected peers` 与 observer/storage 的 `fetch-commit no connected peers`。provider fallback 与 request wait 都是正向缓解，但还不足以收口；下一步应直接调查 peer manager / active transport admission / known peer heads 为何在显式 bootstrap 小拓扑中仍会长期掉成 0。
