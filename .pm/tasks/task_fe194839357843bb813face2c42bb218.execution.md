# task_fe194839357843bb813face2c42bb218 Execution Log

- task_uid: task_fe194839357843bb813face2c42bb218
- title: Unify observer release and trace triad blob availability root cause
- owner_role: runtime_engineer
- worktree_hint: oasis7-p2p-p2parch-6-blob-availability-root-cause

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-08 15:54:55 CST / runtime_engineer
- 完成内容: 对当前三节点真实环境补做 blob-availability root-cause 取证，确认 storage 端 replication root 只有 `node_pos_state.json`，没有 `replication_guard.json`、`replication_commit_messages` 与 replication blob；同时确认 storage 的 `committed_height/network_committed_height` 可以继续前进，因此“高度已追平”不代表 replication 已落盘。
- 完成内容: 结合 `oasis7_node` 代码追真值，定位根因为 replication apply/gap-sync 起点错误复用了 `committed_height`；storage/observer 一旦错过历史 replication 流，就不会再主动回补，而 sequencer 又会在发布新 replication 前被 `storage challenge gate` 卡住，形成真实环境死锁。
- 完成内容: 在当前 worktree 内为 `oasis7_node` 新增独立 `replication_persisted_height` 游标与 `latest_persisted_commit_height()` 读取逻辑，修正 live ingest 与 gap-sync 的 apply/backfill 起点，并新增 regression `replication_gap_sync_backfills_when_consensus_height_already_advanced`。
- 完成内容: 本地已完成 `env -u RUSTC_WRAPPER cargo fmt --all`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node replication_gap_sync_backfills`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node runtime_network_replication_syncs_distfs_commit_files`。
- 完成内容: 已新增 evidence `doc/testing/evidence/p2p-real-env-triad-blob-availability-root-cause-2026-04-08.md`，并回写 P2PARCH-6 project 结论。
- 遗留事项: 修复尚未 rollout 到 ECS sequencer/storage，也未解决本机 `/opt/oasis7/p2p-triad-local` root-owned observer 升级路径；真实环境仍停在 blocked，下一步需要部署当前修复并复采 same-window triad。

## 2026-04-08 17:51:55 CST / runtime_engineer
- 完成内容: 在现有 `replication_persisted_height` 修复基础上，继续补上 provider-aware `fetch-blob` 命中 `found=false` 时的 route fallback，避免 observer / storage challenge gate 因单个缺 blob peer 的“协议层成功但业务层未命中”而直接停住。新增 regression `runtime_network_replication_gap_sync_falls_back_after_provider_route_not_found` 与 `runtime_replication_storage_challenge_gate_falls_back_after_provider_route_not_found`，并复跑 unavailable fallback / distfs sync 相关用例，全部通过。
- 完成内容: 已构建新 release `95ae1e3ee604-blob-route-fallback-20260408`（`sha256=0179d52afb91355821dcfbeb94c83c7bb10eb174fe1d81d41fbf16d27b26329a`），并统一部署到本机 `triad-observer-local`、ECS `triad-sequencer-a`、ECS `triad-storage-b`。
- 完成内容: same-window 复采确认 observer 已恢复 `last_error=null` 且 `committed_height` 从 `16187` 推进到 `16237`；storage 回补文件增长到 `replication_commit_messages/store-blobs = 1056/1056`，说明 replication backlog 正在持续补齐；sequencer 旧的 `fetch-blob NetworkProtocolUnavailable` 已消失，残余失败签名收敛为纯 `storage challenge gate network blob not found`。
- 完成内容: 已回写 evidence 与 P2PARCH-6 project，明确当前 residual 已从“observer/gap-sync 路由直接失败”进一步收敛到“storage 按历史高度顺序回补太慢，而 sequencer challenge gate 仍只抽最新 blob”。
- 遗留事项: triad 仍未完全恢复。sequencer 仍卡在最新 challenge hash 不可达，storage 仍伴随 `fetch-commit NetworkProtocolUnavailable` 间歇抖动但不会阻止持续回补；下一步需要针对 challenge gate 的抽样策略或 bootstrap catch-up 语义继续修复，而不是继续重复 observer/route 层排查。

## 2026-04-08 19:36:01 CST / runtime_engineer
- 完成内容: 继续把 challenge-gate cold-start catch-up fallback 收口到仓库内实现：`enforce_storage_challenge_gate(...)` 现已支持“先抽最新、再退回旧高度样本”的顺序回退；同时把原先受 runtime handler 覆盖与线程时序影响的回归改写成直接 gate 调用验证，新增/稳定通过 `runtime_replication_storage_challenge_gate_falls_back_to_older_samples_during_catchup`，并复跑 threshold / provider-route fallback / gap-sync fallback / distfs sync 相关用例。
- 完成内容: 已构建新 release `95ae1e3ee604-challenge-gate-fallback-20260408`（`sha256=a2cb5191cdb58cfa0b430369e0220666b5d18e22f4cf58b5b0d1a220f1370fea`），并统一部署到本机 `triad-observer-local`、ECS `triad-sequencer-a`、ECS `triad-storage-b`。
- 完成内容: same-window 三节点复采确认新的真实环境行为已发生变化：observer 在约 80 秒窗口内 `committed_height 17062 -> 17124 -> 17187` 且 `known_peer_heads=1 / last_error=null`；storage `replication_commit_messages/store-blobs 2003/2003 -> 2193/2193 -> 2347/2347` 持续增长；sequencer 在同窗口内 `committed_height 57536 -> 57538`，并保持 `last_error=null`，未再采到 `storage challenge gate`。
- 完成内容: 已回写 evidence 与 P2PARCH-6 project，明确当前 blocker 已从 `storage challenge gate network blob not found` 进一步转移到 storage `fetch-commit NetworkProtocolUnavailable` 与三节点 peer/head 重收敛缓慢。
- 遗留事项: triad 仍维持 `blocked`。storage 重启后当前仍停在 `committed_height=0 / network_committed_height=0`，说明 `fetch-commit` 可用性或 peer 选择残留尚未收口；若要继续推进，需要优先追 `fetch-commit` 的 provider/retry/peer-path 真值，而不是回头重复 challenge gate 修补。

## 2026-04-08 21:27:30 CST / runtime_engineer
- 完成内容: 为真实三节点继续补诊断与修复。`/v1/chain/status` 现已暴露 replication 调试块，包括 `local_peer_id`、`connected_peers`、`peer_healths`、`registered_protocols`、`unsupported_protocol_peers` 与 recent transport errors，用于直接核对 triad 实机 peer 真值与 handler 注册状态。
- 完成内容: 通过 diagnostics 确认 triad peer_id 映射：observer=`12D3KooWF7hkwdUYKSnmLzuWsvqgtSuAcpZfq6xKkstJV1oaMqjF`、storage=`12D3KooWG1GJTjZe9PSCcLY74quNxjoSv6yqFRxuJPF48pbvnbGu`、sequencer=`12D3KooWHpsWPJscCUtRPj6mk42eBhTeTcQwaxH45nQwimfuAmud`；同时确认 sequencer/storage 两端本地都已注册 `fetch-commit` 与 `fetch-blob` handler，因此 residual 已不再是“对端未挂 handler”。
- 完成内容: 在 `oasis7_net` 修复 inbound transport route pollution：listener 侧不再把 `send_back_addr` 的临时源端口写回 Kademlia 路由，active transport path 也会优先映射回 peer record 中的稳定 direct addr。新增 `active_transport_path_from_endpoint_maps_inbound_ephemeral_tcp_port_to_known_direct_path` regression 并通过。
- 完成内容: 在 `oasis7_node` 修复 unsupported peer fallback：已被判定为某协议 `unsupported` 的 peer 不再在 request path 里被重新拿来兜底，避免 storage 在启动窗口里明知 observer 不支持 `fetch-commit/fetch-blob` 仍把它当复制源。新增 `filtered_request_peers_excludes_known_unsupported_peers_without_fallback` regression 并通过。
- 完成内容: 三次增量 rollout 复采显示问题已经明显收敛：修完 route sanitize 后，sequencer/storage 能重新恢复彼此直连，storage 不再长期退化成“只剩 observer”；再修 unsupported fallback 后，storage 主签名从误导性的 `fetch-commit NetworkProtocolUnavailable` 收敛成更准确的 `libp2p-replication no connected peers for protocol /aw/node/replication/fetch-commit/1.0.0`，说明当前 residual 已聚焦为启动窗口里可用复制源尚未就绪的时序问题。
- 完成内容: 本轮本地已完成 `env -u RUSTC_WRAPPER cargo fmt --all`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net active_transport_path_from_endpoint`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net filter_request_peers_by_lane`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node filtered_request_peers_excludes_known_unsupported_peers_without_fallback`、`env -u RUSTC_WRAPPER cargo test -p oasis7_node runtime_network_replication_gap_sync_falls_back_after_provider_route_unavailable`、`env -u RUSTC_WRAPPER cargo test -p oasis7 build_chain_status_payload_includes_storage_metrics`。
- 遗留事项: triad 仍未 fully recover。最新 release 下 storage 虽然能同时看到 observer + sequencer，但仍可能在早期 gap-sync 窗口打到 `no connected peers for protocol /aw/node/replication/fetch-commit/1.0.0` 并停在 `committed_height=0`；下一步应继续沿 startup ordering / retry window / head refresh 时序收口，而不是再回头修 handler 注册或 observer lane capability。

## 2026-04-08 21:49:00 CST / runtime_engineer
- 完成内容: 已按仓库门禁完成独立 subagent review，并将 broad diff 收敛到 triad root-cause 相关的 `oasis7_net` / `oasis7_node` / `oasis7_chain_runtime` / project / evidence / task 文档集合后复查通过。
- 完成内容: review 指出的 `unsupported peer no-fallback` 与 lane filter 收紧风险已记录为当前 landing 的已知 tradeoff：本次保留该行为变更，是因为真实三节点已经证明旧逻辑会把 observer 这类已知不支持 `fetch-commit` 的节点反复拿来兜底，持续制造误导性签名并阻断 root-cause 收敛。
- 完成内容: 对应 residual 也已明确收口为后续任务输入：若 `unsupported` / lane 元数据存在误判或过期，仍可能把请求集收缩为空；下一步应补 startup ordering、重试窗口、unsupported 标记衰减或重新探测，而不是回退本轮 triad 修复。
- 遗留事项: `transport_endpoint_family()` 仍是面向当前 triad 部署模型的启发式匹配，默认依赖“同一 IP/TCP 栈下不存在多个竞争性 replication 服务”；若后续出现单机多服务端口并存，需要补更强约束或更多实机证据。
