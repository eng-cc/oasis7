# task_020b9037095d4758a45d48112b7fc669 Execution Log

- task_uid: task_020b9037095d4758a45d48112b7fc669
- title: Add provider bridge ingress quota coverage
- owner_role: tpm
- worktree_hint: /Users/scc/ccwork/worktrees/oasis7-ops-add-provider-bridge-coverage

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
  - Action: ...
  - Validation Command: ...
  - Expected Result: ...
  - Actual Result: ...
  - Blocker / Next Action: ...
-->

## 2026-06-04 11:24:30 CST / tpm
- 完成内容: WORKFLOW BOOTSTRAP DECIDED
- 遗留事项: Implementation and verification pending.
- Repository State Impact: Repository-changing task; add automated coverage for provider bridge public ingress, quota-exhaustion smoke harness, and deposit/reconcile accounting gate integration.
- Isolation Decision: Source worktree was `main`; created canonical task worktree `/Users/scc/ccwork/worktrees/oasis7-ops-add-provider-bridge-coverage` on branch `task/ops-add-provider-bridge-coverage`.
- Task Truth: owner role `tpm`; `.pm` task `task_020b9037095d4758a45d48112b7fc669`; source ref `AGENTS.md`; doc refs provider bridge and NewAPI quota bridge runbooks.
- Routed Next Phase: `repo-owned-workflow-router` -> implementation with focused automated coverage and verification.
- Required Writeback: `.pm/tasks/task_020b9037095d4758a45d48112b7fc669.execution.md` is the mandatory sink.
- Action: Add provider bridge contract smoke automation and gate wiring in the task worktree.
- Validation Command: pending.
- Expected Result: New automated coverage exists and passes targeted verification.
- Actual Result: pending.
- Next Action: Add a provider bridge contract smoke harness with mock tests; wire it into required gate; explicitly include NewAPI bridge service bin tests for deposit/reconcile accounting.
- Blocker / Next Action: None.

## 2026-06-04 11:37:30 CST / tpm
- 完成内容: Added automated provider bridge coverage and wired it into the required gate.
- 遗留事项: Full live public ingress and low-quota exhaustion still require operator-provided real `--base-url` / `--auth-token`; the repo now has an executable harness and mock regression coverage, but no raw secrets are stored in repo.
- Action: Created `scripts/provider-remote-https/provider_bridge_contract_smoke.py` plus shell wrapper and mock tests; updated `scripts/ci-tests.sh` to run the new provider bridge contract smoke test and `cargo test -p oasis7 --bin oasis7_newapi_bridge_service`; updated provider/NewAPI runbooks with public ingress and low-quota automation commands; made provider test wrapper choose Python 3.10+ for existing CLI tests.
- Validation Command: `./scripts/provider-remote-https/provider-bridge-contract-smoke.test.sh`; `./scripts/provider-remote-https/letai-provider-cli.test.sh`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_newapi_bridge_service -- --nocapture`; `bash -n scripts/provider-remote-https/provider-bridge-contract-smoke.sh scripts/provider-remote-https/provider-bridge-contract-smoke.test.sh scripts/provider-remote-https/letai-provider-cli.test.sh scripts/ci-tests.sh`; `python3 -m py_compile scripts/provider-remote-https/provider_bridge_contract_smoke.py scripts/provider-remote-https/provider_bridge_contract_smoke.test.py`; `./scripts/check-script-executable-bits.sh`; `./scripts/doc-governance-check.sh`; `git diff --check`; `./scripts/pm/lint.sh 2>&1 | rg "task_020b9037095d4758a45d48112b7fc669|pm-lint: OK" || true`
- Expected Result: New mock automation passes; existing provider CLI smoke passes under Python 3.10+; NewAPI bridge service deposit/reconcile accounting tests pass; scripts/docs/diff hygiene pass; task-specific PM lint no longer reports this task.
- Actual Result: PASS for new provider contract smoke (`3 tests`), existing provider CLI smoke (`3 tests`), NewAPI bridge service bin tests (`28 passed`), shell syntax, Python compile, executable-bit check, doc governance, and `git diff --check`. Filtered PM lint produced no entries for `task_020b9037095d4758a45d48112b7fc669`; full `./scripts/pm/lint.sh` remains blocked by unrelated pre-existing `.pm` task execution log format failures.
- Blocker / Next Action: None for implementation. If desired, run the new harness against live prod with `--base-url https://t2t.oasis7.tech --auth-token <token>`, and run low-quota with `--decision-count <n> --expect-provider-error-code-substr quota`.

## 2026-06-04 11:46:00 CST / tpm
- 完成内容: User clarified that coverage must be real/live rather than mock-only, so the route changes to add explicit live environment automation while preserving mock tests as side-effect-free regression.
- 遗留事项: Live low-quota and deposit/reconcile tests must avoid mutating existing production bindings; use dedicated live persona inputs or sanitized state-derived selectors only.
- Action: Add live gate script for 204/205 provider decision, 205 public ingress, optional live low-quota exhaustion, and optional live deposit/reconcile accounting; document required credentials/secrets and keep default CI mock gate separate from live gate.
- Validation Command: pending.
- Expected Result: Repo has a real-environment automation entrypoint that can discover live ECS/provider/nginx/quota/accounting failures when intentionally run with credentials.
- Actual Result: pending.
- Blocker / Next Action: Implement live automation and run non-destructive live checks available from current credentials; avoid destructive quota/accounting operations unless dedicated persona inputs are present.

## 2026-06-04 12:20:00 CST / tpm
- 完成内容: Added and exercised real-environment provider bridge live gate.
- 遗留事项: Live low-quota exhaustion and mutating deposit/reconcile accounting were not executed because the real provider path fails before decision success; accounting mutation still requires a dedicated bind payload plus explicit transfer command and mutation acknowledgement.
- Action: Added `scripts/provider-remote-https/provider-bridge-live-gate.sh`, wired it behind `OASIS7_CI_RUN_PROVIDER_LIVE_GATE=true` in `scripts/ci-tests.sh`, and updated operator runbooks to state that mock tests are harness regressions only while live gate is the environment acceptance path.
- Validation Command: `./scripts/provider-remote-https/provider-bridge-live-gate.sh --decision-count 1 --loopback-target 204 --skip-public`; `./scripts/provider-remote-https/provider-bridge-live-gate.sh --decision-count 1 --loopback-target 205 --skip-public`; `./scripts/provider-remote-https/provider-bridge-live-gate.sh --decision-count 1 --skip-loopback`; `./scripts/provider-remote-https/provider-bridge-contract-smoke.test.sh`; `./scripts/provider-remote-https/letai-provider-cli.test.sh`; `python3 -m py_compile scripts/provider-remote-https/provider_bridge_contract_smoke.py scripts/provider-remote-https/provider_bridge_contract_smoke.test.py`; `bash -n scripts/provider-remote-https/provider-bridge-contract-smoke.sh scripts/provider-remote-https/provider-bridge-contract-smoke.test.sh scripts/provider-remote-https/provider-bridge-live-gate.sh scripts/provider-remote-https/letai-provider-cli.test.sh scripts/ci-tests.sh`; `./scripts/check-script-executable-bits.sh`; `./scripts/doc-governance-check.sh`; `git diff --check`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_newapi_bridge_service -- --nocapture`.
- Expected Result: Live gate derives selectors from real 204/205 ECS bridge state, checks loopback provider decision and public nginx ingress, and reports concrete environment failures without printing raw token keys or passwords. Non-live harness, accounting unit tests, script syntax, docs, and diff hygiene pass.
- Actual Result: LIVE FAILURES FOUND. 204 loopback provider decision failed with `curl: (52) Empty reply from server`; 205 loopback provider decision failed with `curl: (52) Empty reply from server`; 205 public nginx ingress failed on `GET /v1/provider/info` with TLS EOF (`EOF occurred in violation of protocol`). Local automation passed: provider contract mock smoke `3 tests OK`, LetAI provider CLI smoke `3 tests OK`, Python compile PASS, bash syntax PASS, executable-bit check PASS, doc governance PASS, `git diff --check` PASS, NewAPI bridge service accounting tests `28 passed`.
- Blocker / Next Action: Fix the live provider bridge/nginx path before claiming long-run quota consumption or low-quota exhaustion as passed. After provider decision succeeds, run `--lowquota-target 204|205|public205 --lowquota-decision-count 20` with a dedicated lowquota persona; for deposit/reconcile live accounting, provide a dedicated bind payload and transfer command, then pass `--i-understand-accounting-mutates-live-state`.

## 2026-06-04 13:50:00 CST / tpm
- 完成内容: Investigated live failure causes and fixed the smoke request schema.
- 遗留事项: 204 still cannot satisfy normal decision smoke because the selected live binding is already below the LetAI precharge amount; current Codex control host still cannot complete public TLS to 205, while 204 can.
- Action: Replaced stale `action_catalog[].description` payload fields with required `summary` fields; expanded quota matching to inspect provider_error message as well as code; added mock coverage for message-only quota errors; reran real 204/205 probes.
- Validation Command: `./scripts/provider-remote-https/provider-bridge-contract-smoke.test.sh`; `python3 -m py_compile scripts/provider-remote-https/provider_bridge_contract_smoke.py scripts/provider-remote-https/provider_bridge_contract_smoke.test.py`; `bash -n scripts/provider-remote-https/provider-bridge-live-gate.sh`; `./scripts/provider-remote-https/provider-bridge-live-gate.sh --decision-count 1 --loopback-target 204 --skip-public`; `./scripts/provider-remote-https/provider-bridge-live-gate.sh --decision-count 1 --loopback-target 205 --skip-public`; `./scripts/provider-remote-https/provider-bridge-live-gate.sh --skip-loopback --skip-public --lowquota-target 204 --lowquota-decision-count 1`; local `curl`/`openssl` to `https://t2t.oasis7.tech`; 204-to-205 public curl/openssl probe.
- Expected Result: Smoke payload matches current provider contract; 205 loopback should reach LetAI; 204 should reveal the real quota failure instead of schema decode failure; public ingress diagnosis distinguishes nginx/upstream from source-path TLS reset.
- Actual Result: Provider logs confirmed the earlier `Empty reply from server` was request decode failure: missing `summary` in action_catalog. After schema fix, 205 loopback decision PASS with `provider_version=letai/gpt-5.4`; 204 decision reaches LetAI but returns HTTP 403 `insufficient_user_quota` (`用户剩余额度: $0.003140`, precharge about `$0.0051`). 204 lowquota gate observes the quota message but fails the normal lowquota standard because there are zero successful decisions before exhaustion. 205 loopback `/v1/provider/info` and `/v1/provider/health` return 200; 205 local nginx TLS returns 200; 204-to-205 public TLS/HTTP returns 200; current Codex control host to 205 public 443 connects TCP but TLS ClientHello is reset before nginx access log, with no host firewall rule found (`ufw inactive`, iptables INPUT ACCEPT).
- Blocker / Next Action: Top up or replace the 204 lowquota persona before running longrun quota consumption. For the current control-host public TLS reset, check Aliyun security group / cloud firewall / regional source filtering for source `111.197.237.94` or run public ingress gate from an allowed external probe host.

## 2026-06-04 14:05:00 CST / tpm
- 完成内容: Confirmed why 204 quota is insufficient.
- 遗留事项: Need operator top-up or a new dedicated lowquota persona before 204 can be used for longrun quota exhaustion.
- Action: Inspected sanitized 204 NewAPI bridge state and systemd arguments without printing raw token keys.
- Validation Command: SSH read of `/etc/oasis7/newapi-bridge/bridge-state.json`; `curl http://127.0.0.1:5852/v1/bridge/health`; `systemctl cat/status oasis7-newapi-bridge.service`.
- Expected Result: Determine whether 204 balance came from deposit/reconcile accounting or from a manually provisioned smoke project.
- Actual Result: 204 state has exactly one active binding and one project binding: project_name `oasis7-204-smoke`, platform_project_id `925`, token present, route_count `0`, ledger_count `0`, bridge health route_count `0`, ledger_count `0`, reconciled_count `0`. The service starts with LetAI config but no `--chain-base-url`, no `--pricing-rule`, and no reconcile interval in the effective command, so this lane is not currently configured as a full deposit/reconcile accounting environment. The insufficient quota is therefore the live LetAI project/token balance being below the model precharge threshold, not a failed local ledger top-up.
- Blocker / Next Action: Treat 204 as a manually provisioned smoke/low-balance project until a dedicated bind/deposit/reconcile path is configured and funded.

## 2026-06-04 14:42:00 CST / tpm
- 完成内容: Compared 204 and 205 after user clarified they should share the same NewAPI project configuration and only deployed code may differ.
- 遗留事项: State drift must be corrected or intentionally documented before treating 204 as equivalent test lane for prod.
- Action: Compared release pointers, binary/script hashes, sanitized env, and sanitized bridge state on both hosts.
- Validation Command: SSH to `39.104.204.172` and `39.104.205.67`; `readlink -f /opt/oasis7/*/current`; `sha256sum` for bridge binaries/scripts; sanitized `/etc/oasis7/*bridge*.env`; sanitized `/etc/oasis7/newapi-bridge/bridge-state.json`.
- Expected Result: If only deployed code differs, NewAPI project/token selector state should point at the same project config, or any intentional state override should be obvious.
- Actual Result: Deployed binaries/scripts are byte-identical between 204 and 205: `oasis7_newapi_bridge_service` hash `0739b6a...`, `oasis7_provider_local_bridge` hash `e16bb66...`, provider CLI hash `bf172e...`, startup scripts match. Env values also match for model/base URL/auth-route-from-bearer/reconcile interval. The actual state differs: 204 binding uses `newapi_user_ref=smoke-204-1780063051`, `project_name=oasis7-204-smoke`, `platform_project_id=925`, token fingerprint `3b7224cd662e`; 205 binding uses `newapi_user_ref=17d0bc...5c545f`, `project_name=bootstrap-default`, `platform_project_id=784`, token fingerprint `54717e315821`.
- Blocker / Next Action: The 204 quota failure is caused by state/config drift to a different LetAI project/token, not by deployed code difference. Align 204 `bridge-state.json` with the intended shared NewAPI project/token configuration or rerun bind/provisioning to recreate the intended project mapping.

## 2026-06-04 14:53:00 CST / tpm
- 完成内容: Mutated 204 live state to match 205 NewAPI project/token configuration after explicit user request.
- 遗留事项: 205 public ingress from current Codex control host still needs cloud/source-path investigation; 204 now matches the 205 project state for loopback provider validation.
- Action: Read 205 `/etc/oasis7/newapi-bridge/bridge-state.json` in memory; stopped 204 `oasis7-remote-provider-bridge.service` and `oasis7-newapi-bridge.service`; backed up 204 old state; wrote 205 state to 204; restarted both 204 services; ran 204 live provider decision smoke.
- Validation Command: Backup/write/start script over SSH; `./scripts/provider-remote-https/provider-bridge-live-gate.sh --decision-count 1 --loopback-target 204 --skip-public`; 204 `curl http://127.0.0.1:5852/v1/bridge/health`.
- Expected Result: 204 active binding/project should match 205 (`bootstrap-default`, platform_project_id `784`, token fingerprint `54717e315821`), services should be active, and 204 loopback provider decision should pass.
- Actual Result: Backup created at `/etc/oasis7/newapi-bridge/bridge-state.json.backup-20260604-144914` with old sha `2f325f73c14940dd737f1d72bfa8fd9772f8d30c9c757116ee56dd7f68c109a3`. 204 new state sha `077836a498dec65a0d8af46d8d371efa500526b47aa3127fe026ad6904a57e10`; active binding now `newapi_user_ref=17d0bc749f79176513da1041b377fe1648179395de5c545f`, `project_name=bootstrap-default`, `platform_project_id=784`, token fingerprint `54717e315821`. 204 `newapi` and `provider` services are active. 204 live gate PASS: `decision_successes=1`, `provider_versions=[letai/gpt-5.4]`. 204 bridge health OK with binding_count `1`, project_binding_count `1`, route_count `0`, ledger_count `0`.
- Blocker / Next Action: None for 204 state alignment. If rollback is needed, stop both 204 services and restore the backup file above, then restart services.

## 2026-06-04 15:41:00 CST / tpm
- 完成内容: Fixed PR CI failure in the required gate after GitHub Actions reported `error: Unrecognized option: 'verbose'`.
- 遗留事项: Await fresh GitHub Actions rerun after pushing the CI fix.
- Action: Changed `run_newapi_bridge_service_accounting_tests` to call `cargo test ... -- --nocapture` directly through `run env -u RUSTC_WRAPPER` instead of `run_cargo`, because `run_cargo` appends `--verbose` at the end when `CI_VERBOSE=1`; after a test-harness separator, that trailing flag is passed to the test binary rather than cargo.
- Validation Command: `bash -n scripts/ci-tests.sh`; `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_newapi_bridge_service -- --nocapture`; `CI_VERBOSE=1 OASIS7_CI_RUN_OASIS7_REQUIRED_TESTS=false OASIS7_CI_RUN_CONSENSUS_TESTS=false OASIS7_CI_RUN_DISTFS_TESTS=false OASIS7_CI_RUN_VIEWER_CONTRACT_TESTS=false OASIS7_CI_RUN_VIEWER_WASM_CHECK=false ./scripts/ci-tests.sh required`; `git diff --check`.
- Expected Result: Accounting tests remain in the required gate and pass under `CI_VERBOSE=1` without forwarding cargo's verbose flag to the Rust test binary.
- Actual Result: PASS. Shell syntax passed; direct accounting tests passed (`28 passed`); scoped `CI_VERBOSE=1 ./scripts/ci-tests.sh required` reached and passed the accounting test command without appending `--verbose` after `-- --nocapture`, then completed required pre-scope checks; `git diff --check` passed.
- Blocker / Next Action: Push the fix and verify PR checks rerun successfully.

## 2026-06-04 16:16:00 CST / tpm
- 完成内容: Addressed unresolved PR review comments on live gate portability and timeout propagation.
- 遗留事项: One review thread on `.pm/roles/tpm/backlog/committed.yaml` is outdated and should be resolved as stale after push; PR remains behind `main` until refreshed.
- Action: Removed developer-machine default credentials path in favor of `OASIS7_PROVIDER_LIVE_CREDENTIALS_FILE` / explicit `--credentials-file`; replaced macOS-specific expect SSH invocation and command-line base64 script transport with `sshpass -e ssh ... bash -s` using a temp stdin script; propagated configured timeout budgets into both public contract smoke and loopback live smoke payloads; added a regression test for timeout payload values.
- Validation Command: `./scripts/provider-remote-https/provider-bridge-contract-smoke.test.sh`; `python3 -m py_compile scripts/provider-remote-https/provider_bridge_contract_smoke.py scripts/provider-remote-https/provider_bridge_contract_smoke.test.py`; `bash -n scripts/provider-remote-https/provider-bridge-live-gate.sh scripts/provider-remote-https/provider-bridge-contract-smoke.sh scripts/provider-remote-https/provider-bridge-contract-smoke.test.sh`; `OASIS7_PROVIDER_LIVE_CREDENTIALS_FILE=/Users/scc/Documents/keys/aliyun_ecs.txt ./scripts/provider-remote-https/provider-bridge-live-gate.sh --decision-count 1 --loopback-target 204 --skip-public`; `OASIS7_PROVIDER_LIVE_CREDENTIALS_FILE=/Users/scc/Documents/keys/aliyun_ecs.txt ./scripts/provider-remote-https/provider-bridge-live-gate.sh --decision-count 1 --loopback-target 205 --skip-public`; `./scripts/doc-governance-check.sh`; `git diff --check`; `rg` checks for stale `/Users/scc`, `UserKnownHostsFile`, `REMOTE_COMMAND_B64`, `base64`, and hard-coded `timeout_budget_ms: 15000` in touched smoke scripts.
- Expected Result: Review-commented portability issues are removed; configured timeout values drive request payloads; live loopback checks still pass on both ECS hosts.
- Actual Result: PASS. Contract smoke tests now run `5 tests`; Python compile, bash syntax, doc governance, and diff hygiene passed. 204 and 205 loopback provider decision live gates both passed with `decision_successes=1` and `provider_versions=[letai/gpt-5.4]`; stale hard-coded path/base64/timeout patterns were not found in the touched smoke scripts.
- Blocker / Next Action: Commit, push, resolve addressed review threads, then re-check PR threads and CI.
