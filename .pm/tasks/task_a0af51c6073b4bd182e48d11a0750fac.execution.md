# task_a0af51c6073b4bd182e48d11a0750fac Execution Log

- task_uid: task_a0af51c6073b4bd182e48d11a0750fac
- title: Aggregate full triad traffic and control-plane detail summaries
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-runtime-traffic-monitor-aggregate-detail

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-20 00:49:05 CST / runtime_engineer
- 完成内容: 扩展共享 `scripts/traffic-monitor-summary.py`，让单节点与 triad summary JSON 在原有 top-N 摘要之外保留完整 delta detail maps；UDP 现写出完整 `by_kind`，libp2p 现写出完整 `by_topic` / `by_protocol`，并为这些 detail maps 追加 entry counts，便于后续程序侧直接消费全量明细。
- 完成内容: 为 triad 布局新增 `aggregate` section，聚合三节点 UDP / libp2p 总 payload、lane share、节点 payload 占比，以及 merged `by_kind` / `by_topic` / `by_protocol` 明细与 top-N；Markdown 同步增加 aggregate 摘要，但仍保持可读 top-N 视图，不把完整明细直接铺在终端输出里。
- 完成内容: 同步更新 `scripts/oasis7-node-traffic-monitor.sh` 与 `scripts/p2p-real-env-traffic-monitor.sh` usage，明确 `latest_summary.json` 已保留全量 detail maps / triad aggregate；回写 `doc/world-runtime/prd.md`、`doc/world-runtime/project.md`，新增 `PRD-WORLD_RUNTIME-037` 与对应任务追踪。
- 完成内容: 验证完成：`python3 -m py_compile scripts/traffic-monitor-summary.py`、`bash -n scripts/oasis7-node-traffic-monitor.sh`、`bash -n scripts/p2p-real-env-traffic-monitor.sh`、`./scripts/doc-governance-check.sh`、`git diff --check` 全通过；另用合成 triad history 验证 aggregate totals 与 merged detail maps 正确求和，并用真实 triad history smoke 验证 aggregate section 已落盘，当前最近窗口总 payload `3,847,279 B`，lane share 为 UDP `33.01%` / libp2p `66.99%`。
- 遗留事项: 当前已能采集和输出“所有数据明细”，但线上节点现有 systemd/cron 仍按既有采样间隔运行；若后续要进一步压流量，还需基于新增 aggregate 明细继续收敛 `peer_record` 与 `UDP hello` 的频率/预算。

## 2026-04-20 15:15:24 CST / runtime_engineer
- 完成内容: 继续在同一 PR / task 上补齐“payload 统计 vs 服务器总网卡流量”的口径差。`scripts/oasis7-node-traffic-monitor.sh` 现会在每次本地采样时额外记录默认路由网卡 `network_interface.{name,rx_bytes,tx_bytes}`；`scripts/p2p-real-env-traffic-monitor.sh` 也已同步对 observer / 两台 ECS 采集同类网卡 counters，避免 triad ad-hoc 汇总只能看到 payload。
- 完成内容: 扩展 `scripts/traffic-monitor-summary.py`，为单节点与 triad aggregate 新增 `network_interface` 汇总：输出网卡 `rx/tx` delta、平均 `bit/s`、payload share、non-payload 差值，以及三节点按网卡总流量排序的节点分布；summary JSON 现可直接对比“payload-only 监控”和“服务器整机网卡吞吐”。
- 完成内容: 同步回写 `doc/world-runtime/prd.md`、`doc/world-runtime/project.md`，把主机网卡 counters 与 payload coverage 差值纳入 `PRD-WORLD_RUNTIME-035` 的成功条件和验收命令，避免后续再次把 payload 统计误报为服务器总带宽。
- 完成内容: 验证完成：`python3 -m py_compile scripts/traffic-monitor-summary.py`、`bash -n scripts/oasis7-node-traffic-monitor.sh`、`bash -n scripts/p2p-real-env-traffic-monitor.sh`、`./scripts/doc-governance-check.sh`、`git diff --check` 全通过；合成 triad history 证明网卡 delta / payload share 求和正确；真实单节点采样已产出 `node.network_interface`；真实 triad 短窗 smoke 证明 observer + sequencer + storage 的 `aggregate.network_interface` 已落盘且非零。
- 遗留事项: 这一轮只把网卡总流量并入监控，没有进一步细分 transport/Kademlia/gossipsub mesh 等未覆盖流量的内部来源；若要继续归因，还需要在 runtime / libp2p 层补更细的控制面计数。

## 2026-04-20 21:52:32 CST / runtime_engineer
- 完成内容: 在 `crates/oasis7_net/src/libp2p_net/traffic_metrics.rs` 为 `Libp2pTrafficMetricsSnapshot` 新增 `control_plane` 累计事件计数，覆盖 request-response failure、Kademlia query/routing、autonat、relay、dcutr、rendezvous 以及连接/监听地址类 transport 事件；`/v1/chain/status.traffic.libp2p_replication` 现能直接暴露 `total_events` 与 `by_kind`。
- 完成内容: 扩展 `scripts/traffic-monitor-summary.py`，让单节点与 triad aggregate 都能对 `control_plane` 做窗口 delta、完整 `by_kind` 汇总与 top kinds Markdown 摘要；shell usage 说明同步标注该字段用于解释 payload-only 与主机网卡总量之间的未归因差值。
- 完成内容: 同步回写 `doc/world-runtime/prd.md`、`doc/world-runtime/project.md` 与 task 元数据，明确 `PRD-WORLD_RUNTIME-035` 现在同时要求 control-plane event 归因，并强调这些统计是 event count，不代表 transport wire bytes。
- 完成内容: 验证完成：`env -u RUSTC_WRAPPER cargo fmt --all --check`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net recorders_accumulate_totals_and_breakdowns -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p oasis7_net libp2p_traffic_metrics_track_requests_and_gossip_payloads -- --nocapture`、`python3 -m py_compile scripts/traffic-monitor-summary.py`、`bash -n scripts/oasis7-node-traffic-monitor.sh`、`bash -n scripts/p2p-real-env-traffic-monitor.sh`、`./scripts/doc-governance-check.sh`、`./scripts/pm/lint.sh`、`git diff --check` 全通过；另用合成 triad history 验证 `control_plane.total_events/by_kind` 的窗口 delta 与 aggregate 求和正确。
- 遗留事项: 当前新增的是控制面事件计数而非线速字节；若后续仍需要把 ECS 网卡大头按字节拆到更细 wire-level 来源，仍需更侵入的 libp2p transport instrumentation。
