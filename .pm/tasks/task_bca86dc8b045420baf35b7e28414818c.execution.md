# task_bca86dc8b045420baf35b7e28414818c Execution Log

- task_uid: task_bca86dc8b045420baf35b7e28414818c
- title: Harden release web strict live progress fallback
- owner_role: qa_engineer
- worktree_hint: /home/scc/worktrees/oasis7-testing-release-web-strict-live-progress-retry

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-05-23 11:11:14 CST / qa_engineer
- 完成内容: 复盘 `Release Packages` run `26297891385` 的 `release-gate-web` 失败，先从 GitHub artifact 抽取 `release-gate-web-summary` / `web_strict.log`，确认 CI 表象是 `viewer-software-safe-step-regression.sh` 在 `llm_bootstrap` 场景下报 `RuntimeError: unreachable`。随后在 task worktree 本地复现 `release-gate-web-strict.sh`，抓到更稳定的串行问题：primary-entry 结束后 software-safe step 会踩同一组 launcher/web/viewer 端口，并把 live-control 长时间卡在 `queued`。
- 完成内容: 调整 `scripts/release-gate-web-strict.sh`，把 software-safe step phase 切到独立端口，并在 headed gate 下固定该 phase 走 headless；同时在 `scripts/viewer-software-safe-step-regression.sh` 中为 `step` 长时间停留 `queued` 的场景补发一次 canonical `play` fallback，避免把 headed live-control stall 直接误判成 release 回归。
- 完成内容: 回写 `doc/testing/prd.md` / `doc/testing/project.md` 的 release Web 串行隔离约束，并用 `bash -n scripts/viewer-software-safe-step-regression.sh scripts/release-gate-web-strict.sh` 与 `xvfb-run -a ./scripts/release-gate-web-strict.sh --scenario llm_bootstrap --out-dir /tmp/release-gate-web-strict-xvfb-mixed-clean --headed` 完成 task worktree 复验；本地 CI 级复现已通过，software-safe-step 产物落于 `/tmp/release-gate-web-strict-xvfb-mixed-clean/software-safe-step/20260523-110955`。
- 遗留事项: 需要提交/提 PR 合入 `main`，然后重新触发 `release-packages.yml` 的 `v0.0.52` 发布并复核 GitHub release 结果。

## 2026-05-23 12:26:20 CST / qa_engineer
- 完成内容: 继续跟进 merge 后重新触发的 `Release Packages` run `26322182778`。确认 `release-gate-runtime-core` / `support` / `sync`、`release-gate-web`、`release-gate-soak` 均已通过，新的阻断点改为 `build-web-dist` job；失败日志显示 launcher web dist 阶段直接执行 `env -u NO_COLOR trunk build --release --dist ../../output/release/web-launcher-dist`，但该 job 未安装 `trunk`，于 `2026-05-23 11:44:45 CST` 以 `env: 'trunk': No such file or directory` 退出。
- 完成内容: 在 `.github/workflows/release-packages.yml` 的 `build-web-dist` job 中补加 `Install trunk` 步骤（`cargo install trunk --locked`），使 launcher web dist 不再依赖 GitHub runner 镜像的预装状态。
- 完成内容: 基于最新 `main` 复验等价构建链路：`npm ci --prefix crates/oasis7_viewer`，随后串行执行 `./scripts/build-viewer-software-safe.sh`、`./scripts/copy-viewer-web-dist.sh --dist-dir output/release/web-dist` 与 `cd crates/oasis7_client_launcher && env -u NO_COLOR trunk build --release --dist ../../output/release/web-launcher-dist`；本地构建通过，`trunk 0.21.14` 输出 `INFO ✅ success`。
- 遗留事项: 需要提交并合入该 workflow 修复，然后再次触发 `release-packages.yml` 的 `v0.0.52`，最终以 `gh release view v0.0.52` 和 release asset 实际存在为准完成收口。
