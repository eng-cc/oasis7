# task_0a012bf9b85b417bb56d591969a07d36 Execution Log

- task_uid: task_0a012bf9b85b417bb56d591969a07d36
- title: stabilize viewer live integration full-tier regression
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-runtime-viewer-live-integration-flake-burn-down

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->
## 2026-05-21 20:04:00 CST / runtime_engineer
- 完成内容: 创建 task worktree `task/world-runtime-viewer-live-integration-flake-burn-down`，绑定 `.pm/tasks/task_0a012bf9b85b417bb56d591969a07d36.yaml`，目标是收口 `scripts/ci-tests.sh full` 对 `viewer_live_integration` 的历史 `skip + retry` 特殊路径。
- 遗留事项: 需要先确认当前 `live_server_accepts_client_and_emits_snapshot_and_event` 是否仍存在实质 flaky，再决定是修测试本身还是直接删除脚本层 workaround。

## 2026-05-21 20:16:00 CST / runtime_engineer
- 完成内容: 在当前 worktree 连续 3 次执行 `env -u RUSTC_WRAPPER cargo test -p oasis7 --features "test_tier_full,wasmtime,viewer_live_integration" --test viewer_live_integration live_server_accepts_client_and_emits_snapshot_and_event -- --nocapture`，`live_server_accepts_client_and_emits_snapshot_and_event` 全部稳定通过，未复现 CI 脚本最初规避的 flaky 签名；据此删除 `scripts/ci-tests.sh` 中对该 case 的 `--skip`、定向单测重跑与 `run_with_retries 3` 历史 workaround，并将 `doc/world-runtime/project.md` 回写为已完成治理切片。
- 遗留事项: 还需补跑脚本与 full-tier 验证，确认 `./scripts/ci-tests.sh full-core` 在移除特殊路径后仍稳定通过，并在收口前完成 `.pm` closeout / commit / PR preflight。

## 2026-05-21 20:21:00 CST / runtime_engineer
- 完成内容: 已补跑 `bash -n scripts/ci-tests.sh` 与 `git diff --check`，均通过；同时启动 `./scripts/ci-tests.sh full-core` 做整条 full-tier 验证。该全量验证已穿过文档治理、Windows path、Rust 体量与 `cargo fmt --check`，并成功进入不再带 workaround 的 `cargo test -p oasis7 --tests --features "test_tier_full,wasmtime,viewer_live_integration"` 主入口。
- 完成内容: `full-core` 后续暴露的失败签名与本次 viewer live 切片无关，已观察到至少 `runtime::tests::builtin_wasm_materializer::materializer_reuses_cached_module_hash_when_manifest_hashes_do_not_match`、`runtime::tests::body::body_action_updates_view_and_costs_resources`、`runtime::tests::body::body_action_rejects_when_insufficient_resources` 等既有 runtime full-tier 红灯；这说明删除 `viewer_live_integration` 的 `skip + retry` workaround 没有成为当前 full-tier 的首个阻断项。
- 遗留事项: 若要把 `full-core` 也收回全绿，需要另开/续做 builtin wasm materializer 与 body action 失败链路的独立修复切片；本任务先聚焦 viewer live 集成回归 workaround 清理。
