# task_fb91c7de413747b9b14712211f6666ea Execution Log

- task_uid: task_fb91c7de413747b9b14712211f6666ea
- title: Reduce libp2p control-plane query churn
- owner_role: runtime_engineer
- worktree_hint: /home/scc/worktrees/oasis7-world-runtime-control-plane-query-budget

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
-->

## 2026-04-21 23:08:42 CST / runtime_engineer
- 完成内容: 在 `libp2p_net` 的 `RefreshPeerDiscovery` 路径增加 control-plane budget gate，新增 `discovery_query_cooldown_ms` 默认值 60s，并把 `get_providers` 启动条件收紧为“当前无同类查询在飞且 cooldown 已到期”；同时让本地 peer record / discovery provider 仅在首次或 republish budget 到期时重发，避免每个短周期 discovery tick 都触发 `put_record` / `start_providing` / `get_providers`。
- 完成内容: 补充 `start_peer_discovery_query_respects_pending_query_and_cooldown` 与 `refresh_peer_discovery_respects_republish_and_query_budgets` 两个定向回归，覆盖 discovery query in-flight gate、cooldown gate，以及 refresh 对本地 republish/query budget 的复用。
- 完成内容: 回写 `doc/world-runtime/prd.md`、`doc/world-runtime/project.md` 与 task metadata，把 PRD-WORLD_RUNTIME-030 的验收口径从 RR peer-record request cooldown 扩展到 periodic discovery 的 DHT control-plane budget。
- 遗留事项: 需要把本分支版本部署到三节点后，再用现有流量监控对照 `control_plane.wire_bytes` 和 `kademlia.outbound_query_progressed` 的最近窗口下降幅度，确认 live triad 收益是否与本地回归一致。
