# task_b7b231c0f8c244f382757499d955f2b6 Execution Log

- task_uid: task_b7b231c0f8c244f382757499d955f2b6
- title: Check public testnet node health
- owner_role: tpm
- worktree_hint: /Users/scc/ccwork/worktrees/oasis7-testing-testnet-health-check-20260623

<!-- Append entries using:
Example:
  ## YYYY-MM-DD HH:MM:SS CST / role_name
  - 完成内容: ...
  - 遗留事项: ...
  - Action: ...
  - Validation Command: ...
  - Expected Result: ...
  - Actual Result: ...
  - Blocker / Next Action: ...
-->

## 2026-06-23 18:55:00 CST / tpm
- 完成内容: Checked current local public_testnet node health and submit endpoint readiness.
- 遗留事项: Do not run full chain/recharge E2E until submit endpoint and local observer return `readiness=ready` with no failed gates.
- Specialist Slice: `blockchain_ops_engineer` bounded read-only health classification `019ef45c-4e94-7153-9108-c8d46e382230`.
- Action: Collected `/v1/chain/status` from local observer `http://127.0.0.1:19083`, public submit endpoint `http://39.104.204.172:6631`, explorer overview `http://39.104.205.67:6632/v1/chain/explorer/overview`, local listener process, and formal manifest metadata from `/Users/scc/ccwork/oasis7-testnet-nodes/fourth-local/manifest.json`.
- Validation Command: `curl -m 5 -fsS http://127.0.0.1:19083/v1/chain/status | jq ...`; `curl -m 5 -fsS http://39.104.204.172:6631/v1/chain/status | jq ...`; `curl -m 5 -fsS http://39.104.205.67:6632/v1/chain/explorer/overview | jq ...`; `lsof -nP -iTCP:19083 -sTCP:LISTEN`; `./scripts/run-local-public-testnet-letai-test-environment.sh --preflight-only --reuse-existing --manifest /Users/scc/ccwork/oasis7-testnet-nodes/fourth-local/manifest.json --skip-newapi-bridge --skip-provider-bridge --skip-static-viewer --no-operator-commands`.
- Expected Result: Report whether the local observer and public submit endpoint are currently ready for local public_testnet E2E.
- Actual Result: Local observer identity is correct (`node_id=triad-testnet-fourth-local`, `role=observer`, `world_id=oasis7-public-testnet-governed-20260606`, `tier=public_testnet`) and process `oasis7_ch` PID `94166` listens on `127.0.0.1:19083`. Explorer/storage reports `ok=true`, `committed_height=412`, `network_committed_height=412`. However latest local observer and submit endpoint samples both report `readiness.status=not_ready` with failed gates `consensus_peer_head_unavailable` and `consensus_peer_head_stale`; peer head age was about 55s, above `peer_head_ttl_ms=30000`, and network head decision was `degraded`. Submit endpoint role is correct (`triad-testnet-sequencer`, `role=sequencer`) but the script preflight correctly failed because the endpoint was not ready.
- Blocker / Next Action: Wait 1-2 peer TTL cycles and resample. If still stale, inspect sequencer/storage/local observer peer gossip and consensus-head logs. Only resume full chain/recharge E2E after submit endpoint and local observer return `ready` with empty failed gates.

## 2026-06-23 20:17:00 CST / tpm
- 完成内容: Investigated root cause for `consensus_peer_head_unavailable` and `consensus_peer_head_stale`.
- 遗留事项: Treat this as a transient peer-head freshness incident unless it recurs; if it recurs, inspect remote sequencer/storage gossip and consensus-head publication logs.
- Specialist Slice: `blockchain_ops_engineer` bounded read-only root-cause classification `019ef46a-2665-7561-baae-bc76ab55b867`.
- Action: Checked local process command/socket state, recent local runtime log, status peer-head freshness fields, and a fresh resample after the earlier degraded window.
- Validation Command: `ps -p 94166 -o pid,ppid,etime,command`; `lsof -p 94166`; `tail -n 220 /Users/scc/ccwork/oasis7-testnet-nodes/fourth-local/logs/chain-runtime.log`; `rg -n "peer_head|stale|unavailable|gossip|network head|degraded|ready|not_ready|replication|timeout|error|warn|consensus" /Users/scc/ccwork/oasis7-testnet-nodes/fourth-local/logs/chain-runtime.log`; fresh `curl` samples for local observer, sequencer submit endpoint, and explorer overview.
- Expected Result: Distinguish direct gate trigger, likely root cause, and non-causes.
- Actual Result: Direct gate trigger was peer head freshness timeout: peer head `age_ms` was about 55s, above `peer_head_ttl_ms=30000`, so status could not select a fresh trusted network head. Most likely root cause was a short peer head gossip / consensus-head heartbeat delay between testnet nodes, not a local manifest/runbook/bridge issue. Non-causes: HTTP broken pipe warnings are status-client disconnects; local observer was not fully offline because it had active TCP connections to sequencer/storage; the chain was not globally stopped because explorer/storage continued to report committed height. Fresh resample at `2026-06-23 20:17 CST` recovered: local observer and sequencer both `readiness=ready`, failed gates empty, peer head age about 8-9s, `fresh=true`, explorer latest/committed/network height `415`.
- Blocker / Next Action: Current incident is resolved. If the same gates recur, collect remote sequencer/storage logs around peer-head publication and gossip transport before changing local test environment scripts.

## 2026-06-23 20:20:00 CST / tpm
- 完成内容: Executed the local public_testnet + LetAI/NewAPI test environment startup script.
- 遗留事项: Startup did not reach build/provider/NewAPI/viewer phases because submit endpoint preflight failed.
- Action: Ran `scripts/run-local-public-testnet-letai-test-environment.sh` with the formal local manifest, existing LetAI config path, `--reuse-existing`, and output dir `/tmp/oasis7-local-public-testnet-letai-test-env`.
- Validation Command: `LETAI_TOKEN_FILE=/Users/scc/Documents/keys/letai-token-local.txt ./scripts/run-local-public-testnet-letai-test-environment.sh --reuse-existing --manifest /Users/scc/ccwork/oasis7-testnet-nodes/fourth-local/manifest.json --output-dir /tmp/oasis7-local-public-testnet-letai-test-env`; retried once after one peer-head TTL cycle.
- Expected Result: Script should preflight submit endpoint and local observer, then start or reuse local services for the test environment.
- Actual Result: First run failed at submit endpoint preflight: `http://39.104.204.172:6631` reported `node_id=triad-testnet-sequencer`, `role=sequencer`, correct `world_id/chain_id/network_id`, height `421`, lag `0`, but `readiness=not_ready` with failed gates `consensus_peer_head_unavailable` and `consensus_peer_head_stale`. Second run after ~35s also failed at submit endpoint preflight: height `422`, lag `0`, `readiness=not_ready`, failed gate `storage_challenge_network_degraded`. Because this is the first preflight gate, the script did not start NewAPI bridge, LetAI provider bridge, viewer live, or static viewer during this attempt.
- Blocker / Next Action: Public submit endpoint is currently not ready. Re-run only after sequencer `/v1/chain/status` returns `readiness=ready` with empty failed gates; if `storage_challenge_network_degraded` persists, inspect remote storage challenge/replication health before testing OC -> NewAPI recharge.

## 2026-06-23 20:59:00 CST / tpm
- 完成内容: Rebased the health-check worktree onto latest `origin/main` and rechecked current `readiness=not_ready` root cause.
- 遗留事项: Remote sequencer/storage replication and storage-challenge logs are needed if we want to repair the testnet itself.
- Action: Committed task health evidence, ran `git fetch origin main && git rebase origin/main`, inspected latest main readiness gate implementation, and sampled sequencer/local observer/storage status endpoints.
- Specialist Slice: `blockchain_ops_engineer` bounded read-only root-cause analysis after rebase `019ef491-d86c-7d01-a481-a37546d12c29`.
- Validation Command: `git fetch origin main && git rebase origin/main`; `rg -n "consensus_peer_head_unavailable|consensus_peer_head_stale|storage_challenge_network_degraded|failed_gates|readiness" crates/oasis7 crates/oasis7_node -g '*.rs'`; `curl -m 5 -fsS http://39.104.204.172:6631/v1/chain/status | jq ...`; `curl -m 5 -fsS http://127.0.0.1:19083/v1/chain/status | jq ...`; `curl -m 5 -fsS http://39.104.205.67:6632/v1/chain/status | jq ...`; `curl -m 5 -fsS http://39.104.205.67:6632/v1/chain/explorer/overview | jq ...`.
- Expected Result: Determine whether `readiness=not_ready` is caused by local observer config, latest main readiness logic, peer-head freshness, or storage challenge/replication.
- Actual Result: Rebase succeeded onto `origin/main` `469277002`. Current sequencer is `not_ready` with failed gates `consensus_peer_head_unavailable`, `consensus_peer_head_stale`, and `storage_challenge_network_degraded`; committed/network height is `422` with `last_commit_age_ms` about 1,488,258ms and peer head age about 565s. Sequencer exposes the storage-challenge reason: `required_matches=2 successful_matches=0`; provider lookup failed with `libp2p command get_providers timed out after 30000ms`, followed by `/aw/node/replication/fetch-blob/1.0.0` request timeout to peer `12D3KooWNphsGixZxpqmZf9RSVhCWHH7hWFYZBN8izmWfXQYAXTQ`. Local observer is also `not_ready`, but only due to stale/unavailable peer heads. Storage node is `not_ready` due to stale peer head, shows a pending proposal at height `423` marked `committed` with full approved stake but about 583s old, and explorer overview is stuck at height `422`. Latest main code confirms these gates map directly to zero fresh peer heads, stale peer heads, and non-empty `storage_challenge_network_degraded_height`.
- Blocker / Next Action: Root cause is remote testnet replication/storage-challenge path failure on the sequencer, not local script or manifest. Inspect remote libp2p provider discovery/fetch-blob route, peer `12D3KooWNphsGixZxpqmZf9RSVhCWHH7hWFYZBN8izmWfXQYAXTQ`, and storage challenge sample availability before retrying full local public_testnet startup.

## 2026-06-23 21:34:00 CST / tpm
- 完成内容: Used the ECS access file to inspect live public_testnet sequencer/storage node truth and narrowed the current `readiness=not_ready` cause after the chain advanced beyond the earlier height `422` sample.
- 遗留事项: Need a controlled remote repair before claiming the local test environment is connected to a healthy public_testnet world state.
- Specialist Slice: `blockchain_ops_engineer` bounded read-only operational root-cause review `019ef4a3-ed8f-7f61-86fa-c3848c03d60f`; note that this slice used the earlier height `422` sample, while the TPM integration below supersedes it with fresher height `450` live samples.
- Action: Read `/Users/scc/Documents/keys/aliyun_ecs.txt` without recording credentials, SSHed into `39.104.204.172` and `39.104.205.67`, inspected running `oasis7_chain_runtime` command lines, listeners/connections, `/opt/oasis7/p2p-testnet/config/node.env`, formal bootstrap peer files, and fresh local/public `/v1/chain/status` payloads.
- Validation Command: `curl -m 5 -fsS http://39.104.204.172:6631/v1/chain/status | jq ...`; `curl -m 5 -fsS http://39.104.205.67:6632/v1/chain/status | jq ...`; `curl -m 5 -fsS http://127.0.0.1:19083/v1/chain/status | jq ...`; SSH `ps -eo pid,ppid,etimes,command | grep oasis7_chain_runtime`; SSH `ss -lntp` / `ss -ntp`; SSH `sed -n '130,185p' /opt/oasis7/p2p-testnet/bin/start-node.sh`; SSH read of `/opt/oasis7/p2p-testnet/config/node.env` and `public-testnet-governed-bootstrap-bootstrap-peers-2026-06-06.txt`.
- Expected Result: Determine whether the local test startup script is blocked by local readiness dependency, stale code after rebase, or live public_testnet node health/config drift.
- Actual Result: Both ECS runtimes are alive and listening, built from `0.0.0+testnet.121.04ca775181aa`. Live status now shows sequencer/storage/local observer at `committed_height=450`, not the earlier `422`; `storage_challenge_network_degraded` is currently cleared, but readiness remains `not_ready` on `consensus_peer_head_unavailable` and `consensus_peer_head_stale`. Storage has a pending proposal `height=451` with `status=committed`, `approved_stake=200`, `required_stake=134`, but rejects the remote committed height with `drop remote committed height 451 without matching persisted replication commit`. Replication endpoints report active peers and registered replication protocols on the two validators, but recent errors still include `fetch-blob` / `fetch-commit/head` `ConnectionClosed` and `Timeout`. Remote `node.env` sets `REPLICATION_NETWORK_BOOTSTRAP_PEERS_CSV` to bare `/ip4/.../tcp/...` values, while the formal bootstrap peer file still contains full `/p2p/<peer-id>` multiaddrs. The formal bootstrap peer ids in the copied artifact (`12D3KooWEW...` / `12D3KooWL6...`) also do not match current live `replication.local_peer_id` values (`12D3KooWMy...` / `12D3KooWAu...`), indicating live node-key/peer truth drift from the formal bootstrap artifact.
- Blocker / Next Action: Current root cause is remote public_testnet replication/head freshness drift, specifically a persisted replication commit/head path failing around height `451`, with deployment config drift between formal bootstrap peers, current node env, and live peer ids. Do not bypass readiness in the local startup script. Safest next operational repair is to refresh or regenerate remote bootstrap peer config from live `replication.local_peer_id`, ensure sequencer/storage `REPLICATION_NETWORK_BOOTSTRAP_PEERS_CSV` uses full `/p2p/<peer-id>` multiaddrs aligned to current node keys, restart the two validator runtimes cleanly via existing service scripts, then verify height advances, peer heads become fresh, and local observer/public endpoints return `readiness=ready`.

## 2026-06-23 22:05:00 CST / tpm
- 完成内容: Implemented the deployment contract correction that formal testnet deployment does not need `REPLICATION_NETWORK_BOOTSTRAP_PEERS_CSV`.
- 遗留事项: Existing live ECS env files may still contain stale CSV values, but `start-node.sh` now ignores them when `NETWORK_TIER_MANIFEST_PATH` is set unless an explicit override is enabled.
- Specialist Slice: `runtime_engineer` / `blockchain_ops_engineer` bounded review requested as `019ef4bb-0fc2-7543-80c7-efca61ab0e4a`.
- Action: Updated `scripts/p2p-triad-node-start.sh` so manifest-backed starts no longer pass `REPLICATION_NETWORK_BOOTSTRAP_PEERS_CSV` as `--replication-network-peer`; chain runtime will use the loaded network-tier manifest `bootstrap_peer_ref` when no CLI peers are provided. Updated `scripts/p2p-public-testnet-preflight.sh` so preflight no longer rewrites observer env files with refreshed CSV values and reports `replication_bootstrap_source=network_tier_manifest`. Added `scripts/p2p-triad-node-start.test.sh` and updated preflight test assertions.
- Validation Command: `./scripts/p2p-triad-node-start.test.sh`; `./scripts/p2p-public-testnet-preflight.test.sh`; `bash ./scripts/p2p-public-testnet-refresh-bootstrap-peers.test.sh`; `bash ./scripts/p2p-public-testnet-local-observer-sync.test.sh`; `git diff --check`.
- Expected Result: Formal testnet startup should rely on `NETWORK_TIER_MANIFEST_PATH` / manifest bootstrap peers and not require or propagate `REPLICATION_NETWORK_BOOTSTRAP_PEERS_CSV`, while legacy non-manifest startup can still use the env CSV.
- Actual Result: All listed checks passed. `p2p-triad-node-start.test.sh` verifies manifest-backed dry-run output includes `--network-tier-manifest` and excludes `--replication-network-peer`, while legacy mode and explicit `ALLOW_NETWORK_TIER_REPLICATION_PEER_ENV_OVERRIDE=1` still pass env peers. Preflight test verifies observer env remains unchanged and summary reports manifest bootstrap source.
- Review Result: `019ef4bb-0fc2-7543-80c7-efca61ab0e4a` returned `no_findings`; residual risk is that `p2p-public-testnet-local-observer-sync.sh` still reads/writes `REPLICATION_NETWORK_BOOTSTRAP_PEERS_CSV`, which is now旁路信息 for manifest-backed starts and should be cleaned up later to reduce operator confusion.
- Blocker / Next Action: No local blocker. Next remote operational step is to deploy the updated `start-node.sh` semantics or equivalent config cleanup to ECS, restart the validator services, and confirm public endpoints return `readiness=ready`.
